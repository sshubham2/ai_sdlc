# AI SDLC — Hybrid Pipeline

Spec-driven SDLC for AI implementers. Hybrid of phase-driven rigor (for knowable domains) and slice-driven iteration (for unknowable ones).

Each skill is a **drop-in Claude Code skill**. Copy any `skills/<name>/` directory into `~/.claude/skills/` and it works.

## Why this exists

Comprehensive-upfront-spec SDLC optimizes for **"never build the wrong thing."** That works for enterprise / compliance / large teams.

It falters when:
- The spec itself is the unknown (product discovery, consumer apps)
- Rework is cheap but late discovery is expensive
- Implementer is AI that benefits from plan-mode groundedness over 500-line sprint files

This pipeline keeps the durable parts of spec-driven work (vault, spikes, verification gates) and adds:

- **Risk-first slicing** — build the scary thing first
- **Two AI personas** — Builder produces, Critic adversarially reviews
- **Three modes** — Minimal / Standard / Heavy, picked by `/triage`
- **Mission brief over sprint file** — 1 page persistent + plan-mode execution
- **Continuous validation** — per slice, not terminal
- **Drift-check as pre-commit** — vault-code sync enforced automatically
- **Thin vault** — only what code can't carry (decisions, risks, slice memory). Components / contracts / schemas / test-plan / frontend live in code. Cure for spec rot at the root.
- **Project CLAUDE.md** — auto-generated by `/triage` to keep Claude on the pipeline across sessions (prevents the "ditch the pipeline and just start coding" failure)
- **Graphify throughout** — multi-modal knowledge graph (code + docs + papers + videos) auto-installed by `/triage`/`/adopt`; PreToolUse hook nudges Claude toward `GRAPH_REPORT.md` before Glob/Grep, and skills query the graph for reachability/blast-radius/path instead of re-scanning files. See [graphify-integration.md](graphify-integration.md).
- **milestone.md per slice** — rolling state file updated by every skill. Stage, next-action, progress, on-resume data all in one place. `/status` reads it as primary source. Survives session death, context clear, model confusion.
- **Risk-tier Critic gating** — `/slice` assigns tier (low/medium/high); `/critique` can skip low-tier slices that don't touch auth / contracts / data model / multi-device / external integrations. No more Critic reviewing CSS tweaks.
- **Honest Heavy mode** — `/heavy-architect` produces only human-authored files (threat model, requirements, NFRs). Code-derived views (components/contracts/schemas) generated on-demand by `/sync` before audits. Thin Vault principle preserved in all modes.

## Five principles

1. Iterate per slice, not per phase
2. Risk-first ordering
3. Two AI personas (Builder + Critic)
4. Reversibility-tagged decisions
5. Vault as commit metadata

See [principles.md](principles.md) for rationale.

## Pipeline at a glance

```
/triage                                      ← open the project, pick mode
  ↓
/discover (with /risk-spike, /user-test)     ← loop until slice 1 is clear
  ↓
[per slice:]
  /slice → /design-slice → /critique → /build-slice → /validate-slice → /reflect
  ↓ next slice
[maintenance: /drift-check, /reduce]
```

See [pipeline.md](pipeline.md) for the full spec.

## Setup (one-time per machine)

If you're using this pipeline, you already have Claude Code. Use it to install:

> **`cd` into the AI SDLC source directory (wherever you cloned/downloaded it), open Claude Code, and say:**
>
> "Install this. Read `INSTALL.md` and follow it."

Claude reads [INSTALL.md](INSTALL.md), detects what's on your machine, asks before mutating shared files (global CLAUDE.md, settings.json), executes the install, and verifies at the end. Idempotent — safe to re-run if you already ran it.

The install does:

1. Verifies / creates `~/.claude/.venv` (Python 3.11+)
2. Installs `graphify` (editable from `~/.claude/packages/graphify` if present, else PyPI)
3. Registers the graphify Claude Code skill at `~/.claude/skills/graphify/`
4. Adds the `PY=...` shared-Python convention to `~/.claude/CLAUDE.md` (with your confirmation)
5. Adds `CLAUDE_CODE_FORK_SUBAGENT=1` to `~/.claude/settings.json` (so `/fork` works)
6. Copies AI SDLC skills + agents into `~/.claude/skills/` and `~/.claude/agents/`

After setup: **restart Claude Code** so the env var loads. Then `/triage` and `/adopt` run a preflight check on first use; if anything is missing, they fail fast and point you back here.

## Files

```
ai_sdlc/
  README.md                 # You are here
  tutorial.md               # ⭐ START HERE — worked examples, scenarios, FAQ
  pipeline.md               # Full pipeline spec
  principles.md             # Five principles + rationale
  graphify-integration.md   # How every skill uses graphify (install, query, hooks, multi-modal)
  VERSION                   # Methodology semver (read by /status; copied to ~/.claude/ai-sdlc-VERSION on install)
  methodology-changelog.md  # Behavior-changing rules with rule reference + defect class + validation
  agents/                   # Named-subagent definitions (system prompts for fresh-context agents)
    AUTHORING.md              # ⭐ How to author or modify a named subagent — frontmatter shape, tool selection, prompt structure, model selection, calibration awareness
    critique.md               # Adversarial Critic — invoked by /critique skill
    critic-calibrate.md       # Meta-Critic — invoked by /critic-calibrate skill
    critique-review.md        # Meta-Critic — invoked by /critique-review skill (DR-1); dual review of first Critic's output
    diagnose-narrator.md      # Narrator — invoked by /diagnose Step 6.5; writes engaging executive summary
    field-recon.md            # Field Reconnaissance — invoked by /risk-spike Step 2.5; live web survey for platform changes, quotas, deprecations
  skills/                   # 22 drop-in Claude Code skills
    triage/SKILL.md           # Greenfield project opener
    adopt/SKILL.md            # Brownfield project opener (existing codebase) — invokes /diagnose + /slice-candidates
    diagnose/                 # Forensic, code-only HTML deliverable for legacy/AI-assisted codebases
      SKILL.md
      assemble.py             # Deterministic HTML assembly (load-bearing — embedded JSON + JS for annotation flow)
      passes/*.md             # 11 analysis pass templates (intent, architecture, dead code, duplicates, etc.)
      schema/finding.yaml     # Finding YAML schema
    slice-candidates/         # Reads annotated diagnosis.html → backlog.md (dependency-ordered slice candidates)
      SKILL.md
      build_backlog.py
    discover/SKILL.md
    risk-spike/SKILL.md       # renamed from 'spike' to avoid conflict
    user-test/SKILL.md
    slice/SKILL.md
    design-slice/SKILL.md
    critique/SKILL.md
    critique-review/SKILL.md  # DR-1 dual review (per-slice second opinion)
    build-slice/SKILL.md
    validate-slice/SKILL.md
    reflect/SKILL.md
    drift-check/SKILL.md
    reduce/SKILL.md
    archive/SKILL.md          # Move completed slices to slices/archive/; maintain _index.md
    critic-calibrate/SKILL.md # Periodic meta-skill: propose Critic prompt updates from "Missed by Critic" patterns
    status/SKILL.md           # Macro state summary — mode, active slice, risks, regression health, next action
    repro/SKILL.md            # Bug-fix discipline: establish failing test + add to shippability BEFORE fix slice
    commit-slice/SKILL.md     # Generate audit-grade commit message from slice's vault artifacts
    heavy-architect/SKILL.md  # Heavy mode only: upfront comprehensive vault
    sync/SKILL.md             # Heavy mode only: bidirectional vault-code reconciliation
  templates/                # Reference templates (also embedded in relevant SKILL.md files)
    mission-brief.md
    milestone.md
    critique-report.md
    reflection.md
  tests/                    # Methodology self-tests (pytest)
    README.md                 # Setup, run, how to add tests for new rules
    methodology/              # Structural grep tests pinning load-bearing prose
      test_critique_agent.py
      test_critic_calibrate_agent.py
      test_diagnose_narrator_agent.py
      test_field_recon_agent.py
      test_slice_skill.py
      test_build_slice_skill.py
      test_validate_slice_skill.py
      test_reflect_skill.py
      test_diagnose_skill.py
      test_methodology_changelog.py
      test_agent_frontmatter.py     # Parametrized over all named agents
      test_skill_model_dispatch.py  # Verifies COST-1 Haiku dispatch directives
      test_diagnose_pass_models.py  # Verifies COST-1.1 per-pass model assignments
      test_status_cadence_enforcement.py  # Verifies CAL-1 critic-calibrate cadence
      test_wiring_matrix_audit.py    # Tests WIRE-1 — wiring matrix format validation
      test_build_checks_audit.py     # Tests BC-1 — build-checks gate (project + global)
      test_triage_audit.py          # Tests TRI-1 — user-owned triage discipline
      test_risk_register_audit.py   # Tests RR-1 — risk register scoring + sorting
      test_test_first_audit.py      # Tests TF-1 — test-first slice variant audit
      test_validate_slice_layers.py # Tests VAL-1 — layered /validate-slice (creds + deps)
      test_walking_skeleton_audit.py # Tests WS-1 — walking-skeleton slice variant
      test_exploratory_charter_audit.py # Tests ETC-1 — charter-based exploratory testing
      test_critique_review_audit.py # Tests DR-1 — dual review structural audit
      test_cross_spec_parity_audit.py # Tests CSP-1 — Heavy-mode cross-spec parity
      test_mock_budget_lint.py      # Tests LINT-MOCK-1 — runs linter on fixtures
      fixtures/                     # Test fixtures (NOT collected by pytest)
        mock_budget_clean.py        # Python fixtures for LINT-MOCK-1
        mock_budget_too_many.py
        mock_budget_internal.py
        mock_budget_seam.py
        syntax_error.py
        mock_budget_clean.ts        # TypeScript fixtures for LINT-MOCK-2
        mock_budget_too_many.ts
        mock_budget_internal.ts
        mock_budget_seam.ts
        syntax_error.ts
        mock_budget_clean.go        # Go fixtures for LINT-MOCK-3 (mock-budget v1)
        mock_budget_too_many.go
        syntax_error.go
        wiring/                     # WIRE-1 fixtures (design.md fragments)
          clean_design.md
          missing_cells_design.md
          missing_rationale_design.md
          no_matrix_design.md
          empty_matrix_design.md
        build_checks/               # BC-1 fixtures (build-checks.md fragments)
          clean_project_checks.md
          one_always_applies.md
          glob_match.md
          keyword_only.md
          missing_severity.md
          invalid_severity.md
          multi_rules.md
          global_checks.md
        triage/                     # TRI-1 fixtures (critique.md fragments)
          clean_critique.md
          needs_fixes_critique.md
          blocked_critique.md
          missing_disposition_critique.md
          missing_rationale_critique.md
          verdict_mismatch_critique.md
          no_triage_section_critique.md
          invalid_disposition_critique.md
        risk_register/              # RR-1 fixtures (risk-register.md fragments)
          clean_register.md
          missing_likelihood.md
          invalid_status.md
          duplicate_id.md
          empty_register.md
          legacy_register.md
          all_retired.md
        test_first/                 # TF-1 fixtures (mission-brief.md fragments)
          clean_brief.md
          test_first_off.md
          missing_section_brief.md
          invalid_status_brief.md
          ac_without_row_brief.md
          pre_finish_pending_brief.md
        validate_layers/            # VAL-1 fixtures (changed-files samples)
          secrets_clean.txt
          secrets_aws.txt
          secrets_github.txt
          secrets_jwt.py
          secrets_private_key.txt
          secrets_anthropic.txt
          secrets_generic.py
          secrets_allowlisted.txt
          secrets_allowlist.txt
          imports_clean.py
          imports_hallucinated.py
          imports_aliased.py
          imports_relative.py
          pyproject_fixture.toml
          requirements_fixture.txt
        walking_skeleton/           # WS-1 fixtures (mission-brief.md fragments)
          clean_brief.md
          walking_skeleton_off.md
          missing_section_brief.md
          empty_table_brief.md
          missing_verification_brief.md
          invalid_status_brief.md
          pre_finish_pending_brief.md
        exploratory_charter/        # ETC-1 fixtures (mission-brief.md fragments)
          clean_brief.md
          exploratory_off.md
          missing_section_brief.md
          empty_table_brief.md
          missing_mission_brief.md
          completed_no_findings_brief.md
          invalid_status_brief.md
          pre_finish_pending_brief.md
        critique_review/            # DR-1 fixtures (critique-review.md fragments)
          clean_review.md
          missing_section_review.md
          invalid_verdict_review.md
          missing_verdict_review.md
        cross_spec_parity/          # CSP-1 fixtures (Heavy artifact fragments)
          clean_threat_model.md
          clean_requirements.md
          clean_nfrs.md
          broken_impl_threat.md
          missing_status_threat.md
          invalid_status_threat.md
          mitigated_no_impl_threat.md
  pytest.ini                # testpaths = tests + pythonpath = .
  tools/                    # Executable methodology tools (Python)
    mock_budget_lint.py       # LINT-MOCK-1/2/3 — AST + tree-sitter linter for TDD-2 mock budget (Python, TS/JS, Go)
    wiring_matrix_audit.py    # WIRE-1 — wiring matrix format validator for slice design.md
    build_checks_audit.py     # BC-1 — build-checks gate; surfaces evergreen rules at /build-slice
    triage_audit.py           # TRI-1 — user-owned triage section validator for slice critique.md
    risk_register_audit.py    # RR-1 — risk register scoring + sorting (Likelihood x Impact)
    test_first_audit.py       # TF-1 — test-first slice variant audit (mission-brief.md)
    validate_slice_layers.py  # VAL-1 — layered /validate-slice (credential scan + dep hallucination)
    walking_skeleton_audit.py # WS-1 — walking-skeleton slice variant audit (mission-brief.md)
    exploratory_charter_audit.py # ETC-1 — charter-based exploratory testing audit
    critique_review_audit.py  # DR-1 — dual-review structural validator (critique-review.md)
    cross_spec_parity_audit.py # CSP-1 — Heavy-mode cross-spec parity (TM/REQ/NFR refs)
```

## Getting started

If you're new: read **[tutorial.md](tutorial.md)** first. It has worked examples for each mode, decision tree, common scenarios, anti-patterns, and FAQ.

After installing the skills, in your project root:

```
/triage "what you're building in one sentence"
```

`/triage` picks a mode, creates `architecture/triage.md` + project CLAUDE.md, and tells you what to run next.

## Skill list

| Skill | Phase | Modes | Purpose |
|-------|-------|-------|---------|
| `/triage` | Open | all | **Greenfield**: pick mode, build risk register, scope pipeline, generate project CLAUDE.md |
| `/adopt` | Open | all | **Brownfield**: use instead of /triage for existing codebases. Reads code (not docs), runs `/diagnose` for forensic analysis, reverse-engineers initial vault, generates brownfield-aware CLAUDE.md with code-truth-over-docs rule |
| `/diagnose` | Brownfield analysis | all | **Forensic, code-only**, owner-facing HTML deliverable. 11 analysis passes + narrative executive summary (via `diagnose-narrator` agent). Owner annotates findings inline; output feeds `/slice-candidates`. Used by `/adopt` Step 3 |
| `/slice-candidates` | Brownfield analysis | all | Reads annotated `diagnosis.html`, produces dependency-ordered `backlog.md` of slice candidates. Used by `/adopt` after the owner annotates the diagnosis |
| `/discover` | Discovery | all | Concept + users + constraints |
| `/risk-spike` | Discovery | all | Validate risky assumptions early |
| `/heavy-architect` | Discovery | **Heavy only** | Upfront comprehensive vault — components, contracts, schemas, threat model, cost estimation |
| `/user-test` | Discovery | Standard, Heavy | Real user validation gate (B2C) |
| `/slice` | Slice | all | Define next thinnest valuable cut |
| `/design-slice` | Slice | all | Just-enough spec for current slice |
| `/critique` | Slice | Standard, Heavy | Adversarial review by Critic persona |
| `/build-slice` | Build | all | Plan mode + verification gates |
| `/validate-slice` | Build | all | Real device / user / data check |
| `/reflect` | Build | all | Capture learnings, update vault |
| `/drift-check` | Maintenance | all | Vault vs code drift detection (pre-commit ready) |
| `/sync` | Maintenance | **Heavy only** | Bidirectional vault-code reconciliation; regenerates code-derived files |
| `/reduce` | Maintenance | all | Complexity budget enforcer (mandatory every 5 slices in Heavy) |
| `/archive` | Maintenance | all | Maintain `slices/_index.md` + `slices/archive/`. Auto-triggered by `/reflect`; manual `/archive --index-only` rebuilds stale indexes |
| `/critic-calibrate` | Maintenance | all | Every 10-20 slices: analyze "Missed by Critic" patterns, propose targeted updates to `critique/SKILL.md`. Never auto-applies — human reviews |
| `/status` | Orientation | all | One-command macro state: mode, active slice, risks, regression health, Critic calibration status, next recommended action. Use at session start |
| `/repro` | Bug fixes | all | Establish failing test + add to shippability BEFORE fix slice. Codifies "tests-first for bug fixes" |
| `/commit-slice` | Finalize | all | Generate audit-grade commit message from mission-brief / build-log / validation / ADRs. No hand-crafting |

## The three modes

**Minimal** — solo dev / MVP / one-off scripts:
- Pipeline: `/triage → /discover → /slice → /design-slice → /build-slice → /validate-slice → /reflect → loop`
- Skips: `/critique`, `/user-test`, `/heavy-architect`, `/sync`
- Vault: thin (triage, concept, decisions, risks, slices)

**Standard** — B2C / small teams / product work (the default):
- Pipeline: `/triage → /discover → (/risk-spike if HIGH risks) → (/user-test if B2C) → /slice → /design-slice → /critique → /build-slice → /validate-slice → /reflect → loop`
- Maintenance: `/drift-check` (pre-commit), `/reduce` (when complexity grows)
- Vault: thin

**Heavy** — compliance / regulated / audit-grade:
- Pipeline: `/triage → /discover (full role-play) → /risk-spike all → /heavy-architect → /user-test → /slice → /design-slice (updates comprehensive vault inline) → /critique (mandatory + human sign-off) → /build-slice (compliance trail) → /validate-slice (reproducible, signed) → /reflect → loop`
- Maintenance: `/drift-check` (pre-commit), `/sync` (every 5-10 slices), `/reduce` (mandatory every 5 slices)
- Vault: comprehensive (components, contracts, schemas, threat model, cost estimation, requirements, NFRs, diagrams)

## In one sentence

**Triage first, slice small, build the scary thing early, let Critic challenge the Builder, make reality validate the vault, reflect every slice.**
