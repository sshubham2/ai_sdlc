# Shippability Catalog

Critical-path tests from every slice. `/validate-slice` runs these at pre-finish to catch regressions introduced by the current slice breaking past slices' critical paths.

Each entry is ONE test that, if it silently broke, would represent the slice having shipped a regression. Keep individual entries to <10 seconds; the full catalog should run in <2 minutes.

| # | Slice | Critical path | Command | Runtime | Machine-cmd |
|---|-------|--------------|---------|---------|---|
| 1 | slice-001-diagnose-orchestration-fix | /diagnose orchestration: fence parser + write_pass.py + normalize_finding + assemble.py error handling + SKILL.md prose pins | `<HOME>/.claude/.venv/Scripts/python.exe -m pytest tests/skills/diagnose/ --no-header -q` | <3s | `<interp> -m pytest tests/skills/diagnose/ --no-header -q` |
| 2 | slice-002-fix-diagnose-contract-and-cwd-mismatch | risk-register.md conforms to RR-1 schema (R-1 + R-2 audit-clean) + canonical contract byte-equal across SKILL.md + 11 pass templates | `<HOME>/.claude/.venv/Scripts/python.exe -m pytest tests/methodology/test_risk_register_audit_real_file.py tests/skills/diagnose/test_skill_md_pins.py::test_pass_templates_match_skill_md_step5_contract tests/skills/diagnose/test_skill_md_pins.py::test_no_legacy_no_bash_no_python_phrase --no-header -q` | <2s | `<interp> -m pytest tests/methodology/test_risk_register_audit_real_file.py tests/skills/diagnose/test_skill_md_pins.py::test_pass_templates_match_skill_md_step5_contract tests/skills/diagnose/test_skill_md_pins.py::test_no_legacy_no_bash_no_python_phrase --no-header -q` |
| 3 | slice-003-add-val-1-imports-allowlist | VAL-1 Layer B: `[tool.setuptools] packages` auto-read + `--imports-allowlist` flag (lenient API / strict CLI) + slice-002 archive replay returns 0 findings + SKILL.md Step 5b documents both new resolution paths | `<HOME>/.claude/.venv/Scripts/python.exe -m pytest tests/methodology/test_validate_slice_layers.py::test_slice_002_archive_replay_zero_findings_with_allowlist tests/methodology/test_validate_slice_layers.py::test_parse_declared_deps_reads_setuptools_packages tests/methodology/test_validate_slice_layers.py::test_validate_slice_skill_documents_imports_allowlist_and_setuptools_packages --no-header -q` | <1s | `<interp> -m pytest tests/methodology/test_validate_slice_layers.py::test_slice_002_archive_replay_zero_findings_with_allowlist tests/methodology/test_validate_slice_layers.py::test_parse_declared_deps_reads_setuptools_packages tests/methodology/test_validate_slice_layers.py::test_validate_slice_skill_documents_imports_allowlist_and_setuptools_packages --no-header -q` |
| 4 | slice-004-fix-rr1-audit-docstring-or-regex | RR-1: docstring + inline comment + risk-register.md L3 prose all show heading examples that match `_RISK_HEADING_RE` + R-1/R-2 scoring invariant preserved | `<HOME>/.claude/.venv/Scripts/python.exe -W error::SyntaxWarning -m pytest tests/methodology/test_risk_register_audit.py::test_docstring_format_examples_match_actual_regex tests/methodology/test_risk_register_audit.py::test_inline_regex_comment_examples_match_actual_regex tests/methodology/test_risk_register_audit.py::test_risk_register_md_schema_description_examples_match_actual_regex tests/methodology/test_risk_register_audit_real_file.py::test_slice_004_no_regression_in_existing_risk_register --no-header -q` | <1s | `<interp> -W error::SyntaxWarning -m pytest tests/methodology/test_risk_register_audit.py::test_docstring_format_examples_match_actual_regex tests/methodology/test_risk_register_audit.py::test_inline_regex_comment_examples_match_actual_regex tests/methodology/test_risk_register_audit.py::test_risk_register_md_schema_description_examples_match_actual_regex tests/methodology/test_risk_register_audit_real_file.py::test_slice_004_no_regression_in_existing_risk_register --no-header -q` |
| 5 | slice-005-add-bc-1-keyword-precision | BC-1 keyword precision: word-boundary regex + Trigger anchors silence slice-003+004 archive false positives, preserve legitimate LLM-fence matches, production rules carry the expected anchor tuples, and the new `anchor-not-in-keywords` parse-violation kind fires | `<HOME>/.claude/.venv/Scripts/python.exe -m pytest tests/methodology/test_build_checks_audit.py::test_slice_003_archive_backtest_no_bc_proj_2_or_global_1_applications tests/methodology/test_build_checks_audit.py::test_slice_004_archive_backtest_no_bc_proj_2_or_global_1_applications tests/methodology/test_build_checks_audit.py::test_legitimate_llm_fence_brief_still_triggers_bc_proj_2_and_global_1 tests/methodology/test_build_checks_audit.py::test_migrated_rules_have_expected_anchors tests/methodology/test_build_checks_audit.py::test_anchor_not_in_keywords_yields_violation --no-header -q` | <1s | `<interp> -m pytest tests/methodology/test_build_checks_audit.py::test_slice_003_archive_backtest_no_bc_proj_2_or_global_1_applications tests/methodology/test_build_checks_audit.py::test_slice_004_archive_backtest_no_bc_proj_2_or_global_1_applications tests/methodology/test_build_checks_audit.py::test_legitimate_llm_fence_brief_still_triggers_bc_proj_2_and_global_1 tests/methodology/test_build_checks_audit.py::test_migrated_rules_have_expected_anchors tests/methodology/test_build_checks_audit.py::test_anchor_not_in_keywords_yields_violation --no-header -q` |
| 6 | slice-006-update-critic-with-cross-cutting-conformance-dimension | Critic agent prompt walks 9 dimensions including Cross-cutting conformance with back-synced Dim 1+4 surgical sub-bullets, resolved Dim 9 cross-references, deliberate citation choice (Kiczales + honest-out), 9-item output-format checklist, and no in-repo drift to "8 dimensions" prose elsewhere (slice-011 superseded the Dim 9 structural-invariant test `_lists_five_sub_clauses` → `_lists_six_sub_clauses` at slice-011 row 11 per PMI-1 versioned-gate supersession discipline applied at the structural-invariant level; slice-013 second-superseded `_lists_six_sub_clauses` → `_lists_seven_sub_clauses` at slice-013 row 13; slice-015 third-superseded `_lists_seven_sub_clauses` → `_lists_eight_sub_clauses` at slice-015 row 15 — N=3 stable structural-invariant supersession events with same-Phase shippability.md propagation per SCPD-1 / slice-015 / methodology v0.30.0) | `<HOME>/.claude/.venv/Scripts/python.exe -m pytest tests/methodology/test_critique_agent.py::test_critique_lists_nine_dimensions tests/methodology/test_critique_agent.py::test_critique_dim_1_has_tooling_doc_vs_impl_parity_sub_bullet tests/methodology/test_critique_agent.py::test_critique_dim_4_has_methodology_audit_conformance_sub_bullet tests/methodology/test_critique_agent.py::test_critique_dim_9_lists_twelve_sub_clauses tests/methodology/test_critique_agent.py::test_critique_dim_9_cross_references_resolve tests/methodology/test_critique_agent.py::test_critique_dim_9_citation_is_deliberate tests/methodology/test_critique_agent.py::test_critique_output_format_lists_nine_dimensions tests/methodology/test_critique_agent.py::test_no_in_repo_drift_on_eight_dimensions_phrase --no-header -q` | <1s | `<interp> -m pytest tests/methodology/test_critique_agent.py::test_critique_lists_nine_dimensions tests/methodology/test_critique_agent.py::test_critique_dim_1_has_tooling_doc_vs_impl_parity_sub_bullet tests/methodology/test_critique_agent.py::test_critique_dim_4_has_methodology_audit_conformance_sub_bullet tests/methodology/test_critique_agent.py::test_critique_dim_9_lists_twelve_sub_clauses tests/methodology/test_critique_agent.py::test_critique_dim_9_cross_references_resolve tests/methodology/test_critique_agent.py::test_critique_dim_9_citation_is_deliberate tests/methodology/test_critique_agent.py::test_critique_output_format_lists_nine_dimensions tests/methodology/test_critique_agent.py::test_no_in_repo_drift_on_eight_dimensions_phrase --no-header -q` |
| 7 | slice-007-add-critique-agent-content-equality-audit | CAD-1: in-repo↔installed sha256 byte-equality on agents/critique.md; audit CLI exit codes 0/1/2 with --repo-root sanity-check refusal; /critic-calibrate skill prose names in-repo as canonical with forward-sync; methodology-changelog v0.22.0 / CAD-1 entry present in-repo + installed (slice-008 superseded the 0.22.0 PMI-1 version-gate test with the 0.23.0 gate at slice-008 row 8) | `<HOME>/.claude/.venv/Scripts/python.exe -m pytest tests/methodology/test_critique_agent_drift.py tests/methodology/test_methodology_changelog.py::test_v_0_22_0_cad_1_entry_present_in_repo tests/methodology/test_install_audit.py::test_canonical_tools_match_plugin_yaml --no-header -q` | <2s | `<interp> -m pytest tests/methodology/test_critique_agent_drift.py tests/methodology/test_methodology_changelog.py::test_v_0_22_0_cad_1_entry_present_in_repo tests/methodology/test_install_audit.py::test_canonical_tools_match_plugin_yaml --no-header -q` |
| 8 | slice-008-refine-bc-1-anchors-with-negative-context | BC-1 v1.2 negative-context anchors: slice-005..007 archives no longer fire BC-PROJ-1 + BC-GLOBAL-1 with build-time --changed-files; slice-001 archive still fires legitimate rules; `Negative anchors` field-name + `final filter` semantics pinned in BOTH project + global build-checks files; always-true + negative-anchor interaction suppresses; `negative-anchor-overlaps-positive` parse violation fires; migrated rules carry the canonical 9-token tuple; methodology-changelog v0.23.0 / BC-1 v1.2 entry present in-repo + installed (slice-009 superseded the 0.23.0 PMI-1 version-gate test with the 0.24.0 gate at slice-009 row 9) | `<HOME>/.claude/.venv/Scripts/python.exe -m pytest tests/methodology/test_build_checks_audit.py::test_slice_005_archive_no_longer_fires_proj1_or_global1 tests/methodology/test_build_checks_audit.py::test_slice_006_archive_no_longer_fires_proj1_or_global1 tests/methodology/test_build_checks_audit.py::test_slice_007_archive_no_longer_fires_proj1_or_global1 tests/methodology/test_build_checks_audit.py::test_slice_001_archive_still_fires_legitimate_rules tests/methodology/test_build_checks_audit.py::test_negative_anchors_schema_documents_field_name_in_both_files tests/methodology/test_build_checks_audit.py::test_negative_anchors_schema_documents_final_filter_semantics tests/methodology/test_build_checks_audit.py::test_always_true_rule_with_negative_anchor_match_is_skipped tests/methodology/test_build_checks_audit.py::test_negative_anchor_overlaps_positive_yields_violation tests/methodology/test_build_checks_audit.py::test_migrated_rules_have_expected_negative_anchors tests/methodology/test_methodology_changelog.py::test_v_0_23_0_bc_1_v_1_2_entry_present_in_repo --no-header -q` | <2s | `<interp> -m pytest tests/methodology/test_build_checks_audit.py::test_slice_005_archive_no_longer_fires_proj1_or_global1 tests/methodology/test_build_checks_audit.py::test_slice_006_archive_no_longer_fires_proj1_or_global1 tests/methodology/test_build_checks_audit.py::test_slice_007_archive_no_longer_fires_proj1_or_global1 tests/methodology/test_build_checks_audit.py::test_slice_001_archive_still_fires_legitimate_rules tests/methodology/test_build_checks_audit.py::test_negative_anchors_schema_documents_field_name_in_both_files tests/methodology/test_build_checks_audit.py::test_negative_anchors_schema_documents_final_filter_semantics tests/methodology/test_build_checks_audit.py::test_always_true_rule_with_negative_anchor_match_is_skipped tests/methodology/test_build_checks_audit.py::test_negative_anchor_overlaps_positive_yields_violation tests/methodology/test_build_checks_audit.py::test_migrated_rules_have_expected_negative_anchors tests/methodology/test_methodology_changelog.py::test_v_0_23_0_bc_1_v_1_2_entry_present_in_repo --no-header -q` |
| 9 | slice-009-refine-dim-9-with-design-md-tables-sub-clause | CCC-1 v1.1: agents/critique.md Dim 9 sub-clause 2 body extended with design-doc-level surface (design.md mechanical tables vs methodology canonical inventories); 3 new prose-pin tests (substring pin + location pin per Critic M1 + example anchors); CAD-1 byte-equality on agents/critique.md preserved post-forward-sync; methodology-changelog v0.24.0 / CCC-1 v1.1 entry present in-repo + installed with substantive canonical phrase `design.md mechanical tables` pinned bidirectionally (per Critic M3 N-surface schema-pin); plugin.yaml.version == VERSION == 0.24.0 at slice-009 (slice-010 superseded the 0.24.0 PMI-1 version-gate with the 0.25.0 gate at slice-010 row 10) | `<HOME>/.claude/.venv/Scripts/python.exe -m pytest tests/methodology/test_critique_agent.py::test_critique_dim_9_tooling_doc_vs_impl_parity_covers_design_md_tables tests/methodology/test_critique_agent.py::test_critique_dim_9_sub_clause_2_body_contains_design_md_table_paragraph tests/methodology/test_critique_agent.py::test_critique_dim_9_design_md_tables_paragraph_cites_slice_006_and_007 tests/methodology/test_critique_agent_drift.py::test_in_repo_and_installed_critique_agent_are_content_equal tests/methodology/test_methodology_changelog.py::test_v_0_24_0_ccc_1_v_1_1_entry_present_in_repo --no-header -q` | <2s | `<interp> -m pytest tests/methodology/test_critique_agent.py::test_critique_dim_9_tooling_doc_vs_impl_parity_covers_design_md_tables tests/methodology/test_critique_agent.py::test_critique_dim_9_sub_clause_2_body_contains_design_md_table_paragraph tests/methodology/test_critique_agent.py::test_critique_dim_9_design_md_tables_paragraph_cites_slice_006_and_007 tests/methodology/test_critique_agent_drift.py::test_in_repo_and_installed_critique_agent_are_content_equal tests/methodology/test_methodology_changelog.py::test_v_0_24_0_ccc_1_v_1_1_entry_present_in_repo --no-header -q` |
| 10 | slice-010-promote-voluntary-critic-on-cross-cutting-to-slice-default-heuristic | MCT-1: skills/slice/SKILL.md Step 4a "Always mandatory Critic" list gains terse bullet `In-house methodology surfaces (skills/*/SKILL.md, agents/*.md, tools/**/*.py, methodology-changelog.md)` between `Security-sensitive paths` and `Heavy mode (always)` + adjacent evidence prose markdown blockquote appended after the existing "When producing the mission brief..." paragraph (per slice-010 Critic M1 split); 5 prose-pin tests (substring + location-pin per slice-009 M1 with widened section bounds `Always mandatory Critic` → `### Step 5:` end anchor + evidence-paragraph N=9/9 + voluntary Critic case-sensitive pin + ≥2 of 4 cross-slice anchors + ≥2 of 4 sub-class anchors per slice-010 Critic M3 ACCEPTED-PENDING); mini-CAD-1 byte-equality on skills/slice/SKILL.md (scope-narrow per-file shape mirroring slice-007 CAD-1; does NOT generalize INST-2); methodology-changelog v0.25.0 / MCT-1 entry present in-repo + installed with substantive canonical phrase `In-house methodology surfaces` pinned bidirectionally + Limitations note per Critic B5 acknowledging /slice-time prose-heuristic semantics (no audit-enforced gate); plugin.yaml.version == VERSION == 0.25.0 at slice-010 ship (slice-011 superseded the 0.25.0 PMI-1 version-gate test with the 0.26.0 gate at slice-011 row 11) | `<HOME>/.claude/.venv/Scripts/python.exe -m pytest tests/methodology/test_slice_skill.py::test_slice_step4a_mandatory_critic_section_contains_in_house_methodology_surfaces_bullet tests/methodology/test_slice_skill.py::test_slice_step4a_in_house_methodology_bullet_location_between_security_paths_and_heavy_mode tests/methodology/test_slice_skill.py::test_slice_step4a_evidence_paragraph_cites_n_9_and_voluntary_critic tests/methodology/test_slice_skill.py::test_slice_step4a_evidence_paragraph_cites_at_least_two_cross_cutting_tooling_slices tests/methodology/test_slice_skill.py::test_slice_step4a_evidence_paragraph_cites_at_least_two_sub_class_anchors tests/methodology/test_slice_skill_drift.py::test_in_repo_and_installed_slice_skill_md_are_content_equal tests/methodology/test_methodology_changelog.py::test_v_0_25_0_mct_1_entry_present_in_repo --no-header -q` | <2s | `<interp> -m pytest tests/methodology/test_slice_skill.py::test_slice_step4a_mandatory_critic_section_contains_in_house_methodology_surfaces_bullet tests/methodology/test_slice_skill.py::test_slice_step4a_in_house_methodology_bullet_location_between_security_paths_and_heavy_mode tests/methodology/test_slice_skill.py::test_slice_step4a_evidence_paragraph_cites_n_9_and_voluntary_critic tests/methodology/test_slice_skill.py::test_slice_step4a_evidence_paragraph_cites_at_least_two_cross_cutting_tooling_slices tests/methodology/test_slice_skill.py::test_slice_step4a_evidence_paragraph_cites_at_least_two_sub_class_anchors tests/methodology/test_slice_skill_drift.py::test_in_repo_and_installed_slice_skill_md_are_content_equal tests/methodology/test_methodology_changelog.py::test_v_0_25_0_mct_1_entry_present_in_repo --no-header -q` |
| 11 | slice-011-promote-recursive-self-application-discipline-to-critique-skill-prose | RSAD-1: agents/critique.md Dim 9 gains new 6th sub-clause `Recursive self-application discipline` between existing `Language-version conformance` sub-clause close and existing `### Bonus: weak graph edges` H3 anchor; sub-clause body names BOTH design-time AND build-time-via-/critique-fix-prose sub-modes with N=3 cumulative evidence (slice-009 M2 + slice-010 design-time stress-test B1+M1+B5 + slice-010 build-time DEVIATION-3 BC-PROJ-2 fire); 5 prose-pin tests (structural-invariant supersession 5→6 sub-clauses + canonical-literal pin + location-pin per slice-009 M1 with scoped `.find()` per slice-009 DEVIATION-2 + slice-010 anchor-uniqueness pre-emption + sub-mode pin per slice-011 Critic B1 fix mitigating slice-009 DEVIATION-1 + strict-both cross-slice anchor pin per slice-011 Critic m2); reused CAD-1 byte-equality on agents/critique.md (slice-007 introduction) — mini-CAD-1 row 3 PASSING → WRITTEN-FAILING → PASSING transition pattern N=4 stable; methodology-changelog v0.26.0 / RSAD-1 entry present in-repo + installed with substantive canonical phrase `Recursive self-application discipline` pinned bidirectionally + Limitations note per slice-010 B5 calibration-trail convention extension (-D suffix signals /critique-time adversarial-prompt discipline heuristic; no audit-enforced gate); plugin.yaml.version == VERSION == 0.26.0 at slice-011 ship (slice-012 superseded the 0.26.0 PMI-1 version-gate test with the 0.27.0 gate at slice-012 row 12; slice-013 second-superseded the structural-invariant test `_lists_six_sub_clauses` → `_lists_seven_sub_clauses` at slice-013 row 13 per PMI-1 versioned-gate supersession discipline applied at the structural-invariant level — no two structural-invariant tests coexist; `_lists_seven_sub_clauses` subsumes slice-011's 6-sub-clause assertion by asserting all 7 titles including the 6th `Recursive self-application discipline`; slice-015 third-superseded `_lists_seven_sub_clauses` → `_lists_eight_sub_clauses` at slice-015 row 15 per N=3 stable structural-invariant supersession; `_lists_eight_sub_clauses` subsumes slice-013's 7-sub-clause assertion by asserting all 8 titles including the 6th `Recursive self-application discipline`) | `<HOME>/.claude/.venv/Scripts/python.exe -m pytest tests/methodology/test_critique_agent.py::test_critique_dim_9_lists_twelve_sub_clauses tests/methodology/test_critique_agent.py::test_critique_dim_9_recursive_self_application_sub_clause_present tests/methodology/test_critique_agent.py::test_critique_dim_9_recursive_self_application_location_pinned tests/methodology/test_critique_agent.py::test_critique_dim_9_recursive_self_application_names_both_sub_modes tests/methodology/test_critique_agent.py::test_critique_dim_9_recursive_self_application_cites_at_least_two_cross_slice_anchors tests/methodology/test_critique_agent_drift.py::test_in_repo_and_installed_critique_agent_are_content_equal tests/methodology/test_methodology_changelog.py::test_v_0_26_0_rsad_1_entry_present_in_repo --no-header -q` | <2s | `<interp> -m pytest tests/methodology/test_critique_agent.py::test_critique_dim_9_lists_twelve_sub_clauses tests/methodology/test_critique_agent.py::test_critique_dim_9_recursive_self_application_sub_clause_present tests/methodology/test_critique_agent.py::test_critique_dim_9_recursive_self_application_location_pinned tests/methodology/test_critique_agent.py::test_critique_dim_9_recursive_self_application_names_both_sub_modes tests/methodology/test_critique_agent.py::test_critique_dim_9_recursive_self_application_cites_at_least_two_cross_slice_anchors tests/methodology/test_critique_agent_drift.py::test_in_repo_and_installed_critique_agent_are_content_equal tests/methodology/test_methodology_changelog.py::test_v_0_26_0_rsad_1_entry_present_in_repo --no-header -q` |
| 12 | slice-012-bc-proj-2-negative-anchor-migration | BC-1 v1.3: `architecture/build-checks.md` BC-PROJ-2 rule body gains `Negative anchors:` line with same 9-token methodology-vocabulary set as BC-PROJ-1 + BC-GLOBAL-1 (slice-008 migration); data-only — no schema change, no audit-code change. 4 audit tests (slice-005 + slice-011 archive backtests confirm BC-PROJ-2 silenced via negative-anchor final filter + slice-001 backward-compat covenant preserved via mini-CAD-1 row 3 PASSING → WRITTEN-FAILING → PASSING transition N=4 → N=5 stable + per-rule-scoped `_parse_rules` + `by_id['BC-PROJ-2'].negative_anchors == expected_tuple` test per /critique B1 ACCEPTED-FIXED recursive-self-application catch — naive substring check would PASS pre-migration because slice-008 already wrote all 9 tokens on BC-PROJ-1's L20 line); methodology-changelog v0.27.0 / BC-PROJ-2 entry present in-repo + installed with substantive canonical phrase `BC-PROJ-2 negative-anchor migration` pinned bidirectionally + N-surface schema-pin discipline N=3 → N=4 ratchet per /critique m1 ACCEPTED-FIXED; PMI-1 versioned-gate supersession N=5 events post-slice-012 with Phase 1b INSERT discipline + Phase 1c narrow-scope Edit discipline per /critique M1 ACCEPTED-FIXED + slice-011 NEW Dim 9 sub-class N=1 entry-pin-vs-PMI-1-gate-conflation ratcheted to N=2 (v0.22.0..v0.26.0 entry-pin functions persist untouched through slice-012's PMI-1 gate supersession); plugin.yaml.version == VERSION == 0.27.0 at slice-012 ship (slice-013 superseded the 0.27.0 PMI-1 version-gate test with the 0.28.0 gate at slice-013 row 13) | `<HOME>/.claude/.venv/Scripts/python.exe -m pytest tests/methodology/test_build_checks_audit.py::test_slice_005_archive_no_longer_fires_proj2 tests/methodology/test_build_checks_audit.py::test_slice_011_archive_no_longer_fires_proj2 tests/methodology/test_build_checks_audit.py::test_slice_001_archive_still_fires_proj2 tests/methodology/test_build_checks_audit.py::test_bc_proj_2_has_methodology_vocabulary_negative_anchors tests/methodology/test_methodology_changelog.py::test_v_0_27_0_bc_proj_2_entry_present_in_repo --no-header -q` | <2s | `<interp> -m pytest tests/methodology/test_build_checks_audit.py::test_slice_005_archive_no_longer_fires_proj2 tests/methodology/test_build_checks_audit.py::test_slice_011_archive_no_longer_fires_proj2 tests/methodology/test_build_checks_audit.py::test_slice_001_archive_still_fires_proj2 tests/methodology/test_build_checks_audit.py::test_bc_proj_2_has_methodology_vocabulary_negative_anchors tests/methodology/test_methodology_changelog.py::test_v_0_27_0_bc_proj_2_entry_present_in_repo --no-header -q` |
| 13 | slice-013-refine-dim-9-with-entry-pin-vs-pmi-1-gate-sub-class | EPGD-1: agents/critique.md Dim 9 gains new 7th sub-clause `Entry-pin-vs-PMI-1-gate semantics conflation` between existing 6th sub-clause `Recursive self-application discipline` close (L172) and existing `### Bonus: weak graph edges` H3 anchor (now L180); body names BOTH `Build-time slip mode` + `Design-time-pre-empted success mode` sub-modes with N=2 cumulative evidence (slice-011 N=1 build-time slip + slice-012 N=2 design-time-pre-empted success); 5 prose-pin tests (structural-invariant supersession 6→7 sub-clauses + canonical-literal pin + location-pin per slice-009 M1 with scoped `.find()` per slice-009 DEVIATION-2 + slice-010 + slice-011 anchor-uniqueness pre-emption + sub-mode pin per slice-011 Critic B1 fix mitigating slice-009 DEVIATION-1 + strict-both cross-slice anchor pin + ≥2-of-4 substantive-discipline anchor pin per slice-013 Critic M2 ACCEPTED-FIXED); slice-011's `_names_both_sub_modes` + `_cites_at_least_two_cross_slice_anchors` end_anchors tightened symmetrically per slice-013 Critic M1 + M-add-1 ACCEPTED-PENDING (M-add-1 dual-review catch via DR-1; first instance of dual-review-catching-pattern-blindness in the project); reused CAD-1 byte-equality on agents/critique.md (slice-007 introduction) — mini-CAD-1 row 3 PASSING → WRITTEN-FAILING → PASSING transition pattern N=5 → N=6 stable; methodology-changelog v0.28.0 / EPGD-1 entry present in-repo + installed with substantive canonical phrase `Entry-pin-vs-PMI-1-gate semantics conflation` pinned across N=3 surfaces (agents/critique.md 7th sub-clause title + in-repo entry + installed entry) per slice-011 RSAD-1 3-surface precedent N=2 instances stable + Limitations note per slice-011 B5 calibration-trail convention extension (-D suffix signals /critique-time + /build-slice-time discipline heuristic; no audit-enforced gate); plugin.yaml.version == VERSION == 0.28.0 at slice-013 ship; PMI-1 versioned-gate supersession N=6 events post-slice-013 with Phase 1b INSERT discipline + Phase 1c narrow-scope Edit discipline per EPGD-1 self-application (v0.22.0..v0.27.0 entry-pin functions persist untouched through slice-013's PMI-1 gate supersession — slice-013 IS canonical reference instance of the discipline it authors; recursive-self-application N=5 cumulative post-RSAD-1 codification) | `<HOME>/.claude/.venv/Scripts/python.exe -m pytest tests/methodology/test_critique_agent.py::test_critique_dim_9_lists_twelve_sub_clauses tests/methodology/test_critique_agent.py::test_critique_dim_9_entry_pin_vs_pmi_1_gate_sub_clause_present tests/methodology/test_critique_agent.py::test_critique_dim_9_entry_pin_vs_pmi_1_gate_location_pinned tests/methodology/test_critique_agent.py::test_critique_dim_9_entry_pin_vs_pmi_1_gate_names_both_sub_modes tests/methodology/test_critique_agent.py::test_critique_dim_9_entry_pin_pmi_1_paragraph_cites_slice_011_and_012 tests/methodology/test_critique_agent.py::test_critique_dim_9_entry_pin_vs_pmi_1_gate_cites_at_least_two_cross_slice_anchors tests/methodology/test_critique_agent_drift.py::test_in_repo_and_installed_critique_agent_are_content_equal tests/methodology/test_methodology_changelog.py::test_v_0_28_0_epgd_1_entry_present_in_repo tests/methodology/test_methodology_changelog.py::test_plugin_yaml_version_matches_version_file_invariant --no-header -q` | <2s | `<interp> -m pytest tests/methodology/test_critique_agent.py::test_critique_dim_9_lists_twelve_sub_clauses tests/methodology/test_critique_agent.py::test_critique_dim_9_entry_pin_vs_pmi_1_gate_sub_clause_present tests/methodology/test_critique_agent.py::test_critique_dim_9_entry_pin_vs_pmi_1_gate_location_pinned tests/methodology/test_critique_agent.py::test_critique_dim_9_entry_pin_vs_pmi_1_gate_names_both_sub_modes tests/methodology/test_critique_agent.py::test_critique_dim_9_entry_pin_pmi_1_paragraph_cites_slice_011_and_012 tests/methodology/test_critique_agent.py::test_critique_dim_9_entry_pin_vs_pmi_1_gate_cites_at_least_two_cross_slice_anchors tests/methodology/test_critique_agent_drift.py::test_in_repo_and_installed_critique_agent_are_content_equal tests/methodology/test_methodology_changelog.py::test_v_0_28_0_epgd_1_entry_present_in_repo tests/methodology/test_methodology_changelog.py::test_plugin_yaml_version_matches_version_file_invariant --no-header -q` |
| 14 | slice-014-refactor-pmi-1-gate-to-version-agnostic-shape | PMI-1 v1.1: version-agnostic cleanliness gate `test_plugin_yaml_version_matches_version_file_invariant` replaces the per-version `_at_0_NN_0` shape (N=6 supersession events stable at slice-013 → terminated at slice-014); 2 AST meta-tests pin the version-agnostic shape against future regression (`_is_version_agnostic_shape` checks no `r"^\d+\.\d+\.\d+$"` `Constant(value=str)` inside the gate body; `_no_per_version_pmi_1_gate_functions_remain` checks no `FunctionDef.name` matching `r"^test_plugin_yaml_version_matches_version_file_at_0_\d+_0$"`); regression test `_fails_with_pinned_message_when_version_files_diverge` with tempdir + `monkeypatch.setattr(sys.modules[__name__], "REPO_ROOT", tmp_path)` (object form anchored on running module per slice-014 DEVIATION-1 — pytest's namespace-package import-mode places module under `methodology.test_methodology_changelog` not `tests.methodology.test_methodology_changelog`; dotted-string-form monkeypatch patches separate importlib copy and silently no-ops) + `pytest.raises(AssertionError)` + per-substring asserts on `"PMI-1"` + `"slice-006 escape"`; methodology-changelog v0.29.0 / PMI-1 v1.1 entry present in-repo + installed with substantive canonical phrase `version-agnostic PMI-1 cleanliness gate` pinned across N=3 surfaces (ADR-013 + in-repo entry + installed entry) per slice-013 EPGD-1 3-surface precedent N=2 → N=3 instances stable + supersession-retired marker phrase `supersession pattern retired at slice-014` pinned bidirectionally; ADR-013 exists at `architecture/decisions/ADR-013-*.md` with canonical phrase per slice-014 AC #4; plugin.yaml.version == VERSION == 0.29.0 at slice-014 ship — atomic bump under PMI-1 v1.1 version-agnostic-gate shape (first such bump); EPGD-1 self-application N=6 cumulative post-RSAD-1 codification (slice-014 IS canonical reference instance of EPGD-1 at the discipline-retirement level — the (a) ↔ (b) duality: slice authoring the supersession-pattern-retirement IS the canonical last application of the pattern; v0.22.0..v0.28.0 entry-pin functions persist untouched through slice-014's PMI-1 gate Edit empirically verified at Phase 4) | `<HOME>/.claude/.venv/Scripts/python.exe -m pytest tests/methodology/test_methodology_changelog.py::test_plugin_yaml_version_matches_version_file_invariant tests/methodology/test_methodology_changelog.py::test_pmi_1_gate_function_is_version_agnostic_shape tests/methodology/test_methodology_changelog.py::test_pmi_1_gate_fails_with_pinned_message_when_version_files_diverge tests/methodology/test_methodology_changelog.py::test_no_per_version_pmi_1_gate_functions_remain tests/methodology/test_methodology_changelog.py::test_v_0_29_0_pmi_1_v1_1_entry_present_in_repo tests/methodology/test_methodology_changelog.py::test_v_0_29_0_entry_names_supersession_pattern_retired tests/methodology/test_methodology_changelog.py::test_adr_013_exists_and_names_pmi_1_refactor_canonical_phrase --no-header -q` | <2s | `<interp> -m pytest tests/methodology/test_methodology_changelog.py::test_plugin_yaml_version_matches_version_file_invariant tests/methodology/test_methodology_changelog.py::test_pmi_1_gate_function_is_version_agnostic_shape tests/methodology/test_methodology_changelog.py::test_pmi_1_gate_fails_with_pinned_message_when_version_files_diverge tests/methodology/test_methodology_changelog.py::test_no_per_version_pmi_1_gate_functions_remain tests/methodology/test_methodology_changelog.py::test_v_0_29_0_pmi_1_v1_1_entry_present_in_repo tests/methodology/test_methodology_changelog.py::test_v_0_29_0_entry_names_supersession_pattern_retired tests/methodology/test_methodology_changelog.py::test_adr_013_exists_and_names_pmi_1_refactor_canonical_phrase --no-header -q` |
| 15 | slice-015-refine-dim-9-with-shippability-catalog-propagation-sub-class | SCPD-1: agents/critique.md Dim 9 gains new 8th sub-clause `Shippability-catalog consumer-reference propagation` between existing 7th sub-clause `Entry-pin-vs-PMI-1-gate semantics conflation` close and existing `### Bonus: weak graph edges` H3 anchor; body names BOTH `Reactive-catch mode` (slice-013 N=1 reactive-catch at /validate-slice Step 5.5) + `Proactive-application mode` (slice-014 N=2 proactive-application at /build-slice Phase 4 pre-/validate-slice) sub-modes with N=2 cumulative cross-slice evidence; 5 prose-pin tests (canonical-literal pin + location-pin per slice-009 M1 + sub-mode pin per slice-011 Critic B1 + strict-both cross-slice anchor pin + ≥2-of-4 substantive-discipline anchor pin per slice-013 M2 mitigation precedent N=1 → N=2 stable at slice-015 — substantive anchor tuple locked at /critique B1 ACCEPTED-FIXED per literal-substring verification against canonical body: `["Phase 5", "shippability.md", "/validate-slice Step 5.5", "supersession"]` — original tuple `["Phase 5", "shippability catalog", "consumer reference", "rename propagation"]` Blocker-rejected because 3 of 4 use hyphenated or never-present forms); slice-013's 3 body-bound tests (`_names_both_sub_modes` + `_paragraph_cites_slice_011_and_012` + `_cites_at_least_two_cross_slice_anchors`) end_anchors tightened from `### Bonus: weak graph edges` → `Shippability-catalog consumer-reference propagation` per slice-015 /critique M1 ACCEPTED-PENDING (mirror of slice-013 M1 + M-add-1 tightening pattern N=1 → N=2 stable at slice-015; generic methodology recurrence — every future Dim 9 sub-clause append tightens predecessor body-bound end_anchors); reused CAD-1 byte-equality on agents/critique.md (slice-007 introduction) — mini-CAD-1 row 3 PASSING → WRITTEN-FAILING → PASSING transition pattern N=6 → N=7 stable; methodology-changelog v0.30.0 / SCPD-1 entry present in-repo + installed with substantive canonical phrase `Shippability-catalog consumer-reference propagation` pinned across N=3 surfaces (agents/critique.md 8th sub-clause title + in-repo v0.30.0 entry + installed v0.30.0 entry) per slice-013 EPGD-1 + slice-014 PMI-1 v1.1 3-surface precedent N=3 → N=4 instances stable + Limitations note per slice-011 B5 / slice-013 EPGD-1 calibration-trail convention extension (-D suffix N=3 stable post-RSAD-1 + EPGD-1 + SCPD-1 signals /critique-time + /build-slice-time discipline heuristic; no audit-enforced gate); ADR-014 exists at `architecture/decisions/ADR-014-*.md` with canonical phrase per slice-015 AC #4; plugin.yaml.version == VERSION == 0.30.0 at slice-015 ship — second atomic bump under PMI-1 v1.1 version-agnostic-gate shape (slice-014 was first; empirical retirement-proof N=1 → N=2 stable); SCPD-1 self-application N=1 standalone post-codification (slice-015 IS canonical reference instance of the discipline it authors — Phase 1f superseded `_lists_seven_sub_clauses` → `_lists_eight_sub_clauses` per PMI-1 structural-invariant supersession; Phase 5 propagated rows 6 + 11 + 13 pytest commands in-line BEFORE /validate-slice catalog run per SCPD-1 proactive-application sub-mode; recursive-self-application N=6 → N=7 cumulative post-RSAD-1 codification); /critique-review DR-1 dual-review M-add-1 catch (`WRITTEN-AS-EDIT` status missing from `tools/test_first_audit.py`'s `_ALLOWED_STATUSES` allowlist) — runtime-prerequisite-completeness blind-spot pattern N=2 → N=3 stable post-slice-013/014 codification | `<HOME>/.claude/.venv/Scripts/python.exe -m pytest tests/methodology/test_critique_agent.py::test_critique_dim_9_lists_twelve_sub_clauses tests/methodology/test_critique_agent.py::test_critique_dim_9_shippability_catalog_propagation_sub_clause_present tests/methodology/test_critique_agent.py::test_critique_dim_9_shippability_catalog_propagation_location_pinned tests/methodology/test_critique_agent.py::test_critique_dim_9_shippability_catalog_propagation_names_both_sub_modes tests/methodology/test_critique_agent.py::test_critique_dim_9_shippability_catalog_propagation_paragraph_cites_slice_013_and_014 tests/methodology/test_critique_agent.py::test_critique_dim_9_shippability_catalog_propagation_cites_at_least_two_cross_slice_anchors tests/methodology/test_critique_agent_drift.py::test_in_repo_and_installed_critique_agent_are_content_equal tests/methodology/test_methodology_changelog.py::test_v_0_30_0_scpd_1_entry_present_in_repo tests/methodology/test_methodology_changelog.py::test_adr_014_exists_and_names_scpd_1_canonical_phrase tests/methodology/test_methodology_changelog.py::test_plugin_yaml_version_matches_version_file_invariant --no-header -q` | <2s | `<interp> -m pytest tests/methodology/test_critique_agent.py::test_critique_dim_9_lists_twelve_sub_clauses tests/methodology/test_critique_agent.py::test_critique_dim_9_shippability_catalog_propagation_sub_clause_present tests/methodology/test_critique_agent.py::test_critique_dim_9_shippability_catalog_propagation_location_pinned tests/methodology/test_critique_agent.py::test_critique_dim_9_shippability_catalog_propagation_names_both_sub_modes tests/methodology/test_critique_agent.py::test_critique_dim_9_shippability_catalog_propagation_paragraph_cites_slice_013_and_014 tests/methodology/test_critique_agent.py::test_critique_dim_9_shippability_catalog_propagation_cites_at_least_two_cross_slice_anchors tests/methodology/test_critique_agent_drift.py::test_in_repo_and_installed_critique_agent_are_content_equal tests/methodology/test_methodology_changelog.py::test_v_0_30_0_scpd_1_entry_present_in_repo tests/methodology/test_methodology_changelog.py::test_adr_014_exists_and_names_scpd_1_canonical_phrase tests/methodology/test_methodology_changelog.py::test_plugin_yaml_version_matches_version_file_invariant --no-header -q` |
| 16 | slice-016-refine-dim-9-with-runtime-prerequisite-completeness-sub-class | RPCD-1: agents/critique.md Dim 9 gains new 9th sub-clause `Runtime-prerequisite completeness on proposed fixes` between existing 8th sub-clause `Shippability-catalog consumer-reference propagation` close and existing `### Bonus: weak graph edges` H3 anchor (already applied live during /critic-calibrate 2026-05-13 (post-slice-015) apply step; CAD-1 byte-equal in-repo↔installed at sha256 `f34c967eaaa34413...`); body names ALL THREE sub-modes — (a) NEW-symbol import-audit (slice-014 M-add-1 N=1) + (b) NEW-status/token allowlist-audit (slice-015 M-add-1 N=1) + (c) NEW-anchor sibling-grep audit (slice-013 M-add-1 N=1) — with N=3 cumulative cross-slice DR-1-catch evidence; 5 prose-pin tests per slice-013/015 5-test precedent including `_sub_clause_present` (bare-substring per slice-015 L463) + `_location_pinned` (scoped-find per slice-015 L475; added at slice-016 /critique-review M-add-1 ACCEPTED-FIXED completing N=3 → N=4 stable `_sub_clause_present` + `_location_pinned` duality) + `_names_three_sub_modes` (strict-three pin on `(a)` + `_ALLOWED_STATUSES` + `sibling`) + `_paragraph_cites_slice_013_014_015` (strict-3-of-3 cross-slice + ≥3-of-4 substantive on `["import", "_ALLOWED_STATUSES", "sibling", "end_anchor"]`) + `_cites_substantive_discipline_anchors` (symmetric add per DR-1 N=4 stable); slice-015's 3 body-bound tests (`_shippability_catalog_propagation_names_both_sub_modes` + `_paragraph_cites_slice_013_and_014` + `_cites_at_least_two_cross_slice_anchors` at test_critique_agent.py:519/554/594) end_anchors tightened from `### Bonus: weak graph edges` → `Runtime-prerequisite completeness on proposed fixes` per slice-016 /critique B1 ACCEPTED-FIXED (mirror of slice-013 M1 + slice-015 M1 tightening pattern N=2 → N=3 stable; generic methodology recurrence — every future Dim 9 sub-clause append tightens predecessor body-bound end_anchors; the 3 `_location_pinned` siblings at L173/304/492 stay at `### Bonus:` per slice-013/015 precedent — structurally load-bearing); reused CAD-1 byte-equality on agents/critique.md (slice-007 introduction) — mini-CAD-1 row 3 PASSING → WRITTEN-FAILING → PASSING transition pattern N=7 → N=8 stable; methodology-changelog v0.31.0 / RPCD-1 entry present in-repo + installed with substantive canonical phrase `Runtime-prerequisite completeness on proposed fixes` pinned across N=3 surfaces (agents/critique.md 9th sub-clause title + in-repo v0.31.0 entry + installed v0.31.0 entry) per slice-013 EPGD-1 + slice-014 PMI-1 v1.1 + slice-015 SCPD-1 3-surface precedent N=4 → N=5 instances stable + Limitations note per slice-011 B5 / slice-013 EPGD-1 / slice-015 SCPD-1 calibration-trail convention extension (-D suffix N=3 → N=4 stable post-RSAD-1 + EPGD-1 + SCPD-1 + RPCD-1 signals /critique-time + /critique-review-time discipline heuristic; no audit-enforced gate); ADR-015 exists at `architecture/decisions/ADR-015-*.md` with canonical phrase per slice-016 AC #3; plugin.yaml.version == VERSION == 0.31.0 at slice-016 ship — third atomic bump under PMI-1 v1.1 version-agnostic-gate shape (slice-014 was first; slice-015 was second; empirical retirement-proof N=2 → N=3 stable); SCPD-1 self-application N=1 → N=2 stable post-codification (slice-015 was first canonical reference; slice-016 is second — Phase 1c superseded `_lists_eight_sub_clauses` → `_lists_nine_sub_clauses` per PMI-1 structural-invariant supersession N=3 → N=4 stable; Phase 5 propagated rows 6 + 11 + 13 + 15 pytest commands in-line BEFORE /validate-slice catalog run per SCPD-1 proactive-application sub-mode; recursive-self-application N=7 → N=8 cumulative post-RSAD-1 codification); /critique-review DR-1 dual-review M-add-1 catch (missing `_location_pinned` sibling test breaking N=3 stable `_sub_clause_present` + `_location_pinned` duality at slice-011 L146/158 + slice-013 L270/286 + slice-015 L463/475) — DR-1 catch class shifted from RPCD-1 sub-mode (a/b/c) at slice-013/014/015 to Wiegers regression-guard coverage symmetry at slice-016 (design-doc-level mechanical-table-vs-canonical-inventory asymmetry per CCC-1 v1.1 sub-clause 2 surface; N=1 watch-list candidate for future /critic-calibrate aggregation if recurs) | `<HOME>/.claude/.venv/Scripts/python.exe -m pytest tests/methodology/test_critique_agent.py::test_critique_dim_9_lists_twelve_sub_clauses tests/methodology/test_critique_agent.py::test_critique_dim_9_runtime_prerequisite_completeness_sub_clause_present tests/methodology/test_critique_agent.py::test_critique_dim_9_runtime_prerequisite_completeness_location_pinned tests/methodology/test_critique_agent.py::test_critique_dim_9_runtime_prerequisite_completeness_names_three_sub_modes tests/methodology/test_critique_agent.py::test_critique_dim_9_runtime_prerequisite_completeness_paragraph_cites_slice_013_014_015 tests/methodology/test_critique_agent.py::test_critique_dim_9_runtime_prerequisite_completeness_cites_substantive_discipline_anchors tests/methodology/test_critique_agent_drift.py::test_in_repo_and_installed_critique_agent_are_content_equal tests/methodology/test_methodology_changelog.py::test_v_0_31_0_rpcd_1_entry_present_in_repo tests/methodology/test_methodology_changelog.py::test_v_0_31_0_rpcd_1_entry_names_three_sub_modes_in_repo tests/methodology/test_methodology_changelog.py::test_adr_015_exists_and_names_rpcd_1_canonical_phrase tests/methodology/test_methodology_changelog.py::test_plugin_yaml_version_matches_version_file_invariant --no-header -q` | <2s | `<interp> -m pytest tests/methodology/test_critique_agent.py::test_critique_dim_9_lists_twelve_sub_clauses tests/methodology/test_critique_agent.py::test_critique_dim_9_runtime_prerequisite_completeness_sub_clause_present tests/methodology/test_critique_agent.py::test_critique_dim_9_runtime_prerequisite_completeness_location_pinned tests/methodology/test_critique_agent.py::test_critique_dim_9_runtime_prerequisite_completeness_names_three_sub_modes tests/methodology/test_critique_agent.py::test_critique_dim_9_runtime_prerequisite_completeness_paragraph_cites_slice_013_014_015 tests/methodology/test_critique_agent.py::test_critique_dim_9_runtime_prerequisite_completeness_cites_substantive_discipline_anchors tests/methodology/test_critique_agent_drift.py::test_in_repo_and_installed_critique_agent_are_content_equal tests/methodology/test_methodology_changelog.py::test_v_0_31_0_rpcd_1_entry_present_in_repo tests/methodology/test_methodology_changelog.py::test_v_0_31_0_rpcd_1_entry_names_three_sub_modes_in_repo tests/methodology/test_methodology_changelog.py::test_adr_015_exists_and_names_rpcd_1_canonical_phrase tests/methodology/test_methodology_changelog.py::test_plugin_yaml_version_matches_version_file_invariant --no-header -q` |
| 17 | slice-017-address-tf-1-plan-staleness-discipline | TPHD-1: 3-surface skill-prose discipline at `skills/critique/SKILL.md` Step 4 end (between L122 and L124) + `skills/critique-review/SKILL.md` Step 3 end (between L83 and L85) + `skills/build-slice/SKILL.md` NEW bullet INTO existing `## Prerequisite check` section (per /critique M2 ACCEPTED-FIXED: NOT a new `### Step 0` — build-slice step numbering is 1,2,3,4,5,6,7,7b,7c,8 with no Step 0; discipline IS structurally a prerequisite verification) codifying TF-1 plan harmonization on test function name + AC row reference changes at /critique fix-prose + /critique-review fix-prose + /build-slice Prerequisite-check pre-flight; 9 prose-pin + entry-pin tests across 4 test files (2 NEW: `test_critique_skill.py` + `test_critique_review_skill.py` with `_present` + `_location_pinned` duality per slice-016 N=4 → N=5 stable + 1 EXTENDED: `test_build_slice_skill.py` with prerequisite-check bullet pins using imperative "Run TPHD-1 pre-flight harmonization" form matching existing bullet style per slice-017 sub-mode (a) self-application + 1 EXTENDED: `test_methodology_changelog.py` with NEW SECTION `# --- Slice-017 / TPHD-1 entry pinning ---` + 3 entry-pin + 1 ADR-pin); methodology-changelog v0.32.0 / TPHD-1 entry present in-repo + installed with substantive canonical phrase `TF-1 plan harmonization discipline` pinned across N=3 surfaces (3 SKILL.md prose blocks + in-repo v0.32.0 entry + installed v0.32.0 entry) per slice-016 RPCD-1 3-surface precedent N=5 → N=6 instances stable + Limitations note per slice-011 B5 / slice-013 EPGD-1 / slice-015 SCPD-1 / slice-016 RPCD-1 calibration-trail convention extension (-D suffix N=4 → N=5 stable post-RSAD-1 + EPGD-1 + SCPD-1 + RPCD-1 + TPHD-1 signals /critique-time + /critique-review-time + /build-slice-time prose-heuristic discipline semantics; no audit-enforced gate; v2 `tools/tphd_1_audit.py` deferred until N≥3 violations recur); ADR-016 exists at `architecture/decisions/ADR-016-*.md` with canonical phrase per slice-017 AC #3; plugin.yaml.version == VERSION == 0.32.0 at slice-017 ship — fourth atomic bump under PMI-1 v1.1 version-agnostic-gate shape (slice-014 first, slice-015 second, slice-016 third; empirical retirement-proof N=3 → N=4 stable); EPGD-1 self-application N=5 → N=6 stable (0 of 12 prior entry-pin functions touched — v0.29.0 doubled per slice-014 (a)↔(b) duality + v0.31.0 doubled per slice-016 RPCD-1 (a)↔(b) duality per slice-017 /critique-review m-add-1 ACCEPTED-FIXED count correction); SCPD-1 stays at N=2 stable (NO Dim 9 sub-clause supersession this slice — `_lists_nine_sub_clauses` stays valid; row 17 added as NEW row, no prior-row touch needed; SCPD-1 proactive-application vacuously satisfied per slice-017 /critique m3 ACCEPTED-FIXED); recursive-self-application N=8 → N=9 cumulative post-RSAD-1 codification (slice-017 first-Critic 7 catches + meta-Critic 1 catch = 8 self-defects on own draft, matching slice-013 N=7 high-water mark + meta-Critic add); TPHD-1 sub-mode (a) self-application empirically demonstrated at slice-017 /critique fix-prose (M2 ACCEPTED-FIXED renamed `_phase_0_step_*` → `_prerequisite_check_bullet_*` in TF-1 plan rows 8-9 in same fix block); DR-1 catch-class diversification N=4 → N=5 stable (slice-017 meta-Critic m-add-1 = Wiegers regression-guard coverage-symmetry at design-doc-level mechanical-table-vs-canonical-inventory — count drift 11 → 12 across 6 sites; SAME class as slice-016 M-add-1; N=2 cumulative watch-list ratchets toward N=3 promotion threshold for future Dim 9 sub-clause refinement at slice-018+) | `<HOME>/.claude/.venv/Scripts/python.exe -m pytest tests/methodology/test_critique_skill.py tests/methodology/test_critique_review_skill.py tests/methodology/test_build_slice_skill.py::test_build_slice_skill_md_tphd_1_prerequisite_check_bullet_present tests/methodology/test_build_slice_skill.py::test_build_slice_skill_md_tphd_1_prerequisite_check_bullet_location_pinned tests/methodology/test_methodology_changelog.py::test_v_0_32_0_tphd_1_entry_present_in_repo tests/methodology/test_methodology_changelog.py::test_v_0_32_0_tphd_1_entry_names_three_sub_modes_in_repo tests/methodology/test_methodology_changelog.py::test_v_0_32_0_tphd_1_entry_names_slice_016_cross_slice_anchor tests/methodology/test_methodology_changelog.py::test_adr_016_exists_and_names_tphd_1_canonical_phrase tests/methodology/test_methodology_changelog.py::test_plugin_yaml_version_matches_version_file_invariant --no-header -q` | <2s | `<interp> -m pytest tests/methodology/test_critique_skill.py tests/methodology/test_critique_review_skill.py tests/methodology/test_build_slice_skill.py::test_build_slice_skill_md_tphd_1_prerequisite_check_bullet_present tests/methodology/test_build_slice_skill.py::test_build_slice_skill_md_tphd_1_prerequisite_check_bullet_location_pinned tests/methodology/test_methodology_changelog.py::test_v_0_32_0_tphd_1_entry_present_in_repo tests/methodology/test_methodology_changelog.py::test_v_0_32_0_tphd_1_entry_names_three_sub_modes_in_repo tests/methodology/test_methodology_changelog.py::test_v_0_32_0_tphd_1_entry_names_slice_016_cross_slice_anchor tests/methodology/test_methodology_changelog.py::test_adr_016_exists_and_names_tphd_1_canonical_phrase tests/methodology/test_methodology_changelog.py::test_plugin_yaml_version_matches_version_file_invariant --no-header -q` |
| 18 | slice-018-cleanup-slice-016-rpcd-1-sibling-test-scoping-flaw | RPCD-1 sibling test-scoping discipline restoration: `tests/methodology/test_methodology_changelog.py::test_v_0_31_0_rpcd_1_entry_names_three_sub_modes_in_repo` refactored body at L910-967 to scope assertions to v0.31.0 entry body via module-level `_extract_v031_body(content: str) -> str` helper (between `## v0.31.0` and `## v0.30.0` boundaries; mirrors slice-017 TPHD-1 sibling pattern at L1086-1094 at the pattern level, NOT literal-code level per slice-018 /critique-review m-add-2 Audit 3 refinement); 5 sub-mode + discipline-anchor assertions reference `v031_body` rather than raw `content`; surface-context-aware pre-validation assert `assert "## v0.31.0" in content, f"{surface_name} methodology-changelog.md missing v0.31.0 entry — entry-pin broken (slice-016 RPCD-1 surface)"` at CALL site INSIDE `for surface_name, content in [...]` loop per slice-018 /critique-review m-add-1 ACCEPTED-FIXED (preserves slice-017 L1088-1090 surface-aware diagnostic pattern; helper assumes pre-validated input); NEW regression test `test_v_0_31_0_rpcd_1_sibling_scoping_rejects_stripped_v031_body` (~L1010-1060) calls SAME `_extract_v031_body` helper on synthetic methodology-changelog content with v0.32.0 markers retained + v0.31.0 body markers stripped + v0.30.0 boundary present — single-code-path discipline per slice-018 /critique M2 ACCEPTED-FIXED: regression-test-passes ↔ sibling-test-fails-on-stripped-fixture link established by execution (NOT code-reading); test asserts `"Sub-mode (a)" not in v031_body` AND `"Sub-mode (a)" in synthetic` (proves global-substring fallacy that slice-016 original sibling test silently masked); docstring at L955 + L1018 + L1031 documents slice-017 DEVIATION-1 evidence anchor + slice-017 TPHD-1 sibling canonical pattern at L1086-1094 (3 grep hits); retires latent regression risk on slice-016 RPCD-1 pin (silent false-positive failure mode where future slice could strip Sub-mode markers from v0.31.0 body while v0.32.0+ retains them, causing original global-substring test to false-positive PASS — methodology pin silently broken); NO methodology version bump (slice-018 is pure cleanup; PMI-1 v1.1 stays at 0.32.0; CAD-1 byte-equality preserved bidirectionally on agents/critique.md at slice-017 ship hash `f34c967eaaa34413` AND on methodology-changelog.md at slice-017 ship hash `06ce0c442874f0aa`); NO SKILL.md prose / no agents/critique.md edit / no new ADR / no Dim 9 sub-clause refinement (cleanup-only); shippability row 16 unchanged per Decision Option 1 function-name preservation + Audit 4 empirical enumeration (row 16 contains preserved function name as 1 of 11 pytest commands; SCPD-1 sub-mode (b) proactive-application vacuously satisfied; SCPD-1 stays at N=2 stable post-codification); EPGD-1 self-application N=6 → N=7 stable (0 of 15 prior entry-pin functions touched at FUNCTION-NAME level — `grep -c "^def test_v_0_" tests/methodology/test_methodology_changelog.py` returns 15 pre-edit + post-edit; structural separation discipline preserved at function-name level despite BODY refactor of one v0.31.0-doublet entry-pin function L910-967); TPHD-1 self-application empirically demonstrated at all 3 sub-modes (slice-018 IS canonical reference instance #2 of TPHD-1 — sub-mode (a) /critique B1 ACCEPTED-FIXED Path A renamed `_scopes_to_v031_body` → `_entry_names_three_sub_modes_in_repo` across 5 surfaces in same fix block; sub-mode (b) /critique-review m-add-2 ACCEPTED-FIXED updated design.md Audit 3 + added Audit 7 in same fix block; sub-mode (c) /build-slice Phase 0 verified TF-1 plan rows match design.md function names BEFORE Phase 1); TPHD-1 self-application N=1 → N=2 stable post-codification at slice-017; recursive-self-application N=9 → N=10 cumulative post-RSAD-1 codification (slice-018 first-Critic B1 = 1 self-defect on own draft — TPHD-1 class); DR-1 catch class diversification N=5 → N=6 cumulative stable (3 new classes at slice-018: m-add-1 diagnostic-quality regression vs slice-017 canonical L1088-1090; m-add-2 helper-extraction asymmetry foreshadowing-decline per Fowler rule-of-three + YAGNI; m-add-3 evidence-trace gap on bidirectional installed `## v0.30.0` empirical verification — all 3 distinct from prior RPCD-1 sub-modes at slice-013/014/015 + Wiegers regression-guard coverage-symmetry at slice-016/017); BC-1 self-application empirically verified clean at /critique fix-prose AND /build-slice Phase 6 (literal output `"No build-checks rules apply to this slice."` both times — BC-PROJ-2 negative-anchor migration from slice-012 silences methodology-vocabulary class as predicted at slice-005/006/007/010/011 N=5 cumulative); 2 build-time DEVIATIONs both methodology-recurrence-layer (NEW first-Critic-MISS classes at N=1 each — DEVIATION-1 mini-CAD-1 ceremonial-transition-cleanup-slice-applicability missed by both Critics, caught by auto-mode classifier blocking agents/critique.md modification attempt; DEVIATION-2 cleanup-slice TF-1 row enumeration discipline missed by both Critics, caught by TF-1 audit `--strict-pre-finish` at /build-slice Phase 6 with 3 violations — 2× ac-without-row + 1× non-passing-pre-finish; both fixed inline; design.md NOT updated post-build); ZERO classical build-time DEVIATIONs at slice-018 (compared to slice-017's 3 classical DEVIATIONs — TPHD-1 codification + slice-018 self-application empirically pays off at the classical-build-deviation layer); 13th consecutive 100% Critic-disposition accuracy slice (running 105/105 first-Critic across slices 6-18 + 108/108 cross-Critic-stack across slices 6-18 — extending slice-017 project records); bidirectional sha256 forensic capture N=14 stable (both agents/critique.md + methodology-changelog.md byte-equal at slice-017 ship hashes); VAL-1 Layer B intra-repo `tests` namespace-package class N=15 → N=16 cumulative recurrence (every slice 003-018 hits it; handled cleanly via `--imports-allowlist tests`); Windows cp1252 console encoding class N=2 → N=3 cumulative recurrence (slice-007 + slice-016 + slice-018; promotion threshold MET; `audit-tools-default-utf8-stdout` candidate ready for slice-019+); plugin.yaml.version == VERSION == 0.32.0 unchanged from slice-017 (cleanup-only; no atomic version bump under PMI-1 v1.1 version-agnostic-gate shape; empirical retirement-proof stays N=4 stable) | `<HOME>/.claude/.venv/Scripts/python.exe -m pytest tests/methodology/test_methodology_changelog.py::test_v_0_31_0_rpcd_1_entry_names_three_sub_modes_in_repo tests/methodology/test_methodology_changelog.py::test_v_0_31_0_rpcd_1_sibling_scoping_rejects_stripped_v031_body tests/methodology/test_methodology_changelog.py::test_v_0_31_0_rpcd_1_entry_present_in_repo tests/methodology/test_critique_agent_drift.py::test_in_repo_and_installed_critique_agent_are_content_equal tests/methodology/test_methodology_changelog.py::test_plugin_yaml_version_matches_version_file_invariant --no-header -q` | <2s | `<interp> -m pytest tests/methodology/test_methodology_changelog.py::test_v_0_31_0_rpcd_1_entry_names_three_sub_modes_in_repo tests/methodology/test_methodology_changelog.py::test_v_0_31_0_rpcd_1_sibling_scoping_rejects_stripped_v031_body tests/methodology/test_methodology_changelog.py::test_v_0_31_0_rpcd_1_entry_present_in_repo tests/methodology/test_critique_agent_drift.py::test_in_repo_and_installed_critique_agent_are_content_equal tests/methodology/test_methodology_changelog.py::test_plugin_yaml_version_matches_version_file_invariant --no-header -q` |
| 19 | slice-019-harden-diagnose-layering-evidence | LAYER-EVID-1: textual import-evidence requirement codified at `/diagnose` 03f-layering pass-template level (Method step 4 with 5 TS variants + 3 re-export forms + multi-line via `re.DOTALL` + alias-aware tsconfig `paths`/`baseUrl` resolution + concrete Rust/Go/Java fallbacks + Severity rubric downgrade rule + Anti-patterns negative-pin); cross-referenced at `skills/diagnose/SKILL.md` Step 5 with one-line paragraph; methodology-changelog v0.33.0 entry present in-repo + installed with canonical phrase `textual import-evidence requirement` pinned across N=3 surfaces; first introduction of **mini-CAD for /diagnose** (bidirectional sha256 byte-equality on SKILL.md + passes/03f-layering.md per slice-007 CAD-1 + slice-010 mini-CAD precedent — single-file convention per /critique B3 ACCEPTED-PENDING: 2 single-file tests, not 1 dual-file test); 12 prose-pin + entry-pin tests across 5 test files including NEW `tests/skills/diagnose/test_layering_pass_textual_evidence.py` (2 integration tests with private `_grep_textual_import` helper covering all language variants + alias resolution + 2 synthetic fixture trees `parallel_types_no_import/` + `parallel_types_real_import/`) + NEW `tests/skills/diagnose/test_diagnose_skill_drift.py` (2 mini-CAD tests) + 3 NEW prose-pin tests in `test_skill_md_pins.py` + 4 NEW entry-pin tests in `test_methodology_changelog.py` (incl. `_extract_v033_body` helper + sibling-scoping regression test per slice-018 N=2 cumulative inheritance) + 1 NEW R-3 audit test in `test_risk_register_audit_real_file.py`; `R-3` added to `architecture/risk-register.md` per RR-1 schema (status=mitigating; reversibility=cheap; broader-class concern: graphify symbol-resolution may conflate same-name cross-file symbols into phantom edges); `ADR-017` exists at `architecture/decisions/ADR-017-textual-evidence-requirement-for-diagnose-boundary-findings.md` with canonical phrase per slice-019 AC #4 (Option A vs Option B trade-off documented with reversibility=cheap, escalation path via R-3 if /critic-calibrate flags symbol-conflation false-positives in other passes/consumers at N≥2 distinct slices); plugin.yaml.version == VERSION == 0.33.0 at slice-019 ship — **fifth atomic bump** under PMI-1 v1.1 version-agnostic-gate shape (slice-014 first, slice-015 second, slice-016 third, slice-017 fourth; empirical retirement-proof N=4 → N=5 stable); EPGD-1 self-application N=6 → N=7 stable (0 of 15 prior entry-pin functions touched at function-name level; slice-019 ADDS-only post-row-19 / new ADR-017 / new R-3 / new v0.33.0 entry without touching v0.22.0..v0.32.0 entry-pin functions); SCPD-1 self-application N=2 → N=3 stable post-codification (slice-015 first canonical reference; slice-016 second; slice-019 third — Phase 5 propagated row 19 in-line BEFORE /validate-slice catalog run per SCPD-1 proactive-application sub-mode); TPHD-1 self-application N=2 → N=3 stable post-codification at all 3 sub-modes (slice-018 was second; slice-019 IS canonical reference instance #3 — sub-mode (a) at /critique fix-prose harmonization of mission-brief TF-1 plan + design.md "What's new" in B3 + M4 + M-add-1 same-fix-blocks; sub-mode (b) at /critique-review fix-prose harmonization of mission-brief AC #1 + ADR-017 in M-add-1 + M-add-2; sub-mode (c) at /build-slice Phase 0 prerequisite-check pre-flight); recursive-self-application N=10 → N=11 cumulative post-RSAD-1 codification (slice-019 first-Critic 10 catches + meta-Critic 2 catches = 12 self-defects on own draft — new project high-water mark, exceeding slice-013 N=8 + slice-017 N=8); **LAYER-EVID-1 self-application N=1 standalone** at codification time (canonical reference instance #1 — the witness investigation manually grep-disproving F-LAYER-bca9c001 IS the rule applied retrospectively to the slice's own witness BEFORE codification; mirrors slice-015 SCPD-1 + slice-017 TPHD-1 canonical-reference-instance-at-codification-time precedent per ADR-017 RSAD-1 section + slice-019 /critique-review M-add-2 wording refinement); DR-1 catch-class diversification N=6 → N=7 stable (slice-019 meta-Critic added 1 NEW class — *Self-application-qualifier coherence on canonical-reference-instance naming* — distinct from prior 6 classes: 3 RPCD-1 sub-modes + 2 Wiegers regression-guard coverage-symmetry + 1 cleanup-slice secondary-cost; watch-list at N=1, promote to Dim 9 sub-clause refinement at N≥3); bidirectional sha256 forensic capture N=14 → N=15 stable with **per-file hash list extending from N=2 files to N=4 files** ({agents/critique.md, methodology-changelog.md} → {agents/critique.md, methodology-changelog.md, skills/diagnose/SKILL.md, skills/diagnose/passes/03f-layering.md}); ZERO classical build-time DEVIATIONs at slice-019 (TPHD-1 self-application N=3 + LAYER-EVID-1 design-time discipline pay off at the classical-build-deviation layer — 1 DEVIATION at slice-018 → 0 at slice-019); 14th consecutive 100% Critic-disposition accuracy slice (running 117/117 first-Critic across slices 6-19 + 122/122 cross-Critic-stack across slices 6-19 — extending slice-018 project records by +10 first-Critic + 2 meta-Critic) | `<HOME>/.claude/.venv/Scripts/python.exe -m pytest tests/skills/diagnose/test_layering_pass_textual_evidence.py tests/skills/diagnose/test_diagnose_skill_drift.py tests/skills/diagnose/test_skill_md_pins.py::test_skill_md_step5_documents_textual_evidence_rule tests/skills/diagnose/test_skill_md_pins.py::test_layering_pass_template_emits_textual_evidence_rule tests/skills/diagnose/test_skill_md_pins.py::test_textual_evidence_rule_byte_equal_across_n_3_surfaces tests/methodology/test_methodology_changelog.py::test_v_0_33_0_layer_evid_1_entry_present_in_repo tests/methodology/test_methodology_changelog.py::test_v_0_33_0_layer_evid_1_entry_names_textual_import_evidence_canonical_phrase tests/methodology/test_methodology_changelog.py::test_v_0_33_0_layer_evid_1_sibling_scoping_rejects_stripped_v033_body tests/methodology/test_methodology_changelog.py::test_adr_017_exists_and_names_layer_evid_1_canonical_phrase tests/methodology/test_risk_register_audit_real_file.py::test_r_3_added_post_slice_019_with_graphify_symbol_conflation_class --no-header -q` | <2s | `<interp> -m pytest tests/skills/diagnose/test_layering_pass_textual_evidence.py tests/skills/diagnose/test_diagnose_skill_drift.py tests/skills/diagnose/test_skill_md_pins.py::test_skill_md_step5_documents_textual_evidence_rule tests/skills/diagnose/test_skill_md_pins.py::test_layering_pass_template_emits_textual_evidence_rule tests/skills/diagnose/test_skill_md_pins.py::test_textual_evidence_rule_byte_equal_across_n_3_surfaces tests/methodology/test_methodology_changelog.py::test_v_0_33_0_layer_evid_1_entry_present_in_repo tests/methodology/test_methodology_changelog.py::test_v_0_33_0_layer_evid_1_entry_names_textual_import_evidence_canonical_phrase tests/methodology/test_methodology_changelog.py::test_v_0_33_0_layer_evid_1_sibling_scoping_rejects_stripped_v033_body tests/methodology/test_methodology_changelog.py::test_adr_017_exists_and_names_layer_evid_1_canonical_phrase tests/methodology/test_risk_register_audit_real_file.py::test_r_3_added_post_slice_019_with_graphify_symbol_conflation_class --no-header -q` |
| 20 | slice-020-codify-bug-fix-repro-prelude-at-slice | BFRD-1: bug-fix repro prelude discipline codified at `skills/slice/SKILL.md` Step 3c (NEW section between Step 3b L140 + Step 4 L142 anchors); methodology-changelog v0.34.0 entry present in-repo + installed with bidirectional sha256 byte-equality at `991a17f439ef7c35...`; canonical phrase `bug-fix repro prelude discipline` pinned across N=3 surfaces (skill prose + in-repo changelog + installed changelog); verification mechanism canonical phrase `shippability.md grep verification` pinned per /critique B2 + /critique-review M-add-2 ACCEPTED-FIXED Option (a) — DROPPED aspirational `bug:` provenance branch (zero precedent in shippability rows 1-19); two detection modes named ((a) name-shape fast-path covering `fix-*` prefix + `*-fix` suffix witnessed at slice-001 + bugfix-* + hotfix-* + defect-* + repair-* + patch-* + harden-*-bug regex variants per /critique B1 ACCEPTED-FIXED + (b) PRIMARY candidate-source signal); ADR-018 exists at `architecture/decisions/ADR-018-bfrd-1-bug-fix-repro-prelude-discipline.md` with canonical phrase per slice-020 AC #3 (reversibility=cheap ~11 sites per /critique B3 ACCEPTED-FIXED recount + /critique-review M-add-1 ACCEPTED-FIXED propagation to ADR-018 L169 Conclusion); plugin.yaml.version == VERSION == 0.34.0 at slice-020 ship — sixth atomic bump under PMI-1 v1.1 version-agnostic-gate shape (slice-014..019 = 5 prior bumps; empirical retirement-proof atomic-bump count N=5 → N=6 stable per /critique m5); EPGD-1 self-application N=6 → N=7 stable (0 of 15 prior entry-pin functions touched at function-name level; body-only refactor at L12-74 generalizing _extract_v031_body + _extract_v033_body to thin wrappers around new _extract_version_body(content, version) helper per /critique M5 ACCEPTED-FIXED Option C rule-of-three promotion N=2 → N=3 stable); SCPD-1 stays at N=3 stable (no Dim 9 sub-clause supersession; row 20 added as NEW row with no prior-row touch needed; SCPD-1 sub-mode (b) vacuously satisfied); TPHD-1 self-application N=3 → N=4 stable post-codification at all 3 sub-modes (slice-020 IS canonical reference instance #4: sub-mode (a) /critique fix-prose harmonized mission-brief TF-1 plan 9 → 11 rows + design.md test-name list + Step 3c content structure + ACs #1/#2/#3 + Must-not-defer in same /critique fix block as B2 verification-mechanism addition; sub-mode (b) /critique-review fix-prose harmonized ADR-018 L169 + design.md Step 3c item 4 + design.md Audit 8 + design.md `Prose-pin test assertion locks` subsection in same /critique-review fix block as M-add-1 + M-add-2 + M-add-3 + M2 SUSPICIOUS clarification; sub-mode (c) /build-slice Phase 0 prerequisite-check verified TF-1 plan ↔ design.md function names BEFORE Phase 1 entry); BFRD-1 self-application N/A at codification time (contingent inapplicability per /critique M1 ACCEPTED-FIXED — slice-020 is NEW-feature methodology-codification slice; first canonical reference instance at slice-021+ prospectively); recursive-self-application N=11 → N=17 cumulative (slice-020 first-Critic 14 + meta-Critic 3 missed = 17 substantive self-defects on own draft — new project HWM exceeding slice-019 N=12; bug-fix-block-completeness blind spot recurred WITHIN B3 fix block at ADR-018 L169 catch by /critique-review M-add-1 — NEW first-Critic-MISS class candidate at N=1); DR-1 catch-class diversification N=7 → N=8 stable with NEW class candidate at N=1 (fix-block-completeness on count-drift Blockers per /critique-review M-add-1); Wiegers regression-guard coverage-symmetry watch-list N=4 → N=5 within slice-020 (B3 first-Critic + M-add-1 meta-Critic = same class within same slice's fix block); RPCD-1 sub-mode (b) self-application N=2 stable post-codification at slice-016 (slice-020 M-add-2 catch + Option (a) fix on `bug:` provenance branch aspirational state); bidirectional sha256 forensic capture N=15 → N=16 stable (methodology-changelog.md `991a17f439ef7c35...` + skills/slice/SKILL.md `cc18b5a05c2220dd...` byte-equal in-repo↔installed); CAD-1 byte-equality preserved on agents/critique.md at `f34c967eaaa34413...` (slice-019 ship hash unchanged through slice-020; not touched); mini-CAD-1 row 3 PASSING → WRITTEN-FAILING → PASSING transition pattern N=10 → N=11 stable; Windows cp1252 console encoding class N=3 → N=4 cumulative recurrence at `tools/critique_review_audit.py` (slice-007 + slice-016 + slice-018 + slice-020; PYTHONIOENCODING=utf-8 workaround applied at /critique-review; `audit-tools-default-utf8-stdout` candidate stays ripe for slice-021+); 11/11 TF-1 rows PASSING; 7 new tests (3 prose-pin + 4 entry-pin/ADR-pin) added; methodology suite 447 + 7 = 454 tests stable; full project PASS in <3.5s; shippability catalog 19 → 20 rows; validate-using-your-own-ship N=17 → N=18 stable; empirical-verification-at-design-time discipline N=18 → N=19 stable (8 audits all VALIDATED at /build-slice); MCT-1 default-trigger self-application N=9 → N=10 stable (slice-020 modifies skills/slice/SKILL.md + methodology-changelog.md, MCT-1 trigger glob match) | `<HOME>/.claude/.venv/Scripts/python.exe -m pytest tests/methodology/test_methodology_changelog.py::test_v_0_34_0_bfrd_1_entry_present_in_repo tests/methodology/test_methodology_changelog.py::test_v_0_34_0_bfrd_1_entry_names_both_detection_modes tests/methodology/test_methodology_changelog.py::test_v_0_34_0_bfrd_1_entry_names_stop_and_route_behavior tests/methodology/test_methodology_changelog.py::test_v_0_34_0_bfrd_1_entry_names_verification_mechanism tests/methodology/test_methodology_changelog.py::test_adr_018_exists_and_names_bfrd_1_canonical_phrase tests/methodology/test_slice_skill.py::test_slice_skill_md_bfrd_1_prelude_present tests/methodology/test_slice_skill.py::test_slice_skill_md_bfrd_1_prelude_location_pinned tests/methodology/test_slice_skill.py::test_slice_skill_md_bfrd_1_verification_mechanism_present tests/methodology/test_slice_skill_drift.py::test_in_repo_and_installed_slice_skill_md_are_content_equal tests/methodology/test_methodology_changelog.py::test_plugin_yaml_version_matches_version_file_invariant --no-header -q` | <2s | `<interp> -m pytest tests/methodology/test_methodology_changelog.py::test_v_0_34_0_bfrd_1_entry_present_in_repo tests/methodology/test_methodology_changelog.py::test_v_0_34_0_bfrd_1_entry_names_both_detection_modes tests/methodology/test_methodology_changelog.py::test_v_0_34_0_bfrd_1_entry_names_stop_and_route_behavior tests/methodology/test_methodology_changelog.py::test_v_0_34_0_bfrd_1_entry_names_verification_mechanism tests/methodology/test_methodology_changelog.py::test_adr_018_exists_and_names_bfrd_1_canonical_phrase tests/methodology/test_slice_skill.py::test_slice_skill_md_bfrd_1_prelude_present tests/methodology/test_slice_skill.py::test_slice_skill_md_bfrd_1_prelude_location_pinned tests/methodology/test_slice_skill.py::test_slice_skill_md_bfrd_1_verification_mechanism_present tests/methodology/test_slice_skill_drift.py::test_in_repo_and_installed_slice_skill_md_are_content_equal tests/methodology/test_methodology_changelog.py::test_plugin_yaml_version_matches_version_file_invariant --no-header -q` |
| 21 | slice-021-add-feature-branch-workflow-at-build-and-commit-slice | BRANCH-1: branch-per-slice workflow codified at `skills/build-slice/SKILL.md` `## Prerequisite check ### Branch state` sub-section + Step 7c canonical `BRANCH=skip — rationale: <text>` shape + Step 6 pre-finish gate audit invocation; `skills/commit-slice/SKILL.md` `--do-commit` → `--merge` swap with 2 pre-flight guardrails + 5-step merge flow; `tools/branch_workflow_audit.py` (NEW) audit with 8 unit tests covering 5 violation classes + 3 default-branch-resolution paths; methodology-changelog v0.35.0 entry present in-repo + installed with bidirectional sha256 byte-equality; canonical phrase `branch-per-slice workflow` pinned across N=3 surfaces (skill prose + in-repo changelog + installed changelog); default-branch resolution canonical phrase across N=3 surfaces (audit + 2 SKILL.md surfaces per Fowler rule-of-three); ADR-019 exists at `architecture/decisions/ADR-019-branch-per-slice-workflow.md` with canonical phrase per slice-021 AC #5 (reversibility=cheap ~25 source files + 5 test-file extends = 30 total touches per /critique-rerun M4-new ACCEPTED-FIXED option (b) canonical single-enumeration in design.md §Files changed); plugin.yaml.version == VERSION == 0.35.0 at slice-021 ship — seventh atomic bump under PMI-1 v1.1 (slice-014..020 = 6 prior bumps; empirical retirement-proof atomic-bump count N=6 → N=7 stable); EPGD-1 self-application N=7 → N=8 stable (0 of 15 prior entry-pin functions touched; ADDS-only `_v_0_35_0_branch_1_*` + ADR-pin); TPHD-1 self-application N=4 → N=5 partial (per /critique-review-rerun M-add-4-rerun ACCEPTED-FIXED — harmonization required 4 review passes: /critique + /critique-review + /critique-rerun + /critique-review-rerun); BRANCH-1 self-application N=1 bootstrap-reference-instance (slice-021 IS bootstrap; first non-bootstrap canonical-reference-instance at next non-/repro slice); CAD-1 byte-equality preserved on `agents/critique.md` at slice-017 ship hash `f34c967eaaa34413` (untouched); mini-CAD-1 row 3 PASSING → WRITTEN-FAILING → PASSING transition pattern N=11 → N=13 stable (slice-021 adds 2 NEW mini-CAD families: build-slice + commit-slice); INST-1 canonical list updated `_CANONICAL_TOOLS` N=15 → N=16 with `tools.branch_workflow_audit`; PMI-1 manifest updated tools list with `tools/branch_workflow_audit.py`; recursive-self-application N=17 → N=28 cumulative HWM (slice-021 lifecycle: 14 first-Critic + 4 /critique-review missed + 11 /critique-rerun + 4 /critique-review-rerun missed = 33 total findings on own draft — new project HWM exceeding slice-020 N=17); DR-1 catch-class diversification N=8 → N=10 cumulative stable (2 NEW class candidates at N=1: cross-slice-branch leakage at upstream skill pipeline + milestone-summary-surface as load-bearing claims-propagation target); Wiegers regression-guard coverage-symmetry watch-list N=5 → N=9 cumulative within slice-021 alone (well past N=3 promotion threshold — ELEVATED to /critic-calibrate slice-022 from slice-024+); Windows cp1252 console encoding class N=4 → N=5 cumulative recurrence at test `write_text` default-encoding (slice-021 logged DEVIATION inline); 29/29 TF-1 rows PASSING; 12 NEW tests + 5 existing-test-file extends; methodology suite 454 → ~470 stable; shippability catalog 20 → 21 rows; validate-using-your-own-ship N=18 → N=19 stable; empirical-verification-at-design-time discipline N=19 → N=20 stable; MCT-1 default-trigger self-application N=10 → N=11 stable (slice-021 modifies skills/build-slice/SKILL.md + skills/commit-slice/SKILL.md + methodology-changelog.md + agents/critique.md untouched, MCT-1 trigger glob match) | `<HOME>/.claude/.venv/Scripts/python.exe -m pytest tests/methodology/test_branch_workflow_audit.py tests/methodology/test_build_slice_skill_branch_create.py tests/methodology/test_build_slice_skill_step_7c_branch_skip_canonical_shape.py tests/methodology/test_commit_slice_skill_merge_flag.py tests/methodology/test_root_claude_md_branch_per_slice_rule.py tests/methodology/test_build_slice_skill_drift.py tests/methodology/test_commit_slice_skill_drift.py tests/methodology/test_slice_skill_drift.py tests/methodology/test_critique_agent_drift.py::test_critique_agent_drift_audit_clean_at_slice_021_ship tests/methodology/test_install_audit.py::test_install_audit_enumerates_branch_workflow_audit tests/methodology/test_plugin_manifest_audit.py::test_plugin_yaml_lists_branch_workflow_audit tests/methodology/test_methodology_changelog.py::test_v_0_35_0_branch_1_entry_present_in_repo tests/methodology/test_methodology_changelog.py::test_v_0_35_0_branch_1_entry_names_three_sub_modes_in_repo tests/methodology/test_methodology_changelog.py::test_adr_019_branch_per_slice_workflow_exists_and_links_to_branch_1 --no-header -q` | <5s | `<interp> -m pytest tests/methodology/test_branch_workflow_audit.py tests/methodology/test_build_slice_skill_branch_create.py tests/methodology/test_build_slice_skill_step_7c_branch_skip_canonical_shape.py tests/methodology/test_commit_slice_skill_merge_flag.py tests/methodology/test_root_claude_md_branch_per_slice_rule.py tests/methodology/test_build_slice_skill_drift.py tests/methodology/test_commit_slice_skill_drift.py tests/methodology/test_slice_skill_drift.py tests/methodology/test_critique_agent_drift.py::test_critique_agent_drift_audit_clean_at_slice_021_ship tests/methodology/test_install_audit.py::test_install_audit_enumerates_branch_workflow_audit tests/methodology/test_plugin_manifest_audit.py::test_plugin_yaml_lists_branch_workflow_audit tests/methodology/test_methodology_changelog.py::test_v_0_35_0_branch_1_entry_present_in_repo tests/methodology/test_methodology_changelog.py::test_v_0_35_0_branch_1_entry_names_three_sub_modes_in_repo tests/methodology/test_methodology_changelog.py::test_adr_019_branch_per_slice_workflow_exists_and_links_to_branch_1 --no-header -q` |
| 22 | slice-022-redesign-commit-slice-for-pr-aware-flow | 3-mode PR-aware /commit-slice taxonomy codified at `skills/commit-slice/SKILL.md` Step 5 sub-steps 5a (default no-flag) / 5b (--merge preserved verbatim from slice-021) / 5c (--push NEW: WT-clean + stale-slice-branch + origin-present + slice-branch-name pre-flight + `git push -u origin slice/NNN-<name>` (NEVER --force) + PR-URL hint with `gh pr create --web` + GitHub.com compare URL; non-ff push STOP + ff re-push explicit-confirm prompt) / 5d (--sync-after-pr NEW: WT-clean + slice-branch + origin + upstream pre-flight + `git fetch --prune origin <default> slice/NNN-<name>` explicit refspec + two-signal merged-state detection (Signal A `git ls-remote` returns non-zero + Signal B two-pass `git cherry` Pass 1 + aggregate-tree-diff Pass 2 with 3 guards: empty-FILES STOP + perf bound N=500 + covers-FILES pinned superset) + `git pull --ff-only origin <default>` + `git branch -d` safe-delete only); new `## When to use which mode` guidance section between Argument modes and Prerequisite check; mutually-exclusive flag set (2+ → STOP); methodology-changelog v0.36.0 entry present in-repo + installed with bidirectional sha256 byte-equality + canonical phrase `3-mode PR-aware /commit-slice taxonomy` pinned across N=3 surfaces (skill prose + in-repo changelog + installed changelog) per slice-019 LAYER-EVID-1 N-surface schema-pin shape ratcheting N=8 → N=9 stable; ADR-020 exists at `architecture/decisions/ADR-020-pr-aware-commit-slice-modes.md` with frontmatter `supersedes: ADR-019` (one-directional per ADR family convention; ADR-019 unmodified per append-only; SUP-1 does NOT apply because that rule scopes to /supersede-slice for archived-slice reflection.md links, NOT ADRs — per slice-022 /critique-review M-add-1 ACCEPTED-FIXED); plugin.yaml.version == VERSION == 0.36.0 at slice-022 ship — eighth atomic bump under PMI-1 v1.1 version-agnostic-gate shape (slice-014..021 = 7 prior bumps; retirement-proof N=7 → N=8 stable); EPGD-1 self-application N=8 → N=9 stable (0 of 15 prior entry-pin functions touched; ADDS-only `_v_0_36_0_pr_aware_commit_slice_*` ×2 + `_adr_020_*` ×2); TPHD-1 self-application N=5 → N=6 partial (sub-mode (c) prerequisite-check CAUGHT TF-1 plan staleness drift at /build-slice — function-name + test-path conventions across 4 surfaces harmonized inline + DEVIATION-1 logged in build-log.md); BRANCH-1 self-application N=1 bootstrap → N=2 stable (slice-022 IS first non-bootstrap canonical-reference instance — per /critique m2 + /critique-review M-add-2 ACCEPTED-FIXED off-by-one correction; slice-021 was bootstrap-reference instance #1); mini-CAD-1 row 3 PASSING → WRITTEN-FAILING → PASSING transition pattern N=13 → N=14 stable (slice-022 modifies skills/commit-slice/SKILL.md; transition spec pinned at TF-1 row 14 per /critique M2 ACCEPTED-FIXED); CAD-1 byte-equality preserved on `agents/critique.md` at slice-017 ship hash `f34c967eaaa34413` (untouched); recursive-self-application N=33 → N=51 cumulative HWM (slice-022 lifecycle: 13 first-Critic + 5 /critique-review missed = 18 substantive self-defects on own draft — exceeds slice-021 N=33 HWM by +18; codification-slice density continues climbing); DR-1 catch-class diversification N=10 → N=12 stable with 2 NEW catch classes from slice-022 /critique-review (SUP-1-misapplication-as-impossible-pre-finish-gate at N=1 + fix-block-completeness-on-count-drift recurrence at N=2 cumulative within slice-022 alone — M-add-2 + M-add-3 are sibling-site propagation failures of /critique m2 + m3 fixes); Wiegers regression-guard coverage-symmetry watch-list N=9 → N=11 cumulative (slice-022 adds 2 NEW: M-add-2 sibling-site + M-add-3 sibling-site + inline must-not-defer count fix); 17/17 slice-022 TF-1 rows PASSING; 2 NEW test files + 2 EXTENDED test files + 4 NEW entry-pin/ADR-pin tests in test_methodology_changelog.py; methodology suite ~470 → ~485 stable; shippability catalog 21 → 22 rows; validate-using-your-own-ship N=19 → N=20 stable; empirical-verification-at-design-time discipline N=20 → N=21 stable | `<HOME>/.claude/.venv/Scripts/python.exe -m pytest tests/methodology/test_commit_slice_skill_push_flag.py tests/methodology/test_commit_slice_skill_sync_after_pr_flag.py tests/methodology/test_commit_slice_skill_merge_flag.py tests/methodology/test_commit_slice_skill_drift.py tests/methodology/test_methodology_changelog.py::test_v_0_36_0_pr_aware_commit_slice_entry_present_in_repo tests/methodology/test_methodology_changelog.py::test_v_0_36_0_entry_names_three_modes_in_repo tests/methodology/test_methodology_changelog.py::test_adr_020_exists_and_supersedes_adr_019 tests/methodology/test_methodology_changelog.py::test_adr_020_documents_three_mode_taxonomy tests/methodology/test_methodology_changelog.py::test_plugin_yaml_version_matches_version_file_invariant tests/methodology/test_critique_agent_drift.py::test_in_repo_and_installed_critique_agent_are_content_equal --no-header -q` | <3s | `<interp> -m pytest tests/methodology/test_commit_slice_skill_push_flag.py tests/methodology/test_commit_slice_skill_sync_after_pr_flag.py tests/methodology/test_commit_slice_skill_merge_flag.py tests/methodology/test_commit_slice_skill_drift.py tests/methodology/test_methodology_changelog.py::test_v_0_36_0_pr_aware_commit_slice_entry_present_in_repo tests/methodology/test_methodology_changelog.py::test_v_0_36_0_entry_names_three_modes_in_repo tests/methodology/test_methodology_changelog.py::test_adr_020_exists_and_supersedes_adr_019 tests/methodology/test_methodology_changelog.py::test_adr_020_documents_three_mode_taxonomy tests/methodology/test_methodology_changelog.py::test_plugin_yaml_version_matches_version_file_invariant tests/methodology/test_critique_agent_drift.py::test_in_repo_and_installed_critique_agent_are_content_equal --no-header -q` |
| 23 | slice-023-audit-tools-default-utf8-stdout | UTF8-STDOUT-1: default UTF-8 stdout in audit tools codified at `tools/_stdout.py` helper (`reconfigure_stdout_utf8()` idempotent function with `errors="replace"`) + `tools/utf8_stdout_audit.py` AST-based structural audit (refuses any `tools/*.py` with `main()` whose first executable statement is not the canonical `_stdout.reconfigure_stdout_utf8()` call; self-applies; JSON output via `--json`; exit 0/1/2) + one-line invocation pattern at every audit tool's `main()` (canonical import `from tools import _stdout` per M4 ACCEPTED-FIXED — 17 tools conform post-slice-023); PMI-1 `_list_actual_tools` extended to filter leading-underscore helpers (B2 ACCEPTED-PENDING — prevents orphan-tool violation on `tools/_stdout.py`); behavioural regression at `tests/methodology/test_utf8_stdout_regression.py` invokes every audit via subprocess under `PYTHONIOENCODING=cp1252 + PYTHONUTF8=0` with U+2192 fixture content (assertion: no `UnicodeEncodeError`/`UnicodeDecodeError` in stderr, NOT exit-code-0 per M1 ACCEPTED-FIXED); methodology-changelog v0.37.0 entry present in-repo + installed with bidirectional sha256 byte-equality; canonical phrase `UTF8-STDOUT-1` pinned across N=3 surfaces (helper docstring + audit docstring + changelog entry) per N-surface schema-pin shape ratcheting N=9 → N=10 stable; ADR-021 exists at `architecture/decisions/ADR-021-utf8-stdout-1-default-utf8-stdout-in-audit-tools.md` (reversibility=cheap; supersedes=null — shell-side `$env:PYTHONIOENCODING="utf-8"` workaround was DEVIATION pattern not ADR-codified discipline); plugin.yaml.version == VERSION == 0.37.0 at slice-023 ship — ninth atomic bump under PMI-1 v1.1 (retirement-proof N=8 → N=9 stable); EPGD-1 self-application N=9 → N=10 stable (0 of 15 prior entry-pin functions touched; ADDS-only `_v_0_37_0_utf8_stdout_1_*` + `_adr_021_*`); UTF8-STDOUT-1 self-application N=1 canonical-reference-instance at codification time (`tools/utf8_stdout_audit.py` itself conforms; audit returns `tools_scanned: 17, tools_with_main: 17, tools_clean: 17`); cumulative Critic-stack at slice-023 = 23 findings (17 first-Critic + 6 meta-Critic — approaching slice-021 HWM N=28); fix-block-completeness watch-list (slice-020 M-add-1) recurred N=4 within slice-023 fix block (M-add-1 row-count + M-add-2 argv-grouping + M-add-3 ADR-021-helper-body + M-add-4 test-file-path 3-site sweep); Wiegers regression-guard coverage-symmetry watch-list N=12 → N=13+ cumulative (B5 count drift across 6 sibling sites swept to canonical 17/17/17); Cross-mission-brief-vs-design-consistency-checking watch-list N=1 → N=3 cumulative (B5 + B3 + M5 cross-file consistency failures — PROMOTION-ELIGIBLE for /critic-calibrate slice-024 Dim 9 sub-clause); Windows cp1252 console encoding class N=6 → RETIRED post-slice-023 (closes the recurring class at slices 007/016/018/020/021/022); CAD-1 byte-equality preserved on agents/critique.md at slice-017 ship hash f34c967eaaa34413 (untouched); 21/21 TF-1 rows PASSING; 3 NEW test files (test_stdout_helper.py + test_utf8_stdout_audit.py + test_utf8_stdout_regression.py) + 1 fixture dir + 3 EXTENDED test files (test_plugin_manifest_audit.py + test_install_audit.py + test_methodology_changelog.py); methodology suite ~485 → ~510 stable; shippability catalog 22 → 23 rows; validate-using-your-own-ship N=20 → N=21 stable; empirical-verification-at-design-time discipline N=21 → N=22 stable | `<HOME>/.claude/.venv/Scripts/python.exe -m pytest tests/methodology/test_stdout_helper.py tests/methodology/test_utf8_stdout_audit.py tests/methodology/test_utf8_stdout_regression.py tests/methodology/test_methodology_changelog.py::test_v_0_37_0_utf8_stdout_1_entry_present_in_repo tests/methodology/test_methodology_changelog.py::test_v_0_37_0_utf8_stdout_1_entry_names_all_three_surfaces tests/methodology/test_methodology_changelog.py::test_v_0_37_0_utf8_stdout_1_entry_pins_canonical_invocation_pattern tests/methodology/test_methodology_changelog.py::test_adr_021_present_and_reversibility_cheap tests/methodology/test_install_audit.py::test_install_audit_enumerates_utf8_stdout_audit tests/methodology/test_plugin_manifest_audit.py::test_plugin_yaml_lists_utf8_stdout_audit tests/methodology/test_plugin_manifest_audit.py::test_list_actual_tools_filters_leading_underscore_helpers --no-header -q` | <8s | `<interp> -m pytest tests/methodology/test_stdout_helper.py tests/methodology/test_utf8_stdout_audit.py tests/methodology/test_utf8_stdout_regression.py tests/methodology/test_methodology_changelog.py::test_v_0_37_0_utf8_stdout_1_entry_present_in_repo tests/methodology/test_methodology_changelog.py::test_v_0_37_0_utf8_stdout_1_entry_names_all_three_surfaces tests/methodology/test_methodology_changelog.py::test_v_0_37_0_utf8_stdout_1_entry_pins_canonical_invocation_pattern tests/methodology/test_methodology_changelog.py::test_adr_021_present_and_reversibility_cheap tests/methodology/test_install_audit.py::test_install_audit_enumerates_utf8_stdout_audit tests/methodology/test_plugin_manifest_audit.py::test_plugin_yaml_lists_utf8_stdout_audit tests/methodology/test_plugin_manifest_audit.py::test_list_actual_tools_filters_leading_underscore_helpers --no-header -q` |
| 24 | slice-024-refine-dim-9-with-fix-block-completeness-sub-clause | FBCD-1: fix-block-completeness discipline codified as `agents/critique.md` Dim 9 10th sub-clause (`Fix-block-completeness discipline`) with two sub-modes — (a) Original-draft cross-file consistency (first-Critic /critique) + (b) Post-ACCEPTED-FIXED sibling-sweep (meta-Critic /critique-review); temporal extension of Dim 9 sub-clause 2 (CCC-1 v1.1) cross-referenced by canonical title strings (not ordinal) so `test_critique_dim_9_cross_references_resolve` stays valid under future renumbering; N=10-cumulative-cross-instance / N=4-distinct-slice evidence base (slice-020 M-add-1 N=1 + slice-021 M-add-1/2/3-rerun N=3 + slice-022 M-add-2/3 N=2 + slice-023 M-add-1/2/3/4 N=4); PMI-1 structural-invariant supersession `_lists_nine_sub_clauses` -> `_lists_ten_sub_clauses` (N=4 -> N=5 stable); 5 NEW FBCD-1 body-bound tests + `_sub_clause_present`+`_location_pinned` duality N=4 -> N=5 stable; 3 slice-016 RPCD-1 body-bound tests' end_anchors tightened `### Bonus:` -> `Fix-block-completeness discipline` (generic methodology recurrence N=3 -> N=4 stable; 4 `_location_pinned` siblings untouched as load-bearing); methodology-changelog v0.38.0 entry present in-repo + installed with bidirectional sha256 byte-equality; canonical phrase `Fix-block-completeness discipline` pinned across N=3 surfaces (critique.md sub-clause title + in-repo changelog + installed changelog) per N-surface schema-pin shape ratcheting N=10 -> N=11 stable; ADR-022 exists at `architecture/decisions/ADR-022-fbcd-1-fix-block-completeness-discipline.md` (reversibility=cheap; supersedes=null); plugin.yaml.version == VERSION == 0.38.0 at slice-024 ship — tenth atomic bump under PMI-1 v1.1 (retirement-proof N=9 -> N=10 stable); EPGD-1 self-application N=10 -> N=11 stable (0 of 17 prior _entry_present_in_repo-family functions touched; ADDS-only `_v_0_38_0_fbcd_1_*` x3 + `_adr_022_exists_and_names_fbcd_1_canonical_phrase`); CAD-1 byte-equality on agents/critique.md re-established post-Phase-1g insert (sha256 f0bd6653cf5a97a0...); first codification slice authored without a prior dedicated /critic-calibrate run (N=2-cross-slice proactive codification convention extended per EPGD-1+SCPD-1 precedent); recursive-self-application closure empirically confirmed (slice committed N=10 FBCD-1 sub-mode (a) violations on own drafts caught at /critique + 3 sub-mode (b) gaps caught at /critique-review M-add-1/2/3); cumulative Critic-stack at slice-024 = 19 findings (16 first-Critic + 3 meta-Critic); SCPD-1 proactive-application N=2 -> N=3 stable (5 pytest-command rows propagated `_lists_nine` -> `_lists_ten` in same /build-slice block before /validate-slice catalog run; 2 historical-narrative occurrences preserved); 13/13 TF-1 rows PASSING; shippability catalog 23 -> 24 rows | <HOME>/.claude/.venv/Scripts/python.exe -m pytest tests/methodology/test_critique_agent.py::test_critique_dim_9_lists_twelve_sub_clauses tests/methodology/test_critique_agent.py::test_critique_dim_9_fix_block_completeness_sub_clause_present tests/methodology/test_critique_agent.py::test_critique_dim_9_fix_block_completeness_location_pinned tests/methodology/test_critique_agent.py::test_critique_dim_9_fix_block_completeness_names_both_sub_modes tests/methodology/test_critique_agent.py::test_critique_dim_9_fix_block_completeness_paragraph_cites_slice_020_021_022_023 tests/methodology/test_critique_agent.py::test_critique_dim_9_fix_block_completeness_cites_substantive_discipline_anchors tests/methodology/test_critique_agent.py::test_critique_dim_9_cross_references_resolve tests/methodology/test_methodology_changelog.py::test_v_0_38_0_fbcd_1_entry_present_in_repo tests/methodology/test_methodology_changelog.py::test_v_0_38_0_fbcd_1_names_both_sub_modes tests/methodology/test_methodology_changelog.py::test_v_0_38_0_fbcd_1_cites_slice_020_021_022_023 tests/methodology/test_methodology_changelog.py::test_adr_022_exists_and_names_fbcd_1_canonical_phrase tests/methodology/test_methodology_changelog.py::test_plugin_yaml_version_matches_version_file_invariant tests/methodology/test_critique_agent_drift.py::test_in_repo_and_installed_critique_agent_are_content_equal --no-header -q | <8s | `<interp> -m pytest tests/methodology/test_critique_agent.py::test_critique_dim_9_lists_twelve_sub_clauses tests/methodology/test_critique_agent.py::test_critique_dim_9_fix_block_completeness_sub_clause_present tests/methodology/test_critique_agent.py::test_critique_dim_9_fix_block_completeness_location_pinned tests/methodology/test_critique_agent.py::test_critique_dim_9_fix_block_completeness_names_both_sub_modes tests/methodology/test_critique_agent.py::test_critique_dim_9_fix_block_completeness_paragraph_cites_slice_020_021_022_023 tests/methodology/test_critique_agent.py::test_critique_dim_9_fix_block_completeness_cites_substantive_discipline_anchors tests/methodology/test_critique_agent.py::test_critique_dim_9_cross_references_resolve tests/methodology/test_methodology_changelog.py::test_v_0_38_0_fbcd_1_entry_present_in_repo tests/methodology/test_methodology_changelog.py::test_v_0_38_0_fbcd_1_names_both_sub_modes tests/methodology/test_methodology_changelog.py::test_v_0_38_0_fbcd_1_cites_slice_020_021_022_023 tests/methodology/test_methodology_changelog.py::test_adr_022_exists_and_names_fbcd_1_canonical_phrase tests/methodology/test_methodology_changelog.py::test_plugin_yaml_version_matches_version_file_invariant tests/methodology/test_critique_agent_drift.py::test_in_repo_and_installed_critique_agent_are_content_equal --no-header -q` |
| 26 | slice-026-enforce-critique-review-prerequisite | CRP-1: critique-review-prerequisite check codified as an audit-enforced /build-slice gate — NEW `tools/critique_review_prerequisite_audit.py` (modeled on BRANCH-1; refuse exit 1 when mode in {STANDARD,HEAVY} + critic-required:true + critique-review.md absent + no canonical `critique-review-skip` milestone.md frontmatter key; escape-hatch-malformed exit 1; usage/mode-unresolvable exit 2; mode from triage.md frontmatter -> CLAUDE.md fallback) + `skills/build-slice/SKILL.md` CRP-1 `## Prerequisite check` sub-block (post critique.md-exists gate, pre `### Branch state`; verbatim STOP routing message) + Step 6 defense-in-depth re-run + Step 7b preserve-key instruction; escape-hatch is milestone.md `critique-review-skip:` frontmatter key NOT build-log.md Events (gate fires pre-build-log) NOR free-form body line (Step 7b clobber + narrative false-positive) per ADR-024; NON-`-D` audit-enforced-gate naming class per ADR-019 (B1 caught + renamed CRPD-1 -> CRP-1 in-round — slice-022 self-violation lesson fired); /critique-review EXTEND added M-add-1 (milestone.md template forward-sync pair under no byte-equality gate — enumeration-discharge, separate-slice scope); slice-026 = CRP-1 bootstrap-reference instance #1 (sub-block cannot self-gate its authoring build; self-application via /critique-review on slice-026 + audit-against-self exit 0 at Step 6); methodology-changelog v0.40.0 entry present in-repo + installed bidirectional byte-equal; canonical phrase `Critique-review prerequisite check` pinned across N=3 surfaces; mini-CAD byte-equality on skills/build-slice/SKILL.md re-established post-edit; milestone.md template propagated in-repo + installed lockstep (M-add-1); ADR-024 exists (reversibility=cheap; supersedes=null); `tools.critique_review_prerequisite_audit` added to install_audit `_CANONICAL_TOOLS` (INST-1 18 -> 19) + plugin.yaml tools list (PMI-1, rule CRP-1) + test_utf8_stdout_regression `_POSITIONAL_SLICE_TOOLS` + roll-up sentinel 18 -> 19 (independent counter from `_CANONICAL_TOOLS`); plugin.yaml.version == VERSION == ~/.claude/ai-sdlc-VERSION == 0.40.0 — twelfth atomic bump under PMI-1 v1.1; SCPD-1 proactive-application N=4 -> N=5 stable; shippability catalog 25 -> 26 rows | <HOME>/.claude/.venv/Scripts/python.exe -m pytest tests/methodology/test_critique_review_prerequisite_audit.py tests/methodology/test_build_slice_skill.py::test_build_slice_prereq_crp_1_refuses_on_absent_mandatory_critique_review tests/methodology/test_build_slice_skill.py::test_build_slice_crp_1_step_7b_preserves_skip_key tests/methodology/test_methodology_changelog.py::test_v_0_40_0_crp_1_entry_present_in_repo tests/methodology/test_methodology_changelog.py::test_v_0_40_0_crp_1_shippability_consumer_propagation tests/methodology/test_build_slice_skill_drift.py::test_build_slice_skill_md_in_repo_byte_equal_installed tests/methodology/test_methodology_changelog.py::test_plugin_yaml_version_matches_version_file_invariant --no-header -q | <6s | `<interp> -m pytest tests/methodology/test_critique_review_prerequisite_audit.py tests/methodology/test_build_slice_skill.py::test_build_slice_prereq_crp_1_refuses_on_absent_mandatory_critique_review tests/methodology/test_build_slice_skill.py::test_build_slice_crp_1_step_7b_preserves_skip_key tests/methodology/test_methodology_changelog.py::test_v_0_40_0_crp_1_entry_present_in_repo tests/methodology/test_methodology_changelog.py::test_v_0_40_0_crp_1_shippability_consumer_propagation tests/methodology/test_build_slice_skill_drift.py::test_build_slice_skill_md_in_repo_byte_equal_installed tests/methodology/test_methodology_changelog.py::test_plugin_yaml_version_matches_version_file_invariant --no-header -q` |
| 25 | slice-025-add-test-file-existence-check-for-non-pytest-rows | PTFCD-1: phantom-test-file-citation discipline codified across 3 surfaces — `agents/critique.md` Dim 9 11th sub-clause (`Phantom test-file citation discipline`, two sub-modes: (a) TF-1-plan-path existence at /build-slice Step 6 + (b) shippability-Command-cell-path existence at /validate-slice Step 5.5; cites slice-023 B4 + slice-024; -D-suffix N=8 stable) + `tools/test_first_audit.py` strict-only `missing-test-path-file` loop gated on `row.status == "PASSING"` (M1 — avoids double-flagging still-PENDING test-first rows) + NEW `tools/shippability_path_audit.py` (post-`pytest` `tests/\S+\.py` predicate per M2, backtick-strip per slice-024 footgun, `::`-split, repo-root resolve; wired as /validate-slice Step 5.5 pre-catalog gate via skills/validate-slice/SKILL.md); PMI-1 structural-invariant supersession `_lists_ten_sub_clauses` -> `_lists_eleven_sub_clauses` (N=5 -> N=6 stable); 4 NEW PTFCD-1 body-bound tests; 4 slice-024 FBCD-1 body-bound tests' end_anchors tightened `### Bonus:` -> `Phantom test-file citation discipline` per slice-018 RPCD-1 sibling-scoping discipline; methodology-changelog v0.39.0 entry present in-repo + installed with bidirectional sha256 byte-equality; canonical phrase `Phantom test-file citation discipline` pinned across N=3 surfaces (critique.md sub-clause title + in-repo changelog + installed changelog) per N-surface schema-pin shape ratcheting N=11 -> N=12 stable; ADR-023 exists at `architecture/decisions/ADR-023-ptfcd-1-phantom-test-file-citation-discipline.md` (reversibility=cheap; supersedes=null); plugin.yaml.version == VERSION == 0.39.0 at slice-025 ship — eleventh atomic bump under PMI-1 v1.1 (retirement-proof N=10 -> N=11 stable); `tools.shippability_path_audit` added to install_audit `_CANONICAL_TOOLS` (INST-1 18 tools) + plugin.yaml tools list (PMI-1, rule PTFCD-1); EPGD-1 self-application N=11 -> N=12 stable (0 of prior _entry_present_in_repo-family functions touched; ADDS-only `_v_0_39_0_ptfcd_1_entry_present_in_repo` + `_adr_023_present_and_reversibility_cheap`); CAD-1 byte-equality on agents/critique.md re-established post-Phase-1 insert (sha256 51041c079dd5d1f8...); recursive-self-application closure empirically confirmed — B1 first-Critic catch was the predicted FBCD-1 sub-mode (a) PTFC-1-vs-PTFCD-1 rule-ID drift + TPHD-1 sub-mode (c) /build-slice pre-flight caught a phantom test-function citation in the slice's own TF-1 plan (the slice codifying the phantom-citation discipline committed and self-caught a phantom citation); SCPD-1 proactive-application N=3 -> N=4 stable (6 pytest-command rows propagated `::test_critique_dim_9_lists_ten_sub_clauses` -> `_lists_eleven` in same /build-slice block before /validate-slice catalog run; 2 historical-narrative occurrences preserved); shippability catalog 24 -> 25 rows | <HOME>/.claude/.venv/Scripts/python.exe -m pytest tests/methodology/test_critique_agent.py::test_critique_dim_9_lists_twelve_sub_clauses tests/methodology/test_critique_agent.py::test_critique_dim_9_phantom_test_file_citation_sub_clause_present tests/methodology/test_critique_agent.py::test_critique_dim_9_phantom_test_file_citation_location_pinned tests/methodology/test_critique_agent.py::test_critique_dim_9_phantom_test_file_citation_names_both_sub_modes tests/methodology/test_critique_agent.py::test_critique_dim_9_phantom_test_file_citation_paragraph_cites_slice_023_024 tests/methodology/test_test_first_audit.py::test_strict_pre_finish_flags_missing_test_path_file tests/methodology/test_test_first_audit.py::test_pending_row_missing_file_emits_exactly_one_violation tests/methodology/test_test_first_audit.py::test_non_strict_does_not_flag_pending_missing_file tests/methodology/test_shippability_path_existence.py::test_flags_phantom_command_cell_test_path tests/methodology/test_shippability_path_existence.py::test_interpreter_and_dash_m_dash_q_tokens_not_flagged tests/methodology/test_methodology_changelog.py::test_v_0_39_0_ptfcd_1_entry_present_in_repo tests/methodology/test_methodology_changelog.py::test_adr_023_present_and_reversibility_cheap tests/methodology/test_methodology_changelog.py::test_plugin_yaml_version_matches_version_file_invariant tests/methodology/test_critique_agent_drift.py::test_in_repo_and_installed_critique_agent_are_content_equal --no-header -q | <8s | `<interp> -m pytest tests/methodology/test_critique_agent.py::test_critique_dim_9_lists_twelve_sub_clauses tests/methodology/test_critique_agent.py::test_critique_dim_9_phantom_test_file_citation_sub_clause_present tests/methodology/test_critique_agent.py::test_critique_dim_9_phantom_test_file_citation_location_pinned tests/methodology/test_critique_agent.py::test_critique_dim_9_phantom_test_file_citation_names_both_sub_modes tests/methodology/test_critique_agent.py::test_critique_dim_9_phantom_test_file_citation_paragraph_cites_slice_023_024 tests/methodology/test_test_first_audit.py::test_strict_pre_finish_flags_missing_test_path_file tests/methodology/test_test_first_audit.py::test_pending_row_missing_file_emits_exactly_one_violation tests/methodology/test_test_first_audit.py::test_non_strict_does_not_flag_pending_missing_file tests/methodology/test_shippability_path_existence.py::test_flags_phantom_command_cell_test_path tests/methodology/test_shippability_path_existence.py::test_interpreter_and_dash_m_dash_q_tokens_not_flagged tests/methodology/test_methodology_changelog.py::test_v_0_39_0_ptfcd_1_entry_present_in_repo tests/methodology/test_methodology_changelog.py::test_adr_023_present_and_reversibility_cheap tests/methodology/test_methodology_changelog.py::test_plugin_yaml_version_matches_version_file_invariant tests/methodology/test_critique_agent_drift.py::test_in_repo_and_installed_critique_agent_are_content_equal --no-header -q` |
| 27 | slice-027-add-pipeline-chain-auto-advance | PCA-1: pipeline-chain auto-advance codified as an audit-enforced gate — NEW `tools/pipeline_chain_audit.py` (modeled on CRP-1/BRANCH-1; dataclass result, exit 0/1/2, `_stdout` UTF-8 first in main(); refuse exit 1 on `malformed-block` / `successor-mismatch` / `auto-advance-mismatch`; usage-error exit 2) verifying all 8 covered skills carry a well-formed `## Pipeline position` block whose successor edges match the canonical loop and `reflect`+`commit-slice` are `auto-advance: false` (terminal — /commit-slice never auto-invoked); normalized `## Pipeline position` H2 section appended to 8 `skills/*/SKILL.md` (7 in-loop + commit-slice terminus) after the existing `## Next step` (left unmodified); gate-halt directive at 5 enumerated user-input gate sites (critique TRI-1 + BLOCKED; build-slice plan-mode + mid-slice smoke; validate-slice FAIL + PARTIAL — the slice-027 /critique-review M-add-1 missed-finding fix); fail-closed model; `tools/pipeline_chain_audit` wired at /build-slice Step 6 pre-finish + added to install_audit `_CANONICAL_TOOLS` (INST-1 19 -> 20) + plugin.yaml tools list (PMI-1, rule PCA-1) + UTF8-STDOUT-1 self-application narrative 17 -> 20; NON-`-D` audit-enforced-gate naming class per ADR-019 (conforms, does not supersede); slice-027 = PCA-1 bootstrap-reference instance #1 (directive absent during own loop — chain ran manually per user directive; discharged by /critique-review EXTEND on slice-027 + audit-against-repo exit 0 at Step 6); methodology-changelog v0.41.0 entry present in-repo + installed bidirectional byte-equal; canonical phrase `Pipeline position` pinned across N=3 surfaces; parametrized `tests/methodology/test_pipeline_position_block_drift.py` byte-equality over all 8 in-repo<->installed skill pairs (AC #5 / first-Critic M1+M2 traceability element); ADR-025 exists (reversibility=cheap; supersedes=null); plugin.yaml.version == VERSION == ~/.claude/ai-sdlc-VERSION == 0.41.0 — thirteenth atomic bump under PMI-1 v1.1; shippability catalog 26 -> 27 rows | <HOME>/.claude/.venv/Scripts/python.exe -m pytest tests/methodology/test_pipeline_chain_audit.py::test_clean_chain_exits_zero tests/methodology/test_pipeline_chain_audit.py::test_missing_block_exits_one tests/methodology/test_pipeline_position_block_drift.py::test_pipeline_position_block_byte_equal_in_repo_vs_installed tests/methodology/test_methodology_changelog.py::test_v_0_41_0_pca_1_entry_present_in_repo tests/methodology/test_methodology_changelog.py::test_v_0_41_0_pca_1_shippability_consumer_propagation tests/methodology/test_methodology_changelog.py::test_adr_025_present_and_reversibility_cheap tests/methodology/test_methodology_changelog.py::test_plugin_yaml_version_matches_version_file_invariant --no-header -q | <8s | `<interp> -m pytest tests/methodology/test_pipeline_chain_audit.py::test_clean_chain_exits_zero tests/methodology/test_pipeline_chain_audit.py::test_missing_block_exits_one tests/methodology/test_pipeline_position_block_drift.py::test_pipeline_position_block_byte_equal_in_repo_vs_installed tests/methodology/test_methodology_changelog.py::test_v_0_41_0_pca_1_entry_present_in_repo tests/methodology/test_methodology_changelog.py::test_v_0_41_0_pca_1_shippability_consumer_propagation tests/methodology/test_methodology_changelog.py::test_adr_025_present_and_reversibility_cheap tests/methodology/test_methodology_changelog.py::test_plugin_yaml_version_matches_version_file_invariant --no-header -q` |
| 28 | slice-028-refactor-utf8-rollup-sentinel-version-agnostic | UTF8-STDOUT-1 v1.1: version-agnostic UTF-8 rollup sentinel (slice-028; ADR-026; rule-ID lineage preserved, NOT a new rule ID — PMI-1 v1.0->v1.1 / ADR-013 precedent). `tests/methodology/test_utf8_stdout_regression.py::test_every_audit_tool_survives_cp1252_stdout_with_u2192_input` no longer asserts a hard-coded `== N` tool count (retires the N=5 slices-023/025/026/027 count-bump maintenance-tax class); asserts bidirectional repo-state-derived coverage-parity `discovered_set == covered_set` — discovered = `tools/*.py` (non-`_`, non-__init__) with a top-level `main` AST node (== `tools_with_main` per `utf8_stdout_audit._find_main_function`), covered = tokens observed from real cp1252 call sites (`_POSITIONAL_SLICE_TOOLS`/`_ROOT_ONLY_TOOLS` parametrize constants [execution-bound] union bespoke `_assert_no_encoding_error` literals [source-presence proxy]). 3-part slice-014 template: (1) derive-real-invariant; (2) `test_rollup_sentinel_is_version_agnostic_shape` AST meta-test (no int-count Compare in sentinel body, no `post-slice-NNN` anchor in sentinel or either set-construction helper) + `test_rollup_sentinel_helpers_exist_and_are_scanned` counter-anchor (set-construction primitives confined to `_discovered_audit_tools`/`_covered_tool_tokens` — closes /critique-review B-add-1 helper-extraction fail-OPEN); (3) `test_rollup_sentinel_fails_with_tool_naming_message_on_parity_break` failure-path regression (slice-014-faithful monkeypatch, parity assertion stays in-body). `_covered_tool_tokens` fails closed on non-`Constant(str)` list/arg shapes (/critique-review M-add-1). Canonical phrase `version-agnostic UTF-8 rollup sentinel` pinned N=3 (sentinel docstring + methodology-changelog v0.42.0 in-repo+installed byte-equal + ADR-026). ADR-026 reversibility=cheap, supersedes=null, with RSAD-1 self-stress section (slice-022 self-violation law fired 2 layers: B2 option-3 + B-add-1 option-4, both dual-Critic-caught pre-build). PMI-1 atomic bump 0.41.0->0.42.0 (VERSION + ~/.claude/ai-sdlc-VERSION + plugin.yaml.version) | `python -m pytest tests/methodology/test_utf8_stdout_regression.py -q` ; `python -m pytest tests/methodology/test_methodology_changelog.py -k v_0_42_0 -q` | <5s | `<interp> -m pytest tests/methodology/test_utf8_stdout_regression.py -q` ; `<interp> -m pytest tests/methodology/test_methodology_changelog.py -k v_0_42_0 -q` |
| 29 | slice-029-make-diagnose-dispatch-sequential | /diagnose Step-5 analysis-pass dispatch is sequential by default (one `Agent` call per message — defeats the R-1 / claude-code #57037 parallel-spawn cascade on the default path); `--parallel` is an explicit position-independent opt-in stripped before TARGET by a 3-arm `case` (unknown `--`-flags warned+ignored, never the path — a flag typo never aborts); Step-5/5.5 dispatch-coupled prose rewritten dispatch-mode-aware incl. a sequential early-exit silent-gap guard. ADR-027 (reversibility=cheap; NO new rule-ID minted — TRI-1 /critique M2 + /critique-review M2 re-scope, option B: rule-ID half follows slice-002 prose-pin precedent, changelog-entry half honors the inclusion heuristic). The ADR-001 subagent contract + canonical contract string (CSP-1) + slice-019 LAYER-EVID-1 N=6 pin + COST-1.1 model routing are byte-unchanged through the Step-5 rewrite (they are the rewrite's regression guard). R-1 transitioned open->mitigating (not retired: independent cwd-mismatch hypothesis + `--parallel` exposure remain). methodology-changelog v0.43.0 `### Changed` entry; PMI-1 atomic bump 0.42.0->0.43.0 (VERSION + ~/.claude/ai-sdlc-VERSION + plugin.yaml.version + ~/.claude/methodology-changelog.md forward-sync). Guarded by 4 slice-029 prose-pins (sequential-default / --parallel-optin / flag-strip-fail-safe / Step-5.5-dispatch-aware+early-exit) + unchanged CSP-1 + LAYER-EVID-1 N=6 + mini-CAD drift + v0.43.0 changelog entry-pin | `<HOME>/.claude/.venv/Scripts/python.exe -m pytest tests/skills/diagnose/test_skill_md_pins.py --no-header -q` | <3s | `<interp> -m pytest tests/skills/diagnose/test_skill_md_pins.py --no-header -q` |
| 30 | slice-030-repair-build-checks-vault-and-harden-shippability | BCI-1 (slice-030A; ADR-028+ADR-029; methodology v0.44.0): build-checks integrity is enforced by a deterministic non-opt-out downstream gate vs git-tracked canonical fixtures (R-4 corruptor `/reflect` Step5b is LLM prose w/ no deterministic source — ADR-029). `tools/build_checks_integrity.py` asserts live build-checks files match `tests/methodology/fixtures/build_checks/canonical_{project,global}_checks.md` on FULL per-rule structural identity `(rule_id,severity,applies_to,trigger_keywords,trigger_anchors,negative_anchors)`+non-empty check (NOT rule-ID-set — meta-M-add-2); absent-global=WARN/exit0, present-nonconformant-incl-empty=HALT/exit1 (meta-M3). BC-1 logic/tuple/schema tests + new all-5-rule literal-constant oracle (incl. first-ever BC-PROJ-3/BC-GLOBAL-2 structural pins — closes v2-B3/meta-M-add-3) assert the TRACKED fixtures (this row therefore reads NO gitignored content — the slice-030A decoupling principle). Wired non-opt-out: `/build-slice` Step-6 + `/reflect` Step-5b fail-loud. User-approved SPLIT of slice-030 (2 BLOCKED dual-Critic loops → meta-Critic non-convergence signal → 030B deferred: shippability rows #5/#8/#12 decoupling + machine-stable command column + meta-M1′ archive-backtest fidelity). N=5 propagation: install_audit `_CANONICAL_TOOLS` 20→21 + `test_utf8_stdout_regression._ROOT_ONLY_TOOLS` (meta-M2 file-location correction) + plugin.yaml rule BCI-1 + PMI-1 atomic 0.43.0→0.44.0 (VERSION + ~/.claude/ai-sdlc-VERSION + plugin.yaml.version + ~/.claude/methodology-changelog.md forward-sync). NON-`-D` audit-enforced-gate naming class (ADR-019; peers BRANCH-1/BC-1/PMI-1/UTF8-STDOUT-1/CRP-1/PCA-1). slice-030A = BCI-1 bootstrap instance #1 (self-run vs repo exit 0 at Step 6). R-4 open→mitigating; R-5 logged (skill-drift-test CRLF/LF fragility, Windows autocrlf). | <HOME>/.claude/.venv/Scripts/python.exe -m pytest tests/methodology/test_build_checks_integrity.py tests/methodology/test_build_checks_audit.py::test_migrated_rules_have_expected_anchors tests/methodology/test_build_checks_audit.py::test_migrated_rules_have_expected_negative_anchors tests/methodology/test_build_checks_audit.py::test_bc_proj_3_and_bc_global_2_have_expected_structural_identity tests/methodology/test_methodology_changelog.py::test_v_0_44_0_bci_1_entry_present_in_repo --no-header -q | <3s | `<interp> -m pytest tests/methodology/test_build_checks_integrity.py tests/methodology/test_build_checks_audit.py::test_migrated_rules_have_expected_anchors tests/methodology/test_build_checks_audit.py::test_migrated_rules_have_expected_negative_anchors tests/methodology/test_build_checks_audit.py::test_bc_proj_3_and_bc_global_2_have_expected_structural_identity tests/methodology/test_methodology_changelog.py::test_v_0_44_0_bci_1_entry_present_in_repo --no-header -q` |

| 31 | slice-031-complete-shippability-decoupling | SCMD-1 (slice-031, split-label 030B; ADR-030+ADR-031; methodology v0.45.0): shippability catalog has a prose-free machine-stable command column + a runtime incidental-decoupling invariant (all-rows mechanical derivation, closed-world AST classify, essential entry-pins recognized-not-flagged → 030C). Incidental archive-backtests decoupled to slice-030A canonical fixtures + ADR-030 verbatim corpus; runner + PTFCD-1 repointed to Machine-cmd | <HOME>/.claude/.venv/Scripts/python.exe -m pytest tests/methodology/test_shippability_decoupling_audit.py tests/methodology/test_shippability_command_column.py --no-header -q | <2s | `<interp> -m pytest tests/methodology/test_shippability_decoupling_audit.py tests/methodology/test_shippability_command_column.py --no-header -q` |
| 32 | slice-032-add-query-design-skill | QD-1 (slice-032; ADR-032; methodology v0.46.0): new out-of-loop read-only `/query-design` codebase-Q&A skill. SKILL.md prose-contract pinned (read-only invariant w/ no escape-hatch loophole + grounding + delegation + 3 error-model clauses + no `## Pipeline position` heading) and in-repo↔installed byte-equal; v0.46.0/QD-1 changelog entry rule-ID-bearing 4-assertion pin (header+QD-1+canonical-phrase+ADR-032, body-scoped, both surfaces); `read-only, delegation-only codebase Q&A` canonical phrase pinned at 2 sites (changelog body + SKILL.md); 4-part PMI-1 atomic bump 0.45.0→0.46.0 + 25-skill INST-1/PMI-1 lockstep | <HOME>/.claude/.venv/Scripts/python.exe -m pytest tests/methodology/test_query_design_skill_drift.py tests/methodology/test_query_design_skill.py tests/methodology/test_methodology_changelog.py::test_v_0_46_0_qd_1_entry_present_in_repo --no-header -q | <3s | `<interp> -m pytest tests/methodology/test_query_design_skill_drift.py tests/methodology/test_query_design_skill.py tests/methodology/test_methodology_changelog.py::test_v_0_46_0_qd_1_entry_present_in_repo --no-header -q` |
| 33 | slice-033-fix-skill-drift-test-crlf-normalization | EOL-DRIFT-1 (slice-033; ADR-033; methodology v0.47.0): `.md` forward-sync drift guards (5 skill-drift modules + CAD-1) compare content modulo line endings via shared `tests/skill_drift_equality.py`; CRLF/LF is not drift, genuine non-EOL divergence still FAILs, guarded surface has no CRLF in working tree. Regression = a future slice reverts the comparator to raw bytes (repro false-FAILs under autocrlf=true) OR normalization masks real drift OR `.gitattributes`/renormalize regresses. | `<HOME>/.claude/.venv/Scripts/python.exe -m pytest tests/methodology/test_skill_drift_normalization.py tests/skills/diagnose/test_diagnose_skill_drift.py tests/methodology/test_critique_agent_drift.py -q` | <2s | `<interp> -m pytest tests/methodology/test_skill_drift_normalization.py tests/skills/diagnose/test_diagnose_skill_drift.py tests/methodology/test_critique_agent_drift.py -q` |
| 34 | slice-034-fix-tf1-audit-field-line-regex | R-7: `tools/test_first_audit.py` reads an annotated `**Test-first**: true  (per TF-1 — ...)` field-line as test-first ENABLED (the idiomatic `/slice` template form), not silent default-off. Regression = a future change re-`$`-anchors the field-line regex (or otherwise reverts) so an annotated field-line silently bypasses the TF-1 gate on a genuinely test-first slice — caught manually only by BC-PROJ-4 before this row existed. | `<HOME>/.claude/.venv/Scripts/python.exe -m pytest tests/methodology/test_tf1_field_line_annotation_regression.py -q` | <1s | `<interp> -m pytest tests/methodology/test_tf1_field_line_annotation_regression.py -q` |
| 35 | slice-035-rename-status-skill-to-pulse | SRCD-1 (slice-035; ADR-035; methodology v0.49.0): the project-pulse skill is named `pulse` not `status` (the `/status` trigger collided with the Claude Code built-in `/status` command); the v0.49.0 SRCD-1 changelog entry with the canonical `skill name MUST NOT collide with a Claude Code built-in command name` phrase + ADR-035 lineage is present bidirectionally (in-repo + installed); the renamed cadence + RR-1 + COST-1 test path-binds resolve against `skills/pulse/SKILL.md`. Regression = a future change re-introduces a `/status`-named skill or backward-compat alias, reverts the rename, drops/dilutes the SRCD-1 entry, or breaks the renamed `test_pulse_*` path-binds. | `<HOME>/.claude/.venv/Scripts/python.exe -m pytest tests/methodology/test_methodology_changelog.py::test_v_0_49_0_srcd_1_entry_present_in_repo tests/methodology/test_pulse_cadence_enforcement.py tests/methodology/test_risk_register_audit.py::test_pulse_skill_references_rr_1 tests/methodology/test_skill_model_dispatch.py --no-header -q` | <3s | `<interp> -m pytest tests/methodology/test_methodology_changelog.py::test_v_0_49_0_srcd_1_entry_present_in_repo tests/methodology/test_pulse_cadence_enforcement.py tests/methodology/test_risk_register_audit.py::test_pulse_skill_references_rr_1 tests/methodology/test_skill_model_dispatch.py --no-header -q` |
| 36 | slice-036-fix-rr1-audit-status-filter | R-9: `tools.risk_register_audit --json` with `--filter-status` / `--filter-band` / `--top` MUST exclude non-matching risks from `out["risks"]` (the list /pulse + /slice consume per their SKILL.md RR-1 usage), not only from `out["view"]`. Pre-fix, JSON mode emitted the full unfiltered `to_dict()` risk list under `out["risks"]`, so retired/mitigating risks leaked into the `--filter-status open` view used for slice-candidate ranking (observed live: real register R-5/R-7 retired surfaced in `--filter-status open` output). Regression = JSON `out["risks"]` again reflects unfiltered risks when any filter/top flag is given. | `<HOME>/.claude/.venv/Scripts/python.exe -m pytest tests/methodology/test_risk_register_audit.py::test_repro_r9_json_filter_status_open_excludes_retired_in_consumed_risks --no-header -q` | <1s | `<interp> -m pytest tests/methodology/test_risk_register_audit.py::test_repro_r9_json_filter_status_open_excludes_retired_in_consumed_risks --no-header -q` |

| 37 | slice-037-extend-ptfcd-1-to-test-function-level | PTFFD-1: a PASSING TF-1 plan row or shippability `Machine-cmd` pytest `::`-selector whose cited test FILE exists but whose cited test FUNCTION is absent is a `missing-test-function` violation (`tools/test_first_audit.py --strict-pre-finish` + `tools/shippability_path_audit.py`, shared `tools/_pyfn.py` tri-state helper); function-column-first then `::`-tail fallback (M3); prose value degrades to FILE-level-only (B2, zero false positives on the live 36-row/228-token catalog); unparseable cited file is skip-with-note not silent (ADR-037 M2); PTFFD-1 refines PTFCD-1 in place, supersedes nothing (ADR-038, NOT `PTFCD-1 v1.1`); function-level Critic-stack blind spot N=3 (slice-025 AC3 + slice-026 AC5 + slice-027 B1) | `<HOME>/.claude/.venv/Scripts/python.exe -m pytest tests/methodology/test_ptffd1_test_first_audit.py::test_phantom_function_in_existing_file_is_violation tests/methodology/test_ptffd1_test_first_audit.py::test_unparseable_file_emits_skip_note_in_human_output tests/methodology/test_ptffd1_test_first_audit.py::test_path_column_selector_used_when_function_column_empty tests/methodology/test_ptffd1_shippability_path_audit.py::test_phantom_function_selector_is_violation tests/methodology/test_ptffd1_shippability_path_audit.py::test_legacy_file_level_phantom_keeps_missing_test_file_kind tests/methodology/test_ptffd1_no_false_positive.py::test_real_shippability_and_full_tf1_corpus_clean_under_func_level tests/methodology/test_critique_agent.py::test_critique_dim_9_phantom_citation_function_level_layer_present tests/methodology/test_critique_agent.py::test_critique_dim_9_phantom_citation_names_ptffd_1_rule_id tests/methodology/test_methodology_changelog.py::test_v_0_50_0_ptffd_1_entry_present_in_repo tests/methodology/test_methodology_changelog.py::test_v_0_50_0_ptffd_1_shippability_consumer_propagation --no-header -q` | <3s | `<interp> -m pytest tests/methodology/test_ptffd1_test_first_audit.py::test_phantom_function_in_existing_file_is_violation tests/methodology/test_ptffd1_test_first_audit.py::test_unparseable_file_emits_skip_note_in_human_output tests/methodology/test_ptffd1_test_first_audit.py::test_path_column_selector_used_when_function_column_empty tests/methodology/test_ptffd1_shippability_path_audit.py::test_phantom_function_selector_is_violation tests/methodology/test_ptffd1_shippability_path_audit.py::test_legacy_file_level_phantom_keeps_missing_test_file_kind tests/methodology/test_ptffd1_no_false_positive.py::test_real_shippability_and_full_tf1_corpus_clean_under_func_level tests/methodology/test_critique_agent.py::test_critique_dim_9_phantom_citation_function_level_layer_present tests/methodology/test_critique_agent.py::test_critique_dim_9_phantom_citation_names_ptffd_1_rule_id tests/methodology/test_methodology_changelog.py::test_v_0_50_0_ptffd_1_entry_present_in_repo tests/methodology/test_methodology_changelog.py::test_v_0_50_0_ptffd_1_shippability_consumer_propagation --no-header -q` |
| 38 | slice-038-pin-shippability-runner-segment-contract | SRSC-1 (slice-038; ADR-039; methodology v0.51.0): the /validate-slice Step-5.5 catalog runner is pinned behind invoked `tools/shippability_runner.py` which REUSES SCMD-1 `_segments()` (per-`;`-segment backtick+ws strip, single-sourced from `tools.shippability_decoupling_audit`) — Step 5.5 invokes it, a hand-rolled loop is forbidden. Closes R-8: a naive outer-fence-only strip mangles segment 2 of the lone multi-segment row #28 into a leading-backtick `argv[0]` → WinError 2 (recurred N=2 slice-032/033 despite prior SKILL.md prose). The load-bearing test exercises the naive-outer-strip-vs-`_segments()` contrast on REAL row #28; the v0.51.0 SRSC-1 entry carries the `do NOT hand-roll the execution loop` + `reuses SCMD-1 _segments()` canonical phrases + ADR-039 + supersedes-nothing, bidirectionally (in-repo + installed). Regression = a future change re-derives the Step-5.5 split-strip ad-hoc, drops the runner invocation from SKILL.md, lets the runner re-implement `_segments`, or dilutes/loses the SRSC-1 changelog entry. | `<HOME>/.claude/.venv/Scripts/python.exe -m pytest tests/methodology/test_shippability_runner_segment_contract.py::test_naive_outer_strip_runner_is_rejected tests/methodology/test_shippability_runner_segment_contract.py::test_real_row_28_both_segments_interpreter_anchored tests/methodology/test_shippability_runner_segment_contract.py::test_multi_segment_row_splits_and_strips_per_segment tests/methodology/test_methodology_changelog.py::test_v_0_51_0_srsc_1_entry_present_in_repo tests/methodology/test_methodology_changelog.py::test_v_0_51_0_srsc_1_shippability_consumer_propagation tests/methodology/test_utf8_stdout_regression.py::test_shippability_runner_survives_cp1252_with_u2192 --no-header -q` | <5s | `<interp> -m pytest tests/methodology/test_shippability_runner_segment_contract.py::test_naive_outer_strip_runner_is_rejected tests/methodology/test_shippability_runner_segment_contract.py::test_real_row_28_both_segments_interpreter_anchored tests/methodology/test_shippability_runner_segment_contract.py::test_multi_segment_row_splits_and_strips_per_segment tests/methodology/test_methodology_changelog.py::test_v_0_51_0_srsc_1_entry_present_in_repo tests/methodology/test_methodology_changelog.py::test_v_0_51_0_srsc_1_shippability_consumer_propagation tests/methodology/test_utf8_stdout_regression.py::test_shippability_runner_survives_cp1252_with_u2192 --no-header -q` |
| 39 | slice-039-apply-critic-calibrate-proposals-to-critique-agent | APED-1 + MEPD-1 (slice-039; ADR-040 + ADR-041; methodology v0.52.0): the two user-ACCEPTED 2026-05-17 /critic-calibrate proposals are applied to agents/critique.md — APED-1 = Dim 9 sub-clause #12 (Bash-execute a changed audit parse-rule against an adversarial battery; Blocker on silent-disable/default-off/false-FAIL/over-match; executed not reasoned; N=3-MISS slice-030A/031/033 + N=1-CATCH slice-034), MEPD-1 = Dim 7 checklist sub-bullet (methodology-surface RULE-ID + entry-pin obligation; (a) rule-path OR (b) verified-against-artifact why-none, never a Builder-asserted precedent). Both NEW minted -D rules, refine/supersede nothing (ADR-040: -D = behavioral class not Dim-9 membership; MEPD-1 first non-Dim-9 -D). _lists_eleven->_lists_twelve structural-invariant supersession (N=7); content-pins are behavioral not CAD-1-byte-equality-tautology (slice-037 M-add-1). Regression = a future edit silently weakens APED-1 to reason-only / drops the MEPD-1 (b)-guard / dilutes-or-loses the v0.52.0 changelog entry / breaks CAD-1 in-repo<->installed parity. | <HOME>/.claude/.venv/Scripts/python.exe -m pytest tests/methodology/test_critique_agent.py::test_critique_dim_9_lists_twelve_sub_clauses tests/methodology/test_critique_agent.py::test_critique_dim_9_audit_parse_rule_empirical_execution_sub_clause_present tests/methodology/test_critique_agent.py::test_critique_dim_9_audit_parse_rule_empirical_execution_location_pinned tests/methodology/test_critique_agent.py::test_critique_dim_9_audit_parse_rule_empirical_execution_names_aped_1_rule_id tests/methodology/test_critique_agent.py::test_critique_dim_9_audit_parse_rule_empirical_execution_cites_evidence_anchors tests/methodology/test_critique_agent.py::test_critique_dim_9_audit_parse_rule_empirical_execution_pins_behavioral_obligation tests/methodology/test_critique_agent.py::test_critique_dim_7_methodology_surface_entry_pin_sub_bullet_present tests/methodology/test_critique_agent.py::test_critique_dim_7_methodology_surface_entry_pin_location_pinned tests/methodology/test_critique_agent.py::test_critique_dim_7_methodology_surface_entry_pin_names_mepd_1_rule_id tests/methodology/test_critique_agent.py::test_critique_dim_7_methodology_surface_entry_pin_names_both_clauses tests/methodology/test_critique_agent_drift.py::test_in_repo_and_installed_critique_agent_are_content_equal tests/methodology/test_methodology_changelog.py::test_v_0_52_0_aped_1_entry_present_in_repo tests/methodology/test_methodology_changelog.py::test_v_0_52_0_mepd_1_entry_present_in_repo tests/methodology/test_methodology_changelog.py::test_v_0_52_0_critique_proposals_shippability_consumer_propagation --no-header -q | <3s | `<interp> -m pytest tests/methodology/test_critique_agent.py::test_critique_dim_9_lists_twelve_sub_clauses tests/methodology/test_critique_agent.py::test_critique_dim_9_audit_parse_rule_empirical_execution_sub_clause_present tests/methodology/test_critique_agent.py::test_critique_dim_9_audit_parse_rule_empirical_execution_location_pinned tests/methodology/test_critique_agent.py::test_critique_dim_9_audit_parse_rule_empirical_execution_names_aped_1_rule_id tests/methodology/test_critique_agent.py::test_critique_dim_9_audit_parse_rule_empirical_execution_cites_evidence_anchors tests/methodology/test_critique_agent.py::test_critique_dim_9_audit_parse_rule_empirical_execution_pins_behavioral_obligation tests/methodology/test_critique_agent.py::test_critique_dim_7_methodology_surface_entry_pin_sub_bullet_present tests/methodology/test_critique_agent.py::test_critique_dim_7_methodology_surface_entry_pin_location_pinned tests/methodology/test_critique_agent.py::test_critique_dim_7_methodology_surface_entry_pin_names_mepd_1_rule_id tests/methodology/test_critique_agent.py::test_critique_dim_7_methodology_surface_entry_pin_names_both_clauses tests/methodology/test_critique_agent_drift.py::test_in_repo_and_installed_critique_agent_are_content_equal tests/methodology/test_methodology_changelog.py::test_v_0_52_0_aped_1_entry_present_in_repo tests/methodology/test_methodology_changelog.py::test_v_0_52_0_mepd_1_entry_present_in_repo tests/methodology/test_methodology_changelog.py::test_v_0_52_0_critique_proposals_shippability_consumer_propagation --no-header -q` |
| 40 | slice-040-realign-validate-slice-step-5-5-prose-pin | SRSC-1/R-10 (slice-040): the slice-031 SCMD-1 B2-v1 mini-CAD prose-pin `test_step4_5_5_consumes_machine_stable_command` is realigned to the SRSC-1 runner-invocation wording slice-038 actually shipped — asserts the two SRSC-1-exclusive anchors (`$PY -m tools.shippability_runner architecture/shippability.md` + `canonical pinned runner`) present in skills/validate-slice/SKILL.md, keeps the SCMD-1 pre-gate + PTFCD-1 Machine-cmd-cell sibling assertions, docstring carries the slice-031→SRSC-1 supersession lineage. Non-tautological: FAILs if Step 5.5 regresses to the pre-SRSC-1 hand-rolled-loop prose. Regression = the realigned pin reverts to the dead `Run each entry's **Machine-cmd** column` anchor, drops a SRSC-1 anchor, or the pin is weakened to a tautology. | `<HOME>/.claude/.venv/Scripts/python.exe -m pytest tests/methodology/test_validate_slice_skill.py::test_step4_5_5_consumes_machine_stable_command --no-header -q` | <1s | `<interp> -m pytest tests/methodology/test_validate_slice_skill.py::test_step4_5_5_consumes_machine_stable_command --no-header -q` |
| 41 | slice-041-reframe-installed-pin-forward-sync-invariant | MCFS-1 (slice-041, split-lineage "030C"; ADR-042 + ADR-043; methodology v0.53.0): the in-repo↔installed `methodology-changelog.md` forward-sync invariant is re-homed from ~33 per-version `test_v_0_NN_0_*` essential reads onto ONE non-catalog whole-file EOL-agnostic gate `tools/methodology_changelog_forward_sync.py` (BCI-1-analogue; ungated `/build-slice` Step 6 + NEW non-Step-5b `/reflect` Step 5b-fs), and `shippability_decoupling_audit`'s essential class becomes a closed-world non-empty `_REGISTERED_INSTALLED_READERS` allowlist registering exactly the slice-019 LAYER-EVID-1 cross-module pin (the read **registered** not **absent**; unregistered essential ⇒ exit 1) — retiring R-4. The row cites the in-repo-only-body v0.53.0 entry-pin + the consumer-propagation pin ONLY: the MCFS-1 regression suite reads installed and is intentionally NOT cited (m-add-2 — a row for it would self-violate `essential-unregistered`). Regression = the decouple is reverted (a `test_v_0_NN_0_*` re-reads installed → essential-unregistered), the allowlist is emptied/over-grown without an ADR, MCFS-1 becomes catalog-cited (M-add-1 relocation), the v0.53.0 MCFS-1 entry is lost/diluted, or the entry-pin regains an installed read (AC3 self-violation). | `<HOME>/.claude/.venv/Scripts/python.exe -m pytest tests/methodology/test_methodology_changelog.py::test_v_0_53_0_mcfs_1_entry_present_in_repo tests/methodology/test_methodology_changelog.py::test_v_0_53_0_mcfs_1_shippability_consumer_propagation --no-header -q` | <2s | `<interp> -m pytest tests/methodology/test_methodology_changelog.py::test_v_0_53_0_mcfs_1_entry_present_in_repo tests/methodology/test_methodology_changelog.py::test_v_0_53_0_mcfs_1_shippability_consumer_propagation --no-header -q` |
| 42 | slice-042-realign-entry-present-pin-names-to-decoupled-shape | ADR-044/ADR-045 (slice-042; no methodology version bump — conformance-fix class): the `_entry_present_in_repo_and_installed` / `_entry_names_*_in_repo_and_installed` family was renamed to drop the over-claiming `_and_installed` suffix (37 active defs + 69 shippability `::`-selectors + 6 prose refs realigned in lockstep; bodies unchanged) so the pin names assert exactly what their bodies check (in-repo presence only, post slice-041 MCFS-1). The FROZEN carve-out (changelog `**Validation**:` lines / every `ADR-*.md` except 044/045 / fixtures / archive / historical vault prose) is a leak-proof predicate proven byte-identical by a regex-independent sha256 snapshot. Regression = any renamed pin reverts to the `_and_installed` shape (a cited `::`-selector fails to collect → the catalog row FAILs), the no-`_sub_` `entry_names_three_modes` edge variant is mis-anchored, or the BC-PROJ-5 promoted-discipline structural pin drifts from its canonical-fixture oracle. | `<HOME>/.claude/.venv/Scripts/python.exe -m pytest tests/methodology/test_methodology_changelog.py::test_v_0_22_0_cad_1_entry_present_in_repo tests/methodology/test_methodology_changelog.py::test_v_0_53_0_mcfs_1_entry_present_in_repo tests/methodology/test_methodology_changelog.py::test_v_0_36_0_entry_names_three_modes_in_repo tests/methodology/test_methodology_changelog.py::test_v_0_31_0_rpcd_1_entry_names_three_sub_modes_in_repo tests/methodology/test_build_checks_audit.py::test_bc_proj_5_has_expected_structural_identity --no-header -q` | <2s | `<interp> -m pytest tests/methodology/test_methodology_changelog.py::test_v_0_22_0_cad_1_entry_present_in_repo tests/methodology/test_methodology_changelog.py::test_v_0_53_0_mcfs_1_entry_present_in_repo tests/methodology/test_methodology_changelog.py::test_v_0_36_0_entry_names_three_modes_in_repo tests/methodology/test_methodology_changelog.py::test_v_0_31_0_rpcd_1_entry_names_three_sub_modes_in_repo tests/methodology/test_build_checks_audit.py::test_bc_proj_5_has_expected_structural_identity --no-header -q` |
| 43 | slice-043-codify-split-slice-folder-naming-convention | ADR-046/R-6 (slice-043; no methodology version bump — conformance/convention-clarification class, why-none verified against the real META-1 `## v`-split assertion + PMI-1 already-listed-tool invariant): the implicit "split-slice follow-up folders are numeric `slice-NNN-`; the `NNNx` letter is a prose lineage label only" convention is codified in `CLAUDE.md` Branch-per-slice + locked by ADR-046. `tools/branch_workflow_audit.py` keeps `_SLICE_FOLDER_RE` strict (numeric-only accept UNCHANGED — behavior-preserving) and adds a diagnostic-only `_SPLIT_SLICE_FOLDER_RE = ^slice-(\d{3})([A-Z]+)-(.+)$` (uppercase-only per /critique m1) consulted ONLY after a strict miss to emit a convention-naming actionable `usage-error` (kind + exit-code 2 unchanged) for a letter-suffixed split-slice folder; R-6 `open → retired`. Regression = the strict accept regex is widened/loosened, the diagnostic over-matches lowercase non-convention names, the message loses the convention/`next free numeric slice number`/ADR-046/BRANCH-1 anchors, or the CLAUDE.md `split-slice follow-up` sub-clause is dropped. | `<HOME>/.claude/.venv/Scripts/python.exe -m pytest tests/methodology/test_branch_workflow_split_slice_folder_convention.py tests/methodology/test_branch_workflow_audit.py tests/methodology/test_root_claude_md_branch_per_slice_rule.py tests/methodology/test_build_checks_audit.py::test_bc_proj_6_has_expected_structural_identity --no-header -q` | <5s | `<interp> -m pytest tests/methodology/test_branch_workflow_split_slice_folder_convention.py tests/methodology/test_branch_workflow_audit.py tests/methodology/test_root_claude_md_branch_per_slice_rule.py tests/methodology/test_build_checks_audit.py::test_bc_proj_6_has_expected_structural_identity --no-header -q` |
| 44 | slice-044-add-state-transition-stale-pin-audit | STP-1 (slice-044; ADR-047; methodology v0.54.0; new minted NON-`-D` audit-gate, refines/supersedes nothing): a state-transition stale-pin gate `tools/state_transition_pin_audit.py` — Sub-form A (SKILL.md-prose-repoint: a positive-membership prose-pin whose folded literal is absent from the full target SKILL.md; `not in` / `Or`-disjunction / mixed-`And` excluded, positive-only `And` checked per-operand) + Sub-form B (risk-status-stale: an anchored `r<num>` + `_<verb>_<status>` test-fn-name token whose claimed status differs from the live `architecture/risk-register.md` Status via the object-identity-reused `_parse_risks`). Per-file AST SyntaxError yields skip-with-note not exit-2 (ADR-037/PTFFD-1); git-independent (slice-044 plan-mode deviation — `architecture/` gitignored). Regression = STP-1 stops detecting either sub-form, the `Or`-disjunction / `not in` false-positive class reopens (live repo exit non-zero), the anchored-r / explicit-status detector regresses (M4/M5), a per-file SyntaxError starts exiting 2 (B4/B5 self-HALT), or the v0.54.0 STP-1 entry / row #44 is lost. | `<HOME>/.claude/.venv/Scripts/python.exe -m pytest tests/methodology/test_stp1_r10_skill_prose_repoint_regression.py tests/methodology/test_state_transition_pin_audit.py tests/methodology/test_methodology_changelog.py::test_v_0_54_0_stp_1_entry_present_in_repo tests/methodology/test_methodology_changelog.py::test_v_0_54_0_stp_1_shippability_consumer_propagation --no-header -q` | <3s | `<interp> -m pytest tests/methodology/test_stp1_r10_skill_prose_repoint_regression.py tests/methodology/test_state_transition_pin_audit.py tests/methodology/test_methodology_changelog.py::test_v_0_54_0_stp_1_entry_present_in_repo tests/methodology/test_methodology_changelog.py::test_v_0_54_0_stp_1_shippability_consumer_propagation --no-header -q` |
| 45 | slice-045-fix-install-pypi-package-name-and-stale-prose | INSTALL.md (the INST-1 install recipe) factual correctness — the graphify pip-install target is the PyPI distribution `graphifyy` not bare `graphify` (the importable module name stays `graphify`); INSTALL.md carries no stale `v0.20.0` literal while VERSION says otherwise; every "N executable methodology tools" claim equals the count of `- path: tools/` entries plugin.yaml enumerates; AND README.md + the tutorial HTML graphify-install sentences name the `graphifyy` PyPI package (not the vague bare "else PyPI"). Regression = INSTALL.md reverts to the wrong PyPI package name, the methodology-version prose re-staleizes to `v0.20.0`, the tool-count claim drifts from plugin.yaml (re-drift guard), or the README/tutorial-HTML descriptive siblings lose the precise `graphifyy` package name. | `<HOME>/.claude/.venv/Scripts/python.exe -m pytest tests/methodology/test_install_md_correctness.py --no-header -q` | <1s | `<interp> -m pytest tests/methodology/test_install_md_correctness.py --no-header -q` |
| 46 | slice-046-add-conditional-repro-auto-advance | BFRD-1 terminal-action reclassification (slice-046; ADR-048 partial-supersedes ADR-018 STOP-route decision; methodology v0.55.0; refines BFRD-1 terminal action, mints no new rule): skills/slice/SKILL.md Step 3c is the conditional confirm-then-auto-invoke /repro edge — the canonical phrase conditional confirm-then-auto-invoke is present in the Step 3c section AND the old unconditional re-invoke-slice hand-off literal is absent; the three pre-existing BFRD-1 positive-pin literals plus the Step 3b/3c/4 anchors are preserved (STP-1 Sub-form A clean by construction, zero stale-pin realignment); the v0.55.0 changelog entry records the ADR-048 partial-supersession lineage and the no-new-rule treatment. Regression = Step 3c silently reverts to the PCA-1-violating unconditional STOP hand-off, the reclassified flow loses the canonical phrase, or the v0.55.0 entry / ADR-048 lineage is lost. | `<HOME>/.claude/.venv/Scripts/python.exe -m pytest tests/methodology/test_slice_skill.py::test_slice_skill_md_bfrd_1_conditional_confirm_then_auto_invoke_reclassified tests/methodology/test_methodology_changelog.py::test_v_0_55_0_bfrd_1_reclassification_entry_present_in_repo --no-header -q` | <1s | `<interp> -m pytest tests/methodology/test_slice_skill.py::test_slice_skill_md_bfrd_1_conditional_confirm_then_auto_invoke_reclassified tests/methodology/test_methodology_changelog.py::test_v_0_55_0_bfrd_1_reclassification_entry_present_in_repo --no-header -q` |
| 47 | slice-047-add-two-scope-install | Slice-047 closed decision-only / withdrawn-feature: the two-scope-install feature was abandoned at TRI-1 as platform-infeasible (Claude Code skills resolve personal>project while subagents resolve project>user — the inverse, yielding a split-resolution state INST-1 false-greens; both Builder-WebFetch-confirmed). ADR-049 was rewritten in place into a global-only decision record retiring the slice-045/046 scope-blindness deferral; no INSTALL.md / VERSION / methodology-changelog change. The durable + most regression-prone deliverable is the BC-GLOBAL-3 build-check promotion (load-bearing external-platform behavior must be official-doc-verified before design lock); its literal-constant structural-identity pin guards the R-4 silent-truncation class together with BCI-1. Regression = a future /reflect or fixture edit truncates or mis-authors BC-GLOBAL-3 (severity/applies-to/keywords/anchors/negative-anchors drift). | `<HOME>/.claude/.venv/Scripts/python.exe -m pytest tests/methodology/test_build_checks_audit.py::test_bc_global_3_has_expected_structural_identity --no-header -q` | <1s | `<interp> -m pytest tests/methodology/test_build_checks_audit.py::test_bc_global_3_has_expected_structural_identity --no-header -q` |
| 48 | slice-048-codify-structured-options-ask-rule | SOAD-1 (slice-048; ADR-050 generalizes ADR-048; methodology v0.56.0; mints a new rule, supersedes nothing): the canonical structured-options-ask sentence is present verbatim inside all 4 opener CLAUDE.md template fenced blocks (/triage Step 5b Fresh+Append, /adopt Step 10 Fresh+Append) AND this repo's own CLAUDE.md `## Ask discipline` section; the M-add-1 self-consistency reword holds (no bare free-text hard-rule ASK literal remains on any SOAD-1 surface, the reworded hard-rule directs a structured-options ask); the v0.56.0 changelog entry records the SOAD-1 / ADR-050-generalizes-ADR-048 / new-rule-supersedes-nothing lineage. Regression = the SOAD-1 sentence dropped or reworded out of any of the 5 surfaces, the bare free-text hard-rule ASK reintroduced (slice-022 self-violation), or the v0.56.0 entry / ADR-050 lineage lost. | `<HOME>/.claude/.venv/Scripts/python.exe -m pytest tests/methodology/test_soad1_structured_options_ask_rule.py tests/methodology/test_methodology_changelog.py::test_v_0_56_0_soad_1_entry_present_in_repo tests/methodology/test_methodology_changelog.py::test_v_0_56_0_soad_1_shippability_consumer_propagation --no-header -q` | <1s | `<interp> -m pytest tests/methodology/test_soad1_structured_options_ask_rule.py tests/methodology/test_methodology_changelog.py::test_v_0_56_0_soad_1_entry_present_in_repo tests/methodology/test_methodology_changelog.py::test_v_0_56_0_soad_1_shippability_consumer_propagation --no-header -q` |
| 49 | slice-049-add-triage-adopt-skill-drift-guards | OSDG-1 (slice-049; ADR-051 extends slice-007 CAD-1 / slice-010 mini-CAD / slice-033 EOL-DRIFT-1; mints a new rule, supersedes nothing): in-repo skills/triage/SKILL.md + skills/adopt/SKILL.md are content-equal modulo line endings (EOL-DRIFT-1 / ADR-033 -- CRLF vs LF is not drift) to their installed copies via the shared assert_md_forward_synced comparator (reused verbatim, zero new compare logic); the v0.57.0 changelog entry records the OSDG-1 / ADR-051-extends-CAD-1-mini-CAD-EOL-DRIFT-1 / new-rule-supersedes-nothing lineage; the CLAUDE.md Self-hosting-discipline Mini-CAD bullet names triage+adopt under OSDG-1 at EOL-DRIFT-1 precision; exactly ONE catalog row covers BOTH guards (one-row-per-slice). Regression = a forgotten forward-sync of either opener SKILL.md (Claude reads stale /triage or /adopt prose at runtime), the v0.57.0 entry / ADR-051 lineage lost, or the CLAUDE.md Mini-CAD bullet regressed below the content-equal-modulo-line-endings / EOL-DRIFT-1 / ADR-033 wording. | `<HOME>/.claude/.venv/Scripts/python.exe -m pytest tests/methodology/test_triage_skill_drift.py tests/methodology/test_adopt_skill_drift.py tests/methodology/test_methodology_changelog.py::test_v_0_57_0_osdg_1_entry_present_in_repo tests/methodology/test_methodology_changelog.py::test_v_0_57_0_osdg_1_shippability_consumer_propagation tests/methodology/test_root_claude_md_cad1_eol_agnostic.py --no-header -q` | <1s | `<interp> -m pytest tests/methodology/test_triage_skill_drift.py tests/methodology/test_adopt_skill_drift.py tests/methodology/test_methodology_changelog.py::test_v_0_57_0_osdg_1_entry_present_in_repo tests/methodology/test_methodology_changelog.py::test_v_0_57_0_osdg_1_shippability_consumer_propagation tests/methodology/test_root_claude_md_cad1_eol_agnostic.py --no-header -q` |
| 50 | slice-050-add-ai-sdlc-version-forward-sync-gate | AVFS-1 (slice-050; ADR-052 extends slice-041 MCFS-1 forward-sync-via-deterministic-downstream-gate lineage; mints a new rule, supersedes nothing): a standalone verbatim MCFS-1 clone tools/ai_sdlc_version_forward_sync.py asserts in-repo VERSION is content-equal modulo line endings (CRLF to LF only -- EOL-DRIFT-1 / ADR-033; NOT trailing-whitespace tolerant, CSP-1 parity to skill_drift_equality._normalized_sha256) to the installed ~/.claude/ai-sdlc-VERSION -- the 4th PMI-1 leg PMI-1 never reads and MCFS-1 does not cover (drifted N=2: slice-035 DEVIATION-2, slice-048 to 049). MCFS-1-identical semantics: in-repo missing exit 2 usage; installed absent WARN exit 0; present-but-divergent (incl. empty-present and whitespace-only-present) HALT exit 1 attributed NOT a slice regression. Wired ungated at /build-slice Step 6 + a dedicated /reflect Step 5b-avfs (NOT folded into the rule-promotion-gated Step 5b). Non-catalog by construction on the slice-029/030A environment-mutable-state ground (the regression suite reads the untracked ~/.claude/ai-sdlc-VERSION) -- NOT essential-unregistered, which keys on the changelog path only; only these two in-repo-only pins carry the row. Regression = a forgotten PMI-1 forward-sync of the installed ai-sdlc-VERSION leg (every installed-copy reader sees a stale version), the v0.58.0 entry / ADR-052 standalone-clone lineage lost, the comparator regressed to trailing-whitespace tolerant, or the 2-point ungated wiring dropped. | `<HOME>/.claude/.venv/Scripts/python.exe -m pytest tests/methodology/test_methodology_changelog.py::test_v_0_58_0_avfs_1_entry_present_in_repo tests/methodology/test_methodology_changelog.py::test_v_0_58_0_avfs_1_shippability_consumer_propagation --no-header -q` | <2s | `<interp> -m pytest tests/methodology/test_methodology_changelog.py::test_v_0_58_0_avfs_1_entry_present_in_repo tests/methodology/test_methodology_changelog.py::test_v_0_58_0_avfs_1_shippability_consumer_propagation --no-header -q` |
| 51 | slice-051-extend-osdg-1-to-reflect-skill | OSDG-1 member-addition (slice-051; ADR-053 extends slice-049/ADR-051 OSDG-1 and the slice-007 CAD-1 / slice-010 mini-CAD / slice-033 EOL-DRIFT-1 lineage; mints NO new rule, supersedes nothing): in-repo skills/reflect/SKILL.md is content-equal modulo line endings (EOL-DRIFT-1 / ADR-033 -- CRLF vs LF is not drift) to the installed ~/.claude/skills/reflect/SKILL.md via the shared assert_md_forward_synced comparator (reused verbatim, zero new compare logic), guarding the in-loop /reflect skill that owns the slice-050 AVFS-1 Step 5b-avfs post-write block (a stale installed copy could silently skip the AVFS-1 /reflect arm). The v0.59.0 changelog entry records the OSDG-1 reflect-membership / ADR-053-extends-ADR-051 / no-new-RULE-ID-supersedes-nothing lineage AND the Opener-Skill-name-is-now-a-historical-label-not-a-scope-boundary decoupling; the CLAUDE.md Self-hosting-discipline Mini-CAD/OSDG-1 bullet names reflect at EOL-DRIFT-1 precision; exactly ONE catalog row covers the guard (one-row-per-slice). Regression = a forgotten forward-sync of skills/reflect/SKILL.md (Claude reads stale /reflect prose at runtime, AVFS-1 /reflect arm may silently skip on a stale install), the v0.59.0 entry / ADR-053 lineage lost, or the CLAUDE.md Mini-CAD/OSDG-1 bullet regressed below the content-equal-modulo-line-endings / EOL-DRIFT-1 / ADR-033 wording. | `<HOME>/.claude/.venv/Scripts/python.exe -m pytest tests/methodology/test_reflect_skill_drift.py tests/methodology/test_methodology_changelog.py::test_v_0_59_0_osdg_1_reflect_member_entry_present_in_repo tests/methodology/test_methodology_changelog.py::test_v_0_59_0_osdg_1_reflect_member_shippability_consumer_propagation tests/methodology/test_root_claude_md_cad1_eol_agnostic.py --no-header -q` | <1s | `<interp> -m pytest tests/methodology/test_reflect_skill_drift.py tests/methodology/test_methodology_changelog.py::test_v_0_59_0_osdg_1_reflect_member_entry_present_in_repo tests/methodology/test_methodology_changelog.py::test_v_0_59_0_osdg_1_reflect_member_shippability_consumer_propagation tests/methodology/test_root_claude_md_cad1_eol_agnostic.py --no-header -q` |
| 52 | slice-052-add-slice-candidates-obo-mode | /slice-candidates --obo operational-parity + ADR-054 mechanical scoped-peek boundary (slice-052; ADR-054; mints no new RULE-ID, supersedes nothing): build_backlog.py --obo-write bakes a decisions map into a NEW diagnosis.annotated.html whose parse_html_state annotations equal collect() semantics (unreviewed findings ABSENT, not empty entries -- M1) AND yields a backlog.md byte-identical to the manual browser round-trip golden (B2 operational parity, NOT browser-output byte-equality); ensure_ascii=False so non-ASCII finding text matches the browser JSON.stringify control (B1); the step-5 script-block insertion is match-span string slicing NOT re.sub, so a literal backslash and a close-script sequence in notes round-trip byte-exact (M-add-2); the original diagnosis.html SHA-256 is asserted unchanged across the call (Hard rule no.3); --obo-peek refuses any path outside the current finding evidence allow-set incl ../ traversal, absolute, ./-prefixed, and unknown-finding (ADR-054 enforcement is mechanical via Path.resolve() containment, NOT honour-system); a duplicate diagnose-data block is refused not silently first-bound (m1, Major); the non-obo default path output is unchanged (AC1). Regression = silent annotated-copy byte-divergence (wrong ensure_ascii or a re.sub-corrupted payload), the scoped-peek allow-set degrading to honour-system / widening, a Hard-rule-no.3 mutation of the original diagnosis.html, or the default backlog.md path altered. | `<HOME>/.claude/.venv/Scripts/python.exe -m pytest tests/methodology/test_slice_candidates_obo.py --no-header -q` | <3s | `<interp> -m pytest tests/methodology/test_slice_candidates_obo.py --no-header -q` |
| 53 | slice-053-wire-backlog-md-into-slice-and-reflect | BCR-1 backlog-consume-and-round-trip discipline (slice-053; ADR-055 extends the BC-PROJ-10 / Inclusion-heuristic lineage; mints a new rule, supersedes nothing): the `/diagnose → /slice → /reflect` loop is closed deterministically on the structural axis via two SKILL.md prose contracts pinned by `tests/methodology/test_bcr_1_backlog_round_trip.py` (8 anchor-presence + position-pin tests on `skills/slice/SKILL.md` + `skills/reflect/SKILL.md`, SOAD-1 multi-surface + BFRD-1 section-scoped precedents). Consume side: when `diagnose-out/backlog.md` exists, `/slice` MUST consult it as source #7 in the 'Gather candidates' block (M1-locked canonical phrase: 'MUST consult diagnose-out/backlog.md as a mandatory candidate source when it exists'). Round-trip side: when mission-brief.md OR reflection.md carries an explicit `**Closes:** SC-\d{3}` sentinel header (M4 sentinel-anchored trigger, NOT bare SC-NNN grep — mentioned-vs-closes disambiguation), `/reflect` MUST append `**Addressed:** slice-NNN-<name> on YYYY-MM-DD` at the END of each closed candidate block AFTER `**Evidence:**` (or after the last top-level metadata bullet if no `**Evidence:**` per M-add-1 graceful-degradation). Both SKILL.md surfaces are OSDG-1-guarded so the BCR-1 anchor inserts are forward-sync-protected; the consumer-side SC-grammar pin (M2 Test #7) hedges against the R-13 producer-side OSDG-1 gap until that gap is structurally closed. Regression = the consume-side directive silently reworded out of MUST-form / the round-trip-side mechanic erodes / the M4 closes-sentinel reverted to bare SC-\d{3} / the M2 SC-grammar literal lost (R-13 cross-skill brittleness re-opened) / the v0.61.0 entry or the ADR-055-extends-BC-PROJ-10 lineage silently lost / the catalog row #53 itself deleted. | `<HOME>/.claude/.venv/Scripts/python.exe -m pytest tests/methodology/test_bcr_1_backlog_round_trip.py tests/methodology/test_methodology_changelog.py::test_v_0_61_0_bcr_1_backlog_round_trip_entry_present_in_repo tests/methodology/test_methodology_changelog.py::test_v_0_61_0_bcr_1_shippability_consumer_propagation tests/methodology/test_slice_skill_drift.py tests/methodology/test_reflect_skill_drift.py tests/methodology/test_root_claude_md_cad1_eol_agnostic.py --no-header -q` | <2s | `<interp> -m pytest tests/methodology/test_bcr_1_backlog_round_trip.py tests/methodology/test_methodology_changelog.py::test_v_0_61_0_bcr_1_backlog_round_trip_entry_present_in_repo tests/methodology/test_methodology_changelog.py::test_v_0_61_0_bcr_1_shippability_consumer_propagation tests/methodology/test_slice_skill_drift.py tests/methodology/test_reflect_skill_drift.py tests/methodology/test_root_claude_md_cad1_eol_agnostic.py --no-header -q` |
| 54 | slice-054-fix-pyproject-toml-version-drift | PVFS-1 + SC-001 (slice-054; ADR-056 mints PVFS-1; BFRD-1 reproduction established at slice-054 /repro): `pyproject.toml` `[project].version` MUST equal the trimmed contents of the `VERSION` file (PVFS-1 — Pyproject Version Forward Sync; a pyproject↔VERSION lock-step gate mirroring the existing PMI-1 plugin.yaml↔VERSION gate at `tools/plugin_manifest_audit.py:207-217` — PMI-1 reads plugin.yaml only and does NOT touch pyproject.toml, so absent this row a pyproject version drift goes ungated). PVFS-1 extends the PMI-1 / AVFS-1 / MCFS-1 forward-sync family on the pyproject.toml leg; no standalone audit tool minted (SOAD-1/BCR-1 rule-without-tool precedent — the pytest assertion IS the gate). Pre-fix observed at slice-054 reproduction: `pyproject.toml` `[project].version = "0.20.0"` while `VERSION = "0.61.0"` (41 minor versions behind); `pip install <repo>` produces `ai-sdlc-tools 0.20.0` as a stale pip artifact label — external pip-install consumers see the wrong version. AC3 stale-literal scrub: pyproject.toml carries no `0.20.0` or `13 audit modules` / `13 tool modules` literals (3 sites enumerated at design.md M2: lines 3+6+66 plus the AC1-bumped line 20). First end-to-end BCR-1 round-trip dogfood (SC-001 → `**Closes:** SC-001` mission-brief sentinel → `/reflect` injects `**Addressed:** slice-054-…` line into `diagnose-out/backlog.md` SC-001 block AFTER `**Evidence:**`). Regression = `pyproject.toml` `[project].version` drifts from `VERSION` again (a future VERSION bump that forgets to update pyproject, or a pyproject edit that hard-codes a stale literal); OR the stale `0.20.0`/`13-modules` literals re-leak into pyproject.toml; OR the BCR-1 traceability axis (row cites BOTH `PVFS-1` AND `SC-001`) is silently severed by a future row rewrite that drops the SC-001 cite (per /critique-review M-add-1). | `<HOME>/.claude/.venv/Scripts/python.exe -m pytest tests/methodology/test_pyproject_version_matches_version_file.py tests/methodology/test_methodology_changelog.py::test_v_0_62_0_pvfs_1_entry_present_in_repo tests/methodology/test_methodology_changelog.py::test_v_0_62_0_pvfs_1_shippability_consumer_propagation tests/methodology/test_bcr_1_round_trip_end_to_end.py::test_bcr_1_sc054_round_trip_inputs_invariant --no-header -q` | <2s | `<interp> -m pytest tests/methodology/test_pyproject_version_matches_version_file.py tests/methodology/test_methodology_changelog.py::test_v_0_62_0_pvfs_1_entry_present_in_repo tests/methodology/test_methodology_changelog.py::test_v_0_62_0_pvfs_1_shippability_consumer_propagation tests/methodology/test_bcr_1_round_trip_end_to_end.py::test_bcr_1_sc054_round_trip_inputs_invariant --no-header -q` |
| 55 | slice-055-add-shippability-runner-execution-tests | SC-005 closure (slice-055; SRSC-1/ADR-039 extension via test coverage; mints no new rule per voluntary-restraint discipline slice-037/046/050/052/055): execution-level pins for `tools.shippability_runner.run_catalog()` + `main()` close the SC-005 owner-confirmed (HIGH severity, large blast, zero-dep) test-coverage gap that "shippability regressions block /reflect" rested on. Pre-fix only `_segments()` (parsing helper) was exercised by `test_shippability_runner_segment_contract`; `run_catalog()` and `main()` had zero test-module predecessors in the graphify graph. 5 tests pin: `run_catalog()` PASS branch on `<interp> -c "pass"` tmp catalog (returncode 0 → `passed=1`); `run_catalog()` FAIL branch on `<interp> -c "raise SystemExit(1)"` tmp catalog (returncode 1 → `failed=1` + structural pins `status="FAIL"`/`line>0`/`detail` truthy + narrow first-line substring `"segment exited 1"` per slice-038 m2 load-bearing-test lesson); `main()` returns 0/1/2 across the three documented CLI paths (`tools/shippability_runner.py:31-34, 193-195, 203`). The `<interp>` placeholder exercises `_normalize_interp` substitution to `sys.executable` (not bypassed). FAIL-branch fixture uses `;`-free Python (`raise SystemExit(1)`, NOT `import sys; sys.exit(1)`) because `_segments` does naive `;`-split BEFORE `shlex.split` (slice-055 /critique B1 empirical verification). Second end-to-end BCR-1 round-trip dogfood SUCCESS (slice-053 minted, slice-054 first dogfood, slice-055 N=2 dogfood): `**Closes:** SC-005` mission-brief sentinel triggered correct `/reflect` injection of `- **Addressed:** slice-055-…` line at BCR-1-mandated position (AFTER LAST `^  - ` Evidence sub-bullet, BEFORE `### SC-006` header) — anchor mechanic `re.finditer(r'^  - ', block, re.MULTILINE)[-1]` (slice-055 /critique B2 fix restores slice-054 AC4 contract; header-only `block.rfind('**Evidence:**')` would have silently passed for `Addressed` placed BEFORE first sub-bullet). Regression = the FAIL-branch substring pin silently weakens (`"segment exited 1"` no longer in detail's first line on a runner format-string edit); OR the structural pins (`status`, `line`, `detail` truthiness) silently weaken; OR `main()` exit-code contract (`0`/`1`/`2`) silently changes; OR the BCR-1 traceability axis (row cites BOTH `SC-005` AND the SRSC-1/ADR-039 extension framing) is silently severed by a future row rewrite that drops the SC-005 cite (per slice-054 /critique-review M-add-1 BCR-1-traceability axis pin discipline); OR the `;`-free constraint at `_make_catalog`'s `machine_cmd` parameter is silently weakened (`<interp> -c "import sys; sys.exit(1)"` fixture re-introduced — would crash `_segments` + `shlex.split`). | `<HOME>/.claude/.venv/Scripts/python.exe -m pytest tests/methodology/test_shippability_runner_execution.py --no-header -q` | <2s | `<interp> -m pytest tests/methodology/test_shippability_runner_execution.py --no-header -q` |
| 56 | slice-056-fix-bcr1-round-trip-test-archive-paths | R-15 retirement part-(a) DONE (slice-056; mints no new rule + no ADR per voluntary-restraint discipline slice-037/046/050/052/055/056 N≥6 cumulative; MEPD-1(b) discharged with META-1 enforcing-assertion verification at `tests/methodology/test_methodology_changelog.py:136`, NOT precedent analogy alone — slice-040/045/055 lineage): the slice-054 BCR-1 input-axis invariant test `test_bcr_1_sc054_round_trip_inputs_invariant` FAILed slice-055-innocently on master post-slice-054-archival because its hardcoded `SLICE_054_DIR = REPO_ROOT / "architecture" / "slices" / "slice-054-fix-pyproject-toml-version-drift"` resolved to a non-existent active path. Slice-056 adds `tests.methodology.conftest._resolve_slice_dir(slice_number: int) -> Path` (active-glob first, archive-glob fallback, AssertionError with both forward-slash patterns named on neither-match per /critique m4 Windows-safe diagnostic format, ValueError on out-of-range / non-int / bool-edge-case per /critique M2 single-arg signature) AND repoints `test_bcr_1_round_trip_end_to_end.py:43`'s `SLICE_054_DIR` to `_resolve_slice_dir(54)`. 6 new tests pin the helper at `tests/methodology/test_resolve_slice_dir.py` — helper-import + structural-bcr_1-pin + archived-resolution + tmp-vault-active-resolution + AssertionError-diagnostic + the M-add-2 corpus class-closure backstop `test_no_new_archive_fragile_literals_in_methodology_corpus` (whole-file regex spans multi-line constructions via `\s*` newline-tolerant matching; whitelist `{("tests/methodology/test_ptffd1_no_false_positive.py", 70)}` tracks the deferred slice-034 archive-path reference; whitelist-shrinkage on the future slice-034 retrofit IS the structural mechanism for R-15 part-(b) retirement per /critique-review M-add-2 ACCEPTED-FIXED). Mid-slice smoke verified BFRD-1 invariant flip FAIL→PASS. Row #54 (which already cites the same BCR-1 test) PASS-flipped from FAIL to PASS as a side effect (per /critique m3 ACCEPTED-FIXED). Per /critique m2 ACCEPTED-FIXED, R-15 STAYS `**Status**: mitigating` after slice-056 ships — retirement gate is two-part: (a) DONE this slice; (b) pending the future slice that demonstrably uses `_resolve_slice_dir` (slice-034 retrofit nominated). Per /critique-review M-add-3 ACCEPTED-FIXED, mission-brief.md Pipeline-position predecessor `/reflect` → `/commit-slice (canonical chain per PCA-1)`. Regression = the helper signature drifts to a 2-arg form with an optional `repo_root` parameter (per /critique M2 explicit-reject — would invite future caller drift); OR the diagnostic-message format reverts to `str(Path(...))` (backslash-on-Windows, breaks AC4c forward-slash assertion per /critique m4); OR the corpus-backstop whitelist gains a non-deferred entry (an R-15-class literal-path-RHS landing in a future test module silently — the M-add-2 backstop fails loudly with the offending file + line per /critique-review M-add-2); OR the BCR-1 traceability axis (row cites BOTH `slice-056` AND `R-15`) is silently severed by a future row rewrite that drops the R-15 cite (per slice-054 /critique-review M-add-1 BCR-1-traceability axis pin discipline, applied analogously to risk-register-driven slices). | `<HOME>/.claude/.venv/Scripts/python.exe -m pytest tests/methodology/test_resolve_slice_dir.py tests/methodology/test_bcr_1_round_trip_end_to_end.py::test_bcr_1_sc054_round_trip_inputs_invariant tests/methodology/test_methodology_changelog.py::test_shippability_row_56_present_and_cites_r15 --no-header -q` | <2s | `<interp> -m pytest tests/methodology/test_resolve_slice_dir.py tests/methodology/test_bcr_1_round_trip_end_to_end.py::test_bcr_1_sc054_round_trip_inputs_invariant tests/methodology/test_methodology_changelog.py::test_shippability_row_56_present_and_cites_r15 --no-header -q` |
| 57 | slice-057-retire-r15-via-slice-034-resolve-slice-dir-retrofit | R-15 retirement part-(b) DONE (slice-057; closes the two-part R-15 retirement gate slice-056 pre-engineered; mints no new rule + no ADR + no methodology-changelog entry + no VERSION bump per voluntary-restraint discipline slice-037/046/050/052/055/056/057 N≥7 cumulative; MEPD-1(b) discharged-by-name with META-1 enforcing-assertion verification at `tests/methodology/test_methodology_changelog.py:136` + slice-040 R-10 / slice-043 R-6 / slice-045 R-11 / slice-056 R-15-part-(a) N=4 precedent — pure conformance / retirement-discharge class): the slice-034 archive-fragile literal-path-RHS at `tests/methodology/test_ptffd1_no_false_positive.py:69-72` (the lone deferred R-15-class entry slice-056 acknowledged-but-did-not-fix per M-add-2 ACCEPTED-FIXED whitelist-shrinkage mechanism) is repointed to the slice-056 `_resolve_slice_dir(34)` helper; the `_R15_CORPUS_WHITELIST` at `tests/methodology/test_resolve_slice_dir.py:214-220` shrinks from one slice-034 tuple to `set()` (explicit empty-set form NOT dict-literal); the corpus class-closure backstop `test_no_new_archive_fragile_literals_in_methodology_corpus` is now structurally satisfied via match-set ⊆ ∅ (whitelist=empty AND matches=empty) without waiting for cross-slice observational evidence (the slice-056 /critique-review M-add-2 closure mechanism). R-15 escalates `**Status**: mitigating` → `retired` in `architecture/risk-register.md:254` with a slice-057-citation `**Retired**:` field-line + a retirement paragraph appended at section end (slice-040 R-10 retirement-precedent shape preserves all prior R-15 prose verbatim as historical record). The shippability row's BCR-1-traceability axis pin discipline (per slice-054 /critique-review M-add-1 → slice-056 row-#56 precedent applied analogously to risk-register-driven slices) cites `R-15` explicitly; the consumer-propagation pin `test_shippability_row_57_present_and_cites_r15` (structural twin of slice-056's `test_shippability_row_56_present_and_cites_r15` at L3768-3806) closes the audit trail at the changelog-test layer. Per /critique-review m-add-2 ACCEPTED-FIXED Builder guard-rail held: the comment-rewrite at `tests/methodology/test_resolve_slice_dir.py:203-220` contains zero literal `REPO_ROOT`-quoted-segment Python-form constructions (English prose only) so the regex-backed corpus-backstop self-skip surface is not stressed. Regression = the slice-034 archive-fragile literal is re-introduced anywhere under `tests/methodology` (fails loudly via the `unexpected = matches - whitelist != set()` half of the corpus backstop, naming offending file + line); OR the whitelist gains a stale entry pointing at a file/line that no longer carries the offending construction (fails loudly via the `missing_whitelist = whitelist - matches != set()` half — the regression-tripwire); OR the BCR-1-traceability axis is silently severed by a future row rewrite that drops the `R-15` cite (per slice-054 /critique-review M-add-1 BCR-1-traceability-axis pin discipline); OR the `_R15_LITERAL_PATH_RE` regex itself is weakened (e.g. forgetting whitespace-tolerance) producing false-negatives. | `<HOME>/.claude/.venv/Scripts/python.exe -m pytest tests/methodology/test_resolve_slice_dir.py::test_no_new_archive_fragile_literals_in_methodology_corpus tests/methodology/test_ptffd1_no_false_positive.py::test_slice034_prose_test_function_is_not_false_positive tests/methodology/test_methodology_changelog.py::test_shippability_row_57_present_and_cites_r15 --no-header -q` | <2s | `<interp> -m pytest tests/methodology/test_resolve_slice_dir.py::test_no_new_archive_fragile_literals_in_methodology_corpus tests/methodology/test_ptffd1_no_false_positive.py::test_slice034_prose_test_function_is_not_false_positive tests/methodology/test_methodology_changelog.py::test_shippability_row_57_present_and_cites_r15 --no-header -q` |
| 58 | slice-058-add-install-wakeup-prompt-guardrail | INSTALL.md Step 3h (the INST-1 wakeup-prompt-discipline install step) is present, idempotent (heading-check skip), confirmation-gated, and embeds the `# Wakeup-prompt discipline` block stating the four load-bearing ScheduleWakeup facts (literal-replay, never-as-no-op / heartbeat / fallback, harness-notifies-on-completion, `/loop` carve-out); INSTALL.md carries no stale current-version literal. Regression = Step 3h is removed or loses its idempotency / confirmation guard, the embedded block drops a load-bearing fact, or INSTALL.md re-staleizes a version literal. | `<HOME>/.claude/.venv/Scripts/python.exe -m pytest tests/methodology/test_install_md_wakeup_guardrail.py --no-header -q` | <1s | `<interp> -m pytest tests/methodology/test_install_md_wakeup_guardrail.py --no-header -q` |
| 59 | slice-059-add-tools-package-version-gate | TVFS-1 (slice-059; ADR-058 extends the slice-050 AVFS-1 + slice-041 MCFS-1 forward-sync-via-deterministic-downstream-gate lineage; mints a new rule, supersedes nothing): a standalone tool tools/ai_sdlc_tools_version_forward_sync.py asserts the ai-sdlc-tools pip distribution installed in the running interpreter's venv site-packages equals trimmed in-repo VERSION. The installed version is resolved via importlib.metadata.distributions scoped to sysconfig.get_path purelib -- NOT the naive importlib.metadata.version, which is shadowed by the in-repo ai_sdlc_tools.egg-info build artifact on sys.path (B1, first-Critic finding; PVFS-1 keeps the egg-info equal to VERSION so the naive read is tautologically green even when the venv is stale). Tri-state exit contract: in-repo VERSION missing exit 2 usage; ai-sdlc-tools not installed in the venv WARN exit 0 (message names sys.executable + purelib, M3); installed but version-divergent HALT exit 1 attributed NOT a slice regression; more than one distribution in purelib exit 2 usage with a distinct stale-duplicate-dist-info remediation message (M-add-2). check() exposes an installed_version_resolver injection seam (B2) so the regression suite drives a genuine non-tautological drift case. Wired ungated at /build-slice Step 6 + a dedicated /reflect Step 5b-tvfs. Non-catalog by construction on the slice-029/030A environment-mutable-state ground (the regression suite reads the venv-installed distribution) -- only these two in-repo-only entry-pins carry the row. Regression = a forgotten pip install --upgrade of the installed ai-sdlc-tools after a version bump (every other adopted project resolves a stale tool set), the v0.63.0 entry / ADR-058 standalone-tool lineage lost, the read mechanism regressed to the CWD-shadowed naive importlib.metadata.version, or the 2-point ungated wiring dropped. | `<HOME>/.claude/.venv/Scripts/python.exe -m pytest tests/methodology/test_methodology_changelog.py::test_v_0_63_0_tvfs_1_entry_present_in_repo tests/methodology/test_methodology_changelog.py::test_v_0_63_0_tvfs_1_shippability_consumer_propagation --no-header -q` | <2s | `<interp> -m pytest tests/methodology/test_methodology_changelog.py::test_v_0_63_0_tvfs_1_entry_present_in_repo tests/methodology/test_methodology_changelog.py::test_v_0_63_0_tvfs_1_shippability_consumer_propagation --no-header -q` |
| 60 | slice-060-add-code-review-skill | CRSI-1 (slice-060; ADR-059 mints a new rule, supersedes nothing; extends the dual-Critic /critique + /critique-review design-review family + OSDG-1 / CAD-1 drift-guard families onto code-as-artifact): /code-review is added to the canonical per-slice pipeline chain between /build-slice and /validate-slice as a walking-skeleton in-loop adversarial code-Critic step. v1 advisory only — findings written to architecture/slices/slice-NNN-/code-review.md but DO NOT block /validate-slice (slice-061 will add AI-bloat passes; slice-062 will add TRI-1 triage gate + verdict-driven block). New skills/code-review/SKILL.md orchestrates Agent-tool spawn with subagent_type='code-review'; new agents/code-review.md inherits structural fields from agents/critique.md verbatim modulo design->code substitution per design.md '9 dimensions reframed for code' table (tools: Read, Glob, Grep, Bash, WebSearch; model: opus; read-only stance via subagent-frontmatter hard runtime constraint per Claude Code docs / Create custom subagents; skill-frontmatter #18837 not applicable). PCA-1 _CANONICAL_CHAIN extended from 8 to 9 entries via two surgical tuple edits at tools/pipeline_chain_audit.py:73-82 + six hardcoded '8' -> '9' propagation sites (pipeline_chain_audit.py:56/225/309 + install_audit.py:84 + test_pipeline_chain_audit.py:4/66 + test_pipeline_position_block_drift.py docstring). install_audit.py _CANONICAL_SKILLS gains 'code-review' between 'build-slice' and 'commit-slice' (ASCII-correct insertion); _CANONICAL_AGENTS prepends 'code-review' as first entry. 5-part PMI-1 atomic bump 0.63.0 -> 0.64.0 (VERSION + plugin.yaml.version + pyproject.toml [project].version + ## v0.64.0 header + installed ~/.claude/ai-sdlc-VERSION). OSDG-1 family-add via test_code_review_skill_drift.py + CAD-1 family-add via test_code_review_agent_drift.py (both reuse tests/skill_drift_equality.py::assert_md_forward_synced for EOL-DRIFT-1/ADR-033 EOL-agnostic comparison). Dual-Critic: first NEEDS-FIXES 5B/5M/4m (all VALID); meta EXTEND with M-add-1 (Phase B forward-sync omitted edited build-slice + validate-slice SKILL.md — Fowler refactoring discipline fan-out) + M-add-2 (TF-1 plan omitted BC-PROJ-10:173 paired _shippability_consumer_propagation test — N>=17 instances) + m-add-1 (Dim 9 reframe clarification on phantom-import-in-.py as code-as-artifact under Dim 1/Dim 5); TRI-1 verdict CLEAN. Regression = the /code-review skill or agent loses its forward-sync (OSDG-1/CAD-1 drift), the PCA-1 chain extension reverts to 8 entries, the v0.64.0 entry's CRSI-1 / ADR-059 / Code-Review Skill Insertion / mints a new rule / supersedes nothing / 5-part PMI-1 atomic bump / Rule reference anchors are silently lost, the BCR-1 traceability axis (row cites BOTH CRSI-1 AND ADR-059) is silently severed by a future row rewrite, OR the walking-skeleton self-dogfood artifact at architecture/slices/slice-060-add-code-review-skill/code-review.md is silently deleted. | `<HOME>/.claude/.venv/Scripts/python.exe -m pytest tests/methodology/test_code_review_skill_drift.py tests/methodology/test_code_review_agent_drift.py tests/methodology/test_methodology_changelog.py::test_v_0_64_0_crsi_1_entry_present_in_repo tests/methodology/test_methodology_changelog.py::test_v_0_64_0_crsi_1_shippability_consumer_propagation tests/methodology/test_pipeline_chain_audit.py::test_canonical_chain_includes_code_review_edge tests/skills/code_review/test_code_review_skill.py::test_self_dogfood_produces_code_review_md_on_slice_060 --no-header -q` | <3s | `<interp> -m pytest tests/methodology/test_code_review_skill_drift.py tests/methodology/test_code_review_agent_drift.py tests/methodology/test_methodology_changelog.py::test_v_0_64_0_crsi_1_entry_present_in_repo tests/methodology/test_methodology_changelog.py::test_v_0_64_0_crsi_1_shippability_consumer_propagation tests/methodology/test_pipeline_chain_audit.py::test_canonical_chain_includes_code_review_edge tests/skills/code_review/test_code_review_skill.py::test_self_dogfood_produces_code_review_md_on_slice_060 --no-header -q` |
| 61 | slice-061-fix-install-python-detection-and-prompt-fallback | INSTALL.md (the INST-1 install recipe) python-detection brittleness fix: Step 3a no longer hardcodes `python3 -m venv ~/.claude/.venv` (uses the interpreter Step 1 detected — reuses a captured variable or recomputes the `command -v python3 || command -v python` fallback chain inline), Step 2's `**No Python found**` ambiguity branch directs Claude to ASK the user for the Python interpreter path or to install Python via SOAD-1 structured options (not a bare free-text hard-fail with `Install Python 3.11+ and re-run`), AND Step 2's `**Both system Python and conda available**` row no longer hardcodes `python3 -m venv` as the default (tracks the Step 3a fix so the prose default cannot reintroduce the bug). Regression = INSTALL.md Step 3a re-hardcodes `python3 -m venv` (breaks installs on systems where only `python` is on PATH — Windows default, distros without `python-is-python3`, bare macOS), the `**No Python found**` branch reverts to a bare-prompt hard-fail (SOAD-1 violation + no recovery for installed-but-not-on-PATH Python), or the Step 2 conda-default re-hardcodes `python3 -m venv` (silent re-introduction via prose default). | `<HOME>/.claude/.venv/Scripts/python.exe -m pytest tests/methodology/test_install_md_python_detection.py --no-header -q` | <1s | `<interp> -m pytest tests/methodology/test_install_md_python_detection.py --no-header -q` |
| 62 | slice-062-extend-r15-corpus-class-closure-scope | R-15 corpus class-closure backstop scope extension (slice-062; ADR-060 mints no new rule, supersedes nothing; extends the slice-056 / slice-057 R-15 backstop scope to wider test corpora — naming-class peer of ADR-053 OSDG-1-member-addition shape): the slice-056-minted backstop `tests/methodology/test_resolve_slice_dir.py::test_no_new_archive_fragile_literals_in_methodology_corpus` is extended via shared `_scan_corpus_for_r15_literals(corpus_dir)` helper + 3 per-corpus test functions + 1 aggregated whitelist-integrity test to cover all three test corpora: `tests/methodology/**/*.py` + `tests/skills/**/*.py` + `tests/agents/**/*.py`. Each per-corpus test's pytest-collection + pass IS the structural walk-proof that its corpus is walked (closes the slice-056 N=1 walk-proof lesson). Closes the slice-061 N=1 watch-list scope-gap by repointing `tests/skills/code_review/test_code_review_skill.py::test_self_dogfood_produces_code_review_md_on_slice_060` via lazy `_resolve_slice_dir(60)` (slice-056 helper) inside the test function — NOT a module-level binding (lazy resolution gives a cleaner test-time AssertionError surface vs the module-import-time collection-error class). R-15 STAYS `**Status**: retired` (slice-057 disposition unchanged; the slice-056/057 mechanism that retired R-15 IS sound for its declared scope; slice-062 widens the mechanism's reach not the underlying rule). `_R15_CORPUS_WHITELIST` stays empty post-slice (the slice-061-surfaced offender is REPOINTED, not whitelisted). 5-part PMI-1 atomic bump 0.64.0 → 0.65.0 (VERSION + plugin.yaml.version + pyproject.toml [project].version + ## v0.65.0 header + installed ~/.claude/ai-sdlc-VERSION). Dual-Critic: first NEEDS-FIXES 1B/1M/4m (B1 PVFS-1/5-part PMI-1 enumeration gap, M1 slice-060 reflection L37 TRI-1 nomination silently re-deferred, m1 fabricated lessons-learned citation, m2 EPGD-1 SECTION header, m3 TPHD-1 sub-mode (a) TF-1 table harmonization, m4 R-15 historical-prose framing — all VALID at correct severity, all ACCEPTED-FIXED in same fix block); meta-Critic EXTEND with 3 missed findings (M-add-1 Blocker — the m3 ACCEPTED-FIXED fix-block edit ITSELF introduced TF-1 audit violations via multi-AC labels `"1, 3"` + `"(audit-trace)"` markers that `tools/test_first_audit.py:_normalize_ac_label` rejects → 2 `ac-without-row` violations; EXACT same defect class as slice-056 /critique-review M-add-1 — N=2 slice-040 N+1 doctrine recurrence + slice-022 RSAD-1 self-violation pattern extending to dual-Critic stack same-fix-block edits; M-add-2 cross-document option-enumeration incoherence; M-add-3 design.md Phase C step 11 "4-part" stale-carry adjacent to step 12's correct 5-part PMI-1 anchor); all 3 ACCEPTED-FIXED at /critique-review fix block; TF-1 audit empirically re-verified clean post-fix (violation_count: 0, 8 rows across 4 ACs); TRI-1 verdict CLEAN. Regression = a new archive-fragile literal-path-RHS authored in tests/skills/** or tests/agents/** goes uncaught (the wider-scope per-corpus tests removed), the lazy `_resolve_slice_dir(60)` repoint reverts to a module-level archive-path literal (re-introduces the slice-061 N=1 class), the v0.65.0 entry's R-15 / ADR-060 / R-15 corpus class-closure backstop scope extension / mints no new rule / supersedes nothing / 5-part PMI-1 atomic bump / Rule reference anchors silently lost, OR the BCR-1 traceability axis (row cites BOTH R-15 AND ADR-060) silently severed by a future row rewrite. | `<HOME>/.claude/.venv/Scripts/python.exe -m pytest tests/methodology/test_resolve_slice_dir.py::test_no_new_archive_fragile_literals_in_methodology_corpus tests/methodology/test_resolve_slice_dir.py::test_no_new_archive_fragile_literals_in_tests_skills_corpus tests/methodology/test_resolve_slice_dir.py::test_no_new_archive_fragile_literals_in_tests_agents_corpus tests/methodology/test_resolve_slice_dir.py::test_r15_corpus_whitelist_has_no_orphan_entries tests/skills/code_review/test_code_review_skill.py::test_self_dogfood_produces_code_review_md_on_slice_060 tests/methodology/test_methodology_changelog.py::test_v_0_65_0_r15_scope_extension_entry_present_in_repo tests/methodology/test_methodology_changelog.py::test_v_0_65_0_r15_scope_extension_shippability_consumer_propagation --no-header -q` | <2s | `<interp> -m pytest tests/methodology/test_resolve_slice_dir.py::test_no_new_archive_fragile_literals_in_methodology_corpus tests/methodology/test_resolve_slice_dir.py::test_no_new_archive_fragile_literals_in_tests_skills_corpus tests/methodology/test_resolve_slice_dir.py::test_no_new_archive_fragile_literals_in_tests_agents_corpus tests/methodology/test_resolve_slice_dir.py::test_r15_corpus_whitelist_has_no_orphan_entries tests/skills/code_review/test_code_review_skill.py::test_self_dogfood_produces_code_review_md_on_slice_060 tests/methodology/test_methodology_changelog.py::test_v_0_65_0_r15_scope_extension_entry_present_in_repo tests/methodology/test_methodology_changelog.py::test_v_0_65_0_r15_scope_extension_shippability_consumer_propagation --no-header -q` |
| 63 | slice-063-add-build-slice-new-agent-warning | NAW-1 (slice-063; ADR-061 mints a new rule, supersedes nothing; the first audit-enforced gate on the discovery-gate axis adjacent to but NOT extending the PMI-1 / PVFS-1 / AVFS-1 / MCFS-1 / TVFS-1 forward-sync family): /build-slice Step 6 emits a non-blocking WARN when the slice diff adds any agents/star.md file naming the new agent path + session-restart-before-next-slice recommendation + R-18 cross-reference. Retires R-18 (mitigating to retired) — N=2 cumulative recurrence at slice-061 + slice-062 surfaced the Claude Code agent registry session-cache-miss class methodology-discoverably BEFORE the next slice's chain runs. Binary exit contract by construction (0 clean / 0 warn / 2 usage; NEVER exit 1 — a new-agent slice is NOT a slice regression). Union-of-three-sources read mechanism (working-tree-vs-base + git ls-files --others + commits-vs-base per ADR-061 Decision; closes B1 critique-fix falsifier class where naive git diff base...HEAD is commit-vs-commit only and would return EMPTY for uncommitted Step-6 work). Two injection seams (default_branch_resolver + added_files_resolver) for deterministic regression testing per slice-059 TVFS-1 seam precedent. 5-part PMI-1 atomic bump 0.65.0 to 0.66.0 (VERSION + plugin.yaml.version + pyproject.toml [project].version + ## v0.66.0 header + installed ~/.claude/ai-sdlc-VERSION). BC-PROJ-9 5-inventory fan-out for new tools/*.py module (install_audit._CANONICAL_TOOLS + test_utf8_stdout._ROOT_ONLY_TOOLS + plugin.yaml tools block + INSTALL.md 27 to 28 at L22 + L166 + this shippability row). Dual-Critic: first BLOCKED 1B/4M/5m (B1 read-mechanism falsified by git docs + execution; M1 phantom test-file PTFCD-1; M2 --strict-pre-finish flag drift FBCD-1 sub-mode (a); M3 BRANCH-1 fallback-main misattribution against tools/branch_workflow_audit.py:127-146 implementation; M4 AC#2 test-housing wrong file Mini-CAD-1 vs structural-anchor; m1-m5 minor cleanups) all 10 ACCEPTED-FIXED at /critique fix block. Meta-Critic EXTEND with 3 missed: M-add-1 Major 5-part PMI-1 leg enumeration drift design.md L17 vs ADR-061 L85 (same defect class as slice-060 B2 / slice-062 B1 cross-doc enumeration drift); M-add-2 Minor EPGD-1 anchor list incomplete missing ## v0.66.0 header + Rule reference META-1-mandatory; M-add-3 Minor design.md L218 Step 6 audit list wrongly included plugin_manifest. All 3 ACCEPTED-FIXED at /critique-review fix block. TF-1 audit empirically re-verified clean post-fix (violation_count: 0, 9 rows across 5 ACs). TRI-1 verdict CLEAN. Regression = a future agent-shipping slice ships without surfacing the WARN at /build-slice Step 6, the audit's exit contract drifts from binary (0/2) to tri-state (0/1/2), the WARN message loses its R-18 anchor or agent-path citation, the v0.66.0 entry's NAW-1 / ADR-061 / New-Agent Warning / mints a new rule / supersedes nothing / Rule reference / 5-part PMI-1 atomic bump anchors silently lost, OR the BCR-1 traceability axis (this row cites BOTH NAW-1 AND R-18 AND ADR-061) silently severed by a future row rewrite. | `<HOME>/.claude/.venv/Scripts/python.exe -m pytest tests/methodology/test_new_agent_warning_audit.py tests/methodology/test_build_slice_skill.py::test_build_slice_step_6_invokes_new_agent_warning_audit tests/methodology/test_risk_register_audit_real_file.py::test_r_18_retired_post_slice_063 tests/methodology/test_methodology_changelog.py::test_v_0_66_0_naw_1_entry_present_in_repo tests/methodology/test_methodology_changelog.py::test_v_0_66_0_naw_1_shippability_consumer_propagation --no-header -q` | <2s | `<interp> -m pytest tests/methodology/test_new_agent_warning_audit.py tests/methodology/test_build_slice_skill.py::test_build_slice_step_6_invokes_new_agent_warning_audit tests/methodology/test_risk_register_audit_real_file.py::test_r_18_retired_post_slice_063 tests/methodology/test_methodology_changelog.py::test_v_0_66_0_naw_1_entry_present_in_repo tests/methodology/test_methodology_changelog.py::test_v_0_66_0_naw_1_shippability_consumer_propagation --no-header -q` |
| 64 | slice-064-fix-code-review-diff-resolution-falsifier | Extension of NAW-1 union-of-three-sources read mechanism (slice-063; ADR-061) to `/code-review` SKILL.md Step 1 (slice-064; ADR-062 mints no new rule, supersedes nothing — sibling-surface application of NAW-1's pattern; Inclusion-heuristic firing on scope-extension precedent N=8 cumulative inclusive of slice-064; methodology v0.67.0; 5-part PMI-1 atomic bump 0.66.0 → 0.67.0): `skills/code-review/SKILL.md` Step 1 diff resolution rewritten to invoke three bash commands in sequence — (i) `git diff "$base" --name-only --diff-filter=ACMR -- ':(exclude)architecture/**' ':(exclude)docs/**'` working-tree-vs-base + (ii) `git ls-files --others --exclude-standard -- ':(exclude)architecture/**' ':(exclude)docs/**'` untracked + (iii) `git diff "$base"...HEAD --name-only --diff-filter=ACMR -- ':(exclude)architecture/**' ':(exclude)docs/**'` commits-vs-base — with inline literal pathspecs on each leg (POSIX-portable; NOT bash-array per /critique M1) and an explicit post-bash-block prose instruction "Union the three outputs by path; deduplicate. The resulting file list is the in-scope diff scope handed to Step 2." (Claude's runtime aggregation obligation is SKILL.md-prose-binding per /critique M2 / CLAUDE.md "skill prose IS executable contract"). Step 2 prompt-template `# Diff content` block aligned to per-file `git diff "$base" -- <file>` (WT-vs-base) instead of the stale aggregate `git diff <base>...HEAD -- <files>` form. Closes the B1 falsifier class on /code-review SKILL.md surface (N=2 cumulative across NAW-1's audit-tool surface + /code-review's SKILL.md surface; slice-063 retired the class on NAW-1, slice-064 retires it on /code-review). Dual-Critic: first BLOCKED 2B/4M/5m (B1 AC#4 phantom test path + phantom function PTFCD-1+PTFFD-1 double — empirically verified via `tools.test_first_audit --strict-pre-finish` returning `missing-test-path-file`; B2 design.md L17 entry-pin pair enumerated but mission-brief AC#5 plans only entry-pin half — slice-060 M-add-2 / N≥17 BC-PROJ-10:173 recurrence; M1 bash-array → POSIX inline pathspecs; M2 no runtime-union prose-pin in SKILL.md; M3 wide-slice per-file diff token cost watch-list R-X3 N=1; M4 BC-PROJ-9 5-inventory enumeration drift; m1 EPGD-1 anchor count; m2 disjunctive placeholder; m3 Test-first HTML-comment R-7 footgun OVERRIDDEN as empirically tolerated; m4 N=7 vs N=8 cumulative count drift; m5 "SOLE forward-sync" misleading) 10 ACCEPTED-FIXED + 1 OVERRIDDEN. Meta-Critic EXTEND with 3 missed findings (M-add-1 Major TPHD-1 sub-mode (a) cross-file harmonization for B1 fix INCOMPLETE — 3 residual stale-path sites at mission-brief.md L21+L78 + design.md L109 — same defect class as slice-062 /critique-review M-add-1 Blocker + slice-022 RSAD-1 self-violation pattern; M-add-2 Minor N=8 vs N=9 anchor-count drift between ADR-062 L105 and design.md L18; M-add-3 Minor design.md L18 EPGD-1 enumeration omits rule-name-expansion anchor class per slice-063 anchor (d) precedent) all 3 ACCEPTED-FIXED at /critique-review fix block. TF-1 audit empirically re-verified clean post-fix (7 rows; AC#1 + AC#5 multi-row-per-AC per slice-056+062 lesson). TRI-1 user-ratified verdict CLEAN (14/14 dispositions). Regression = /code-review SKILL.md Step 1 reverts to a single `git diff "$base"...HEAD` commit-vs-commit-only diff and `/code-review` silently writes `Result: NO-CODE-CHANGES — nothing to review` on a governed slice with uncommitted Step-6 work; the union-instruction prose pin is lost; the Step 2 prompt-template `# Diff content` block re-introduces the aggregate form; the v0.67.0 entry's NAW-1 / ADR-062 / Extend NAW-1 union-of-three-sources to /code-review rule-name expansion / 5-part PMI-1 / Rule reference / `mints no new rule` + `supersedes nothing` compound lineage anchors silently lost; or the BCR-1 traceability axis (this row cites BOTH ADR-062 AND NAW-1 AND ADR-061) silently severed. | `<HOME>/.claude/.venv/Scripts/python.exe -m pytest tests/skills/code_review/test_code_review_skill.py::test_skill_md_step_1_diff_resolution_uses_union_of_three_sources tests/skills/code_review/test_code_review_skill.py::test_skill_md_step_1_all_three_legs_share_filter_shape tests/skills/code_review/test_code_review_skill.py::test_skill_md_step_1_union_aggregation_prose_pinned tests/skills/code_review/test_code_review_skill.py::test_skill_md_step_2_diff_content_block_aligned_with_step_1 tests/methodology/test_code_review_skill_drift.py tests/methodology/test_methodology_changelog.py::test_v_0_67_0_naw_extend_entry_present_in_repo tests/methodology/test_methodology_changelog.py::test_v_0_67_0_naw_extend_shippability_consumer_propagation --no-header -q` | <2s | `<interp> -m pytest tests/skills/code_review/test_code_review_skill.py::test_skill_md_step_1_diff_resolution_uses_union_of_three_sources tests/skills/code_review/test_code_review_skill.py::test_skill_md_step_1_all_three_legs_share_filter_shape tests/skills/code_review/test_code_review_skill.py::test_skill_md_step_1_union_aggregation_prose_pinned tests/skills/code_review/test_code_review_skill.py::test_skill_md_step_2_diff_content_block_aligned_with_step_1 tests/methodology/test_code_review_skill_drift.py tests/methodology/test_methodology_changelog.py::test_v_0_67_0_naw_extend_entry_present_in_repo tests/methodology/test_methodology_changelog.py::test_v_0_67_0_naw_extend_shippability_consumer_propagation --no-header -q` |
| 65 | slice-065-bundle-064-code-critic-cleanup | Bundled cleanup of slice-064 code-Critic 3 advisory findings (slice-065; mints NO new rule + NO new ADR + NO methodology-changelog entry + NO VERSION bump per voluntary-restraint discipline N≥9 cumulative inclusive of slice-065 — slice-037/046/050/052/055/056/057/061 precedent; Inclusion-heuristic disposition explicit per BC-PROJ-10; in-family extension of ADR-061 §Decision exit-2 contract + ADR-062 sibling-surface application — slice-064 mirrored NAW-1's union-of-three-sources read mechanism into SKILL.md bash, slice-065 mirrors ADR-061's exit-2 fail-fast STOP-guard contract into the same surface): closes slice-064 reflection's "slice-065+ bundled cleanup nomination" of 3 code-Critic v1 walking-skeleton advisories — (a) M1 substring-leak retirement on `test_skill_md_step_1_diff_resolution_uses_union_of_three_sources` Source-(iii) assertion (tightened to bash_block-scoped full-filter-shape literal `'git diff "$base"...HEAD --name-only --diff-filter=ACMR'`; pre-slice-065 `step_1`-scoped substring `'git diff "$base"...HEAD'` was satisfied by L37 preamble prose alone — deleting L51 bash command would still pass; bash_block scoping + flag-tail double-redundancy retires the leak), (b) m1 default-branch resolver no-programmatic-STOP fix at `skills/code-review/SKILL.md` L41-L42 (inserted `[ -z "$default" ] && { echo "default-branch-unresolvable: neither origin/HEAD nor init.defaultBranch resolved" >&2; exit 2; }` between L41 second-resolver fallback and L42 `git merge-base` — pre-fix silently fell through to `git merge-base "" HEAD` undefined behavior; post-fix mirrors NAW-1's exact exit-2 contract from ADR-061 §Decision), (c) m2 semantic-invariant tightening on `test_skill_md_step_1_all_three_legs_share_filter_shape` count assertion (replaced `assert diff_with_filter >= 2` with bash_block-scoped semantic invariant `assert diff_with_filter == bash_block.count('git diff "$base"')` — admits no 3-leg / 2-filtered regression; the Critic-flagged `step_1`-scoped form would have evaluated 2 != 3 FAIL due to L37 preamble leak in RHS). PLUS adds new structural pin `test_skill_md_step_1_default_branch_resolver_stops_on_empty` (AC#4 — three composable bash_block-scoped assertions: count `[ -z "$default" ] >= 2` additive STOP-guard pin + canonical NAW-1 brace-group form `&& { echo "default-branch-unresolvable` + `exit 2` literals + structural co-location `fallback_idx < stop_idx < merge_base_idx` with -1 short-circuit safety per /critique B2 proposed-fix (1)+(2)+(3) + /critique m2 brace-group form pinning + /critique-review m-add-1 narrative-manifest parity restoration). Dual-Critic: first BLOCKED 2B/3M/3m (B1 AC#2 substring-leak in slice's own assertion draft empirically verified `step_1.count('git diff "$base"')` = 3 not 2 — textbook RSAD-1 slice-040 N+1 doctrine catch on slice-064 section-scoped-substring-assertion pattern; B2 AC#4 substring-leak `'[ -z "$default" ]' in step_1` + `'default-branch-unresolvable' in step_1` both already TRUE pre-fix via L41 fallback + L68 prose; M1 backwards "between L42 and L41" within-design phrasing FBCD-1 sub-mode (a); M2 mission-brief "non-zero exit" vs design.md hardcoded "exit 2" granularity drift; M3 weak Inclusion-heuristic class-precedent slice-061 R-16 INSTALL.md prose-correctness — too weak; m1 verification §3 inline-bash check tests POSIX-shell semantics but SKILL.md prose is Claude-runtime-binding not bash-exec; m2 `'exit 2'` loose substring; m3 ADR-061 §Decision L60-67 coordinate-only pin) all 8 ACCEPTED-FIXED in same fix block per TPHD-1 sub-mode (a) / FBCD-1 cross-surface harmonization. Builder self-catch (slice-057 N+1 doctrine precedent) of Phase A2/A3 stale literals at design.md L180-L181 pre-meta-Critic invocation. Meta-Critic EXTEND with 1 missed Minor (m-add-1 shippability row #65 Description residual FBCD-1 cross-cell drift — first-Critic harmonized test-assertion cells + SKILL.md-insertion cell but missed shippability row Description cell; ACCEPTED-FIXED via narrative-manifest parity append). TRI-1 user-ratified verdict CLEAN (9/9 dispositions all ACCEPTED-FIXED). TF-1 audit empirically re-verified clean post-fix (6 rows; PASSING after Phase C SKILL.md fix lands). CRSI-1 v1 walking-skeleton advisory feedback loop closed — slice-064's 3 advisories retired in-slice with structurally-correct test assertions empirically grounded. Regression = `skills/code-review/SKILL.md` Step 1 silently reverts to the pre-fix silent fall-through to `git merge-base "" HEAD` (loses the `[ -z "$default" ] && { echo "default-branch-unresolvable: …" >&2; exit 2; }` brace-group guard); the AC#1/AC#2 bash_block-scoped assertions silently weaken back to section-scoped substring form admitting L37 preamble leak class; the AC#4 three composable assertions (count + brace-group + co-location) silently weaken to bare substring `'[ -z "$default" ]' in step_1` form that was already TRUE pre-fix; OR the BCR-1 traceability axis (this row cites BOTH slice-065 AND closed slice-064 advisory IDs M1/m1/m2 AND new test function `test_skill_md_step_1_default_branch_resolver_stops_on_empty`) silently severed by a future row rewrite. | `<HOME>/.claude/.venv/Scripts/python.exe -m pytest tests/skills/code_review/test_code_review_skill.py::test_skill_md_step_1_diff_resolution_uses_union_of_three_sources tests/skills/code_review/test_code_review_skill.py::test_skill_md_step_1_all_three_legs_share_filter_shape tests/skills/code_review/test_code_review_skill.py::test_skill_md_step_1_default_branch_resolver_stops_on_empty tests/methodology/test_code_review_skill_drift.py --no-header -q` | <2s | `<interp> -m pytest tests/skills/code_review/test_code_review_skill.py::test_skill_md_step_1_diff_resolution_uses_union_of_three_sources tests/skills/code_review/test_code_review_skill.py::test_skill_md_step_1_all_three_legs_share_filter_shape tests/skills/code_review/test_code_review_skill.py::test_skill_md_step_1_default_branch_resolver_stops_on_empty tests/methodology/test_code_review_skill_drift.py --no-header -q` |
| 66 | slice-066-add-worktree-per-slice-discipline | BRANCH-2 (slice-066; ADR-063 mints a new rule; partial-supersedes ADR-019 / BRANCH-1 sub-mode (a) build-time branch-create only — sub-mode (b) was already partial-superseded by ADR-020 at slice-022; sub-mode (c) audit-time pre-finish refusal is EXTENDED in place via 4 new violation kinds, NOT superseded): `/build-slice` `## Prerequisite check ### Branch state` creates a filesystem-isolated worktree at the canonical sibling-dir path `<main-parent>/<main-name>-wt/slice-NNN-<slice-name>` via `git worktree add ... -b slice/NNN-<slice-name> <default>` + `cd`, replacing BRANCH-1's `git checkout -b` on the main tree. `/commit-slice --merge` (Step 5b) + `/commit-slice --sync-after-pr` (Step 5d) extended with idempotent worktree-remove guard (LOG-and-skip if no worktree exists — covers slice-066 bootstrap + any future WORKTREE=skip slice) BEFORE the existing safe branch-delete (order load-bearing — git refuses `branch -d` on checked-out branch per git-scm.com/docs/git-worktree). `tools/branch_workflow_audit.py` gains 4 new violation kinds (worktree-not-registered + worktree-cwd-mismatch + worktree-path-shape-violation + worktree-skip-malformed) + 4 new helpers + new `_WORKTREE_SKIP_LINE_RE` regex mirroring `_BRANCH_SKIP_LINE_RE`'s shape. Path-comparison semantics use `Path.resolve(strict=False)` + `samefile()` + `os.path.normcase(os.path.realpath(...))` fallback to tolerate Windows symlinks/junctions/case-insensitivity per microsoft/vscode#101244 evidence. WORKTREE=skip escape-hatch grammar pinned across N=3 surfaces (build-slice SKILL.md Step 7c + commit-slice SKILL.md cleanup reminders + branch_workflow_audit.py `_WORKTREE_SKIP_LINE_RE`) per RPCD-1 cross-spec parity discipline; BRANCH=skip preserved as legacy parallel escape-hatch per ADR-063 §Scope of supersession 4th-surface inheritance. Retires **R-17** (mitigating → retired — the candidate-fix-(b) at risk-register.md:295 verbatim; uncommitted-slice-A-WIP-contaminates-slice-B class closed STRUCTURALLY via worktree filesystem isolation; pre-create cleanliness audit candidate-fix-(a) DECLINED as redundant defense-in-depth per ADR-063 §"Options considered" option 3). ADR-063 is the N=2 application of the slice-022 ADR-020 partial-supersession encoding pattern (single `supersedes:` frontmatter slot + body-level scope enumeration; ADR-019 stays unmodified across N=2 partial supersessions per append-only). Bootstrap (slice-066 only): WORKTREE=skip-bootstrap Events line + idempotent worktree-remove guard make slice-066's own /commit-slice --merge safe; slice-067 onward inherits a self-gating BRANCH-2 audit. 5-part PMI-1 atomic bump 0.67.0 → 0.68.0 (VERSION + plugin.yaml.version + pyproject.toml [project].version + ## v0.68.0 header + installed ~/.claude/ai-sdlc-VERSION). Dual-Critic + Builder self-catch ratchet: 14 first-Critic findings (5B/5M/4m all ACCEPTED-FIXED + m4 ACCEPTED-PENDING) + 6 meta-Critic missed findings (5 ACCEPTED-FIXED + M-add-5 ACCEPTED-PENDING) + 8 Builder Phase A/B self-catches (PMI-1 enum × 3 + TF-1 path × 2 + BC-PROJ-10 paired-pin × 1 + section-scoping × 2) = N=28 cumulative findings caught + closed; textbook slice-022 "codification slices commit exactly the violation their codified discipline catches" pattern. Regression = `/build-slice` Branch state reverts to bare `git checkout -b slice/` on main tree (R-17 re-opens), worktree-teardown order reverses (branch-delete before worktree-remove → git refuses), the v0.68.0 entry's BRANCH-2 / ADR-063 / `Worktree-per-slice + branch` rule-name / `mints a new rule` / `partial-supersedes ADR-019` / `Rule reference` / `5-part PMI-1 atomic bump` anchors silently lost, the WORKTREE=skip cross-spec parity drops on one of the N=3 surfaces, OR the BCR-1 traceability axis (this row cites BOTH BRANCH-2 AND ADR-063 AND R-17) silently severed by a future row rewrite. | `<HOME>/.claude/.venv/Scripts/python.exe -m pytest tests/methodology/test_branch_workflow_audit.py tests/methodology/test_adr_063_exists_and_supersedes_adr_019.py tests/methodology/test_r17_retirement.py tests/methodology/test_build_slice_skill.py::test_branch_state_subsection_invokes_git_worktree_add tests/methodology/test_build_slice_skill.py::test_branch_state_subsection_uses_sibling_wt_path_shape tests/methodology/test_build_slice_skill.py::test_branch_state_subsection_does_not_use_bare_checkout_b tests/methodology/test_commit_slice_skill_merge_flag.py::test_merge_mode_invokes_git_worktree_remove_post_merge tests/methodology/test_commit_slice_skill_merge_flag.py::test_teardown_order_pins_remove_before_branch_delete tests/methodology/test_commit_slice_skill_sync_after_pr_flag.py::test_sync_after_pr_mode_invokes_git_worktree_remove_post_merge tests/methodology/test_root_claude_md_branch_per_slice_rule.py::test_branch_per_slice_paragraph_rewritten_to_worktree_per_slice tests/methodology/test_methodology_changelog.py::test_v_0_68_0_branch_2_entry_present_in_repo tests/methodology/test_methodology_changelog.py::test_v_0_68_0_branch_2_shippability_consumer_propagation --no-header -q` | <5s | `<interp> -m pytest tests/methodology/test_branch_workflow_audit.py tests/methodology/test_adr_063_exists_and_supersedes_adr_019.py tests/methodology/test_r17_retirement.py tests/methodology/test_build_slice_skill.py::test_branch_state_subsection_invokes_git_worktree_add tests/methodology/test_build_slice_skill.py::test_branch_state_subsection_uses_sibling_wt_path_shape tests/methodology/test_build_slice_skill.py::test_branch_state_subsection_does_not_use_bare_checkout_b tests/methodology/test_commit_slice_skill_merge_flag.py::test_merge_mode_invokes_git_worktree_remove_post_merge tests/methodology/test_commit_slice_skill_merge_flag.py::test_teardown_order_pins_remove_before_branch_delete tests/methodology/test_commit_slice_skill_sync_after_pr_flag.py::test_sync_after_pr_mode_invokes_git_worktree_remove_post_merge tests/methodology/test_root_claude_md_branch_per_slice_rule.py::test_branch_per_slice_paragraph_rewritten_to_worktree_per_slice tests/methodology/test_methodology_changelog.py::test_v_0_68_0_branch_2_entry_present_in_repo tests/methodology/test_methodology_changelog.py::test_v_0_68_0_branch_2_shippability_consumer_propagation --no-header -q` |
| 67 | slice-067-add-parallel-slice-queue-output | PSQ-1 (slice-067; ADR-064 mints a new rule; supersedes nothing; first rule on the parallel-slice family axis adjacent to BRANCH family — BRANCH-2 makes parallel work physically possible, PSQ-1 makes it discoverable; PSQ-2 / PSQ-3 nominated slice-068 / slice-069): `/slice` Step 6.5 (NEW) writes `architecture/slice-queue.md` containing the top-10 parallel-safe candidates from Step 1 source-fan-out (sources 1 through 8), each entry tagged with graphify-derived blast-radius and a 4-value `Parallel-safety` enum (`NON-OVERLAPPING` or `OVERLAPS-WITH-slice-NNN` or `UNKNOWN-NO-HINT-FILES` or `UNKNOWN-NO-GRAPH`) computed against the union of expected blast-radius file sets for currently-active slices. New helper module `tools/slice_queue_writer.py` (~430 LOC; library API write_slice_queue + compute_parallel_safety + derive_active_slice_blast_radius + format_queue_md + _call_graphify_blast_radius + CLI; injection seam blast_resolver per slice-059 TVFS-1 / slice-063 NAW-1 precedent for deterministic regression testing). New OSDG-1-guarded prose Step 6.5 at `skills/slice/SKILL.md` L378-379 (after the mission-brief template close-fence, before `## Critical rules`); the helper invocation wrapped in try/except ImportError for bootstrap-defense (slice-067 itself + future PSQ-1-revert scenario covered by ONE idempotent mechanism per slice-066 idempotent-guard lesson). 4-value enum precedence (highest first): `UNKNOWN-NO-GRAPH` (graphify graph missing — caller-side override) then `UNKNOWN-NO-HINT-FILES` (candidate has empty hint_files set) then `OVERLAPS-WITH-slice-NNN` (non-empty intersection with one or more active slices) then `NON-OVERLAPPING` (non-empty hint_files, empty intersection — including the zero-active-slices case per AC4-(b)). Atomicity via `.tmp` sibling + `os.replace()` (must-not-defer #1); crash-durability via `os.fsync()` explicitly OUT OF SCOPE (queue is regenerable on every `/slice` invocation). 5-part PMI-1 atomic bump 0.68.0 → 0.69.0 (VERSION + plugin.yaml.version + pyproject.toml [project].version + `## v0.69.0` header + installed ~/.claude/ai-sdlc-VERSION); separate post-bump forward-sync obligations (NOT PMI-1 atomic legs per slice-063 M-add-1 leg-enumeration discipline + slice-067 /critique-review M-add-4 fix): installed ~/.claude/methodology-changelog.md MCFS-1 + installed ~/.claude/skills/slice/SKILL.md OSDG-1 + venv ai-sdlc-tools 0.69.0 TVFS-1 via `$PY -m pip install --upgrade .`. BC-PROJ-9 5-inventory fan-out for new tools/*.py module (plugin.yaml tools block + install_audit._CANONICAL_TOOLS + test_utf8_stdout._ROOT_ONLY_TOOLS + INSTALL.md 28 to 29 at L22 + L166 + this shippability row). BC-PROJ-10 paired-entry-pin functions: `test_v_0_69_0_psq_1_entry_present_in_repo` + `test_v_0_69_0_psq_1_shippability_consumer_propagation` per slice-066 v0.68.0 BRANCH-2 entry-pin precedent at `tests/methodology/test_methodology_changelog.py:4433` + `:4513`. Dual-Critic: first NEEDS-FIXES 2B/3M/3m (B1 BC-PROJ-10 paired-pin discipline gap on wiring-matrix test naming + missing sibling test + zero TF-1 entry-pin rows — textbook slice-064 B2 / slice-060 M-add-2 / N≥17 BC-PROJ-10:173 recurrence; B2 compute_parallel_safety enum drift + zero-active-AND-empty-hint collision rule unspecified; M1 BCR-1 round-trip non-trigger declaration prose-only — Builder OVERRIDDEN with rationale corrected per /critique-review SEVERITY-WRONG-ON-RATIONALE; M2 mid-slice smoke bootstrap-impossible — defense-in-depth dual-option fix; M3 SKILL.md insertion position unspecified; m1-m3 ADR-064 sibling enum drift + fsync gap + R-19 hedge). Meta-Critic EXTEND with 4 missed findings all Builder-fix-block-introduced regressions (M-add-1 AC6 violates ≤5-AC rule — documented deviation rationale added; M-add-2 Pre-finish gate "5 ACs" not updated to 6; M-add-3 design.md L21 "dogfood seed" residual after M2 fix; M-add-4 ADR-064 L50 5-part PMI-1 leg enumeration drift `venv ai-sdlc-tools` substituted for `## v0.69.0 header`). 12 dispositions ratified at TRI-1 (11 ACCEPTED-FIXED + 1 OVERRIDDEN); final verdict CLEAN. Branch state: WORKTREE=skip ratified per /build-slice prerequisite check — slice-067 surfaced a structural conflict between BRANCH-2 worktree-per-slice + gitignored `architecture/` (line 13 of .gitignore — `git worktree add` creates a worktree without slice vault files); user-ratified WORKTREE=skip per ADR-063 §Scope of supersession 4th-surface inheritance preservation (BRANCH-1 fallback path); slice-068+ nomination to formally reconcile (vault-copy step OR un-gitignore architecture/ OR symlink discipline). Slice-040 N+1 first-governed-slice doctrine extends to N=16 cumulative on BRANCH-2 surface. Bootstrap (slice-067 only): slice-067's own `/slice` invocation did NOT write `architecture/slice-queue.md` — helper does not exist at /slice time; first write at /build-slice Phase E mid-slice smoke. Regression = `/slice` Step 6.5 disappears or stops writing `architecture/slice-queue.md`; the helper module deletes or its tests regress; the 4-value Parallel-safety enum collapses to 2-value or 3-value missing UNKNOWN-NO-HINT-FILES; the precedence rule reorders silently; the atomic write reverts to non-atomic; the v0.69.0 entry's PSQ-1 / ADR-064 / Parallel-slice queue output / mints a new rule / 5-part PMI-1 atomic bump / Rule reference / NON-OVERLAPPING / UNKNOWN-NO-HINT-FILES / UNKNOWN-NO-GRAPH anchors silently lost; the BCR-1 traceability axis (this row cites BOTH PSQ-1 AND ADR-064 AND test_v_0_69_0_psq_1_entry_present_in_repo AND test_v_0_69_0_psq_1_shippability_consumer_propagation) silently severed by a future row rewrite; OR slice-068 PSQ-2 claim machinery extends the queue file format in a way that breaks PSQ-1's 5-field-per-entry minimum contract. | `<HOME>/.claude/.venv/Scripts/python.exe -m pytest tests/skills/slice/test_slice_queue_output.py tests/methodology/test_slice_skill_drift.py::test_in_repo_and_installed_slice_skill_md_are_content_equal tests/methodology/test_methodology_changelog.py::test_v_0_69_0_psq_1_entry_present_in_repo tests/methodology/test_methodology_changelog.py::test_v_0_69_0_psq_1_shippability_consumer_propagation --no-header -q` | <2s | `<interp> -m pytest tests/skills/slice/test_slice_queue_output.py tests/methodology/test_slice_skill_drift.py::test_in_repo_and_installed_slice_skill_md_are_content_equal tests/methodology/test_methodology_changelog.py::test_v_0_69_0_psq_1_entry_present_in_repo tests/methodology/test_methodology_changelog.py::test_v_0_69_0_psq_1_shippability_consumer_propagation --no-header -q` |
| 69 | slice-069-track-vault-in-git | ADR-066 vault-in-git philosophy (slice-069; mints new architectural philosophy; partial supersession of ADR-028 § Options-1 only — BCI-1 Decisions 1-3 remain accepted unchanged): `.gitignore architecture/` removed retiring N=4 cumulative gitignored-vault-vs-worktree class STRUCTURALLY (Phase H4 empirical proof: `git worktree add --detach ../slice-069-verify HEAD` propagated 634 files in `architecture/` with no `cp -r` DEVIATION). BC-PROJ-8 rule-content supersession revised live `architecture/build-checks.md:134-147` + canonical fixture `tests/methodology/fixtures/build_checks/canonical_project_checks.md:134-147` in lockstep narrowing trigger keywords 7-tuple to 6-tuple (drops `gitignored` token) preserving rule prescription unchanged; STP-1 Sub-form B docstring at `tools/state_transition_pin_audit.py:28-37` rationale reframed origin to preference preserving slice-044 historical anchor; `skills/code-review/SKILL.md` M5 INCLUDE direction admits `architecture/slices/*/{build-log,validation,reflection}.md` post-build artifacts to /code-review scope via 10 surgical exclusions per leg with `:(glob,exclude)architecture/*.md` magic for single-segment-match correctness (catches FNM_PATHNAME-absent fnmatch behavior caught mid-/code-review). PII redaction sweep scoped to `architecture/**` only (4 patterns covering OneDrive private-project absolute path standalone private-project name GitHub-handle home-directory absolute-path prefix) 36 files changed 141 substitutions verbatim corpus at `tests/methodology/fixtures/archive_backtest_corpus/` UNCHANGED per ADR-030 zero residuals final. 5-part PMI-1 atomic bump 0.69.0 to 0.70.0 (VERSION + plugin.yaml.version + pyproject.toml [project].version + `## v0.70.0` header + installed ~/.claude/ai-sdlc-VERSION). ADR-066 § Reversibility taxonomy NEW section promotes 3-class framework inline (cheap / expensive / irreversible with worked examples) replacing originally-fabricated ADR-019 citation per /critique M1 ACCEPTED-FIXED. 3-Critic stack N=6 cumulative (slice-063 through slice-069) code-Critic surfaces 2M+6m on CLEAN design+meta stack deferred to slice-070+ bundled cleanup nomination (19-finding backlog accumulating across slice-066/067/068/069 advisory findings). MEPD-1 INCLUDE posture; BC-PROJ-10 paired-pin tests `test_v_0_70_0_adr_066_entry_present_in_repo` + `test_v_0_70_0_adr_066_shippability_consumer_propagation` added at slice-071 / slice-069 M2 FIX completion in `tests/methodology/test_methodology_changelog.py` per slice-067/066/058 paired-pin precedent. Regression = `.gitignore` re-introduces `architecture/` line silently re-gitignoring the vault and re-opening N=4 class; BC-PROJ-8 trigger keywords silently revert to 7-tuple including `gitignored` token or rule prescription changes shape; STP-1 Sub-form B docstring rationale reverts to slice-044 it-was-gitignored framing losing post-slice-069 preference reframing; `skills/code-review/SKILL.md` Step 1 bash block reverts to catch-all `:(exclude)architecture/**` re-excluding the post-build artifacts M5 INCLUDE admits OR drops `:(glob)` magic on `architecture/*.md` re-introducing the over-match defect class; ADR-066 frontmatter or 6-section structure silently truncated; PMI-1 5-part atomic bump partial-completed (any of VERSION/plugin.yaml/pyproject.toml/methodology-changelog header/installed `~/.claude/ai-sdlc-VERSION` falls out of lockstep at v0.70.0); the v0.70.0 entry's ADR-066 / vault-in-git / `tracked in git` / `BC-PROJ-8 rule-content supersession` / `STP-1 Sub-form B docstring rationale reframed` / `skills/code-review/SKILL.md M5 INCLUDE direction` / `5-part PMI-1 atomic bump` anchors silently lost; OR the BCR-1 traceability axis (this row cites BOTH ADR-066 AND the revised BC-PROJ-8 6-tuple AND STP-1 Sub-form B docstring AND M5 INCLUDE `:(glob)` magic AND N=4 retirement empirical proof) silently severed by a future row rewrite. | `<HOME>/.claude/.venv/Scripts/python.exe -m pytest tests/methodology/test_build_checks_audit.py::test_bc_proj_8_has_expected_structural_identity tests/methodology/test_state_transition_pin_audit.py tests/skills/code_review/test_code_review_skill.py::test_skill_md_step_1_all_three_legs_share_filter_shape tests/methodology/test_code_review_skill_drift.py --no-header -q` | <2s | `<interp> -m pytest tests/methodology/test_build_checks_audit.py::test_bc_proj_8_has_expected_structural_identity tests/methodology/test_state_transition_pin_audit.py tests/skills/code_review/test_code_review_skill.py::test_skill_md_step_1_all_three_legs_share_filter_shape tests/methodology/test_code_review_skill_drift.py --no-header -q` |
| 70 | slice-070-fix-psq-1-blast-radius-dict-leak | SC-027 closure: `tools/slice_queue_writer.py::_call_graphify_blast_radius` resolves graphify node-object IDs via a `_build_id_to_path_map(graph_path)` lookup (graphify blast-radius CLI emits `{id, label, type:"", path:""}` with `path` structurally empty in this repo's graph; the real path lives in `graph.json`'s `source_file` field under matching `id`); rendered `Blast-radius:` cells in `architecture/slice-queue.md` contain only path-shaped tokens — every backtick-quoted token in any `Blast-radius:` cell value matches the positive-shape regex `^(?:[^\s\`]*[/\\][^\s\`]+\|[^\s\`]*\.[A-Za-z0-9]{1,8}\|\.[A-Za-z][A-Za-z0-9_.-]*)$` (contains a path separator OR ends with `.alphanum` extension 1-8 chars OR is a leading-dot dotfile like `.gitignore`). | `<HOME>/.claude/.venv/Scripts/python.exe -m pytest tests/bugs/test_psq_1_blast_radius_dict_leak.py --no-header -q` | <1s | `<interp> -m pytest tests/bugs/test_psq_1_blast_radius_dict_leak.py --no-header -q` |
| 68 | slice-068-add-vault-root-constant | VAULT_ROOT constant seam (slice-068; ADR-065 mints a new architectural pattern — env-var-overridable leading-underscore-helper path constant module; supersedes nothing; first rule on the vault-path-constant axis): introduces `tools/_vault_paths.py` exporting module-level `VAULT_ROOT: Path` defaulting to `Path("architecture")` overridable via env var `AI_SDLC_VAULT_ROOT` at module-import time; routes 8 `tools/*.py` filesystem-resolving `"architecture/"` literals through the constant (`build_checks_integrity.py:78` + `critique_review_prerequisite_audit.py:154` + `cross_spec_parity_audit.py:152,305,307,309` + `risk_register_audit.py:381` + `slice_queue_writer.py:79,80,442,525,584` + `state_transition_pin_audit.py:367` + `supersede_audit.py:171` + `validate_slice_layers.py:577`). Two-marker convention per `/critique-review` M-add-1 ACCEPTED-FIXED: 8 migrated sites carry `# VAULT_ROOT-routed (slice-068)` inline marker; 5 enumerated EXCLUDED error-message-string sites (`cross_spec_parity_audit.py:335` + `state_transition_pin_audit.py:374,388,400` + `validate_slice_layers.py:521`) carry `# NOT VAULT_ROOT-routed (slice-068) — error-message prose`. Read-at-import + consumer-freeze cascade is deliberate production-correctness contract per ADR-065 §Decision (env var read once per process at startup; downstream consumer constants freeze at their own import time; in-process monkeypatch of `tools._vault_paths.VAULT_ROOT` does NOT propagate to frozen consumers — pinned by `test_consumer_constants_are_frozen_at_first_import` per `/critique` M1 ACCEPTED-FIXED). Naming: leading-underscore-helper precedent from `tools/_stdout.py` / UTF8-STDOUT-1 (`tools/plugin_manifest_audit.py:148` `_list_actual_tools` filter auto-excludes from PMI-1 inventory). MEPD-1 EXCLUDE posture (NO `## v0.70.0` methodology-changelog entry + NO PMI-1 bump + NO consumer-propagation obligation): pure mechanical refactor mints no new rule, adds no audit surface, no SKILL.md/agent edit, no user-facing behavior change; ships at v0.69.0 unchanged. UNBLOCKS slice-069 (`rename-architecture-to-sdlc-and-track-in-git`) by reducing the rename blast radius from 14 files to 3 (1-line `_DEFAULT` flip + `.gitignore` edit + new philosophy ADR). DEFERRED per `/critique` M3 ACCEPTED-FIXED scope-back: `tests/methodology/conftest.py` + ~15 other tests/methodology/*.py files (~42 occurrences of `REPO_ROOT / "architecture"`) NOT migrated — preserves R-15 audit regex at `tests/methodology/test_resolve_slice_dir.py:233` UNCHANGED + avoids opening the first test-tree-to-tools-tree import dependency. Three-Critic stack: `/critique` 1B/3M/2m all ACCEPTED-FIXED + `/critique-review` 6 CONFIRMED + 1 missed M-add-1 ACCEPTED-FIXED + 1 severity adjustment ACCEPTED-FIXED + `/code-review` advisory 1M/4m (M1 + m1 + m2 + m3 + m4) advisory per CRSI-1 v1 walking-skeleton — bundled to SC-028 backlog entry for follow-on test-quality hardening. m2 SC-NNN backlog round-trip: SC-027 filed for slice-067 PSQ-1 raw-dict-leak per `/critique-review` severity adjustment medium/medium. Slice-067 N=3 → slice-068 N=4 cumulative gitignored-vault-vs-worktree conflict (architecture/, diagnose-out/, graphify-out/ cp -r DEVIATION at /build-slice Phase E) — structural fix is slice-069 un-gitignore. Three-Critic complementarity validated at N=2 cumulative (slice-067 N=1 + slice-068 N=2): design-Critic catches conceptual+citation, meta-Critic catches Builder-fix-block-sweep gaps, code-Critic catches test-quality+regex+spec-vs-code drift. Bootstrap (slice-068 only): _MIGRATION_SITE_ALLOWLIST 8-element frozenset pinned by `test_migration_site_allowlist_pinned` (catches any new tool module hardcoding `Path("architecture")` without migrating; would have caught the rev-1 grep's missed `build_checks_integrity.py:78` site per `/critique` B1 lesson). Regression = `tools/_vault_paths.py` deleted OR `VAULT_ROOT` constant renamed/restructured without sweeping the 8 consumer imports; OR `_MIGRATION_SITE_ALLOWLIST` 8-element frozenset diverges from the actual 8 importers (a new `tools/*.py` module added that hardcodes `Path("architecture")` without migrating, OR a current allowlist member stops importing VAULT_ROOT); OR the read-at-import semantic flipped to lazy (would break `test_consumer_constants_are_frozen_at_first_import` freeze contract); OR the two-marker convention silently regressed (a new EXCLUDED site added without the `# NOT VAULT_ROOT-routed` marker would fail `test_no_orphan_architecture_literal_in_migrated_tools`); OR the BCR-1 traceability axis (this row cites BOTH VAULT_ROOT AND ADR-065 AND `_MIGRATION_SITE_ALLOWLIST` AND `test_migration_site_allowlist_pinned`) silently severed by a future row rewrite. | `<HOME>/.claude/.venv/Scripts/python.exe -m pytest tests/methodology/test_vault_root_constant.py --no-header -q` | <1s | `<interp> -m pytest tests/methodology/test_vault_root_constant.py --no-header -q` |
| 71 | slice-071-bundle-066-to-070-code-critic-cleanup | 31-finding bundled code-Critic cleanup across slices 066-070 (closes SC-028 — slice-068 test_vault_root_constant.py hardening bundle): `tools/slice_queue_writer.py` 7 source FIXes (slice-070 M1 4-step canonical path-normalization algorithm via `_discover_known_repo_roots` + `_normalize_abs_to_repo_relative` + slice-070 M2 module-level `_PATH_SHAPED_RE` constant verbatim-imported from `tests/bugs/test_psq_1_blast_radius_dict_leak.py` + slice-070 M3 PRIMARY `_is_path_shaped` filter at `_node_to_path` + slice-070 M4 `types.MappingProxyType` wrap of `_build_id_to_path_map` return + slice-070 m4 `id_to_path` build moved post-JSON-parse + slice-070 m5 `_FORWARD_COMPAT_PATH_KEYS` module-level constant + slice-067 m1 `write_slice_queue out_path` kwarg + main() collapse); `tools/branch_workflow_audit.py` 4 source FIXes (slice-066 M1 gitdir-depth + .git-existence guards in `_is_repo_root_a_worktree` + slice-066 m1 `AuditResult.worktree_skip_used` + `worktree_skip_rationale` symmetry surface + slice-066 m3 `import os` module-level hoist + slice-069 m5 `--detach HEAD` docstring codification); `skills/build-slice/SKILL.md` 2 source FIXes (slice-066 M2 `wt_base` via `git rev-parse --show-toplevel` + slice-066 m4 PowerShell-portability parenthetical) + OSDG-1 forward-sync to ~/.claude/skills/build-slice/SKILL.md; `tests/methodology/test_vault_root_constant.py` SC-028 bundle (slice-068 M1 `test_vault_paths_module_is_leaf` AST-walk test + slice-068 m1 idempotency 4-shape regex expansion + slice-068 m2 freeze-pin 3-step assertion + slice-068 m3 `test_two_marker_convention_asymmetry_documented` sentinel with VERBATIM-aligned docstring substrings per /critique-review M-add-1 ACCEPTED-FIXED + test count 10→12); `tools/supersede_audit.py` slice-068 m4 PEP-8 blank line; `methodology-changelog.md` retroactive in-place TEXT corrections to existing `## v0.70.0` entry per MEPD-1 EXCLUDE / SUP-1 §Append-only-of-DECISIONS-not-of-FACTS interpretation (slice-069 M1 `_SECRET_PATTERNS` count "9 → 10" + slice-069 m1 vault file count "~633 → 644 files / 65504 insertions" + slice-069 m2 PII narrative "113/9/1 → 36 files / 141 substitutions" + slice-069 m3 600-word headline paragraph split); `ADR-066-track-vault-in-git.md` count "9 → 10" at L49 + L59 + L80 (slice-069 M1 cross-artifact propagation); `.gitignore` comment-block consolidation (slice-069 m4); `tests/methodology/test_methodology_changelog.py` BC-PROJ-10 paired-pin completion (slice-069 M2 — `test_v_0_70_0_adr_066_entry_present_in_repo` + `test_v_0_70_0_adr_066_shippability_consumer_propagation`); `architecture/shippability.md` row #69 updated to cite both BC-PROJ-10 paired-pin test function names; `tests/bugs/test_psq_1_blast_radius_dict_leak.py` 6 paired tests (test-side `_PATH_SHAPED_RE` import + autouse `cache_clear()` fixture + 5 `patch.object(subprocess, "run", ...)` → `patch("tools.slice_queue_writer.subprocess.run", ...)` SUT-bound + `test_unknown_cell_is_documented_sentinel_not_an_offender` + `test_call_graphify_blast_radius_falls_back_to_from_flag_on_node_not_found` + `test_forward_compat_path_keys_docstring_in_sync_with_constant` drift-prevention test + 4 positive + 3 negative parametrized regex cases); `tests/methodology/test_branch_workflow_audit.py` slice-066 m2 fixture rewrite with APED-1 load-bearing negative assertion + `test_audit_result_surfaces_worktree_skip_fields`; `tests/skills/slice/test_slice_queue_output.py` `test_main_cli_custom_output_uses_canonical_write_path`. **3-Critic stack N=8 cumulative** (slice-063 → slice-071) — first-Critic BLOCKED 2B/6M/4m all VALID per meta-Critic + meta-Critic EXTEND 4 missed (M-add-1 M6 docstring/assertion verbatim mismatch RSAD-1 recursive + M-add-2 M5 Phase B1/B2/B3 ordering residual + m-add-1 m4 promise-not-landed + m-add-2 5-site count drift); user-ratified TRI-1 NEEDS-FIXES (14 ACCEPTED-FIXED in-band + 2 ACCEPTED-PENDING for /build-slice — m1 WIRE-1 verification + m4 drift-prevention test both discharged at /build-slice); code-Critic 0B/0M/5m (m1-m5; AC#5 "0 new structural Majors" SATISFIED; all 5 deferred to slice-072+ bundled-cleanup nomination per CRSI-1 v1 walking-skeleton voluntary-restraint precedent N=7 cumulative). MEPD-1 EXCLUDE posture (no NEW v0.71.0 entry; no PMI-1 bump; no new ADR; ships at v0.70.0 unchanged); voluntary-restraint discipline N=12 cumulative (slice-037/046/050/052/055/056/057/061/065/067/070/071). 9 new test functions + ~5 modifications of existing tests; pytest 966/966 PASS (was 950 at slice-070 baseline; +16 net); shippability 70/70 PASS; 14 Step-6 audits 22 clean + 2 Important defer-with-rationale (BC-1 BC-GLOBAL-2 N=3 cumulative; LINT-MOCK-1 13 inherited). BCR-1 round-trip: `**Closes:** SC-028` sentinel → `- **Addressed:** slice-071-bundle-066-to-070-code-critic-cleanup on 2026-05-26` line appended to both worktree's + main-tree's `diagnose-out/backlog.md` SC-028 block (gitignored content dual-tree replication per slice-070 reflection L80 lesson). **Discovered**: R-20 cp-r tax pattern (N=6 cumulative; user-flagged "we need a better solution"; 4 candidate fix classes enumerated; slice-072+ active nomination). **Slice-040 N+1 first-governed-slice doctrine extends to N=18 cumulative** (first 31-finding bundled cleanup at 10× slice-065 reference-precedent scale; surfaces R-20 visibility at mid-slice smoke). Regression = any of the 28 FIX dispositions silently regresses (any of the 9 new test functions disappears OR returns expected-PASS on wrong contract); OR the per-cluster paired build-phase order shape silently reverts to two-pass test-first ordering breaking M-add-2 fix; OR the `_PATH_SHAPED_RE` module constant silently drifts from the test-side regex breaking slice-070 M2 single-source-of-truth seam; OR the `_FORWARD_COMPAT_PATH_KEYS` module constant silently drifts from its docstring breaking slice-070 m5 drift-prevention test; OR the `_normalize_abs_to_repo_relative` 4-step algorithm silently degrades to single-step `.relative_to()` breaking slice-070 M1 worktree-absolute-path fix; OR the BCR-1 traceability axis (this row cites BOTH SC-028 closure AND the 9 new test functions AND R-20 discovery AND the 3-Critic stack N=8 cumulative) silently severed by a future row rewrite. | `<HOME>/.claude/.venv/Scripts/python.exe -m pytest tests/bugs/test_psq_1_blast_radius_dict_leak.py tests/methodology/test_vault_root_constant.py tests/methodology/test_branch_workflow_audit.py tests/skills/slice/test_slice_queue_output.py tests/methodology/test_methodology_changelog.py::test_v_0_70_0_adr_066_entry_present_in_repo tests/methodology/test_methodology_changelog.py::test_v_0_70_0_adr_066_shippability_consumer_propagation --no-header -q` | <3s | `<interp> -m pytest tests/bugs/test_psq_1_blast_radius_dict_leak.py tests/methodology/test_vault_root_constant.py tests/methodology/test_branch_workflow_audit.py tests/skills/slice/test_slice_queue_output.py tests/methodology/test_methodology_changelog.py::test_v_0_70_0_adr_066_entry_present_in_repo tests/methodology/test_methodology_changelog.py::test_v_0_70_0_adr_066_shippability_consumer_propagation --no-header -q` |
| 73 | slice-073-add-rebase-and-conflict-discipline | PSQ-3 parallel-slice queue rebase-and-conflict discipline (slice-073; ADR-068 mints a new rule; supersedes nothing; sibling on the parallel-slice family axis to PSQ-1 / ADR-064 + PSQ-2 / ADR-067 + BRANCH-2 / ADR-063; first rule on the rebase-discipline axis adjacent to but NOT extending the queue-output / claim-machinery / worktree-isolation axes). Inserts a NEW sub-step 2.5 in `skills/commit-slice/SKILL.md` Step 5b (`--merge` sub-mode) between existing sub-step 2 (commit on slice branch) and sub-step 3 (default-branch resolution + checkout + no-ff merge). Sub-step 2.5 resolves default via canonical 2-step pattern (primary `git symbolic-ref refs/remotes/origin/HEAD` + fallback `git config init.defaultBranch`; STOP exit 2 if neither resolves per NAW-1 ADR-061 §Decision exit-2 contract verbiage) then runs `git rebase <default>` on the slice branch (slice branch stays checked out — `git rebase <upstream>` with no `<branch>` arg does NOT switch branches per git-scm.com/docs/git-rebase). Three outcome paths: Fast-forward no-op (default has not advanced since slice's branch-point; `git rebase` exits 0 without replay; proceed to sub-step 3); Clean replay (default has advanced + slice replays cleanly atop new default tip — common second-to-merge case; proceed to sub-step 3); Conflict-STOP (print `git status --porcelain` U-prefixed entries + `git rebase --abort` recovery hint + SOAD-1 3-option ask — first SOAD-1 invocation in /commit-slice; the other 6 confirmation sites at SKILL.md L173/L175/L202/L203/L205/L255 remain raw `(yes/no)` since they are binary confirmations). Scope is `--merge` only — `--push` (sub-mode a) is out of scope (PR-based workflows delegate rebase to GitHub's PR merge-queue / `gh pr merge --rebase` / reviewer-side rebase); `--sync-after-pr` (sub-mode d) is out of scope (post-PR-merge cleanup where rebase is moot — PR already merged on GitHub; local default fast-forwards via `git pull --ff-only`). NO new tool module per ADR-068 §Options-#3 — `git rebase` itself IS the runtime gate (no-op fast-forward when at-tip; clean exit 0 when behind without conflict; non-zero with U-prefixed conflicting files via `git status --porcelain` when behind with conflict); a separate audit-tool would duplicate `git rebase`'s own behavior. Structural-pin tests on SKILL.md prose mirror the existing `test_commit_slice_skill_merge_flag.py::test_commit_slice_skill_md_specifies_no_ff_merge_and_safe_local_branch_delete` precedent — `--merge`'s `git merge --no-ff` invocation is pinned via prose-pin rather than a separate merge-audit module. Cross-family precedent in parallel-slice rule family is mixed: PSQ-1 + PSQ-2 minted new tool modules (queue write + claim CLI library APIs); PSQ-3 has neither — its entire surface is one extra `git rebase` invocation in SKILL.md prose. 5-part PMI-1 atomic bump 0.71.0 → 0.72.0 (VERSION + plugin.yaml.version + pyproject.toml [project].version + `## v0.72.0` header + installed `~/.claude/ai-sdlc-VERSION`). BC-PROJ-10 paired-pin tests: `test_v_0_72_0_psq_3_entry_present_in_repo` + `test_v_0_72_0_psq_3_shippability_consumer_propagation` per slice-072 v0.71.0 PSQ-2 + slice-067 v0.69.0 PSQ-1 + slice-066 v0.68.0 BRANCH-2 entry-pin precedent. The structural-pin test module `test_commit_slice_skill_rebase_flag.py` houses the 5 prose-pin tests (test_step_5b_contains_git_rebase_invocation + _rebase_precedes_no_ff_merge + _rebase_target_resolved_via_canonical_2_step + _conflict_stops_with_porcelain_u_entries + _conflict_surfaces_git_rebase_abort_hint) — see test module for full assertion shapes. Test-first plan: all 5 structural-pin tests authored WRITTEN-FAILING before SKILL.md edit (4 actually failed; 1 — _rebase_target_resolved_via_canonical_2_step — passed structurally because the canonical 2-step default-branch resolution literal already exists at the pre-existing sub-step 3 and PSQ-3 sub-step 2.5 reuses it). 3-Critic stack N=10 cumulative (slice-063 through slice-073) — first-Critic 6B/6M/4m all VALID + meta-Critic EXTEND 6 missed findings (2 Major + 4 minor) ALL VALID; 22/22 dispositions ACCEPTED-FIXED at TRI-1; final verdict CLEAN. Builder-fix-block-introduces-N+1-regressions pattern extends to N=7 cumulative (slice-062/064/067/070/071/072/073); canonical M-add-1 instance: Builder applied M4's slice-064-precedent retirement at design.md L156 but missed two ADR-068 sites carrying the same defect class. Adversarial model per ADR-068 §Adversarial model: cooperative coordination NOT a security boundary; a malicious local actor with filesystem write to the worktree can bypass any methodology rule by editing files directly. SOAD-1 first-instance in /commit-slice: PSQ-3 introduces the first SOAD-1 structured-options ask in skills/commit-slice/SKILL.md (the other 6 confirmation sites remain raw yes/no — binary confirmations don't need SOAD-1; retrofitting is scope-creep tracked as a future /critic-calibrate candidate). Regression = the `git rebase` invocation silently removed from Step 5b sub-step 2.5 (PSQ-3's behavior surface drops); OR the rebase ordering inverts (rebase appears AFTER `git merge --no-ff` rather than before — mis-rebase the wrong direction); OR the canonical 2-step default-branch resolution drifts between sub-step 2.5 and sub-step 3 (rebase target ≠ merge target footgun); OR the conflict-STOP block silently auto-resolves (`-X theirs` / `-X ours` / merge driver introduced — violates /commit-slice Critical rules); OR the SOAD-1 3-option ask silently degrades to raw `(yes/no)` (convention asymmetry inverts); OR the `git rebase --abort` recovery hint disappears (user stranded in rebase-in-progress state); OR the `U-prefixed` entries language disappears (conflict-STOP enumeration becomes ambiguous); OR scope silently expands to `--push` and/or `--sync-after-pr` (PSQ-4+ work prematurely shipped); OR the v0.72.0 entry's PSQ-3 / ADR-068 / `rebase-and-conflict` / `mints a new rule` / `5-part PMI-1 atomic bump` / `Rule reference` / `git rebase` / `git rebase --abort` / `Step 5b` / `--merge` anchors silently lost; OR BC-PROJ-10 paired-pin test function names cited in this row are silently severed from the test module (catalog-row-to-test traceability broken); OR the 5 structural-pin test functions in `test_commit_slice_skill_rebase_flag.py` lose their `_step_5b_section` scoping (regression to non-section-scoped substring match where a stray Step 5c/5d mention could satisfy the assertions). | `<HOME>/.claude/.venv/Scripts/python.exe -m pytest tests/methodology/test_commit_slice_skill_rebase_flag.py tests/methodology/test_methodology_changelog.py::test_v_0_72_0_psq_3_entry_present_in_repo tests/methodology/test_methodology_changelog.py::test_v_0_72_0_psq_3_shippability_consumer_propagation --no-header -q` | <2s | `<interp> -m pytest tests/methodology/test_commit_slice_skill_rebase_flag.py tests/methodology/test_methodology_changelog.py::test_v_0_72_0_psq_3_entry_present_in_repo tests/methodology/test_methodology_changelog.py::test_v_0_72_0_psq_3_shippability_consumer_propagation --no-header -q` |
| 72 | slice-072-add-psq-2-claim-machinery | PSQ-2 parallel-slice queue claim machinery (slice-072; ADR-067 mints a new rule; supersedes nothing; sibling on the parallel-slice family axis to PSQ-1; first rule on the claim-machinery axis adjacent to BRANCH-2 worktree-isolation + PSQ-1 queue-output): adds optional `Claimed-by: <git user.name> <user.email>` + `Claimed-at: <ISO-8601 UTC>` field lines per entry on `architecture/slice-queue.md` (additive schema extension on PSQ-1's stable on-disk contract per ADR-064 § Consequences); new `tools/slice_queue_claim.py` CLI provides `--claim` / `--release` / `--force-claim` / `--queue` flags with mutually-exclusive group; identity read via `git config user.name + user.email` with 3-case exit-code-based absence detection (returncode==1 unset / returncode==0 + empty stdout configured-empty / other non-zero git-tool-error all map to loud exit 2; no anonymous fallback per AC2); `tools/slice_queue_writer.write_slice_queue` modified to read existing claims via `parse_queue_text` at regen time and merge them onto candidates whose names survive into the new top-10 (silently discards claims on dropped candidates per AC4); `_format_entry` inserts the 2 claim lines AFTER `Risk-retired:` AND BEFORE the trailing empty-line entry-separator (index minus-two per Critic m1 ACCEPTED-FIXED); `_extra_field_lines` list pass-through preserves unknown PSQ-3+ forward-compat field lines verbatim on roundtrip (per Critic M3 + meta-Critic m-add-2 ACCEPTED-FIXED); writes use explicit `Path.write_text(..., newline="")` for LF-only byte-deterministic emission on Windows (per Critic M1 — modelcontextprotocol/python-sdk#2433); CRLF-tolerant reads via `text.replace("\r\n", "\n")` before parsing. Retires R-19 mitigating-to-retired (claim machinery makes queue freshness a hint not a load-bearing collision-safety signal). 5-part PMI-1 atomic bump 0.70.0 to 0.71.0 (VERSION + plugin.yaml.version + pyproject.toml [project].version + `## v0.71.0` header in methodology-changelog + installed `~/.claude/ai-sdlc-VERSION`). BC-PROJ-9 5-inventory fan-out for new tools/*.py module (plugin.yaml tools block + install_audit._CANONICAL_TOOLS + INSTALL.md L22 + L166 tool-count literals 29 to 30 + this shippability row). BC-PROJ-10 paired-entry-pin functions: `test_v_0_71_0_psq_2_entry_present_in_repo` + `test_v_0_71_0_psq_2_shippability_consumer_propagation` per slice-067 v0.69.0 PSQ-1 + slice-066 v0.68.0 BRANCH-2 entry-pin precedent. Identity model git-identity-only per ADR-067 § Options considered Option 1 + § Identity model implications; ADR-064 L37 + R-19 mid-2026-05 mitigation prose both anticipated session-id detection but ADR-067 explicitly rules it out — predecessor-spec drift preserved verbatim per slice-040 R-10 retirement-precedent (NOT supersession; ADR-064 append-only). 14/14 ACCEPTED-FIXED at TRI-1 (11 first-Critic + 3 meta-Critic missed including M-add-1 TPHD-1 sub-mode (a) N=6 cumulative intra-design.md L18-19 stale anchor on AC5-to-AC6 split fix-block). AC count 6 documented deviation from <=5-AC rule per slice-067 N=1 to slice-072 N=2 promotion of the AC-count pattern signal per Critic M4 ACCEPTED-FIXED. Adversarial model per ADR-067 § Adversarial model: cooperative coordination NOT a security boundary; a malicious local actor with filesystem write to slice-queue.md can forge any claim. Regression = the `Claimed-by:` or `Claimed-at:` field lines silently get reordered or moved before `Risk-retired:` or after the trailing blank (entry-block separation breaks); `parse_queue_text` returns 3-arg dict signature instead of 4-arg with `_extra_field_lines` (forward-compat preservation loses unknown PSQ-3+ lines); `apply_claim` `force=False` on already-claimed silently overwrites instead of refusing; `apply_release` on missing-candidate silently no-ops instead of exit 2 typo-rejection; `read_git_config_user` silently falls back to `$USER` or anonymous on absent git config (loses ownership-identity guarantee); `write_slice_queue` regen overwrites instead of merging claims (loses preservation across /slice Step 6.5 calls); the `newline=""` on `.tmp` write reverts to default (Windows CRLF translation re-introduces byte-equal claim-preservation round-trip flakiness); the v0.71.0 entry's PSQ-2 / ADR-067 / `claim machinery` / `mints a new rule` / `5-part PMI-1 atomic bump` / `Rule reference` / `Claimed-by` / `Claimed-at` / `git config user` / `R-19` anchors silently lost; BC-PROJ-10 paired-pin test function names cited in this row are silently severed from the test module (catalog-row-to-test traceability broken). | `<HOME>/.claude/.venv/Scripts/python.exe -m pytest tests/methodology/test_psq_2_claim_machinery.py tests/methodology/test_utf8_stdout_regression.py::test_slice_queue_claim_survives_cp1252_with_u2192 tests/methodology/test_methodology_changelog.py::test_v_0_71_0_psq_2_entry_present_in_repo tests/methodology/test_methodology_changelog.py::test_v_0_71_0_psq_2_shippability_consumer_propagation --no-header -q` | <2s | `<interp> -m pytest tests/methodology/test_psq_2_claim_machinery.py tests/methodology/test_utf8_stdout_regression.py::test_slice_queue_claim_survives_cp1252_with_u2192 tests/methodology/test_methodology_changelog.py::test_v_0_71_0_psq_2_entry_present_in_repo tests/methodology/test_methodology_changelog.py::test_v_0_71_0_psq_2_shippability_consumer_propagation --no-header -q` |
| 75 | slice-076-add-pcr-1-conflict-diagnostic-and-soft-regen | PCR-1 parallel-conflict-resolution v1 — diagnostic + SOFT-class auto-regen at `/commit-slice --merge` Step 5b sub-step 2.5 (slice-076; [[ADR-069]] mints a new rule on a NEW family axis `PCR-N` (parallel-conflict-resolution), sibling to PSQ-N (parallel-slice-queue) but distinct layer — PSQ-3 = detect conflict; PCR-1 = resolve SOFT conflicts BEFORE PSQ-3's existing SOAD-1 STOP fires; supersedes nothing; methodology v0.73.0; MEPD-1 posture INCLUDE; 5-part PMI-1 atomic bump 0.72.0 → 0.73.0; BC-PROJ-9 5-inventory bump 30 → 31 for new tool module `tools/parallel_conflict_resolver.py`). Inserts a NEW PCR-1 dispatch paragraph in `skills/commit-slice/SKILL.md` Step 5b sub-step 2.5 (the PSQ-3 conflict-outcome path) BEFORE the existing SOAD-1 3-option STOP block — strictly additive, preserves existing 3-option SOAD-1 ask + `git rebase --abort` recovery hint verbatim. Invokes `python -m tools.parallel_conflict_resolver --resolve-soft --json` to attempt SOFT-class auto-resolution; branches on the resolver's JSON `action` field + exit code: exit 0 + APPLIED → log breadcrumb + proceed to sub-step 3 (skip SOAD-1); exit 0 + STOP (VAULT_CLAIM / HARD / MIXED) → print full-detail diagnostic from `--diagnose --json` (concerned slices + blast-radius + claim history + commit times + mission-brief links per U-file) + fall through to existing SOAD-1; exit 1 (UNKNOWN — APED-1 fail-closed criterion: NEVER silent-default to SOFT) → print stderr + fall through to SOAD-1; exit 2 (malformed inputs) → print stderr + fall through to SOAD-1. **5-class taxonomy** per ADR-069 § Decision: **SOFT** (all U-files in 2-member `_SOFT_FILE_SET` = `{architecture/slice-queue.md, architecture/shippability.md}` AND no same-candidate-different-identity claim collision) → auto-regen via `_overlay_claims_on_queue_text` (textual claim-overlay on stage `:3:` per /critique-review M-add-1 ACCEPTED-FIXED option 2; NO `write_slice_queue` round-trip avoids B1 phantom-parser trap) + row-union merge by leading pipe-NN-pipe slice number prefix for shippability.md + `git add` + `git rebase --continue` + audit-log append — shipped in **this slice**; **VAULT_CLAIM** (sole U-file slice-queue.md + same-candidate-different-identity claim — per /critique B4 gate) → STOP fail-closed; deferred to slice-077 (PCR-2) for timestamp-winner + light Critic; **HARD** (any source / ADR / SKILL.md / `_index.md` / `methodology-changelog.md` U-file — any path not in 2-member SOFT-set and not pure-claim VAULT_CLAIM diff) → STOP fail-closed; deferred to slice-077 for full `/critique` + `/critique-review` + TRI-RESOLVE-1 Critic stack; **MIXED** (SOFT + non-SOFT U-files coexist; both SOFT+HARD and SOFT+VAULT_CLAIM combos per /critique M4 ACCEPTED-FIXED disambiguation) → STOP fail-closed per atomicity; deferred to slice-077; **UNKNOWN** (empty u_files despite rebase-in-progress OR both stages missing OR git-state unexpected) → STOP fail-closed per APED-1 silent-disable / default-off-on-malformed criterion; shipped fail-closed behavior in this slice. **`_index.md` is NOT in SOFT** — `/archive`'s regen is Haiku-LLM-dispatched per COST-1 (not deterministic; PCR-1 cannot reproduce Haiku's lessons-block synthesis); falls to HARD. **`methodology-changelog.md` is NOT in SOFT** — PMI-1 5-leg atomic-bump risk on concurrent bumps means subtly inconsistent merged entries; falls to HARD. **NEW helper module** `tools/parallel_conflict_resolver.py` (~600 LOC across 11 functions + `_SoftResolutionError` + 2 utility helpers): library API `diagnose_conflict(repo_root)` → frozen `ConflictDiagnostic` (`u_files: tuple[str, ...]` forward-slash strings per /critique M1 + `concerned_slices: dict[str, tuple[ConcernedSlice, ...]]` + `claim_history: tuple[ClaimEntry, ...]`) + `classify_conflict(diag) -> ConflictClass` (pure-logic 5-way classifier with VAULT_CLAIM gate via `_has_same_candidate_different_identity`) + `resolve_soft_conflict(diag, repo_root=None)` (optional repo_root kwarg defaulting to `Path.cwd()` for testability; early-return STOP for non-SOFT + try/except `_SoftResolutionError` wrap + subprocess git add + git rebase --continue + best-effort `_append_audit_log`); private helpers `_extract_u_files` (subprocess `git status --porcelain`, forward-slash strings, parses UU/AU/UA/DU/UD/AA/DD), `_git_show_stage`, `_derive_concerned_slices` (active-slice `mission-brief.md` scan with last-commit ISO via `git log -1 --format=%cI <slice-branch>`), `_extract_claim_diff` (lazy-import `parse_queue_text` from `tools.slice_queue_claim`), `_regen_slice_queue` (overlay on `:3:` with `:2:` fallback per design.md edge case (a); `_SoftResolutionError(UNKNOWN)` on both-stages-missing), `_overlay_claims_on_queue_text` (surgical line-level overlay tracking current `### candidate` — emits `### heading` → tracks `current_candidate` → skips existing `- **Claimed-by:**` / `- **Claimed-at:**` lines under overlay candidates → emits `Risk-retired:` line → appends new `- **Claimed-by:** <value>` + `- **Claimed-at:** <value>` lines; drops orphan candidates absent from queue_text per design.md L137), `_merge_shippability` (regex row-key by leading pipe-NN-pipe slice number prefix + row-union across stages + same-slice-different-content escalates to HARD-class `_SoftResolutionError` per design.md Edge-cases column + sort ascending + prelude preserved verbatim), `_parse_shippability_rows` returning `(numbered_rows_dict, prelude_lines)`, `_merge_claim_dicts` (newest-`Claimed-at`-wins union), `_append_audit_log` (lazy-create at `architecture/parallel-conflict-resolution-log.md` with `_AUDIT_LOG_HEADER` + ISO-8601 UTC timestamp + `git rev-parse HEAD` SHA + U-files CSV + concerned-slices CSV + per-file resolution action lines; best-effort per design.md error model — write failures log to stderr but do NOT block resolution; race-acceptance per ADR-067 cooperative-not-adversarial); CLI `main()` with mutually-exclusive flag group `--diagnose` / `--classify` / `--resolve-soft` plus `--json` plus `--repo-root` flags + `_to_jsonable` helper for dataclass/enum→dict conversion; exit codes per design.md (0 on success or coherent STOP classification; 1 on UNKNOWN; 2 on malformed argparse — argparse's mutually_exclusive_group required=True prevents this). **NEW audit log** `architecture/parallel-conflict-resolution-log.md` (lazy-created on first PCR-1 soft-conflict auto-resolution; append-only; mirrors `architecture/critic-calibration-log.md` pattern). Each entry: ISO-8601 UTC timestamp + repo HEAD SHA pre-resolution + U-files list + concerned-slices map + per-file resolution actions. BC-PROJ-10 paired-pin tests: `test_v_0_73_0_pcr_1_entry_present_in_repo` + `test_v_0_73_0_pcr_1_shippability_consumer_propagation` per slice-073 v0.72.0 PSQ-3 + slice-072 v0.71.0 PSQ-2 + slice-067 v0.69.0 PSQ-1 entry-pin precedent. BC-PROJ-9 5-inventory fan-out: plugin.yaml tools block + `tools/install_audit.py::_CANONICAL_TOOLS` + INSTALL.md L22 + L166 tool-count literals 30 → 31 (TWO-SITE pin per /critique M6 ACCEPTED-FIXED; `test_parallel_conflict_resolver_in_canonical_tools_plugin_manifest_install_md_at_l22_and_l166`) + `tests/methodology/test_utf8_stdout_regression.py::_ROOT_ONLY_TOOLS` (PCR-1 helper uses `--repo-root` with no positional slice arg → root-only bucket per slice_queue_writer precedent) + this shippability row #75. **Test scaffold**: 28 PCR-1 tests across 9 new test files in `tests/skills/parallel_conflict_resolver/` (test_soft_file_set.py + test_diagnose_conflict.py + test_classify_conflict.py + test_overlay_claims_on_queue_text.py + test_resolve_soft_conflict.py + test_audit_log.py + test_cli.py) + 4 new methodology test files (test_pcr_1_adr_present.py + test_pcr_1_taxonomy_documented.py + test_commit_slice_skill_pcr_1_diagnostic.py + test_parallel_conflict_resolver_tool_inventory.py) + 3 v0.73.0 entry-pin tests appended to test_methodology_changelog.py. Phase-A R-21 latent bug fix (`### R-21` h3 → `## R-21` h2 — RR-1 audit's H2-only regex misparsed R-21's `Status: open` as belonging to R-20, masking R-20's `retired` status) surfaced + fixed in-band at Phase B. Phase-B test-fixture format mismatch (`**Claimed-by**:` colon-outside-bold vs real queue's `- **Claimed-by:**` per `slice_queue_writer.py:647`) corrected in-band at Phase C. R-21 newly discovered (open): "SOFT auto-regen produces semantically-different content from manual-resolve baseline at a corner case" per /critique-review m5 ACCEPTED-PENDING + ADR-069 § Reversibility anticipated-failure-mode discipline; candidate fix classes deferred to slice-077+ on empirical refutation. **Adversarial model** per ADR-069: cooperative coordination NOT a security boundary; a malicious local actor with filesystem write to the worktree can bypass any methodology rule by editing files directly. Regression = the `python -m tools.parallel_conflict_resolver --resolve-soft --json` dispatch silently removed from Step 5b sub-step 2.5 (PCR-1 SOFT-class auto-resolution returns to manual SOAD-1 STOP per parallel merge); OR the PCR-1 dispatch silently inverts to AFTER the SOAD-1 ask (user prompted before resolution had a chance to run); OR `_SOFT_FILE_SET` silently expands beyond `{slice-queue.md, shippability.md}` (auto-merge surface grows to files whose merge semantics are NOT mechanical); OR `_SOFT_FILE_SET` silently shrinks (re-opens the 5-session parallel-slice deadlock); OR the VAULT_CLAIM gate via `_has_same_candidate_different_identity` silently drops the cross-branch-stage comparison (claim-collision races silently auto-resolve via PSQ-2's newest-wins instead of escalating); OR `_overlay_claims_on_queue_text` silently regresses from line-level claim-only overlay to whole-text rewrite (loses non-claim entry preservation); OR `_merge_shippability` silently allows same-slice-number-different-content auto-resolution (HARD escalation lost); OR UNKNOWN class silent-defaults to SOFT (APED-1 silent-disable violation); OR the 5-class taxonomy SOFT/VAULT_CLAIM/HARD literals silently disappear from ADR-069 (taxonomy contract lost); OR the v0.73.0 entry's PCR-1 / ADR-069 / `parallel-conflict-resolution` / `mints a new rule` / `5-part PMI-1 atomic bump` / `Rule reference` anchors silently lost; OR BC-PROJ-10 paired-pin test function names cited in this row are silently severed from the test module (catalog-row-to-test traceability broken); OR the 28 PCR-1 test functions in `tests/skills/parallel_conflict_resolver/` + `tests/methodology/test_pcr_1_*` + `test_commit_slice_skill_pcr_1_diagnostic.py` + `test_parallel_conflict_resolver_tool_inventory.py` lose their PCR-1 invariant coverage. | `<HOME>/.claude/.venv/Scripts/python.exe -m pytest tests/skills/parallel_conflict_resolver tests/methodology/test_pcr_1_adr_present.py tests/methodology/test_pcr_1_taxonomy_documented.py tests/methodology/test_commit_slice_skill_pcr_1_diagnostic.py tests/methodology/test_parallel_conflict_resolver_tool_inventory.py tests/methodology/test_methodology_changelog.py::test_v_0_73_0_pcr_1_entry_present_in_repo tests/methodology/test_methodology_changelog.py::test_v_0_73_0_pcr_1_shippability_consumer_propagation tests/methodology/test_methodology_changelog.py::test_version_files_synchronized_at_v_0_77_0 --no-header -q` | <3s | `<interp> -m pytest tests/skills/parallel_conflict_resolver tests/methodology/test_pcr_1_adr_present.py tests/methodology/test_pcr_1_taxonomy_documented.py tests/methodology/test_commit_slice_skill_pcr_1_diagnostic.py tests/methodology/test_parallel_conflict_resolver_tool_inventory.py tests/methodology/test_methodology_changelog.py::test_v_0_73_0_pcr_1_entry_present_in_repo tests/methodology/test_methodology_changelog.py::test_v_0_73_0_pcr_1_shippability_consumer_propagation tests/methodology/test_methodology_changelog.py::test_version_files_synchronized_at_v_0_77_0 --no-header -q` |
| 77 | slice-077-enhance-pulse-with-worktree-awareness | Pulse worktree-awareness (slice-077; [[ADR-070]] mints no methodology rule per MEPD-1 EXCLUDE — per-skill enhancement bounded to `/pulse`; ships at v0.73.0 unchanged with no PMI-1 bump): closes the post-slice-076 witnessed-gap class (R-22) where `/pulse` read `architecture/slices/<active>/milestone.md` from the main tree only, mis-reporting stage + raising false-positive vault-forward-population drift flag when a BRANCH-2 worktree held a fully-built/validated/reflected slice. New helper `tools/pulse_worktree_resolver.py` (~430 LOC, 4 frozen dataclasses incl. WorktreeState enum / WorktreeInfo / WorktreeStateClassification; library API `detect_active_worktrees(repo_root)` + `classify_worktree_state(worktree, default_branch, repo_root)` + `should_suppress_vault_forward_population_flag(detected, installed_home)` + `augment_pulse_state_dict(base, worktrees, classifications)` + CLI `--detect` / `--classify <slice-NNN>` / `--json` / `--repo-root`). 4-state taxonomy per ADR-070: `IN_PROGRESS` (stage != reflect; regardless of ancestry) / `BUILT_BUT_NOT_MERGED` (stage == reflect + HEAD NOT ancestor of default) / `MERGED` (HEAD IS ancestor of default; transient cleanup-candidate WARN) / `UNKNOWN` (fail-closed with 8 enumerated sub-reasons — `fresh-worktree-no-milestone` / `milestone-missing-in-active-and-archive` / `milestone-frontmatter-malformed` / `detached-head` / `dirty-worktree` / `merge-base-error` / `head-unresolvable` / `slice-folder-name-drift`; mirrors PCR-1 UNKNOWN fail-closed semantics per ADR-069). `skills/pulse/SKILL.md` Step 1 + Step 2 + Step 3 prose augmentations: Step 1 inserts `git worktree list --porcelain` + `tools.pulse_worktree_resolver --detect --json` mandatory pre-read BEFORE existing main-tree milestone.md read (worktree's milestone is canonical for slice-NNN's state during BRANCH-2 worktree window); Step 2 inserts 3-level override-precedence paragraph + full 4×CAL-1 precedence table + Step-2-deterministic-not-Step-3-Haiku location anchor (the override is computed main-thread; Haiku consumes the resolved `recommended_next_action_override` from the augmented state-dict); Step 3 inserts vault-forward-population suppression paragraph (3-surface all-match + content-equal-modulo-EOL per ADR-033/EOL-DRIFT-1; UNKNOWN WARN-not-silent policy). Override-precedence ordering (worktree-state > CAL-1 cadence-overdue > stage-derived per ADR-070 § Override-precedence). Drift-flag suppression predicate: all-match across `{~/.claude/methodology-changelog.md, ~/.claude/ai-sdlc-VERSION, ~/.claude/skills/pulse/SKILL.md}` × worktree's `{methodology-changelog.md, VERSION, skills/pulse/SKILL.md}` — any divergence disqualifies (fail-closed against partial-sync); EOL-tolerant per CAD-1/OSDG-1 carve-out. Reuses `tools.branch_workflow_audit._resolve_default_branch` (single-sourced; NAW-1 ADR-061 exit-2 contract preserved). Cross-spec parity with PCR-1 sibling helper `tools/parallel_conflict_resolver.py`: argparse mutually-exclusive group required=True + `--repo-root` parse-time default `Path(".")` + post-parse `args.repo_root.resolve()` (per /critique-review M-add-1 ACCEPTED-FIXED — earlier draft incorrectly claimed PCR-1 used `Path(".").resolve()` at parse time) + JSON keys `{action, error}` on stderr / `{action, ...}` on stdout + Exit 0/1/2 + `_stdout.reconfigure_stdout_utf8()` first line of `main()`. BC-PROJ-9 5-inventory fan-out: `plugin.yaml` tools block + `tools/install_audit.py::_CANONICAL_TOOLS` + INSTALL.md L22 + L166 tool-count literals 31 → 32 (TWO-SITE pin per slice-076 M6 precedent; `test_pulse_worktree_resolver_in_canonical_tools_plugin_manifest_install_md_at_l22_and_l166`) + `tests/methodology/test_utf8_stdout_regression.py::_ROOT_ONLY_TOOLS` (helper uses `--repo-root` no positional slice arg → root-only bucket per slice-067/PCR-1 precedent) + this shippability row #77. R-22 minted at /design (mirrors slice-076 R-21 registration precedent; flips to retired at /reflect). 3-Critic stack N=12 cumulative (slice-063 → slice-077) — first-Critic 4B/9M/6m + meta-Critic 3 missed (M-add-1 PCR-1 default + M-add-2 R-22 RR-1 vocabulary + M-add-3 N=3 parser-count framing) all ACCEPTED-FIXED at TRI-1; final verdict CLEAN. Test scaffold: 20 tests across 7 test modules (test_pulse_skill_drift.py + test_pulse_skill_worktree_awareness.py + test_pulse_worktree_resolver_tool_inventory.py + tests/skills/pulse/{test_classify_worktree_state, test_drift_flag_suppression, test_detect_active_worktrees, test_cli, test_state_dict_shape}.py). MEPD-1 EXCLUDE rationale (per ADR-070 § Honest precedent inspection): structurally similar to slice-076 PCR-1 (helper + 5-inventory + multiple test modules) BUT PWA would be skill-internal (no cross-skill ABI) whereas PCR-1's SOFT taxonomy + classify_conflict are called from skills/commit-slice/SKILL.md Step 5b. The differentiator is load-bearing cross-skill contract, not ships-a-helper-module. Slice-058 (ADR-only mint, no rule) is the closer rule-axis precedent. Promotion trigger: N=2 cross-skill consumers of WorktreeState emerge (`/drift-check` or `/build-checks` need similar worktree-awareness). Regression = `_resolve_milestone_path` silently regresses to scanning main-tree's filesystem instead of the worktree's (re-opens R-22 witnessed gap); OR `WorktreeState` taxonomy silently drops UNKNOWN sub-reasons or collapses 4-state to 3-state (fail-closed contract lost); OR override-precedence ordering inverts (CAL-1 supersedes worktree-state); OR drift-flag suppression predicate silently allows any-match instead of all-match (re-opens partial-sync false-positive class); OR suppression predicate silently drops EOL-tolerance carve-out (CRLF/LF noise becomes false drift); OR the v0.73.0 PCR-1 sibling-helper cross-spec parity convention drifts (parallel-slice-family-parity-audit slice would flag); OR the 20 PCR-1-class test functions in tests/methodology/test_pulse_* + tests/skills/pulse/* lose their PWA invariant coverage. | `<HOME>/.claude/.venv/Scripts/python.exe -m pytest tests/methodology/test_pulse_skill_drift.py tests/methodology/test_pulse_skill_worktree_awareness.py tests/methodology/test_pulse_worktree_resolver_tool_inventory.py tests/skills/pulse/ --no-header -q` | <3s | `<interp> -m pytest tests/methodology/test_pulse_skill_drift.py tests/methodology/test_pulse_skill_worktree_awareness.py tests/methodology/test_pulse_worktree_resolver_tool_inventory.py tests/skills/pulse/ --no-header -q` |
| 78 | slice-078-add-pcr-2a-vault-claim-resolver | PCR-2a parallel-conflict-resolution v2a vault-claim sub-mechanism (slice-078; [[ADR-071]] mints a new rule on the existing `PCR-N` family axis sibling to PSQ-N; refines PCR-1 / [[ADR-069]]'s class taxonomy in-place; supersedes nothing; methodology v0.74.0; MEPD-1 posture INCLUDE; 5-part PMI-1 atomic bump 0.73.0 → 0.74.0). Closes the VAULT_CLAIM-class auto-resolution gap PCR-1 left open: `tools/parallel_conflict_resolver.py` gains `resolve_vault_claim_conflict(diag, repo_root=None)` public API + `_VaultClaimDispatch` sentinel exception + 4 file-local helpers (`_collect_same_candidate_different_identity` returning collision list — `_has_same_candidate_different_identity` becomes thin `bool(...)` wrapper preserving the PCR-1 classify_conflict consumer API; `_select_timestamp_winner` strict-newer with tie-STOP; `_parse_queue_candidates_for_replacement` regex-based file-order-preserving parser per /critique B1 ACCEPTED-FIXED — `tools/slice_queue_writer.py` exposes no public parser AND `tools.slice_queue_claim.parse_queue_text` explicitly strips `Parallel-safety` per docstring at L230-234; `_pick_loser_replacement(queue_text, exclude_names)` in-memory text input per /critique-review M-add-2 ACCEPTED-FIXED — disk-read during VAULT_CLAIM rebase-in-progress would encounter git's `<<<<<<<` / `=======` / `>>>>>>>` conflict markers around the U-file and silently return `none-available`) + `_format_vault_claim_audit_entry` (`## Vault-claim resolution - <ISO>` section heading with UNIFORM hyphen-space separator per /critique M2 ACCEPTED-FIXED — section-type distinguished at prefix word `Vault-claim` vs `Soft-conflict`, NOT at trailing dash decoration; avoids Windows smart-dash autocorrect fragility) + 2 modified call sites (`resolve_soft_conflict` L242 gains new VAULT_CLAIM dispatch branch ABOVE the existing `if cls is not ConflictClass.SOFT:` guard per /critique B3 ACCEPTED-FIXED — without this the CLI `--resolve-soft` path short-circuits VAULT_CLAIM to STOP before reaching the defense-in-depth backstop; `_regen_slice_queue` L627-638 raise leg swaps from `_SoftResolutionError(VAULT_CLAIM)` to `_VaultClaimDispatch()` sentinel caught by `resolve_soft_conflict`'s exception loop + rerouted to `resolve_vault_claim_conflict`; UNKNOWN-class raise leg at L605-609 UNTOUCHED; `_append_audit_log` dispatches on `result.conflict_class` to either the existing SOFT-row formatter or the new VAULT_CLAIM formatter — single-open `O_APPEND` lazy-create pattern reused verbatim). **7-step Resolution algorithm** per design.md § Resolution algorithm: (1) collect collisions — STOP on multi or empty; (2) select strict-newer winner — STOP on tie per honest framing of `tools/slice_queue_claim.py:629-633` second-precision `_now_iso8601_utc()` `~1-second tie window`; (3) read stage 2/3 baselines + overlay winner identity onto baseline (`_overlay_claims_on_queue_text` writes winner regardless of stage origin — without this `_regen_slice_queue:614` `baseline_text = text_3 if text_3 else text_2` would silently demote a stage-2 winner per /critique M4 ACCEPTED-FIXED winner-in-stage-2 case) + defensive post-overlay `re.search(rf'### {candidate}.*?Claimed-by:\\*\\* {winner.claimed_by}', overlaid, re.DOTALL)` verification per /critique-review M-add-1 ACCEPTED-FIXED — STOP on silent-drop; (4) `_pick_loser_replacement(queue_text=overlaid, exclude_names={winner.candidate_name})` read-only loser-replacement from in-memory overlaid text; (5) atomic write + `git add` + `git rebase --continue` via existing stage-then-commit pattern + STOP on subprocess failure; (6) best-effort `_append_audit_log` (OSError stderr-printed without changing APPLIED result); (7) return APPLIED. **SKILL.md L185-192 prose edits** per /critique M3 ACCEPTED-FIXED APED-1 structural-pin discipline: Pin #1 — L185 dispatch paragraph gains VAULT_CLAIM-in-APPLIED-context sentence (co-occurs with `auto-resolves`); Pin #2 — L192 closing summary DROPS `VAULT_CLAIM` from the multi-class `(class1)(...) + HARD (...)` fall-closed enumeration. Cooperative-not-adversarial threat model carried from [[ADR-067]] / [[ADR-069]] § Threat model. R-22 retired by slice-077 (pulse worktree-awareness); R-23 newly opened — "Cross-machine clock-skew in PCR-2a strict-newer rule producing wrong-winner-by-staleness" (low likelihood / low impact / reversibility cheap; corrigibility hook = audit log records both Claimed-at timestamps for forensic skew detection; remediation candidates = monotonic claim sequence number queue candidate `add-claim-sequence-number-for-clock-skew-detection` OR PCR-2b Critic-stack adjudication). BC-PROJ-10 paired-pin tests: `test_v_0_74_0_pcr_2a_entry_present_in_repo` + this row's catalogued failing repro `test_pcr_2a_repro_vault_claim_gate_closed.py::test_vault_claim_gate_closed_returns_resolution_result`. **3-Critic stack N=13 cumulative** (slice-063 through slice-078) — first-Critic 3B/4M/9m = 16 findings (B1 parser mismatch / B2 single-direction repro / B3 CLI dispatch site / M1 tie-rate honest framing / M2 em-dash vs hyphen / M3 SKILL.md APED-1 pin literal / M4 write semantics winner-in-stage-2 / m1-m9 minors) — 14 ACCEPTED-FIXED + 1 OVERRIDDEN (m3 HEAD-SHA field forensic value preserved for SOFT/VAULT_CLAIM/HARD section-type symmetry) + 1 DEFERRED (m4 L192 cosmetic wording → `bundle-074-075-077-code-critic-cleanup`) + 1 ACCEPTED-PENDING (m9 R-23 + queue candidate added during /build-slice Phase F); meta-Critic EXTEND (0 SUSPICIOUS, 2 MISSED, 0 SEVERITY-WRONG — M-add-1 Minor overlay-silently-dropped + M-add-2 Major `_pick_loser_replacement` signature change to in-memory text input — both ACCEPTED-FIXED in same fix block per TPHD-1 sub-mode (b)); 18/18 dispositions ratified at TRI-1 2026-05-29 user accept-all; final verdict NEEDS-FIXES (m9 ACCEPTED-PENDING applies during build; 16 ACCEPTED-FIXED already applied to artifacts). Self-validating-slice property N=2 (extends slice-077) — slice-078's own /commit-slice --merge may exercise VAULT_CLAIM auto-resolution if a peer claims concurrently; cooperative-acceptable corner case. Regression = the `resolve_vault_claim_conflict` public API silently disappears or its strict-newer-wins behavior inverts to first-claim-wins (contradicts the `_merge_claim_dicts` newest-Claimed-at-wins precedent at PCR-1 L648-666); OR the `resolve_soft_conflict` L242 VAULT_CLAIM dispatch branch silently disappears (CLI `--resolve-soft` short-circuits VAULT_CLAIM to STOP — re-opens the closed gate); OR the `_regen_slice_queue` L627-638 sentinel raise silently reverts to `_SoftResolutionError(VAULT_CLAIM)` (defense-in-depth defeats); OR the UNKNOWN-class raise leg at L605-609 is silently removed (fail-closed contract lost); OR `_pick_loser_replacement` signature reverts to disk-read `(repo_root, exclude_names)` shape (M-add-2 regression — silently returns `none-available` during VAULT_CLAIM rebase-in-progress; re-opens AC#1 silent-degradation class); OR the defensive post-overlay `re.search` verification silently disappears (M-add-1 regression — overlay silent-drop ships stale claim); OR the strict-newer `Claimed-at` comparison silently switches direction (`e2.claimed_at > e3.claimed_at` becomes `<`); OR the audit-row section heading silently switches to em-dash `## Vault-claim resolution — ` (M2 regression — section-type sentinel collapses to 1-char Unicode distinction); OR the L185 SKILL.md dispatch paragraph silently drops the VAULT_CLAIM-in-APPLY co-occurrence (Pin #1 regression); OR L192 SKILL.md closing summary silently re-adds VAULT_CLAIM to the `(class1)(...) + HARD (...)` multi-class fall-closed enumeration (Pin #2 regression); OR the BC-PROJ-9 5-inventory drifts (no new tool module shipped this slice — `parallel_conflict_resolver` already exists; tool count stays 32); OR the v0.74.0 entry's PCR-2a / ADR-071 / `parallel-conflict-resolution` / `mints a new rule` / `5-part PMI-1 atomic bump` / `Rule reference` anchors silently lost; OR BC-PROJ-10 paired-pin test function names cited in this row are silently severed from the test module (catalog-row-to-test traceability broken); OR the 29 PCR-2a test functions across 5 new test modules (`tests/methodology/test_pcr_2a_vault_claim_resolver.py` 20 tests + `test_pcr_2a_regen_slice_queue_dispatch.py` 3 + `test_commit_slice_skill_vault_claim_dispatch.py` 3 + `test_parallel_conflict_resolution_log_vault_claim.py` 2 + `test_pcr_2a_repro_vault_claim_gate_closed.py` 1) lose their PCR-2a invariant coverage. | `<HOME>/.claude/.venv/Scripts/python.exe -m pytest tests/methodology/test_pcr_2a_vault_claim_resolver.py tests/methodology/test_pcr_2a_regen_slice_queue_dispatch.py tests/methodology/test_commit_slice_skill_vault_claim_dispatch.py tests/methodology/test_parallel_conflict_resolution_log_vault_claim.py tests/methodology/test_pcr_2a_repro_vault_claim_gate_closed.py tests/methodology/test_methodology_changelog.py::test_v_0_74_0_pcr_2a_entry_present_in_repo --no-header -q` | <3s | `<interp> -m pytest tests/methodology/test_pcr_2a_vault_claim_resolver.py tests/methodology/test_pcr_2a_regen_slice_queue_dispatch.py tests/methodology/test_commit_slice_skill_vault_claim_dispatch.py tests/methodology/test_parallel_conflict_resolution_log_vault_claim.py tests/methodology/test_pcr_2a_repro_vault_claim_gate_closed.py tests/methodology/test_methodology_changelog.py::test_v_0_74_0_pcr_2a_entry_present_in_repo --no-header -q` |
| 79 | slice-079-bundle-074-075-077-078-code-critic-cleanup | build-slice SKILL.md prose-fix cluster (slice-079 Fix A+B+C+D+E+F per design.md §"Components touched"; bundled-cleanup discharge of slice-074 M1/m1/m2/m3/m4/m5 = 6 deferred code-Critic v1 advisory findings + P1.1 source-pending duplicate of slice-074 M1; MEPD-1 EXCLUDE — conformance / cleanup-discharge class, no new RULE-ID, no methodology-changelog entry, no VERSION bump; precedent class N=7 cumulative via slice-040/043/045/057/061/071 per design.md §"Decisions made (ADRs)" MEPD-1(b) discharge). Six surgical edits at `skills/build-slice/SKILL.md`: (A) extract `default=` + `repo_root=` + `wt_base=` variable assignments from point 1's codefence into a shared pre-amble ABOVE the numbered list — closes slice-074 M1 / P1.1 variable-scope footgun where point 4's codefence references vars defined only inside point 1's codefence body (4 numbered points are mutually-exclusive branches; the dirty-tree branch did NOT execute point 1's codefence); (B) replace `git add <scaffolding files>` placeholder at L78 with concrete pathspec `git add architecture/slices/slice-NNN-<slice-name>/ architecture/slice-queue.md` — closes slice-074 m1 placeholder underspecification; (C) tighten `test_cp_r_lines_use_if_then_guard_for_source_dir_absence` assertion from `len(matches) >= 2` to `len(matches) == 4` exact count — closes slice-074 m2 silent-count-divergence-permitting `>=2` test; (D) add 2-line docstring example to `point_4_no_dash_b_pattern` regex naming negative-lookahead's "before `#`" comment-exclusion scope — closes slice-074 m3 regex re-readability concern; (E) extract `_branch_state_section` byte-equivalent duplicated function to NEW shared helper `tests/methodology/_skill_parse_helpers.py` consumed by `test_build_slice_skill_cp_r_step.py` + `test_build_slice_skill_dirty_tree_resolution.py` — closes slice-074 m4 DRY violation in test corpus; (F) `tests/methodology/test_r_20_retired.py` replaces `subprocess.run(..., check=True)` with explicit returncode + stderr-in-failure-message — closes slice-074 m5 subprocess-error-surface debuggability concern. OSDG-1 lock-step forward-sync of `skills/build-slice/SKILL.md` to `~/.claude/skills/build-slice/SKILL.md` at Phase C7 per CAD-1 byte-equality modulo EOL discipline. Regression = the variable-scope pre-amble silently regresses to inline-in-point-1 (M1 footgun returns); OR the `git add` placeholder silently regresses to `<scaffolding files>` prose-only form OR `git add .`/`git add -A` over-broad form (m1 returns); OR the cp-r exact-count test silently relaxes back to `>=2` permitting silent count drift (m2 returns); OR the `_branch_state_section` helper is silently duplicated back into the 2 test modules (m4 DRY returns); OR `test_r_20_retired.py` silently re-introduces `check=True` masking stderr (m5 debuggability regresses); OR the OSDG-1 lock-step forward-sync drifts CRLF/LF byte-equality (mini-CAD-1 fails). | `<HOME>/.claude/.venv/Scripts/python.exe -m pytest tests/methodology/test_build_slice_skill_cp_r_step.py tests/methodology/test_build_slice_skill_dirty_tree_resolution.py tests/methodology/test_r_20_retired.py tests/methodology/test_skill_parse_helpers.py tests/methodology/test_build_slice_skill_branch_state_preamble.py --no-header -q` | <2s | `<interp> -m pytest tests/methodology/test_build_slice_skill_cp_r_step.py tests/methodology/test_build_slice_skill_dirty_tree_resolution.py tests/methodology/test_r_20_retired.py tests/methodology/test_skill_parse_helpers.py tests/methodology/test_build_slice_skill_branch_state_preamble.py --no-header -q` |
| 80 | slice-079-bundle-074-075-077-078-code-critic-cleanup | commit-slice SKILL.md test-pin fix (slice-079 Fix G per design.md §"Components touched"; bundled-cleanup discharge of slice-075 m1 latent narration-leakage RSAD-1 sub-class). One surgical edit at `tests/methodology/test_commit_slice_skill_merge_wt_clean_preflight_ordering.py`: replace `_extract_substep_2_1_block`'s unanchored `section.find("2.1.")` substring extraction with line-start anchored extraction (`re.search(r'^2\.1\.\s', section, re.MULTILINE).start()` for `two_one_pos` + matching shape for `two_five_pos`) + post-extraction guard `assert block.count("silent-WT-discard") == 1, "narration leakage detected"` — closes slice-075 m1 latent RSAD-1 sub-class N=3-cumulative-on-slice-075 where post-fix `section.find("2.1.")` returns the L169 narration paragraph offset (1131) NOT the actual sub-step 2.1. body marker (offset 1812), widening extracted block to include 681-char narration prefix where a future refactor removing silent-WT-discard from the actual sub-step body but leaving narration intact would still PASS the existing assertion. Skill-prose surface: `skills/commit-slice/SKILL.md` Step 5b sub-step 2.1. UNCHANGED (the fix is in the test pin, not the prose). Regression = the line-start anchor silently regresses to `.find("2.1.")` substring (narration-leakage returns); OR the post-extraction `block.count("silent-WT-discard") == 1` guard silently drops (narration-leakage class re-opens). | `<HOME>/.claude/.venv/Scripts/python.exe -m pytest tests/methodology/test_commit_slice_skill_merge_wt_clean_preflight_ordering.py --no-header -q` | <1s | `<interp> -m pytest tests/methodology/test_commit_slice_skill_merge_wt_clean_preflight_ordering.py --no-header -q` |
| 81 | slice-079-bundle-074-075-077-078-code-critic-cleanup | pulse_worktree_resolver.py fix cluster (slice-079 Fix K+L+M+N per design.md §"Components touched"; bundled-cleanup discharge of slice-077 m5 design→code translation gap + m6 bare-repo edge + m8 UTF-8 BOM + m9 stage:-prefix-match fragility). Four surgical edits at `tools/pulse_worktree_resolver.py`: (K) add module-level constant `_UNKNOWN_REASON_WARN_TEMPLATES: Mapping[str, str]` keyed by the canonical 8 `_UNKNOWN_REASONS` per `pulse_worktree_resolver.py:77-86` (`fresh-worktree-no-milestone` / `milestone-missing-in-active-and-archive` / `milestone-frontmatter-malformed` / `detached-head` / `dirty-worktree` / `merge-base-error` / `head-unresolvable` / `slice-folder-name-drift`) — MAP-ONLY shape per /critique-review B1+M1 ACCEPTED-FIXED; NO new public helper, NO new JSON state-dict field, NO change to CLI text-mode emission; slice-077 m5 design→code translation gap closure via option (a) Haiku-side prose-interpretation consumer reading the constant at SKILL.md Drift & flags prose-interpretation time; MEPD-1 EXCLUDE preserved (no new contract surface); (L) `_parse_worktree_porcelain` detects bare-repo via `"bare" in block[0]` AND-only per slice-077 m6 original prescription — closes slice-077 m6 bare-repo edge case; `detect_active_worktrees` returns `tuple()` + WARN to stderr for bare-repo; no behavior change for non-bare; (M) `_parse_milestone_stage` strips UTF-8 BOM (`﻿` prefix) before `text.startswith("---")` check — closes slice-077 m8 PowerShell-saved-milestone.md-with-BOM-silently-returns-malformed-UNKNOWN class; (N) replace `stripped.startswith("stage:")` prefix-match with `stripped.split(":", 1)[0].strip() == "stage"` exact-key match — closes slice-077 m9 `stage_owner:`/`stage-history:` prefix-collision fragility. Cross-spec parity: B1 keys byte-equal to `_UNKNOWN_REASONS` tuple per APED-1 clause-5 grep at /critique time + /critique-review M-add-1 + Builder Phase E verification. Regression = the 8 canonical `_UNKNOWN_REASONS` keys in `_UNKNOWN_REASON_WARN_TEMPLATES` silently drift to a different tuple (consumer SKILL.md lookup falls through to fallback on every call; defeats fix); OR a 9th key silently appears in `_UNKNOWN_REASONS` without paired template (test invariant `set(_UNKNOWN_REASON_WARN_TEMPLATES.keys()) == set(_UNKNOWN_REASONS)` fails); OR the MAP-ONLY constant grows a new public helper `format_unknown_warn` + new state-dict key `"unknown_warn"` (M1 contract widening returns; would require ADR); OR `_parse_worktree_porcelain` silently regresses to OR-logic between `worktree` key absence + `bare` field presence (m2 m2 returns); OR `_parse_milestone_stage` silently regresses to non-BOM-tolerant startswith check (m8 returns); OR `stage:` exact-key match silently regresses to prefix `.startswith()` (m9 prefix-collision fragility returns). | `<HOME>/.claude/.venv/Scripts/python.exe -m pytest tests/skills/pulse/test_unknown_warn_templates.py tests/skills/pulse/test_detect_active_worktrees_bare_repo.py tests/skills/pulse/test_classify_worktree_state.py tests/skills/pulse/test_parse_milestone_stage_bom_tolerance.py --no-header -q` | <2s | `<interp> -m pytest tests/skills/pulse/test_unknown_warn_templates.py tests/skills/pulse/test_detect_active_worktrees_bare_repo.py tests/skills/pulse/test_classify_worktree_state.py tests/skills/pulse/test_parse_milestone_stage_bom_tolerance.py --no-header -q` |
| 82 | slice-079-bundle-074-075-077-078-code-critic-cleanup | pulse tests cleanup cluster (slice-079 Fix H+I+J per design.md §"Components touched"; bundled-cleanup discharge of slice-077 M2 pytest.skip-silent-defect + m3 unused-pytest-import + m4 unused-WorktreeStateClassification-import). Three surgical edits across pulse test modules: (H) `tests/skills/pulse/test_cli.py::test_cli_classify_json_returns_state_for_given_slice` drops the `pytest.skip("classify returned error...")` branch at L90-105 + configures the synthetic repo's default branch deterministically (`_git("config", "init.defaultBranch", "master")` + `_git("symbolic-ref", "HEAD", "refs/heads/master")` + `_git("update-ref", "refs/remotes/origin/HEAD", "refs/remotes/origin/master")`) + ADDS separate explicit `test_cli_classify_returns_error_on_unresolvable_default_branch` exercising the stderr/exit-1 fallback path — closes slice-077 M2 pytest-skip-masking-load-bearing-assertion class per Hendrickson "skipped tests are not passing tests; they are absent tests"; (I) drops unused `import pytest` from `tests/skills/pulse/test_classify_worktree_state.py:15` + `tests/skills/pulse/test_detect_active_worktrees.py:14` — closes slice-077 m3; (J) drops unused `WorktreeStateClassification` from `tests/skills/pulse/test_classify_worktree_state.py:17-22` import tuple — closes slice-077 m4. NEW structural-pin module `tests/methodology/test_pulse_tests_have_no_unused_imports.py` uses AST walker to assert no `pytest` / `WorktreeStateClassification` import without consumer in the pulse test corpus. Regression = `pytest.skip` silently re-introduced in CLI classify test (M2 silent-defect class returns); OR the synthetic-repo default-branch configuration silently drops (M2 silent-skip class returns under fresh-shell CI conditions); OR the `test_cli_classify_returns_error_on_unresolvable_default_branch` paired test silently disappears (stderr/exit-1 path silent-uncovered); OR `pytest` / `WorktreeStateClassification` imports silently re-introduced without consumer (m3/m4 returns); OR the AST-walker structural-pin silently degrades to substring match (allows mis-categorized imports). | `<HOME>/.claude/.venv/Scripts/python.exe -m pytest tests/skills/pulse/test_cli.py tests/skills/pulse/test_classify_worktree_state.py tests/skills/pulse/test_detect_active_worktrees.py tests/methodology/test_pulse_tests_have_no_unused_imports.py --no-header -q` | <2s | `<interp> -m pytest tests/skills/pulse/test_cli.py tests/skills/pulse/test_classify_worktree_state.py tests/skills/pulse/test_detect_active_worktrees.py tests/methodology/test_pulse_tests_have_no_unused_imports.py --no-header -q` |
| 83 | slice-079-bundle-074-075-077-078-code-critic-cleanup | parallel_conflict_resolver PCR-2a fix cluster (slice-079 Fix O+P+Q+R per design.md §"Components touched" + /critique-review M-add-1 ACCEPTED-FIXED signature-citation correction; bundled-cleanup discharge of slice-078 m1+m2+m3+m5 + M-add-1 design-meta signature-shape accuracy). Four surgical edits at `tools/parallel_conflict_resolver.py`: (O) extend `_format_vault_claim_audit_entry`'s signature from actual pre-fix 4-arg `(diag, result, timestamp, head_sha) -> str` (per code reality at `tools/parallel_conflict_resolver.py:923-928`; corrected from design.md's pre-meta-fix 2-arg mis-citation per /critique-review M-add-1) to 6-arg `(diag, result, timestamp, head_sha, winner: ClaimEntry | None, loser: ClaimEntry | None) -> str`; winner/loser computed inside `_append_audit_log`'s existing scope near L1270-1277 alongside the existing `timestamp` + `head_sha` computations, BEFORE the VAULT_CLAIM-branch dispatch at L1277; `_append_audit_log` PUBLIC 3-arg surface UNCHANGED — single-source-of-computation preserved (DRY goal of Fix O satisfied without widening `_append_audit_log`'s public boundary); `(unavailable)` defensive fallback kept with `# pragma: no cover` marker + docstring rationale per /critique M2 ACCEPTED-FIXED — closes slice-078 m1 audit-formatter re-derivation DRY violation; (P) `_parse_queue_candidates_for_replacement._flush()` default changes from `"UNKNOWN-NO-GRAPH"` to new `"MISSING-FIELD"` sentinel distinct from PSQ-1 enumeration — closes slice-078 m2 sentinel-overload; downstream filter at `_pick_loser_replacement` rejects both equivalent (semantic-preserving observability gain); (Q) `resolve_vault_claim_conflict`'s Step 5 gains 5-line atomicity docstring naming single-file blast-radius argument (`"Single-file scope → inline write acceptable; PCR-1's pending_writes batching addressed multi-file partial-resolution windows that don't apply here"`) — closes slice-078 m3 atomicity-asymmetry documentation gap; (R) `_VaultClaimDispatch` sentinel exception gains 2-line inline comment after docstring (`# Sibling to _SoftResolutionError; both inherit directly from Exception — catch-order in resolve_soft_conflict's exception loop is independent (not load-bearing by inheritance)`) — closes slice-078 m5 catch-order-clarity concern. Regression = `_format_vault_claim_audit_entry` signature silently regresses to 4-arg dropping winner/loser (formatter re-derives from claim_history; m1 DRY violation returns); OR `_append_audit_log` 3-arg public surface silently widens with winner/loser params (M1 contract widening; would require ADR); OR `(unavailable)` defensive branch silently deletes without `# pragma: no cover` marker (M2 pragma-pick ambiguity returns); OR `_parse_queue_candidates_for_replacement` default silently reverts to `"UNKNOWN-NO-GRAPH"` (m2 sentinel-overload returns; observability gain lost); OR Step 5 atomicity docstring silently drops `"Single-file scope"` + `"PCR-1's pending_writes"` substrings (m3 documentation gap returns); OR `_VaultClaimDispatch` sentinel comment silently disappears (m5 catch-order ambiguity returns). | `<HOME>/.claude/.venv/Scripts/python.exe -m pytest tests/methodology/test_pcr_2a_audit_formatter_signature.py tests/methodology/test_pcr_2a_parse_queue_missing_field_sentinel.py tests/methodology/test_pcr_2a_step_5_atomicity_docstring.py tests/methodology/test_pcr_2a_vault_claim_dispatch_comment.py --no-header -q` | <2s | `<interp> -m pytest tests/methodology/test_pcr_2a_audit_formatter_signature.py tests/methodology/test_pcr_2a_parse_queue_missing_field_sentinel.py tests/methodology/test_pcr_2a_step_5_atomicity_docstring.py tests/methodology/test_pcr_2a_vault_claim_dispatch_comment.py --no-header -q` |
| 84 | slice-079-bundle-074-075-077-078-code-critic-cleanup | slice_queue_writer.py UTF-8 structural-pin (slice-079 Fix S per design.md §"Components touched" + /critique B2+M3 ACCEPTED-FIXED REFRAMED; P3.10 source-pending discharge via regression-guard not root-cause fix). One NEW structural-pin test module `tests/methodology/test_slice_queue_writer_utf8_encoding.py::test_no_encoded_io_site_lacks_encoding_kwarg` (AST-walks `tools/slice_queue_writer.py` for every `Path.{read,write}_text(...)` / `open(...)` / `subprocess.run(..., text=True, ...)` call + asserts each carries `encoding="utf-8"` keyword arg). Empirical APED-1 grep at /critique time (B2 evidence per /critique-review confirmation) demonstrated all 9 encoded-I/O sites at `tools/slice_queue_writer.py:134, 183, 214, 266, 351, 450, 703, 790, 847` ALREADY carry explicit `encoding="utf-8"` — Fix S is REGRESSION-GUARD ONLY (the original P3.10 premise that the helper lacks `encoding="utf-8"` is empirically false; the slice-074 /slice Step 6.5 mojibake observation is rooted upstream — likely `tools/slice_pick.py` subprocess invocations / `PYTHONIOENCODING` env defaults at Windows console codepage layer / `/slice` skill's subprocess invocation; root-cause investigation routed to new source-pending entry P3.10' `find-real-mojibake-source` for a future slice). FAIL→PASS contrast: pre-fix synthetic mutation — test-fixture stashes the module, deletes `encoding="utf-8"` from one of the 9 sites (e.g., L790 `tmp_path.write_text`), re-imports → AST walker flags the missing-encoding site → test FAILS; post-fix (canonical) — all 9 sites carry the kwarg → test PASSES. Honest AC#5 obligation discharge per /critique B2+M3 ACCEPTED-FIXED reframe. Regression = any of the 9 `encoding="utf-8"` kwargs silently deletes from `tools/slice_queue_writer.py` (mojibake class re-opens at this surface); OR the AST walker silently drops one of the 3 detected call-shapes (`Path.read_text` / `Path.write_text` / `open()` / `subprocess.run(text=True)`); OR the structural-pin test silently degrades to substring match permitting partial-arg false-POSITIVES. | `<HOME>/.claude/.venv/Scripts/python.exe -m pytest tests/methodology/test_slice_queue_writer_utf8_encoding.py --no-header -q` | <1s | `<interp> -m pytest tests/methodology/test_slice_queue_writer_utf8_encoding.py --no-header -q` |
| 74 | slice-075-close-merge-substep-3-worktree-collision | Closes 2 adjacent prose defects in `skills/commit-slice/SKILL.md` Step 5b that block a fresh Builder from running `/commit-slice --merge` under BRANCH-2 without an undocumented ad-hoc cd-to-main-tree workaround (slice-075; closes P1.2 + P2.4 from `architecture/slices/slice-075-close-merge-substep-3-worktree-collision/source-pending-items.txt`; parents [[ADR-063]] BRANCH-2 + [[ADR-068]] PSQ-3 + [[ADR-020]] commit-slice 3-mode; MEPD-1 EXCLUDE — in-band methodology-prose-fix; ships at methodology v0.72.0 unchanged with NO new ADR mint and NO PMI-1 atomic bump). Three surgical prose edits at Step 5b: (1) **WT-clean preflight removed from pre-flight guardrails block** (was L168) — closes the WT-clean-vs-sub-step-2-commit-ordering contradiction P2.4 (pre-fix the preflight required empty `git status --porcelain` BEFORE sub-step 2's commit could execute, contradicting sub-step 2's expectation that the slice work is uncommitted); (2) **NEW sub-step `2.1.` post-commit WT-clean guardrail** inserted between sub-step 2 (commit on slice branch) and sub-step 2.5 (PSQ-3 rebase), preserving the silent-WT-discard local-state-loss protection intent (per /critique M5 ACCEPTED-PENDING) at the new post-commit position — vacuous on PSQ-3 re-entry by construction per [[ADR-068]] §Re-entry semantics; decimal `2.1.` marker mirrors slice-073 PSQ-3 sub-step `2.5.` precedent for inserting intermediate sub-steps between integer-numbered steps (CommonMark ordered-list compliant — per /critique m2 ACCEPTED-FIXED rejecting earlier "2-bis" draft which Markdown renders as bold prose not list item); (3) **sub-step 3 main-tree-transition prepend** — adds `main_tree=$(git worktree list --porcelain | awk '/^worktree / {print $2; exit}')` resolution + `cd "$main_tree"` BEFORE the existing `git checkout $default` invocation, closing the BRANCH-2 worktree-vs-main-tree default-branch-checkout collision P1.2 (`fatal: '<default>' is already checked out at '<main-tree-path>'`). The main_tree extraction is a SIBLING-BUT-DISTINCT idiom from Step 5b sub-step 5's existing `awk -v b="refs/heads/slice/NNN-<name>" '/^worktree / {p=$2} $0=="branch "b {print p; exit}'` specific-branch-extraction — shared `/^worktree /` regex anchor with divergent AWK action because divergent semantics (sub-step 3 wants the first-listed worktree = main tree per git porcelain ordering invariant per git-scm.com/docs/git-worktree; sub-step 5 wants a specific branch's worktree path); per /critique m1 ACCEPTED-FIXED. Structural-pin tests on SKILL.md prose: 4 new test functions across 2 new modules — `tests/methodology/test_commit_slice_skill_merge_substep_3_main_tree_transition.py` (`test_substep_3_includes_main_tree_transition_before_checkout` for AC#1 + `test_substep_3_main_tree_transition_uses_canonical_worktree_list_awk_extraction` for AC#3 paired pin) + `tests/methodology/test_commit_slice_skill_merge_wt_clean_preflight_ordering.py` (`test_wt_clean_preflight_does_not_contradict_substep_2_commit` for AC#2 + `test_wt_clean_preflight_preserves_silent_wt_discard_protection_intent` for AC#4 paired pin). ALL 4 test functions are Step-5b-section-scoped via local `_step_5b_section()` helper (mirrors slice-073 `test_commit_slice_skill_rebase_flag.py::_step_5b_section()` precedent; section-scoping mandatory per /critique M2 ACCEPTED-FIXED because `Pre-flight guardrails (run BEFORE any state change):` header appears 3x in SKILL.md at L166/L207/L242 and Steps 5c/5d legitimately retain WT-clean preflights). AC#2 + AC#4 further sub-step-scoped via the unique post-fix `2.1.` anchor with prelude guard `assert two_one_pos != -1` (per /critique-review M-add-1 + M-add-2 ACCEPTED-FIXED — without `2.1.` anchoring the assertions would PASS pre-fix because L168 already contained `silent-WT-discard` + `STOP` + `Print:` literals and L181 PSQ-3 conflict-STOP already contained `git status --porcelain` AFTER L173 sub-step 2's `git commit`, violating TF-1 WRITTEN-FAILING). AC#1 ordering check pins the canonical invocation literal `git checkout $default` (NOT bare `git checkout`) — narration mentions of `git checkout` would pollute the offset check otherwise (build-time RSAD-1 tightening discovered at smoke-gate; FINDING in build-log.md). 3-Critic stack N=11 cumulative (slice-063 through slice-075) — first-Critic 0B/2M/2m all VALID + meta-Critic EXTEND 2 missed (M-add-1 + M-add-2; both RSAD-1 class) ALL VALID; 6/6 dispositions ACCEPTED-FIXED in-band at TRI-1; final verdict CLEAN. Voluntary-restraint discipline N=16 cumulative if any code-Critic v1 advisory findings defer. Calibration signal: RSAD-1 assertion-strength MISSED-by-first-Critic class N=2 cumulative (slice-074 + slice-075); if recurs at slice-076+, candidate for `/critic-calibrate` proposal. Regression = the `cd "$main_tree"` main-tree-transition silently removed from Step 5b sub-step 3 (the P1.2 collision returns); OR the new sub-step `2.1.` marker silently degrades to integer `2.` or non-Markdown `2-bis` shape (CommonMark list rendering breaks AND the paired-pin tests' `2.1.` anchor extraction fails); OR the `git worktree list --porcelain | awk '/^worktree / {print $2; exit}'` canonical first-worktree AWK extraction drifts to sub-step 5's specific-branch shape (semantic confusion between main-tree and slice-tree extraction); OR the WT-clean preflight silently returns to pre-flight position (P2.4 contradiction returns); OR the silent-WT-discard local-state-loss protection prose silently disappears (M5 ACCEPTED-PENDING intent lost); OR the PSQ-3 re-entry semantics regress (sub-step 2.1. ceases to be vacuous on re-entry); OR sub-step 5 idempotent worktree-remove ordering before sub-step 6 `git branch -d` regresses (ADR-063 §Decision sub-step ordering load-bearing); OR the `_step_5b_section()` helper section-scoping silently degrades to whole-file substring match (false-PASS/FAIL from Steps 5c/5d sibling pre-flight headers); OR the `2.1.` block-anchored prelude guard `assert two_one_pos != -1` silently drops (TF-1 WRITTEN-FAILING semantics regress); OR the canonical invocation literal `git checkout $default` test pin reverts to bare `git checkout` (narration-pollution false-PASS class returns); OR the sibling-but-distinct idiom framing at design.md §Decisions Question A (a) silently changes to "identical-form" (predecessor-spec drift). | `<HOME>/.claude/.venv/Scripts/python.exe -m pytest tests/methodology/test_commit_slice_skill_merge_substep_3_main_tree_transition.py tests/methodology/test_commit_slice_skill_merge_wt_clean_preflight_ordering.py --no-header -q` | <2s | `<interp> -m pytest tests/methodology/test_commit_slice_skill_merge_substep_3_main_tree_transition.py tests/methodology/test_commit_slice_skill_merge_wt_clean_preflight_ordering.py --no-header -q` |
| 85 | slice-080-harden-bc1-critical-rules-exit-gate | BC-1 build-checks audit acknowledgment gate (ADR-072 / BCSG-1, option 2 — the acknowledgment-flag design, NOT the rejected blunt-count option 1): under `--strict`, an applicable Critical-severity rule whose `rule_id` is NOT in the `--ack-critical <RULE-ID...>` set becomes an `unacknowledged-critical` violation → gate-failure exit 1 (via the unchanged `return 1 if result.violations else 0` at `tools/build_checks_audit.py`); under `--strict`, exit 0 when every applicable Critical rule is acknowledged OR none applies (no false-fire); default (no `--strict`) exit behavior is byte-unchanged (applicable Critical rules stay informational). Closes `diagnose-out/backlog.md` SC-008 (F-HALF-9b2e44d1) where the pre-fix exit logic never consulted `critical_applicable`, so a tooling consumer treating the exit code as the BC-1 gate signal passed slices violating non-deferrable Critical rules while the tool printed "Per BC-1, Critical rules MUST be addressed". Repro `tests/bugs/test_bc1_critical_rule_exit_gate.py` FAILed pre-fix (argparse rejected the unknown `--strict` flag with exit 2; bare exit was 0 on an applicable unacknowledged Critical rule) → PASSes post-fix. `/build-slice` Step 6 invokes the audit with `--strict --ack-critical <addressed Critical rule IDs>`. Regression = under `--strict` the gate stops emitting `unacknowledged-critical` for an applicable un-acked Critical rule (re-opens the silent-pass class); OR an acknowledged applicable Critical rule still fires (gate becomes un-clearable); OR `--strict` false-fires when no Critical rule applies; OR the default (no-`--strict`) path stops being byte-equal to legacy behavior; OR `/build-slice` Step 6 silently drops the `--strict`/`--ack-critical` flags from its `build_checks_audit` invocation. BC-PROJ-10 paired-pin tests: `test_v_0_75_0_bcsg_1_entry_present_in_repo` + `test_v_0_75_0_bcsg_1_shippability_consumer_propagation`. | `<HOME>/.claude/.venv/Scripts/python.exe -m pytest tests/bugs/test_bc1_critical_rule_exit_gate.py --no-header -q` | <1s | `<interp> -m pytest tests/bugs/test_bc1_critical_rule_exit_gate.py --no-header -q` |
| 86 | slice-081-fix-drift-check-enforcement-gap | Repro for the /drift-check enforcement gap. `/drift-check` is listed as a `/build-slice` Step 6 pre-finish gate AND a CLAUDE.md "Run /drift-check before commit" rule, but — unlike every sibling discipline (BC-1 `tools/build_checks_audit`, PMI-1 `tools/plugin_manifest_audit`, CRP-1 `tools/critique_review_prerequisite_audit`, PCA-1 `tools/pipeline_chain_audit`, NAW-1 `tools/new_agent_warning_audit`, each a `tools/*_audit.py` HALT gate) — has NO `tools/drift_check_audit.py` and NO installed pre-commit hook (`.git/hooks/` holds only `.sample` files), only an honor-system `- [ ] /drift-check passes` checkbox. So a slice can finish AND commit with the drift-check gate entirely unenforced (the R-7 / slice-022 silent-disable failure class every other gate was hardened against). Repro `tests/bugs/test_drift_check_enforcement_gap.py` asserts (1) a `tools/drift_check_audit.py` module with a `main()` entry point exists + (2) `skills/build-slice/SKILL.md` Step 6 invokes `tools.drift_check_audit`; both FAIL pre-fix (no tool; Step 6 carries only the manual checkbox) → PASS once the enforcement gate is wired. Regression = the drift-check audit tool is removed OR `/build-slice` Step 6 stops invoking it (gate reverts to honor-system). | `<HOME>/.claude/.venv/Scripts/python.exe -m pytest tests/bugs/test_drift_check_enforcement_gap.py --no-header -q` | <1s | `<interp> -m pytest tests/bugs/test_drift_check_enforcement_gap.py --no-header -q` |
| 87 | slice-081-fix-drift-check-enforcement-gap | DCE-1 drift-check enforcement gate (ADR-073). `tools/drift_check_audit.py` refuses `/build-slice` Step 6 with `drift-check-not-run` (exit 1) when mode ∈ {STANDARD, HEAVY} AND no `**Trigger**:` line in `architecture/drift-log.md` references the slice number AND no canonical `drift-check-skip` milestone key; accepts (exit 0) on a slice-referencing `**Trigger**:` marker / documented skip / MINIMAL mode; `escape-hatch-malformed` (exit 1) on an off-canonical skip; usage/mode-unresolvable → exit 2. The matcher is LINE-anchored to `**Trigger**:` lines AND slice-number-anchored (`slice[- ]?0*<N>\b`) so a cross-slice mention in another entry's Scope/Notes/heading does NOT false-ACCEPT (per /critique-review M-add-1; mirrors CRP-1 keyed-not-substring per ADR-024). Producer template at `skills/drift-check/SKILL.md` canonicalized `sliceNN`→`slice-NNN` (B1). Regression = the gate stops emitting `drift-check-not-run` for an unmarked enforced-mode slice (reopens R-7/slice-022 silent-disable) OR false-ACCEPTs a cross-slice mention (M-add-1) OR `/build-slice` Step 6 drops the `tools.drift_check_audit` invocation OR Step 7b stops preserving `drift-check-skip:`. Pinned by `test_v_0_76_0_dce_1_entry_present_in_repo` + `test_v_0_76_0_dce_1_shippability_consumer_propagation` (changelog entry-pin pair) + the audit's own `tests/methodology/test_drift_check_audit.py` battery. | `<HOME>/.claude/.venv/Scripts/python.exe -m pytest tests/methodology/test_drift_check_audit.py tests/methodology/test_methodology_changelog.py::test_v_0_76_0_dce_1_entry_present_in_repo tests/methodology/test_methodology_changelog.py::test_v_0_76_0_dce_1_shippability_consumer_propagation --no-header -q` | <2s | `<interp> -m pytest tests/methodology/test_drift_check_audit.py tests/methodology/test_methodology_changelog.py::test_v_0_76_0_dce_1_entry_present_in_repo tests/methodology/test_methodology_changelog.py::test_v_0_76_0_dce_1_shippability_consumer_propagation --no-header -q` |
| 88 | slice-082-harden-pcr-1-soft-regen-corner-case | PCR-1 SOFT auto-regen equivalence guard (ADR-074 / R-21 fix-class (b)). `_verify_soft_equivalence` in `tools/parallel_conflict_resolver.py` runs READ-ONLY after the `pending_writes` empty-check and BEFORE the commit phase in `resolve_soft_conflict`; proves regenerated `slice-queue.md`/`shippability.md` content is in a deterministic equivalence class vs both rebase stages along 3 invariants — (1) queue claim-preservation over the CLAIMED subset of `merged_claims` (M-add-1: unclaimed candidates excluded so a faithful regen is not false-STOPped), (2) shippability numbered-row completeness, (3) SYMMETRIC shippability prelude set-equality (catches non-numbered/split-row content-mutation in EITHER stage); unprovable → raise `_SoftResolutionError(UNKNOWN)` → fail-closed STOP with NO writes (atomicity per ADR-069) + a best-effort `(equivalence-guard STOP)` audit-log entry. A claimed orphan that had a heading in the discarded stage gets a loud `cross-stage-claim-drop` stderr warn (M2) WITHOUT STOP (legitimate top-10 churn must not false-STOP). Closes R-21 (the SOFT-auto-regen silent-wrong-resolution corner-case class). Regression = the guard stops STOPping on a malformed-block claim drop (R-21 reopens); OR invariant #1 starts checking unclaimed candidates and false-STOPs the happy path (M-add-1 regression); OR symmetric invariant #3 regresses to the asymmetric discarded-stage-only check (M1; non-numbered content-mutation slips); OR a guard-STOP silently writes/stages/continues the rebase (atomicity breach); OR the `cross-stage-claim-drop` becomes a STOP (M2 false-STOP). | `<HOME>/.claude/.venv/Scripts/python.exe -m pytest tests/methodology/test_pcr_1_soft_regen_equivalence_guard.py --no-header -q` | <2s | `<interp> -m pytest tests/methodology/test_pcr_1_soft_regen_equivalence_guard.py --no-header -q` |
| 89 | slice-083-add-pcr-2b-hard-class-conflict-resolution | PCR-2b HARD/MIXED conflict resolution via gate-on-hand-resolve + TRI-RESOLVE-1 user-triage gate (ADR-075; mints two new rules on the parallel-conflict-resolution `PCR-N` axis + a TRI-RESOLVE-N triage-gate axis; refines ADR-069 HARD/MIXED rows; v0.77.0; MEPD-1 INCLUDE; 5-part PMI-1 bump 0.76.0→0.77.0). Closes the last two open conflict-classes PCR-1/PCR-2a left bare-STOP-only. `tools/parallel_conflict_resolver.py` gains `resolve_hard_conflict` (HARD/MIXED dispatch — STOP carrying gate context, NEVER auto-merges / NEVER `git rebase --continue`) + `_verify_resolution_clean` (git-native `--diff-filter=U` empty + line-anchored `<<<<<<<`/`>>>>>>>` opener scan — NOT `=======`, NOT `git diff --cached --check`, per B2/M-add-1 setext-false-positive fix) + `_format_hard_audit_entry` (`## Hard-conflict resolution -` uniform-hyphen-space section) + `_record_hard_resolution` + `--verify-resolution`/`--record-hard-resolution` CLI modes; `skills/commit-slice/SKILL.md` sub-step 2.5 gains the HARD/MIXED gate branch (hand-resolve → `--verify-resolution` → `code-review` agent on the resolved diff [M-add-2 — NOT the design-calibrated critique agents] → TRI-RESOLVE-1 SOAD-1 user gate, two-condition fail-closed apply → `git rebase --continue`). MIXED routes through HARD (atomicity); SOFT→HARD shippability escalation keyed on returned `conflict_class` (M2). Regression = `resolve_hard_conflict` auto-merges or runs `git rebase --continue`; OR `_verify_resolution_clean` reverts to a `=======`/`git diff --cached --check` scan (re-opens the setext false-STOP); OR the HARD/MIXED dispatch branch in `resolve_soft_conflict` silently disappears (HARD falls to bare STOP, no gate); OR MIXED auto-resolves the SOFT portion (atomicity breach); OR the TRI-RESOLVE-1 gate gains a silent auto-continue path (fail-closed breach); OR the `## Hard-conflict resolution -` section heading loses its uniform-hyphen-space separator; OR the v0.77.0 / PCR-2b / TRI-RESOLVE-1 / ADR-075 entry anchors are lost. BC-PROJ-10 paired pins: `test_v_0_77_0_pcr_2b_tri_resolve_1_entry_present_in_repo` + `test_v_0_77_0_pcr_2b_shippability_consumer_propagation`. | `<HOME>/.claude/.venv/Scripts/python.exe -m pytest tests/methodology/test_pcr_2b_hard_conflict_dispatch.py tests/methodology/test_pcr_2b_verify_resolution.py tests/methodology/test_pcr_2b_mixed_routes_to_hard.py tests/methodology/test_pcr_2b_repro_hard_gate_closed.py tests/methodology/test_parallel_conflict_resolution_log_hard.py tests/methodology/test_commit_slice_skill_tri_resolve_gate.py tests/methodology/test_methodology_changelog.py::test_v_0_77_0_pcr_2b_tri_resolve_1_entry_present_in_repo --no-header -q` | <5s | `<interp> -m pytest tests/methodology/test_pcr_2b_hard_conflict_dispatch.py tests/methodology/test_pcr_2b_verify_resolution.py tests/methodology/test_pcr_2b_mixed_routes_to_hard.py tests/methodology/test_pcr_2b_repro_hard_gate_closed.py tests/methodology/test_parallel_conflict_resolution_log_hard.py tests/methodology/test_commit_slice_skill_tri_resolve_gate.py tests/methodology/test_methodology_changelog.py::test_v_0_77_0_pcr_2b_tri_resolve_1_entry_present_in_repo --no-header -q` |
