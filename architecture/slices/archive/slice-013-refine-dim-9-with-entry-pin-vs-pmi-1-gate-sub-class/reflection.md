# Reflection: Slice 013 refine-dim-9-with-entry-pin-vs-pmi-1-gate-sub-class

**Date**: 2026-05-13
**Shipped**: YES

## Errata (added 2026-05-13 post-/reflect)

This reflection (and the lessons-learned.md + slices/_index.md entries it generated) repeatedly framed slice-014 as a MANDATORY `/critic-calibrate` boundary based on the "cumulative cross-cutting misses 11 across slices 6-13 = 5.5× the ≤2 target" framing. **That framing was stale at write time.** The 2026-05-13 `/critic-calibrate` run (which executed BEFORE slice-013, recorded in `architecture/critic-calibration-log.md`) had already resolved this:

1. **Zero proposals generated.** The Meta-Critic explicitly found no new patterns at ≥N=3-distinct-slices promotion threshold worth codifying into the Critic prompt.
2. **The quantitative ≤2-cumulative-misses-across-slices-6-15 target was REFUTED by Meta-Critic as over-ambitious**, with explicit recommendation to accept the override on qualitative grounds. The qualitative trajectory (cross-cutting Dim 9 catch rate range-bound 60-100% on N=8 evidence stable post-slice-013) is the real signal — not the quantitative cumulative count.
3. **The 2026-05-10 Proposals 1 + 2 were STRONGLY EFFECTIVE** — their pattern categories went 3 → 0 in slices 6-12. The Critic prompt is performing well across structural categories.
4. **Entry-pin-vs-PMI-1-gate-conflation was on the watch-list but NOT proposed** ("surplus to need" since slice-012 demonstrated the Critic could catch this class without explicit prompt cuing). Slice-013 codifying EPGD-1 was still a valid /slice-time judgment per the user's choice at /slice-time, but it was NOT driven by calibration urgency.
5. **Next /critic-calibrate trigger: after slice-013-020 archives**, OR earlier if any watch-list N=1 sub-class hits N=3 distinct-slice recurrence.

**Slice-014 is NOT a mandatory calibration boundary.** Strongest #1 candidate is `refactor-pmi-1-gate-to-version-agnostic-shape` (N=6 supersession events stable; per-bump cost trivial but compounds), NOT `/critic-calibrate`. Slice-013's NEW N=1 candidates (shippability-catalog-propagation + body-bound-widening-grep-for-siblings) join the watch-list awaiting N=2 cross-slice recurrence.

**Lesson for future slices**: when /reflect surfaces a "calibration-urgent" framing, verify against `architecture/critic-calibration-log.md` to confirm the framing isn't stale. The cumulative-misses count IS a tracked metric but ONLY relative to whatever target Meta-Critic last ratified — the 2026-05-13 run explicitly retired the ≤2 target.

References below to "/critic-calibrate at slice-014" (in `## Deferred`, `## Lessons for next slice`, etc.) are superseded by this Errata. The rest of the reflection (validated/corrected/discovered/Critic calibration sections + vault updates) stands.

## Validated

- **Audit 1 (location-pin anchor uniqueness)** validated by `test_critique_dim_9_entry_pin_vs_pmi_1_gate_location_pinned` PASSING — anchors `Recursive self-application discipline` (1 occurrence at L168) + `### Bonus: weak graph edges` (1 occurrence post-edit at L180) verified unique pre-AC-lock; scoped `.find()` avoided first-occurrence-wins collision per slice-009 DEVIATION-2 mitigation. Empirical-verification-at-design-time discipline N=12 stable.
- **Audit 2 (canonical literal absence)** validated at /build-slice — canonical `Entry-pin-vs-PMI-1-gate semantics conflation` was 0 occurrences pre-Phase-1a (clean slate); transitioned PENDING → WRITTEN-FAILING → PASSING genuinely per TF-1 N=10 stable.
- **Audit 3 (BC-1 self-application prediction)** validated at /build-slice Phase 4 — `applicable=[]`, `skipped=['BC-PROJ-1', 'BC-PROJ-2', 'BC-GLOBAL-1']` on slice-013's own mission-brief + design.md. BC-1 v1.3 negative-anchor mechanism silences all 3 project-relevant rules via 9-token methodology-vocabulary set per slice-008 + slice-012 v1.2 + v1.3 migrations. Validate-using-your-own-ship pattern N=11 stable.
- **Audit 4 (EPGD-1 self-application pre-Edit structural-separation prediction)** validated empirically at /validate-slice — 7 entry-pin functions present (v_0_22_0..v_0_28_0); exactly 1 PMI-1 versioned-gate (`_at_0_28_0`); **0 of 6 prior entry-pin functions touched by Phase 1c narrow-scope Edit**. The slice authoring EPGD-1 IS the canonical reference instance of the discipline it authors. RSAD-1 self-application N=5 cumulative.
- **Audit 5 (recursive self-application N=5 enumeration prediction)** validated at /critique B1 — Critic empirically caught the rule-ID drift (17 EPG-1 occurrences in mission-brief vs design.md/ADR-012/milestone.md EPGD-1 locked) — exactly the class RSAD-1 (Dim 9 6th sub-clause, design-time mode) was codified to catch at slice-011. Strongest empirical validation of RSAD-1's pay-off at slice-013.
- **Audit 6 (RSAD-1 build-time-via-/critique-fix-prose sub-mode prediction)** validated at /build-slice — slice's mission-brief + design.md + ADR-012 mention BC-PROJ-1/2/GLOBAL-1 positive-anchor strings as historical context, but BC-1 v1.3 negative-anchor mechanism silenced all 3 rules at Phase 4 self-application. Zero BC-1 firing despite slice carrying methodology-vocabulary. Slice-010 DEVIATION-3 build-time recursive-self-application pattern did NOT recur at slice-013.
- **ZERO build-time DEVIATIONs at slice-013** — third in slice-005..013 series (N=2 cumulative at slice-012 → N=3 stable post-slice-013). Strong /critique discipline + pre-AC-lock empirical audits at /design-slice continue to pre-empt build-time DEVIATIONs across cross-cutting tooling slices.
- **All 5 ACs PASS with evidence** at /validate-slice (4/4 + 5/5 + 5/5 + 1/1 + 9/9 = 24/24 critical-path tests across ACs).
- **EPGD-1 self-application empirically VALIDATED at /build-slice + /validate-slice**: all 6 prior entry-pin functions (v_0_22_0..v_0_27_0) PASS unchanged. Slice-013 IS canonical reference instance of the discipline it authors.

## Corrected

- **Mission-brief N-surface count**: said `N=2 surfaces` (in-repo entry title + installed entry title) for EPGD-1 canonical phrase pin (AC #4); reality is **N=3 surfaces** (agents/critique.md Dim 9 7th sub-clause title + in-repo entry + installed entry) per slice-011 RSAD-1 3-surface schema-pin precedent. Corrected at /critique M3 ACCEPTED-FIXED — updated mission-brief.md AC #4 (line 19) wording + propagated corrected framing to `test_v_0_28_0_epgd_1_entry_present_in_repo_and_installed` docstring.
- **Mission-brief rule-ID hedge**: said `Rule reference candidate **EPG-1** ... suffix convention deferred to /design-slice` and `naming **EPG-1** (or final rule ID chosen at /design-slice)` and `(EPG-1 or final)` at 3 sites; reality is /design-slice locked **EPGD-1** in design.md + ADR-012 + milestone.md (slice-011 RSAD-1 -D-suffix convention N=2 stable extension). Corrected at /critique B1 ACCEPTED-FIXED — global rename mission-brief 17 EPG-1 → EPGD-1 + removed 3 hedge phrasings. Slice's own draft showed a class of specification defect RSAD-1 was codified to catch.
- **slice-011 `_names_both_sub_modes` + `_cites_at_least_two_cross_slice_anchors` end_anchors**: had `end_anchor = "### Bonus: weak graph edges"`; reality is once slice-013 appends 7th sub-clause body between L172 (RSAD-1 close) and L174 (Bonus H3), the body widens to include 7th sub-clause body — silently undermining precision of 6th sub-clause regression-guard. Corrected at /critique M1 + M-add-1 ACCEPTED-PENDING — both tests' end_anchors tightened to `"Entry-pin-vs-PMI-1-gate semantics conflation"` at /build-slice Phase 1f.
- **No design.md corrections needed**: design phase plan executed exactly as specified. Audit 4 + Audit 5 structural-separation predictions validated; Audits 1-3 + Audit 6 ran clean at build-time.

## Discovered

- **NEW Dim 9 sub-class candidate at N=1**: **shippability-catalog-consumer-reference-propagation-after-PMI-1-structural-invariant-supersession** (caught at /validate-slice Step 5.5). When a slice supersedes a structural-invariant test name (e.g., slice-013's `_lists_six_sub_clauses` → `_lists_seven_sub_clauses` per PMI-1 versioned-gate supersession discipline applied at the structural-invariant level), the rename propagation must reach ALL consumer references — including `architecture/shippability.md` rows that reference the superseded test name. Slice-013 Phase 1f superseded in test_critique_agent.py + Phase 5 added row 13 + updated row 12 header — but rows 6 + 11 commands still referenced the deleted `_lists_six_sub_clauses` test. Caught at /validate-slice by both rows FAILING with `no match in any of...`; fixed in-line. **Sibling defect class to EPGD-1** (which targets PMI-1 versioned-gate Edits in test_methodology_changelog.py); distinct in (a) surface (`architecture/shippability.md` vs `tests/methodology/test_methodology_changelog.py`) and (b) discipline (rename-propagation across consumers vs Edit-scope at supersession site). **N=1 cumulative evidence at slice-013**; promote to Dim 9 sub-class refinement at N=2 if recurs at slice-014+.
- **First-instance-of-dual-review-catching-pattern-blindness in the project** (DR-1 empirical effectiveness signal at slice-013): meta-Critic at /critique-review caught M-add-1 missed by first Critic — first Critic identified body-bound widening defect class on slice-011's `_names_both_sub_modes` but didn't generalize via grep across siblings with identical anchors (`_cites_at_least_two_cross_slice_anchors` shared the same anchors). Meta-Critic surfaced it. **Calibration observation logged for /critic-calibrate at slice-014**: candidate Dim 9 sub-class refinement at N=1 — *"having found a body-bound widening defect, grep for all tests sharing those anchors"*. DR-1 pays off N=1 at first invocation post-codification.
- **4-layer-defense slice (first in project)**: slice-013 is the FIRST slice in the project to have 3 distinct defect classes caught across all 4 Critic-stack layers in a single slice: (1) first Critic catches B1+M1+M2+M3+m1+m2+m3; (2) meta-Critic catches M-add-1 via DR-1; (3) slice's own design-stage audits validate 6 predictions at /build-slice; (4) /validate-slice shippability catalog catches the shippability-catalog-propagation slip as final layer. **Layered-defense pattern empirically validated** — each layer catches what prior layers missed.
- **Recursive-self-application N=5 cumulative post-RSAD-1 codification ratchets to "canonical methodology practice"**: slice-009 M2 (N=1) → slice-010 design-time + build-time (N=2) → slice-011 4-of-7 findings (N=3) → slice-012 B1 (N=4) → **slice-013 B1 rule-ID drift (N=5)**. Five consecutive slices post-RSAD-1 with own-draft defects caught by Dim 9 sub-clause 6. Pattern is now load-bearing — slice authoring methodology refinement will reliably have defects in own draft, and Critic at /critique catches them via RSAD-1.

## Deferred

- **`/critic-calibrate`** — STRONGEST candidate for slice-014. Cumulative cross-cutting Critic misses **11 across slices 6-13** (slice-012 was 10 + slice-013 adds the shippability-catalog-propagation slip = 11; **5.5× the ≤2 target across slices 6-15**). **2 slices remaining in slice-006-15 window** = effectively impossible without recalibration NOW. Per `~/.claude/skills/critic-calibrate/SKILL.md`: walks reflections 6-13, classifies misses, proposes Critic prompt additions; human reviews + manually applies. ~1 hour invocation cost.
- **`refactor-pmi-1-gate-to-version-agnostic-shape`** — N=6 supersession events at slice-013 (was N=5 at slice-012); per-bump cost still ~1 min trivial; deferred one more cycle. **Watch slice-014+ for friction escalation**: at N=6 supersession events, the version-gate function's exists-in-name-only feels increasingly mechanical; if /critic-calibrate (slice-014) flags as recurring noise, promote.
- **`fix-val-1-layer-b-intra-repo-tests-namespace-via-pytest-testpaths`** — N=11 cumulative recurrence at slice-013 (was N=10 at slice-012); handled cleanly via `--imports-allowlist tests`; per-slice friction ~1 sec; deferred indefinitely until /critic-calibrate flags as recurring noise pattern.
- **Promotion of NEW Dim 9 sub-class `shippability-catalog-consumer-reference-propagation`** — N=1 evidence at slice-013; promote to Dim 9 sub-class at N=2 if recurs at slice-014+ via either /critic-calibrate or dedicated refinement slice.
- **Promotion of NEW Dim 9 sub-class `having-found-a-body-bound-widening-defect-grep-for-siblings-sharing-anchors`** (meta-Critic catch at DR-1) — N=1 evidence at slice-013; promote at N=2.
- **INST-2 generalization** (general content-equality across all installed files) — slice-007 carryover; still N=1 evidence; defer to N=2 (drift on `agents/critique-review.md`, `agents/critic-calibrate.md`, or `~/.claude/build-checks.md`).
- **R-1 deeper fix for /diagnose cwd-mismatch** — risk-register HIGH-band open; documented-constraint workaround acceptable per slice-002; needs `/risk-spike` first to disambiguate cwd vs parallel-spawn-cascade hypotheses.
- **Case-sensitivity canonical-literal pin discipline** (slice-009 DEVIATION-1 carryover) — N=1; defer to N=2.
- **`.find()`-collision in location-pin test discipline** (slice-009 DEVIATION-2 carryover) — N=1 stable; pre-empted at slices 010/011/012/013 via design-time anchor-uniqueness verification + scoped `.find()`; defer to N=2.

## Critic calibration

Per TRI-1, score each finding via the disposition in `critique.md` -> `## Triage` table + reality observed during build/validate:

- **B1 (Rule-ID drift EPG-1 vs EPGD-1)**: **VALIDATED** — disposition ACCEPTED-FIXED at /critique; reality confirmed at /build-slice Phase 0 (pre-rename verification: 17 EPG-1 occurrences vs 21 EPGD-1 in design.md / 13 in ADR-012 — exact mismatch the Critic flagged). RSAD-1 design-time mode catch.
- **M1 (slice-011 `_names_both_sub_modes` precision-degradation)**: **VALIDATED** — disposition ACCEPTED-PENDING at /critique; reality confirmed at /build-slice mid-slice smoke gate (Phase 3 PASS after end_anchor tighten + symmetric add). Without M1 fix, body would have widened silently to include 7th sub-clause body per Critic's prediction.
- **M-add-1 (sibling `_cites_at_least_two_cross_slice_anchors` precision-degradation)**: **VALIDATED** — disposition ACCEPTED-PENDING at /critique-review (DR-1 meta-Critic catch); reality confirmed at /build-slice — slice-013's 7th sub-clause body contains the substring `M1` (in "Critic M1 ACCEPTED-FIXED") which would have leaked into the 6th sub-clause's sc_count assertion exactly as meta-Critic predicted. **First instance of dual-review-catching-pattern-blindness in the project** — first Critic missed this generalization; meta-Critic surfaced it; reality confirmed both Critics' analyses.
- **M2 (`_paragraph_cites` anchor list under-specified)**: **VALIDATED** — disposition ACCEPTED-FIXED at /critique; reality confirmed at /build-slice Phase 1f — the formalized strict-both `["slice-011", "slice-012"]` + ≥2-of-4 substantive-discipline anchors semantics in design.md gave the Builder deterministic test-implementation specification. Without M2 fix, /build-slice author would have had ambiguity over anchor list choice (the under-specification Critic flagged).
- **M3 (N-surface count drift N=2 → N=3)**: **VALIDATED** — disposition ACCEPTED-FIXED at /critique; reality confirmed at /build-slice + /validate-slice Phase 4 AC #4 evidence — substantive canonical phrase pinned across 3 actual surfaces (critique.md + in-repo + installed), not 2. Without M3 fix, mission-brief AC #4 wording + entry-pin test docstring would have propagated the miscount to slice-014+ as canonical methodology.
- **m1 (ADR-012 L40 phrasing ambiguity)**: **VALIDATED** — disposition ACCEPTED-FIXED at /critique; cosmetic fix applied. Reality confirmed at ADR review — phrasing now unambiguous.
- **m2 (ADR-012 "N=2 stable" pre-empts validation)**: **VALIDATED** — disposition ACCEPTED-FIXED at /critique; cosmetic accuracy fix applied at L42 + L55 + L89 of ADR-012. Reality confirmed at /reflect — N=2 has indeed empirically promoted to stable in this reflection per the slice-011 N=1 → slice-013 N=2 ratchet pattern, exactly as m2 predicted "pending empirical validation at /build-slice + /reflect".
- **m3 (verification plan row 4 EPG-1 hedge)**: **VALIDATED** — disposition ACCEPTED-FIXED at /critique; folded into B1 global rename sweep. Reality confirmed at /build-slice — no `EPG-1` substring remains in mission-brief.

**Missed by Critic** (slice-cycle level):

- **Shippability-catalog-consumer-reference-propagation slip** (NEW Dim 9 sub-class candidate at N=1) — Critic at /critique + /critique-review BOTH missed this. Both passes focused on test_critique_agent.py + test_methodology_changelog.py + EPGD-1 Edit-scoping discipline; neither generalized the "supersession → consumer-reference-propagation" lesson to shippability.md. Caught at /validate-slice Step 5.5 by rows 6 + 11 FAILING with `no match in any of...`. Fixed in-line at validate-time. Distinct from EPGD-1 (Edit-scope discipline at supersession site) AND distinct from M1/M-add-1 (body-bound test scoping). **Promote to Dim 9 sub-class at N=2 if recurs at slice-014+**.

**Pattern**:

- **All 7 first-Critic findings VALIDATED post-disposition** — seventh consecutive 100% Critic-disposition accuracy slice. Running total **66/66 across slices 6-13** (was 59/59 at slice-012; +7 first-Critic catches at slice-013). Strongest streak in the project.
- **Plus meta-Critic M-add-1 VALIDATED** — first instance of dual-review-catching-pattern-blindness. Cross-Critic-stack total **67/67 across slices 6-13** post-slice-013.
- **1 MISSED at slice-cycle level** (shippability-catalog-propagation slip) — caught at /validate-slice; fixed in-line. Sibling to EPGD-1.
- **Cross-cutting-conformance Dim 9 catch rate at slice-013** = **87.5% (7 of 8 sub-class hits caught at /critique + /critique-review combined)** — 7 caught (B1+M1+M2+M3+M-add-1+m1+m2+m3 = 8 findings, all Dim 9 sub-class hits per recursive-self-application discipline) minus the 1 missed (shippability-catalog-propagation). Falls within range-bound 60-100% framing from slice-008..012 (N=8 evidence stable post-slice-013). Trajectory: 0% (slices 1-5) → 25% → 60% → 100% → 60% → 87.5% → 80% → 80-100% (slice-012) → **87.5% (slice-013)**.
- **Recursive-self-application N=5 cumulative post-RSAD-1 codification** — slice-013 IS the 5th consecutive slice with own-draft defects caught by Dim 9 sub-clause 6. RSAD-1 codification empirically load-bearing at N=5; pattern stable.
- **DR-1 empirical effectiveness signal N=1 standalone post-codification** — meta-Critic at /critique-review catches missed-by-first-Critic findings. First post-codification instance. Need N=2 evidence cross-slice to ratchet to stable.

## Lessons for next slice

- **`/critic-calibrate` at slice-014 is MANDATORY** — cumulative cross-cutting Critic misses **11 across slices 6-13 (5.5× the ≤2 target); only 2 slices remaining in the slice-006-15 window**. Window-edge crossed; recalibration target downward OR refinement of Critic prompt with 2 new sub-class candidates (shippability-catalog-propagation + body-bound-widening-grep-for-siblings) becomes the ONLY way to close the gap. Per `~/.claude/skills/critic-calibrate/SKILL.md`: ~1 hour invocation. STRONGEST slice-014 candidate.
- **PMI-1 structural-invariant supersession discipline requires consumer-reference propagation across ALL surfaces**: when superseding `_lists_six_sub_clauses` → `_lists_seven_sub_clauses` (slice-013) or `_at_0_27_0` → `_at_0_28_0` (slice-013 PMI-1 gate), the supersession Edit at the test site is necessary-but-not-sufficient. Phase 5 (shippability.md updates) MUST scan ALL existing rows for the superseded test name AND propagate the rename. Generic methodology lesson — applies symmetrically to any future structural-invariant supersession at slice-014+.
- **`having-found-a-body-bound-widening-defect-grep-for-siblings` (meta-Critic-internal discipline)**: when reviewing a slice that widens body bounds of a body-bound test, the meta-Critic SHOULD grep the test file for all sibling tests sharing the same `start_anchor` + `end_anchor` pair, not just the one explicitly flagged by the first Critic. Mirrors the slice-009 → slice-010 anchor-uniqueness pre-emption discipline at a higher level. N=1 at slice-013; promote to Critic prompt discipline at N=2.
- **4-layer-defense pattern empirically validated** — first Critic + meta-Critic + design-stage audits + validate-slice shippability catalog. Future cross-cutting tooling slices should explicitly plan all 4 layers; relying on first-Critic-only or first-Critic-plus-meta-Critic leaves slice-cycle-level misses at risk of escaping to /reflect.
- **EPGD-1 empirically load-bearing post-codification** — slice-013's own ship validates the discipline at N=1 standalone post-codification (slice's Phase 1c PMI-1 supersession Edit narrow-scoped + Phase 1b INSERT under new SECTION header). Awaiting N=2 cross-slice recurrence at slice-014+ for "promote-EPGD-1-to-audit-enforced-gate" consideration. v2 candidate `tools/epgd_1_audit.py` walking design.md Phase plans deferred.

## Vault updates made (thin vault)

- `agents/critique.md` — appended 7th sub-clause `Entry-pin-vs-PMI-1-gate semantics conflation` between RSAD-1 close (L172) and `### Bonus: weak graph edges` (now L180). Body covers BOTH `Build-time slip mode` (slice-011 N=1) + `Design-time-pre-empted success mode` (slice-012 N=2) sub-modes.
- `~/.claude/agents/critique.md` — forward-synced byte-equal (Phase 2; CAD-1 invariant from slice-007). sha256 `0346D39EF988FA61`.
- `methodology-changelog.md` — v0.28.0 EPGD-1 entry prepended (in-repo) with N=2 cross-slice anchors + rule reference + Limitations note + canonical phrase pinned across N=3 surfaces.
- `~/.claude/methodology-changelog.md` — forward-synced byte-equal. sha256 `03DF671C2CCD4251`.
- `VERSION` — bumped 0.27.0 → 0.28.0.
- `~/.claude/ai-sdlc-VERSION` — bumped 0.27.0 → 0.28.0. sha256 `A80BFF70756FF2E0` byte-equal in-repo.
- `plugin.yaml` — version field 0.27.0 → 0.28.0 (PMI-1 atomic).
- `tests/methodology/test_critique_agent.py` — 5 new tests added (canonical-substring + location-pin + names_both_sub_modes + paragraph_cites + cites_at_least_two_cross_slice_anchors for 7th sub-clause); 1 supersession (`_lists_six_sub_clauses` → `_lists_seven_sub_clauses` per PMI-1 structural-invariant supersession discipline applied at the structural-invariant level — N=2 stable post-slice-013); 2 narrow Edits (slice-011 `_names_both_sub_modes` + `_cites_at_least_two_cross_slice_anchors` end_anchors tightened per M1 + M-add-1 ACCEPTED-PENDING).
- `tests/methodology/test_methodology_changelog.py` — new SECTION header `# --- Slice-013 / EPGD-1 entry pinning ---` + `test_v_0_28_0_epgd_1_entry_present_in_repo_and_installed` function INSERTed (Phase 1b under NEW dedicated SECTION header structurally separated from PMI-1 gate); `_at_0_27_0` PMI-1 gate SECTION header + function body Edit narrow-scoped to `_at_0_28_0` (Phase 1c per EPGD-1 self-application).
- `architecture/shippability.md` — row 13 added (slice-013 EPGD-1 critical path: 9 tests / <2s). Row 12 header updated (slice-013 supersedes `_at_0_27_0`). **Plus rows 6 + 11 commands fixed in-line at /validate-slice** for stale `_lists_six_sub_clauses` references (propagated to `_lists_seven_sub_clauses`); row 6 + row 11 headers updated to note slice-013 supersession discipline applied at structural-invariant level.
- `architecture/decisions/ADR-012-promote-entry-pin-vs-pmi-1-gate-discipline-to-critique-dim-9-sub-clause.md` — created at /design-slice; m1 + m2 cosmetic fixes applied at /critique.
- `architecture/slices/slice-013-.../mission-brief.md` — B1 global rename EPG-1 → EPGD-1 (17 sites); M2 anchor list formalization; M3 N-surface count correction (N=2 → N=3); m3 hedge removed (folded into B1).
- `architecture/slices/slice-013-.../design.md` — M2 strict-both + ≥2-of-4 anchor list formalization in "What's new" section.
- `architecture/slices/slice-013-.../milestone.md` — updated per-stage; final stage `complete`.

NOT updated: `architecture/risk-register.md` (no new HIGH-band runtime risks introduced; the shippability-catalog-propagation slip is methodology-internal at sub-class level, not project-level risk). `architecture/concept.md` (slice does not change project concept).
