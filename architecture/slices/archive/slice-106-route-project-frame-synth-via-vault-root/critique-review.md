# Critique Review: Slice 106 route-project-frame-synth-via-vault-root

**Reviewed by**: critique-review agent (DR-1)
**Date**: 2026-06-03
**First-Critic verdict**: NEEDS-FIXES
**Dual-review verdict**: ACCEPT

## Summary
The first Critic's review is sound and empirically grounded. I independently re-ran `audit_root(REPO_ROOT)` and re-applied the orphan-literal regex against the real code: B1 is a confirmed true build-breaker, M1/M2/m1/m2 are correctly identified and correctly severitied, and the eight dimensions surface no false negative. The first Critic's one headline claim ("Critic executed the audit") checks out exactly. ACCEPT.

## Confirmed findings

First-Critic findings the meta-Critic agrees with (VALID + correct severity):

- **B1 (Blocker)** — CONFIRMED, severity Blocker is correct. I ran `python -m tools.vault_flip_readiness_audit --json`: the repo has **exactly 4** `must-rewrite-before-flip` occurrences, all `tools/project_frame_synth.py` (L121/L122/L185/L194), all `surface=production`; the tests surface yields **0** `MUST_REWRITE` (counts show `test-update-at-flip: 154`, `must-rewrite-before-flip: 4`). Routing those 4 to 0 makes `assert MUST_REWRITE in classes` at `test_vault_flip_readiness_audit.py:47` evaluate `False` → the test fails. Because shippability rows #108 (slice-100, L117) and #109 (slice-102, L118) both invoke the full `test_vault_flip_readiness_audit.py`, the break also reds the catalog at `/validate-slice`. This is a genuine in-slice build-breaker the design's first-pass test enumeration missed (it traced `_BASELINE` but not the sibling real-repo assertion in the same module — APED-1 live). **On the Blocker-vs-Major question**: Blocker is the right call. The deciding factor is not "is it in scope" (it is) but "does shipping the slice as-designed-pre-fix leave the methodology suite + shippability catalog RED". It does — and an AC explicitly promises "Full methodology suite green; shippability unregressed" (AC5). A finding that, left unaddressed, guarantees a red gate AND falsifies a written AC is correctly a Blocker, not a Major. The Builder draft (ACCEPTED-PENDING, synthetic-fixture rework enumerated in design §What's-new #4 + §Consequence-chain #3) is the correct disposition.

- **M1 (Major)** — CONFIRMED, severity Major is correct. AC4/AC5 as originally worded read as promising a new named regression test + new shippability row; the design reuses existing `test_vault_root_constant.py` machinery. Per Wiegers (unambiguously-testable AC), the tension is real and the Builder couldn't tell whether a new test+row is in-scope. **On whether emptying a baseline / joining the allowlist requires a shippability row under RPCD-1/SCPD-1**: No, and the reconciliation is correct. The project's convention adds a catalog row when a genuinely-new test/audit/behavior is introduced; this slice adds **no new test function** (confirmed: the only test-file edits are `_BASELINE → ()`, the allowlist membership bump, the B1 fixture rework of an *existing* test, and narrative rewrites) and **no new audit rule**. The existing consumer references (rows #108/#109 running `test_vault_flip_readiness_audit.py`; the slice-068 row running `test_vault_root_constant.py`) remain valid, so RPCD-1/SCPD-1 propagation is already satisfied. The Builder draft (ACCEPTED-FIXED) correctly resolves to no-new-test / no-new-row.

- **M2 (Major)** — CONFIRMED, severity Major is correct. The stale-"4 sites" narrative is real in all four named spots: shippability row #108 prose ("the 4 bare-`"architecture"` `/`-BinOp … sites in `project_frame_synth.py` classify `must-rewrite-before-flip`"), row #109 prose ("leave the production `_BASELINE` (4 `/`-BinOp sites) BYTE-IDENTICAL"), the `_BASELINE` in-module comment `vault_flip_readiness_audit.py:163-170`, and `test_production_baseline_unchanged_vs_slice100`'s name/comment. AP-13/SCPD-1 consumer-reference propagation. Major (not Minor) is justified: stale prose in the shippability catalog and an in-module baseline comment that actively misdescribes the post-slice state is documentation rot on the load-bearing flip-gate artifact. Builder draft (ACCEPTED-PENDING, all four enumerated + convention-check flagged) is correct.

- **m1 (Minor)** — CONFIRMED, severity Minor is correct. The FBCD-1 two-numbers trap is real: I confirmed `test_vault_root_constant.py:199` hard-pins `assert test_count == 15`, while the allowlist `_MIGRATION_SITE_ALLOWLIST` membership is live-15 (going to 16). The stale "14-element" prose at L41 + L291 is genuinely wider than the count-pin, and the `==15` function-count pin must NOT be bumped (this slice adds no new test function to that module). Minor is right — it's a build-correctness footgun, not a gate-breaker, and the design now distinguishes the two numbers. Builder draft (ACCEPTED-FIXED) is correct.

- **m2 (Minor)** — CONFIRMED, severity Minor is correct. "No ADR" is the right call (mechanical application of the existing ADR-065 seam + ADR-091 baseline, with slice-068/slice-098 precedent for routing files onto VAULT_ROOT without a per-file ADR), and a one-line MEPD-1 EXCLUDE justification against the codebase's "deviations need an ADR" rule is the appropriate hygiene fix. Builder draft (ACCEPTED-FIXED) is correct.

## Suspicious findings

No suspicious findings. I specifically pressure-tested whether the first Critic over-reached on **m1** ("is the Builder unlikely to bump the `==15` pin, making m1 a non-issue?"). It is NOT over-reach: the `==15` pin and the "14-element" allowlist prose sit in the *same file* the slice is actively editing (the allowlist edit is at L45-63, the count-pin at L199, the stale prose at L41/L291) — exactly the spatial-proximity condition under which a cross-number reflexive bump happens. Filing it as a Minor (not dropping it) is calibrated correctly. No first-Critic finding rests on an unverified design cross-reference or a speculative concern.

## Missed findings

No missed findings — the first Critic's coverage is complete. I independently re-applied all eight dimensions and specifically hunted for another real-repo assertion or consumer that breaks when production must-rewrite → 0 or when project_frame_synth joins the allowlist:

- I grepped `tests/` for every reader of `MUST_REWRITE` / `must-rewrite` / `_BASELINE` / `baseline_tuple` / `project_frame_synth`. The only real-repo (non-tmp-fixture) assertions touching the production must-rewrite set are: `test_emits_classified_inventory_with_evidence` (B1, breaks — covered), `test_must_rewrite_baseline_pinned` (both sides empty → passes; design item 4 handles the narrative), and `test_production_baseline_unchanged_vs_slice100` (both sides empty → passes; M2 handles the misleading name). Every other classifier test (`test_new_unrouted_literal_fails_gate`, `test_every_hit_has_exactly_one_class`, etc.) uses synthetic `tmp_path` fixtures and is unaffected by the real-repo set emptying. No fourth real-repo breaker exists.
- **Behavior-preservation guard (AC3)**: I verified `test_project_frame_synth.py::_build_fixture` writes `root/"architecture"/concept.md` etc. and calls `synthesize_frame(tmp_path, slice_dir)` with no `AI_SDLC_VAULT_ROOT` env and no git-common-dir config, so `VAULT_ROOT == Path("architecture")` and `repo_root / VAULT_ROOT / "concept.md"` resolves byte-identically to the pre-routing `repo_root / "architecture" / "concept.md"`. The routing is a true no-op against the existing standing guard — no missed AC3 break.
- **Orphan-literal consequence (design §Consequence-chain #2)**: I ran the exact `test_no_orphan_architecture_literal_in_migrated_tools` regex (`["\']architecture[/"\\]`) against the real `project_frame_synth.py`: it matches L44/L121/L122/L185/L194, and NONE are skipped by the comment/docstring `startswith('#'/'"'/"'")` heuristic. This confirms the design's claim that the L44 docstring example WOULD fail the orphan guard once the file joins the allowlist and must be rephrased — and confirms the first Critic was right to treat this as already-handled-in-design rather than a miss. The space-prefixed L48 (`--slice-dir architecture/...`) and L304 argparse `help=` do NOT match (no quote/apostrophe immediately before `architecture`), as the design states.
- **B1-fix soundness (does the synthetic-fixture rework lose meaningful real-repo coverage?)**: It does not, and a `TEST_UPDATE_AT_FLIP`-on-real-repo substitute is NOT required. The `assert MUST_REWRITE in classes` real-repo assertion was only ever satisfied by the existence of unrouted production debt (the 4 project_frame_synth sites); it is an incidental coupling to a state the ADR-091 readiness program intends to drive to zero, not a deliberate invariant. The classifier's positive-case non-vacuity ("a bare-segment path-construction DOES classify must-rewrite") is independently and permanently proven by the synthetic `test_new_unrouted_literal_fails_gate` (a `tmp_path` fixture, unaffected by the real-repo set). So the Builder's choice — move the `MUST_REWRITE`-presence proof onto a synthetic injected-literal fixture mirroring that existing pattern, keep the `ALREADY_SEAM_ROUTED`/`DOC_EXAMPLE_SAFE` real-repo presence asserts — is sound and arguably the only correct option. Asserting a real-repo `MUST_REWRITE` after deliberately emptying it would be asserting the slice's own goal failed.

No Blocker-class blind spot, no severity-pattern blindness (the first Critic did not flag 3 minors while missing a major — its single Blocker is the one true build-breaker, correctly the headline).

## Severity adjustments

No severity adjustments. Every finding's severity is calibrated correctly (B1 Blocker, M1/M2 Major, m1/m2 Minor — each verified above against its production-impact / gate-impact path).

## Notes
High confidence in this ACCEPT. Every load-bearing claim was verified by execution against the real code (the audit JSON, the orphan regex against the live file, the test-module source, and the shippability rows) rather than trusted from the first Critic's prose — and all four checks reproduced the first Critic's findings exactly. Calibration observation: this is a well-disciplined first-Critic pass — it ran `audit_root` and the orphan regex empirically (it did not reason from the design's prose), correctly distinguished a true Blocker from the surrounding Majors/Minors, and resisted the temptation to manufacture findings on a clean routing change (Security/Contracts/Over-engineering all correctly "none"). The one reservation worth flagging to TRI-1 is not a critique gap but a build-execution risk the design already names: B1, M2, and the L44 orphan-rephrase are all ACCEPTED-PENDING / build-time edits, so the slice's correctness hinges on the Builder actually applying the enumerated four-file coupled chain AND running the B1 fix red-before/green-after — the mid-slice smoke gate (`[production] 0 must-rewrite` + byte-identical frame output) is the right tripwire for catching a partial application. No new finding is warranted; the design's enumeration is complete.
