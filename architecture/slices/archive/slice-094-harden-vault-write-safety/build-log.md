# Build log: Slice 094 harden-vault-write-safety

**Date**: 2026-06-01
**Result**: SHIPPED (pre-finish gate PASS; 6/6 tasks; full suite 1405 passed; AC4 redesigned mid-build via the full Critic stack)

## Events (append-only — Step 7c flight recorder)

- 2026-06-01 16:22 BUILD: v2 plan approved (flip-readiness, 6 tasks + R-33 Step-0 merge); stage→build. Running in a real BRANCH-2 worktree slice/094 (worktree discipline honored, not skip-escaped).
- 2026-06-01 16:22 BUILD: Step 0 R-33 — `git merge master` (095/096) → 1 conflict (slice-queue.md, additive); resolved `--theirs` (master's post-096 regen, regenerates at next /slice); merge commit ff0a919.
- 2026-06-01 16:22 BUILD: integration sane — VERSION=0.79.0 (→094 bumps 0.80.0), `_vault_write.py` still 094-base (CRLF bug intact for Task 1), SVW-1 infra (vault_edit / skill_vault_write_safety_audit) present.
- 2026-06-01 16:30 BUILD: Task 1 byte-fix `_vault_write.py` — `safe_write_text` +`newline=""`, `safe_append_text` +`| getattr(os,"O_BINARY",0)` (POSIX-safe no-op). Verified on live module: small + 4002B multibyte both LF, byte-identical to canonical. `test_vault_safe_write.py` +3 (nt byte-identity / structural guard / append-semantics) → 10/10 pass.
- 2026-06-01 16:32 BUILD: Task 2 route 2 seam writers — `slice_queue_writer:819` + `slice_queue_claim._atomic_write_text` → `safe_write_text` (byte-transparent, RMW-window left as B2 flip-residual). Imports OK; 77 queue/PSQ byte-equality tests pass (no regression).
- 2026-06-01 18:05 BUILD: Task 3 START (resume). Grounded write-op corpus vs disk: PCR has 7 raw vault ops (6 resolve via ≤1-hop/module-const — 5 `log_path.open("a")` + `:1546` out_path write_text; `:430` loop-var = documented residual); seam writers + `vault_edit:116` route via safe_write_text/safe_append_text; `_vault_write` EXEMPT; NO other tool has any write op (slice_queue os.replace grep-hits were docstrings — AST necessity confirmed). `_AUDIT_LOG_PATH` = `Path("architecture/parallel-conflict-resolution-log.md")` (module AnnAssign const). Writing tools/vault_write_safety_audit.py (per-write-target AST).
- 2026-06-01 18:20 BUILD: Task 3 DONE. `tools/vault_write_safety_audit.py` (per-write-target AST, ≤1-hop+module-const depth, VAULT_ROOT-derivation tertiary signal, fail-closed, EXEMPT=_vault_write, scoped-out PCR COUNT-pinned=6). Real corpus CLEAN exit 0 (43 tools; 6 write_ops all scoped-out PCR; 4 routed; 0 violations). `tests/methodology/test_vault_write_safety_audit.py` APED-1 battery 27/27 PASS (planted-raw→VIOLATION incl. all 5 channels; ≤1-hop depth-lock→VIOLATION; reader-with-non-vault-write/error-prose-only/read_text→CLEAN; routed/tmp→CLEAN; _vault_write→EXEMPT; PCR-module→SCOPED-OUT; 2-hop-alias + container-element residuals pinned VISIBLE; COUNT-pin; CLI 0/1/2 + unparseable→2).
- 2026-06-01 18:24 SMOKE: mid-slice smoke (audit half) — reverted real `slice_queue_writer:817` safe_write_text→`out_path.write_text` → audit exit 1 naming `tools/slice_queue_writer.py:817 [.write_text]` (VAULT_ROOT-derivation fires on real corpus); restored via Edit (NOT git checkout — preserves Task-2 WIP) → audit clean exit 0, no stray marker. Concurrency half runs after Task 4.
- 2026-06-01 18:40 FINDING (Task 4 probe — re-interrogate the Critic, mission-brief mandate): empirically reconciled AC4's two proofs vs THIS machine (Win11, CPython 3.13.13, multiprocessing spawn). **Proof 1 (os.replace EPERM) REPRODUCES** — natural concurrent replace (8 workers×40) lost 21 to WinError 5; held-handle variant deterministic 150/150; `safe_write_text` (lock-serialize + bounded EPERM-retry) absorbed ALL → 0. NON-vacuous. **Proof 2 (>1024B append interleaving) does NOT reproduce at ANY size** — 4KB / 64KB / 512KB × 8 workers → 0 corruption, locked AND unlocked. Root cause: `safe_append_text` writes via a SINGLE `os.write(fd, data)` → one atomic Windows `WriteFile` regardless of payload size, so concurrent `O_APPEND` never splits/interleaves. The meta-Critic's `bugs.python.org#15723` ">1024B" claim is about buffered/text-mode `f.write()` chunking — NOT the raw-`os.write` channel this primitive uses. AC4's Proof-2 interleaving assertion is therefore VACUOUS-by-construction (mutation would NOT fail). This is the design-is-wrong-mid-build gate → STOP + AskUserQuestion (probe retained pending decision; not yet deleted).
- 2026-06-01 18:45 DEVIATION: build HALTED at Task 4 (4/7) by user TRI decision → **"Halt + formal redesign"** (NOT inline deviation). Per build-slice "design-is-wrong → revise": re-run /design-slice (rewrite AC4 Proof 2 vs verified reality) → /critique + /critique-review on the change → TRI-1 → re-present build plan → resume /build-slice Task 4. Tasks 1–3 + their tests stand (unaffected by the AC4-Proof-2 correction). Grounding the redesign with one more probe (exclusive-holder EPERM for os.open/os.replace) so the new Proof 2 asserts a VERIFIED hazard, not another unverified claim.
- 2026-06-01 18:55 FINDING (redesign grounding — VERIFIED, deterministic): an EXCLUSIVE external handle (`CreateFileW` dwShareMode=0 — faithful OneDrive/AV/indexer sim, the exact scenario `_vault_write`'s docstring cites) makes BOTH raw ops raise WinError 5: `os.replace` raw_eperm=1 / `safe_write_text` retry→0; `os.open(O_APPEND)` raw_eperm=1 / `safe_append_text` retry→0. So the APPEND path HAS a real reproducible hazard the lock+retry defends (EPERM-on-open, the m2 fix) — deterministic, non-vacuous by mutation. Corrected Proof 2 = append EPERM-resilience via exclusive-holder (replaces the structurally-impossible >1024B-interleaving claim). This is STRONGER than the original (deterministic vs timing-dependent). Probe deleted; evidence captured here. Redesign proceeds: /design-slice → /critique → /critique-review.
- 2026-06-01 19:30 FINDING (v3 /critique returned **BLOCKED** — B1): the Critic ran its OWN probe and claims unlocked concurrent `O_APPEND` loses WHOLE writes (3-4/8 workers, ≥512B, "Windows os.open O_APPEND is emulated lseek+write, not atomic FILE_APPEND_DATA") — a DIFFERENT corruption mode than my probe measured (I checked interleaving + line-count; got 0 loss). CONTRADICTS my probe v2. If true, my "drop the append proof" premise is FALSE and the append lock is load-bearing (prevents lost-updates = the real R-32 hazard). Reconciling NOW by reproducing the Critic's EXACT construction (1 os.write/worker, length + distinct-worker check) before drafting B1 disposition — neither rubber-stamp nor dismiss.
- 2026-06-01 19:45 FINDING (B1 RECONCILED — **the Critic was RIGHT; my v3 premise was FALSE**): my reconcile probe (Critic's exact construction, NO barrier) showed 0 loss — but that was a SPAWN-STAGGER artifact (workers start ~100ms apart → never actually overlap). The DECISIVE barrier-synchronized probe (`mp.Barrier`, all workers released simultaneously): UNLOCKED concurrent `O_APPEND` loses whole writes MASSIVELY — 16 workers→5-10 present (6-11 lost), 32 workers→7-15 present (17-25 lost), EVERY size 64B-64KB, every trial; LOCKED (safe_append_text)→16/16 & 32/32, 0 lost, every run. Mechanism confirmed: `os.write` prevents byte-INTERLEAVING (present workers' bytes are contiguous) but concurrent `O_APPEND` EOF-positioning is NON-atomic on Windows → lost UPDATES. So the append lock is LOAD-BEARING (prevents the real R-32 lost-update hazard the primitive's docstring promises), NOT dead weight. My v3 "os.write atomic → 0 corruption → drop append proof" conflated no-interleaving with no-corruption. B1 ACCEPTED. Corrected Proof 2 = barrier-synchronized append LOST-UPDATE prevention (deterministic, non-vacuous by mutation, no timing coupling). Two-persona model caught my error — exactly its purpose.
- 2026-06-01 20:30 BUILD: v3 redesign through full stack — /design-slice (AC4 corrected: Proof 2 = append LOST-UPDATE, barrier-synchronized; Proof 1 = whole-file EPERM Event-gated; M2 cross-platform canary) → /critique BLOCKED (B1 confirmed by my barrier probe) → /critique-review ADJUST (B1 confirmed independently, B2→Major, +M-add-1 POSIX-canary) → TRI-1 ratified NEEDS-FIXES (B2 ACCEPTED-FIXED + M-add-1 ACCEPTED-FIXED applied to design/mission-brief; B1/M1/M2/m1/m2 ACCEPTED-PENDING). triage_audit + critique_review_audit clean.
- 2026-06-01 20:40 TEST: Task 4 DONE — `tests/methodology/test_vault_write_safety_concurrency.py` 4/4 PASS (spawn+mp.Barrier+bounded-timeout; spawn-reimport pre-verified by throwaway probe). Proof 1 whole-file EPERM (Event-gated holder, M1): raw os.replace→WinError 5, safe_write_text absorbs. Proof 2 append LOST-UPDATE (barrier, B1): mutation raw os.open(O_APPEND) loses writes, safe_append_text→all 20 survive; safe-path cross-platform canary (M2).
- 2026-06-01 20:42 TEST: NON-VACUITY by mutation (slice-092, gold-standard) — stripped the REAL `safe_append_text` lock (`with _file_lock`→`nullcontext`, spawn children re-import from disk) → `test_append_safe_loses_zero_writes` FAILED "LOST 10 of 20 writes"; reverted → 4/4 pass, no stray marker. Lock is load-bearing, proven on the real primitive. m1 note added to slice-093 thread test (points to this authoritative proof); m2 mooted (Proof 2 is lost-update not EPERM; Proof 1 keeps .winerror==5 on os.replace per Critic).
- 2026-06-01 20:45 SMOKE: mid-slice smoke gate PASS — VWS-1 audit clean exit 0 + 41 slice tests pass (10 byte-identity + 27 VWS-1 APED-1 + 4 concurrency). Integration checkpoint clean before the v0.80.0 version-bump fan-out (Task 5).
- 2026-06-01 21:10 BUILD: Task 5 DONE — v0.80.0 5-part PMI-1 bump (VERSION + pyproject + plugin.yaml version+tool-entry + installed ~/.claude/ai-sdlc-VERSION) + install_audit _CANONICAL_TOOLS + cp1252 _ROOT_ONLY_TOOLS + INSTALL.md 38→39 + methodology-changelog v0.80.0 entry (repo + forward-synced to ~/.claude/, MCFS-1) + EPGD-1: version-gate test renamed _at_v_0_79_0→_at_v_0_80_0 (prior entry-pins preserved) + new test_v_0_80_0_vws_1_entry_present_in_repo + _shippability_consumer_propagation. pip install --upgrade → ai-sdlc-tools-0.80.0 (TVFS-1). Verified: PMI-1/INST-1/UTF8/MCFS-1/AVFS-1/TVFS-1 all exit 0; changelog+cp1252 tests 175 pass.
- 2026-06-01 21:18 BUILD: Task 6 DONE — shippability #104 (RECONCILED vs R-33 post-merge: catalog at 103 not 101 — 096+095 merged; appended via `vault_edit append` = SVW-1 safe channel, dogfooded) + .gitignore `*.tmp` (M4; `*.lock` already present from slice-093/095) + risk-register R-32 reframe (slice-094 paragraph: Python-writer sub-class ENFORCED, flip-READINESS, stays `mitigating`, explicit flip residual). All 6 tasks complete; mid-slice smoke already PASS.
- 2026-06-01 21:30 BUILD: pre-finish gate — BC-1 applicable Critical rules attested: **BC-PROJ-3 + BC-GLOBAL-2** (git-revert safety): this slice performed NO destructive `git checkout`/`reset`/`restore`/`stash` of uncommitted work — the smoke-gate writer-revert AND the concurrency non-vacuity lock-strip were both reversible *file Edits*, restored via Edit (reflog confirms only commits + the R-33 merge). **BC-PROJ-7** (new audit-tool wiring): `vault_write_safety_audit` added to the cp1252 `_ROOT_ONLY_TOOLS` coverage list AND shippability row #104 is pipe-free (7 pipes = well-formed 6-col). BC-1 `--strict --ack-critical BC-PROJ-3 BC-PROJ-7 BC-GLOBAL-2` → exit 0. Important rules (BC-PROJ-1/4/5/6/8/9/10/11 + BC-GLOBAL-1) addressed by the version-bump fan-out + entry-pins (no defer). LINT-MOCK on 5 changed test files → no violations (the new tests use real spawn/files/AST, zero mocks).
- 2026-06-01 21:50 BUILD: /code-review (mandated 2nd APED-1 author) → 0 Blockers / 1 Major / 3 Minors (advisory). **M1 ACCEPTED-FIXED**: VWS-1 fail-OPEN for un-enumerated channels (code-Critic executed: planted `os.rename`/`io.open`/`shutil.move`→ status=clean). Closed by adding `os.rename` (twin of os.replace), `io.open`, `shutil.move/copyfile/copy/copy2` to `_write_target` + extended APED-1 battery (planted each → VIOLATION); design §Detection model + audit docstring aligned. **m1 ACCEPTED-FIXED**: dropped bare `O_CREAT` from `_WRITE_OPEN_FLAGS` (O_RDONLY|O_CREAT no longer FP) + negative test. **m2/m3 ACCEPTED-AS-DOCUMENTED** (both fail-safe, benign in corpus; logged for /reflect). Re-verified: VWS-1 audit clean exit 0 (no corpus instance for new channels); VWS-1 battery 46 pass (was 41, +5).

## Summary (filled at slice end)

### Plan executed
- Step 0 — R-33 master-merge: DONE (ff0a919; master fully integrated, version target 0.80.0)
- Task 1 — byte-fix primitives + byte-identity test: DONE (10/10; live-verified small+4002B multibyte LF)
- Task 2 — route 2 seam writers: DONE (77 queue/PSQ tests, byte-transparent)
- Task 3 — VWS-1 audit + APED-1 battery: DONE (`tools/vault_write_safety_audit.py` clean exit 0 on real corpus; `test_vault_write_safety_audit.py` 27/27 — planted-raw→VIOLATION incl. 5 channels, ≤1-hop depth-lock, reader/error-prose/read_text→CLEAN, EXEMPT/SCOPED-OUT, residuals pinned, COUNT-pin, CLI 0/1/2)
- Task 4 — concurrency proof: DONE — **AC4 redesigned mid-build** (v3 /design+/critique+/critique-review+TRI-1: the >1024B-interleaving framing was empirically false; the append lock IS load-bearing for lost-UPDATE). `test_vault_write_safety_concurrency.py` 4/4 (spawn+mp.Barrier): Proof 1 whole-file EPERM (Event-gated holder, M1), Proof 2 append LOST-UPDATE (barrier; M2 cross-platform canary). Non-vacuity by real-lock-strip mutation: 10/20 lost → 0/20.
- Task 5 — v0.80.0 5-part PMI-1 bump + EPGD-1 entry-pins + pip re-install: DONE (PMI-1/INST-1/UTF8/MCFS-1/AVFS-1/TVFS-1 exit 0; version-gate renamed _at_v_0_80_0 + 2 v0.80.0 entry-pins; INSTALL.md 38→39 + 2 inventory-pin tests reconciled)
- Task 6 — shippability #104 (R-33-reconciled: catalog at 103 not 101) + .gitignore `*.tmp` + risk-register R-32 reframe: DONE

### Mid-slice smoke gate
**Result**: PASS
**Evidence**: VWS-1 audit clean exit 0 + real-file revert-loud-then-restore (`slice_queue_writer:817` un-routed → exit 1 naming `:817 [.write_text]`; restored → exit 0); 41 slice tests pass (10 byte-identity + 27 VWS-1 APED-1 + 4 concurrency).

### Pre-finish gate
- [x] All ACs pass with evidence — concurrency 4/4, audit 27/27, byte-identity 10/10 (validation.md at /validate-slice)
- [x] Must-not-defer addressed — shippability #104 (RPCD-1); cp1252-safe audit (UTF8-STDOUT-1); fail-closed (R-7); non-vacuous concurrency (proven by lock-strip mutation); BOTH primitives byte-faithful; APED-1 executed (+ /code-review = mandated 2nd APED-1 author, next); 5-part bump; EPGD-1 entry-pins
- [x] /drift-check (DCE-1) — slice-094 Trigger marker in drift-log.md; audit clean
- [x] Mid-slice smoke still passes (no regression)
- [x] No new TODOs / FIXMEs / debug prints (grep-verified)
- [x] Ran in a real BRANCH-2 worktree (branch_workflow_audit pass)
- [x] R-33 master-merge FIRST (ff0a919; version target reconciled to 0.80.0)
- [x] Full suite: **1405 passed, 0 failed**
- [x] 18-audit gate: PMI-1/INST-1/UTF8-STDOUT-1/CRP-1/PCA-1/BCI-1/MCFS-1/STP-1/AVFS-1/TVFS-1/NAW-1/DCE-1/SVW-1/BRANCH-2/WIRE-1/BC-1(strict)/VWS-1/TF-1(n-a) all PASS; LINT-MOCK clean

### Design deviations
- **AC4 concurrency-proof redesign (mid-build, user-ratified "Halt + formal redesign")**: the v2/v3-first-pass ">1024B append interleaving" proof was empirically falsified at build (`os.write` byte-atomic); replaced with append LOST-UPDATE prevention (the Critic's B1, Builder-confirmed by barrier-synchronized probe). design.md/mission-brief/ADR-context updated; v3 /critique NEEDS-FIXES + /critique-review ADJUST, TRI-1 ratified. NOT a silent deviation — full review stack re-run.

### Files changed
- NEW: `tools/vault_write_safety_audit.py`, `tests/methodology/test_vault_write_safety_audit.py`, `tests/methodology/test_vault_write_safety_concurrency.py`
- MODIFIED (code): `tools/_vault_write.py` (byte-fix), `tools/slice_queue_writer.py` + `tools/slice_queue_claim.py` (routed), `tools/install_audit.py` (+tool)
- MODIFIED (inventory/version): `plugin.yaml`, `VERSION`, `pyproject.toml`, `INSTALL.md`, `methodology-changelog.md` (+ installed `~/.claude/` copies + `ai-sdlc-VERSION`), `.gitignore` (`*.tmp`)
- MODIFIED (tests): `test_vault_safe_write.py` (m1 note), `test_methodology_changelog.py` (version-gate rename + 2 entry-pins), `test_utf8_stdout_regression.py` (cp1252 list), `test_stranded_slice_audit_tool_inventory.py` + `test_pulse_worktree_resolver_tool_inventory.py` (count 38→39)
- MODIFIED (vault): `architecture/shippability.md` (#104 + row-#75 citation rename), `architecture/risk-register.md` (R-32 reframe), `architecture/drift-log.md` (slice-094 marker), slice-094 `mission-brief.md`/`design.md`/`critique.md`/`critique-review.md`/`milestone.md`/`build-log.md`
