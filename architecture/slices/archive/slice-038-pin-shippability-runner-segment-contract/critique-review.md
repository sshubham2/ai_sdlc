# Critique Review: Slice 038 pin-shippability-runner-segment-contract

**Reviewed by**: critique-review agent (DR-1)
**Date**: 2026-05-17
**First-Critic verdict**: NEEDS-FIXES
**Dual-review verdict**: ADJUST

## Summary

The first Critic's review is substantively sound: B1, M1, M2, m1, m2 are all VALID with correct severities and verify cleanly against the real artifacts. The one adjustment is B2's severity — the obligation is real but the Builder's own evidence-correction (only PTFFD-1/slice-037 has the paired changelog test; QD-1/EOL-DRIFT-1/TFFL-1/SRCD-1 do not) demotes it from a settled-convention Blocker to a Major: a single-precedent inference, not a violated N≥3 invariant. No missed findings of Blocker/Major weight surface from independent re-review; the recursion analysis is the one place the first Critic excelled (B1 caught the load-bearing UTF8-STDOUT-1 hazard the Builder's original draft missed).

## Confirmed findings

- **B1** (new `main()` breaks UTF8-STDOUT-1 parity = row #28 dogfood) — CONFIRMED; Blocker correct. Verified `test_utf8_stdout_regression.py` L278-298 `_discovered_audit_tools()` globs every `tools/*.py` non-`_`/non-`__init__` with top-level `main`; L387-399 asserts `discovered == covered`, no exemption hatch. Runner's `main()` enters `discovered_set` automatically; `architecture/shippability.md:36` row #28 seg 1 = `test_utf8_stdout_regression.py` = the slice's own dogfood. Textbook slice-022 self-violation; first Critic caught what the Builder's original recursion note waved off. Applied fix fully discharges it.
- **M1** (presence-pin vs content-pin) — CONFIRMED; Major correct. `test_methodology_changelog.py` L2900-2912 (PTFFD-1) asserts a phrase + ADR + "supersedes nothing", not bare token presence. Applied fix mirrors the canonical instance.
- **M2** (PMI-1 4-part not 3-part) — CONFIRMED; Major correct. 4-part atomic bump is the N≥3 precedent; 3-part shorthand drops the INST-1-audited `~/.claude/ai-sdlc-VERSION` leg. Applied fix states all four legs + sequences installed reconciliation before INST-1.
- **m1** (dual non-identical enumeration surfaces) — CONFIRMED; Minor correct. `_CANONICAL_TOOLS` and `plugin.yaml` tools list differ in membership; `main()`-bearing non-`_` tool must be in BOTH.
- **m2** (negative fixture must be a contrast) — CONFIRMED; Minor correct. `_segments()` is correct by construction (L179-188); asserting it is correct is tautological. Applied fix specifies the explicit naive-strip-vs-`_segments()` contrast on real row #28 + smoke-gate enforcement.

## Suspicious findings

None. Every first-Critic finding verifies against the real artifacts. The reuse claims (the `shippability_decoupling_audit`→`shippability_path_audit` import precedent L69-74; `_segments()` L179-188; SKILL.md L213; R-8 lineage risk-register:148-158) are all real — re-checked the import precedent directly, it holds. No over-reach anywhere.

## Missed findings

No missed findings of Blocker or Major weight. Independent re-application of all 8 dimensions to design.md, including the briefed candidates:

- **(a) UTF8-STDOUT-1 fix self-recursion / invocation-path change** — Checked. `_POSITIONAL_SLICE_TOOLS` (L81-92) → `_positional_slice_argv` = `[PY,"-m",tool,str(fixture_dir)]`; runner CLI is exactly that shape (single positional catalog path). Correct bucket (not `_ROOT_ONLY_TOOLS`); fix adds runner to exactly the two parity-input sets, fixed-point in one step. No second-order recursion. Not missed.
- **(b) `_CANONICAL_TOOLS` "18 entries" claim** — **Inaccurate: actual 22, not 18.** Documentation-only annotation with zero behavioral path (`install_audit` reads the tuple, not the parenthetical), but FBCD-1 count-drift class (slice-020 watch-list). Filed **m-add-1 (Minor)**: design.md change "18 entries"→"22 entries (23 after this slice)"; grep-verify live count at build. Does not change m1's disposition.
- **(c) Cross-module reuse of underscore-private `_segments` et al. — CSP-1/INST-1 concern?** — Checked, no. Precedented: `shippability_decoupling_audit` itself imports `_TEST_PATH_RE`/`_find_repo_root`/`_is_separator_row`/`_parse_table_cells` from `shippability_path_audit` (L69-74). Repo convention is cross-`tools` private reuse = CSP-1-avoidance (single source beats copy-paste divergence); ADR-039 §Consequences names it an intentional single-point-of-edit property. First Critic correctly did not flag. Not missed.
- **(d) BFRD-1 / not routed through `/repro`** — Checked, no. mission-brief declares `Test-first: true` with explicit failing-first repro (`test_naive_outer_strip_runner_is_rejected` fails before, passes after), TF-1 plan maps every AC to a failing-first row, mid-slice smoke gate enforces "negative case MUST fail without the pin". R-8 is a tooling false-FAIL class, not a product-code runtime bug; the TF-1 negative-fixture path + `test_first_audit.py --strict-pre-finish` is the legitimate enforced failing-first guarantee. Neither Builder nor first Critic missed a STOP-route. Not missed.
- **(e) SRSC-1-vs-SCMD-1 split vs TFFL-1↔TF-1 in-place precedent** — Checked, the split survives. SCMD-1 owns grammar *validation* (never executes); SRSC-1 owns *execution* (the runner). Distinct surfaces, failure modes, consumers (Step 5.5 item 3a gate vs item 4 invocation), risk lineage (SRSC-1 closes R-8, which recurred N=2 *despite* SCMD-1 existing). ADR-039 correctly invokes the slice-037 lesson: `-D` IDs exclusively for `-D`-family in-place refinements; a new non-`-D` gate-class earns a fresh `vN.N` ID (peers BRANCH-1/BC-1/CRP-1/PCA-1/BCI-1). TFFL-1↔TF-1 was a same-surface scope extension; this is a new surface — in-place would be the *wrong* precedent. First Critic correctly logged under Dim 7. Not missed.

## Severity adjustments

- **B2** (TF-1 plan omits the SCPD-1 `_shippability_consumer_propagation` changelog test) — **SEVERITY-WRONG: filed Blocker, recommend Major (M3).** The obligation is real and the prescription correct (verified `test_methodology_changelog.py` L2915-2941: the PTFFD-1 `_shippability_consumer_propagation` instance asserts `"PTFFD-1" in catalog` + the consumer-test filenames present in `architecture/shippability.md`; SRSC-1 has a shippability consumer per mission-brief AC4/Must-not-defer so it should mirror this). But the first Critic's justification was over-generalized ("every prior RULE-ID slice has the pair") and the Builder's evidence-correction confirms it: only PTFFD-1 (slice-037, v0.50.0) carries the pair; QD-1/EOL-DRIFT-1/TFFL-1/SRCD-1 (v0.46-0.49) ship `_entry_present_in_repo_and_installed` only. A Blocker should rest on a violated invariant or N≥3 convention; this rests on a single most-recent precedent (N=1) + a sound first-principles SCPD-1 argument. Per the same calibration discipline applied to M2 (N≥3 justified Major) and the Wiegers severity rubric (Blocker = will cause defect/violation if shipped as-is; Major = real gap, fix this slice, not a hard stop), an unenforced AC4 sub-claim backed by one precedent is a **Major**. Recommend re-filing B2 as **M3 (Major)**. Disposition stays ACCEPTED-FIXED — the applied remediation (design.md L17 + mission-brief TF-1 + ADR-039 L33) is correct and complete; the adjustment is for calibration honesty, not for what the Builder must do.

## Notes

Confidence high. The two load-bearing claims verified empirically — B1's `_discovered_audit_tools()`/`covered_set` parity (L278-399, no exemption hatch) and the PTFFD-1 propagation-precedent count for B2 (Builder's correction confirmed: N=1, not "every prior") — both check out exactly. First-Critic calibration: strong on the recursive-self-application dimension (B1 is this slice family's standing blind spot and the first Critic caught it where the Builder's draft dismissed it). One weakness: B2 severity inflation from an over-generalized evidence base — a Blocker resting on "every prior RULE-ID slice has the pair" that is actually N=1; the Builder caught the evidence error but kept the Blocker label, and the first Critic embedded the over-generalization rather than challenging it. Mild Critic-stack tendency to round single-precedent inferences up to Blocker — watch-list note for `/critic-calibrate` if it recurs (N=1 here). m-add-1 filed: stale "18 entries" count (actual 22) — documentation-only, no behavioral path, but the FBCD-1 count-drift class this codebase watch-lists; grep-verify live counts at build.
