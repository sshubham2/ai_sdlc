# Reflection: Slice 063 add-build-slice-new-agent-warning

**Date**: 2026-05-23
**Shipped**: YES (5/5 ACs PASS; all 17 audits clean; shippability runner 63/63 PASS; full pytest suite 892/892 PASS in 28.44s)

## Validated

- **NAW-1's union-of-three-sources read mechanism behaves correctly across all 3 documented sources** — empirically verified at Phase B mid-slice smoke + /code-review re-run + /validate-slice: source (i) `git diff --diff-filter=A {base}` returns staged-uncommitted; source (ii) `git ls-files --others` returns untracked; source (iii) `git diff --diff-filter=A {base}...HEAD` returns commits-vs-base; the deduplicated union covers every state a new agent file can occupy at /build-slice Step 6 per ADR-061 §Decision L60-67. The B1 critique-fix mechanism shipped exactly as designed.
- **Binary exit contract by construction** — empirically validated by `pytest tests/methodology/test_new_agent_warning_audit.py::test_audit_exits_0_on_new_agent_diff_with_warn_line` asserting `result.exit_code == 0` on the WARN branch; structurally validated by inspection of `tools/new_agent_warning_audit.py` (the only `result.exit_code` assignments are at L284/293/301/306 = 2 and L321/327 = 0; no code path sets exit_code to 1). NEVER exit 1 by construction holds.
- **WARN-line template anchors** — `_format_warn_line("agents/foo.md")` returns a string containing all 3 anchors (agent path + "restart Claude Code" + "R-18") per ADR-061 §Decision Attributed WARN message; pinned by `test_warn_line_cites_agent_path_session_restart_and_r_18`.
- **PMI-1 5-inventory fan-out is fully discharged** — empirically verified by /code-review agent reading all 5 sites + by INST-1 reporting `28/28 tool modules` post-bump (slice-063 +1). `plugin.yaml`, `install_audit._CANONICAL_TOOLS`, `test_utf8_stdout._ROOT_ONLY_TOOLS`, `INSTALL.md` L22 + L166, `shippability.md` row #63 all changed and consistent.
- **5-part PMI-1 atomic bump 0.65.0 → 0.66.0 cleanly composed** — VERSION + plugin.yaml.version + pyproject.toml [project].version (PVFS-1) + `## v0.66.0` header + installed `~/.claude/ai-sdlc-VERSION` (AVFS-1) all match; installed `~/.claude/methodology-changelog.md` (MCFS-1) cp-synced; installed venv `ai-sdlc-tools` 0.66.0 (TVFS-1) via `pip install --upgrade .`; PMI-1 / PVFS-1 / AVFS-1 / MCFS-1 / TVFS-1 all exit 0 post-bump.
- **R-18 retirement structurally backstopped** — `**Status**: mitigating → retired` flip at risk-register.md:303 per BC-PROJ-6 single-source-of-truth discipline; RR-1 audit confirms R-18 NOT in open set, IS in retired set; STP-1 Sub-form B clean (the slice-063 pin `test_r_18_retired_post_slice_063` aligns with new `retired` status — no contradictory `_stays_*` pin exists).
- **Slice-063 self-application vacuous-pass** — running `$PY -m tools.new_agent_warning_audit` against slice-063's own working tree returns `{status: "clean", exit_code: 0, warnings: [], divergences: []}` because slice-063 adds zero `agents/*.md` files (only `tools/` + `tests/` + `skills/` edits + risk-register flip). Bootstrap discharge per ADR-061 §Bootstrap clause.

## Corrected

- **mission-brief AC#4 prose** — originally said "tmp_repo fixture with a single synthetic added `agents/foo.md`" (singular positive contrast), but post-fix-block TF-1 plan + Verification plan expanded to TWO positive contrasts (untracked source (ii) + staged source (i)) covering the union-of-three-sources. Harmonized at pre-meta-Critic Builder self-catch per slice-057 lesson L93 TPHD-1 sub-mode (b) carry-over discipline. Caught early (pre-/critique-review launch); kept dual-Critic stack focused on real findings.
- **design.md L17 "5-part PMI-1 atomic bump" leg enumeration** — originally listed 7 things and called them "5-part" (MCFS-1 + TVFS-1 incorrectly included as PMI-1 legs). Meta-Critic M-add-1 caught it (same defect class as slice-060 B2 + slice-062 B1 cross-doc enumeration drift). Corrected at /critique-review fix block: 5 canonical PMI-1 legs explicitly enumerated (VERSION + plugin.yaml.version + pyproject.toml.version + `## v0.66.0` header + installed `~/.claude/ai-sdlc-VERSION`); MCFS-1 + TVFS-1 documented separately as post-bump forward-sync obligations bucketed under BC-PROJ-9 fan-out.
- **design.md item 8 EPGD-1 anchor list** — originally 6 anchors; meta-Critic M-add-2 caught the missing `## v0.66.0` header anchor + `Rule reference` literal (META-1 mandatory enforcing-assertion at `test_methodology_changelog.py:136`). Extended to 8 anchors at /critique-review fix block per slice-060 + slice-062 precedent shape.
- **design.md L218 Step 6 audit list** — originally listed 14 audits including `plugin_manifest`, but `skills/build-slice/SKILL.md` Step 6 (L138-155) has only 13 audits (PMI-1 is enforced via paired tests, not a Step 6 checklist line). Meta-Critic M-add-3 caught it; corrected at /critique-review fix block.
- **mission-brief AC#5 Test-path cell** — originally `tests/methodology/test_risk_register.py (extended)`; that file doesn't exist (PTFCD-1 phantom-citation). First Critic M1 caught it; corrected at /critique fix block to `tests/methodology/test_risk_register_audit_real_file.py (extended)`. Then at /build-slice Phase D4, PTFCD-1 strict-pre-finish revealed the `(extended)` suffix itself doesn't resolve to a real file (slice-054 lesson recurrence); corrected in-band by removing the suffix on AC#2 + AC#5 rows.

## Discovered

- **R-18 RUNTIME recurrence is non-deterministic across Claude Code sessions** — slice-061 (N=1) + slice-062 (N=2) both hit `Agent type 'code-review' not found` at `/code-review` auto-advance in the same session as slice-060's agent install. Slice-063's own `/code-review` did NOT hit this — the agent spawned cleanly. The agent registry session-cache miss is NOT a deterministic class signal; runtime behavior depends on session-start ordering vs install-time ordering that is not fully observable from outside Claude Code. **Implication**: NAW-1's methodology-side mitigation (surface the WARN BEFORE the next slice's chain runs) is structurally correct REGARDLESS of runtime non-determinism — methodology-discoverability is the load-bearing property, not runtime-deterministic-prevention. The slice-061 + slice-062 reflection L40 + L78 nomination of R-18 as slice-063 candidate was correct on the discoverability axis even though the runtime axis turns out to be non-deterministic.

- **/code-review SKILL.md v1 carries the SAME B1 falsifier class slice-063 just retired for NAW-1** — `git diff <base>...HEAD` is commit-vs-commit only; at `/build-slice` Step 6 (which is exactly where `/code-review` auto-advances from), slice work is uncommitted in WT (commits at `/commit-slice` per PCA-1 HARD-STOP terminal contract). The empty-diff path normally writes `Result: NO-CODE-CHANGES` and auto-advances — silent false-positive. User-ratified SOAD-1 augmentation at /code-review Step 1 routed around the falsifier with a WT-aware file list (`git diff --name-only HEAD` + `git ls-files --others --exclude-standard`). **N=2 cumulative on the B1 falsifier class** (NAW-1 surface + /code-review SKILL.md surface). **Slice-064+ candidate**: mirror NAW-1's union-of-three-sources fix to `/code-review`'s diff resolution. Meta-irony: the slice fixing the read-mechanism falsifier on NAW-1's surface immediately surfaced the SAME defect class on `/code-review`'s own surface.

- **Code-Critic m1 finding (advisory)** — `tools/new_agent_warning_audit.py:177-178` has a SECOND unreachable `try/except FileNotFoundError` block (the L163-166 handler already absorbs FileNotFoundError + returns None); plus a UX wart where missing-`git` environment surfaces `_USAGE_DEFAULT_BRANCH_UNRESOLVABLE` ("Configure an `origin/HEAD` ref...") instead of `_USAGE_GIT_MISSING` ("`git` command not found on PATH"). Severity Minor (functional behavior unchanged — exit 2 either way). Declined in-band fix per CRSI-1 v1 walking-skeleton advisory-only discipline. **Slice-064+ candidate**: drop BOTH `try/except FileNotFoundError` blocks from `_resolve_default_branch` and let propagation handle it via `check()`'s explicit L299-303 `_USAGE_GIT_MISSING` handler.

- **PTFCD-1 `(extended)` Test-path suffix recurrence (N=2 cumulative: slice-054 first observed + slice-063 here)** — TF-1 strict-pre-finish at /build-slice Phase D6 surfaced 2 violations on AC#2 + AC#5 Test-path cells (`(extended)` annotation suffix breaks the path-resolves-to-disk check). Corrected in-band per slice-054 lesson by removing the suffix. **N=2 watch-list candidate** for /critic-calibrate: "TF-1 plan Test-path cells must be bare file paths; annotations like `(extended)` belong in Notes section below the table." User declined BC-1 promotion this slice (recommended option); track as N=2 candidate — promote to BC-PROJ-NNN if N=3 emerges.

- **First-Critic vs meta-Critic specialization signal extends to N=2 cumulative** (slice-062 N=1 → slice-063 N=2): first Critic's findings concentrated on prose drift + cross-doc citation hygiene + cross-document coherence (slice-063 first-Critic 8/10 findings); meta-Critic's missed findings concentrated on cross-document enumeration drift + empirical-execution checks (slice-063 meta-Critic 3 missed all in these classes). Pattern: first-Critic specializes in surface-level drift catching; meta-Critic specializes in cross-document mechanical consistency + audit-execution. If N=3 specialization confirms at slice-064+, consider documenting as a stable Critic-stack design observation (NOT a Critic-prompt dimension extension — the dual-Critic stack IS the structural backstop for the cross-doc-coherence class by design).

- **Code-Critic third-pass adds genuine novel findings on a slice with already-CLEAN dual-Critic stack** — slice-063's first-Critic + meta-Critic both passed CLEAN at TRI-1 (13/13 ACCEPTED-FIXED); the code-Critic m1 finding is a novel small-scope observation the dual-Critic stack didn't reach (because they reviewed mission-brief + design.md + ADR-061 prose, not the actual code). N=1 data point on CRSI-1's value proposition: even on a slice with strong design-stack precision, the code-Critic third-pass catches genuine code-level observations the design-stack structurally couldn't reach. Supports the slice-060 / CRSI-1 thesis.

## Deferred

- **/code-review SKILL.md v1 diff-resolution falsifier fix** (slice-064 ACTIVE nomination) — mirror NAW-1's union-of-three-sources read mechanism to /code-review's own diff resolution. Strongest slice-064 candidate per the meta-irony — closing the same B1 falsifier class on its sibling surface. SOAD-1-augmented workaround held for slice-063, but slice-064+ must codify the fix or every future slice's /code-review runs the same SOAD-1 augmentation manually.

- **Code-Critic m1 finding** — drop both `try/except FileNotFoundError` blocks from `_resolve_default_branch` per code-Critic m1 proposed fix option (b). Minor + UX-improvement-only; no functional regression at v1. Coupled with the slice-064 /code-review SKILL.md fix as a bundled cleanup OR as a separate trivial slice.

- **/code-review v2 (TRI-1 routing + verdict-driven block on /validate-slice + AI-bloat passes)** — explicitly nominated in slice-060 reflection L37 + slice-062 reflection L38 + slice-063 mission-brief Out-of-scope L65 as "highest-priority" next slice-063+ candidate. slice-063 picked R-18 mitigation first (correct call — /code-review v2's own /code-review step would have hit AGENT-UNSPAWNABLE without R-18 closed first; though it turned out R-18 was non-deterministic). Active slice-064+ candidate alongside the /code-review SKILL.md v1 falsifier fix above.

- **R-17 BRANCH-1 clean-tree precondition fix** — separate pipeline-hygiene slice candidate (slice-064+ nomination per slice-062 reflection L39). slice-063's own /build-slice prerequisite check encountered R-17 (the slice-062-bump-artifacts-uncommitted-on-slice-062-branch case); resolved via `/commit-slice --merge` on slice-062 first per user-ratified SOAD-1 option. R-17 still mitigating; methodology surface for the precondition gate remains uncodified.

- **R-13 OSDG-1 extension to `/slice-candidates`** — lower-priority drift-guard family extension; slice-064+ candidate.

- **Multi-session / parallel-slice / worktree-per-slice execution** — user-raised at slice-062's /slice invocation; remains backlog candidate (multi-day; touches BRANCH-1, PCA-1, OSDG-1, multiple audits).

- **PTFCD-1 `(extended)` Test-path annotation discipline** — N=2 watch-list candidate; user declined BC-1 promotion this slice. Promote to BC-PROJ-NNN if N=3 emerges, or sharpen via /critic-calibrate route.

## Critic calibration

Per TRI-1: scoring each finding from `critique.md` `## Triage` table + `critique-review.md` missed findings + reality observed during build/validate.

**First Critic** (10 findings — all from `critique.md`):

- **B1 (read mechanism `git diff <base>...HEAD` cannot observe uncommitted slice work)**: **VALIDATED** — disposition ACCEPTED-FIXED. Empirically reproduced at /build-slice Phase B (the audit's binary-exit assertion would have FAILed with empty diff if the union-of-three-sources fix hadn't been applied). The meta-irony is that /code-review SKILL.md v1 carries the SAME falsifier class on its own diff resolution — discovered at slice-063's /code-review run. Highest-value finding of the slice; the B1 fix held.
- **M1 (phantom test-file `test_risk_register.py` — PTFCD-1)**: **VALIDATED** — disposition ACCEPTED-FIXED. Caught at /critique. The corrected path `test_risk_register_audit_real_file.py` exists; the new function `test_r_18_retired_post_slice_063` lives there per slice-041 R-4 retirement-precedent housing.
- **M2 (`--strict-pre-finish` CLI flag drift — FBCD-1 sub-mode (a))**: **VALIDATED** — disposition ACCEPTED-FIXED. NAW-1's CLI has no `--strict-pre-finish` flag (binary discovery audit, not a PENDING→PASSING state machine); the assertion would have FAILed with argparse exit 2.
- **M3 (BRANCH-1 "fallback `main`" misattribution)**: **VALIDATED** — disposition ACCEPTED-FIXED. `tools/branch_workflow_audit.py:127-146` empirically returns `None` (no `main` fallback); NAW-1's resolver mirrors that semantic correctly.
- **M4 (AC#2 test housed in wrong file — Mini-CAD-1 drift test vs structural-anchor test)**: **VALIDATED** — disposition ACCEPTED-FIXED. The structural-anchor test `test_build_slice_step_6_invokes_new_agent_warning_audit` correctly lives in `test_build_slice_skill.py` per slice-017 TPHD-1 sub-mode (c) lineage.
- **m1 (BC-PROJ-9 precedent misattribution slice-050 → slice-059)**: **VALIDATED** — disposition ACCEPTED-FIXED. The 5-inventory completeness was first articulated at slice-059.
- **m2 (overbroad `agents/*.md` pathspec)**: **VALIDATED** — disposition ACCEPTED-FIXED. `agents/AUTHORING.md` is empirically present and not in `_CANONICAL_AGENTS`; the false-positive class is real and documented in ADR-061 §Consequences.
- **m3 (bootstrap-brittle self-application test)**: **VALIDATED** — disposition ACCEPTED-FIXED. The seam-driven `test_self_application_audit_against_real_repo_is_clean_or_warn_with_known_agents` is regression-stable across slice-064+ per slice-059 TVFS-1 `installed_version_resolver` precedent.
- **m4 (CAD-1/OSDG-1 family-confusion phrasing)**: **VALIDATED** — disposition ACCEPTED-FIXED. CAD-1 (agents) vs Mini-CAD-1 / OSDG-1 (skills) distinction is real per CLAUDE.md "Self-hosting discipline".
- **m5 (missing EPGD-1 anchor list)**: **VALIDATED** — disposition ACCEPTED-FIXED (initial 6-anchor list at /critique; extended to 8 at /critique-review per M-add-2).

**Meta-Critic** (3 missed findings from `critique-review.md` + 10 first-Critic findings confirmed VALID):

- **M-add-1 (Major — 5-part PMI-1 leg enumeration drift design.md L17 vs ADR-061 L85)**: **VALIDATED** — disposition ACCEPTED-FIXED at /critique-review fix block. Same defect class as slice-060 B2 + slice-062 B1 cross-doc enumeration drift; meta-Critic's pattern-recognition across documents caught it. Highest-impact missed finding of the slice.
- **M-add-2 (Minor — EPGD-1 anchor list incomplete: missing `## v0.66.0` header + `Rule reference` META-1-mandatory)**: **VALIDATED** — disposition ACCEPTED-FIXED. Verified by inspection of slice-062 v0.65.0 entry-pin test at `test_methodology_changelog.py:4060-4109` (8 anchors including both missing-from-slice-063-design ones).
- **M-add-3 (Minor — design.md L218 Step 6 audit count miscount: `plugin_manifest` not in Step 6)**: **VALIDATED** — disposition ACCEPTED-FIXED. Empirically verified by grep `skills/build-slice/SKILL.md` for `plugin_manifest` (zero hits in Step 6 checklist).

**Code-Critic** (1 minor finding from `code-review.md`):

- **m1 (dead-code + UX-wart in `_resolve_default_branch` second `try/except FileNotFoundError`)**: **VALIDATED** — disposition declined in-band fix per CRSI-1 v1 advisory-only discipline. Severity Minor (functional behavior unchanged — exit 2 either way). Documented as slice-064+ candidate alongside /code-review SKILL.md v1 falsifier fix.

**Missed by Critic** (build-time-reachable, gate-caught classes; per slice-037 law NO BC-1 promotion for build-time-reachable classes):

1. **PTFCD-1 `(extended)` Test-path annotation recurrence (N=2 cumulative; slice-054 first observed)** — caught at /build-slice Phase D6 TF-1 strict-pre-finish; corrected in-band by removing the `(extended)` suffix. NOT a Critic-prompt promotion candidate (it's a TF-1 audit semantic the gate already enforces). N=2 watch-list /critic-calibrate candidate.

2. **/code-review SKILL.md v1 diff-resolution falsifier (N=2 cumulative on B1 falsifier class; NAW-1 surface + /code-review surface)** — caught at /code-review Step 1 attempt (returned empty diff despite 11 modified + 2 untracked in-scope files in WT). NOT a first-Critic / meta-Critic / code-Critic finding because none reviewed the /code-review SKILL.md prose against the canonical PCA-1 chain's WT-uncommitted state at /build-slice end. **Slice-064+ ACTIVE nomination**: mirror NAW-1's union-of-three-sources fix to /code-review's diff resolution. Cross-slice runtime-state class — same family as R-18, methodologically similar mitigation shape.

**Pattern**:

- **Cumulative N=13 zero-false-alarm streak on codification-class slices** holds (046/048/050/051/052/053/054/055/056/057/058/061/062 + 063). All 10 first-Critic findings + 3 meta-Critic missed findings VALIDATED at correct severity; zero false alarms; zero override-misjudged. Code-Critic m1 is the first-ever CRSI-1 finding that arrived advisory-only post-CLEAN-design-stack and was declined in-band per v1 walking-skeleton.
- **First-Critic vs meta-Critic specialization signal extends to N=2** (slice-062 N=1 + slice-063 N=2 in the same class): first Critic on prose drift + cross-doc citation hygiene; meta-Critic on cross-document enumeration drift + empirical audit re-execution. If N=3 emerges at slice-064+, document as stable Critic-stack design observation.

## Lessons for next slice

- **The B1 falsifier class (`git diff <base>...HEAD` is commit-vs-commit only) extends to N=2 cumulative across surfaces** — NAW-1 retired it on its own surface; `/code-review` SKILL.md v1 still carries it on its own diff resolution. Pattern: any methodology surface that resolves a slice's code diff via git must handle uncommitted working-tree state. The union-of-three-sources fix (`git diff --diff-filter=A {base}` + `git ls-files --others --exclude-standard` + `git diff --diff-filter=A {base}...HEAD`) is the canonical mitigation shape. Slice-064 ACTIVE nomination: mirror to `/code-review`. (slice-063)

- **R-18 retirement is methodology-discoverability, NOT runtime guarantee** — slice-063's own `/code-review` did NOT hit AGENT-UNSPAWNABLE this session, demonstrating that R-18's runtime behavior is non-deterministic. NAW-1's mitigation (surface the WARN at /build-slice Step 6 BEFORE the next slice's chain runs) is structurally correct regardless. The retirement is sound. (slice-063)

- **PTFCD-1 `(extended)` Test-path annotation recurrence is N=2 cumulative** (slice-054 + slice-063). Discipline: NEVER use parenthesized annotations in TF-1 plan Test-path cells; cite the bare file path; put annotations in Notes section below the table. Build-time TF-1 strict-pre-finish gate IS the structural backstop (slice-037 law); user-declined BC-1 promotion at N=2 keeps watch-list discipline. Promote at N=3 via /critic-calibrate route. (slice-063)

- **CRSI-1 walking-skeleton v1 value proposition validated at N=1** — code-Critic third-pass added a genuine novel m1 finding on a slice whose dual-Critic stack passed CLEAN at TRI-1 (13/13 ACCEPTED-FIXED). The design-stack and code-stack catch structurally-different defect classes; CRSI-1 v1's advisory-only walking-skeleton is the right minimum-viable shape. Slice-064 v2 (TRI-1 + verdict-block + AI-bloat passes) remains highest-priority next-loop candidate. (slice-063)

- **First-Critic vs meta-Critic emerging specialization signal extends to N=2** — first Critic on prose drift + cross-doc citation; meta-Critic on cross-document enumeration drift + empirical audit re-execution. Specialization is structural (different review surfaces, different attention budgets); if N=3 confirms at slice-064+, document as stable Critic-stack design observation, NOT a Critic-prompt dimension extension. (slice-063)

- **MEPD-1 Inclusion-heuristic firing on new-mechanism slice (slice-063 ADR-061 NAW-1 minted)** — per the slice-049/050/054/059/060 N=5 new-RULE-ID precedent (NAW-1 makes it N=6 cumulative). The voluntary-restraint-zero-bump-zero-ADR posture (slice-037/046/050/052/055/056/057/061 N=8 cumulative) is RESERVED for retirement-discharge / part-(b) closure shapes; NAW-1 is a structurally-new mechanism on the discovery-gate axis (adjacent to but NOT extending the forward-sync family). 5-part PMI-1 bump shape held verbatim per slice-060 + slice-062 precedent. (slice-063)

## Vault updates made (thin vault — small list)

- [[methodology-changelog.md]] — appended `## v0.66.0 — 2026-05-23` entry (NAW-1 mint; ADR-061 lineage; 8 EPGD-1 anchors required by entry-pin test); installed copy `~/.claude/methodology-changelog.md` forward-synced per MCFS-1.
- [[VERSION]] / [[plugin.yaml]] / [[pyproject.toml]] — 5-part PMI-1 atomic bump 0.65.0 → 0.66.0 (incl. PVFS-1 leg pyproject.toml [project].version).
- [[~/.claude/ai-sdlc-VERSION]] — AVFS-1 forward-sync (installed copy).
- Venv site-packages `ai-sdlc-tools` 0.65.0 → 0.66.0 (TVFS-1 via `pip install --upgrade .`).
- [[INSTALL.md]] — `27 → 28` at L22 + L166 (BC-PROJ-9 fan-out for the new tool).
- [[risk-register.md]] — R-18 status flipped `mitigating → retired` per BC-PROJ-6 (single-source-of-truth `**Status**:` field-line); `**Retired**: slice-063-add-build-slice-new-agent-warning (2026-05-23)` provenance field-line appended; N=2 cumulative recurrence note appended to Mitigation paragraph; retirement paragraph at section end citing NAW-1/ADR-061/v0.66.0 per slice-040 R-10 retirement-precedent shape.
- [[shippability.md]] — appended row #63 (cites BOTH NAW-1 AND R-18 AND ADR-061 per BCR-1 traceability axis; pytest command pins NAW-1's 7 audit tests + AC#2 SKILL-anchor pin + AC#5 R-18 retirement pin + 2 entry-pin tests).
- [[decisions/ADR-061-mint-naw-1-new-agent-warning.md]] — authored at /design-slice (accepted; reversibility: cheap; mints NAW-1; supersedes nothing; the first audit-enforced gate on the discovery-gate axis adjacent to but NOT extending the PMI-1 / PVFS-1 / AVFS-1 / MCFS-1 / TVFS-1 forward-sync family); §Decision read mechanism + worked example post-/critique B1; §Consequences overbroad-pathspec + seam-driven self-application sub-bullets per /critique m2 + m3.
- [[skills/build-slice/SKILL.md]] — Step 6 checklist line after TVFS-1 + new `#### New-agent warning audit (NAW-1)` prose sub-section; forward-synced to installed `~/.claude/skills/build-slice/SKILL.md` per Mini-CAD-1 (slice-021 / slice-007 CAD-1 hybrid lineage).
- [[tools/new_agent_warning_audit.py]] — NEW; 369 LOC; binary exit contract; union-of-three-sources read mechanism per ADR-061 §Decision; two injection seams (`default_branch_resolver` + `added_files_resolver`); UTF8-STDOUT-1 first-statement convention.
- [[tools/install_audit.py]] — `_CANONICAL_TOOLS` alphabetic insert of `"tools.new_agent_warning_audit"`.
- [[tests/methodology/test_new_agent_warning_audit.py]] — NEW; 7 tests covering 4 audit states + WARN-line anchors + seam-driven self-application.
- [[tests/methodology/test_build_slice_skill.py]] — appended `test_build_slice_step_6_invokes_new_agent_warning_audit` (structural-anchor pin per slice-017 TPHD-1 sub-mode (c) lineage).
- [[tests/methodology/test_risk_register_audit_real_file.py]] — appended `test_r_18_retired_post_slice_063` (slice-041 R-4 retirement-precedent housing).
- [[tests/methodology/test_methodology_changelog.py]] — appended 2 NEW entry-pin tests under NEW SECTION header `# --- Slice-063 / NAW-1 entry pinning ---` (EPGD-1 design-time-pre-empted-success-mode discipline).
- [[tests/methodology/test_utf8_stdout_regression.py]] — `_ROOT_ONLY_TOOLS` append of `tools.new_agent_warning_audit`.
- This slice's [[mission-brief.md]] — AC#4 prose harmonization mid-design (slice-057 lesson L93 Builder self-catch); Pre-finish gate Mini-CAD-1 phrasing (m4) + Must-not-defer NAW-1-specific 5-part PMI-1 + new-RULE-ID posture; TF-1 plan flipped PENDING → WRITTEN-FAILING → PASSING per TF-1 v0.13.0 discipline; AC#2 + AC#5 `(extended)` suffix removed at /build-slice Phase D6 (slice-054 PTFCD-1 lesson recurrence corrected in-band).
- This slice's [[design.md]] — 4 surfaces harmonized at /critique fix block (B1 read-mechanism + M3 BRANCH-1 prose + m1 BC-PROJ-9 attribution + m5 anchor list) + 3 surfaces harmonized at /critique-review fix block (M-add-1 5-part leg enumeration + M-add-2 anchor list extension + M-add-3 Step 6 audit list).
- [[lessons-learned.md]] — chronological entry for slice 063 (appended below in Step 5).

## BCR-1 round-trip status

NO-OP CLEAN. Slice-063 is a risk-register-driven mitigation slice (R-18 retirement via NAW-1 mint), NOT a backlog-driven slice. Neither `mission-brief.md` nor this `reflection.md` carries a `**Closes:** SC-\d{3}` sentinel header. `diagnose-out/backlog.md` exists but is not touched by this slice. The BCR-1 trigger correctly no-ops per slice-053 ADR-055 mention-vs-application disambiguation. 23 SC candidates remain open in `diagnose-out/backlog.md` post slice-063.
