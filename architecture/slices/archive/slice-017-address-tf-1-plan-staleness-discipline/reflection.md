# Reflection: Slice 017 address-tf-1-plan-staleness-discipline

**Date**: 2026-05-13
**Shipped**: YES

## Validated

- TPHD-1 codification template matches slice-010 MCT-1 / slice-011 RSAD-1 / slice-013 EPGD-1 / slice-015 SCPD-1 / slice-016 RPCD-1 precedent exactly (methodology-changelog v0.32.0 entry + ADR-016 + 3 SKILL.md prose insertions + atomic version bump + shippability row + 9 prose-pin/entry-pin tests) — validated by 5/5 ACs PASS at /validate-slice + 17/17 shippability catalog rows PASS in 9.75s.
- 3-surface skill-prose discipline (vs 1-surface for MCT-1) covers 3 distinct Critic-stack fix-prose moments — /critique Step 4 end + /critique-review Step 3 end + /build-slice Prerequisite check bullet. Each insertion point empirically appropriate to its Critic-stack layer's fix-prose timing.
- PMI-1 v1.1 retirement-proof N=3 → **N=4 stable** — fourth atomic version bump 0.31.0 → 0.32.0 with zero modification on `test_plugin_yaml_version_matches_version_file_invariant` gate body. Per-version-bump test churn permanently retired through slice-017 inclusive.
- EPGD-1 self-application N=5 → **N=6 stable** — 0 of 12 prior entry-pin functions (v_0_22_0..v_0_31_0 spans 10 minor versions; v0.29.0 doubled + v0.31.0 doubled per slice-014/016 (a)↔(b) duality respectively per /critique-review m-add-1 ACCEPTED-FIXED count correction) touched through slice-017's Phase 1b NEW SECTION header insertion.
- TPHD-1 sub-mode (a)+(b)+(c) self-application empirically demonstrated — slice-017 IS canonical reference instance #1 of the discipline it codifies:
  - **Sub-mode (a)** N=1: /critique M2 ACCEPTED-FIXED renamed test function names from `_phase_0_step_*` → `_prerequisite_check_bullet_*` in TF-1 plan rows 8-9 → mission-brief harmonized in SAME fix block per the discipline
  - **Sub-mode (b)** N=1: /critique-review m-add-1 count correction (11 → 12 entry-pin functions) across 6 sites in same fix block (correctly N/A for TF-1 plan since no function names changed)
  - **Sub-mode (c)** N=1: /build-slice Phase 0 pre-flight verified 12 TF-1 rows match design.md function names BEFORE Phase 1b started
- BC-PROJ-2 silenced on this methodology-vocabulary slice via trigger-anchors final-filter (0/3 anchors `fence`/`code-block`/`llm` match; would-have-fired-on positive trigger keywords `agent`/`prompt`/`output`/`response`). Slice-012 BC-PROJ-2 negative-anchor migration discipline N=2 → **N=3 stable evidence** at slice-017.
- VAL-1 layered safety checks N=15 cumulative recurrence at Layer B `tests` namespace-package class — every slice 003-017 hits it; handled cleanly via `--imports-allowlist tests`. Slice-017 confirms v1 carry-over pattern stable.

## Corrected

- **ADR-016 Context paragraph initially misrepresented `tools/test_first_audit.py --strict-pre-finish` semantics** — claimed third clause "OR if any row's Test function name doesn't exist at the named Test path"; reality at `tools/test_first_audit.py:65 + 320 + 350-364` is status-only check (`row.status != "PASSING"`); no AST/grep introspection of test bodies. Caught at /critique B1 ACCEPTED-FIXED. Context paragraph rewritten with two-failure-mode distinction (status-staleness mode caught by TF-1 audit + function-name-staleness mode NOT caught). TPHD-1 value proposition reframed: prophylactic against an audit gap (function-name-staleness mode) + defense-in-depth for status-staleness mode.
- **Mission-brief AC #3 + design.md L124 magnitude claim contradicted ADR-016 L141-L154** — claimed ~3-5 sites; actual is ~13-16 sites per ADR-016 14-site Reversibility enumeration. Caught at /critique M1 ACCEPTED-FIXED. Both surfaces updated; CCC-1 v1.1 sub-clause 2 doc-vs-canonical-inventory parity restored.
- **TPHD-1 sub-mode (c) initial placement was NEW `### Step 0` between `## Your task` (L24) and `### Step 1` (L26)** — anomalous because `skills/build-slice/SKILL.md` step numbering is 1,2,3,4,5,6,7,7b,7c,8 with no Step 0 elsewhere; AND discipline IS structurally a prerequisite verification (the existing `## Prerequisite check` section L17-L22 was the natural home). Caught at /critique M2 ACCEPTED-FIXED. Placement moved to NEW bullet INTO existing `## Prerequisite check` section. TPHD-1 sub-mode (a) self-application empirically demonstrated by the fix block itself harmonizing TF-1 rows 8-9 function name renames in lockstep.
- **design.md Audit 6 (BC-1 self-application) listed wrong BC-PROJ-2 trigger keywords + negative anchors** — claimed 9-token set includes `methodology`, `Critic`, `slice`, `dim`, `Dimension`; reality at `architecture/build-checks.md:33-35` is `parse, fence, code-block, backtick, llm, agent, prompt, output, response` (trigger keywords) + `fence, code-block, llm` (trigger anchors) + `defer-with-rationale, aggregated lessons, false positive, meta-discussion, vocabulary, Critic-MISSED, back-sync, Dim 9, forward-sync` (negative anchors). Caught at /critique M3 ACCEPTED-FIXED. Audit 6 fully rewritten with empirical token enumeration + accurate substring counts + accurate fire-or-silence prediction.
- **design.md Audit 4 + mission-brief.md + 4 other sites claimed "11 prior entry-pin functions"** — actual empirical count via `grep "^def test_v_0_" tests/methodology/test_methodology_changelog.py` returns 12 functions at lines 76 / 111 / 152 / 202 / 250 / 304 / 370 / 437 / 499 (v0.29.0 second) / 747 / 844 / 910 (v0.31.0 second). The v0.31.0 doubling per slice-016 RPCD-1 (a)↔(b) duality (`_present_in_repo_and_installed` + `_names_three_sub_modes_in_repo_and_installed`) was missed in initial Audit 4 prose. Caught at /critique-review m-add-1 ACCEPTED-FIXED. Corrected across 6 sites (mission-brief L66 + design.md L32 + L92 + L138 + L206 + L208 + critique.md L87). EPGD-1 self-application guarantee 0/N preserved at corrected N=12.
- **mission-brief.md Must-not-defer #1 + design.md L245 enumerated anti-form rule-ID strings (`TPH-1` / `TPHD1` / `tphd-1` lowercase)** — would have re-introduced those strings into the slice's own artifacts per RSAD-1 sub-mode (b) re-introduction class (N=2 stable at slice-010 DEVIATION-3 + slice-011 codification body). Caught at /critique m1 ACCEPTED-FIXED. Replaced with positive-form assertion `grep -c "TPHD-1" <file>` returns ≥1 hit per surface (canonical-form presence check).
- **mission-brief.md mid-slice smoke gate referenced "Phase 1a-1f"** — design.md L138-L146 phase plan only enumerates Phases 1a-1e (no 1f). Caught at /critique m2 ACCEPTED-FIXED. Corrected to "Phase 1a-1e".
- **mission-brief.md AC #5 claimed "SCPD-1 proactive-application N=2 → N=3 stable"** — slice-017 has NO Dim 9 sub-clause supersession event, so SCPD-1 proactive-application is vacuously satisfied (no active propagation event occurs; row 17 is added as a NEW row with no prior-row touch needed). The counter increment was unfounded; SCPD-1 stays at N=2 stable. Caught at /critique m3 ACCEPTED-FIXED.
- **Test scoping flaw inherited from slice-016 RPCD-1 sibling**: `test_v_0_32_0_tphd_1_entry_names_three_sub_modes_in_repo_and_installed` initially copied global-substring pattern from slice-016 sibling at L910-L951; false-positive PASS on prior v0.31.0 markers ("Sub-mode (a)", "Sub-mode (b)", "Sub-mode (c)" already in v0.31.0 entry). Caught at /build-slice Phase 2b empirical pytest failure analysis — the test "passed" when it shouldn't have. Corrected inline: scoped to v0.32.0 body between `## v0.32.0` and `## v0.31.0` boundaries. Now correctly FAILS at WRITTEN-FAILING and PASSES at PASSING. **The slice-016 RPCD-1 sibling at L910-L951 STILL carries this flaw** — out-of-scope per slice-017 mission brief; flagged as discovered + deferred to next codification slice.
- **Test assertion style mismatch on existing SKILL.md Prerequisite-check bullet**: `test_build_slice_skill_md_tphd_1_prerequisite_check_bullet_present` initially asserted `"Per **TPHD-1**" in BUILD` (paragraph form used at /critique + /critique-review insertions); actual bullet uses imperative `**Run TPHD-1 pre-flight harmonization**` form matching existing Prerequisite-check bullet style. Caught at /build-slice Phase 2c empirical pytest failure. Corrected inline per **TPHD-1 sub-mode (a) self-application empirically demonstrated at /build-slice itself**: test assertion harmonized in same fix block to `"Run TPHD-1 pre-flight harmonization" in BUILD`. Same correction applied to `_location_pinned` sibling.

## Discovered

- **NEW first-Critic-MISS class at N=1: "test-scoping-flaw-inherited-across-codification-slice-siblings"** — slice-016 RPCD-1 sibling test at L910-L951 STILL carries the global-substring scoping flaw I caught at slice-017 DEVIATION-1. It currently passes only because no later `_names_three_sub_modes` test exists in the file. **Latent regression risk at slice-018+**: if slice-018+ codification adds a `_names_N_sub_modes` test for v0.33.0+, slice-016's sibling would false-positive PASS on the v0.33.0 markers. Both first-Critic AND meta-Critic missed this at slice-017 /critique + /critique-review time. Caught at /build-slice Phase 2b as third-Critic-stack-layer defense (empirical pytest behavior surfaced the inheritance). Promote to /critic-calibrate aggregation if recurs at slice-018+; N=2 distinct-slice promotion threshold.

- **NEW first-Critic-MISS class at N=1: "prose-pin-test-style-must-match-section-style"** — both first-Critic and meta-Critic missed that `_prerequisite_check_bullet_present` should assert imperative bullet form ("Run TPHD-1 pre-flight harmonization") NOT paragraph form ("Per **TPHD-1**"). Test design was extrapolated from /critique + /critique-review precedent which DOES use "Per **TPHD-1**" paragraph form (those are paragraph insertions, not bullets). The Prerequisite-check section is a bulleted imperative list ("Find active slice folder", "Read mission-brief.md...", etc.), so the new bullet must match imperative style. Surfaced at /build-slice Phase 2c. Generic methodology lesson at N=1; promote to /critic-calibrate aggregation if recurs at slice-018+.

- **DR-1 Wiegers regression-guard coverage-symmetry watch-list ratchets to N=2 cumulative**: slice-016 M-add-1 (missing `_location_pinned` sibling test breaking N=3 stable `_sub_clause_present` + `_location_pinned` duality) + **slice-017 m-add-1 (entry-pin count drift 11 → 12 across 6 sites)** — SAME class (design-doc-level mechanical-table-vs-canonical-inventory asymmetry per CCC-1 v1.1 sub-clause 2 surface). **One more recurrence at slice-018+ hits N=3 promotion threshold** for Dim 9 sub-clause refinement. Strongest /critic-calibrate trigger candidate at slice-018+ if it recurs.

- **ZERO-classical-build-deviation streak broken at N=5**: prior 5 slices (008+012+013+015+016) shipped with zero classical build-time DEVIATIONs; slice-017 shipped with 3 build-time DEVIATIONs (DEVIATION-1 test-scoping flaw inheritance + DEVIATION-2 prose-pin test style mismatch + DEVIATION-3 mechanical row-order). All 3 caught + fixed inline (zero design.md updates required post-build). DEVIATION-1 + DEVIATION-2 are GENERIC METHODOLOGY LESSONS, not slice-specific bugs — emerged as patterns at codification-slice density. DEVIATION-3 is mechanical (well-trodden). **Pattern observation**: codification slices that introduce a NEW test family or extend an EXISTING SKILL.md section are at higher risk of methodology-lesson-class build-time DEVIATIONs than prior slices (008+012+013+015+016 each modified narrower test surfaces or extended canonical content within existing patterns). Slice-017 extended TWO new test surfaces (NEW `test_critique_skill.py` + NEW `test_critique_review_skill.py`) AND inserted prose INTO an existing SKILL.md section style (Prerequisite-check bullet vs paragraph) — surface area for style-mismatch was structurally higher.

- **TPHD-1 self-application density at codification slices empirically high**: slice-017 demonstrated ALL THREE sub-modes (a/b/c) on its own draft simultaneously. Confirms slice-016 lesson "codification slices that codify a discipline almost always commit instances of that discipline on their own draft. Strong prior." Slice-017 N=8 self-defects (7 first-Critic + 1 meta-Critic) matches slice-013 N=7 high-water mark + meta-Critic add. Recursive-self-application N=8 → **N=9 cumulative stable** post-RSAD-1 codification at slice-017.

- **/critique-review m-add-1 catch-class confirmed at the design-doc-level mechanical-table-vs-canonical-inventory layer**: prior 3 DR-1 catches (slice-013/014/015 M-add-1) were RPCD-1 sub-mode (a/b/c) instances; slice-016 M-add-1 was Wiegers coverage symmetry; slice-017 m-add-1 is the SAME Wiegers coverage-symmetry class. DR-1 catch-class diversification N=4 → **N=5 stable**. The pattern is that meta-Critic catches what first-Critic misses on mechanical-inventory counts/symmetry — first-Critic empirically focuses on semantic/discipline-class catches.

## Deferred

- **slice-016 RPCD-1 sibling test scoping-fix at L910-L951** — out-of-scope per slice-017 mission brief; latent regression risk only manifests if slice-018+ codification adds a `_names_three_sub_modes` test for v0.33.0+. Defer to next codification slice that touches the `_names_N_sub_modes` family OR a dedicated cleanup slice if user prioritizes. Reason: slice-017 mission brief explicitly scoped to TPHD-1 codification; cleanup of slice-016 sibling is methodology-hygiene work that should batch with related discoveries.
- **`tools/tphd_1_audit.py` standalone audit tooling** — v2 candidate per ADR-016 Cost summary; deferred until N≥3 TPHD-1 violations recur post-codification at slice-018+. Reason: prose-heuristic discipline sufficient at codification time; audit-tooling risks false-positive signal-to-noise erosion (mirrors slice-016 ADR-015 `tools/rpcd_1_audit.py` deferral).
- **`agents/critique.md` Dim 9 10th sub-clause for TPHD-1** — TPHD-1 is a timing/sequencing discipline, not adversarial-prompt content. Separate v2 candidate if N≥3 first-Critic-MISS instances of "TF-1 plan staleness at /critique time" recur post-TPHD-1 codification. Reason: misclassifies the discipline (slice-017 /critique-review Option 3 evaluation REJECTED).
- **mini-CAD-1 byte-equality on `skills/critique/SKILL.md` + `skills/critique-review/SKILL.md` + `skills/build-slice/SKILL.md`** — defer to v2 if drift surfaces (mirrors slice-010 `test_slice_skill_drift.py` precedent — INST-2 generalization deferred).
- **Open R-1 (cwd-mismatch /diagnose) + R-2 (no programmatic /diagnose warning test)** — stale risks since slice-001/002; untouched for 15 slices. Out-of-scope per slice-017 mission brief; require `/repro` first if pursued. Reason: stale; not in active scope.
- **3-layer-Critic-stack-accountability Dim 9 sub-class refinement** — slice-014 N=1 + slice-016 N=2 watch-list; slice-017 doesn't add to the count since it's the slice that RESOLVED the pattern via TPHD-1 codification. Promote at N=3 if a 3-layer-Critic-stack-accountability NEW instance recurs at slice-018+. Reason: TPHD-1 should reduce future 3-layer-accountability instances; track over slices 018-020 before concluding the pattern is still ripe.
- **`audit-tools-default-utf8-stdout` (Windows cp1252 console encoding)** — N=2 cumulative watch-list (slice-007 + slice-016); slice-017 didn't hit this class. Deferred; promote to /critic-calibrate aggregation if N=3 recurs at slice-018+.
- **NEW watch-list candidates from slice-017**: test-scoping-flaw-inheritance (N=1) + prose-pin-test-style-must-match-section-style (N=1) — both join the /critic-calibrate aggregation pool for next-run trigger evaluation at slice-021+.

## Critic calibration

### First-Critic findings (from `critique.md` `## Findings`)

- **B1 (ADR-016 Context misrepresents TF-1 audit semantics)**: **VALIDATED** — disposition ACCEPTED-FIXED at /critique fix-prose; reality at /validate-slice confirmed `tools/test_first_audit.py:350-364` checks status only (no function-existence check). Without fix, TPHD-1's value proposition would have rested on a false premise about what /build-slice Phase 6 already catches. Strongest first-Critic catch on slice-017 — load-bearing factual correction reframing the entire lesson basis.
- **M1 (magnitude inconsistency ~3-5 vs ~13-16 sites)**: **VALIDATED** — disposition ACCEPTED-FIXED; reality at /build-slice confirmed enumeration is 13-16 surfaces (build-log "Files changed" list = 18 distinct surfaces; same magnitude class). CCC-1 v1.1 sub-clause 2 catch.
- **M2 (`### Step 0` vs `## Prerequisite check` placement)**: **VALIDATED** — disposition ACCEPTED-FIXED; reality at /build-slice empirically confirmed bullet form fits naturally into existing Prerequisite-check section (4 sibling bullets in imperative style; slice-017 added a 5th matching style). Architectural-coherence catch.
- **M3 (Audit 6 wrong BC-PROJ-2 tokens)**: **VALIDATED** — disposition ACCEPTED-FIXED; reality at /build-slice + /validate-slice confirmed BC-PROJ-2 silenced via trigger-anchors final-filter (0 fires at /validate-slice Phase 4). Slice-012 BC-PROJ-2 N=2 → N=3 stable evidence.
- **m1 (RSAD-1 sub-mode (b) re-introduction)**: **VALIDATED** — disposition ACCEPTED-FIXED; reality at /validate-slice confirmed positive-form assertion grep returns 179 TPHD-1 hits across 12 surfaces.
- **m2 (Phase 1a-1f vs design.md 1a-1e mismatch)**: **VALIDATED** — disposition ACCEPTED-FIXED; cosmetic textual drift.
- **m3 (SCPD-1 N=2 → N=3 counter increment unfounded)**: **VALIDATED** — disposition ACCEPTED-FIXED; reality at /build-slice confirmed slice-017 has no Dim 9 sub-clause supersession; SCPD-1 stays N=2 stable.

### Meta-Critic findings (from `critique-review.md` Missed)

- **m-add-1 (entry-pin count drift 11 → 12)**: **VALIDATED** — disposition ACCEPTED-FIXED at /critique-review fix-prose; reality at /build-slice confirmed empirical grep returns 12 functions (8 singles + v0.29.0 doubled + v0.31.0 doubled). EPGD-1 self-application guarantee 0/N preserved at corrected N=12. DR-1 catch-class diversification N=4 → **N=5 stable** with Wiegers regression-guard coverage-symmetry confirmed at N=2 cumulative.

### Missed by Critic

- **TF-1 plan / TPHD-1 sub-mode (a) self-application at the test-pin-style mismatch on existing SKILL.md sections**: both first-Critic and meta-Critic missed that the test assertion for `_prerequisite_check_bullet_present` would mismatch the imperative bullet style of the Prerequisite-check section (test was written based on /critique + /critique-review paragraph-style precedent). First-Critic + meta-Critic empirically validated PROSE insertion would use bullet form (per M2 ACCEPTED-FIXED) but missed that the corresponding TEST assertion would need to harmonize. Caught at /build-slice Phase 2c empirical pytest failure as third-Critic-stack-layer defense. **NEW first-Critic-MISS class at N=1**: "prose-pin-test-style-must-match-section-style" — promote to /critic-calibrate aggregation if recurs at slice-018+.
- **Test scoping flaw inherited across codification slice siblings**: both first-Critic and meta-Critic missed that `_names_three_sub_modes` test would inherit the global-substring scoping flaw from slice-016 RPCD-1 sibling. Surfaced at /build-slice Phase 2b empirical pytest behavior analysis. **NEW first-Critic-MISS class at N=1**: "test-scoping-flaw-inherited-across-codification-slice-siblings" — promote to /critic-calibrate aggregation if recurs at slice-018+.

### Pattern observation

- **First-Critic disposition accuracy streak 87/87 → 94/94 across slices 6-17** — 12th consecutive slice with 100% first-Critic accuracy at /critique-review (no SUSPICIOUS, no SEVERITY-WRONG, no OVERRIDE-MISJUDGED). Strongest streak in project history continues.
- **Cross-Critic-stack 96/96 across slices 6-17** (was 88/88 at slice-016; +7 first-Critic + 1 meta-Critic at slice-017, all VALIDATED at /build-slice + /validate-slice).
- **DR-1 catch-class diversification N=4 → N=5 stable** with class confirmation at Wiegers regression-guard coverage-symmetry layer (slice-016 N=1 + slice-017 N=2 cumulative at design-doc-level mechanical-table-vs-canonical-inventory asymmetry per CCC-1 v1.1 sub-clause 2 surface). DR-1's value extends consistently beyond just-RPCD-1 sub-mode catches.
- **NEW first-Critic-MISS class shift at slice-017** — prior N=3 DR-1 catches at slices 013/014/015 were RPCD-1 sub-mode (a/b/c) classes; slice-016 + slice-017 Wiegers coverage-symmetry at N=2 cumulative is now the dominant DR-1 catch-class; slice-017 ALSO introduces TWO new first-Critic-MISS classes (test-scoping-flaw inheritance + prose-pin test-style mismatch) — both surface at /build-slice Phase 2 (test-first WRITTEN-FAILING / PASSING transitions are now the third Critic-stack-layer detection mechanism for these classes, alongside TF-1 audit + pytest catalog).
- **Methodology-lesson-class build-time DEVIATIONs surfacing at codification-slice density**: 2 new generic methodology lessons from slice-017 (test-scoping flaw inheritance + prose-pin style mismatch) join the watch-list. If slice-018 hits either of these or generates 1-2 more, /critic-calibrate at slice-018+ would have N=3-4 unique watch-list classes ready for aggregation.

## Lessons for next slice

- **Codification slices that add a NEW test file copy from a sibling — but the sibling may carry latent flaws.** Slice-017 inherited slice-016 RPCD-1's `_names_three_sub_modes` global-substring scoping flaw verbatim by copy. Generic lesson: when copying a test pattern from a prior codification slice, verify the source pattern's scoping discipline before propagating. Add to /build-slice Phase 2a pre-flight: "if copying test from prior codification slice's sibling, audit the source pattern for scoping discipline first." Promote to /critic-calibrate aggregation at N=2 if recurs at slice-018+.

- **Prose-pin tests must match the prose style of the existing SKILL.md section they assert into.** Bullets get imperative `Run RULE-ID action` form; paragraphs get `Per **RULE-ID**` form. Slice-017 missed this at /critique + /critique-review and surfaced at /build-slice Phase 2c. Generic methodology lesson at N=1; add to /design-slice + /critique self-application audits at slice-018+ as additional sub-class probe for codification slices that extend existing SKILL.md sections.

- **TPHD-1 sub-modes (a)+(b)+(c) self-application empirically high-density at codification slices** — slice-017 demonstrated all 3 sub-modes on its own draft simultaneously, including catching itself via the discipline it codifies (sub-mode (a) at /critique M2 + sub-mode (b) at /critique-review m-add-1 + sub-mode (c) at /build-slice Phase 0). Pattern confirms: future codification slices should explicitly enumerate the codified discipline's sub-modes as design-time audits + expect Critic-stack catches on own draft.

- **DR-1 catch-class diversification N=5 stable** — first-Critic empirically focuses on semantic/discipline-class catches; meta-Critic empirically catches mechanical-inventory counts/symmetry. Future codification slices should NOT expect first-Critic to catch design-doc-level mechanical-table-vs-canonical-inventory asymmetry — that's meta-Critic's empirical wheelhouse.

- **ZERO-classical-build-deviation streak broken at N=5 was structurally predicted by codification-slice scope expansion** — slice-017 extended TWO new test surfaces + inserted prose INTO an existing SKILL.md section style. Future codification slices with similar scope expansion should expect 1-3 build-time DEVIATIONs as methodology-lesson-class catches (not slice-specific bugs). The discipline is to catch + fix inline + flag for /reflect propagation, not to chase a zero-DEVIATION streak.

- **Empirical-verification-at-design-time discipline N=16 stable post-slice-017** — 6 design-time audits at slice-017 (RPCD-1 sub-modes a/b/c + EPGD-1 + Wiegers + BC-1) all VALIDATED at /build-slice + /validate-slice. The audit-table discipline introduced at slice-009 continues to pay off; future codification slices should preserve this pattern.

- **Latent regression risk from inherited test patterns**: the slice-016 RPCD-1 sibling test at L910-L951 STILL has the global-substring scoping flaw; will be triggered when slice-018+ adds a `_names_N_sub_modes` test for v0.33.0+. If slice-018+ is a codification slice, bundle the slice-016 sibling cleanup in same fix-prose block via TPHD-1 sub-mode (a) self-application.

## Vault updates made

- [[methodology-changelog.md]] — v0.32.0 TPHD-1 entry added; forward-synced to installed copy (byte-equal sha256 `06ce0c442874f0aa22a4f9e7b9b2fd0b45c45c7ea5617db65bfff1a98a2089ff`)
- [[decisions/ADR-016]] — NEW; reversibility cheap with magnitude justification ~13-16 sites; supersedes=null; extends ADR-009 (MCT-1)
- [[architecture/shippability.md]] — row 17 added; rows 1-16 NOT touched (SCPD-1 vacuous; no Dim 9 sub-clause supersession this slice)
- `VERSION` + `plugin.yaml` + `~/.claude/ai-sdlc-VERSION` atomic 0.31.0 → 0.32.0
- `skills/critique/SKILL.md` — TPHD-1 prose paragraph appended at end of Step 4 (between L122 and L124)
- `skills/critique-review/SKILL.md` — TPHD-1 prose paragraph appended at end of Step 3 (between L83 and L85)
- `skills/build-slice/SKILL.md` — TPHD-1 NEW bullet INTO existing `## Prerequisite check` section (between L22 and L24); placement per /critique M2 ACCEPTED-FIXED (NOT new `### Step 0`)
- `tests/methodology/test_critique_skill.py` — NEW file with 2 prose-pin tests (substring + scoped-find location-pin)
- `tests/methodology/test_critique_review_skill.py` — NEW file with 2 prose-pin tests
- `tests/methodology/test_build_slice_skill.py` — +2 prose-pin tests appended for `## Prerequisite check` section bullet (imperative-form assertion per slice-017 sub-mode (a) self-application at DEVIATION-2 fix)
- `tests/methodology/test_methodology_changelog.py` — NEW SECTION header `# --- Slice-017 / TPHD-1 entry pinning ---` + 3 entry-pin tests (`_present_in_repo_and_installed` + `_names_three_sub_modes_in_repo_and_installed` scoped-to-v0.32.0-body + `_names_slice_016_cross_slice_anchor`) + 1 ADR-pin test (`_adr_016_exists_and_names_tphd_1_canonical_phrase`)
- This slice's [[design.md]] — corrected at /critique fix-prose (B1 + M1 + M2 + M3 + m1 + m2 + m3) + /critique-review fix-prose (m-add-1 count corrections across 4 sites) + /build-slice (no further updates needed — DEVIATIONs were test-implementation refinements not design-content changes)
- This slice's [[mission-brief.md]] — corrected at /critique fix-prose (AC #2 + AC #3 + AC #5 + Must-not-defer #1 + mid-slice smoke gate + must-not-defer TPHD-1 self-application probe + TF-1 plan rows 8-9 function name rename per sub-mode (a)) + /critique-review fix-prose (Must-not-defer #6 EPGD-1 count correction) + /build-slice (TF-1 plan status flips PENDING → WRITTEN-FAILING → PASSING)
- This slice's [[critique.md]] — added meta-Critic m-add-1 triage row + scoping correction note at Dim 9 line
- This slice's [[critique-review.md]] — NEW; EXTEND verdict; 1 missed finding m-add-1 + 7 first-Critic confirmations
- No risk-register changes (no new risks surfaced; existing R-1 + R-2 remain stale-since-slice-001/002)

## Build-checks promotion candidates evaluated

Per BC-1 Step 5b: did this slice surface a recurring pattern that should become a build-check?

- **Test-scoping-flaw inheritance across codification-slice siblings** — N=1 at slice-017; below typical N=3 promotion threshold; **NOT YET promote**; revisit at slice-019+ if recurs at slice-018+.
- **Prose-pin test style mismatch on existing SKILL.md sections** — N=1 at slice-017; below typical N=3 promotion threshold; **NOT YET promote**; revisit at slice-019+ if recurs at slice-018+.
- **Wiegers regression-guard coverage-symmetry on design-doc-level mechanical-table-vs-canonical-inventory** — N=2 cumulative (slice-016 + slice-017); below typical N=3 promotion threshold; **NOT YET promote**; ratchets toward N=3 at slice-018+ — strongest candidate for /critic-calibrate aggregation at slice-018-021.

No build-checks promoted this run. All 3 candidates join the watch-list for /critic-calibrate aggregation at slice-021+ default OR earlier if any hits N=3 distinct-slice recurrence.
