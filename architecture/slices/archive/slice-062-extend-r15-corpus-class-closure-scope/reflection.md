# Reflection: Slice 062 extend-r15-corpus-class-closure-scope

**Date**: 2026-05-23
**Shipped**: YES-WITH-DEFERRALS (/code-review AGENT-UNSPAWNABLE per R-18 recurrence; user-ratified deferral per slice-061 precedent; all other gates ALL GREEN; 4/4 ACs PASS)

## Validated

- **Helper extraction is per-line equivalent refactor** — empirically confirmed at mid-slice smoke gate (existing `test_no_new_archive_fragile_literals_in_methodology_corpus` PASSES with extracted helper; same regex behavior; same self-skip semantics; same whitelist subset assertion); NO behavior change at the methodology-corpus call site as predicted by ADR-060 §"Decision" paragraph 3.
- **Walk-proof discipline falls out structurally via pytest collection** — the mid-slice smoke gate produced the verbatim predicted FAIL signature: `test_no_new_archive_fragile_literals_in_tests_skills_corpus` failing with `('tests/skills/code_review/test_code_review_skill.py', 17)` exactly as design.md L132-138 predicted. Each per-corpus test's existence + pass IS the walk-proof its corpus is walked; the absence of any from the pytest report would be the visible alarm.
- **Lazy `_resolve_slice_dir(60)` resolves cleanly to archive** — slice-060 IS archived; `_resolve_slice_dir(60)` returns `archive/slice-060-add-code-review-skill/code-review.md` empirically (verified at /design-slice + /build-slice Phase B + Phase D shippability runner).
- **Cross-package import `from tests.methodology.conftest import _resolve_slice_dir` works from `tests/skills/code_review/`** — empirically verified at /design-slice cross-package import smoke test + /build-slice Phase B test-suite run + Phase D shippability runner row #62 self-application (7/7 PASS). The pytest.ini:6 `pythonpath = .` + explicit `__init__.py` in `tests/methodology/` + `tests/skills/code_review/` make `tests.methodology.conftest` resolvable from any test file.
- **5-part PMI-1 bump 0.64.0 → 0.65.0 cleanly composed** — VERSION + plugin.yaml + pyproject.toml [project].version (PVFS-1) + `## v0.65.0` header + installed `~/.claude/ai-sdlc-VERSION` (AVFS-1) + installed `~/.claude/methodology-changelog.md` (MCFS-1) + installed venv `ai-sdlc-tools` 0.64.0 → 0.65.0 (TVFS-1 via pip install --upgrade); PMI-1 / PVFS-1 / AVFS-1 / MCFS-1 / TVFS-1 all exit 0 post-bump. The B1 ACCEPTED-FIXED 5-part discipline propagation landed correctly at all 7 sites.
- **`_R15_CORPUS_WHITELIST` stays empty post-slice** — the slice-061-surfaced offender at `tests/skills/code_review/test_code_review_skill.py:17` is REPOINTED via `_resolve_slice_dir(60)`, not whitelisted; aggregated `test_r15_corpus_whitelist_has_no_orphan_entries` PASSES vacuously.
- **R-15 STAYS `retired`; STP-1 Sub-form B clean** — `risk_register_audit --filter-status retired` confirms R-15 retired (no flip); STP-1 audit clean (zero `test_r_15_*_stays_retired` functions exist to contradict the unchanged status).
- **BC-1 5 rules surface, all vacuously satisfied** — empirically verified: no `git checkout --` / `git restore` / `git stash` in any of 7 in-tree changed files (BC-PROJ-3 / BC-GLOBAL-2 vacuously satisfied); no identifier rename (function names PRESERVED per ADR-060 m1 directive — BC-PROJ-5 vacuously satisfied); no INSTALL.md/README.md edits (BC-PROJ-11 vacuously satisfied); BC-PROJ-4 real-corpus discipline satisfied at Phase A mid-slice smoke + Phase B PASSING + Phase D strict-pre-finish.
- **Shippability runner 62/62 PASS** — no past slice's critical-path test was silently broken by this slice; slice-062's own new row #62 PASSES alongside the 61 pre-existing rows.

## Corrected

- **mission-brief.md frontmatter L5 "Risk retired" claim** — originally read "R-15 part-(b) — closes the slice-061 N=1 scope-gap watch-list" but R-15 was already retired at slice-057; Builder misread the slice-061 reflection wording "R-15's existing retired-but-class-still-open status" as implying R-15 was still mitigating. Corrected mid-design (post-/slice, pre-/critique) to "none (R-15 is already retired per slice-057; this slice extends the backstop *mechanism* that retired R-15 to wider corpora)". Caught early (same /slice + /design-slice sequence; no downstream artifacts had referenced the original wording).
- **mission-brief.md AC#4 wording** — originally said "status update reflects the part-(b) disposition — open→retired if the post-extension whitelist is empty, otherwise open→mitigating with the residual whitelisted sites listed" — wrong because R-15 was already retired. Corrected to "R-15 `**Status**:` stays `retired` (no flip)". Same fix block as the L5 correction.
- **mission-brief.md verification-plan row 4** — paired correction to AC#4; reframed from "JSON output reflects the R-15 status update" to "still includes R-15 (status unchanged); the file's R-15 block contains the new scope-extension paragraph; STP-1 Sub-form B audit stays clean".
- **/critique fix-block m3 ACCEPTED-FIXED edit introduced /critique-review M-add-1** — my own /critique fix block (replacing the mission-brief 4-row TF-1 plan with the 7-row design.md table verbatim) used multi-AC labels `"1, 3"` + `"(audit-trace)"` markers that `tools/test_first_audit.py:_normalize_ac_label` rejects → 2 `ac-without-row` violations on AC#3 + AC#4. Meta-Critic caught it by empirically running the audit. Required a second fix-block at /critique-review to re-harmonize to bare-numeric AC labels + multi-row-per-AC shape per slice-056 precedent. **N=2 slice-040 N+1 doctrine recurrence on TF-1-multi-AC-label sub-class** (slice-056 /critique-review M-add-1 + slice-062 /critique-review M-add-1). **N=2 slice-022 RSAD-1 self-violation pattern recurrence extending to dual-Critic stack same-fix-block edits**.
- **ADR-060 §"Decision" paragraph 1 fabricated lessons-learned.md citation** — originally quoted "regex pattern needs a self-test that the walker actually visits the corpus" attributed to `architecture/lessons-learned.md`; grep-verified by meta-Critic m1 to not exist anywhere in the repo. Replaced with paraphrased reference to slice-056 M-add-2 + slice-057 Discovered section (with explicit "paraphrased here, not quoted" disclaimer per Cockburn evidence-traceability discipline).
- **design.md / mission-brief.md cross-document option-enumeration incoherence** — 3 schemes existed: ADR-060 numerals 1-4, design.md "option B/A" mentions, mission-brief (a)/(b)/(c). Chosen ADR option 3 literally absent from mission-brief (a)/(b)/(c) enumeration. Meta-Critic M-add-2 caught it. Updated all 4 sites to use ADR-060 numeric 1-4 as the authoritative scheme.
- **design.md Phase C step 11 "4-part Inclusion-heuristic structure" stale-carry** — novel terminology not appearing elsewhere in the repo; confusable with the 4-part PMI-1 bump that B1 just corrected to 5-part. Meta-Critic M-add-3 caught it. Rewrote step 11 to describe the v0.59.0/v0.64.0 entry-body template explicitly + made entry-body vs bump-shape distinction explicit ("The bump shape is 5-part PMI-1 per step 12 below — explicitly NOT 4-part").

## Discovered

- **N=2 slice-022 RSAD-1 self-violation pattern recurrence extending to dual-Critic stack same-fix-block edits** (BUILD-time-reachable, gate-caught at /critique-review meta-Critic): the /critique m3 ACCEPTED-FIXED fix-block edit ITSELF introduced a new defect class (M-add-1) that the same TF-1 audit discipline was supposed to catch. This extends the slice-022 RSAD-1 lineage from the originally-codified rule-application-on-its-own-codifying-slice surface to dual-Critic stack same-fix-block edits. Surfaced as a class signal that the first-Critic's table-shape-only verification is structurally too weak to catch audit-rejection-of-its-own-output cases; empirical audit re-execution (meta-Critic's discipline) is the right structural backstop. Mitigation: per slice-037 law, build-time-reachable classes don't get a Critic-prompt dimension — the gate IS the backstop. But this is a viable /critic-calibrate-route candidate (sharpening the first-Critic's prompt with "if the fix edits a TF-1/structural-audit-parseable table, run the audit empirically before declaring ACCEPTED-FIXED"). N=2 cumulative; if N=3 emerges, promote.
- **First-Critic vs meta-Critic emerging specialization signal** — slice-062 first Critic's 5/6 findings on cross-document coherence + naming + citation hygiene; only 1/6 on substantive technical correctness (B1 PVFS-1). Meta-Critic added 3 missed findings, 1 of which (M-add-1) was a Blocker that the first Critic structurally couldn't catch (would have required empirical audit re-run on its own fix-block output — a discipline the first Critic doesn't currently apply). Suggests an emerging division of labor: first Critic on prose drift + cross-doc citation; meta-Critic on empirical audit re-execution + same-fix-block-edit-introduced regressions. N=1 specialization signal at this granularity; if N=2 confirms (slice-063 + repeat first-Critic-prose-vs-meta-Critic-empirical-audit split), consider documenting as a Critic-stack design observation.
- **R-18 N=2 cumulative recurrence on slice-060 N+1 governed-slice axis** (predicted by slice-061 reflection L82, empirically confirmed this slice). The `/code-review` agent registry-session-cache miss WILL recur on every slice immediately following an agent-shipping slice unless the user restarts Claude Code. slice-062 hit it as N=2; slice-061 reflection's mitigation candidates (a) `/build-slice` post-build new-agent warning + (b) `/code-review` error-semantic widening to auto-skip + (c) Claude Code platform fix are now ACTIVELY nominated for slice-063+, not just watch-list. Per slice-037 law, this is build-time-reachable (caught by the `/code-review` SKILL's `agent-unspawnable` error class) — but the recurrence cost is real (every N+1 session governed by the chain extension hits HALT for user-input gate). Slice-063 highest-priority candidate.
- **/code-review walking-skeleton advisory-only v1 working as intended** — when AGENT-UNSPAWNABLE fired this slice, the slice still SHIPPED. v1 is advisory only; no verdict-driven block on /validate-slice. Walking-skeleton discipline (Cockburn) working as designed: the in-loop code-review surface exists in the chain but doesn't block when infrastructure fails. slice-062 mission-brief explicitly declined to bundle the TRI-1 + verdict-block work (slice-060 reflection L37 explicit slice-062 nomination acknowledged as out-of-scope per M1 fix). slice-063 inherits the v2 nomination.
- **MEPD-1 Inclusion-heuristic firing on scope-extension precedent: N=6 cumulative** — slice-049 OSDG-1 mint + slice-050 AVFS-1 + slice-051 OSDG-1 member-add + slice-057 OSDG-1 + slice-058 AVFS-1 + slice-059 TVFS-1 = N=5; slice-062 ADR-060 R-15-backstop-scope-extension makes it **N=6**. Cumulative confirmation that drift-guard family scope-extension (or analogous extends-not-mints decisions) warrants a methodology-changelog entry + 4/5-part PMI-1 bump + shippability row + ADR, even when no new RULE-ID is minted. The slice-056 voluntary-restraint-zero-bump-zero-ADR precedent (and slice-057 retirement-discharge precedent) is RESERVED for retirement-discharge / part-(b) closure shapes, NOT for scope-extension shapes — the discipline distinguishes by what the slice substantively does, not by slice severity.

## Deferred

- **/code-review v2 enhancements (slice-063+ highest priority)**: AI-bloat passes (originally nominated for slice-061 per slice-060 reflection L36, slipped past slice-061, deferred from slice-062 per mission-brief Out-of-scope bullet 1) + TRI-1 triage gate + verdict-driven block on /validate-slice (originally nominated for slice-062 by slice-060 reflection L37 — explicit by-ID nomination — declined here with acknowledged-divergence note per M1 fix-block). slice-063 highest-priority candidate per /code-review walking-skeleton v1's advisory-only disposition.
- **R-17 BRANCH-1 clean-tree precondition fix** (slice-062-or-later nomination from slice-061): extend `tools/branch_workflow_audit.py` with pre-create cleanliness invariant OR offer `git worktree add` path in /build-slice Prerequisite check. Not addressed this slice; remains slice-063+ candidate on the pipeline-hygiene track.
- **R-18 mitigation (slice-063 ACTIVE nomination)** — strengthened from watch-list to active per N=2 cumulative recurrence. Likely (a) `/build-slice` post-build new-agent warning is the cheapest + most generalizable per slice-061 reflection L26. Could also be (b) `/code-review` error-semantic widening to auto-skip + structured-options ASK (which is what /code-review SKILL Step 2's `agent-unspawnable` error class already does — extending to auto-skip + structured ASK is incremental).
- **Multi-session / parallel-slice / worktree-per-slice execution (user-raised at /slice invocation 2026-05-23)** — user's original /slice request; flagged at /slice Step 3 as requiring a design-first sequence of slices (option #2 from /slice Step 3 menu: "slice-062a design parallel-slice exec model"; pure ADR + R-19 risk-register entry + design.md sketching artifact-ownership model; NO code). Remains backlog / future-slice-sequence candidate. The user's intent is real (workflow ask) but the implementation is multi-day, touches BRANCH-1, PCA-1, OSDG-1, multiple audits, and requires a coordinated artifact-ownership model (shared vault files: `_index.md`, `lessons-learned.md`, `methodology-changelog.md`, `critic-calibration-log.md`, `shippability.md`, `risk-register.md`). Worth picking up as slice-064+ on the workflow track (after slice-063 R-18 mitigation lands), OR earlier if the user prioritizes.
- **BC-PROJ-11 sibling-coverage Critic-prompt dimension extension** (slice-061 N=1 watch-list) — slice-062 surfaced no new instances; remains N=1 watch-list. /critic-calibrate route at N=2.
- **`/critic-calibrate` candidate: "empirical-audit-execution as /critique surface check"** — N=2 slice-040 N+1 doctrine recurrence on TF-1-multi-AC-label sub-class (slice-056 /critique-review M-add-1 + slice-062 /critique-review M-add-1). Sharpen Critic-prompt with: "if the fix edits a TF-1 plan table (or any structural-audit-parseable table), run `tools/test_first_audit.py --strict-pre-finish` (or relevant audit) EMPIRICALLY before declaring ACCEPTED-FIXED." Active /critic-calibrate nomination; if N=3 emerges (slice-063+ same class), promote.

## Critic calibration

Per TRI-1: scoring each finding from `critique.md` `## Triage` table + `critique-review.md` missed findings + reality observed during build/validate.

**First Critic** (6 findings — all from `critique.md`):

- **B1 (PVFS-1 / 5-part PMI-1 bump enumeration omitted pyproject.toml leg)**: **VALIDATED** — disposition ACCEPTED-FIXED. Direct re-introduction of slice-060 B2 defect class would have FAILed PVFS-1's `test_repro_sc001_pyproject_project_version_matches_version_file` at /build-slice Step 6 with `'0.64.0' == '0.65.0'`. Strongest finding. Severity Blocker correctly calibrated. The 7-site propagation fix (design.md What's-new + Components-touched + Phase C step 12 + Phase D step 17 + CSP-1 row; ADR-060 Consequences; mission-brief Pre-finish gate) all landed cleanly and were empirically re-verified at Phase D.
- **M1 (slice-060 reflection L37 TRI-1 nomination silently re-deferred)**: **VALIDATED** — disposition ACCEPTED-FIXED. Wiegers requirements-design traceability discipline; the slice-060 reflection L37 explicit by-ID nomination of slice-062 was acknowledged with 3-clause justification (build-time gate priority; slice-061 ranking; TRI-1 remains slice-063 highest-priority).
- **m1 (fabricated lessons-learned.md citation in ADR-060 §Decision)**: **VALIDATED** — disposition ACCEPTED-FIXED. Cockburn evidence-traceability discipline; meta-Critic re-verified the grep + the paraphrase replacement with disclaimer.
- **m2 (EPGD-1 section-header structural-separation reminder)**: **VALIDATED** — disposition ACCEPTED-FIXED. Fowler naming consistency; slice-060 L3943 anchor pattern preserved verbatim in slice-062's directive.
- **m3 (TPHD-1 sub-mode (a) mission-brief 4-row vs design.md 7-row TF-1 plan harmonization)**: **VALIDATED at surface; INCOMPLETE at audit-execution** — disposition ACCEPTED-FIXED in same fix block. The harmonization itself was correct (mission-brief should mirror design.md's table); but the table copy used multi-AC labels that TF-1 audit rejects → introduced M-add-1 (Blocker). The first Critic verified table-shape harmonization without running the audit empirically; meta-Critic caught the audit-rejection class. **Mixed finding**: VALIDATED for the surface issue (mission-brief was indeed stale), INCOMPLETE for the audit-execution gap (the fix should have run the audit).
- **m4 (R-15 historical-prose framing for slice-057 paragraph)**: **VALIDATED** — disposition ACCEPTED-FIXED. The slice-057 paragraph at risk-register.md:266 retains its `tests/methodology/*.py` scope claim verbatim as historical record per slice-040 R-10 retirement-precedent; the new slice-062 paragraph opens with explicit historical-record framing.

**Meta-Critic** (3 missed findings from `critique-review.md` + 6 first-Critic findings confirmed):

- **M-add-1 (TF-1 strict-pre-finish gate WILL FAIL — multi-AC labels + (audit-trace) markers)**: **VALIDATED** — disposition ACCEPTED-FIXED at /critique-review fix block. Meta-Critic empirically ran `tools.test_first_audit --strict-pre-finish` and reproduced 2 `ac-without-row` violations on AC#3 + AC#4. The fix was a re-harmonization to bare-numeric AC labels with multi-row-per-AC shape per slice-056 precedent; post-fix audit re-verified clean (`violation_count: 0`, 8 rows across 4 ACs). **N=2 slice-040 N+1 doctrine recurrence + N=2 slice-022 RSAD-1 self-violation pattern extending to dual-Critic stack same-fix-block edits**. Highest-impact missed finding of the dual-review stack this slice.
- **M-add-2 (cross-document option-enumeration incoherence)**: **VALIDATED** — disposition ACCEPTED-FIXED. 3-scheme drift was structurally unguarded by any audit; meta-Critic's pattern-match across 3 documents caught it. Updated all 4 sites to ADR-060 numeric 1-4 as authoritative scheme.
- **M-add-3 (design.md Phase C step 11 "4-part" stale-carry)**: **VALIDATED** — disposition ACCEPTED-FIXED. Adjacent to step 12's correct 5-part PMI-1 anchor; novel terminology not appearing elsewhere in the repo. Cheap disambiguation; the LIVE BEHAVIOR was already pinned by the CSP-1 entry-pin substring assertion (so this was prose-quality only).

**Missed by Critic** (build-time-reachable, gate-caught classes; per slice-037 law NO BC-1 promotion for build-time-reachable classes — the gates ARE the structural backstop):

1. **/code-review AGENT-UNSPAWNABLE R-18 recurrence** — caught at /code-review SKILL Step 2's `agent-unspawnable` error class (the gate fired correctly). N=2 cumulative; slice-063+ active mitigation nomination. NOT a Critic-prompt promotion candidate (it's a methodology workflow issue, not a slice-design issue).

**Pattern (cumulative N≥10 confirmation of slice-037 audit-vs-real-artifact law)**: dual-Critic stack precision held cleanly on slice-062 (6/6 first-Critic findings VALIDATED at correct severity by meta-Critic; meta-Critic added 3 VALIDATED missed findings with zero false positives across both layers); 0 false alarms; the 1 missed class was build-time-reachable + gate-caught. Cumulative streak N≥10 on codification-class slices (046/048/050/051/052/053/054/055/056/057/062 + the partial N=11 if slice-061 is counted as codification despite being a fix slice). Reaffirms: **do NOT add Critic dimensions for build-time-reachable classes; the existing gates ARE the structural backstop.** The cure for missed classes at this level is calibration of WATCH-LIST entries and `/critic-calibrate` periodic review, not new Critic dimensions.

**Specialization signal (N=1, watch-list)**: first-Critic 5/6 prose-coherence + 1 substantive technical (B1); meta-Critic 1 empirical-audit Blocker (M-add-1) + 2 prose-coherence (M-add-2, M-add-3). The empirical-audit-execution Blocker class is structurally reachable ONLY by running the audit empirically — the first-Critic's table-shape check is too weak. If N=2 specialization confirms at slice-063+, document as Critic-stack design observation (NOT a Critic-prompt dimension extension — the meta-Critic IS the structural backstop for this class by design).

## Lessons for next slice

- **The slice-040 N+1 first-governed-slice doctrine extends to N=12 cumulative on the slice-022 RSAD-1 self-violation axis**: slice-022 minted RSAD-1 (recursive self-application discipline); slice-056 /critique-review M-add-1 was the first observed dual-Critic-stack same-fix-block self-violation (TF-1 multi-AC-label sub-class); slice-062 /critique-review M-add-1 is N=2 cumulative on that specific sub-class + N=12 cumulative on the broader N+1 first-governed-slice axis. Per slice-037 law: NO Critic dimensions for build-time-reachable classes. The meta-Critic IS the structural backstop. /critic-calibrate candidate: sharpen the first-Critic prompt with "if the fix edits a structural-audit-parseable table (TF-1 plan, CSP-1 row, shippability row, etc.), run the relevant audit empirically before declaring ACCEPTED-FIXED — not just verifying table-shape harmonization." (slice-062)

- **MEPD-1 Inclusion-heuristic firing on scope-extension precedent extends to N=6 cumulative**: when a slice extends an existing rule's coverage set (drift-guard family member-addition, audit-scope-extension, etc.) without minting a new RULE-ID, the slice STILL warrants a methodology-changelog entry + ADR + PMI-1 bump + shippability row per the Inclusion heuristic. RESERVED for the voluntary-restraint-zero-bump-zero-ADR posture: retirement-discharge / part-(b) closure shapes (slice-040/043/045/056/057 N=5 cumulative). slice-062 makes the scope-extension count N=6 cumulative across slice-049/050/051/057/058/059/062. The discipline distinguishes by what the slice substantively does (extending coverage vs discharging a retirement gate), not by slice severity. (slice-062)

- **R-18 N=2 cumulative recurrence on slice-060 N+1 governed-slice axis** — predicted by slice-061 reflection L82, empirically confirmed this slice. Strengthens slice-063+ R-18 mitigation candidate from watch-list to **active nomination**. Cheapest fix: (a) `/build-slice` post-build new-agent warning. Mid-cost: (b) `/code-review` error-semantic widening to auto-skip + structured-options ASK (extends /code-review SKILL Step 2's existing `agent-unspawnable` handler to a more graceful auto-deferral path with documentation pin). Out-of-scope: (c) Claude Code platform fix. Strong slice-063 candidate. (slice-062)

- **First-Critic vs meta-Critic specialization signal (N=1 watch-list)**: emerging division of labor — first Critic on prose drift + cross-doc citation hygiene; meta-Critic on empirical audit re-execution + same-fix-block-edit-introduced regressions. If N=2 specialization confirms, document as Critic-stack design observation (NOT a Critic-prompt dimension extension — the meta-Critic IS the structural backstop for the empirical-audit-execution class by design). (slice-062)

- **Builder mid-design self-correction without /critique invocation IS a viable lightweight feedback path** — the mission-brief frontmatter L5 "Risk retired" / AC#4 misframings were corrected at /design-slice time (in the same fix block as design.md authoring) without firing /critique. The slice-061 BFRD-1 confirm-then-auto-invoke pattern is one canonical example of pre-/critique correction; this slice's mid-design correction is another. Class: "if Builder catches their own /slice-time misreading during /design-slice grounding, correct in the same fix block — don't carry forward stale claims into /critique." Documented inline in mission-brief.md via the correction edits. (slice-062)

- **N=12 cumulative codification-class voluntary-restraint discipline holds**: slice-062 minted ADR-060 + methodology-changelog v0.65.0 + 5-part PMI-1 bump + shippability row, BUT did NOT mint a new RULE-ID. The voluntary-restraint posture (slice-037/046/050/052/055/056/057/061 N=8 cumulative) applies cleanly to retirement-discharge slices; the **extends-not-mints** posture (slice-051/057/058/059/062 N=5 cumulative) is the analogous discipline for scope-extension slices. Both are tunable knobs driven by what the slice substantively does, not by slice severity. (slice-062)

## Vault updates made (thin vault — small list)

- [[methodology-changelog.md]] — appended `## v0.65.0 — 2026-05-23` entry (extends-not-mints; ADR-060 lineage; 8 substring anchors required by entry-pin test); installed copy `~/.claude/methodology-changelog.md` forward-synced per MCFS-1
- [[VERSION]] / [[plugin.yaml]] / [[pyproject.toml]] — 5-part PMI-1 bump 0.64.0 → 0.65.0 (incl. PVFS-1 leg pyproject.toml [project].version)
- [[~/.claude/ai-sdlc-VERSION]] — AVFS-1 forward-sync (installed copy)
- Venv site-packages `ai-sdlc-tools` 0.64.0 → 0.65.0 (TVFS-1 via `pip install --upgrade .`)
- [[risk-register.md]] — appended slice-062 scope-extension paragraph to R-15 block with explicit historical-record framing per slice-040 R-10 retirement-precedent (status STAYS `retired`; no flip; STP-1 Sub-form B clean)
- [[shippability.md]] — appended row #62 (cites BOTH R-15 AND ADR-060 per BCR-1 traceability axis; pytest command pins 4 corpus tests + repointed code-review test + 2 entry-pin tests)
- [[decisions/ADR-060-extend-r15-corpus-backstop-scope.md]] — authored at /design-slice (accepted; reversibility: cheap; mints no new RULE-ID; supersedes nothing; extends slice-056 / slice-057 R-15 backstop scope; naming-class peer of [[ADR-053]] OSDG-1-member-addition shape); §Decision paragraph 1 paraphrased to remove fabricated citation per /critique m1 + Consequences bullet 4 propagated to 5-part PMI-1 shape per /critique B1
- This slice's [[design.md]] — 7 surfaces harmonized at /critique fix block (B1 ×5 + m2 + m4) + 4 surfaces harmonized at /critique-review fix block (M-add-1 TF-1 plan + M-add-2 option enumeration + M-add-3 Phase C step 11 + L162-174 design-table mirror of M-add-1)
- This slice's [[mission-brief.md]] — AC#4 + verification-plan #4 corrected mid-design (R-15 stays retired) + Out-of-scope bullet 1 acknowledged-divergence note (M1) + Test-first plan section 4-row → 7-row → 8-row harmonized (m3 → M-add-1) + Pre-finish gate audit enumeration PVFS-1 named (B1) + Must-not-defer #5 deferred to ADR-060 enumeration (M-add-2) + Test-first plan flipped PENDING → PASSING at /build-slice Phase D (TF-1 v0.13.0 discipline)
- [[tests/methodology/test_resolve_slice_dir.py]] — extracted `_scan_corpus_for_r15_literals(corpus_dir)` helper + `_assert_corpus_clean(matches, label)` shared helper; rewrote existing `test_no_new_archive_fragile_literals_in_methodology_corpus` as thin wrapper (function name PRESERVED); added 3 NEW per-corpus tests + 1 NEW aggregated whitelist-integrity test
- [[tests/skills/code_review/test_code_review_skill.py]] — added `from tests.methodology.conftest import _resolve_slice_dir` import; REMOVED module-level `_SLICE_060_CODE_REVIEW = (...)` literal binding; lazy `_resolve_slice_dir(60) / "code-review.md"` inside `test_self_dogfood_produces_code_review_md_on_slice_060`
- [[tests/methodology/test_methodology_changelog.py]] — appended 2 NEW entry-pin tests under NEW SECTION header `# --- Slice-062 / R-15-scope-extension entry pinning ---` at end-of-file (EPGD-1 design-time-pre-empted-success-mode discipline)
- [[lessons-learned.md]] — chronological entry for slice 062 (appended below in Step 5)

**What was NOT updated** (because the slice didn't touch them):
- No new RULE-ID minted (ADR-060 is an extends-not-mints decision per the slice-051/ADR-053 precedent shape)
- No `~/.claude/{skills,agents,tools}/*` files (no skill/agent/tool added or modified — only test files + version files + documentation)
- No `tools/` modifications (the 5-part PMI-1 bump is the cleanest test-infrastructure scope-extension shape; no new tool, no tool refactor)
- No new ADR for the parallel-slice / worktree-per-slice user proposal (explicitly out-of-scope per /slice Step 3 decision; remains backlog candidate)
- No `architecture/build-checks.md` rule promotion (no recurring pattern N≥2 warranting BC-1 promotion — the TF-1-multi-AC-label N=2 class is build-time-reachable + gate-caught per slice-037 law; the empirical-audit-execution-on-fix-block-edits class is a /critic-calibrate route candidate, not a BC-1 rule)
- No `diagnose-out/backlog.md` round-trip (slice-062 is risk-register-driven NOT backlog-driven; mission-brief carries zero `**Closes:** SC-NNN` sentinels — BCR-1 correctly no-ops)
