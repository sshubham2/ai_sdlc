# Archived Slices Catalog

Chronological. Most recent at top.

| # | Slice | Shipped | Mode | Risk-tier | Result | Critic findings | Discoveries |
|---|-------|---------|------|-----------|--------|----------------|-------------|
| 081 | [slice-081-fix-drift-check-enforcement-gap](slice-081-fix-drift-check-enforcement-gap/) | 2026-05-29 | Standard | medium | SHIPPED | design-Critic 2B/3M/3m (B1/B2/M2/M3/m1/m2/m3 VALIDATED; M1 false-premise → meta re-graded Minor) + meta-Critic EXTEND (M-add-1 false-ACCEPT MAJOR + m-add-1 bootstrap Minor, both VALIDATED) + code-Critic 0B/0M/3m (m1 left-anchor + m2 AC5-wording fixed in-band; m3 no-change) | left-anchor regex gap (m1); new-audit-tool + version-bump 8-site drift fan-out (BC-PROJ-7 enumerates only 2) — /critic-calibrate candidate; procedural was-it-marked gate is a reusable CRP-1-clone family |
| 080 | [slice-080-harden-bc1-critical-rules-exit-gate](slice-080-harden-bc1-critical-rules-exit-gate/) | 2026-05-29 | Standard | medium | SHIPPED | design-Critic 3B+3M+2m (all VALIDATED, zero FALSE-ALARM) + meta-Critic EXTEND +1 (m-add-1, VALIDATED); code-Critic 0B/1M/2m (M1+m1 fixed in-loop, m2 deferred-cosmetic) | BCSG-1 / ADR-072 BC-1 strict critical-gate via `--strict`+`--ack-critical` acknowledgment (closes SC-008); standing-ack consequence (BC-PROJ-3+BC-GLOBAL-2 always-on Critical → every slice acks at Step 6); output-honesty is a code-Critic surface; "found-the-class-under-swept-surfaces" N=2 (/critic-calibrate candidate); v0.75.0 5-leg PMI-1 bump |
| 079 | [slice-079-bundle-074-075-077-078-code-critic-cleanup](slice-079-bundle-074-075-077-078-code-critic-cleanup/) | 2026-05-29 | Standard | medium | SHIPPED-WITH-DEFERRALS | design-Critic 2B+3M+6m (all ACCEPTED-FIXED, m6 PENDING) + meta-Critic +1 (M-add-1); code-Critic 0B/1M/4m (M1+m2 addressed in-loop, m1 won't-fix, m3/m4 deferred) | DEFER-1..6 (archive-immutability + extraction-trigger + AC#5 carve-out); design→code gap N=15; forward-handoff stable-home established (architecture/source-pending-items.txt); P3.10' routed; APED-1 clause-5 first-governed-slice |
| 078 | [slice-078-add-pcr-2a-vault-claim-resolver](slice-078-add-pcr-2a-vault-claim-resolver/) | 2026-05-29 | Standard | medium (critic-required: true — touches in-house methodology surfaces `tools/parallel_conflict_resolver.py` + `skills/commit-slice/SKILL.md` L185-192; mandatory-Critic trigger fires; MEPD-1 INCLUDE — methodology-changelog v0.74.0 entry; 5-part PMI-1 atomic bump 0.73.0 → 0.74.0; PCR-2a minted as new RULE-ID on existing `PCR-N` family axis sibling to PSQ-N; refines PCR-1's class taxonomy in-place; supersedes nothing; ADR-071 minted with reversibility: cheap) | SHIPPED — validation PASS (5/5 ACs PASS with per-criterion real-environment evidence: AC#1 `resolve_vault_claim_conflict` correctness across stage-2/stage-3 winner + tie/multi/silent-drop STOP + in-memory disk-ignore via 20 unit + integration tests; AC#2 both `resolve_soft_conflict` L242-253 + `_regen_slice_queue` L627-638 dispatch + UNKNOWN-still-fail-closed via 3 tests using real tmp_path-rooted git repos with staged rebase-in-progress state; AC#3 SKILL.md L185 Pin #1 + L192 Pin #2 APED-1-executed + OSDG-1 forward-sync via 3 tests; AC#4 audit-log row format + mixed-section append-only via 2 tests including lazy-create-on-first-of-each-section; AC#5 catalogued post-fix regression `test_vault_claim_gate_closed_returns_resolution_result` PASS + shippability row #78 added). VAL-1 0 secrets + 0 imports; SCMD-1 + PTFCD-1/PTFFD-1 pre-catalog gates clean (77 rows; 828 cited fns; 396 test-path tokens). 15+ Step-6 audits ALL CLEAN (TF-1 20/0/0 + WIRE-1 6 entries + BRANCH-2 on slice/078 + UTF8-STDOUT-1 32/32 + CRP-1 + PCA-1 9 skills + BCI-1 + MCFS-1 + STP-1 + AVFS-1 + TVFS-1 + NAW-1 + PMI-1 v0.74.0 26/6/32 + RR-1 only R-23 low/open + CAD-1). Full pytest **1072/1072 PASS** (was 1061 at slice-077 baseline; +11 net new = 5 PCR-2a test modules across 29 functions − 2 stale tests repaired in Phase D − net is +11 after Phase D rename + Phase E v0.74.0 entry-pin). Shippability runner **77/77 PASS 0 FAIL** post-row-add #78. 0 reality surprises. R-23 newly opened (cross-machine clock-skew in PCR-2a strict-newer rule producing wrong-winner-by-staleness; low/low/cheap/open; corrigibility hook = audit log records both Claimed-at timestamps; remediation candidates = monotonic claim sequence number queue candidate OR PCR-2b Critic-stack adjudication). | **3-Critic stack N=13 cumulative — 16 first + 2 meta + 5 code = 23 findings; 0 FALSE-ALARMs across all 3 layers; 5 of 5 code-Critic findings Missed-by-design+meta-Critic-stack structurally**. First-Critic NEEDS-FIXES 3B/4M/9m (B1 `parse_queue_md` non-existence + `parse_queue_text` strips Parallel-safety — both empirically verified; B2 single-direction repro structural-incoherence — pre-fix and post-fix same git state; B3 AC#2 omits CLI-facing `resolve_soft_conflict` dispatch site — guard at L243 short-circuits VAULT_CLAIM to STOP; M1 tie-rate honest framing — `_now_iso8601_utc` second-precision means ~1s tie window not "rare"; M2 em-dash vs hyphen audit-row separator 1-char sentinel fragility; M3 APED-1 pin literal under-specification on L185-192 600-char prose; M4 write semantics winner-in-stage-2 demotion via `_regen_slice_queue:614` baseline_text=text_3 default; m1-m9 minors including line-number drift + multi-collision helper shape + HEAD-SHA field redundancy override + L192 cosmetic wording defer + 3 none-available branches + PMI-1 entry-pin name + audit log absent on disk + wiring matrix omission + clock-skew corrigibility R-23). Meta-Critic EXTEND with 2 MISSED (M-add-1 Minor overlay silent-drop when winner block lacks Risk-retired pivot per PCR-1 `_overlay_claims_on_queue_text:687` silent-drop semantics — defensive post-overlay `re.search` shipped; M-add-2 Major step-3/step-4 disk-read sequencing race — `_pick_loser_replacement` signature changed `(repo_root, exclude_names)` → `(queue_text, exclude_names)` so disk-read during VAULT_CLAIM rebase-in-progress structurally impossible). **18/18 dispositions ratified at TRI-1 2026-05-29 user accept-all**: 16 ACCEPTED-FIXED in same fix block per TPHD-1 sub-mode (a)+(b) before /build-slice + 1 OVERRIDDEN (m3 HEAD-SHA section-type symmetry preserved) + 1 DEFERRED (m4 L192 cosmetic → bundle) + 1 ACCEPTED-PENDING (m9 R-23 + queue candidate discharged at Phase F). Final pre-build verdict NEEDS-FIXES. Code-Critic FINDINGS 0B/0M/5m advisory per CRSI-1 v1: m1 `_format_vault_claim_audit_entry` re-derives winner/loser from `diag.claim_history` (DRY violation — resolver has them in scope); m2 `_pick_loser_replacement`'s missing-Parallel-safety default `"UNKNOWN-NO-GRAPH"` overloads PSQ-1 valid sentinel; m3 Step 5 inline write-then-stage inverts PCR-1 SOFT path's batch-after-success discipline; m4 design.md `AC#1, 4 tests` claim stale vs actual 20 test functions (TPHD-1 sub-mode (a) recurrence; build-log captures Conformance deviation); m5 `_VaultClaimDispatch` catch-clause-order-by-accident not by inheritance — needs clarity comment. All 5 DEFERRED to slice-079+ `bundle-074-075-077-078-code-critic-cleanup` per voluntary-restraint **N=18 cumulative** (slice-037/046/050/052/055/056/057/061/065/067/070/071/072/073/074/075/077/078). **3-Critic stack value-validation N=13 cumulative continues** — code-Critic catches what design+meta cannot reach (audit-formatter DRY + sentinel-overload + atomicity-asymmetry + stale-doc-count + sibling-inheritance-clarity span code-Critic-only analysis lanes). | **PCR-2a minted on existing PCR-N family axis** (refines PCR-1's class taxonomy in-place; methodology v0.74.0 per 5-part PMI-1 atomic bump). **Pin #2 regex over-broad shape required mid-build refinement** — first-Critic M3 spec'd APED-1 discipline FOR ITSELF but didn't apply it to its own proposed pin literal; APED-1 execution at build time caught the over-broad shape (`VAULT_CLAIM[^\n]+fall[- ]closed` matched legitimate corner-case prose; refined to `VAULT_CLAIM\s*\([^)]+\)\s*\+\s*HARD\s*\(` — pre-fix multi-class enumeration only). **Design→code translation gap N=15 cumulative** (extends slice-077 N=14); strongest /critic-calibrate proposal target. **Self-validating-slice property N=2 cumulative** (extends slice-077 N=1) — both slice-077 + slice-078 touched parallel-slice family AND structurally dogfooded themselves at /commit-slice --merge time. Watch slice-079 PCR-2b for N=3 codification trigger. **MEPD-1 INCLUDE empirically supported when scope refines an existing rule's axis in-place** — PCR-2a refines PCR-1 on existing PCR-N axis; methodology v0.74.0 entry ships with full 5-part PMI-1 atomic bump correctly. Inverse counterexample to slice-077's MEPD-1 EXCLUDE pattern. **Voluntary-restraint discipline N=18 cumulative** (5 advisory code-Critic findings DEFERRED). **R-23 newly opened (cross-machine clock-skew accepted-residual)** with corrigibility hook = audit log records both Claimed-at timestamps; queue candidate `add-claim-sequence-number-for-clock-skew-detection` registered. **PCR-2a sized as "SMALL-MEDIUM" effort but shipped wider surface than initial AC#1 "1 test module" assumption** — sizing signal for PCR-2b. **When a Critic finding triggers a Builder-added algorithm spec in the post-fix design, re-interrogate THAT new spec at meta-pass time** — slice-078 meta-Critic M-add-2 caught the disk-read race in the Builder-added 7-step Resolution algorithm; pattern N=1 watch-list. **Strong follow-up nominations**: slice-079 PCR-2b (HARD-conflict full Critic stack + TRI-RESOLVE-1 user triage); slice-080+ SP-1 (/slice no-arg auto-pick); bundle-074-075-077-078-code-critic-cleanup (5 + ~13 + ~2 + ~5 = ~25 findings; voluntary-restraint N=18 promotion-overdue); /critic-calibrate (severely overdue — design→code translation gap N=15 + voluntary-restraint N=18 + APED-1-execution-at-/critique-not-build pattern). NOT a BCR-1 round-trip (zero `**Closes:** SC-NNN` sentinels — risk-register-driven from R-21/R-23 design-axis derivative of slice-076 PCR-1 forward-references). |
| 076 | [slice-076-add-pcr-1-conflict-diagnostic-and-soft-regen](slice-076-add-pcr-1-conflict-diagnostic-and-soft-regen/) | 2026-05-29 | Standard | medium (critic-required: true — touches in-house methodology surfaces `skills/commit-slice/SKILL.md` Step 5b sub-step 2.5 + mints new rule PCR-1 + new ADR-069; mandatory-Critic trigger fires; MEPD-1 INCLUDE — methodology-changelog v0.73.0 entry; 5-part PMI-1 atomic bump 0.72.0 → 0.73.0; BC-PROJ-9 5-inventory bump 30 → 31) | SHIPPED-WITH-DEFERRALS — /validate-slice 5/5 ACs PASS with per-criterion real-environment evidence (AC1 PCR-1 rule + ADR-069 + 5-class taxonomy + 5/5 entry-pin tests PASS; AC2 SKILL.md L185 PCR-1 dispatch + 4 mandated diagnostic fields + ordering invariant + 2/2 prose-pin tests PASS; AC3 14/14 soft-resolve+overlay+audit-log tests PASS including 3 net-new fix-in-band regression tests for M1+M2+M3 + APED-1 battery 28/28 cases observed=expected; AC4 real CLI invocations exits 0/0/1 per design + 11/11 helper-module unit tests PASS; AC5 1040/1040 full pytest + 75/75 shippability + 18 Step-6 audits clean modulo 2 BC-1 Important defer-with-rationale + CAD-1 byte-equality clean). VAL-1 layered safety clean (0 secrets across 33 changed files + 0 hallucinated imports; --imports-allowlist tests). SCMD-1 + PTFCD-1 pre-catalog gates clean (75 rows; 790 cited fns; 387 test-path tokens). 18 Step-6 audits ALL CLEAN (TF-1 31/0/0 + WIRE-1 + BRANCH-2 on slice/076 + UTF8-STDOUT-1 31/31 + CRP-1 + PCA-1 9 skills + BCI-1 + MCFS-1 + STP-1 + AVFS-1 + TVFS-1 + NAW-1 + CAD-1 32ac61463 + PMI-1 v0.73.0 26/6/31 + INST-1 26/26+6/6+4/4+31/31 + shippability runner). 2 BC-1 Important defer-with-rationale per **N=7 cumulative** prose-vs-automation false-positive class (BC-PROJ-11 keyword-glob false-positive on INSTALL.md `30→31` integers + BC-GLOBAL-2 slice-076 doesn't add git checkout/restore/stash automation). Full pytest 1040/1040 PASS (was 1036 pre-fix-in-band + 3 regression tests for M1+M2+M3 + 1 BC-PROJ-12 oracle test); shippability runner 75/75 PASS, 0 FAIL. Multi-instance: N/A per ADR-069 § Adversarial model (PCR-1 is local-only cooperative-coordination convention, not security boundary). 0 reality surprises. R-21 open residual tracked for empirical refutation. | **3-Critic stack N=12 cumulative — 16 first-Critic + 5 meta-Critic + 9 code-Critic = 30 findings; 0 FALSE-ALARMs across all 3 layers; 9 of 9 code-Critic findings Missed-by-design+meta-Critic-stack structurally**. First-Critic NEEDS-FIXES 4B/7M/5m (B1 parse_queue_text wrong module + B2 SOFT file-set 4→2 sweep + B3 _index.md Haiku-non-deterministic + B4 VAULT_CLAIM gate spec; M1 Windows backslash + M2 stage-missing edge + M3 _index.md AC coupling + M4 UNKNOWN/MIXED ACs + M5 SELF-WITHDRAWN + M6 INSTALL.md two-site + M7 audit-log race; m1 APED-1 citation + m2 Path-keyed dict + m3 BC-PROJ-10 5-anchor + m4 PCA-1 ref + m5 R-21 entry). Meta-Critic EXTEND with 5 M-add findings (M-add-1 phantom helper function name; M-add-2 symmetric stage-missing row; M-add-3 BC-PROJ-10 anchor (e) reverted to 3-word literal; M-add-4 ADR-069 reversibility 3→2 sweep; M-add-5 APED-1 4-predicate enumeration). 21/21 ACCEPTED-FIXED at TRI-1 (only M5 OVERRIDDEN via Critic self-withdrawal). **Code-Critic FINDINGS 0B/3M/6m (CRSI-1 v1 advisory): M1 EOL-DRIFT-1 / ADR-033 (3 write_text + 1 open("a") omit newline=""; PSQ-2 sibling-writers correctly opt in but PCR-1 silently opted out at Phase C); M2 atomicity gap (mid-loop write-then-raise leaves slice-queue.md silently overwritten while resolver returns STOP — defeats ADR-069 atomicity contract); M3 missing defense-in-depth VAULT_CLAIM gate IN-HELPER (design.md L136 step 3 + critique B4 fix (c) EXPLICITLY mandated it; impl just didn't do it — strongest design→code translation gap evidence); m1 __import__("os") inline anti-pattern; m2 bare except Exception swallow; m3 silent claim-drop on malformed candidate block; m4 6 unused pytest imports; m5 porcelain rename-with-arrow not handled; m6 audit-log TOCTOU lazy-create first-writer-loses**. **User chose option-3 fix-all at TRI-disposition** (first instance of fix-in-band user override at /code-review per CRSI-1 v1 walking-skeleton — N=1 cumulative); ALL 9 findings closed in-band at fix-in-band commit 90f6976 (~60 min including 3 net-new regression tests: test_append_audit_log_writes_lf_only_no_crlf_translation + test_resolve_soft_conflict_atomicity_preserves_slice_queue_on_helper_error + test_regen_slice_queue_vault_claim_defense_in_depth_gate_fires_on_different_identity). **Voluntary-restraint N=16 cumulative UNCHANGED** (user chose fix-in-band over defer; no bundle queue accumulation this slice). **TPHD-1 sub-mode (a) HELD at N=8 cumulative** (no new instance — Builder fix-block discipline tight: 19 design-time fixes 0 meta-Critic regressions). **RSAD-1 self-introduced-pollution N=4 cumulative this-slice-alone** (vs slice-075 N=3 — slowing rate suggests slice-075 reflection lesson on literal-uniqueness had carryover). | **PCR-1 minted on a new family axis** (parallel-conflict-resolution v1 sibling to PSQ-N; first rule on resolve-not-detect layer; methodology v0.73.0 per 5-part PMI-1 atomic bump). **Stage-then-commit atomicity refactor (Phase H)** structurally enforces ADR-069's "atomicity — never partial auto-resolve" contract: helpers return (Path, str) tuples without writing; resolve_soft_conflict batch-writes only on all-success. **Design→code translation gap canonical instance (slice-076 M3)** — when critique fix prose mandates an algorithm with named code change (B4 fix (c) "defense-in-depth defeats this here" with algorithm spelled out), Builder MUST implement it at /build-slice Phase C; otherwise code-Critic catches at post-build. /critic-calibrate slice-077+ proposal target: codify a pre-finish grep check that walks critique.md ACCEPTED-FIXED fixes + verifies named code changes exist. Watch-list N=1; promote to BC-1 build-check at N≥2. **User fix-in-band disposition N=1 cumulative** — first instance of option-3 user override of voluntary-restraint default at /code-review; new methodology data point: high-confidence Major findings warrant fix-in-band; bundle queue empty this slice; watch-list for N≥3 to formalize disposition heuristic. **BC-PROJ-12 promoted at /reflect Step 5b** ("New helpers writing markdown files must use newline='' to preserve LF on Windows" at N=2 cumulative threshold; PSQ-2 slice-072 + PCR-1 slice-076; BCI-1 + literal-constant oracle test PASS). **3-Critic stack value-validation N=12 cumulative continues** — code-Critic catches what design+meta cannot reach. **R-21 newly opened** (anticipated SOFT auto-regen corner-case residual per /critique-review m5 ACCEPTED-PENDING; not surfaced during validation; tracked for empirical refutation in future parallel-slice usage). **Methodology-prose-pipe-leakage RSAD-1-adjacent class N=1** (slice-076 shippability row #75 pipe-leakage caught at SCMD-1 + fixed in-band; structurally-self-evident at row-write time via `awk -F'|' '{print NF}'`). **TF-1 grammar patterns**: `(manual)` Test path refused as PASSING (PTFCD-1) + phantom test fn names refused (PTFFD-1) — both caught in-band at Phase G. **Phase D (mid-slice smoke gate) folded into Phase E** — smoke-gate assertions require Phase E artifacts to be meaningful; folded cleanly with no semantic loss. **Strong follow-up nominations**: slice-077 PCR-2 (VAULT_CLAIM timestamp-winner + HARD full Critic stack + TRI-RESOLVE-1 user triage); slice-078 SP-1 (/slice no-arg auto-pick); slice-077+ `slice-NNN-bundle-074+075+076-code-critic-cleanup` (8 carry-over findings: bundle-074 6 + bundle-075 2; bundle-076 zero since fix-in-band); /critic-calibrate slice-077+ (cumulative signals: design→code translation gap N=1 watch-list + user fix-in-band disposition N=1 watch-list + pipe-leakage discipline N=1 + EOL-DRIFT-1 BC-1 promoted; runs every 10-20 slices, last at 2026-05-28 covering 061-075). NOT a BCR-1 round-trip (zero `**Closes:** SC-NNN` sentinels — risk-register-driven from 2026-05-28-surfaced 5-session parallel-slice deadlock pattern). |
| 075 | [slice-075-close-merge-substep-3-worktree-collision](slice-075-close-merge-substep-3-worktree-collision/) | 2026-05-28 | Standard | medium (critic-required: true — in-house methodology surface touched: `skills/commit-slice/SKILL.md` Step 5b 3 prose edits; MEPD-1 EXCLUDE — in-band methodology-prose-fix to existing ADR-063 BRANCH-2 + ADR-068 PSQ-3 + ADR-020 commit-slice 3-mode contracts; no methodology-changelog entry; no PMI-1 bump; ships at v0.72.0 unchanged) | SHIPPED — validation PASS (5/5 ACs PASS with per-criterion real-environment evidence: AC#1 cd "$main_tree" before git checkout $default + AC#2 WT-clean lifted to sub-step 2.1. + AC#3 canonical awk extraction + AC#4 silent-WT-discard intent preserved + AC#5 meta-AC via shippability 74/74). VAL-1 0 secrets + 0 imports; SCMD-1 + PTFCD-1 pre-catalog gates clean (74 rows; 783 cited fns; 381 test-path tokens). 16+ Step-6 audits ALL CLEAN (TF-1 5/5 PASSING + WIRE-1 + BRANCH-2 on slice/075 + UTF8-STDOUT-1 30/30 + CRP-1 + PCA-1 9 skills + BCI-1 + MCFS-1 + STP-1 + AVFS-1 + TVFS-1 + NAW-1 + CAD-1 + PMI-1 v0.72.0 unchanged + SCMD-1 + shippability runner + LINT-MOCK clean). 3 BC-1 defer-with-rationale per **N=7 cumulative** prose-vs-automation false-positive class (slice-069/070/071/072/073/074/075); BC-PROJ-4 satisfied. Full pytest 1006/1006 PASS (was 1002 at slice-074 baseline; +4 new structural-pin tests); shippability runner 74/74 PASS 0 FAIL. | **3-Critic stack N=11 cumulative — 4 first + 2 meta + 2 code = 8 findings; ZERO FALSE-ALARMs**. Pass-1 first-Critic NEEDS-FIXES 0B/2M/2m all VALIDATED (M1 shippability arithmetic ≥75/75 → ≥74/74 empirical baseline 73 + M2 AC#2 test scoping ambiguous because pre-flight header appears 3x in SKILL.md + m1 misleading "matches sub-step 5" → "SIBLING-BUT-DISTINCT idiom" + m2 "2-bis" Markdown → "2.1." sibling-convention symmetry). Pass-2 meta-Critic EXTEND with 2 missed Major RSAD-1-class findings (M-add-1 AC#4 paired-pin would PASS pre-fix because L168 has all 3 intent literals; M-add-2 AC#2 sub-assertion (b) would PASS pre-fix because L181 PSQ-3 conflict-STOP has `git status --porcelain` after L173 `git commit`). 6/6 ACCEPTED-FIXED in-band at TRI-1; final verdict CLEAN. Build-time 2 additional RSAD-1-class defects surfaced + resolved in-band at mid-slice smoke (self-introduced SKILL.md annotation literals polluting structural-pin tests; resolved by rephrasing annotations + tightening AC#1 test pin to canonical invocation `git checkout $default`). Code-Critic FINDINGS 0B/0M/2m advisory per CRSI-1 v1: m1 `_extract_substep_2_1_block` narration-leakage (`find("2.1.")` returns offset 1131 narration vs 1812 real marker; 681-char prefix gap; latent AC#4 regression vector) + m2 source-document-move stale-anchor sweep (5 in-vault refs to old `enable-parallel-slice-pending-items.txt` path in mission-brief + design after move to slice scaffold). Both DEFERRED to slice-076+ `slice-NNN-bundle-075-code-critic-cleanup` per voluntary-restraint **N=16 cumulative**. **RSAD-1 annotation-literal-pollution sub-class N=3 cumulative this-slice-alone** (2 build-time + 1 post-finish) — strongest /critic-calibrate proposal target. | **3 surgical prose edits at SKILL.md Step 5b closed P1.2 + P2.4** from source-pending-items.txt — codifies slice-074's empirical Builder ad-hoc cd-to-main-tree workaround into prose. **/critic-calibrate SEVERELY OVERDUE at SIX simultaneous signals** (extends slice-074 FIVE): (1) TPHD-1 sub-mode (a) N=8 cumulative with slice-075 sub-class variant "file-move-but-anchor-not-swept"; (2) BC-1 BC-GLOBAL-2 prose-vs-automation N=7 cumulative; (3) AC-count > 5 N=3 (slice-075 deliberately reverted to 5 ACs); (4) MEPD-1-EXCLUDE-vs-shippability-row N=2 cumulative (slice-068 + slice-075 confirms axes independent); (5) Scope-expansion-at-/build-slice-plan-mode N=1; (6) **NEW**: RSAD-1 annotation-literal-pollution N=3 this-slice-alone. **For ANY structural-pin test that asserts a literal in prose-as-executable-contract surfaces, the pinned literal MUST be UNIQUE-TO-THE-INVOCATION** (not a noun-phrase appearing in informative narration). **For file-move operations within a slice, sweep all in-vault references to the old path** — TPHD-1 sub-mode (a) sub-class variant. **3-Critic stack N=11 cumulative complementarity stable** — code-Critic m1 + m2 are structurally unreachable by design+meta-Critic stack (substring-vs-line-start anchor granularity + file-move stale-anchor span the code-Critic's analysis lane). **Voluntary-restraint N=16 cumulative** + **Slice-074's switch-commit-switch + R-20 cp -r codifications functioned cleanly at slice-075 Phase A prereq** (N+1 first-governed-slice empirical validation PASSED). **Strong follow-up nominations**: slice-076 candidate `/critic-calibrate` (highest priority — 6 accumulated signals); slice-076 candidate `slice-NNN-bundle-075-code-critic-cleanup` (2 findings + lineage extends bundle-074 deferrals); P2.2 SOAD-1 retrofit in /commit-slice; P2.3 PSQ-3 conflict-STOP re-entry semantics; P3.6 parallel-slice-family cross-spec parity audit; P6.3 empirical parallel-slice readiness validation with 2 concurrent Claude sessions; R-13 OSDG-1 extension to /slice-candidates (open low-band risk). NOT a BCR-1 round-trip (zero `**Closes:** SC-NNN` sentinels — risk-register/source-document-driven from slice-074-named pending-items). |
| 074 | [slice-074-codify-cp-r-in-branch-2-skill](slice-074-codify-cp-r-in-branch-2-skill/) | 2026-05-28 | Standard | low (critic-required: true — in-house methodology surface touched: `skills/build-slice/SKILL.md ### Branch state` point 1 cp -r codification + point 4 switch-commit-switch codefence; OSDG-1 / CAD-1 family; MEPD-1 EXCLUDE — operationalizes existing R-20 candidate (a) + codifies N=5 cumulative switch-commit-switch empirical pattern; no methodology-changelog entry; no PMI-1 bump; ships at v0.72.0 unchanged) | SHIPPED-WITH-DEFERRALS — validation PASS (6/6 ACs PASS with per-criterion real-environment evidence: AC1 cp -r at point 1 via 2 structural-pin tests + grep + R-20 reference comment at SKILL.md L61; AC2 set-e-safe if-then-fi guard via structural-pin test + POSIX semantic walk-through; AC3 OSDG-1 sync via drift test post-cp; AC4 R-20 retired via audit + risk-register L344+L346 cite bootstrap-exception per CRP-1/ADR-024; AC5 switch-commit-switch + no-`git stash` via 2 structural-pin tests + grep L77/L79/L80/L81; AC6 both worktree-create forms via test + grep L59 (-b) + L81 (no-`-b`)). VAL-1 0 secrets + 0 imports; SCMD-1 + PTFCD-1 pre-catalog gates clean. 16 Step-6 audits ALL CLEAN (TF-1 8 rows all PASSING + BRANCH-2 on slice/074 + UTF8-STDOUT-1 30/30 + CRP-1 + PCA-1 + BCI-1 + MCFS-1 + STP-1 + AVFS-1 + TVFS-1 + NAW-1 + WIRE-1 + LINT-MOCK-1 0 violations + INST-1 + PMI-1 v0.72.0 + CAD-1). 5 BC-1 defer-with-rationale: BC-PROJ-3 + BC-GLOBAL-2 Critical defer-with-rationale per **N=6 cumulative** prose-vs-automation false-positive class (slice-069/070/071/072/073/074); BC-PROJ-4 satisfied (BRANCH-2 audit ran clean); BC-PROJ-5 + BC-PROJ-11 keyword false-positives (slice doesn't rename identifiers + doesn't edit INSTALL.md/README.md). Full pytest 1002/1002 PASS (was 995 at slice-073 baseline; +7 net new = 3 cp-r-step + 1 r20-retired + 3 dirty-tree-resolution including no-`git stash` discipline pin); shippability runner 73/73 PASS 0 FAIL (no row #74 added per design.md L96 carve-out; flagged in Discovered as /critic-calibrate signal — slice-068 EXCLUDE precedent contradicts: slice-068 DID add row #67). R-20 cp -r tax fired **N=9 cumulative** at slice-074 Phase A (slice-067 N=3 → slice-074 N=9; FROZEN at N=9 — R-20 retired by THIS slice; slice-075+ structural reset point reads codified prose at Phase A). | **3-Critic stack N=10 cumulative — pass-1 + pass-2 + code = 15 + 5 + 6 + 2-missed = 28 findings, ZERO FALSE-ALARMs across both /critique passes + meta-Critic ACCEPT on pass 2**. **First scope-expansion-at-/build-slice-plan-mode methodology pattern N=1**: user-approved at PCA-1 plan-mode gate (option 2 over recommended option 1); pass-2 /critique + /critique-review run on AC#5+AC#6 expansion delta only; pass-1 AC#1-AC#4 clearance preserved. Pass-1 first-Critic NEEDS-FIXES 0B/4M/4m all VALIDATED (M1 recursive-self-application bootstrap contradiction — slice-074 reads installed pre-slice SKILL.md at own Phase A; M2 section-extraction regex over-terminates on `## ` codefence comments — tightened to `(?=^## [A-Z])` via APED-1; M3 cp -r regex over-matches on comment-only mentions — guard-prefix anchor; M4 MEPD-1 EXCLUDE rationale precedent-axis confusion + SEVERITY-WRONG Major→Minor accepted at TRI-1; m1 +4-vs-5-rows arithmetic; m2 PMI-1 contingency anachronism; m3 chained-form set-e fragility; m4 TF-1 row 4 prose-vs-function-name). Pass-1 meta-Critic ADJUST with 2 missed Minor (m-add-1 single-line if/then/fi constraint pinned via RSAD-1 + m-add-2 Phase A/B/C glossary added). Pass-2 first-Critic NEEDS-FIXES 1B/2M/2m all VALIDATED (**B1 AC#6 no-`-b` regex falsified by canonical `# no -b; branch exists` trailing comment — APED-1 evidence; would have STALLED Phase A red-test verification absent comment-aware lookahead `(?!(?:[^#\n]*?)-b\s)`**; M1 AC#5 token-search PASSES on prose-only narrative without codefence — codefence-scoped `_point_4_codefence_body` + 5th scaffold-commit anchor; M2 `_point_4_block` no upper boundary — shared mechanism with M1; m1 no slice-070 reflection L127 citation; m2 NO-`git stash` discipline not structurally pinned — new test `test_point_4_codefence_does_not_contain_git_stash`). **Pass-2 meta-Critic ACCEPT (0 sus / 0 missed / 0 sev-adj — cleanest possible outcome)**; meta-Critic re-executed B1 fix against 10 synthetic edge cases (tab + path-with-`-b`/`-branch` variants) — empirically sound. 15 dispositions ratified across TRI-1 + TRI-1-EXT (all ACCEPTED-FIXED in-band on mission-brief + design before /build-slice). Code-Critic FINDINGS 0B/1M/5m advisory per CRSI-1 v1 walking-skeleton — **M1 `$wt_base`/`$repo_root`/`$default` variable-scope at point 4 (real semantic defect; mutually-exclusive branches; point 1's codefence variable assignments don't execute at point 4)**; m1 `<scaffolding files>` placeholder underspecification; m2 cp-r seed duplicated at point 4 — TPHD-1 sub-mode (a) recurrence trap; m3 AC#6 regex comment-position-fragile false-alarm; m4 `_branch_state_section` helper duplicated; m5 `check=True` masks subprocess errors. All 6 DEFERRED to slice-075+ `slice-NNN-bundle-074-code-critic-cleanup` per voluntary-restraint precedent **N=15 cumulative** (slice-037/046/050/052/055/056/057/061/065/067/070/071/072/073/074). **TPHD-1 sub-mode (a) HELD at N=7 cumulative** (no new instance — Builder fix-block discipline tight; ~17 design-time fixes applied with 0 meta-Critic-caught regressions across both /critique passes). | **R-20 cp-r tax + switch-commit-switch-worktree patterns BOTH structurally retired by slice-074 codifications in `skills/build-slice/SKILL.md ### Branch state`** — slice-075+ first Phase A prereq reads codified prose; slice-074 itself is bootstrap instance per CRP-1/ADR-024 slice-026 mirror. **/critic-calibrate SEVERELY OVERDUE at FIVE simultaneous signals** — TPHD-1 N=7 + BC-GLOBAL-2 N=6 + AC-count > 5 N=3 (slice-067/072/074 carve-out confirmed) + MEPD-1-EXCLUDE-vs-shippability-row-disentanglement N=1 (design.md L96 self-justifying claim contradicts slice-068 actual practice) + scope-expansion-at-plan-mode + re-Critic-the-delta discipline N=1. **Scope-expansion-at-/build-slice-plan-mode + re-Critic-the-delta + TRI-1-EXT methodology pattern** N=1 cumulative — methodology-strict path validated (pass-2 found 5 ACCEPTED-FIXED including 1 Blocker that would have stalled Phase A); watch-list for N≥3 codification. **APED-1 execution on regex-shape contracts is the gold-standard rigor** for /critique on prose-as-executable-contract surfaces — pass-2 first-Critic APED-1 on 3 of 5 findings before filing is the model for slice-075+ design-Critic runs on codification slices; promote to /critic-calibrate proposal as Critic-prompt addition. **3-Critic stack N=10 cumulative complementarity stable** — design-Critic + meta-Critic + code-Critic each catch a structurally-distinct defect class. Code-Critic M1 (point 4 variable-scope) is canonically a post-code APED-1-execution-only-surface defect class (design-Critic stack reviewed prose before codefence written; gap surfaced only at post-build empirical execution). **Switch-commit-switch codification's pass-2 m2 (NO-`git stash` discipline pin)** is the first instance of declared-in-out-of-scope-then-pinned-structurally promotion (slice-022 codify-empirical-discipline axis; AC#1 M3 guard-prefix anchor sibling precedent). **MEPD-1 EXCLUDE precedent for codification-of-empirical-pattern slices** held cleanly — codification-of-empirical-pattern → MEPD-1 EXCLUDE; rule-mint → MEPD-1 INCLUDE. **Voluntary-restraint discipline extends to N=15 cumulative** (9 consecutive cycles). **R-20 cp-r tax frozen at N=9 cumulative** (retired by THIS slice; slice-075+ structural reset point). **Switch-commit-switch pattern frozen at N=5 cumulative** (codified at point 4 retires pre-codification visibility). **Strong follow-up nominations**: slice-075 candidate `/critic-calibrate` (highest priority — five accumulated signals SEVERELY overdue); slice-075 candidate `slice-NNN-bundle-074-code-critic-cleanup` (6 findings); slice-075 N+1 first-governed-slice for both codifications (empirical test of M1 variable-scope defect surfaces if dirty-tree-on-default fires at slice-075's Phase A); R-13 OSDG-1 extension to /slice-candidates (open low-band risk); close-merge-substep-3-worktree-collision-stop-asymmetry (queue candidate #7 — different surface from slice-074, deferred). NOT a BCR-1 round-trip (zero `**Closes:** SC-NNN` sentinels — risk-register-driven from slice-067-073 R-20 nomination + user-approved scope expansion at plan-mode). |
| 073 | [slice-073-add-rebase-and-conflict-discipline](slice-073-add-rebase-and-conflict-discipline/) | 2026-05-28 | Standard | medium (critic-required: true — in-house methodology surfaces touched: `skills/commit-slice/SKILL.md` Step 5b sub-step 2.5 insertion + `methodology-changelog.md` v0.72.0 entry + new `ADR-068-mint-psq-3-rebase-and-conflict-discipline.md` + new structural-pin test module + 5-part PMI-1 atomic bump 0.71.0 → 0.72.0; PSQ-3 minted as new RULE-ID on parallel-slice family axis sibling to PSQ-1 / PSQ-2 / BRANCH-2; supersedes nothing; first rule on the *rebase-discipline* axis) | SHIPPED — validation PASS (5/5 ACs PASS with per-criterion real-environment evidence: AC1 rebase-before-merge via 3 structural-pin tests + Read of SKILL.md sub-step 2.5; AC2 conflict-STOP via 2 structural-pin tests + Read of conflict-STOP block + SOAD-1 3-option ask; AC3 PSQ-3 minted via entry-present test + ADR-068 file existence + new test module; AC4 BC-PROJ-10 paired-pin tests PASS + shippability row #73 + TF-1 strict-pre-finish clean; AC5 5-part PMI-1 atomic bump via version-sync test + AVFS-1 + TVFS-1). VAL-1 0 secrets + 0 imports; SCMD-1 + PTFCD-1 pre-catalog gates clean. 18 Step-6 audits ALL CLEAN (PMI-1 26/6/30 v0.72.0 + AVFS-1 + MCFS-1 + TVFS-1 + CAD-1 + BRANCH-2 + UTF8-STDOUT-1 30/30 + CRP-1 + PCA-1 + BCI-1 + STP-1 + NAW-1 vacuous + WIRE-1 + RR-1 + INST-1 + SCMD-1 + PTFCD-1 + critique-review + triage). 1 BC-1 BC-GLOBAL-2 Critical defer-with-rationale per **N=5 cumulative** prose-vs-automation false-positive class (slice-069/070/071/072/073 — `/critic-calibrate` slice-074+ proposal target genuinely overdue at THREE simultaneous signals: TPHD-1 N=7 + BC-GLOBAL-2 N=5 + AC-count N=2). Full pytest 995/995 PASS (was 987 at slice-072 baseline; +8 net new = 5 rebase-flag + 2 paired-pin v0.72.0 entry tests + 1 version-sync test); shippability runner 73/73 PASS 0 FAIL. R-20 cp -r tax fired **N=8 cumulative** (slice-067 N=3 → slice-073 N=8; severely overdue structural fix via R-20 candidate (a) codify-cp-r-in-BRANCH-2-SKILL.md). | **3-Critic stack N=9 cumulative — 16 first + 6 meta + 4 code = 26 findings, ZERO FALSE-ALARMs**. First-Critic NEEDS-FIXES 6B/6M/4m all VALIDATED (B1 PTFCD-1 phantom `tests/skills/commit_slice/` test dir; B2 AC3 design-narrowing not back-propagated; B3 AC1 scope contradiction `--merge` vs `--sync-after-pr`; B4 Must-not-defer items demand audit module; B5 paired-pin name `_in_changelog` vs `_in_repo`; B6 ADR-068 filename `add-` vs `mint-psq-3-`; M1 worktree-vs-main-tree collision; M2 conflict-STOP re-entry semantics; M3 SOAD-1 mid-skill unjustified; M4 slice-064 precedent misleading; M5 Verification plan stale audit; M6 option (c) operational path; m1-m4 cosmetics). Meta-Critic EXTEND with 6 missed findings ALL Builder-fix-block-introduced regressions (M-add-1 Major M4 sweep regression — ADR-068 L29 + L53 still carried slice-064 appeal — canonical **TPHD-1 sub-mode (a) N=7 cumulative instance**; M-add-2 Major M1 fix introduced citation error — SKILL.md L259 is `--sync-after-pr` not `--merge`; m-add-1 off-by-one "5 confirmation sites" should be "6"; m-add-2 stale "open to /critique review" post-/critique prose; m-add-3 ADR-068 L37 N=10 vs 11-item enumeration; m-add-4 fast-forward-no-op framing inverted). 22 dispositions ratified at TRI-1 (22/22 ACCEPTED-FIXED in-band before /build-slice); final verdict CLEAN. Code-Critic FINDINGS 0B/1M/3m advisory per CRSI-1 v1 walking-skeleton — M1 `2>/dev/null` cross-spec parity violation between sub-step 2.5 (redirected; canonical from Step 5d sub-step 2) + sub-step 3 (unredirected; pre-existing divergent leg) — design.md §Contracts L74 "MUST use the same 2-step pattern" violated at byte level; m1 substring-leak co-occurrence-not-co-location risk on U-entries test; m2 3-Critic stack N-count off-by-one ("N=10" should be "N=9"); m3 `git rebase --continue` literal not pinned by any structural-pin test. All 4 DEFERRED to slice-074+ `slice-NNN-bundle-073-code-critic-cleanup` per CRSI-1 v1 walking-skeleton + voluntary-restraint precedent **N=14 cumulative** (slice-037/046/050/052/055/056/057/061/065/067/070/071/072/073). **Zero-FALSE-ALARM property holds across all 26 findings this slice** — 16 + 6 + 4 = 26 findings, 26 VALIDATED, 0 false alarms. | **PSQ-3 closes the local-merge coordination surface of the parallel-slice family at N=3 sibling rules** — BRANCH-2 + PSQ-1 + PSQ-2 + PSQ-3 collectively shape multi-session parallel work. **`/critic-calibrate` is genuinely overdue at THREE simultaneous signals** — TPHD-1 sub-mode (a) N=7 cumulative + BC-1 BC-GLOBAL-2 prose-vs-automation N=5 cumulative + AC-count > 5 N=2; strongest slice-074 candidate. **R-20 cp-r tax structural fix is SEVERELY OVERDUE at N=8 cumulative** — codify-cp-r-in-BRANCH-2-SKILL.md (R-20 candidate (a)) — alongside `/critic-calibrate`, the strongest slice-074 candidate. **For slices that mint a new RULE-ID + inherit a canonical form from a sibling site, run `grep -n <canonical-form-literal>` across ALL sibling sites and verify byte-level identity** — slice-073 code-Critic M1 catch (`2>/dev/null` divergence) shows the design-Critic stack structurally cannot reach this; APED-1 scope-extension class N=3 cumulative. Pattern N=2 cumulative on "code-Critic Majors that pivot on coexistence-of-multi-surfaces escape the design-Critic stack" (slice-072 + slice-073). **The "scope-narrowing in design.md but mission-brief not back-propagated" pattern is the canonical TPHD-1 sub-mode (a) trigger** — 14 of 16 first-Critic findings on slice-073 fell in this single class; bottleneck is Builder discipline in the design phase, not Critic acuity. **3-Critic stack N=9 cumulative complementarity stable** — 26 findings, 26 VALIDATED, zero FALSE-ALARMs; design-Critic catches conceptual + ADR identifier + citation hygiene + missing-cases; meta-Critic catches Builder fix-block sweep gaps + sibling-cell-survival regressions + count-vs-enumeration drift; code-Critic catches cross-spec parity at byte level + ordering invariants + structural-pin gaps. **Voluntary-restraint discipline N=14 cumulative** + **Switch-commit-switch-worktree pattern N=4 cumulative**. **Strong follow-up nominations**: slice-074 candidate `/critic-calibrate` (highest priority — three accumulated signals); slice-074 candidate `address-R-20-cp-r-tax-codify-in-BRANCH-2-SKILL` (severely overdue); slice-NNN-bundle-073-code-critic-cleanup (4 advisory findings ready); PSQ-4 (`--push` time rebase — enumerated in ADR-068 §Future flexibility); R-13 OSDG-1 extension to /slice-candidates (open low-band risk). NOT a BCR-1 round-trip (zero `**Closes:** SC-NNN` sentinels — risk-register-driven from slice-067 + slice-072 reflection nominations). |
| 072 | [slice-072-add-psq-2-claim-machinery](slice-072-add-psq-2-claim-machinery/) | 2026-05-27 | Standard | medium (critic-required: true — in-house methodology surfaces touched: `tools/slice_queue_writer.py` + new `tools/slice_queue_claim.py` + `skills/slice/SKILL.md` Step 6.5 + `methodology-changelog.md` + new ADR-067 mints PSQ-2 as new RULE-ID on parallel-slice family axis sibling to PSQ-1; supersedes nothing; first rule on the claim-machinery axis adjacent to PSQ-1 queue-output + BRANCH-2 worktree-isolation; methodology v0.71.0; 5-part PMI-1 atomic bump 0.70.0 → 0.71.0; retires R-19 mitigating → retired) | SHIPPED-WITH-DEFERRALS — validation PASS (6/6 ACs PASS with per-criterion real-environment evidence: AC1 schema additivity + CRLF tolerance + forward-compat via live `--claim` `git diff` + 6 unit tests; AC2 claim CLI + 3-case git-config absence detection + atomic write + `--queue` override + cp1252 bespoke via live CLI + 6 unit tests; AC3 release + force-claim + bare-claim refuse + release typo-rejection via live CLI sequence + 4 unit tests; AC4 /slice Step 6.5 preservation via 2 unit tests; AC5 R-19 retired via `risk_register_audit --filter-status mitigating --json` + 1 unit test; AC6 v0.71.0 + 5-part PMI-1 + paired-pin tests + shippability row #72 via PMI-1 audit + 2 paired-pin tests). VAL-1 0 secrets + 0 imports; SCMD-1 + PTFCD-1 pre-catalog gates clean. 14+ Step-6 audits ALL CLEAN (PMI-1 26/6/30 v0.71.0 + AVFS-1 + MCFS-1 + TVFS-1 + CAD-1 + BRANCH-2 + UTF8-STDOUT-1 30/30 + CRP-1 + PCA-1 + BCI-1 + STP-1 + NAW-1 vacuous + WIRE-1 + RR-1 R-19-not-in-mitigating + INST-1 + SCMD-1 + PTFCD-1 + critique-review + triage + SUP-1). 1 BC-1 BC-GLOBAL-2 Critical defer-with-rationale per N=4 cumulative prose-vs-automation false-positive class (slice-069/070/071/072 — `/critic-calibrate` slice-073+ active nomination). Full pytest 987/987 PASS (was 966; +21 net new = 18 PSQ-2 unit tests + 1 R-19 retirement test + 1 cp1252 bespoke + 2 paired-pin v0.71.0 entry tests); shippability runner 72/72 PASS 0 FAIL. R-20 cp -r tax fired N=7 cumulative with NEW surface at /validate-slice Step 5.5 (was previously /build-slice Phase E only); workaround per slice-070/071 canonical (cp -r from main tree); slice-073+ structural-fix nomination via R-20 candidate (a) OVERDUE. | **3-Critic stack N=9 cumulative — 11 first + 3 meta + 9 code = 23 findings**. First-Critic NEEDS-FIXES 2B/4M/5m all VALIDATED (B1 `_ROOT_ONLY_TOOLS` cp1252 bucket mismatch — slice_queue_claim has no `--root` flag; B2 R-19 + ADR-064 L37 stale `session-id detection` forward-references contradict ADR-067 identity model; M1 Windows CRLF round-trip via TextIOWrapper newline=None default — modelcontextprotocol/python-sdk#2433; M2 `--release` exit-code ambiguity present-unclaimed vs not-in-queue; M3 `parse_queue_text` forward-compat to PSQ-3+ field lines unspecified; M4 AC5 packs 3 ratchets — N=2 promotion of slice-067 AC-count signal). Meta-Critic EXTEND with 3 missed findings ALL Builder-fix-block-introduced regressions (M-add-1 Major design.md L18-19 stale anchor TPHD-1 sub-mode (a) N=6 cumulative intra-design.md self-inconsistency on AC5→AC6 split fix-block; m-add-1 Minor milestone.md L29 prose stale Builder-commitment-not-honored; m-add-2 Minor `parse_queue_text` unclaimed-entry shape underspecified). 14 dispositions ratified at TRI-1 (14 ACCEPTED-FIXED in-band on mission-brief/design/ADR-067/milestone); final verdict CLEAN. Code-Critic FINDINGS 0B/4M/5m advisory per CRSI-1 v1 walking-skeleton (M1 `apply_claim` vs `_format_entry` emission-order divergence — Fowler "Duplicated Code" between two coexisting code surfaces; M2 `write_slice_queue` swallows ClaimUsageError via `except Exception` defeating coordination-safety contract; M3 malformed sibling-entry blocks healthy `apply_release`/`apply_claim` noisy-neighbor; M4 `_atomic_write_text` leaks orphan `.tmp` on `os.replace` failure; m1 case-sensitivity diagnostic ambiguity; m2 duplicate `Claimed-by:` silent last-write-wins; m3 blank-line between `Risk-retired:` and `Claimed-by:` silently drops claim; m4 test fixture XDG_CONFIG_HOME isolation gap; m5 methodology-changelog "17 tests" prose-vs-actual-18). All 9 DEFERRED to slice-073+ `slice-NNN-bundle-072-code-critic-cleanup` per CRSI-1 v1 walking-skeleton voluntary-restraint precedent N=13 cumulative. | **PSQ-2 ships claim machinery as the second rule on the parallel-slice family axis** — BRANCH-2 + PSQ-1 + PSQ-2 layer cleanly into multi-session parallel work; cooperative-not-adversarial threat model per ADR-067 §Adversarial model was the right call. **3-Critic stack N=9 cumulative complementarity validated** — design-Critic + meta-Critic + code-Critic each catch a different defect class structurally unreachable by the others (M1 emission-order divergence between two coexisting code surfaces is the canonical instance). **TPHD-1 sub-mode (a) cross-doc harmonization gap empirically-stable at N=6 cumulative recurrence** (slice-062/064/067/070/071/072) — `/critic-calibrate` proposal overdue: first-Critic should perform stale-anchor sweep across ALL design.md / mission-brief.md / ADR sections after applying AC-restructuring or count-changing fixes. **AC count > 5 N=2 promotion of slice-067 signal confirmed** — new-mechanism mints with paired-pin meta-AC + PMI-1 atomic-bump leg meta-AC genuinely need 6 ACs; carve-out ripe for `/critic-calibrate` proposal. **R-20 cp -r tax N=7 cumulative with 2 surfaces** (/build-slice Phase E + /validate-slice Step 5.5 NEW) — slice-073+ structural-fix nomination via R-20 candidate (a) OVERDUE. **BC-1 BC-GLOBAL-2 prose-vs-automation false-positive N=4 cumulative** (slice-069/070/071/072) — past N=3 promotion threshold; `/critic-calibrate` slice-073+ proposal target ready for action (BC-1 negative-anchor refinement). **Voluntary-restraint discipline N=13 cumulative** (slice-037/046/050/052/055/056/057/061/065/067/070/071/072). **Switch-commit-switch-worktree pattern stable at N=3 cumulative** (slice-070/071/072) — codify in BRANCH-2 SKILL.md slice-073+ candidate. **Strong follow-up nominations**: slice-073+ `slice-NNN-bundle-072-code-critic-cleanup` (9 findings + R-20 + BC-GLOBAL-2 = ~11+ findings); `/critic-calibrate` periodic run (TPHD-1 N=6 + AC-count N=2 + BC-GLOBAL-2 N=4); PSQ-3 (rebase + conflict discipline at /commit-slice — sibling on parallel-slice family axis); R-13 OSDG-1 extension to /slice-candidates (open low-band risk; standing slice-067 nomination). NOT a BCR-1 round-trip (zero `**Closes:** SC-NNN` sentinels — R-19 driven). |
| 071 | [slice-071-bundle-066-to-070-code-critic-cleanup](slice-071-bundle-066-to-070-code-critic-cleanup/) | 2026-05-26 | Standard | medium (critic-required: true — in-house methodology surfaces touched: `tools/slice_queue_writer.py` + `tools/branch_workflow_audit.py` + `tools/supersede_audit.py` + `skills/build-slice/SKILL.md` + `methodology-changelog.md` + `ADR-066`; MEPD-1 EXCLUDE posture verified — NO new `## v0.71.0` entry, NO PMI-1 bump, NO new ADR; retroactive in-place corrections to existing v0.70.0 entry per SUP-1 §Append-only-of-DECISIONS-not-of-FACTS interpretation per /critique B2 ACCEPTED-FIXED; ships at v0.70.0 unchanged; voluntary-restraint discipline N=12 cumulative; closes diagnose-out backlog SC-028 via BCR-1 round-trip dual-tree replication) | SHIPPED-WITH-DEFERRALS — validation PASS (all 5 ACs PASS with per-criterion evidence: AC#1 slice-066 6 disposition rows grep=6; AC#2 slice-067+slice-068 6 disposition rows grep=6; AC#3 slice-069 8 disposition rows grep=8 incl. M2 paired-pin completion; AC#4 slice-070 11 disposition rows grep=11; AC#5 pytest 966/966 + shippability 71/71 PASS + 14 Step-6 audits 22 clean + 2 Important defer-with-rationale + /code-review surfaces 0 new structural Majors). VAL-1 0 secrets + 0 hallucinated imports; SCMD-1 + PTFCD-1 pre-catalog gates clean. 14 Step-6 audits 22 clean (BRANCH-2 + UTF8-STDOUT-1 29/29 + CRP-1 + PCA-1 + BCI-1 + MCFS-1 + STP-1 + AVFS-1 + TVFS-1 + NAW-1 + PMI-1 26/6/29 + INST-1 + WIRE-1 + TF-1+WS-1+ETC-1 vacuous + SUP-1 + RR-1 + DR-1 + TRI-1 + CAD-1 + OSDG-1 11/11) + 2 Important defer-with-rationale (BC-1 BC-GLOBAL-2 prose-vs-automation N=3 cumulative; LINT-MOCK-1 13 Important findings inherited from slice-070). 28 FIX + 1 DOCUMENT-AS-DESIGNED + 1 ALREADY-FIXED-AT-/reflect + 1 honest DEFER-AGAIN (slice-069 m6; relabeled from initial-draft ACKNOWLEDGED-NO-FIX per /critique M2 prose-honesty). 9 new test functions + ~5 modifications of existing tests; mid-slice smoke gate PASS after cp -r diagnose-out/ + graphify-out/ from main tree (N=6 cumulative cp-r tax → promoted to risk-register R-20); BCR-1 round-trip dual-tree replication for SC-028. | **3-Critic stack N=8 cumulative — 12 first + 4 meta + 5 code = 21 findings**. First-Critic BLOCKED 2B/6M/4m all VALIDATED — B1 empirically demonstrated originally-drafted `_PATH_SHAPED_RE` regex was BROKEN (`re._parser.parse` decomposed `[/\\]` as `[/\]` terminating char class early; `unknown` matched as path-shaped; would have inverted slice-070 M5 inside its own M2 fix). Builder fix-block 10 ACCEPTED-FIXED in-band + 2 ACCEPTED-PENDING discharged at /build-slice. Meta-Critic EXTEND with 4 missed findings ALL Builder-fix-block-introduced regressions (M-add-1 M6 sentinel-test docstring/assertion VERBATIM mismatch — RSAD-1 recursive instance; meta-Critic empirically demonstrated 2-of-4 assertions would fail silently; M-add-2 M5 Phase B1/B2/B3 ordering residual; m-add-1 m4 drift-prevention test promise not inscribed; m-add-2 5-site count drift sweep). 16 dispositions ratified at TRI-1; user ratified all 16 Builder drafts as-is. Code-Critic FINDINGS 0B/0M/5m advisory per CRSI-1 v1 walking-skeleton (m1 Step 2 validation symmetry gap; m2 per-node `.resolve()` perf wart; m3 ADR-066:82 still carries `~631 files` partial-coverage of slice-069 m1; m4 fixture near-duplicate ~15 LOC; m5 `main()` `out_arg` un-resolved). **AC#5 "0 new structural Majors" SATISFIED**; all 5 deferred to slice-072+ bundled-cleanup nomination per CRSI-1 v1 walking-skeleton voluntary-restraint precedent N=7 cumulative. **3-Critic stack complementarity at N=3 cumulative on Builder-fix-block-introduced-regression catch** (slice-067 + slice-068 + slice-071). Zero-false-alarm streak holds at N=17 cumulative (slice-046+ window). | **31-finding bundled-cleanup-at-10x-precedent shape is viable** with per-finding disposition table + per-cluster paired build phases + explicit non-deferral closure dispositions + mid-slice smoke pressure-valve. **APED-1 scope extension N=2 cumulative** (slice-070 dotfile blind-spot + slice-071 broken regex) — strongest /critic-calibrate active nomination for slice-072+: empirical execution of any minted regex/pathspec/glob rule against actual production data BEFORE /critique declares done. **R-20 cp-r tax pattern persists post-vault-in-git** (N=6 cumulative; user-flagged "we need a better solution"; 4 candidate fix classes in risk-register R-20); slice-072+ highest-priority structural-fix nominee. **Builder fix-block prose-honesty discipline propagated correctly into slice-071** — /critique M2 caught slice-069 m6 disposition-rename anti-pattern; Builder accepted ACCEPTED-FIXED in-band; honest DEFER-AGAIN = 1. **3-Critic stack N=8 cumulative complementarity-on-CLEAN-dual-Critic-stack** (slice-063 → slice-071); structurally non-collapsible. **Voluntary-restraint discipline at N=12 cumulative** (slice-037/046/050/052/055/056/057/061/065/067/070/071). **N=3 cumulative on BC-1 BC-GLOBAL-2 prose-vs-automation false-positive class** (slice-069 + slice-070 + slice-071) — promotes to /critic-calibrate active nomination. **N=5 cumulative on TPHD-1 sub-mode (a) cross-doc harmonization gap** (slice-062/064/067/070/071 — meta-Critic catches reliably). **Slice-040 N+1 first-governed-slice doctrine extends to N=18 cumulative** (first 31-finding bundled cleanup at 10× precedent scale surfaces R-20 visibility at mid-slice smoke as a new structural axis). **Strong follow-up nominations**: slice-072+ `bundle-071-code-critic-cleanup` OR `address-R-20-cp-r-tax` (highest priority); /critic-calibrate periodic run (APED-1 scope-extension + BC-1 GLOBAL-2 discrimination promotions); PSQ-2 + PSQ-3 (slice-067 standing nominations); WS-1 + ETC-1 R-7-class TFFL-1 extension; SC-006 + SC-007 + SC-008 documented-but-unenforced-gate cluster `implement-or-downgrade-drift-check`. |
| 070 | [slice-070-fix-psq-1-blast-radius-dict-leak](slice-070-fix-psq-1-blast-radius-dict-leak/) | 2026-05-26 | Standard | low (critic-required: true — in-house methodology surface `tools/slice_queue_writer.py` ∈ `tools/**/*.py`; MEPD-1 EXCLUDE posture — NO methodology-changelog entry + NO PMI-1 bump + NO new ADR; ships at v0.70.0 unchanged per voluntary-restraint discipline N=11 cumulative; closes diagnose-out backlog SC-027 via BCR-1 round-trip) | SHIPPED-WITH-DEFERRALS — validation PASS (4/4 ACs with real-environment per-criterion evidence: AC1 `/repro` WRITTEN-FAILING test PASSES post-fix via mock subprocess + mock `_build_id_to_path_map` → id-lookup resolves real graphify shape `{id, label, type:"", path:""}` to `tools/slice_queue_writer.py`; AC2 4-shape coverage 5/5 unit tests PASSING — legacy list-of-strings + drops-non-path-strings + real-shape + dict-with-`nodes`-keyed + forward-compat populated `path`/`source_file`/`name`; AC3 live committed `architecture/slice-queue.md` regenerated post-fix contains only path-shaped tokens per positive-shape regex `^(?:[^\s\`]*[/\\][^\s\`]+\|[^\s\`]*\.[A-Za-z0-9]{1,8}\|\.[A-Za-z][A-Za-z0-9_.-]*)$` — separator OR `.alphanum{1,8}` ext OR leading-dot dotfile; AC4 `**Closes:** SC-027` sentinel triggers BCR-1 round-trip → `- **Addressed:** slice-070-fix-psq-1-blast-radius-dict-leak on 2026-05-26` line appended after Evidence sub-list before SC-028 header in both worktree + main-tree `diagnose-out/backlog.md` copies). 14 Step-6 audits clean (BRANCH-2 worktree-mode on `slice/070-fix-psq-1-blast-radius-dict-leak` branch in `<HOME>/ai_sdlc-wt/slice-070-fix-psq-1-blast-radius-dict-leak` via switch-commit-switch-worktree path + TF-1 strict-pre-finish 7/7 PASSING + UTF8-STDOUT-1 29/29 + CRP-1 + PCA-1 9-skills + BCI-1 + MCFS-1 + STP-1 1-skip-noted ADR-037 syntax fixture + AVFS-1 + TVFS-1 + NAW-1 zero new agents + PMI-1 26/6/29/v0.70.0 + WIRE-1 zero-row); shippability runner **70/70 PASS 0 FAIL**; VAL-1 0 secrets + 0 hallucinated imports via `--imports-allowlist tests`; WS-1 + ETC-1 + multi-instance N/A per slice scope; full pytest suite **950/950 PASS** (was 944 pre-slice-070; +6 new bug tests = expected baseline shift); /drift-check covered. BC-1 surfaces 3 deferred-with-rationale items (BC-GLOBAL-2 known false-positive on prose-discussion-of-git-commands N=2 slice-069+070 cumulative; BC-PROJ-11 scope-mismatch on mission-brief version-literal mentions; LINT-MOCK Important on `_build_id_to_path_map` deliberate test-injection seam mirroring `blast_resolver` pattern from slice-059 TVFS-1 + slice-063 NAW-1). 2 DEVIATIONs logged at build-log Events: AC#3 regex widened mid-/build-slice to accept `.gitignore`-style dotfiles (empirical Builder-discovery — original /critique-time literal blind-spotted `.gitignore` from `rename-architecture-to-sdlc` candidate's hint_files; harmonized across all 4 surfaces in same fix-block per TPHD-1 sub-mode (a) — test file regex + mission-brief AC#3 + design.md TF-1 supplement + shippability.md row #70; contract unchanged); 2 stale test-function-name anchors swept in mission-brief.md L18 (AC#1) + L20 (AC#3) — original /repro placeholder names persisted through /critique fix-block; caught at /build-slice via visual inspection (5th cumulative recurrence of TPHD-1 sub-mode (a) cross-doc harmonization gap; slice-062/064/067/070-meta/070-build). N+1 first-governed-slice catch on BRANCH-2 + vault-in-git intersection: slice-070 is FIRST post-vault-in-git slice whose pre-build pipeline artifacts (mission-brief + design + critique + critique-review + repro test + shippability row + regenerated slice-queue) trip BRANCH-2 prereq rule 4 "STOP if dirty"; user-ratified the switch-commit-switch-worktree pattern (option 1) — now the canonical post-slice-069 sequence. `graphify-out/` + `diagnose-out/` cp-r tax N=5 cumulative post-slice-069 vault-in-git; BCR-1 closure for SC-027 manually replicated in main-tree diagnose-out/backlog.md (worktree edit doesn't propagate via merge — gitignored content stays per-worktree). BCR-1 round-trip: SC-027 closed. | **3-Critic stack 12 first + 3 meta + 11 code = 26 findings + 2 Builder self-catches at /build-slice = N=28 cumulative findings caught + closed**. First-Critic NEEDS-FIXES 2B/6M/4m all VALIDATED per meta-Critic — 11 ACCEPTED-FIXED in same fix-block per TPHD-1 sub-mode (a) (B1 path→name→id precedence APED-1 empirical-execution catch — proposed precedence falls through to opaque IDs since all path values empty + name key absent + only id universally populated; B2 fictional graphify shape in /repro mocks; M1 AC3 weak-proxy regex tightened to positive-shape; M2 R-X2 inverted framing — silent-PRODUCTION not silent-drop; M3 R-X3 collapsed into M1; M4 AC#2 4-shape enumeration added actual current shape; M5 ADR-064 citation reframed to "option 1 durable across sessions rationale + backlog.md SC-027 PSQ-2-blocks framing"; M6 LOC count excised; m1 smoke gate broadened to full module; m2 slice-069 wiring matrix verified; m4 shippability placeholder excised) + 1 OVERRIDDEN with rationale verified by /reflect SKILL.md L59 BCR-1 prose (m3 BCR-1 multi-mention FALSE-ALARM — confirmed not a mechanical defect; multi-mention does NOT double-trigger). Meta-Critic EXTEND with 3 missed findings (M-add-1 design.md L66 stale `path → name → id` precedence in Decisions section + M-add-2 mission-brief.md L51 must-not-defer #3 stale precedence + M-add-3 TPHD-1 sub-mode (a) N=4 cumulative recurrence flag — slice-062/064/067/070); all 3 ACCEPTED-FIXED in /critique-review fix-block; post-sweep grep returns 0 stale-precedence matches. TRI-1 user-ratified CLEAN: 14 ACCEPTED-FIXED + 1 OVERRIDDEN; mechanical Final verdict CLEAN per /critique Step 4.5 vocabulary. Code-Critic FINDINGS 0B/6M/5m advisory per CRSI-1 v1 walking-skeleton — all 11 DEFERRED to slice-071+ `bundle-066-to-070-code-critic-cleanup` candidate (30-finding cumulative backlog across slice-066/067/068/069/070 crosses "explicit /slice scope decision" threshold). Notable code-Critic catches: M1 worktree-absolute-path latent silent-corruption surface (overlap-detection-correctness bounded today by single-source-graphify-out; theoretical under multi-graph scenarios); M2 `_is_path_shaped`-vs-AC#3-regex contract drift on dotfiles + uppercase + non-canonical extensions; M3 PRIMARY id-lookup unfiltered defense-in-depth gap; M4 `functools.lru_cache` shared-by-reference mutable-return foot-gun; M5 `\`unknown\`` cell breaks AC#3 in routine degraded states (slice-067 AC4 documents this); M6 `subprocess.run` patching scope too broad. Code-Critic m3 build-log honesty correction (Builder originally framed abs/rel-path duplication as "not introduced by this slice" — corrected at /reflect since `_build_id_to_path_map` IS slice-070 code). **3-Critic stack value-validation extends to N=7 cumulative** (slice-063 → slice-070 inclusive). | **The bundle-cleanup slice nomination at slice-071 is structurally overdue** — 30 code-Critic advisory findings accumulated across slice-066:6 + slice-067:1 + slice-068:4 + slice-069:8 + slice-070:11; voluntary-restraint discipline holds N=11 cumulative but the cleanup slice itself is now LARGE; recommend explicit /slice scope-check (possibly split into code-quality refactors vs spec-prose corrections); slice-067 + slice-069 + slice-070 reflections all nominate as next candidate. **For any slice that mints or modifies a parse-rule / regex / glob / pathspec, perform empirical execution against actual production data BEFORE declaring /critique done** — extends APED-1's audit-parse-rule discipline to regex/glob/pathspec rules more broadly; slice-070 AC#3 dotfile blind-spot is /critic-calibrate signal N=1; promote APED-1 scope extension if 2nd recurrence. **TPHD-1 sub-mode (a) cross-doc harmonization gap is N=5 cumulative** (slice-062/064/067/070-meta-Critic/070-build-time) — first-Critic fix-block sweep keeps missing peripheral prose anchors; meta-Critic catches reliably; pattern is structural; warrants /critic-calibrate proposal — instruct first-Critic to perform stale-anchor sweep AFTER drafting fix-block dispositions BEFORE finalizing critique. **Switch-commit-switch-worktree pattern for vault-in-git pre-build artifacts is now the canonical post-slice-069 sequence** — BRANCH-2 SKILL.md should codify it; future post-vault-in-git slices will re-encounter the same prereq trip-up otherwise: /slice → /critique-review creates tracked artifacts on master tree BEFORE /build-slice creates the worktree, tripping BRANCH-2 prereq rule 4. **`graphify-out/` + `diagnose-out/` cp-r tax stable N=5 cumulative recurrence** post-slice-069 vault-in-git; un-gitignoring both per ADR-066 precedent is now structurally justifiable (slice's BCR-1 round-trip Addressed line had to be manually replicated in main tree's diagnose-out/ because the worktree's edit doesn't propagate via merge — audit-grade smell). **Code-Critic stack continues highest-signal Critic surface for slices touching `tools/*.py` code** — N=7 cumulative complementarity proof (slice-063→070); do NOT collapse 3-Critic stack. **Builder fix-block prose-honesty discipline**: credit defect provenance accurately when deferring; "not a defect this slice introduced" was wrong framing when the slice ships the introducing code. **Voluntary-restraint discipline carries forward N=11 cumulative** (slice-037/046/050/052/055/056/057/061/065/070; BC-1 promotion at /reflect Step 5b skipped per user choice). |
| 069 | [slice-069-track-vault-in-git](slice-069-track-vault-in-git/) | 2026-05-25 | Standard | medium (critic-required: true — in-house methodology surfaces: `.gitignore` line-11 removal + line-10 comment rewrite; new `architecture/decisions/ADR-066-track-vault-in-git.md` philosophy ADR with NEW `## Reversibility taxonomy` section replacing fabricated ADR-019 citation; BC-PROJ-8 rule-content supersession in lockstep on live + canonical fixture; STP-1 Sub-form B docstring rationale reframed; `skills/code-review/SKILL.md` M5 INCLUDE direction with `:(glob)` pathspec-magic fix discovered mid-/code-review; PII redaction sweep scoped to `architecture/**`; 5-part PMI-1 atomic bump 0.69.0→0.70.0 + post-bump MCFS-1 + TVFS-1 forward-syncs; massive 644-file content commit of `architecture/` vault; N=4 cumulative gitignored-vault-vs-worktree class STRUCTURALLY RETIRED via empirical Phase H4 throwaway-worktree verification) | SHIPPED-WITH-DEFERRALS — validation PASS (5/5 ACs with real-environment per-criterion evidence: AC1 `git check-ignore -v architecture/risk-register.md` silent unignored + `graphify-out/graph.json` + `diagnose-out/backlog.md` still ignored at `.gitignore:18` + `:19`; AC2 ADR-066 frontmatter `id: ADR-066` + `reversibility: expensive` + `status: accepted` + `supersedes: null` + 6 H2 sections incl. NEW Reversibility taxonomy; AC3 CLAUDE.md no-op grep returns 0 stale-term matches + .gitignore line-10 comment revised; AC4 WIP commit `9230029` lands 644 files / 65504 insertions / 38 deletions + VAL-1 scan 0 hits across 10 patterns + PII residuals 0/0/0 in architecture/ with verbatim corpus preserved per ADR-030; AC5 pytest 944/944 + BCI-1 + SCMD-1 + R-15 + BC-PROJ-8 structural-identity + /code-review skill-drift pins all PASS). N=4 EMPIRICAL retirement verification at Phase H4 (`git worktree add --detach ../slice-069-verify HEAD` propagated 634 files in `architecture/`; risk-register.md present; worktree cleanly removed). 14 Step 6 audits clean (BRANCH-2 + TF-1 default-off + UTF8-STDOUT-1 29/29 + CRP-1 + PCA-1 9-skills + BCI-1 + MCFS-1 + STP-1 1-skip-noted ADR-037 syntax fixture + AVFS-1 + TVFS-1 + NAW-1 zero new agents + PMI-1 26-skills/6-agents/29-tools/v0.70.0 + WIRE-1 zero-row + CAD-1); shippability runner **68/68 PASS 0 FAIL** in 1m19.979s; VAL-1 0 secrets + 0 hallucinated imports via `--imports-allowlist tests`; WS-1 + ETC-1 + multi-instance N/A per slice scope; full pytest suite **944/944 PASS** in 30.43s, zero regressions; /drift-check covered via 14 audits. BC-1 surfaces 2 deferred-with-rationale escalations (BC-GLOBAL-1 Important + BC-GLOBAL-2 Critical) on prose-keyword false-positive class (ADR §Reversibility prose mentioning git revert/reset/checkout — no slice code introduces the actual defect class); /critic-calibrate watch-list N=1 candidate for BC-1 keyword-trigger model refinement. NOT a BCR-1 round-trip (zero `**Closes:** SC-NNN` sentinels — user-intent + risk-register driven from /query-design 2026-05-25, not backlog-driven). | **Three-Critic stack 16 first + 4 meta + 8 code = 28 findings + 0 Builder self-catches at /build-slice = N=28 cumulative findings caught + closed**. First-Critic NEEDS-FIXES 6B/6M/4m all VALIDATED per meta-Critic → 9 ACCEPTED-FIXED in-band + 7 ACCEPTED-PENDING for /build-slice scope expansion (B1 secrets-grep ineffectiveness → VAL-1 canonical wrapper; B2 PII redaction sweep needed scoped to architecture/** with verbatim corpus preserved; B3 ADR-028 §Options-#1 partial scope-supersession encoded via prose §Supersession scope block + frontmatter supersedes: null; B4 BC-PROJ-8 evergreen rule false post-slice-069 → in-lockstep live + fixture + test pin revision; B5 STP-1 Sub-form B docstring stale rationale → origin → preference reframing; B6 co-tracking failure modes unanalyzed → 3 modes enumerated; M1 fabricated ADR-019 reversibility taxonomy citation → NEW ADR-066 § Reversibility taxonomy section promoted inline; M2 mid-slice smoke Bash tool annotation; M3 >1000 vs 631 expected count → loose target with empirical 634; M4 throwaway-worktree N=4 verification → Phase H4 EMPIRICALLY RETIRED; M5 code-review SKILL.md sweep direction → user-chosen INCLUDE at /build-slice; M6 operational scaling pre-merge dry-run → Phase H2 644 files / 65504 insertions; m1-m4 frontmatter title + AC4 verification format + Option 4 cons reorder + /critic-calibrate probe narrowing). Meta-Critic EXTEND with **3 missed findings** ALL ACCEPTED-FIXED in-band (M-add-1 third co-tracking failure mode `git rm`; M-add-2 secrets-scanning trade-off VAL-1 9 vs gitleaks 150+; M-add-3 PII sweep blast-radius scope-pin to architecture/** only) + **1 severity-note** (M4 throwaway-worktree path swap from off-canonical /tmp/... to BRANCH-2 canonical sibling-dir) + **1 direction-unverified note** (M5 INCLUDE vs keep-exclusion re-opened at /build-slice) + **B4 reservation** (revised BC-PROJ-8 text drafted in build-log Events FIRST before pin re-alignment lands). 20 dispositions ratified at TRI-1. **Code-Critic FINDINGS (CRSI-1 v1 advisory)** — 0B / 2M / 6m all DEFERRED to slice-070+ bundled cleanup per CRSI-1 v1 walking-skeleton + slice-064/065/066/067/068 N=5 cumulative voluntary-restraint precedent: M1 `_SECRET_PATTERNS` count drift 9 vs 10 across methodology-changelog v0.70.0 + ADR-066 + mission-brief AC4 (slice-050 BC-PROJ-9 inventory-count class N=2 cumulative); M2 MEPD-1 INCLUDE posture declared but no `test_v_0_70_0_*` paired-pin tests + no shippability row 69 at /code-review time (BC-PROJ-10 paired-pin discipline gap; RSAD-1 self-application gap); m1-m6 (vault file-count drift 631/633/634; PII-redaction count divergence 93/113/141; methodology-changelog v0.70.0 first-paragraph 600-word blob; .gitignore comment seam; build-log H4 `--detach HEAD` pattern uncodified; M5 INCLUDE enlarges BC-1 false-positive surface). Shippability row #69 added in /reflect Step 5.3 (M2 partial-discharge); paired-pin tests deferred. **3-Critic stack value-validation extends to N=6 cumulative** (slice-063 → slice-069 inclusive). Bundled-cleanup backlog accumulating: slice-066 (6) + slice-067 (1) + slice-068 (4) + slice-069 (8) = **19-finding slice-070+ active nomination**. | **N=4 cumulative gitignored-vault-vs-worktree class STRUCTURALLY RETIRED** via ADR-066 vault-in-git philosophy (empirical Phase H4 throwaway-worktree proof: 634 files propagated automatically, no `cp -r` DEVIATION). Cross-machine parallel slice development enabled; methodology-as-history visible for forks. **Bash-quote silent backslash-stripping in Python regex character classes embedded in heredocs** (Phase C silent failure): bash heredocs strip one backslash from `\\` sequences via JSON-decode + bash-tokenize pipeline; `r"[\\/]"` regex compiles to `[\/]` (character class with `/` only, NOT `\` or `/`). Fix via `chr(92)*2` (literal 2-backslash sequence) construction. /critic-calibrate watch-list N=1. **`git worktree add <branch>` collision when branch is already checked out** (Phase H4 mid-stream fix): fix via `--detach HEAD` pattern; not codified in `tools/branch_workflow_audit.py` docstring — slice-070+ bundle nomination. **M5 INCLUDE `:(exclude)architecture/*.md` pathspec over-match** caught mid-/code-review when slice's own diff scope didn't include the build-log.md M5 was supposed to admit (classic slice-040 N+1 first-governed-slice catch — mechanism-specific bugs reachable only by empirical execution of just-minted mechanism). Fix `:(glob,exclude)architecture/*.md` (glob magic adds FNM_PATHNAME restricting `*` to single segment). **BC-1 keyword-trigger false-positive on ADR §Reversibility prose**: BC-GLOBAL-2 fires on documentation of revert paths (`git revert`, `git reset --hard`, `git filter-repo`) where rule intent is automation-code; rule scope mismatch deferred-with-rationale. /critic-calibrate watch-list N=1; M5 INCLUDE further enlarges the false-positive surface via build-log §Discovered prose. **Count claims in design/ADR/mission-brief prose** need design-Critic hand-verification against production source code (`_SECRET_PATTERNS` 9-vs-10 + slice-050 BC-PROJ-9 N=2 cumulative — propose /critique dimension at N=3). **MEPD-1 INCLUDE posture triggers BC-PROJ-10 paired-pin obligation** that BC-PROJ-10's keyword-match classifier doesn't structurally verify (RSAD-1 self-application gap); /critic-calibrate signal N=1. **3-Critic stack value-validation extends to N=6 cumulative** (slice-063 → slice-069); structural-differentiation pattern (design-stack on conceptual + ADR identifiers + citation hygiene; meta-stack on Builder fix-block sweep + empirical audit re-execution; code-stack on cross-document numerical consistency + self-application discipline + mechanic-specific bugs) empirically stable. **/critic-calibrate probe narrowing** (Must-not-defer #11): gitignored-vault probe scope narrows from `architecture/` (now tracked) to `diagnose-out/` + `graphify-out/` (still gitignored). **Slice-070 unblocked**: rename `architecture/` → `.sdlc/` is now a 1-line `_DEFAULT` flip in `tools/_vault_paths.py` + `.gitignore` edit + 73-file prose sweep (slice-068 VAULT_ROOT seam + slice-069 tracking flip combined make rename tractable). **Slice-070+ bundled cleanup** is the strongest standing nomination (19-finding backlog from slice-066/067/068/069 code-Critic advisory findings + slice-069 paired-pin tests + count-claim sweep). **Strong follow-up nominations**: slice-070 bundled cleanup (highest priority); slice-070+ codify `--detach HEAD` worktree-verification pattern; CRSI-1 v2 (TRI-1 + verdict-driven block + AI-bloat passes); WS-1 + ETC-1 R-7-class TFFL-1 extension (N=2 cumulative carryforward); /critic-calibrate proposals for count-claim hand-verification + MEPD-1 INCLUDE empirical-scan-of-prior-INCLUDE-artifacts + BC-1 keyword-trigger negative-anchor filtering. |
| 068 | [slice-068-add-vault-root-constant](slice-068-add-vault-root-constant/) | 2026-05-25 | Standard | medium (critic-required: true — in-house methodology surfaces: 8 `tools/*.py` audit-tool modules + new `tools/_vault_paths.py` leaf utility + new `tests/methodology/test_vault_root_constant.py` + new ADR-065 mints env-var-overridable vault-root constant architectural pattern; MEPD-1 EXCLUDE posture — NO methodology-changelog v0.70.0 entry + NO PMI-1 bump; ships at v0.69.0 unchanged) | SHIPPED-WITH-DEFERRALS — validation PASS (4/4 ACs with real-environment per-criterion evidence: AC1 `tools/_vault_paths.py` exports VAULT_ROOT + env override via subprocess test; AC2 8-site migration + two-marker convention per `test_no_orphan_architecture_literal_in_migrated_tools` + `test_shippability_decoupling_audit_tuples_preserve_literal`; AC3 full pytest 944/0 zero-regressions + idempotency pin; AC4 10 unit tests + `_MIGRATION_SITE_ALLOWLIST` 8-element frozenset pinned + `test_consumer_constants_are_frozen_at_first_import` production-correctness freeze contract). 14 Step 6 audits clean (TF-1 strict-pre-finish 10/10 PASSING + BRANCH-2 worktree-mode on `slice/068-add-vault-root-constant` branch in `<HOME>/ai_sdlc-wt/slice-068-add-vault-root-constant` + UTF8-STDOUT-1 29/29 + CRP-1 + PCA-1 9-skills + BCI-1 + MCFS-1 + STP-1 + AVFS-1 + TVFS-1 + NAW-1 silent + WIRE-1 + PMI-1 26-skills/6-agents/29-tools/v0.69.0 + CAD-1); shippability runner **68/68 PASS 0 FAIL** post-row-add from worktree (main-tree run fails until /commit-slice merge per expected transient state — slice/068 branch code not yet on master); VAL-1 0 secrets + 0 hallucinated imports via `--imports-allowlist tests`; WS-1 + ETC-1 + multi-instance N/A per slice scope; full pytest suite **944/944 PASS** in 29.95s, zero regressions; 1 DEVIATION logged at build-log.md (cp -r `architecture/`, `diagnose-out/`, `graphify-out/` from main tree to worktree for full-pytest collection — N=4 cumulative gitignored-vault-vs-worktree conflict pattern; structural fix is slice-069); 2 BC-1 global rules surfaced as informational (BC-GLOBAL-2 git-revert + BC-GLOBAL-3 identifier-rename) — neither applies to slice-068 (no git checkout/restore/stash usage; no identifier rename — constant introduction + 8 import additions only); 2 SC entries filed to `diagnose-out/backlog.md` per BCR-1 (SC-027 slice-067 PSQ-1 raw-dict-leak medium/medium per `/critique-review` severity adjustment; SC-028 slice-068 `test_vault_root_constant.py` test-quality hardening bundle — 4 `/code-review` advisory findings bundled into one follow-on slice candidate). | `/critique` 1B + 3M + 2m all ACCEPTED-FIXED in `## Triage` table (B1 9th-site `tools/build_checks_integrity.py:78` missed-by-rev-1-grep; M1 consumer-freeze cascade hazard pinned by new TF-1 row 10; M2 false `conftest.py L37` precedent citation removed; M3 R-15 audit regex backstop preserved via tests/ migration scope-back to follow-on slice; m1 argparse default-eval freeze folded into M1; m2 SC-NNN backlog round-trip → SC-027 filed). `/critique-review` EXTEND (all 6 first-Critic findings CONFIRMED + 0 SUSPICIOUS + 1 missed M-add-1 two-marker convention asymmetry between design.md TF-1 row 4 and mission-brief AC2/must-not-defer #2/Verification-plan row 2 ACCEPTED-FIXED with second fix-block sweep across 3 sibling-cell sites + 1 m2 severity adjustment low/small → medium/medium per multi-consumer-artifact rule). `/code-review` (CRSI-1 v1 advisory; no Blockers) 1M + 4m: M1 `test_vault_paths_module_is_leaf` regression-pin missing despite design.md L25 + mission-brief.md L54 explicit spec promise + m1 `test_migration_is_idempotent` regex under-coverage + m2 `test_consumer_constants_are_frozen_at_first_import` assertion gap + m3 two-marker convention regex coverage gap + m4 PEP-8 blank line cosmetic — all 5 bundled to SC-028 for follow-on test-quality hardening slice. **Three-Critic stack complementarity validated at N=2 cumulative** (slice-067 N=1 + slice-068 N=2): design-Critic catches conceptual+citation+missing-cases; meta-Critic catches Builder-fix-block-sweep gaps + sibling-cell-survival regressions; code-Critic catches test-quality+regex-coverage+spec-vs-code drift+cosmetic. 4 `/critic-calibrate` proposals queued. | **N=4 cumulative gitignored-vault-vs-worktree conflict** (slice-067 N=3 → slice-068 N=4): BRANCH-2 worktree-per-slice + gitignored `architecture/`/`diagnose-out/`/`graphify-out/` is now a recurring class with structural cost — every BRANCH-2 slice pays the `cp -r` tax + risk of forgetting; slice-069 `rename-architecture-to-sdlc-and-track-in-git` is the structural fix and should be accelerated. **Spec-prose-claims-a-test-that-isn't-in-TF-1-plan** fresh class surfaced via `/code-review` M1 (neither design-Critic nor meta-Critic caught design.md L25 + mission-brief L54 promising `test_vault_paths_module_is_leaf` not enumerated in TF-1 plan; only code-Critic caught it via direct AST inspection) — /critic-calibrate target: design-Critic dimension addition. **Slice-067 PSQ-1 raw-dict-leak** in `tools/slice_queue_writer.py` blast-radius renderer (filed as SC-027, medium/medium per `/critique-review` severity adjustment + multi-consumer-artifact rule). |
| 067 | [slice-067-add-parallel-slice-queue-output](slice-067-add-parallel-slice-queue-output/) | 2026-05-25 | Standard | medium (critic-required: true — in-house methodology surfaces: `skills/slice/SKILL.md` Step 6.5 inserted at L378-379 + Pipeline-position block extended; new `tools/slice_queue_writer.py` ~430 LOC + new ADR-064 + methodology-changelog v0.69.0 + 5-part PMI-1 0.68.0→0.69.0; BC-PROJ-9 5-inventory fan-out across plugin.yaml + install_audit + test_utf8_stdout + INSTALL.md ×2 + shippability row #67) | SHIPPED-WITH-DEFERRALS — validation PASS (6/6 ACs with real-environment per-criterion evidence: AC1 `/slice` Step 6.5 writes `architecture/slice-queue.md` per Phase E mid-slice smoke; AC2 5 required fields per entry verified via grep 15=5×3; AC3 4-way classification + precedence verified across 2 unit tests + real smoke; AC4 4 edge-case fixtures all PASS — zero candidates / zero active slices / missing graph / empty hint_files collision; AC5 idempotent overwrite + provenance timestamp verified; AC6 BC-PROJ-10 paired-pin pair `test_v_0_69_0_psq_1_entry_present_in_repo` + `test_v_0_69_0_psq_1_shippability_consumer_propagation` both PASS). 14 Step 6 audits clean (TF-1 strict-pre-finish 13/13 PASSING + BRANCH-1 + UTF8-STDOUT-1 29/29 + CRP-1 + PCA-1 + BCI-1 + MCFS-1 + STP-1 + AVFS-1 + TVFS-1 + NAW-1 silent + WIRE-1 after rationale-token self-catch fix + PMI-1 26-skills/6-agents/29-tools/v0.69.0 + OSDG-1 drift); shippability runner **67/67 PASS 0 FAIL** with new row #67 citing PSQ-1 + ADR-064 + both BC-PROJ-10 paired-pin test functions per BCR-1 traceability axis; VAL-1 0 secrets + 0 hallucinated imports via `--imports-allowlist tests`; WS-1 + ETC-1 + multi-instance N/A per slice scope; full pytest suite **934/934 PASS** in 35.86s, zero regressions; /drift-check CLEAN. **DEVIATION-1 WORKTREE=skip**: slice-040 N+1 first-governed-slice catch on BRANCH-2's structural conflict with gitignored `architecture/` (line 13 of .gitignore — `git worktree add` creates worktree without slice vault files); user-ratified per SOAD-1; BRANCH-1 fallback path per ADR-063 §Scope of supersession 4th-surface inheritance; slice-068+ active nomination to formally reconcile. NOT a BCR-1 round-trip (zero `**Closes:** SC-NNN` sentinels — risk-register-driven, slice-066 reflection nominated slice-067 explicitly). | **Three-Critic stack 8 design + 4 meta + 1 code = 13 findings + 2 Builder self-catches = N=15 cumulative findings caught + closed**. First-Critic NEEDS-FIXES 2B/3M/3m: B1 BC-PROJ-10 paired-pin discipline gap (wiring-matrix test naming + missing sibling test + zero TF-1 entry-pin rows — textbook N≥17 BC-PROJ-10:173 recurrence); B2 `compute_parallel_safety()` enum drift + zero-active-AND-empty-hint collision rule unspecified; M1 BCR-1 round-trip non-trigger declaration prose-only — **Builder OVERRIDDEN** with rationale corrected per /critique-review SEVERITY-WRONG-ON-RATIONALE (BCR-1 trigger discipline enforced by /reflect's own runtime logic, NOT by `test_bcr_1_backlog_round_trip.py`'s 8 prose-presence anchors); M2 mid-slice smoke bootstrap-impossible — defense-in-depth dual-option fix; M3 SKILL.md insertion position unspecified — pinned to L378-379; m1 ADR-064 enum sibling-drift auto-closed via B2; m2 atomic-write fsync gap → OUT-OF-SCOPE note; m3 R-19 hedge dropped. Meta-Critic EXTEND with **4 missed findings ALL Builder-fix-block-introduced regressions** (M-add-1 AC6 violates ≤5-AC rule — documented deviation rationale added; M-add-2 Pre-finish gate "5 ACs" not updated to 6 — N=4 cumulative TPHD-1 sub-mode (a) residual-site survival; M-add-3 design.md L21 "dogfood seed" residual after M2 fix — N=5 cumulative; M-add-4 ADR-064 L50 5-part PMI-1 leg enumeration drift `venv ai-sdlc-tools` substituted for `## v0.69.0 header` — N=4 cumulative cross-doc enumeration drift; first-Critic claimed "5-part PMI-1 leg enumeration consistent" — false-clean). 12 dispositions ratified at TRI-1 (11 ACCEPTED-FIXED + 1 OVERRIDDEN); final verdict CLEAN. **Zero-false-alarm streak broken at N=15** — slice-067 M1 OVERRIDDEN is the second FALSE-ALARM in post-slice-046 window (slice-064 m3 N=1 + slice-067 M1 N=2 — both "Critic demanded structural-test evidence for a no-op-self-evident class where cost-benefit favors deferring to existing runtime infrastructure"); pattern signal — if N=3 emerges, `/critic-calibrate` proposal target. Plus 2 Builder self-catches at /build-slice (Phase B test 3-digit slice-NNN padding 9/10→10/10 PASS; Phase F WIRE-1 `rationale:` literal token in 2 exemption cells) — well below the ~10/slice mean on codification slices. Code-Critic FINDINGS 0B/0M/1m advisory per CRSI-1 v1 — m1 `main()` custom-output branch DRY duplication (Fowler over-engineering; ~38 lines structurally-identical to `write_slice_queue()` body); declined in-band per slice-064/065/066 precedent → **slice-068+ bundled cleanup nomination** combined with slice-066's 6 advisories = **7-finding backlog**. **CRSI-1 walking-skeleton v1 validated at N=5 cumulative** (slice-063 + 064 + 065 + 066 + 067). **Meta-Critic specialization signal stable at N=3 cumulative on BRANCH-2 first-governed-slice N+1 axis** (slice-062 + slice-064 + slice-067). **Slice-040 N+1 first-governed-slice doctrine extends to N=16 cumulative on BRANCH-2 surface**. | **PSQ-1 minted as first rule on parallel-slice family axis** (slice-067; ADR-064 mints a new rule, supersedes nothing; adjacent to BRANCH family — BRANCH-2 makes parallel work physically possible, PSQ-1 makes it discoverable; PSQ-2 / PSQ-3 nominated slice-068 / slice-069). `/slice` Step 6.5 (NEW) writes `architecture/slice-queue.md` with top-10 parallel-safe candidates + graphify-derived blast-radius + 4-value `Parallel-safety` enum (`NON-OVERLAPPING` \| `OVERLAPS-WITH-slice-NNN[, slice-MMM]` \| `UNKNOWN-NO-HINT-FILES` \| `UNKNOWN-NO-GRAPH`) + precedence rule (UNKNOWN-NO-GRAPH > UNKNOWN-NO-HINT-FILES > OVERLAPS-WITH-* > NON-OVERLAPPING). New helper module `tools/slice_queue_writer.py` (~430 LOC; 5-function library API + CLI + injection seam `blast_resolver` per slice-059/063 precedent); atomicity via `.tmp` + `os.replace()`; crash-durability via `os.fsync()` explicitly OUT OF SCOPE. 5-part PMI-1 atomic bump 0.68.0 → 0.69.0; TVFS-1 + MCFS-1 + AVFS-1 + OSDG-1 forward-syncs clean. **R-19 stale-queue-file-misleads-session added as mitigating-low-band** (design-time-anticipated; mitigation: provenance line + idempotent overwrite + slice-068's PSQ-2 claim machinery planned structural close). **Voluntary-restraint discipline N=9 cumulative carries forward** — user-explicit-pick ordering at /slice ("Parallel-slice queue first") deferred slice-066's 6 code-Critic advisories + slice-067's 1 advisory to slice-068+ bundled cleanup (7-finding combined backlog). **Bootstrap (slice-067 only)**: slice-067's own `/slice` invocation did NOT write `architecture/slice-queue.md` — helper does not exist at /slice time (bootstrap-reference instance #1 per slice-066 WORKTREE=skip-bootstrap precedent); first write at /build-slice Phase E mid-slice smoke. **Graphify CLI node-ID discovery**: bare basename `--file <basename>` works; `--from=<full-path>` + `--file <full-path>` both fail with "node not found"; `_call_graphify_blast_radius` handles via basename-first + full-path-fallback retry loop. **Strong next-slice candidates**: slice-068 PSQ-2 claim machinery (the user's nominated 4-slice family next-up — likely bundled with BRANCH-2 + gitignored architecture/ reconciliation); slice-068+ bundled cleanup of 7-finding backlog; slice-069 PSQ-3 rebase + conflict discipline (depends on PSQ-2); WS-1 + ETC-1 audit R-7-class TFFL-1 extension (carried forward from slice-066 reflection — /critic-calibrate proposal target). |
| 066 | [slice-066-add-worktree-per-slice-discipline](slice-066-add-worktree-per-slice-discipline/) | 2026-05-25 | Standard | medium (critic-required: true — in-house methodology surfaces: `skills/build-slice/SKILL.md` Branch state sub-section rewrite + `skills/commit-slice/SKILL.md` Step 5b/5d worktree-teardown extension + `tools/branch_workflow_audit.py` 4 new violation kinds + new ADR-063 + methodology-changelog v0.68.0 + CLAUDE.md L32 rewrite + 5-part PMI-1 0.67.0→0.68.0) | SHIPPED — validation PASS (5/5 ACs with real-environment per-criterion evidence — AC1 build-slice prereq invokes git worktree add at sibling-dir path; AC2 commit-slice --merge + --sync-after-pr tear worktree down with idempotent guard before branch-delete order-load-bearing; AC3 audit module gains 4 new violation kinds + 5 helpers + WORKTREE=skip regex + 3-shape call enumeration + Path.resolve(strict=False) + samefile() + normcase(realpath) fallback for Windows path equivalence per microsoft/vscode#101244; AC4 ADR-063 mints BRANCH-2 supersedes ADR-019 sub-mode (a) only — N=2 application of slice-022 partial-supersession encoding + methodology v0.68.0 + CLAUDE.md L32 rewrite preserves Branch-per-slice historical anchor + 5-part PMI-1 atomic bump; AC5 R-17 mitigating → retired). 14/14 Step 6 audits clean (PMI-1 + TF-1 21/21 PASSING + BRANCH-1 + UTF8-STDOUT-1 + MCFS-1 + AVFS-1 + TVFS-1 + NAW-1 + CRP-1 + PCA-1 + BCI-1 + STP-1 + OSDG-1 build-slice + OSDG-1 commit-slice + CAD-1); shippability runner **66/66 PASS 0 FAIL** with new row #66 citing BRANCH-2 + ADR-063 + R-17 per BCR-1 traceability axis; VAL-1 0 secrets + 0 hallucinated imports; WS-1 5/5 EXERCISED; full pytest suite **869/869 PASS** in 30.95s, zero regressions; /drift-check CLEAN. NOT a BCR-1 round-trip (zero `**Closes:** SC-NNN` sentinels — risk-register-driven, R-17 retired). | **Three-Critic stack 14 first + 6 meta + 6 code = 26 findings + 10 Builder self-catches = N=30 cumulative findings caught + closed** on this codification slice. First-Critic NEEDS-FIXES 5B/5M/4m — most consequential B1 ADR identifier conflation (BRANCH-1 = ADR-019 not ADR-021 — would have shipped ADR-063 superseding the wrong ADR; load-bearing design-level catch); B2 Windows path-comparison semantics; B3 audit call-shape 3-enumeration; B4 cross-spec parity TF-1 row gap; B5 bootstrap discharge via idempotent guard. Meta-Critic EXTEND 6 missed: M-add-1 Major residual ADR-021 at mission-brief.md L152 escaped B1 global rename (classic FBCD-1 sub-mode (a) recursive-self-application N=10); M-add-2 TF-1 count drift design.md L179/L190 introduced by B4 fix; M-add-3 shell-portability awk/grep parenthetical; M-add-4 mode-interaction matrix missing PR-pending row; M-add-5 APED-1 explicit bootstrap-variant fixture; M-add-6 BRANCH=skip 4th-surface enumeration in ADR-063 §Scope of supersession. 18 ACCEPTED-FIXED + 2 ACCEPTED-PENDING (m4 graphify CLI watch-list + M-add-5 Builder discretion); TRI-1 user-ratified verdict NEEDS-FIXES. Plus 10 Builder self-catches during /build-slice Phase A+B+C+D (PMI-1 enum × 3 + TF-1 path × 2 + BC-PROJ-10 paired-pin × 1 + AC2 section-scoping × 2 + cp1252 fixture × 1 + PowerShell Write-Host pipeline × 1 + CLAUDE.md historical anchor × 1) — N=11 self-catches; slice-022 codification-commits-the-class law extends to density ≈N=11 per slice (new HWM; was N=14 at slice-064). Code-Critic FINDINGS 0B/2M/4m all advisory per CRSI-1 v1 — M1 `_is_repo_root_a_worktree` shallow-gitdir walk-off; M2 SKILL.md `wt_base` from `$(pwd)` divergent with audit's `.git`-ancestor walk; m1 asymmetric AuditResult; m2 `test_honours_canonical_worktree_skip_rationale_line` not load-bearing (APED-1 catch); m3 inline `import os`; m4 PowerShell-portability parenthetical. All 6 declined in-band per slice-064/065 precedent → **slice-067+ bundled cleanup nomination** (`slice-NNN-bundle-066-code-critic-cleanup`; voluntary-restraint N≥10 cumulative). **CRSI-1 walking-skeleton v1 validated at N=4 cumulative** (slice-063 + 064 + 065 + 066 — code-Critic finds genuine novel findings on already-CLEAN dual-Critic stack). Zero false-alarms streak EXTENDED on codification slices N=17 cumulative (slice-046/048/050/051/052/053/054/055/056/057/058/061/062/063/064/065/066). | **R-17 retired structurally** via candidate-fix-(b) worktree-per-slice; candidate-fix-(a) clean-tree audit DECLINED as redundant defense-in-depth per ADR-063 §"Options considered" option 3 — option (b) makes the WIP-contamination class structurally impossible. **WS-1 audit has R-7 silent-default-off class bug — TFFL-1 fix not propagated** (witnessed mid-slice — mission-brief frontmatter `**Walking-skeleton**: true  <!-- comment -->` causes WS-1's anchored regex to break; reports "not enabled" → silent default-off; same class TF-1 had until slice-034 TFFL-1 fix). N=2 cumulative if ETC-1 confirmed (untested). **Strongest slice-067+ `/critic-calibrate` proposal target**: apply slice-034 TFFL-1 pattern verbatim (regex relaxation + value-agnostic field-present malformed branch) to BOTH WS-1 and ETC-1 audits. Slice-067/068/069 sibling nominees (parallel-slice queue + claim state machine + rebase discipline) now physically possible (filesystem-isolated worktrees unblock parallel work). |
| 065 | [slice-065-bundle-064-code-critic-cleanup](slice-065-bundle-064-code-critic-cleanup/) | 2026-05-23 | Standard | medium (critic-required: true — in-house methodology surface: `skills/code-review/SKILL.md` modified at L42 STOP-guard insertion; Standard-mode cross-cutting tooling default) | SHIPPED — validation PASS (5/5 ACs with real-environment per-criterion evidence — AC#1 substring-leak retired via bash_block scoping + full-filter-shape literal `'git diff "$base"...HEAD --name-only --diff-filter=ACMR'`; AC#2 semantic invariant `bash_block.count(filter) == bash_block.count(git_diff)` empirically 2==2; AC#3 SKILL.md L42 `[ -z "$default" ] && { echo "default-branch-unresolvable: …" >&2; exit 2; }` mirroring NAW-1 ADR-061 §Decision exit-2 contract; AC#4 STOP-behavior test pin via 3 composable assertions (count + brace-group + co-location with -1 short-circuit safety); AC#5 OSDG-1 drift clean + shippability runner 65/65 PASS + /code-review self-dogfood; VAL-1 both layers clean 0 secrets / 0 hallucinated imports via `--imports-allowlist tests`; WS-1 / ETC-1 N/A default-off; TF-1 strict-pre-finish 6/6 PASSING; 20+ Step-6 audits clean incl. BRANCH-1 / UTF8-STDOUT-1 / CRP-1 / PCA-1 / BCI-1 / MCFS-1 / STP-1 / AVFS-1 / TVFS-1 / NAW-1 (silent — zero agents/*.md added) / WIRE-1 / PMI-1 / INST-1 / CAD-1 / mini-CAD / BC-1 / Mock-budget; SCMD-1 + PTFCD-1 pre-gates clean (65 rows / 343 test-path tokens / 579 cited fns); shippability_runner **65/65 PASS 0 FAIL** with new row #65; full pytest **899/899 PASS in 27.13s** (slice-064 baseline 898 + 1 net new test_skill_md_step_1_default_branch_resolver_stops_on_empty); /drift-check CLEAN; NOT a BCR-1 round-trip — advisory-driven, zero `**Closes:** SC-NNN` sentinels) | **Three-Critic stack 9 design + 4 code-Critic findings — 13/13 VALIDATED at correct severity, ZERO false-alarms**. First-Critic BLOCKED 2B/3M/3m all VALIDATED (B1 AC#2 substring-leak — `step_1.count('git diff "$base"')` = 3 not 2, textbook RSAD-1 slice-040 N+1 doctrine catch on slice-064 section-scoped-substring-assertion pattern; B2 AC#4 substring-leak — `'[ -z "$default" ]' in step_1` + `'default-branch-unresolvable' in step_1` both already TRUE pre-fix via L41 fallback + L68 prose; M1 backwards "between L42 and L41" within-design phrasing FBCD-1 sub-mode (a); M2 mission-brief "non-zero exit" vs design.md hardcoded "exit 2" granularity drift; M3 weak Inclusion-heuristic class-precedent slice-061 R-16 INSTALL.md prose-correctness too weak; m1 verification §3 inline-bash POSIX-parity check vs prose-as-executable-contract mismatch; m2 `'exit 2'` loose substring; m3 ADR-061 §Decision L60-67 coordinate-only pin) all 8 ACCEPTED-FIXED in same fix block per TPHD-1 sub-mode (a) / FBCD-1 cross-surface harmonization + Builder self-catch (slice-057 N+1 precedent) of Phase A2/A3 stale literals pre-meta-Critic. Meta-Critic EXTEND with 1 missed Minor (m-add-1 shippability row #65 Description residual FBCD-1 cross-cell drift — third-pass catch of same RSAD-1 class) ACCEPTED-FIXED at /critique-review fix block. TF-1 audit empirically re-verified clean post-fix (6 rows; AC#3+AC#4 shared-function + AC#5 multi-row-per-AC per slice-056/062 lesson). TRI-1 user-ratified CLEAN (9/9 ACCEPTED-FIXED via SOAD-1 "Ratify all 9" structured-options choice). **Code-Critic FINDINGS (CRSI-1 v1 advisory)** — 4 advisories all declined in-band per slice-063/064 precedent (N=3 cumulative) + slice-066+ bundled cleanup nomination: m1 SKILL.md L37 sibling coordinate-pin `[[ADR-061]] §Decision L60-67 pattern` survives (third-pass FBCD-1 cross-cell drift catch — design-Critic m3 fixed 2 design.md cells, meta-Critic m-add-1 fixed shippability row, code-Critic m1 catches third sibling at SKILL.md L37 — same RSAD-1 class); m2 3× DRY duplication of canonical bash_block scoping idiom (voluntary-restraint at N=3 carrying cost acceptable; helper extraction at N≥4); m3 SKILL.md L69 "BRANCH-1-shaped error" vs bash `default-branch-unresolvable:` parity drift (informal narrative); m4 design.md "byte-for-byte translation" overclaim per empirical Python-bash message-body diff (class-level equivalent is correct framing; out-of-scope per `:(exclude)architecture/**` pathspec). **CRSI-1 walking-skeleton v1 validated at N=3 cumulative** (slice-063 N=1 + slice-064 N=2 + slice-065 N=3). **Slice-040 N+1 first-governed-slice doctrine extends to N=15 cumulative + 3-tranche structure stabilized** — slice-064 introduced section-scoped-substring-assertion pattern; slice-065 first governed slice; 3-pass Critic stack caught 3-tranche residual drift (design-Critic on drafts + meta-Critic on cross-cell + code-Critic on third sibling-cell). **First-Critic vs meta-Critic vs code-Critic specialization signal stabilizes at N=3 cumulative** (slice-063 + slice-064 + slice-065 — promote to stable Critic-stack design observation if N=4 confirms at slice-066+). **Voluntary-restraint discipline N=9 cumulative** (slice-037/046/050/052/055/056/057/061/065). **Missed by 3-pass Critic-stack (N=1 watch-list)**: PTFFD-1 phantom function name `test_run_catalog_returns_zero_on_all_pass` in mission-brief TF-1 row #5 (Builder typo; actual `test_main_returns_0_on_all_pass` from slice-055) — caught at /build-slice TPHD-1 sub-mode (c) pre-flight gate; canonical division-of-labor (function-existence is STRUCTURAL gate, NOT Critic-prompt dimension). NO BC-1 promotion per slice-037 audit-vs-real-artifact law. | **Bundled cleanup of slice-064 code-Critic 3 advisories** (slice-065; mints NO new rule + NO new ADR + NO methodology-changelog entry + NO VERSION bump per voluntary-restraint discipline — slice-064 ADR-062 "extends-without-minting" in-family extension precedent is the load-bearing authority; Inclusion-heuristic disposition explicit per BC-PROJ-10). Closes slice-064 reflection's "slice-065+ bundled cleanup nomination": (a) M1 substring-leak retirement on `test_skill_md_step_1_diff_resolution_uses_union_of_three_sources` Source-(iii) assertion via canonical bash_block scoping idiom + full-filter-shape literal; (b) m1 default-branch resolver no-programmatic-STOP fix at `skills/code-review/SKILL.md` L41-L42 (inserted `[ -z "$default" ] && { echo "default-branch-unresolvable: …" >&2; exit 2; }` mirroring NAW-1's ADR-061 §Decision exit-2 contract at the class-level — exit code + stderr stream + kebab class identifier shared; rule-ID prefix + remediation hint intentionally diverge per code-Critic m4 calibration); (c) m2 semantic-invariant tightening on `test_skill_md_step_1_all_three_legs_share_filter_shape` count assertion (`assert diff_with_filter == bash_block.count('git diff "$base"')` — admits no 3-leg / 2-filtered regression). New structural pin `test_skill_md_step_1_default_branch_resolver_stops_on_empty` (AC#4 — three composable bash_block-scoped assertions: count + brace-group + co-location). 4 code-Critic advisories declined in-band per CRSI-1 v1 walking-skeleton + slice-063/064 precedent (N=3 cumulative — m1 SKILL.md L37 sibling coordinate-pin + m2 3× DRY duplication + m3 L69 prose drift + m4 design.md overclaim) → slice-066+ bundled cleanup nomination. **The canonical bash_block scoping idiom emerges as a stable pattern** (3× duplication; helper extraction at N≥4). **Slice-040 N+1 doctrine 3-tranche structure stabilizes** (design-Critic + meta-Critic + code-Critic each catch a different tranche of canonical drift at first-governed-slice). **Voluntary-restraint discipline at N=9 cumulative operates uniformly across MEPD-1 + CRSI-1 v1 advisory dispositions** — both shapes resist mid-slice scope expansion + nominate next-slice bundled cleanup. **TPHD-1 sub-mode (c) Builder self-catch at /build-slice prerequisite remains the canonical defense layer for PTFFD-1 phantom function names** — function-existence verification is a STRUCTURAL gate (TPHD-1 sub-mode (c)), NOT a Critic responsibility. 24 SC candidates remain open in `diagnose-out/backlog.md` post-slice-065 (unchanged). **Strong next-slice candidates**: slice-066 bundle-065-code-critic-cleanup (4 code-Critic advisories — parallel of slice-064→065 chain; m1 SKILL.md L37 coordinate-pin + m2 DRY helper extraction + m3 L69 prose drift + m4 design.md overclaim); /code-review v2 enhancements (TRI-1 routing + verdict-driven block + AI-bloat passes — standing slice-060→065 chain nomination); R-17 BRANCH-1 clean-tree precondition fix; SC-007 /drift-check enforcement (HIGH severity backlog SC); R-13 OSDG-1 extension to /slice-candidates. |
| 064 | [slice-064-fix-code-review-diff-resolution-falsifier](slice-064-fix-code-review-diff-resolution-falsifier/) | 2026-05-23 | Standard | medium (critic-required: true — in-house methodology surface: `skills/code-review/SKILL.md` + `methodology-changelog.md` v0.67.0 + entry-pin pair tests; Standard-mode cross-cutting tooling default) | SHIPPED — validation PASS (5/5 ACs with real-environment per-criterion evidence — multi-row-per-AC plan PASSING=7 across 5 ACs; slice-064 dogfooded its own fix at /code-review Step 1 — Source (i) WT-vs-base caught 7 in-scope files, Source (iii) commits-vs-base alone EMPTY confirming B1 falsifier class retirement; VAL-1 both layers clean 0 secrets / 0 hallucinated imports; WS-1 / ETC-1 N/A default-off; TF-1 strict-pre-finish 7/7 PASSING; 17 Step-6 audits clean incl. BRANCH-1 / UTF8-STDOUT-1 / CRP-1 / PCA-1 / BCI-1 / MCFS-1 / STP-1 / AVFS-1 / TVFS-1 / NAW-1 / WIRE-1 / PMI-1 / INST-1 / CAD-1 / PVFS-1 / BC-1 / Mock-budget / triage_audit / critique_review_audit; SCMD-1 + PTFCD-1 pre-gates clean (64 rows / 339 test-path tokens / 575 cited fns); shippability_runner **64/64 PASS 0 FAIL** with new row #64; full pytest **898/898 PASS** in 29.28s (slice-063 baseline 892 + 6 net new); /drift-check CLEAN; NOT a BCR-1 round-trip — risk-class-driven, zero `**Closes:** SC-NNN` sentinels) | **Three-Critic stack 14 design + 3 code-Critic findings — 13/14 design VALIDATED + 1 OVERRIDDEN reality-confirmed FALSE-ALARM (m3 R-7 footgun "in case it propagates" hedge — first FALSE-ALARM on codification slices in slice-046+ N=15 cumulative window — calibration signal: first-Critics should DECLINE to author findings under empirically-tolerated forward-hedges); 3/3 code-Critic VALIDATED**. First-Critic BLOCKED 2B/4M/5m (B1 AC#4 phantom test path + phantom function PTFCD-1+PTFFD-1 double — empirically verified via `tools.test_first_audit --strict-pre-finish`; B2 design.md L17 entry-pin pair enumerated but mission-brief AC#5 only entry-pin half — slice-060 M-add-2 / N≥17 BC-PROJ-10 recurrence; M1 bash-array → POSIX inline pathspecs; M2 no runtime-union prose-pin — Claude's aggregation obligation SKILL.md-binding not ADR-only; M3 wide-slice per-file diff token cost watch-list R-X3 N=1; M4 BC-PROJ-9 5-inventory enumeration drift; m1 EPGD-1 anchor count+lineage anchor; m2 disjunctive placeholder; m3 R-7 footgun OVERRIDDEN-VALIDATED FALSE-ALARM; m4 N-count drift; m5 SOLE-forward-sync misleading) 10 ACCEPTED-FIXED + 1 OVERRIDDEN. Meta-Critic EXTEND 3 missed (M-add-1 Major TPHD-1 sub-mode (a) cross-file harmonization for B1 fix INCOMPLETE — 3 residual stale-path sites — **slice-022 RSAD-1 self-violation pattern N=3 cumulative on dual-Critic-stack same-fix-block edits**; M-add-2 Minor N=8 vs N=9 anchor-count drift ADR-062 vs design.md — standardized to 8 anchors via compound lineage clause per slice-063 precedent; M-add-3 Minor design.md L18 EPGD-1 enumeration omits rule-name-expansion anchor class per slice-063 anchor (d) precedent) all 3 ACCEPTED-FIXED at /critique-review fix block. TF-1 audit empirically re-verified clean post-fix (7 rows; AC#1 + AC#5 multi-row-per-AC per slice-056+062 lesson). TRI-1 user-ratified CLEAN (14/14 dispositions via SOAD-1). **Code-Critic FINDINGS (CRSI-1 v1 advisory)** — 3 advisories all declined in-band per slice-063 precedent + slice-065+ bundled cleanup nomination: M1 Major Source-(iii) substring-leak in BFRD-1 repro test; m1 Minor default-branch resolver no-programmatic-STOP at SKILL.md L40-42 (clean-room CI corner case); m2 Minor `>= 2` filter-shape count assertion forward-looking gap. **CRSI-1 walking-skeleton v1 value proposition validated at N=2 cumulative** (slice-063 N=1 + slice-064 N=2). **Slice-040 N+1 first-governed-slice doctrine extends to N=14 cumulative** — slice-063 minted v0.66.0 entry-pin precedent (8-anchor list with compound lineage clause); slice-064 first governed slice; meta-Critic caught canonical N+1 anchor-count + rule-name-expansion drift on v0.67.0 entry-pin. **First-Critic vs meta-Critic specialization signal N=3 cumulative watch-list** (slice-062 + slice-063 + slice-064). | **B1 falsifier class N=2 cumulative retirement complete + extend NAW-1 union-of-three-sources read mechanism to /code-review SKILL.md** (slice-064; ADR-062 mints no new rule, supersedes nothing — sibling-surface application of NAW-1's pattern; methodology v0.67.0; 5-part PMI-1 atomic bump 0.66.0 → 0.67.0; Inclusion-heuristic firing on scope-extension precedent N=8 cumulative inclusive). `/code-review` SKILL.md Step 1 rewritten: single-command `git diff "$base"...HEAD` (commit-vs-commit only — silently writes `Result: NO-CODE-CHANGES` at /build-slice Step 6 where slice work is uncommitted in WT per PCA-1 HARD-STOP) → three-command union-of-three-sources block (WT-vs-base + ls-files-others + commits-vs-base) + post-bash union-instruction prose "Union the three outputs by path; deduplicate." (M2 critique fix); inline literal pathspecs on each leg (POSIX-portable; M1 critique fix). Step 2 prompt-template aligned to per-file `git diff "$base" -- <file>` form (WT-aware) instead of stale aggregate. Closes B1 falsifier class on /code-review SKILL.md surface (slice-063 retired NAW-1 audit-tool surface; slice-064 retires /code-review SKILL.md surface — N=2 cumulative both known carriers). 5-part PMI-1 atomic bump 0.66.0 → 0.67.0 (VERSION + plugin.yaml + pyproject.toml + ## v0.67.0 header + installed ai-sdlc-VERSION) + post-bump forward-syncs (MCFS-1 methodology-changelog + TVFS-1 venv ai-sdlc-tools via `$PY -m pip install --upgrade .`). BC-PROJ-9 fan-out does NOT fire (no new tools/skills/agents; the only new inventory is shippability row #64 which the BCR-1 traceability axis cites BOTH ADR-062 AND NAW-1 AND ADR-061). **MEPD-1 Inclusion-heuristic firing on scope-extension precedent extends to N=8 cumulative** (slice-049/050/051/057/058/059/062 + slice-064). **Slice-022 RSAD-1 self-violation pattern N=3 cumulative on dual-Critic-stack same-fix-block edits** (slice-056 + slice-062 + slice-064): meta-Critic IS the structural backstop by design. **Three-Critic stack delivers value across the full review depth**: first-Critic prose drift + cross-doc citation hygiene + APED-1 empirical-audit-execution; meta-Critic cross-document mechanical consistency + same-fix-block-edit-introduced regressions; code-Critic line-level test-tightness + runtime-correctness. Zero design deviations; zero deferrals at /build-slice. 23 SC candidates remain open in `diagnose-out/backlog.md` post-slice-064 (unchanged). **Strong next-slice candidates**: slice-065+ bundled code-Critic cleanup (3 findings: M1 substring-leak + m1 default-branch resolver no-STOP + m2 `>= 2` count); /code-review v2 (TRI-1 + verdict-block + AI-bloat passes — finally unblocked by slice-064 B1 fix); R-17 BRANCH-1 clean-tree precondition fix; R-13 OSDG-1 extension to /slice-candidates. |
| 063 | [slice-063-add-build-slice-new-agent-warning](slice-063-add-build-slice-new-agent-warning/) | 2026-05-23 | Standard | medium (critic-required: true — in-house methodology surfaces: `skills/build-slice/SKILL.md` + new `tools/new_agent_warning_audit.py` + `methodology-changelog.md` v0.66.0 + risk-register flip; Standard-mode cross-cutting tooling default per slice-007/008/009/etc N=10/10 voluntary-Critic precedent) | SHIPPED — validation PASS (5/5 ACs with real-environment per-criterion evidence — `pytest tests/methodology/test_new_agent_warning_audit.py` 7/7 PASS + `pytest tests/methodology/test_build_slice_skill.py::test_build_slice_step_6_invokes_new_agent_warning_audit` PASS + `pytest tests/methodology/test_risk_register_audit_real_file.py::test_r_18_retired_post_slice_063` PASS + live `$PY -m tools.new_agent_warning_audit --json` against slice-063's own diff → `{status: "clean", exit_code: 0, warnings: [], divergences: []}` vacuous-pass + RR-1 audit confirms R-18 not in open / in retired; VAL-1 both layers clean 0 secrets / 0 hallucinated imports via `--imports-allowlist tests`; WS-1 / ETC-1 N/A default-off; TF-1 strict-pre-finish 9/9 PASSING across 5 ACs; 17 Step-6 audits clean incl. BRANCH-1 / UTF8-STDOUT-1 / CRP-1 / PCA-1 / BCI-1 / MCFS-1 / STP-1 / AVFS-1 / TVFS-1 / **NAW-1 self-application** / WIRE-1 / PMI-1 / INST-1 / CAD-1 / PVFS-1 (5-part PMI-1 leg) / BC-1 / Mock-budget; SCMD-1 + PTFCD-1 pre-gates clean (63 rows / 332 test-path tokens); shippability_runner 63/63 PASS 0 FAIL with new row #63 (slice-063's own row 5/5 PASS self-application); full methodology+skills+agents suite **892/892 PASS in 28.44s**; /drift-check CLEAN; NOT a BCR-1 round-trip — risk-register-driven, zero `**Closes:** SC-NNN` sentinels) | **Three-Critic stack 13/13 design + 1/1 code-Critic minor — all VALIDATED, ZERO false-alarms**. First-Critic NEEDS-FIXES 1B/4M/5m all VALIDATED (B1 read-mechanism falsifier `git diff <base>...HEAD` is commit-vs-commit only — web-confirmed via git-scm.com docs + execution-confirmed empty output on current slice/062 branch; M1 phantom test-file `test_risk_register.py` PTFCD-1; M2 `--strict-pre-finish` flag drift mission-brief vs design.md FBCD-1 sub-mode (a); M3 BRANCH-1 "fallback `main`" misattribution against `tools/branch_workflow_audit.py:127-146` implementation; M4 AC#2 test housed in wrong file Mini-CAD-1 drift vs structural-anchor pin) all ACCEPTED-FIXED at /critique fix block. Meta-Critic EXTEND with 3 missed (M-add-1 Major 5-part PMI-1 leg enumeration drift design.md L17 vs ADR-061 L85 — same defect class as slice-060 B2 + slice-062 B1 cross-doc enumeration drift recurrence; M-add-2 Minor EPGD-1 anchor list incomplete missing `## v0.66.0` header + `Rule reference` META-1-mandatory enforcing-assertion anchors; M-add-3 Minor design.md L218 Step 6 audit list wrongly included `plugin_manifest`) all ACCEPTED-FIXED at /critique-review fix block. **Code-Critic FINDINGS — 1 minor only** per CRSI-1 v1 walking-skeleton advisory-only (m1 dead-code/UX-wart in `tools/new_agent_warning_audit.py:177-178` second `try/except FileNotFoundError` unreachable + missing-git UX wart — declined in-band fix per v1 contract; slice-064+ candidate). TRI-1 user-ratified CLEAN (13/13 ACCEPTED-FIXED via SOAD-1 "Accept all 13" structured-options choice). **Zero-false-alarm streak N=14 cumulative on codification slices** (046/048/050/051/052/053/054/055/056/057/058/061/062/063). **CRSI-1 walking-skeleton v1 value proposition validated at N=1**: code-Critic third-pass adds genuine novel observations the dual-Critic stack structurally cannot reach (design-stack reviews mission-brief + design.md + ADR prose; code-stack reviews the just-written code diff). **/code-review SKILL.md v1 diff-resolution falsifier surfaced** (N=2 cumulative on B1 falsifier class — meta-irony: the slice fixing the falsifier on NAW-1's surface immediately surfaced the SAME defect on /code-review's own surface; SOAD-1-augmented WT-aware diff resolution held for slice-063; slice-064+ candidate to mirror NAW-1's union-of-three-sources fix to /code-review). **R-18 RUNTIME non-determinism observed**: slice-061 + slice-062 hit AGENT-UNSPAWNABLE at /code-review; slice-063's /code-review spawned cleanly — methodology-discoverability is the load-bearing axis, NOT runtime-determinism. NAW-1 retirement structurally sound regardless. **First-Critic vs meta-Critic emerging specialization signal extends to N=2 cumulative** (slice-062 + slice-063): first Critic on prose drift + cross-doc citation hygiene; meta-Critic on cross-document enumeration drift + empirical audit re-execution. | **NAW-1 minted; R-18 retired** (slice-063; ADR-061 mints a new rule, supersedes nothing; the first audit-enforced gate on the **discovery-gate** axis adjacent to but NOT extending the PMI-1 / PVFS-1 / AVFS-1 / MCFS-1 / TVFS-1 forward-sync family). New standalone tool `tools/new_agent_warning_audit.py` (369 LOC; binary exit contract 0/2 by construction — NEVER exit 1; union-of-three-sources read mechanism per ADR-061 §Decision L60-67 closes B1 falsifier class; two injection seams `default_branch_resolver` + `added_files_resolver` per slice-059 TVFS-1 seam precedent) wired at `/build-slice` Step 6 as the last warn-only gate after TVFS-1; emits non-blocking WARN naming the new agent path + session-restart-before-next-slice recommendation + R-18 cross-reference. 1-point wiring (NO `/reflect` Step 5b-naw counterpart — NAW-1 is a discovery gate, not forward-sync). R-18 retirement preserves slice-061 + slice-062 prior Mitigation prose verbatim per slice-040 R-10 retirement-precedent (BC-PROJ-6). 5-part PMI-1 atomic bump 0.65.0 → 0.66.0 (VERSION + plugin.yaml.version + pyproject.toml [project].version + ## v0.66.0 header + installed ~/.claude/ai-sdlc-VERSION); plus separate post-bump forward-sync obligations (NOT PMI-1 atomic-bump legs per M-add-1 fix): installed `~/.claude/methodology-changelog.md` (MCFS-1) + installed venv `ai-sdlc-tools` 0.66.0 (TVFS-1). BC-PROJ-9 5-inventory fan-out: plugin.yaml + install_audit._CANONICAL_TOOLS + test_utf8_stdout._ROOT_ONLY_TOOLS + INSTALL.md L22 + L166 `27 → 28` + shippability row #63. **MEPD-1 Inclusion-heuristic firing on new-mechanism slice extends to N=6 cumulative on the new-RULE-ID precedent** (slice-049/050/054/059/060/063). **PTFCD-1 `(extended)` Test-path annotation recurrence is N=2 cumulative** (slice-054 + slice-063): TF-1 strict-pre-finish caught it; corrected in-band per slice-054 lesson by removing `(extended)` suffix from AC#2 + AC#5 Test-path cells. User-declined BC-1 promotion at N=2 (recommended) keeps watch-list discipline; promote to BC-PROJ-NNN if N=3 emerges. 23 SC candidates remain open in `diagnose-out/backlog.md` post-slice-063 (unchanged — slice closed no SC; not a BCR-1 round-trip). **Strong next-slice candidates**: slice-064 /code-review SKILL.md v1 diff-resolution falsifier fix (mirror NAW-1's union-of-three-sources fix; ACTIVE nomination); slice-064 /code-review v2 (TRI-1 + verdict-block + AI-bloat passes — slice-060 L37 + slice-062 L38 + slice-063 mission-brief Out-of-scope nomination, finally unblocked now that R-18 is retired); R-17 BRANCH-1 clean-tree precondition fix (separate pipeline-hygiene track); code-Critic m1 dead-code cleanup (could bundle with /code-review SKILL.md falsifier fix); R-13 OSDG-1 extension to `/slice-candidates` (slice-052 standing nomination still pending). |
| 062 | [slice-062-extend-r15-corpus-class-closure-scope](slice-062-extend-r15-corpus-class-closure-scope/) | 2026-05-23 | Standard | medium (critic-required: true — in-house methodology surfaces: `tests/methodology/test_resolve_slice_dir.py` extends R-15 corpus class-closure backstop; `tests/methodology/test_methodology_changelog.py` adds entry-pin tests; `tests/skills/code_review/test_code_review_skill.py` repointed; methodology-changelog.md + 5-part PMI-1 bump; Standard-mode cross-cutting tooling default per slice-007/008/009/etc N=9/9 voluntary-Critic precedent) | SHIPPED-WITH-DEFERRALS — validation PASS (4/4 ACs with real-environment per-criterion evidence — pytest 9/9 on test_resolve_slice_dir.py + pytest 1/1 on repointed code-review test + grep-verified `_resolve_slice_dir` import at L31 + call site at L99 + build-log Events timestamped WRITTEN-FAILING → PASSING transition + risk_register_audit R-15 status retired unchanged + grep-found slice-062 scope-extension paragraph in R-15 block + STP-1 Sub-form B clean; VAL-1 both layers clean 0 secrets / 0 hallucinated imports via `--imports-allowlist tests`; WS-1 / ETC-1 N/A default-off; TF-1 strict-pre-finish 8/8 PASSING; 18+ Step-6 audits clean incl. BRANCH-1 / UTF8-STDOUT-1 / CRP-1 / PCA-1 / BCI-1 / MCFS-1 / STP-1 / AVFS-1 / TVFS-1 / WIRE-1 / PMI-1 / INST-1 / CAD-1 / PVFS-1 (5-part PMI-1 leg); SCMD-1 + PTFCD-1 pre-gates clean (62 rows / 327 test-path tokens); shippability_runner 62/62 PASS with new row #62 (slice-062's own row 7/7 PASS self-application); full methodology+skills+agents suite 880 PASS; /drift-check CLEAN; NOT a BCR-1 round-trip — risk-register-driven, zero `**Closes:** SC-NNN` sentinels) | **Dual-Critic 6/6 first-Critic + 3/3 meta-Critic missed all VALIDATED, ZERO false-alarms** (B1 PVFS-1/5-part PMI-1 enumeration gap — direct re-introduction of slice-060 B2 defect class would FAIL `test_repro_sc001_pyproject_project_version_matches_version_file` at Step 6; M1 slice-060 reflection L37 TRI-1 nomination silently re-deferred — Wiegers requirements-design traceability; m1 fabricated lessons-learned citation in ADR-060 §Decision — Cockburn evidence-traceability grep-verified by meta-Critic; m2 EPGD-1 SECTION-header structural-separation reminder; m3 TPHD-1 sub-mode (a) mission-brief 4-row vs design.md 7-row TF-1 plan harmonization; m4 R-15 historical-prose framing for slice-057 paragraph at risk-register.md:266; M-add-1 BLOCKER — the m3 ACCEPTED-FIXED fix-block edit ITSELF introduced TF-1 audit violations via multi-AC labels `"1, 3"` + `"(audit-trace)"` markers that `tools/test_first_audit.py:_normalize_ac_label` rejects → 2 `ac-without-row` violations on AC#3 + AC#4; **N=2 slice-040 N+1 doctrine recurrence on TF-1-multi-AC-label sub-class** [slice-056 + slice-062] + **N=2 slice-022 RSAD-1 self-violation pattern extending to dual-Critic stack same-fix-block edits**; meta-Critic empirically ran `tools.test_first_audit --strict-pre-finish` and reproduced violations; M-add-2 cross-document option-enumeration incoherence ADR-060 1-4 vs design.md B/A vs mission-brief (a)/(b)/(c) — chosen ADR option 3 absent from mission-brief enumeration; M-add-3 design.md Phase C step 11 "4-part Inclusion-heuristic structure" novel terminology stale-carry adjacent to step 12 correctly-stated 5-part PMI-1 anchor) → all 9 ACCEPTED-FIXED → TRI-1 user-ratified CLEAN. **Zero-false-alarm streak N=13 cumulative on codification slices** (046/048/050/051/052/053/054/055/056/057/058/061/062). **N+1 doctrine held at N=12 cumulative on slice-022 RSAD-1 self-violation axis**; meta-Critic IS the structural backstop for empirical-audit-execution-on-fix-block-edits class. **Missed by Critic: /code-review AGENT-UNSPAWNABLE R-18 recurrence N=2** — caught at /code-review SKILL Step 2's `agent-unspawnable` error class (the gate fired correctly; build-time-reachable, slice-037 law → not a Critic-prompt promotion candidate). **First-Critic vs meta-Critic emerging specialization signal (N=1 watch-list)**: first Critic 5/6 prose-coherence + 1 substantive technical (B1); meta-Critic 1 empirical-audit Blocker (M-add-1) + 2 prose-coherence (M-add-2, M-add-3) — empirical-audit-execution is structurally reachable only by running the audit, the first-Critic's table-shape check is too weak; meta-Critic IS the structural backstop by design for this class. | **R-15 corpus class-closure backstop scope extension** (slice-062; ADR-060 mints no new rule, supersedes nothing; extends slice-056/057 R-15 backstop scope to wider test corpora; naming-class peer of ADR-053 OSDG-1-member-addition shape; methodology v0.65.0 entry per Inclusion-heuristic extends-not-mints precedent N=6 cumulative). slice-056 backstop extended via shared helper + 3 per-corpus tests + 1 aggregated whitelist-integrity test to cover `tests/methodology/**/*.py` + `tests/skills/**/*.py` + `tests/agents/**/*.py`. slice-061 N=1 watch-list scope-gap closed: `tests/skills/code_review/test_code_review_skill.py:17` archive-fragile literal repointed via lazy `_resolve_slice_dir(60)` inside test function. R-15 STAYS `retired` (no flip; STP-1 Sub-form B clean). 5-part PMI-1 atomic bump 0.64.0→0.65.0 covers VERSION + plugin.yaml + pyproject.toml + ## v0.65.0 header + installed ~/.claude/ai-sdlc-VERSION + installed methodology-changelog.md MCFS-1 + venv ai-sdlc-tools TVFS-1 (`pip install --upgrade .`). **R-18 N=2 cumulative recurrence on slice-060 N+1 governed-slice axis** (predicted by slice-061 reflection L82; empirically confirmed; user-ratified deferral per slice-061 precedent via SOAD-1 structured options; slice-063+ active R-18 mitigation nomination — cheapest fix per slice-061 reflection: methodology-side warning at /build-slice post-build on new-agent diff). **N=2 slice-022 RSAD-1 self-violation pattern extending to dual-Critic stack same-fix-block edits** (slice-056 + slice-062 /critique-review M-add-1); meta-Critic IS the structural backstop by design for the empirical-audit-execution class. **Builder mid-design self-correction** (N=2 cumulative with slice-061 BFRD-1 confirm-then-auto-invoke precedent): mission-brief frontmatter L5 "Risk retired" + AC#4 originally mis-framed R-15 as still-mitigating; Builder corrected mid-/design-slice without firing /critique. **MEPD-1 Inclusion-heuristic firing on scope-extension precedent (N=6 cumulative)**: slice-049/050/051/057/058/059/062 confirm drift-guard family scope-extension warrants Inclusion-heuristic firing even when no new RULE-ID is minted; voluntary-restraint discipline (N=8 cumulative on retirement-discharge slices 037/046/050/052/055/056/057/061) RESERVED for retirement-discharge / part-(b) closure shapes, NOT for scope-extension shapes. 23 SC candidates remain open in `diagnose-out/backlog.md` (unchanged — slice closed no SC; not a BCR-1 round-trip). **Strong next-slice candidates**: slice-063 R-18 mitigation (active nomination, N=2 cumulative); slice-063 /code-review v2 enhancements (AI-bloat passes + TRI-1 + verdict-block — slice-060 reflection L37 explicit by-ID nomination acknowledged-and-deferred at slice-062 per M1 fix); R-17 BRANCH-1 clean-tree precondition fix (slice-062-or-later); the user-raised multi-session/parallel-slice/worktree-per-slice work (backlog; future-slice-sequence candidate; slice-062 /slice Step 3 menu option #2 "design parallel-slice exec model" remains the recommended entry point). |
| 061 | [slice-061-fix-install-python-detection-and-prompt-fallback](slice-061-fix-install-python-detection-and-prompt-fallback/) | 2026-05-23 | Standard | high (critic-required: true — in-house methodology surface: INSTALL.md is the INST-1 install recipe; high tier mandates re-critique on any design change) | SHIPPED-WITH-DEFERRALS — validation PASS (5/5 ACs with real-environment per-criterion evidence — pytest assertions against the real INSTALL.md prose post-edit + slice-045 4-pin regression-guard PASS; VAL-1 both layers clean 0 secrets / 0 hallucinated imports; WS-1 / ETC-1 N/A default-off; TF-1 strict-pre-finish 5/5 PASSING; 14 Step-6 audits clean incl. BRANCH-1 / UTF8-STDOUT-1 / CRP-1 / PCA-1 / BCI-1 / MCFS-1 / STP-1 / AVFS-1 / TVFS-1 / WIRE-1 / PMI-1 / INST-1; SCMD-1 + PTFCD-1 pre-gates clean (61 rows / 320 test-path tokens); shippability_runner 5/6 PASS with 1 user-approved deferral on pre-existing slice-060 R-15-class row #60 — NOT a slice-061 regression; full methodology+skills+agents suite 874 PASS + 1 deselected; /drift-check CLEAN; NOT a BCR-1 round-trip — user-reported defect-driven, zero `**Closes:** SC-NNN` sentinels) | **Dual-Critic 12/12 first-Critic + 2/2 meta-Critic missed all VALIDATED, ZERO false-alarms** (B1 `$BOOT_PYTHON` cross-bash-invocation persistence broken via grep'ing INSTALL.md bash-fences L46/L115/L135/L152/L164/L196/L252; M1 unbounded "I'll wait" loop bound via option-removed-on-retry; M2 abort-path wording precision; M3 AC4 section-scoping via `_step_1_section`; M4 PowerShell routing risk pinned to Bash-tool routing; M5 `>= 3.10` hardcoded → dynamic pyproject grep + L71 stale `3.11+` removal; M6 R-17 canonical filing surface; M7 R-16 broader-class Notes paragraph; M8 MEPD-1(b) discharge-by-assertion not analogy; m1 WIRE-1 canonical slice-051 form; m2 ADR criteria mechanical strengthening; m3 installer-suggestion de-versioning; M-add-1 INSTALL.md not in /code-review in-scope list → user-ratified Option (b) at TRI-1: out-of-scope for /code-review v1, structural coverage delegated to AC1-AC4 + slice-045/058 + INST-1; m-add-1 `$user_path`/`$AI_SDLC_DIR` prose-templating mechanism unnamed → one-sentence design.md addition) → all 14 ACCEPTED-FIXED → TRI-1 user-ratified CLEAN. **Zero-false-alarm streak N=12 cumulative on codification slices** (046/048/050/051/052/053/054/055/056/057/058/061). **N+1 doctrine held at N=12 cumulative** — slice-060 minted /code-review + CRSI-1 in-loop adversarial code-review surface; slice-061 first governed slice; meta-Critic caught M-add-1 (fresh-surface scope-list class). **Missed by both Critic layers (N=1 watch-list, BC-PROJ-11 sibling-coverage class)**: the M5 fix targeted path-validation surface but `Python 3.10 or newer` literal at L73 instance of same class — caught at /build-slice Step 6 BC-1 audit-time backstop, fixed in-band (build-time-reachable, slice-037 law → `/critic-calibrate` watch-list, no BC-1 promotion at N=1). | **R-16 born-retired** (INSTALL.md install-recipe python-detection brittleness; slice-045 R-11 precedent for user-reported INSTALL.md recipe defects; with Notes paragraph naming broader-class coverage — conda-only / NixOS / Termux / Docker — per slice-057/R-15 pattern; MEPD-1(b) discharged-by-name vs META-1 `^## v…`-split assertion at `tests/methodology/test_methodology_changelog.py:136`; vacuously satisfied — zero new `## v…` sections). **R-17 minted open-mitigating** (BRANCH-1 prerequisite check does not verify clean working tree before /build-slice creates `slice/NNN-` branch; soft-prose enforcement exists but no audit-layer; candidate fix: extend `tools/branch_workflow_audit.py` with pre-create cleanliness invariant OR offer `git worktree add` path; slice-062+ candidate). **R-18 minted open-mitigating** (newly-installed Claude Code subagents not hot-loaded into running session; recurring class for any slice immediately following an agent-shipping slice in same session; witnessed N=1 at slice-061 /code-review auto-advance hop where `code-review` agent installed by slice-060 was invisible to runtime; candidate fixes: methodology-side warning at /build-slice post-build on new-agent diff / /code-review error-semantic widening / Claude Code platform fix). **Two user-approved deferrals**: (a) pre-existing slice-060 R-15-class stale-archive-path defect at `tests/skills/code_review/test_code_review_skill.py` — slice-056 corpus class-closure backstop scopes only `tests/methodology/*.py`; slice-062 nominated to repoint via `_resolve_slice_dir(60)` + extend backstop scope to `tests/skills/**/*.py` + `tests/agents/**/*.py` (slice-055 precedent N=2); (b) `/code-review` AGENT-UNSPAWNABLE skipped via user-ratified structured-options at TRI-1-equivalent gate. **Pattern: cross-slice runtime-state interactions are a NEW Critic blind-spot class** — neither first-Critic, meta-Critic, nor code-Critic can reach into runtime-state characteristics of the Claude Code harness itself; build-time + risk-register tracking are the right structural tooling. **No methodology-changelog entry / no VERSION bump / no ADR** — defect-repair / conformance class; voluntary-restraint discipline N=8 cumulative on codification slices (slice-037/046/050/052/055/056/057/061). slice-061 also corrected slice-060's missed `_index.md` + `archive/_index.md` updates (slice-060's row added retroactively as part of slice-061 catalog repair). 23 SC candidates remain open in `diagnose-out/backlog.md` post-slice-061 (unchanged — slice-061 closed no SC). **Strong next-slice candidates**: slice-062 (R-17 + R-18 + R-15-class scope extension all couple naturally into one pipeline-hygiene slice); R-13 (OSDG-1 extension to `/slice-candidates` — slice-052 standing nomination still pending). |
| 060 | [slice-060-add-code-review-skill](slice-060-add-code-review-skill/) | 2026-05-23 | Standard | high (critic-required: true — in-house methodology surfaces: skills/code-review/SKILL.md + agents/code-review.md + tools/pipeline_chain_audit.py + tools/install_audit.py + methodology-changelog.md; high tier) | SHIPPED — validation PASS (CRSI-1 walking-skeleton v1 advisory only; ACs all PASS at /validate-slice; 5-part PMI-1 atomic bump 0.63.0→0.64.0; OSDG-1 family-add + CAD-1 family-add via new drift-guards; PCA-1 _CANONICAL_CHAIN extended 8→9 entries; INST-1 _CANONICAL_SKILLS + _CANONICAL_AGENTS updated; shippability row #60) | **Dual-Critic NEEDS-FIXES → EXTEND**: first NEEDS-FIXES 5B/5M/4m all VALID; meta EXTEND with M-add-1 (Phase B forward-sync omitted edited build-slice + validate-slice SKILL.md — Fowler refactoring discipline fan-out) + M-add-2 (TF-1 plan omitted BC-PROJ-10:173 paired `_shippability_consumer_propagation` test — N≥17 instances) + m-add-1 (Dim 9 reframe clarification on phantom-import-in-`.py` as code-as-artifact under Dim 1/Dim 5); TRI-1 user-ratified CLEAN. | **CRSI-1 minted** (ADR-059; methodology v0.64.0; mints a new rule, supersedes nothing; extends dual-Critic /critique + /critique-review design-review family + OSDG-1/CAD-1 drift-guard families onto code-as-artifact). `/code-review` added to canonical per-slice pipeline chain between `/build-slice` and `/validate-slice` as walking-skeleton in-loop adversarial code-Critic; v1 advisory only (slice-061 = AI-bloat passes deferred; slice-062 = TRI-1 + verdict-driven block deferred). Bootstrap CRSI-1 self-application via `tests/skills/code_review/test_code_review_skill.py::test_self_dogfood_produces_code_review_md_on_slice_060` (NOTE: post-archive this test became R-15-class stale-archive-path failure surfaced at slice-061; slice-062 nominated for fix). **Slice-060's `/reflect` step did NOT update `_index.md` or `archive/_index.md` — corrected retroactively as part of slice-061's catalog repair.** |
| 059 | [slice-059-add-tools-package-version-gate](slice-059-add-tools-package-version-gate/) | 2026-05-23 | Standard | medium (critic-required: true — in-house methodology surfaces: tools/*.py + skill SKILL.md + methodology-changelog.md) | SHIPPED — validation PASS (5/5 ACs with real-environment per-criterion evidence — live `--root` mismatch run → DRIFT exit 1, synced exit 0, 8-test regression suite, wiring + forward-sync, registration; VAL-1 both layers clean 0 secrets / 0 hallucinated imports; WS-1 / ETC-1 / TF-1 N/A default-off; 14 Step-6 audits clean incl. TVFS-1 self-application bootstrap / AVFS-1 / MCFS-1 / PMI-1 / INST-1 / BRANCH-1 / UTF8-STDOUT-1 / CRP-1 / PCA-1 / BCI-1 / STP-1 / WIRE-1 / DR-1-struct / BC-1; SCMD-1 + PTFCD-1 pre-gates clean; shippability_runner 59/59 PASS 0 FAIL with new row #59; full methodology suite 807/807 PASS; /drift-check CLEAN 0 blockers / 0 majors; NOT a BCR-1 round-trip — gap-driven, zero `**Closes:** SC-NNN` sentinels) | **TVFS-1 minted** (ADR-058; methodology v0.63.0; mints a new rule, supersedes nothing; extends the slice-050 AVFS-1 + slice-041 MCFS-1 forward-sync-via-deterministic-downstream-gate lineage onto the installed-pip-artifact leg). **first-Critic BLOCKED** (3B/3M/2m all VALIDATED, 0 FALSE-ALARM, 0 OVERRIDE-MISJUDGED — B1 highest-value, APED-1 empirical attack: rewrote the in-repo ai_sdlc_tools.egg-info/PKG-INFO to 9.9.9, proved importlib.metadata.version() resolves the egg-info not the venv .dist-info → the original mechanism tautologically green; B2 no installed_version_resolver injection seam → AC3 non-tautological drift case unmeetable; B3 bootstrap pip-install unowned by build sequence + shared-venv-from-branch side effect; M1 --root carried without the deadness analysis applied to _normalized_bytes; M2 shippability row# unspecified; M3 WARN-exit-0 weaker than AVFS-1's, must name sys.executable; m1 install_audit.py count literals; m2 verified-head note) → **DR-1 EXTEND** (8/8 first-Critic VALID at correct severity + 0 suspicious + 0 severity adjustments + 2 missed — M-add-1 the :25 count-scrub rewords the --strict flag's docstring, itself B1-class CWD-shadowed, without noting it / M-add-2 the >1 duplicate-.dist-info case lumped into the generic exit-2 message — both VALIDATED) → all 10 ACCEPTED-FIXED → TRI-1 user-ratified CLEAN. **Zero-false-alarm streak N=12 on codification slices** (046/048/050/051/052/053/054/055/056/057/058/059). **N+1 doctrine held** — the first Critic caught the novel mechanism edge (B1) directly via APED-1; the meta-Critic backstop added 2 Minor completeness misses. **Missed by Critic**: the full new-tools/-module registration touchpoint set — INSTALL.md's test-enforced tool-count literal + test_utf8_stdout_regression.py's cp1252-coverage _ROOT_ONLY_TOOLS list — surfaced as 2 methodology-suite FAILures on the first full-suite run at build Step 6, fixed in-band (build-time-reachable, gate-caught at intended latency per the slice-037 law). **Pattern**: dual-Critic excellent on the novel mechanism, under-enumerated the mechanical registration breadth; the build-time methodology suite (BC-PROJ-4) was the backstop. | **No new risk minted** — slice-059 retires no registered risk (the gap it closed — installed pip package ungated — is now CLOSED by TVFS-1; the one discovered residual, install_audit.py _check_tool_modules CWD-import-shadowing, is a low-severity known-limitation practically mooted by TVFS-1's version axis, captured in reflection.md not minted as R-NN). **TVFS-1 closes the version-bearing-leg gate family**: PMI-1 (plugin.yaml) / PVFS-1 (pyproject.toml) / AVFS-1 (installed ai-sdlc-VERSION) / MCFS-1 (installed changelog) / TVFS-1 (installed pip distribution) — every version-bearing surface now gated. **TVFS-1 self-gated its own slice** — at slice-059's Step 6 the venv was 0.62.0 vs VERSION 0.63.0; the named pip install --upgrade . build step refreshed it; TVFS-1 then exited 0 synced (ADR-052 bootstrap-honesty precedent). **No BC-1 promotion** — the registration-touchpoint miss is N=1 + build-time-reachable (slice-037 law → /critic-calibrate watch-list candidate, not a new first-Critic dimension). **Adding a tools/ module touches five inventories** (plugin.yaml, install_audit.py _CANONICAL_TOOLS, shippability.md, INSTALL.md tool-count, test_utf8_stdout_regression.py cp1252-coverage list) — enumerate all five at design time. **An empirical Critic attack (APED-1) is the right move when a slice's correctness rests on a runtime-resolution claim** — B1 was provable only by executing importlib.metadata, not reading the design. 4-part PMI-1 bump 0.62.0→0.63.0. Zero design deviations, zero deferrals. 23 SC candidates remain open in diagnose-out/backlog.md (unchanged — slice closed no SC). **Strong next-slice candidates**: R-13 (OSDG-1 extension to /slice-candidates — maintainer's standing nomination); fix install_audit.py _check_tool_modules CWD-import-shadowing (low-priority residual); SC-002 (declare tomllib dependency). |
| 058 | [slice-058-add-install-wakeup-prompt-guardrail](slice-058-add-install-wakeup-prompt-guardrail/) | 2026-05-22 | Standard | medium (critic-required: true — in-house methodology surface: INSTALL.md is the INST-1 install recipe + the slice defines content appended to the user's global ~/.claude/CLAUDE.md) | SHIPPED — validation PASS (3/3 ACs with real-artifact per-criterion evidence — pytest 3/3 on the real INSTALL.md + shippability.md, genuine 3-FAIL-pre-edit → 3-PASS-post-edit contrast; VAL-1 both layers clean 0 secrets / 0 hallucinated imports; WS-1 / ETC-1 N/A default-off; TF-1 3/3 PASSING --strict-pre-finish; all 18 Step-6 audits clean incl. BC-1 / WIRE-1 / BRANCH-1 / UTF8-STDOUT-1 / CRP-1 / PCA-1 / BCI-1 / MCFS-1 / STP-1 / AVFS-1 / LINT-MOCK-1; SCMD-1 + PTFCD-1 pre-gates clean; SRSC-1 runner 58/58 PASS 0 FAIL with new row #58; /drift-check CLEAN 0 blockers / 0 majors; NOT a BCR-1 round-trip — /query-design-originated, not backlog-driven, zero **Closes:** SC-NNN sentinels) | **NO methodology-changelog entry + NO VERSION bump + NO new RULE-ID** (slice-045 INSTALL.md-prose conformance class; MEPD-1(b) discharged by name vs the real META-1 ^## v-split assertion at tests/methodology/test_methodology_changelog.py:136 — zero new ## v sections so vacuously satisfied; BOTH Critics independently UPHELD the no-bump classification, explicitly rejecting the slice-049-B2 / slice-052 counter-precedent). first-Critic NEEDS-FIXES (2B/2M/2m — B1 cross-file test-fn-name inconsistency FBCD-1, B2 stale v0.54.0 literal at INSTALL.md:18, M1 AC3 row-count ambiguity, M2 genuine-contrast under-specified, m1 ADR-057 numbering verified-clean, m2 v0.63.0 reference verified-correct) → **DR-1 EXTEND** (6/6 first-Critic VALID at correct severity + 0 suspicious + 2 missed — M-add-1 AC2 test pins a still-"Draft" block / M-add-2 new install step placement unspecified) all 8 ACCEPTED-FIXED → TRI-1 user-ratified CLEAN. **Zero-false-alarm streak N=11 on codification slices** (046/048/050/051/052/053/054/055/056/057/058); first Critic 6/6 VALID 0 false-alarm. **Missed by Critic: none** — both Critic layers together caught every issue; the build executed verbatim with zero deviations and every Step-6 gate passed first-run. **Pattern**: meta-Critic EXTEND added 2 findings in dimensions the first Critic structurally under-weighted — recipe completeness (step placement) + test/text freeze coupling (pinning a "Draft" artifact) — N+1 to the slice-040 doctrine. | **BC-PROJ-11 promoted** (recipe / methodology docs must reference VERSION dynamically, never hard-code a methodology version literal; Important; Applies to INSTALL.md + README.md; N=2 recurrence — slice-045 v0.20.0 → re-drifted v0.54.0 → slice-058): authored into the git-tracked canonical fixture + byte-copied to the live build-checks.md + a test_bc_proj_11 structural-identity pin; BCI-1 integrity audit PASS, 53/53 build-checks tests PASS. **No new risks** — the slice is preventive and retires no registered risk; the B2 re-drift pattern is captured as BC-PROJ-11 + a lessons-learned entry, not an open risk. Discovered: slice-045's INSTALL.md version-literal fix was itself drift-prone (refreshing a stale literal just resets the drift clock — only removal / VERSION-reference is durable); a test-pinned prose artifact must be FROZEN at design time with named verbatim anchors (M-add-1); a new step in a numbered procedure needs an explicit design-time placement decision incl. the renumber-vs-append call (M-add-2). Originated from a /query-design → /slice handoff: the design tradeoff (INSTALL.md global-append vs per-project /triage+/adopt) was pre-weighed in the /query-design conversation, so /design-slice decided it cleanly via ADR-057. Zero design deviations, zero deferrals. 23 SC candidates remain open in diagnose-out/backlog.md (unchanged — slice closed no SC). **Strong next-slice candidates**: R-13 (OSDG-1 extension to /slice-candidates — maintainer's standing nomination); SC-002 (declare tomllib dependency — VAL-1 Layer B silently disabled on Python 3.10); R-2 (/diagnose cwd-mismatch warning has no programmatic test, LOW band). |
| 057 | [slice-057-retire-r15-via-slice-034-resolve-slice-dir-retrofit](slice-057-retire-r15-via-slice-034-resolve-slice-dir-retrofit/) | 2026-05-21 | Standard | low (critic-required: true — voluntary Critic on R-15-family methodology-vault-adjacent slice; slice-056/051/049 voluntary-Critic precedent on codification slices) | SHIPPED — validation PASS (5/5 ACs with per-criterion evidence — git diff + grep + pytest exit-0 + RR-1 audit + shippability runner; VAL-1 both layers clean 0 secrets / 0 hallucinated imports; WS-1 / ETC-1 / TF-1 N/A default-off; all 22 Step-6 audits clean incl. BC-1 / BCI-1 / RR-1 / CAD-1 / PMI-1 / INST-1 / DR-1-struct / WIRE-1 / CSP-1 / SUP-1 / STP-1 both sub-forms / SCMD-1 / PTFCD-1 / BRANCH-1 / UTF8-STDOUT-1 / CRP-1 / PCA-1 / MCFS-1 / AVFS-1 / triage_audit; LINT-MOCK clean; SCMD-1+PTFCD-1 pre-gates clean; shippability_runner **57/57 PASS 0 FAIL** with new row #57 PASSing on first authored run; full methodology suite **792/792 PASS** up from 791 with +1 new test_shippability_row_57_present_and_cites_r15; MCFS-1 + AVFS-1 forward-syncs PASS at both Step-6 + /reflect Step 5b-fs/avfs; **NOT a BCR-1 round-trip** — risk-register-driven (R-15) NOT backlog-driven (zero `**Closes:** SC-NNN` sentinels in mission-brief OR reflection per slice-053 M4 mention-vs-application disambiguation; correct no-op)) | **R-15 RETIRED part-(b) DONE** (slice-057 closes the two-part R-15 retirement gate slice-056 pre-engineered; **NO new rule + NO ADR + NO methodology-changelog entry + NO VERSION bump** per voluntary-restraint discipline slice-037/046/050/052/055/056/057 N=7 cumulative — pure conformance / retirement-discharge class; MEPD-1(b) discharged-by-name with META-1 enforcing-assertion verification at `tests/methodology/test_methodology_changelog.py:136` + slice-040 R-10 / slice-043 R-6 / slice-045 R-11 / slice-056 R-15-part-(a) N=5 same-class precedent). first-Critic NEEDS-FIXES (0B/1M/4m — M1 mid-slice gate omits row-#57 test, m1 `:69-72` vs `:70-71` line-number citation drift, m2 `:214-218` vs `:214-220` line-span drift, m3 methodology-surface vs test-pinning-surface terminology overstated, m4 build-log per-audit non-applicability enumeration) → **DR-1 EXTEND** (5/5 first-Critic VALIDATED + 0 suspicious + 0 severity adjustments + 2 missed m-add-1 AC5 named-binding-missing + m-add-2 comment-rewrite-could-self-match guard-rail-missing — both VALIDATED; **slice-040 N+1 doctrine canonical catch**: slice-056 minted the M-add-2 corpus class-closure backstop mechanism — a structurally-novel surface combining whitelist-shrinkage + self-skip clause + dual-direction assertion — and the meta-Critic specifically targeted the novel structural edge that the first-Critic optimized-away in favor of the mechanical pin/changelog surface) → TRI-1 user-ratified Accept-all (6 ACCEPTED-FIXED in-skill + 1 ACCEPTED-PENDING m4 discharged at /build-slice Phase F via per-audit non-applicability enumeration). **Zero-false-alarm streak N=10 on codification slices** (046/048/050/051/052/053/054/055/056/057). **Slice-040 N+1 doctrine N=11 cumulative** across slice-026/029/038/039/046/049/050/051/054/055/056/057 ("discipline rule minted in slice N is a first-Critic blind spot on slice N+1; the DR-1 meta-Critic is the structural backstop" — now a stable structural law for codification-class slices). **TPHD-1(b) Builder self-catch pre-/critique-review held** (slice-049 N+1 lineage): caught design.md §"Validation strategy" stale "two affected tests" → "three" carry-over from /critique M1 ACCEPTED-FIXED before launching meta-Critic; kept dual-Critic stack focused on real findings vs Builder carry-over noise. **Missed by both Critic layers (N=0)**: zero missed-by-both-layers findings on slice-057. The dual-Critic stack reached every concern that materialized during /build-slice and /validate-slice. | **R-15 RETIRED** (Status mitigating → retired in `architecture/risk-register.md:254` with `**Retired**: slice-057-… (2026-05-21)` field-line + retirement paragraph appended at section end citing all 5 discharge components: literal repoint at `tests/methodology/test_ptffd1_no_false_positive.py:69-72` PRE-EDIT 4-line span — NOT `:70-71` per m1; whitelist shrink to `set()`; corpus-backstop `missing_whitelist == set()` structural witness; META-1 enforcing-assertion verification; slice-040/043/045/056 precedent corroboration. Preserves slice-056 part-(a) DONE + "Why mitigating, not retired (post-slice-056)" + "Why not Critic-promotion" verbatim per slice-040 R-10 retirement-precedent shape; retired-count 8→9). **Slice-056's two-part R-15 retirement gate engineered correctly**: the M-add-2 corpus class-closure backstop's whitelist-shrinkage mechanism fired structurally on Task 2 alone (whitelist→`set()` + zero corpus matches = both halves of `unexpected = matches - whitelist == set()` AND `missing_whitelist = whitelist - matches == set()` trivially satisfied — no cross-slice observational evidence needed; the design pattern slice-056 invented generalized cleanly to its first governed slice). **`_resolve_slice_dir(34)` resolves to archived slice-034 directory on first authored consumer** (active-glob miss + archive-glob hit per the helper's specified semantics). **Builder guard-rail discipline for novel-structural-mechanism slices** (reusable): for any slice whose central mechanism is a corpus-backstop or self-matching-regex surface, include in design.md §"What's new" (a) regex match shape that would trigger a self-match, (b) self-skip clause's exact scope, (c) safe alternatives if in-comment example needed, (d) prose-mention-is-safe-by-construction guarantee. **Retirement-discharge slice class is N=5 stable** (slice-040 R-10 / slice-043 R-6 / slice-045 R-11 / slice-056 R-15-part-(a) / slice-057 R-15-part-(b)): canonical artifact set is (i) flip Status field-line in risk-register.md per slice-043 BC-PROJ-6 (RR-1 parses status SOLELY from this field), (ii) append retirement paragraph at section end with full discharge mechanics, (iii) preserve all prior prose verbatim per slice-040 R-10 retirement-precedent. **No BC-1 promotion** — N=0 missed-by-both-Critic-layers findings; slice-037 audit-vs-real-artifact law (N=10+ cumulative on codification slices) continues to hold; existing gates ARE the structural backstop. 23 SC candidates remain open in `diagnose-out/backlog.md` post-slice-057 (unchanged from slice-056 baseline — risk-register-driven slice, no SC closures). **Strong next-slice candidates**: R-13 (OSDG-1 extension to `/slice-candidates`, slice-052 reflection nominated as "strong next-slice candidate", verbatim-clone shape from slice-049/051); R-14 Residual-axis-1 (BCR-1 human-judgement axis, future `/critic-calibrate` candidate if N=2 recurrence emerges); R-2 (`/diagnose` cwd-mismatch warning has no programmatic test, LOW band score 2 open since slice-002). |
| 056 | [slice-056-fix-bcr1-round-trip-test-archive-paths](slice-056-fix-bcr1-round-trip-test-archive-paths/) | 2026-05-21 | Standard | medium (critic-required: true — in-house-methodology-surface trigger: `tests/methodology/*.py` + risk-register edit) | SHIPPED — validation PASS (5/5 ACs + AC5b PASS with real-environment evidence; VAL-1 both layers clean 0/0; WS-1/ETC-1 N/A default-off; TF-1 8/8 PASSING --strict-pre-finish; all 14+ Step-6 audits clean incl. BC-1/BCI-1/MCFS-1/AVFS-1/STP-1/BRANCH-1/UTF8-STDOUT-1/CRP-1/PCA-1/WIRE-1/triage_audit/critique_review_audit/PMI-1; SCMD-1+PTFCD-1 pre-gates clean; SRSC-1 runner **56/56 PASS 0 FAIL** incl. row #54 PASS-flip from FAIL — AC5b verification side effect; full methodology suite **791/791 PASS** up from 789 with +2 new tests; **NOT a BCR-1 round-trip** — risk-register-driven (R-15) NOT backlog-driven (zero `**Closes:** SC-NNN` sentinels; slice-053 M4 mention-vs-application disambiguation correctly no-ops the round-trip) | **NO new rule minted + NO ADR** (deliberate voluntary-restraint per slice-037/046/050/052/055/056 N≥6 cumulative — R-15 is class-signal-N=1 with structural whitelist-shrinkage backstop available; helper + tests + risk-register transition IS the right artifact). first-Critic NEEDS-FIXES (0B/2M/5m+1info — M1 N=1 corpus claim wrong → N=2 acknowledged + slice-034 retrofit deferred with rationale; M2 helper signature 1-arg vs 2-arg → 1-arg PINNED + 2-arg REJECTED at /critique-disposition; m1 AC3 SLICE_054_DIR-removal prose vs evidence asymmetry → harmonized across 3 sites; m2 R-15 retirement gate is two-part → R-15 STAYS mitigating + slice-056 part-(a) DONE; m3 row #54 PASS-flip uncited in AC5 → AC5b + verification-row 5b added; m4 Windows path-separator diagnostic-message hazard → forward-slash f-string template pinned; m5 pathlib glob case sensitivity → 2-line cross-platform note; m6 META-1 grounding verified informational) → **DR-1 EXTEND** (7/7 first-Critic VALID at correct severity + 0 suspicious + M-add-1 TF-1 strict-pre-finish AC-numbering build-time-reachable miss + M-add-2 corpus class-closure backstop missed + M-add-3 mission-brief vs design Pipeline-position predecessor inconsistency + m4-severity-question correctly LEFT AS Minor by meta-Critic) all ACCEPTED-FIXED → TRI-1 user-ratified CLEAN (11/11 ACCEPTED-FIXED). Builder empirical override on M-add-1 proposed-fix-mechanism: meta-Critic recommended option (a) "split body AC into 4a./4b./4c." which would NOT work (`_AC_ITEM_RE` rejects `4a.`); Builder chose multi-row-per-AC with plain `4` cells (underlying M-add-1 finding remained VALIDATED; specific implementation recommendation was wrong at regex layer). **Zero-false-alarm streak N=9 on codification slices (046/048/050/051/052/053/054/055/056)**. **Missed by both Critic layers (N=3 build-time-reachable, gate-caught classes)**: (i) R-15 explanatory comment containing matched literal-path-RHS substring created circular self-detection at mid-slice smoke (AC3 + corpus backstop both FAILed on the comment text); (ii) BC-PROJ-7 pipe-violation inside backtick-fenced code span in shippability row Description text (`Path \| None = None`) shifted columns under SCMD-1's naive `inner.split("|")` — first fix attempt with `\|` escape did NOT help (parser ignores markdown escapes); final fix: reword prose to avoid pipe entirely; (iii) meta-Critic's PROPOSED FIX mechanism was wrong at regex layer. All caught in-band by Step 6 gates. Per slice-037 law: NO BC-1 promotion (build-time-reachable + gate-caught all 3; N=1 watch-list class signals). | **R-15 part-(a) DONE** (slice-056 ships helper + repoint + class-closure backstop; STAYS mitigating; part-(b) pending the future slice that demonstrably uses `_resolve_slice_dir` — slice-034 retrofit nominated). **3 N=1 watch-list class signals**: (i) doc-comment-vs-audit-literal self-detection (paraphrase matched literals in comments, don't include verbatim); (ii) BC-PROJ-7 scope clarification: pipe-free row applies inside backtick-fenced code spans inside Description text (SCMD-1 parser doesn't honor markdown `\|` escapes); (iii) meta-Critic's PROPOSED FIX mechanism needs Builder empirical verification before adoption (Builder recompute-don't-trust extends to meta-Critic recommendations, N≥6 cumulative on broader pattern). **Helper-with-call-time-globals + monkeypatch is the canonical Python pattern** for test-utility-with-injectable-REPO_ROOT (reusable). **R-15 part-(b) retirement is structurally backstopped** via corpus-backstop whitelist-shrinkage mechanism (reusable pattern for any "future slice demonstrably uses the discipline" gate — dual-direction whitelist check makes it structural, not just observational). **Voluntary-restraint discipline tunable knob driven by failure-class recurrence + structural-mechanism availability, NOT severity** (slice-054 minted PVFS-1 on recurring class; slice-056 minted nothing on class-signal-N=1 with structural backstop). 23 SC candidates remain open in `diagnose-out/backlog.md` post slice-056 (R-15-focus, zero SC closures this slice). **Strong next-slice candidates**: slice-034 retrofit (R-15 part-(b) discharge — use `_resolve_slice_dir(34)`); SC-002 (declare-tomllib-or-raise-python-3-11, backlog topo next after SC-001); R-13 (OSDG-1 extension to `/slice-candidates`). |
| 055 | [slice-055-add-shippability-runner-execution-tests](slice-055-add-shippability-runner-execution-tests/) | 2026-05-21 | Standard | medium (critic-required: true — in-house-methodology-surface trigger: `tools/**/*.py` coverage target on SRSC-1 gate runner) | SHIPPED-WITH-DEFERRALS — validation PASS (5/5 ACs real-evidence; VAL-1 both layers clean 0/0; WS-1/ETC-1 N/A default-off; TF-1 N/A `Test-first=false`; all 17 Step-6 audits clean incl. BC-1/BRANCH-1/PCA-1/UTF8-STDOUT-1/CRP-1/BCI-1/MCFS-1/STP-1/AVFS-1/WIRE-1/LINT-MOCK; OSDG-1 via per-skill pytest 13/13; SCMD-1+PTFCD-1 pre-gates clean; SRSC-1 runner self-dogfood **53/54 PASS, 1/54 FAIL** on pre-existing slice-054 stale-archive-path — verified NOT a slice-055 regression by reproduction on master; user-approved deferral via TRI-1; **second end-to-end BCR-1 round-trip dogfood SUCCESS** — `**Closes:** SC-005` sentinel triggered `/reflect` injection of `- **Addressed:** slice-055-…` line at BCR-1-mandated position; output-axis verified `addr_pos=1328 > last_ev_pos=1185` via `re.finditer(r'^  - ', block, re.MULTILINE)[-1]` LAST-sub-bullet anchor) | NO new rule minted (deliberate voluntary-restraint per slice-037/046/050/052/055 N≥5 — SC-005 is one-shot test-coverage gap on already-pinned SRSC-1/ADR-039; test module IS the right artifact). first-Critic NEEDS-FIXES (2B/3M/2m → all Builder-applied: 5 ACCEPTED-FIXED + 1 OVERRIDDEN-with-rationale on m1 + 1 ACCEPTED-FIXED on m2). B1 (FAIL-branch `;`-split crash on `<interp> -c "import sys; sys.exit(1)"` — `_segments` naive `machine_cmd.split(";")` BEFORE `shlex.split` would `ValueError` both halves; verified empirically; fix: `raise SystemExit(1)`). B2 (BCR-1 position-pin regression — `block.rfind('**Evidence:**')` weak header-only anchor; fix: `re.finditer(r'^  - ', block, re.MULTILINE)[-1]` LAST-sub-bullet anchor restores slice-054 AC4 contract). M1 (mid-slice smoke gate cross-site name drift `_records_pass` / `_records_fail` suffix; FBCD-1 fix). M2 (PowerShell-unfriendly `grep -c` → `& $PY -c "import ast, ..."` AST count). M3 (brittle substring-only assertion → structural pins + narrowed first-line substring, slice-038 m2 load-bearing-test lesson). m1 (forecast-section bias — OVERRIDDEN with rationale; meta-Critic VALIDATED override). m2 (helper-signature backtick attribution → corrected `_catalog_rows` vs `_segments` layering). Meta-Critic ACCEPT (all 7 findings VALID; 0 suspicious/0 missed/0 severity adjustments). **Zero-false-alarm streak N=8** on codification slices (046/048/050/051/052/053/054/055). **Missed by both Critic layers (N=2 build-time-reachable, gate-caught classes)**: (a) mission-brief pre-finish-gate template referenced wrong `tools.skill_drift_audit` module (OSDG-1 is enforced via per-skill pytest tests `tests/methodology/test_*_skill_drift.py`, not a single tool); (b) shippability dogfood surfaced pre-existing slice-054 stale-archive-path failure in `test_bcr_1_sc054_round_trip_inputs_invariant` (`SLICE_054_DIR` hardcoded to active path; slice-054 archived → path broken; one-slice latency). Both caught in-band by Step 6 gates. Per slice-037 law: NO BC-1 promotion. | **R-15 minted** (Archive-aware vault-test discipline; medium×low=2 mitigating; class signal N=1): tests pinning invariants on a slice's own active-path vault files break at `/reflect`'s archival because path moves to `archive/`. Documented `_resolve_slice_dir(NNN)` resolver helper pattern + slice-056 candidate fix. Watch-list `/critic-calibrate` candidate (track if N=2 emerges). **N=2 BCR-1 round-trip dogfood structurally robust** — sentinel-anchored trigger + LAST-evidence-sub-bullet position-pin mechanics work cleanly across both rule-minting (slice-054 PVFS-1) and rule-restraint (slice-055 no-mint) ends of the codification spectrum. **AC bifurcation pattern (input-axis at /validate, output-axis at /reflect) confirmed reusable** from slice-054 AC4 → slice-055 AC5. **Bash pipe `$?` captures `tail`'s exit code** — for runners whose exit-code is load-bearing: redirect output to file + capture `$?` BEFORE any tail/grep. Shell discipline, not code risk. 23 SC candidates remain open in `diagnose-out/backlog.md`. |
| 054 | [slice-054-fix-pyproject-toml-version-drift](slice-054-fix-pyproject-toml-version-drift/) | 2026-05-21 | Standard | low (critic-required: true — methodology-surface rule-minting + 4-part PMI-1 atomic bump + Inclusion-heuristic ride) | SHIPPED — validation PASS (4/4 ACs real-environment evidence; VAL-1 both layers clean 0/0; WS-1/ETC-1 n/a default-off; TF-1 4/4 PASSING --strict-pre-finish; all 22 Step-6 audits green incl. PMI-1/INST-1/MCFS-1/AVFS-1/BCI-1/STP-1/PCA-1/CRP-1/BRANCH-1/UTF8-STDOUT-1/SCMD-1/PTFCD-1/PTFFD-1/WIRE-1/LINT-MOCK-1; SRSC-1 runner **54/54 PASS 0 FAIL**; full methodology suite **820 passed**; 4-part PMI-1 atomic 0.61.0→0.62.0; MCFS-1 + AVFS-1 forward-syncs PASS at both Step-6 + /reflect Step 5b-fs/avfs; **first end-to-end BCR-1 round-trip dogfood SUCCESS** — `**Closes:** SC-001` sentinel triggered `/reflect` injection of `- **Addressed:** slice-054-…` at BCR-1-mandated position AFTER `**Evidence:**` sub-list / BEFORE next `### SC-NNN` header; position-pinned awk + line-number check PASSED) | **PVFS-1 minted** (ADR-056; v0.62.0; mints a new rule, supersedes nothing; extends PMI-1 / AVFS-1 / MCFS-1 forward-sync family on the pyproject.toml leg; Route B chosen at /design-slice over A born-retired + C standalone-tool — SOAD-1/BCR-1 rule-without-tool precedent + pyproject in-repo so pytest sufficient, AVFS-1 needed out-of-repo runtime read for the analogue). first-Critic NEEDS-FIXES (0B/4M/2m — M1 AC3 pin test "TBD" → minted `test_pyproject_has_no_stale_0_20_0_or_count_literals` co-located in existing test_pyproject_version_matches_version_file.py; M2 stale-literal enumeration 2-of-3 → expanded to lines 3+6+66; M3 FBCD-1 cross-file smoke-gate literal inconsistency mission-brief `0.61.0` vs design `0.62.0` → mission-brief realigned to `0.62.0`; M4 AC4 BCR-1 grep position-semantic gap → tightened to position-pinned awk + line-number check; m1 stale `VERSION = 0.59.0` heading DEFERRED to `/critic-calibrate` per BCR-1 append-only discipline; m2 Components-touched parenthetical → clean bullet list) → **DR-1 EXTEND** (6/6 first-Critic VALID + 0 suspicious + 0 severity adjustments; +M-add-1 PVFS-1 shippability-consumer-propagation should pin BOTH PVFS-1 AND SC-001 — BCR-1 traceability axis on first dogfood; +m-add-1 function-name `v_0_20_0` vs assertion bare `0.20.0` → renamed via TPHD-1 sub-mode (b) harmonization; +m-add-2 self-withdrawn-on-cell-verification, DR-1 self-verification discipline opposite of rubber-stamp) all ACCEPTED-FIXED → TRI-1 user-ratified CLEAN. Calibration: 6/6 first-Critic VALIDATED, 2/2 meta-Critic VALIDATED, 0 FALSE-ALARM both layers, **zero-false-alarm streak N=7 on codification slices (046/048/050/051/052/053/054)**. | **No new risks** (none discovered; m1 deferral is methodology-refinement candidate). **Missed by both Critic layers (N=2 build-time-reachable, gate-caught)**: TF-1 enum-strict cell (PASSING + parenthetical notes failed enum check) + PTFCD-1 Test-path-resolvability ("(row #54)" suffix tripped phantom-path); both caught in-band at /build-slice Phase F TF-1 strict-pre-finish first run, quick re-edit fix. Per slice-037 law: NO BC-1 promotion (build-time gates work; both N=1 only — N≥7 cumulative confirmation of the audit-vs-real-artifact-interaction law). Discovered: TF-1 cell discipline (Status bare enum, Test-path must resolve) for any TF-1:true brief; BCR-1 trace-axis dual-anchor pin discipline (cite BOTH RULE-ID AND SC-NNN in shippability row) for next BCR-1-closing slice; AC4-class /reflect-deferred output-axis verification pattern (input-contract at /validate via pytest + output-contract at /reflect via actual round-trip — reusable for any future AC whose output is /reflect-time); ~/.claude/* Write tool auto-mode-classifier denial → Bash echo escape (slice-035 DEVIATION-2 + slice-048→049 recurrence class). Cross-slice rule-minting-then-dogfooding pattern (slice-053→054): slice-053 BCR-1 mint + name-the-expected-dogfood + mention-vs-application disambiguation made the handoff clean. 24 SC candidates remain open in `diagnose-out/backlog.md` post slice-054 closing SC-001. **Strong next-slice candidates**: R-13 (OSDG-1 extension to `/slice-candidates`), SC-005 (HIGH-severity shippability_runner.run_catalog untested), SC-024 (parallel install_audit.py stale-prose scrub). |
| 053 | [slice-053-wire-backlog-md-into-slice-and-reflect](slice-053-wire-backlog-md-into-slice-and-reflect/) | 2026-05-21 | Standard | medium (critic-required: true — in-house methodology surfaces: 2 OSDG-1-guarded SKILL.md edits + new audit module + new ADR + methodology-changelog bump + CLAUDE.md self-hosting bullet) | SHIPPED — validation PASS (5/5 ACs); shippability catalog 53/53 PASS; full methodology suite 774 passed | 1B + 4M + 5m first-Critic + 3M-add meta-Critic, all ACCEPTED-FIXED at TRI-1 (user-ratified); dual-Critic 13/13 VALIDATED zero-FALSE-ALARM (joins slice-050+052 zero-false-alarm streak N=6 on codification slices); first-Critic NEEDS-FIXES + meta-Critic EXTEND. **Missed by BOTH Critic layers (N=1 each, watch-list only per slice-037 law)**: (a) multi-site-literal contrast handling — 4 of 8 mid-slice smoke contrasts FAIL-TO-FAIL on `count=1` because canonical literals appear at 2-3 sites in scoped sections; `assert "X" in section` only fails when X disappears entirely; resolved via `count=-1` for multi-site literals + documented for future audit-module slices; (b) linter renumber `7.→8.` on `/slice` SKILL.md numbered list — markdown formatter auto-renumber produced contiguity gap `1,2,3,4,5,6,8`; resolved via stable-leading-literal anchor `**Diagnose-out backlog**` without numeric prefix. Both reachable only at build-time (smoke gate + system-reminder respectively); existing build-time gates ARE the structural backstops per slice-037 law. | **BCR-1 minted** (ADR-055 extends BC-PROJ-10 / Inclusion-heuristic lineage; methodology v0.61.0; mints a new rule, supersedes nothing): closes the `/diagnose → /slice → /reflect` loop deterministically — `/slice` consults `diagnose-out/backlog.md` as source #7; `/reflect` round-trips closed `SC-NNN` findings via `**Closes:** SC-\d{3}` sentinel-anchored trigger (M4 mentioned-vs-closes disambiguation). 8 anchor-presence + position-pin tests + 2 v0.61.0 entry-pins + 4-part PMI-1 atomic bump + CLAUDE.md bullet + shippability row #53. **R-14 minted** (BCR-1 deterministic axis closed; human-judgement axis open as future `/critic-calibrate` candidate; BCR-1↔R-13 producer-side cross-skill dependency disclosed). R-13 remains OPEN as strong next-slice candidate. **No BC-1 promotion** (both N=1 misses watch-list only). slice-054 SC-001 fix-pyproject-toml-version-drift queued as the immediate next slice via the now-wired BCR-1 channel. **Pattern: audit-codification slices self-apply at runtime in ways the dual-Critic stack structurally cannot predict — N+1 to slice-032/037/044 law; the build-time gates + system-reminders ARE the structural backstops.** Slice was itself minted from Claude self-demonstrating the missing-wire on its own /slice invocation (user interrupted with "this is a major bug/deviation requiring immediate fix") — the dogfood is the slice itself; first-ever BCR-1 round-trip will fire at slice-054. |
| 052 | [slice-052-add-slice-candidates-obo-mode](slice-052-add-slice-candidates-obo-mode/) | 2026-05-20 | Standard | medium (critic-required: true — in-house methodology surface + Hard-rule deviation ADR) | SHIPPED — validation PASS (5/5 ACs); shippability catalog 52/52 PASS; methodology suite 764 passed | 2B + 4M + 3m all ACCEPTED-FIXED (user-ratified TRI-1); DR-1 meta-Critic EXTEND (+2M missed: M-add-1 Defer-terminal reopen path, M-add-2 step-5 `re.sub` corruption hazard; m1 severity-bumped Minor→Major); 9/9 VALIDATED, zero FALSE-ALARM. **Missed by BOTH Critic layers**: v0.60.0 4-part PMI-1 bump classification (mission-brief's "drift posture unaffected" conflated PMI-1 artifact-enumeration with Inclusion-heuristic axis; N=2 with slice-049 B2 — caught at /build-slice plan-mode self-application halt, user-ratified scope expansion via structured options) | R-12 mitigating (Hard-rule-#2 controlled relaxation via mechanical `--obo-peek` `Path.resolve()` containment); R-13 open (OSDG-1 drift guard not yet extended to `/slice-candidates` — strong next-slice candidate); **BC-PROJ-10 promoted** (Builder-side gate: Inclusion-heuristic classification required before /critique for any slice minting a new ADR / adding a user-facing skill capability — closes the slice-049/052 N=2 missed-by-Critic class); pre-existing `build_backlog.py` Windows stdout/stderr crash class fixed at shared `_reconfigure_stdout()` entrypoint |
| 051 | [slice-051-extend-osdg-1-to-reflect-skill](slice-051-extend-osdg-1-to-reflect-skill/) | 2026-05-19 | Standard | medium (critic-required: true — in-house methodology surfaces) | SHIPPED — validation PASS (5/5 ACs) | 0B + 3M + 1m all ACCEPTED-FIXED; DR-1 meta-Critic ACCEPT (4/4 VALIDATED, 0 FALSE-ALARM) | BC-1 Critical process-deviation: mid-slice git-checkout restore tripped BC-PROJ-3/BC-GLOBAL-2 (provably harmless — slice does not edit skills/reflect/SKILL.md; worktree raw-byte==HEAD; BC-1 build-time gate is the backstop, MISSED by both Critic layers). Not promoted to risk-register (controlling rules already exist; N=1-with-control, slice-048/049 precedent). |
| 050 | [slice-050-add-ai-sdlc-version-forward-sync-gate](slice-050-add-ai-sdlc-version-forward-sync-gate/) | 2026-05-19 | Standard | medium (critic-required: tools/**/*.py + methodology surface + new RULE-ID) | SHIPPED — validation PASS | first-Critic NEEDS-FIXES (B1/B2/B3 Blockers, M1-M4 Majors, m1-m3 Minors — all 10 VALIDATED, 0 FALSE-ALARM, 0 OVERRIDE-MISJUDGED; M3 highest-value: recomputed essential-unregistered does NOT transfer → environment-mutable-state ground); DR-1 EXTEND (+M-add-1 unguarded reflect leg, +M-add-2 build-slice skill-drift pin — both VALIDATED, 0 suspicious, 0 severity-wrong) | Unguarded `reflect/SKILL.md` leg (closing ai-sdlc-VERSION opened analogous N+1 leg — NOT in OSDG-1 set; next-slice nomination); Missed-by-Critic: INSTALL.md hard-coded tool-count (×2) → **BC-PROJ-9 promoted** (new-`tools/*.py` ~6-surface inventory fan-out) |
| 049 | [slice-049-add-triage-adopt-skill-drift-guards](slice-049-add-triage-adopt-skill-drift-guards/) | 2026-05-19 | Standard | low (critic-required: methodology surface) | SHIPPED | first-Critic NEEDS-FIXES (B1/B2 Blockers, M1/M2 Majors, m1/m2 Minors — all VALIDATED, 0 FALSE-ALARM); DR-1 EXTEND (+B-add-1 Blocker, +M-add-1 Major — both VALIDATED, 0 false positives both layers) | Pre-existing slice-048 `~/.claude/ai-sdlc-VERSION`=0.55.0 4-part-PMI-1 installed-leg drift (N=2 w/ slice-035 DEVIATION-2); MCFS-1 guards the changelog leg but no deterministic gate guards the ai-sdlc-VERSION leg — next-slice candidate |
| 048 | [slice-048-codify-structured-options-ask-rule](slice-048-codify-structured-options-ask-rule/) | 2026-05-19 | Standard | medium (critic-required: true — in-house methodology surfaces: `skills/triage/SKILL.md`, `skills/adopt/SKILL.md`, `CLAUDE.md`, `methodology-changelog.md`) | SHIPPED — validation PASS (5/5 ACs real-artifact evidence; VAL-1 both layers clean 0/0; WS-1/ETC-1/TF-1 n/a default-off; all Step-6 audits green incl. PMI-1/PCA-1/STP-1/BRANCH-1/CRP-1/BCI-1/MCFS-1/WIRE-1/UTF8-STDOUT-1/CAD-1; LINT-MOCK n/a; SCMD-1+PTFCD-1 pre-gates exit 0; shippability_runner **48/48 PASS 0 FAIL**; 4-part atomic bump 0.55.0→0.56.0) | first-Critic NEEDS-FIXES (1B/2M/2m — **B1** false-precedent (design wrongly claimed MCFS-1 retired the per-version changelog pin; reversed + v0.54.0-STP-1-shape entry-pin pair added), M1 AC5-pin-inconsistent, M2 free-text-escape-hatch-conflation, m1 brittle-line-citations, m2 row#48-forward-asserted) all ACCEPTED-FIXED → **DR-1 EXTEND** (all 5 VALID / 0 suspicious / 0 severity; +1 MISSED **M-add-1** SOAD-1 self-violation inside host templates) → TRI-1 user-ratified CLEAN (M-add-1 fix option (a): reword hard-rule ASK to structured options in all 5 surfaces). Calibration: B1/M1/M2/m1/m2 all VALIDATED + 0 FALSE-ALARM (first-Critic MEPD-1(b) recompute-don't-trust exemplary on B1), M-add-1 VALIDATED-via-DR-1. | **No new risks**. SOAD-1 minted (ADR-050; methodology v0.56.0; generalizes ADR-048 gate-specific structured-options-ask requirement pipeline-wide; mints a new rule, supersedes nothing) — canonical sentence verbatim in 4 opener CLAUDE.md template blocks + this repo's CLAUDE.md (self-hosting dogfood, RSAD-1 N=1 clean). Discovered: triage/adopt SKILL.md have no mini-CAD drift guard (N=1 latent gap, next-slice candidate, not a risk). Deferred: 24-skill prose retrofit, executable free-text-ask lint, personal ~/.claude/CLAUDE.md (all user-decided out of scope → backlog). No BC-1 promotion (M-add-1 DR-1-cured per slice-037 law; B1 = existing MEPD-1(b)/BC-PROJ-4 recurrence). Zero design deviations (post-critique-corrected design executed as written). |
| 047 | [slice-047-add-two-scope-install](slice-047-add-two-scope-install/) | 2026-05-19 | Standard | high (critic-required: true — in-house methodology surface: `INSTALL.md` is the INST-1 install recipe + new ADR-049 + a planned methodology-changelog change) | **NOT SHIPPED — feature WITHDRAWN at TRI-1 as platform-infeasible.** `/build-slice` & `/validate-slice` never ran (premise refuted at critique). Salvaged: **ADR-049 rewritten in place** (same slice/ID; no SUP-1 event — never shipped) feature-design ADR → **global-only decision record** retiring the slice-045/046 scope-blindness deferral by *deciding* it (global-only). No INSTALL.md / VERSION / methodology-changelog change (no-behavior-change / decision-only class; slice-043/045 precedent). | first-Critic **BLOCKED** — B1 ESCALATED (project-scope silently shadowed: Claude Code skills resolve personal `~/.claude/` over project; Builder-WebFetch-confirmed; **0 false positives across 7 findings**). M1/M2/M3/m1/m2 VALID but conditional on a shipping feature. → **DR-1 EXTEND**: +1 MISSED **M-add-1** (subagents resolve project>user — the inverse → a split-resolution runtime state INST-1 false-greens, worse than the flagged no-op; Builder-WebFetch-confirmed) → **TRI-1 user-ratified**: abandon feature, ADR-049 → global-only decision record. Calibration: **B1 VALIDATED**, **M-add-1 MISSED-by-Critic** (verify-one-artifact-class-then-generalize on a platform precedence claim), M1/M2/M3/m1/m-add-2 NOT-YET→closed-obsolete, m2 VALIDATED. | **No new risks** (scope-blindness deferral closed by *decision*, not carried as exposure). Discovered: Claude Code skill vs subagent install-scope precedence is **asymmetric** (skills personal>project; subagents project>user); split-resolution runtime hazard INST-1 false-greens; the only future path to per-project scope is a Claude Code **plugin** re-architecture (namespaced `plugin-name:skill-name` cannot collide). **BC-GLOBAL-3 promoted** at `/reflect` Step-5b (load-bearing external-platform behavior must be official-doc-verified before design lock; BCI-1 + literal pin; user-approved global promotion). shippability row 47. Deferred: per-project scope (plugin re-arch — backlog); next candidate via fresh `/slice`. Zero design deviations (no build). |
| 046 | [slice-046-add-conditional-repro-auto-advance](slice-046-add-conditional-repro-auto-advance/) | 2026-05-19 | Standard | medium (critic-required: true — in-house methodology surface: `skills/slice/SKILL.md` + `methodology-changelog.md` + ADR-048) | SHIPPED — validation PASS (5/5 ACs real-artifact evidence; VAL-1 both layers clean 0/0; WS-1/ETC-1/TF-1 n/a default-off; all Step-6 audits green incl. PCA-1/STP-1/BRANCH-1/CRP-1/BCI-1/MCFS-1/WIRE-1/UTF8-STDOUT-1 + CAD-1/PMI-1/INST-1; mock-budget clean; SCMD-1+PTFCD-1 pre-gates exit 0; shippability_runner **46/46 PASS 0 FAIL**; 4-part atomic bump 0.54.0→0.55.0) | first-Critic NEEDS-FIXES (0B/1M/2m — M1 genuine-contrast negative-anchor-literal-unspecified; m1 v0.55.0 META-1 em-dash+ISO header shape; m2 stale-ADR-excerpt **FALSE-POSITIVE**) M1/m1 ACCEPTED-FIXED, m2 OVERRIDDEN → **DR-1 EXTEND** (all 3 VALID / 0 suspicious / 0 severity; +1 MISSED **m-add-1** unbounded post-auto-invoke re-verification loop, ACCEPTED-FIXED) → TRI-1 user-ratified CLEAN. Calibration: M1/m1 VALIDATED, m2 FALSE-ALARM, m-add-1 VALIDATED-via-DR-1 (first-Critic missed the novel control-flow edge — the slice's raison d'être). | **No new risks**. ADR-048 partial-supersedes ADR-018 (STOP-route decision only); BFRD-1 Step 3c reclassified: unconditional STOP-route → `conditional confirm-then-auto-invoke` `/repro` edge (PCA-1 v0.41.0 alignment + 2026-05-19 user directive; confirm checkpoint preserves ADR-018 Failure-mode-2); v0.55.0 changelog; refines BFRD-1 terminal action, mints no new rule. STP-1 Sub-form A clean by construction (zero pre-existing test realignment). Discovered: first-Critic under-weights the novel control-flow edge on behavior-reclassification slices → DR-1 backstop (`/critic-calibrate` watch-list, N=1). Deferred: slice-047 `add-two-scope-install`. No BC-1 promotion (N=1 patterns journaled). Zero design deviations. |
| 045 | [slice-045-fix-install-pypi-package-name-and-stale-prose](slice-045-fix-install-pypi-package-name-and-stale-prose/) | 2026-05-19 | Standard | low (critic-required: true — in-house methodology surface: `INSTALL.md` is the INST-1 install recipe; shippability.md amended) | SHIPPED — validation PASS (5/5 ACs real-file evidence; VAL-1 both layers clean 0/0; WS-1/ETC-1 n/a default-off; TF-1 7/7 PASSING; all Step-6 audits green incl. BRANCH-1/UTF8-STDOUT-1/CRP-1/PCA-1/BCI-1/MCFS-1/STP-1/WIRE-1/LINT-MOCK-1 + CAD-1/PMI-1/INST-1; SCMD-1+PTFCD-1 pre-gates exit 0; shippability_runner **45/45 PASS 0 FAIL**; no VERSION bump — no-VERSION-bump conformance/prose-correctness class, MEPD-1(b) discharged) | **R-11 born-retired** (no ADR/no methodology-changelog entry/no VERSION bump). User-reported install-recipe defect (`/query-design`), BFRD-1 confirm-then-auto-invoke `/repro` → shippability #45. Fix: `INSTALL.md:93` `graphify`→`graphifyy` (PyPI dist only; module/CLI unchanged, grep-verified); 4× `v0.20.0` removed (:18→`v0.54.0 — see VERSION`; :26/:168/:232 reworded); `13`→`25` tool count (plugin.yaml-anchored, #45-guarded); README:69 + tutorial HTML:1050 name `graphifyy`. first-Critic NEEDS-FIXES (0B/1M/2m — M1 MEPD-1-not-discharged-by-name + precedent mis-cited reflection.md→risk-register.md; m1 :232-no-currency vs still-frozen install_audit.py; m2 AC4-phrase-pin) all ACCEPTED-FIXED → **DR-1 ACCEPT** (0 suspicious/0 missed/0 severity; first-Critic did NOT recur slice-040/R-10 freshly-minted-rule blind spot — invoked MEPD-1 by name; R-11 verified RR-1-clean + precedent-faithful) → TRI-1 user-ratified CLEAN. Calibration: M1/m1/m2 all VALIDATED, 0 FALSE-ALARM. | **No new risks** (R-11 born-retired). Discovered: TF-1 `ac-without-row` plan-completeness gap (a `Test-first:true` brief must map EVERY AC to a TF-1 row — audit ignores prose exemptions; NEW first-Critic-miss class, both layers missed it). Deferred: slice-046 `add-conditional-repro-auto-advance` (codifies user's confirm-then-auto-invoke `/repro` feedback), slice-047 `add-two-scope-install` (structural + ADR). No BC-1 promotion (2 N=1 patterns journaled; MEPD-1-enumeration-boundary → `/critic-calibrate` watch-list). Two strengthening deviations, no re-triage (:18 `— see VERSION`; AC4 real test-first regression test closing Critic-m2 gap). |
| 044 | [slice-044-add-state-transition-stale-pin-audit](slice-044-add-state-transition-stale-pin-audit/) | 2026-05-18 | Standard | medium (critic-required: true — in-house methodology surface: `tools/**`, `skills/build-slice/SKILL.md`, `methodology-changelog.md`) | SHIPPED-WITH-DEFERRALS — validation PASS (5/5 ACs real-artifact self-application dogfood; VAL-1 both layers clean 0/0; WS-1/ETC-1 n/a default-off; all Step-6 audits green incl. BRANCH-1/UTF8-STDOUT-1/CRP-1/PCA-1/BCI-1/MCFS-1/STP-1-self/PMI-1/INST-1/SCMD-1/PTFFD-1/DR-1/TRI-1; SRSC-1 runner **44/44 PASS**; full methodology suite **705 passed**; 4-part PMI-1 atomic 0.53.0→0.54.0) | **STP-1 minted** (ADR-047; v0.54.0; new NON-`-D` `vN.N` audit-gate, refines/supersedes nothing) — `tools/state_transition_pin_audit.py`, two git-diff-independent standing-invariant sub-forms (A SKILL.md-prose-repoint binding-tracer + B risk-status-stale vs live register via object-identity-reused `_parse_risks`). first-Critic BLOCKED (3B/3M/2m all ACCEPTED-FIXED) → DR-1 EXTEND (+B-add-1/M-add-1/m-add) → TRI-1 CLEAN → **plan-mode design-wrong DEVIATION** (Sub-form B git-merge-base inapplicable: vault gitignored) → targeted /critique BLOCKED (B4/B5/M4/M5/m3/m4) → DR-1 EXTEND (+m-add-R2-1) → Round-2 TRI-1 CLEAN. Calibration: ~20 findings 2-rounds×2-layers ~100% VALIDATED ~0 FALSE-ALARM (precise on what reviewed). | **5 build-time defects the dual-Critic+DR-1 stack structurally could not reach (gitignored-vault, `Or`-disjunction FP 8-live-pins, recursive double-count, markdown-`|`-in-regex column shift, cp1252 coverage-parity) — the slice-037/044 audit-vs-real-artifact law fired N≥5 inside its own construction (strongest /critic-calibrate input).** Deferred: Sub-form B literal-detection leg (false-positive-prone, no witnessed instance), ADR accepted→superseded sub-form (no machine signal), plan-mode-stage STP-1 invocation. **BC-PROJ-7** (new-audit-tool cp1252-list+pipe-free-shippability-row; Critical) + **BC-PROJ-8** (vault-targeting tools must use live on-disk reads not git; Important) promoted, BCI-1 PASS. |
| 043 | [slice-043-codify-split-slice-folder-naming-convention](slice-043-codify-split-slice-folder-naming-convention/) | 2026-05-18 | Standard | medium (critic-required: true — in-house methodology surface: `tools/branch_workflow_audit.py` + `CLAUDE.md`) | SHIPPED-WITH-DEFERRALS — validation PASS (4/4 ACs; VAL-1 both layers clean; WS-1/ETC-1/TF-1 n/a default-off; all Step-6 audits green incl. BRANCH-1/UTF8-STDOUT-1/CRP-1/PCA-1/BCI-1/MCFS-1/WIRE-1/BC-1/LINT-MOCK-1; SCMD-1+PTFCD-1 pre-gates exit 0; shippability_runner **43/43 PASS**; full methodology suite 680/680; no VERSION bump — conformance/convention-clarification class, why-none Builder-verified vs real META-1/PMI-1) | **R-6 RETIRED** (ADR-046; option (a) — codify convention + enrich rejection message, keep strict `\d{3}` accept regex): `tools/branch_workflow_audit.py` keeps `_SLICE_FOLDER_RE` strict (numeric-only accept UNCHANGED — behavior-preserving) + adds diagnostic-only `_SPLIT_SLICE_FOLDER_RE` (`^slice-(\d{3})([A-Z]+)-(.+)$`, uppercase-only per m1) consulted only after a strict miss to emit a convention-naming actionable `usage-error` (kind+exit-2 unchanged) for letter-suffixed split-slice folders; convention codified in CLAUDE.md Branch-per-slice; regression-pinned (genuine 3-FAIL→5-PASS contrast). first-Critic NEEDS-FIXES (0B/2M/2m — M1 wrong status-filter axis + M2 under-specified RR-1 `**Status**:`-flip + m1 regex lowercase over-match all ACCEPTED-FIXED; m2 shippability-cataloguing ACCEPTED-PENDING, discharged in-slice) → **DR-1 ACCEPT** (all 4 VALIDATED, Builder fixes did not relocate the flaw) → TRI-1 user "accept all". Calibration: M1/M2/m1/m2 all VALIDATED, 0 FALSE-ALARM. | **No new risks.** Deferred: none (m2 discharged in-slice). **BC-PROJ-6 promoted** (user-approved): risk-retirement edits must flip the `**Status**:` field line (RR-1's only parsed status surface) + re-derive the verification filter axis from the risk's actual prior state (open→retired ⇒ assert absent from `--filter-status open`, NOT `--filter-status mitigating`). Patterns: copy-a-sibling-pattern-without-recomputing-from-this-case's-actuals is the M1+M2 root; dual-Critic precision holds on conformance/tooling slices (4/4 VALIDATED); MEPD-1(b) Builder-recompute now routine at plan-mode. |
| 042 | [slice-042-realign-entry-present-pin-names-to-decoupled-shape](slice-042-realign-entry-present-pin-names-to-decoupled-shape/) | 2026-05-18 | Standard | low (critic-required: true — methodology surface tests/methodology/** + shippability.md) | SHIPPED — validation PASS (5/5 ACs; runner 42/42) | 13 (first Critic 3B+3M+1m NEEDS-FIXES → all ACCEPTED-FIXED; DR-1 EXTEND +B-add-1/2/3+m-add-1/2+M3-sev) all ACCEPTED-FIXED | FROZEN-proof-by-regex is measurement-fragile (→sha256); a /critique fix-prose edit re-introduced the literal it corrected (slice-032 class N+1, caught build-T0); recompute-don't-trust on own fix N≥4 → BC-PROJ-5 promoted |
| 041 | [slice-041-reframe-installed-pin-forward-sync-invariant](slice-041-reframe-installed-pin-forward-sync-invariant/) | 2026-05-18 | Standard | high (critic-required: true — in-house methodology surface: `tools/**`, `tests/methodology/**`, shippability catalog; documented M-add-1 relocation hazard) | SHIPPED — validation PASS (5/5 ACs real-artifact executed; VAL-1 both layers clean; WS-1/ETC-1/TF-1 n/a default-off; all 11 Step-6 audits green incl. CAD-1/PMI-1/INST-1/BCI-1/**MCFS-1 self-gating**/PCA-1/CRP-1/BRANCH-1/UTF8-STDOUT-1; SCMD-1 + PTFCD-1(b) pre-gates exit 0; shippability_runner **41/41 PASS**; full suite 714/714; 4-part PMI-1 atomic 0.52.0→0.53.0) | **MCFS-1 / R-4 RETIRED** (ADR-042 + ADR-043; v0.53.0; split-lineage "030C" — completes slice-030 split 030A-substance→030B-incidental→030C-essential): in-repo↔installed `methodology-changelog.md` forward-sync re-homed from ~33 per-version `test_v_0_NN_0_*` essential reads onto ONE **non-catalog whole-file EOL-agnostic gate** `tools/methodology_changelog_forward_sync.py` (BCI-1-analogue; local CRLF→LF norm CSP-1-parity-pinned to `_normalized_sha256`; installed-absent→WARN/divergent→HALT/empty≠absent; ungated `/build-slice` Step 6 + NEW non-Step-5b `/reflect` Step 5b-fs — m1); ~33 in-module reads decoupled (read-leg dropped BOTH syntactic forms — 268 lines/37 fns incl. 4 defined-but-uncited; in-repo assertions retained; **no rename — TRI-1 scope-cut** to a separate identifier-truth slice). `shippability_decoupling_audit` essential class reframed passive-recognized-never-flagged → **closed-world non-empty `_REGISTERED_INSTALLED_READERS`** registering exactly the slice-019 LAYER-EVID-1 cross-module pin ("the read **registered** not **absent**"; unregistered essential ⇒ exit 1, relocation-proof). **3-revision /critique convergence**: rev-1 BLOCKED (essential≠the-32) → rev-2 BLOCKED#2 (flaw RELOCATED — leg-drop can't reclassify the cross-module pin; rev-1 first-Critic + rev-1 DR-1 BOTH reasoned-not-executed `classify_fn`) → slice-030A/031 non-convergence **structural pivot** (not a rev-3 patch) → rev-3 first-Critic CLEAN + DR-1 EXTEND (execution-verified genuine; +m-add-1 two-syntactic-form leg, +m-add-2 regression-suite-not-cataloged). Calibration: rev-1/rev-2/rev-3 all-findings VALIDATED; **rev-1 DR-1 reasoned-not-executed false-confirm = highest-signal meta-Critic miss to date (strong /critic-calibrate input)**. 4 slice-owned harmonizations (slice-039 same-fix-block: incl. `test_r_4_..._stays_mitigating`→`_retired_by_slice_041`). R-4 `mitigating`→`retired`. | **No new risks.** Deferred: the `_entry_present_in_repo_and_installed`→`_entry_present` **rename** (TRI-1 scope-cut; DR-1-verified orthogonal to R-4; 41-changelog-line + 27-shippability + ~17-ADR frozen-history-vs-rewrite blast radius into append-only shipped history) → **strongest standing next-slice candidate** (`realign-entry-present-pin-names-to-decoupled-shape`). Patterns: DR-1 execute-don't-reason N≥3; state-transition slices must realign OLD-state pins same-fix-block; split→one-pass-convergence 3rd confirmation. |
| 040 | [slice-040-realign-validate-slice-step-5-5-prose-pin](slice-040-realign-validate-slice-step-5-5-prose-pin/) | 2026-05-18 | Standard | medium (critic-required: true — in-house methodology surface: `tests/methodology/**` + `methodology-changelog.md`) | SHIPPED — validation PASS (5/5 ACs real-artifact; AC2 non-tautology proven via in-memory monkeypatch; VAL-1 clean; WS-1/ETC-1/TF-1 n/a default-off; all 12 Step-6 audits green incl. CAD-1/PMI-1/META-1/BCI-1/PCA-1/CRP-1/BRANCH-1/SCMD-1/PTFCD-1; shippability_runner 40/40 PASS; no VERSION bump — PMI-1 stays 0.52.0) | **R-10 retired** (slice-040; conformance-fix class; no ADR / no rule-ID / no VERSION bump — MEPD-1(b) discharged vs the real META-1 `re.split(r"^## v\S+ — date")` assertion at `test_methodology_changelog.py:136`, slice-036/R-9 corroborating not load-bearing — slice-032 false-precedent guard): the slice-031 SCMD-1 B2-v1 mini-CAD prose-pin `test_step4_5_5_consumes_machine_stable_command` realigned to the SRSC-1 runner-invocation wording slice-038 shipped (dead `Run each entry's **Machine-cmd** column` anchor → `$PY -m tools.shippability_runner architecture/shippability.md` + `canonical pinned runner`; SCMD-1/PTFCD-1 siblings kept verbatim; docstring carries slice-031→SRSC-1 supersession lineage; function not renamed). Closes the slice-038-rooted methodology-suite false-FAIL (659/1 → 660/0). **first-Critic NEEDS-FIXES** (M1 `### Fixed`-heading-absent + M2 missing-META-1-Rule-reference → Builder's deeper precedent-faithful fix DROP-the-changelog-entry-entirely; m1 watch-list-canonical-surface; m2 grep-disambiguation) all ACCEPTED-FIXED; **DR-1 EXTEND** (no SUSPICIOUS, 4 CONFIRMED + **M-add-1**: MEPD-1 — minted slice-039 to govern exactly this no-changelog decision — never invoked by name, missed on the first slice it governs; substance already compliant, discipline-citation added) ACCEPTED-FIXED; **TRI-1 CLEAN** (user "accept all"). Calibration: M1/M2/m1/m2 VALIDATED, **M-add-1 VALIDATED (load-bearing DR-1 self-hosting catch)**; DR-1 keeps paying on methodology-surface slices N+1 (026/029/038/039/040). | **New blind-spot class: a rule minted in slice N is a first-Critic blind spot on slice N+1 (the first slice it governs)** — DR-1 is the backstop; `/critic-calibrate` watch-list. **Mini-CAD prose-pins must be catalogued** (Step 5.3) — uncatalogued = invisible to the catalog runner (the slice-038→R-10 ~5-slice detection-latency class); slice-040 catalogued the realigned pin as #40. Deferred: `-D` rule for "SKILL.md-repointing must supersede its mini-CAD prose-pin" still N=1 (slice-038 miss, slice-040 fix) — 2nd occurrence promotes. |
| 039 | [slice-039-apply-critic-calibrate-proposals-to-critique-agent](slice-039-apply-critic-calibrate-proposals-to-critique-agent/) | 2026-05-18 | Standard | medium (critic-required: true — in-house methodology surface: `agents/critique.md` + `methodology-changelog.md` + `tests/methodology/**`) | SHIPPED-WITH-DEFERRALS — validation PASS (5/5 ACs real-artifact; VAL-1 clean; WS-1/ETC-1 n/a default-off; TF-1 default-off; all 13 Step-6 audits green incl. CAD-1/PMI-1/INST-1/BCI-1/PCA-1/CRP-1/SCMD-1/PTFFD-1-path; shippability_runner 39/39 PASS; 4-part PMI-1 atomic 0.51.0→0.52.0) | **APED-1 + MEPD-1** (ADR-040+ADR-041; v0.52.0): the two user-ACCEPTED 2026-05-17 `/critic-calibrate` proposals applied to `agents/critique.md` — APED-1 = Dim 9 sub-clause #12 (Bash-execute a changed audit parse-rule vs an adversarial battery; Blocker on silent-disable/default-off/false-FAIL/over-match; executed-not-reasoned; N=3-MISS 030A/031/033 + N=1-CATCH 034); MEPD-1 = Dim 7 checklist sub-bullet (methodology-surface RULE-ID + entry-pin obligation; (a) rule-path OR (b) verified-against-artifact why-none; **first deliberately non-Dim-9 `-D` rule** per ADR-040 — `-D` = behavioral class not Dim-9 membership). Both new minted `-D`, refine/supersede nothing. `_lists_eleven`→`_lists_twelve` structural-invariant supersession (N=7) + 5 RPCD-1 sibling end_anchor tightens + 14 SCPD-1 selector-token renames across rows 6/11/13/15/16/24/25; content-pins behavioral not CAD-1-tautology (slice-037 M-add-1). **first-Critic NEEDS-FIXES** (M1 APED-1-content-pin-lacks-behavioral-verbs; m1 changelog:266-frozen-enumeration; m2 SCPD-1-count/formulation) all ACCEPTED-FIXED; **DR-1 EXTEND** M-add-1 (shippability line-34 = 3rd FROZEN slice-025 occurrence → count 15 not 14; m2's `not in catalog` pin a GUARANTEED false-FAIL; selector-prefix discriminator reformulation — re-opened m2, mechanically verified on real catalog) ACCEPTED-FIXED; **TRI-1 CLEAN** (user ratified 4). Calibration: M1/m1 VALIDATED, m2 VALIDATED-IN-DIRECTION-but-remedy-SUPERSEDED, **M-add-1 VALIDATED (load-bearing DR-1 catch — averted guaranteed /validate false-FAIL)**; DR-1 keeps paying on codification slices N+1. | **R-10** (open; high/low; cheap): slice-038 SRSC-1 left a stale slice-031 SCMD-1 prose-pin — `test_step4_5_5_consumes_machine_stable_command` asserts the pre-SRSC-1 Step-5.5 wording, failing slice-innocently on master since slice-038 (NOT catalogued → catalog regression unaffected; rigorously proven git-identical-to-master + zero slice-039 surface). A slice-038 reflection gap (SCPD-1/mini-CAD propagation applied to prose-pins). **Strongest standing follow-up candidate.** |
| 038 | [slice-038-pin-shippability-runner-segment-contract](slice-038-pin-shippability-runner-segment-contract/) | 2026-05-17 | Standard | medium (critic-required: true — in-house methodology surfaces: `tools/*.py` + `skills/validate-slice/SKILL.md` + `methodology-changelog.md`) | SHIPPED — validation PASS (5/5 ACs real-evidence; VAL-1 0 secrets/0 imports; WS-1/ETC-1 n/a; TF-1 8/8 PASSING; all Step-6 audits green incl. BCI-1/PCA-1/CRP-1/SCMD-1; **SRSC-1 dogfood: new runner ran full real 38-row catalog 38/38 PASS, row #28 PASS no WinError 2**; 4-part PMI-1 atomic 0.50.0→0.51.0) | **SRSC-1** (v0.51.0): retires **R-8** — `/validate-slice` Step-5.5 catalog runner pinned behind an INVOKED `tools/shippability_runner.py` that **reuses SCMD-1 `_segments()`** by object identity (imports `_segments`/`_catalog_rows`/`_machine_cmd_cell` from `tools.shippability_decoupling_audit` — NOT re-derived; CSP-1 cross-`tools` reuse pattern). SKILL.md Step 5.5 item 4 invokes it + "do NOT hand-roll the execution loop" pin (in-repo+installed). Closes the R-8 false-FAIL class that recurred N=2 (slice-032/033) DESPITE pre-existing SKILL.md prose — prose binds nothing executable. NEW non-`-D` `vN.N` rule, supersedes nothing (SCMD-1 owns *grammar*, SRSC-1 owns *execution*; shared `_segments()` artifact only; TFFL-1↔TF-1 in-place precedent rejected). Dual enum (install_audit `_CANONICAL_TOOLS` 22→23 + plugin.yaml); paired SCPD-1 content-pin changelog tests; shippability #38 | **first-Critic NEEDS-FIXES** (B1 new-`main()`-breaks-UTF8-STDOUT-1-parity=row-#28-dogfood [slice-022 self-violation; Builder's own draft had waved it off]; B2 SCPD-1 paired-changelog-test omitted; M1 presence-vs-content-pin; M2 PMI-1 3-vs-4-part; m1 dual-enum-surface; m2 tautology-vs-contrast) all ACCEPTED-FIXED; **DR-1 ADJUST** (B2 Blocker→Major: obligation real but N=1 precedent not N≥3 — Builder evidence-correction confirmed; +missed m-add-1 stale "18 entries"=actual 22) ACCEPTED-FIXED; **TRI-1 CLEAN** (user ratified 7) | **ADR-039** (cheap, supersedes null): pin Step-5.5 behind invoked `tools/shippability_runner.py` reusing SCMD-1 `_segments()`; SRSC-1 new non-`-D` rule. **R-8 → retired** (durable: load-bearing naive-strip-vs-`_segments()` contrast on real row #28 + catalogued shippability #38 + SRSC-1 dogfood). No new risk. **7/7 VALIDATED, 0 FALSE-ALARM; 1 Critic-stack MISS — BOTH layers blessed `_POSITIONAL_SLICE_TOOLS` (passes a slice-folder arg; runner needs a catalog file); meta-Critic explicitly "verified correct bucket" by reasoning about `_positional_slice_argv` shape without checking the sibling precedent or running argv. Caught only at /build-slice reading the sibling shippability tools' non-membership. slice-032 reason-about-the-claim law N+1; slice-034 empirical-execute remedy would have caught it; build-time artifact-read is the structural backstop (no new Critic dimension). DR-1 reliably recalibrates first-Critic single-precedent→Blocker inflation.** |
| 037 | [slice-037-extend-ptfcd-1-to-test-function-level](slice-037-extend-ptfcd-1-to-test-function-level/) | 2026-05-17 | Standard | medium (critic-required: true — In-house methodology surfaces: `tools/*.py` + `agents/critique.md` + `methodology-changelog.md`) | SHIPPED — validation PASS (AC1-5 PASS, 19 PTFFD-1 tests fresh; VAL-1 0 secrets/0 imports; WS-1/ETC-1 n/a; TF-1 19/19 PASSING; all Step-6 audits clean; **shippability 37/37 PASS no regression**; 682/682 pytest; 4-part PMI-1 atomic 0.49.0→0.50.0) | **PTFFD-1** (v0.50.0): FILE-level PTFCD-1 (slice-025) extended to test-FUNCTION level — shared `tools/_pyfn.py` tri-state helper across `test_first_audit.py` + `shippability_path_audit.py`; a PASSING TF-1 row / shippability `::`-selector whose file exists but cited fn absent → `missing-test-function`; closes N=3 Critic blind spot (slice-025 AC3 + slice-026 AC5 + slice-027 B1); prose value degrades to FILE-level-only (B2); unparseable→skip-with-note (ADR-037); NEW `-D` ID refines PTFCD-1 in place, supersedes nothing (NOT `PTFCD-1 v1.1`; TFFL-1↔TF-1 precedent) | **first-Critic BLOCKED** (B1 own-AC4-phantom=slice-022 self-violation; B2 prose-discriminator unmet on slice-034 real row; B3 `-D`-vs-`vN.N`; M1 SCPD-1 row-37; M2 ADR-037 observability undelivered; M3 `::`-strip double-source-escape; m1/m2) all ACCEPTED-FIXED; **DR-1 EXTEND** (+M-add-1 AC4 tautological-green; +M-add-2 row-37 2nd-order catalog self-application; +m-add-1 `to_dict()` undeclared; +m-add-2 both-cols-disagree) ACCEPTED-FIXED; **TRI-1 CLEAN** (user ratified 12) | **ADR-037** unparseable→skip-with-note + M2 observability + M3 precedence + m-add-2; **ADR-038** PTFFD-1 new `-D` ID refines PTFCD-1 in place, supersedes nothing. **12/12 VALIDATED, 0 FALSE-ALARM; 2 Critic-stack MISSES — `tests/tools/`→`tests/methodology/` phantom-path (slice-027-B1 class) caught at /build-slice plan-mode; row-37 SCMD-1 incidental-coupling caught at pre-finish SCMD-1 gate. slice-022 self-violation law at maximal depth; audit-codification self-applies at 3 layers (TF-1-plan / catalog-row-resolution / catalog-row-incidental-coupling).** |
| 036 | [slice-036-fix-rr1-audit-status-filter](slice-036-fix-rr1-audit-status-filter/) | 2026-05-17 | Standard | medium (critic-required: true — in-house methodology surface: `tools/risk_register_audit.py`) | SHIPPED — 4/4 ACs PASS real-evidence (real register: `--filter-status open`→only open, `--filter-band high`→only high, `--top 3 --sort score`→3 score-desc `[6,4,4]`, no `view` key; 30/30 RR-1 module; VAL-1 0 secrets/0 imports; all Step-6 audits green incl. BCI-1/PCA-1/CRP-1); /validate **PASS** — shippability **36/36 no regression** (incl. new #36) | **R-9 RETIRED** (ADR-036; cheap; NO RULE-ID / NO version bump — conformance bug fix, RR-1 rule unchanged; PMI-1 clean at 0.49.0, changelog suite 70/70 without entry). `main()` `if args.json:` emitted unfiltered `result.to_dict()["risks"]`; filtered list went to redundant `out["view"]`; `/pulse`+`/slice` read `risks` → `--filter-status/--filter-band/--top` silently ignored in JSON (latent footgun, NOT live corruption — consumers read per-risk `status`). Fix: 1 line `out["risks"] = [r.to_dict() for r in view]`, drop `view`; `summary` register-wide. BFRD-1: `/repro` shippability #36 first. 6 tests (repro + 5 pins). | **first-Critic** NEEDS-FIXES (B1 Intent-vs-authoritative-R-9 contradiction, B2 CSP-1-mis-cited-Heavy-only-no-op, M1 no-flag-ordering-unpinned, M2 band/top-oracle-fragility, m1 stale-lines, m2 AC3-gate-cmd) → all ACCEPTED-FIXED pre-triage; **DR-1 EXTEND** (+M-add-1: B2-replacement prose-pin tests over-claimed — `*_references_rr_1` pin only bare RR-1 substring not flag/exclusion-prose) → ACCEPTED-FIXED → **TRI-1 CLEAN** (user ratified 7). 7/7 VALIDATED zero-false-alarm at reflection. **Missed by Critic-stack (3)**: (1) blessed "AC3 carve-out survives strict TF-1" — `test_first_audit --strict-pre-finish` refused `ac-without-row` (slice-022 self-violation N+1; BC-PROJ-4 caught); (2) proposed factually-wrong oracle `{R1,R3}` (R3 medium band — reasoned without computing `_band_for_score`; plan-mode caught); (3) never raised methodology-changelog/RULE-ID obligation. | **ADR-036** (cheap): single filtered `risks` list, drop `view`, `summary` register-wide; no RULE-ID (conformance fix; RR-1 unchanged); empirically not gate-required (slice-029 rule-ID-less precedent). **R-9 → retired** (durable repro = shippability #36; +5 contract/oracle/prose pins incl. durable M-add-1 prose-pin). No new risk. **Pattern: dual-Critic stack high filing precision (7/7 VALIDATED) but 3 misses = exactly 2026-05-17 /critic-calibrate ACCEPTED Proposals 1+2 (audit-self-violation; RULE-ID-obligation) still NOT applied to agents/critique.md → strong pre-codification baseline; BC-PROJ-4 real-artifact pre-finish run remains the structural backstop. New watch sub-class N=1: Critic must COMPUTE fixture-derived expected values not eyeball.** **Strongest next**: (a) **slice-030C** (essential-coupling reframe; R-4→retired); (b) `pin-shippability-step-5.5-runner-contract` (R-8, N≥2); (c) **apply 2026-05-17 /critic-calibrate Proposals 1+2 to agents/critique.md** (3rd consecutive recurrence); (d) `/critic-calibrate` window-run (16-36). |
| 035 | [slice-035-rename-status-skill-to-pulse](slice-035-rename-status-skill-to-pulse/) | 2026-05-17 | Standard | medium (critic-required: true — in-house methodology surfaces: skills/, plugin.yaml, tools/, methodology-changelog.md) | SHIPPED — 5/5 ACs PASS real-evidence (name:pulse + no skills/status/ + colliding triggers dropped + registry reload shows /pulse; PMI-1 clean v0.49.0; INST-1 25/25; full methodology suite 616 pass; VAL-1 0 secrets/0 imports; all Step-6 audits green; Bucket A post-edit grep EMPTY); /validate **PASS** — shippability **35/35 no regression** (row #28 R-8 ad-hoc-runner outer-backtick false-FAIL → PASS under correct per-`;`-segment parse) | **SRCD-1 minted** (ADR-035): `/status`→`/pulse` resolving the Claude Code built-in `/status` collision; no backward-compat alias. Bucket A/B/C reference taxonomy (executable filesystem-path binds vs prose vs frozen-as-history) + mandated pre+post-edit inventory grep. 6 `test_status_*`→`test_pulse_*` fn renames; `test_risk_register_audit.py:334`/`test_skill_model_dispatch.py:25` path binds repointed. 4-part PMI-1 atomic bump 0.48.0→0.49.0 + `test_v_0_49_0_srcd_1` entry-pin + shippability #35. `agents/critique.md` NOT touched (no `/status` skill ref; CAD-1 N/A). | **first-Critic** BLOCKED (5B/3M/2m) → all ACCEPTED-FIXED + disciplined B3 anti-over-reach retraction (false `critique.md:194` citation); **DR-1** EXTEND (+4 missed: **B-add-1 load-bearing** 4-part-bug-vs-"triple"; M-add-1 changelog `## How /status` live section; M-add-2 SRCD-1 shippability row; m-add-1 README:149 filename) → all ACCEPTED-FIXED → **TRI-1 CLEAN** (user ratified all 14). first-Critic 10/10 VALIDATED at reflection; all 4 DR-1 VALIDATED. **Missed by first-Critic**: B-add-1 (caught by DR-1 — installed-side-config blind spot, slice-006/007 recurrence N≥3). | **ADR-035** (cheap): rename + no-alias + SRCD-1 + 4-part-bump + frozen-as-history exclusions (ADR-030 corpus, lessons-learned.md:414, historical changelog narratives). methodology **v0.49.0** (in-repo+installed); PMI-1 atomic 0.48.0→0.49.0; INST-1 25-skill lockstep (rename-in-place, no tool delta). No new risk (R-8 re-confirmed, already open — ad-hoc Step-5.5 runner outer-backtick footgun; runner contract still unpinned). **Pattern: dual-Critic stack at its best — first-Critic 10/10 + B3 retraction; meta-Critic earned its cost (B-add-1 only caught by DR-1). Strong /critic-calibrate input: first-Critic Dim-7/9 sub-bullet enumerating all 4 PMI-1 atomic-bump legs incl. installed `~/.claude/ai-sdlc-VERSION`.** **Strongest next**: (a) `pin-shippability-step-5.5-runner-contract` (R-8, N≥2); (b) `/critic-calibrate` window-run (16-35 — incl. installed-side-config + empirical-regex-execution Dim-7/9 candidates); (c) `fix-rr1-filter-status-open` (R-9). |
| 034 | [slice-034-fix-tf1-audit-field-line-regex](slice-034-fix-tf1-audit-field-line-regex/) | 2026-05-17 | Standard | medium (critic-required: true — in-house methodology surface: `tools/test_first_audit.py` + `methodology-changelog.md`) | SHIPPED — 5/5 ACs PASS real-evidence (repro 3/3; AC3 real-artifact malformed brief → loud `malformed-test-first-field` exit 1 attributed, not silent; BC-PROJ-4 dogfood on own bare-field brief = "clean. 9 row(s)" ENABLED; TF-1 strict 9/9 PASSING; VAL-1 clean; full suite 656 pass; all Step-6 audits green); /validate **PASS** — shippability **34/34 no regression** (row #28 R-8 multi-segment PASS under correct per-`;`-segment parse) | **R-7 RETIRED** (TFFL-1; ADR-034): `_TEST_FIRST_FIELD_RE` → standalone-token lookahead `(true\|false)(?=[\s(]\|$)` (annotated true engages; `false-positive`/`true.` do NOT silently parse) + value-agnostic field-present matcher → loud `malformed-test-first-field` (never silent default-off) + `_format_human` guard ("not enabled" only on genuine absence); same `(true\|false)` matcher → `**Test-first**: false` clean (M2 invariant). RULE-ID **TFFL-1** minted (refines TF-1 in place, supersedes nothing); 4-part atomic PMI-1 0.47.0→0.48.0 + `test_v_0_48_0_tffl_1` entry-pin. | **first-Critic** NEEDS-FIXES (0B/2M [M1 regex `\b`-overmatch self-violation, M2 both-booleans invariant]/2m) → all ACCEPTED-FIXED; **DR-1** EXTEND (+**M-add-1** missed Major: RULE-ID/`test_v_0_48_0_*` entry-pin obligation — slice-032 DEVIATION-1 class) → ACCEPTED-FIXED → **TRI-1 NEEDS-FIXES** (user ratified all 5). All VALIDATED at reflection. **Missed by first-Critic**: M-add-1 (caught by DR-1 — dual-stack worked). **Counter-move**: M1 (R-7-class self-violation in the fix's own regex) was CAUGHT at /critique by the first Critic *empirically running the proposed pattern* — first design-stage catch of the audit-vs-artifact class slice-031/033 deemed Critic-unreachable. | **ADR-034** (cheap): relax-AND-loud-malformed (Option 3); RULE-ID **TFFL-1** (refines TF-1; supersedes nothing; EOL-DRIFT-1↔CAD-1 lineage); methodology **v0.48.0** (in-repo+installed); PMI-1 atomic 0.47.0→0.48.0; INST-1 lockstep (no tool). **R-7 → retired** (durable repro = shippability #34). **R-9 opened**: `tools.risk_register_audit --filter-status open` lists RETIRED risks (pre-existing — R-5+R-7 both appear; consumers read `status` field; out-of-scope, low/cheap). **Pattern: slice-022 self-violation law ~N≈11; the empirical-regex-execution Critic prompt is a second backstop beside BC-PROJ-4 — strong /critic-calibrate input.** **Strongest next**: (a) **slice-030C** (essential-coupling reframe; R-4→retired); (b) `pin-shippability-step-5.5-runner-contract` (R-8, N=2); (c) `/critic-calibrate` window-run (16-34 — incl. empirical-regex-execution + RULE-ID-checklist Dim-7/9 candidates); (d) `fix-rr1-filter-status-open` (R-9). |
| 033 | [slice-033-fix-skill-drift-test-crlf-normalization](slice-033-fix-skill-drift-test-crlf-normalization/) | 2026-05-17 | Standard | medium (critic-required: true — in-house methodology surface: `tools/critique_agent_drift_audit.py` + CAD-1/mini-CAD self-hosting discipline) | SHIPPED — 5/5 ACs PASS real-evidence (13 skill-drift+CAD-1 pass under autocrlf=true; repro that FAILed N+2 now PASSES; AC2 must-not-mask both surfaces; AC3 working-tree-state no-CRLF; AC5 governing-surface pins; VAL-1 clean; full suite 648 pass; all Step-6 audits green); /validate **PASS** — shippability **32/32 no regression** (R-5 rows #1: 41 passed, #19: 12 passed) | **R-5 RETIRED** environment-independently: shared `tests/skill_drift_equality.py::assert_md_forward_synced` (CRLF→LF normalized) sole comparator for 5 skill-drift modules; CAD-1 `_sha256_of` same normalization (exit-code contract unchanged; existing genuine-divergence tests bound as must-not-mask proof); `.gitattributes` guarded-surface `eol=lf` + targeted renormalize; governing surface in-slice (changelog v0.47.0 EOL-DRIFT-1 + atomic 4-part bump 0.46.0→0.47.0 + CLAUDE.md L33/L36). | **first-Critic** NEEDS-FIXES (2B/4M/2m) → all ACCEPTED-FIXED; **DR-1** ADJUST (M3 SEVERITY-WRONG Major→Minor; +m-add-1 minted RULE-ID `EOL-DRIFT-1` + restored 007–032 pin-name parity; m-add-2 informational) → **TRI-1 CLEAN**. All findings VALIDATED at reflection (M1 confirmed EXACTLY — .gitattributes index-only, working tree needed re-checkout). **Missed by Critic**: R-7 field-line-annotation silent-bypass — both first-Critic AND meta-Critic missed it despite R-7 OPEN; caught only by BC-PROJ-4 real-artifact pre-finish gate run. | **ADR-033** (cheap): `.md` drift guards EOL-agnostic; RULE-ID **EOL-DRIFT-1**; methodology **v0.47.0** (in-repo+installed); PMI-1 atomic 0.46.0→0.47.0; CLAUDE.md L33/L36 "MUST be byte-equal"→"content-equal modulo line endings". **R-5 → retired** (`;`-split residual split out). **R-7 N+1** reproduced LIVE at pre-finish (TF-1 silent default-off; fixed bare field-line + HTML comment). **R-8 opened**: Step-5.5 runner `;`-split/per-segment-backtick contract unpinned. shippability row 33. **Pattern: slice-022 self-violation law ~N≈10; BC-PROJ-4 is the structural backstop the Critic stack cannot reach.** **Strongest next**: (a) **slice-030C** (essential-coupling reframe; R-4→retired); (b) `fix-tf1-audit-field-line-regex` (R-7, N+1, regex-hardening); (c) runner-hardening for R-8; (d) `/critic-calibrate` window-run (16-33 — incl. the R-7 dual-Critic blind spot). |
| 032 | [slice-032-add-query-design-skill](slice-032-add-query-design-skill/) | 2026-05-17 | Standard | medium (critic-required: true — in-house methodology surfaces: skills/, plugin.yaml, tools/, methodology-changelog.md) | SHIPPED-WITH-DEFERRALS — 5/5 ACs PASS real-evidence (drift+prose-pin+PMI-1+INST-1+test_v_0_46_0 green; VAL-1 clean; full methodology suite 601 pass; all Step-6 audits clean); /validate **PASS** (0 slice-032 regressions; #28 ad-hoc-runner non-`;`-split false-FAIL; #1/#19 pre-existing slice-innocent diagnose-drift **R-5 N+2** user-approved-deferred → backlog `fix-skill-drift-test-crlf-normalization`) | New out-of-loop read-only `/query-design` skill: truthful grounded codebase Q&A, changes nothing, declinable `/slice` handoff (never authors). SKILL.md prose-contract pinned (loophole-free read-only invariant + grounding + delegation + 3 error-model clauses + no `## Pipeline position` heading) + byte-equal drift test; AC4 = deterministic prose-pin (replaces slice-022-violating dry-run per /critique B3). 4-part PMI-1 atomic bump 0.45.0→0.46.0; INST-1 25-skill lockstep; `test_v_0_46_0_qd_1` rule-ID-bearing 4-assertion entry-pin; canonical phrase 2-site pin. | **v1**: /critique CLEAN (3B/3M/2m all ACCEPTED-FIXED, m2 OVERRIDDEN) + /critique-review EXTEND (M-add-1 INSTALL 3-not-1-site). **DEVIATION-1**: `/build-slice` plan-mode caught design.md m1 citing a *non-existent* "slice-029 no-rule-lineage precedent" (24/24 versions pinned, slice-029 incl.) — false precedent the **full v1 dual-Critic stack rubber-stamped**. User→correct-design+full-re-critique: **v2** /critique NEEDS-FIXES (M1-v2 correction self-committed the slice-022 violation it cited; M2-v2 bearing-vs-less; m1-v2 format) + /critique-review-v2 EXTEND (M-add-v2-1 fix re-introduced single-site silent-drift N=2) → all ACCEPTED-FIXED → CLEAN. All v1+v2 VALIDATED at reflection except m1=**OVERRIDE-MISJUDGED** (false precedent), m2=FALSE-ALARM. **Missed by Critic**: v1 first-Critic AND meta-Critic both rubber-stamped the false slice-029 precedent — caught only at build-plan-mode. | **ADR-032** (cheap): `/query-design` out-of-loop read-only delegation-only; no `## Pipeline position` block; declinable `/slice` handoff; read-only enforced by prose+QD-1+drift-test (no `allowed-tools` — 0 in-repo skills use it). methodology **v0.46.0** (in-repo+installed); PMI-1 atomic 0.45.0→0.46.0; install_audit+plugin.yaml QD-1 25-skill lockstep; INSTALL.md 3 hard-count sites→count-agnostic; shippability row 32. **R-5 N+2** recurrence logged + catalog-runner `;`-split discovery. **Strongest next**: (a) `fix-skill-drift-test-crlf-normalization` (R-5 N+2, overdue; also pin Step-5.5 runner `;`-split); (b) **slice-030C** (essential-coupling reframe; R-4→retired); (c) `/critic-calibrate` window-run (16-32 — incl. the dual-Critic-stack false-precedent blind spot). |
| 031 | [slice-031-complete-shippability-decoupling](slice-031-complete-shippability-decoupling/) | 2026-05-17 | Standard | high (critic-required: true — in-house methodology surface + catalog-schema change; split-lineage label "030B") | SHIPPED-WITH-DEFERRALS — 5/5 ACs PASS real-evidence (SCMD-1 self-run clean incidental=0; 632 full-suite pass; all Step-6 audits green; BC-PROJ-4 + BCI-1 recheck PASS); /validate **PARTIAL → user-approved R-5 deferral** (shippability #1/#19 = pre-existing slice-innocent diagnose-skill-drift CRLF artifact N+1; identical rows+test slice-030A deferred; R-5/D-1 out-of-scope per TRI-1-ratified mission-brief). SCMD-1: 6th machine-stable Machine-cmd column + all-rows-derived incidental-decoupling invariant; 11 incidental backtests → slice-030A canonical fixtures + git-tracked 8-folder verbatim corpus; essential ~20 entry-pins recognized-NOT-flagged → chartered **slice-030C**; R-4 stays mitigating | **User-ratified b-split CONVERGED**: v1 /critique BLOCKED (3B/4M/2m all VALID) + /critique-review EXTEND(+M-add-1: in-place mechanism relocates 4th time) → **TRI-1 b-split** → redesign incidental-only → v2 /critique NEEDS-FIXES (2B/2M/2m ACCEPTED-FIXED) + /critique-review EXTEND(+M-add-A: prose-discriminator asserted-not-pinned) → CLEAN. v2 6/6 + M-add-A all VALIDATED at reflection (100% post-split). **Missed by all 4 passes**: 3 mid-slice-smoke-caught classifier bugs + R-6 + R-7 — methodology-audit-interaction blind spot, design-stage-invisible (slice-024/029/030A class N+3). Builder D-3 watch PASS (meta spot-checked substantive). | **ADR-030** (cheap): archive-backtest verbatim git-tracked corpus, derived-not-listed; **ADR-031** (cheap): SCMD-1 machine-stable column + closed-world incidental-decoupling + principled essential-class exclusion. methodology **v0.45.0** (in-repo+installed); PMI-1 atomic 0.44.0→0.45.0; install_audit+plugin.yaml SCMD-1 tool; UTF8-STDOUT-1 covered-set extended; shippability row 31; **BC-PROJ-4 promoted** (Important; canonical-fixture+literal-pin+BCI-1). **R-6** (BRANCH-1 numeric-NNN vs split-label folder), **R-7** (TF-1 field-line trailing-annotation silently disables gate) added; **R-4 stays mitigating** (030C-gated); R-5 N+1 recurrence noted. Folder/branch corrected `slice-030B-`→`slice-031-` mid-build. **Strongest next**: (a) **slice-030C** (essential-coupling reframe + M-add-1; R-4→retired); (b) `fix-skill-drift-test-crlf-normalization` (R-5, N+1); (c) methodology-hardening for R-6/R-7; (d) `/critic-calibrate` window-run (16-31). |
| 030 | [slice-030-repair-build-checks-vault-and-harden-shippability](slice-030-repair-build-checks-vault-and-harden-shippability/) | 2026-05-16 | Standard | medium (critic-required: true — in-house methodology surface) | SHIPPED-WITH-DEFERRALS (030A split scope) — 5/5 ACs PASS real-evidence (mid-slice smoke 39/0; BCI-1 self-run exit 0; PMI-1/INST-1 v0.44.0); VAL-1/PTFCD-1 clean; /validate PASS w/ **user-approved deferral** of spurious CRLF/LF shippability #1/#19 (content byte-identical — R-5). **030B deferred** (user split): shippability #5/#8/#12 + changelog-test decoupling, machine-stable command column, meta-M1′ archive-backtest fidelity | **2 BLOCKED dual-Critic loops → meta-Critic non-convergence signal → user SPLIT**. v3 /critique NEEDS-FIXES→CLEAN (B1/B2/B3/M1/M2 + m1/m2 ACCEPTED-FIXED; 5/5 VALIDATED) → /critique-review v1 EXTEND (+M-add-1 circular hole), v2 EXTEND (+M-add-1/2/3 non-convergence), v3 ADJUST (M1→ADR-028 propagation, completed). **Missed by all 4 passes: CRLF/LF drift-test fragility (R-5) — died at real-command validate (slice-024/029 class N+1).** Process: 2-loop autonomous rubber-stamping + v1-audit-trail overwrite (→critique-history-v1.md) + silently-weakened ratified disposition — /critic-calibrate signal | **ADR-028** (cheap): all-5-rule literal-constant tracked-fixture oracle + non-opt-out BCI-1; **ADR-029** (cheap): `/reflect` Step5b has no deterministic source → enforce invariant downstream. **BCI-1** rule (NON-`-D` audit-gate; ADR-019); methodology **v0.44.0** (in-repo+installed); PMI-1 atomic 0.43.0→0.44.0; install_audit 20→21 tools; shippability row 30. **R-4 open→mitigating** (substance retired; catalog residual=030B); **R-5 added** (skill-drift-test CRLF/LF, Windows autocrlf). Strongest next: (a) **slice-030B** (deferred decoupling + machine-stable command column); (b) `fix-skill-drift-test-crlf-normalization` (R-5); (c) `/critic-calibrate` window-run (16-30, rubber-stamp pattern). |
| 029 | [slice-029-make-diagnose-dispatch-sequential](slice-029-make-diagnose-dispatch-sequential/) | 2026-05-16 | Standard | medium (critic-required: true — in-house methodology surface: `skills/diagnose/SKILL.md`) | SHIPPED-WITH-DEFERRALS — slice ACs 1-5 PASS w/ real evidence (AC2 = real-shell exec of the 3-arm flag-strip under Git-Bash 5.2.37 MINGW64, 7/7 correct; M-add-1 portability empirically discharged); /validate-slice **PARTIAL** — shippability surfaced 3 PRE-EXISTING non-slice-029 FAILs (#5/#8/#12, gitignored local `build-checks.md` truncated pre-slice) → user-approved deferral + R-4 logged | **/critique NEEDS-FIXES** (3B+3M+2m ALL ACCEPTED-FIXED) → **/critique-review EXTEND** (8/8 confirmed VALID, M2 SUSPICIOUS-partial re-scoped, +M-add-1..4 ALL ACCEPTED-FIXED). 13/13 VALIDATED, 0 FALSE-ALARM, 0 OVERRIDE-MISJUDGED, 0 true MISS. **slice-022 self-violation law N≈9** — meta-Critic predicted + pinpointed M-add-2 (ADR-027 Reversibility still said "retire" inside M1's own fix artifact). First-Critic "all-8-ACCEPTED-FIXED" = predicted too-easy signature; meta-Critic re-audited the *applied fixes* and caught the residuals. | **ADR-027** (cheap): `/diagnose` Step-5 dispatch sequential-by-default + `--parallel` opt-in; **methodology v0.43.0 `### Changed`, NO rule-ID** (TRI-1 M2 option B); R-1 open→**mitigating** (+RR-1 Mitigation field); 4 prose-pins; CSP-1 + LAYER-EVID-1 N=6 + COST-1.1 byte-verbatim; PMI-1 atomic 0.42.0→0.43.0; shippability row 29. **R-4 logged** (local build-checks.md corruption — BC-1 coverage degraded; suspected `/reflect` Step-5b append-vs-overwrite defect; failing repro `test_build_checks_audit.py` already exists). BC-1 promotion deferred into R-4. **Strongest next**: (a) **HIGH** `repair-build-checks-vault-and-harden-shippability` (R-4; repro exists); (b) `extend-ptfcd-1-to-function-level` (N=3); (c) `/critic-calibrate` window-run (16-29). |
| 028 | [slice-028-refactor-utf8-rollup-sentinel-version-agnostic](slice-028-refactor-utf8-rollup-sentinel-version-agnostic/) | 2026-05-16 | Standard | medium (critic-required: true — In-house methodology test infra) | SHIPPED — validation PASS | dual-Critic 10/10 VALIDATED (B1/B2/M1/M2/M3/m1/m2 + meta B-add-1/M-add-1/B2-adj); 0 FALSE-ALARM/0 OVERRIDE-MISJUDGED; meta-Critic EXTEND added B-add-1 (3-part-template part2×part3 composition-blindness, slice-022 law N≈8 deepest yet) | 1: destructive-`git checkout`-on-uncommitted-slice-work hazard (false-green nearly masked total slice loss) → BC-PROJ-3 + BC-GLOBAL-2 |
| 027 | [slice-027-add-pipeline-chain-auto-advance](slice-027-add-pipeline-chain-auto-advance/) | 2026-05-16 | Standard | high (critic-required: true — In-house methodology surfaces: `skills/*/SKILL.md` + `tools/*.py` + `methodology-changelog.md`) | SHIPPED — validation PASS (5/5 ACs PASS with **live-dogfooding evidence**: this session auto-advanced `/slice→/design-slice→/critique→/critique-review→/build-slice→/validate-slice` and HALTED at TRI-1 + plan-mode exactly as PCA-1 specifies; full methodology suite 555 PASS; shippability **27 rows, 280/280 PASS no regression**; VAL-1 Layer A 0 secrets + Layer B 0 imports clean; WS-1/ETC-1/TF-1 n/a; full Step-6 gauntlet clean; atomic 0.40.0→0.41.0 ×3; 8-pair `## Pipeline position` byte-equality drift test green) | **/critique NEEDS-FIXES**: 2B+3M+3m, all ACCEPTED-FIXED in-round except m3 (ACCEPTED-PENDING /reflect note), ALL VALIDATED post-build, 0 FALSE-ALARM (B1 = predicted slice-022-law self-violation: phantom `tests/tools/` path vs canonical `tests/methodology/`; B2 nonexistent `architecture/ai-sdlc-VERSION`; M1/M2 7-pair forward-sync traceability → 8-pair drift test; M3 gate-enumeration completeness; m1/m2 doc + propagation-sentinel). **/critique-review EXTEND** (8/8 first-Critic VALID, 0 suspicious, **+1 Major M-add-1** `/validate-slice` PARTIAL unenumerated gate — same defect class as M3 one row down + **1 Minor m-add-1** `/critique` conditional/self-loop successor under-modeled; both ACCEPTED-FIXED). 10/10 Critic dispositions VALIDATED (100% streak holds). **0 true MISSED** — the audit `_CANONICAL_CHAIN` successor-mismatch was a build-time self-catch (Critics review design, not the not-yet-written audit code). | **PCA-1 codified** as audit-enforced gate (methodology v0.40.0→v0.41.0 atomic; NON-`-D` per ADR-019, conforms-not-supersedes; naming-class peers BRANCH-1/BC-1/PMI-1/UTF8-STDOUT-1/CRP-1): NEW `tools/pipeline_chain_audit.py` (20th tool; CRP-1/BRANCH-1-modeled; PMI-1/INST-1 registered) + normalized `## Pipeline position` H2 on 8 `skills/*/SKILL.md` (7 in-loop `auto-advance: true` + `commit-slice` terminus, after untouched `## Next step`) + 5 inline gate-halt directives + build-slice Step 6 wiring; ADR-025 (cheap; supersedes=null). slice-027 = PCA-1 bootstrap-reference instance #1 (directive absent during own loop — chain ran manually per user directive; discharged by /critique-review EXTEND + audit-against-repo exit 0). Propagation: install_audit `_CANONICAL_TOOLS` 19→20 + comment, plugin.yaml +path (rule PCA-1), VERSION+plugin.yaml+ai-sdlc-VERSION 0.40.0→0.41.0, UTF8 roll-up sentinel 19→20 + `_ROOT_ONLY_TOOLS` +pipeline_chain_audit, UTF8 narrative 17→20, shippability 26→27, parametrized 8-pair `## Pipeline position` byte-equality drift test (AC5/M1/M2 element). **NEW empirical patterns**: (1) new-tool consumer-propagation roll-up sentinel N=5 — DOMINANT new-tool-codification friction (strongest slice-028 candidate: version-agnostic refactor); (2) audit-shipping slices have a canonical-constant self-violation hotspot — pre-suite audit-against-own-repo pass is load-bearing; (3) slice-022 self-violation law N≈7, fired 3× on own drafts all caught; (4) function-level-PTFCD-1 now N=3 corroboration (slice-025 AC3 + slice-026 AC5 + slice-027 B1); (5) pre-build count-grep must be count-agnostic. **Strongest slice-028 candidates**: (a) **HIGH** `refactor-utf8-rollup-sentinel-version-agnostic` (N=5 dominant friction); (b) `extend-ptfcd-1-to-function-level` (N=3); (c) `add-skill-drift-audit` (slice-026 M-add-1 watch-list); (d) `/critic-calibrate` window-run (slices 16-27). |
| 026 | [slice-026-enforce-critique-review-prerequisite](slice-026-enforce-critique-review-prerequisite/) | 2026-05-16 | Standard | medium (critic-required: true — In-house methodology surfaces: `skills/build-slice/SKILL.md` + `tools/*.py` + `methodology-changelog.md`) | SHIPPED — validation PASS (5/5 ACs with real-CLI evidence — refuse/canonical-skip/malformed/present all confirmed against constructed temp slices; version lockstep 0.40.0 ×3; shippability row 26 + consumer ref; mini-CAD drift + bootstrap audit-against-self exit 0; full methodology suite 538 PASS; shippability **26/26 PASS no regression**; VAL-1 Layer A 0 secrets + Layer B 0 imports clean; WS-1/ETC-1 n/a; 8/8 TF-1 PASSING; full Phase-6 gauntlet clean + drift-check 0/0; 1 prerequisite DEVIATION logged — TPHD-1 sub-mode (c) pre-flight harmonized AC5 phantom test-FN citation + AC3/AC4 off-convention names) | **/critique CLEAN**: 2B+4M+2m all ACCEPTED-FIXED at TRI-1, ALL VALIDATED post-build, 0 FALSE-ALARM (B1 = predicted slice-022-law self-violation: slice first named rule `CRPD-1` with `-D` contradicting ADR-019 L13 — renamed in-round + NON-`-D` v0.40.0 entry-pin regression-guard; B2 escape-hatch build-log→milestone-frontmatter, survives Step 7b + kills narrative false-positive; M1 wiring-premise correction; M2 "FIRST audit" claim dropped + deterministic placement; M3 install_audit stale comment + independent-counter note; M4 bootstrap-reference-instance). **/critique-review EXTEND** (8 confirmed VALID, 0 suspicious, **+1 missed Major M-add-1**: milestone.md template forward-sync pair under no byte-equality gate — genuine FBCD-1-class first-Critic blind spot the meta-Critic caught; ACCEPTED-FIXED by enumeration-discharge). 9/9 Critic dispositions VALIDATED (100% streak holds). **MISSED by Critic** (function-level phantom-citation, **N=2 — PROMOTION THRESHOLD MET**): slice's own AC5 cited `test_..._byte_equals_installed` vs real `..._byte_equal_installed` + AC3/AC4 off-convention names; neither Critic layer flagged; caught only by /build-slice TPHD-1 sub-mode (c) pre-flight (slice-025 AC3 = N=1; slice-025 reflection named the function-level target). | **CRP-1 codified** as audit-enforced `/build-slice` gate (methodology v0.39.0→v0.40.0 atomic; NON-`-D` per ADR-019, naming-class peers BRANCH-1/BC-1/PMI-1/UTF8-STDOUT-1): NEW `tools/critique_review_prerequisite_audit.py` (19th tool; BRANCH-1-modeled; PMI-1/INST-1 registered) + `skills/build-slice/SKILL.md` CRP-1 `## Prerequisite check` sub-block + Step 6 defense-in-depth + Step 7b preserve-key instruction; escape-hatch = optional `critique-review-skip:` milestone.md **frontmatter key** per ADR-024 (cheap; supersedes=null); closes the slice-025 L37 silent-skip gap; slice-026 = CRP-1 bootstrap-reference instance #1 (sub-block cannot self-gate its authoring build; discharged via /critique-review on slice-026 + audit-against-self). Propagation: install_audit `_CANONICAL_TOOLS` 18→19 + stale comment fix, plugin.yaml +path, VERSION+plugin.yaml+ai-sdlc-VERSION 0.39.0→0.40.0, UTF8 roll-up sentinel 18→19 (independent counter), shippability 25→26, milestone.md template in-repo↔installed lockstep (M-add-1). **NEW empirical patterns**: (1) function-level phantom-test-citation Missed-by-Critic N=2 — promotion threshold met (slice-025 named the target); (2) slice-022 self-violation law N≈6 stable (codification slices 020-026); (3) DR-1 catches the surfaces a /critique fix-block itself creates (M-add-1); (4) template forward-sync unguarded at N=1 (M-add-1 watch-list); (5) new-tool roll-up-sentinel propagation N=4. **Strongest slice-027 candidates**: (a) **HIGH** `extend-ptfcd-1-to-function-level` (N=2 named target); (b) `refactor-utf8-rollup-sentinel-version-agnostic` (N=4); (c) `add-template-drift-audit` (M-add-1 N=1); (d) `/critic-calibrate` window-run (slices 16-26). |
| 025 | [slice-025-add-test-file-existence-check-for-non-pytest-rows](slice-025-add-test-file-existence-check-for-non-pytest-rows/) | 2026-05-16 | Standard | medium (critic-required: true — In-house methodology surfaces: `tools/*.py` + `agents/critique.md` + `methodology-changelog.md`) | SHIPPED — validation PASS (initial PARTIAL on AC4-DR-1, remediated in-flight by running the mandatory `/critique-review`; AC1-5 all PASS with live evidence; full methodology suite 522 PASS; shippability **25/25 PASS no regression**; VAL-1 Layer A 0 secrets + Layer B 0 import findings clean; WS-1/ETC-1 n/a; 16/16 TF-1 PASSING; full Phase-6 audit gauntlet clean; 1 prerequisite DEVIATION logged — TPHD-1 sub-mode (c) pre-flight harmonized AC3 phantom test-fn citation) | **/critique CLEAN**: 2B+3M+2m all ACCEPTED-FIXED at TRI-1, ALL VALIDATED, 0 FALSE-ALARM, 0 OVERRIDE-MISJUDGED (B1 = predicted FBCD-1 sub-mode (a) recursive-self-application catch: `PTFC-1`↔`PTFCD-1` rule-ID drift; B2 PMI-1/INST-1 enumeration; M1 PASSING-gate; M2 token-predicate scoping; M3 wiring; m1/m2 doc). **/critique-review ACCEPT** (0 suspicious / 0 missed / 0 severity; independently verified the ≥7-site SCPD-1 `_lists_ten`→`_lists_eleven` sweep + 4 FBCD-1 end_anchor tightenings left no stale sibling — FBCD-1 sub-mode (b) on Builder's own fix-block, clean). **MISSED by Critic** (function-level phantom-citation, **N=1**): first Critic stat-checked TF-1 *paths* (path-lock table) but not *function* names; the slice's own AC3 cited a non-existent `test_critique_agent_byte_equal_in_repo_vs_installed` — caught only by /build-slice TPHD-1 sub-mode (c) pre-flight, not /critique. PTFCD-1 is FILE-level by design; this slice's Out-of-scope foreshadowed the function-level recurrence site → promote at N=2. **2nd MISSED (process)**: neither Critic layer can reach a skipped `/critique-review` — only /validate-slice's DR-1 audit did, and only because AC4 baked it. **Recursive-self-application closure at 4 layers — deepest for an audit-gated -D discipline.** | **PTFCD-1 codified** as `agents/critique.md` Dim 9 **11th sub-clause** `Phantom test-file citation discipline` (methodology v0.38.0→v0.39.0 atomic) with two sub-modes ((a) TF-1-plan-path existence @ /build-slice Step 6 / (b) shippability-Command-cell-path existence @ /validate-slice Step 5.5); execution-reachable structural sibling to RPCD-1; cites slice-023 B4 + slice-024; -D-suffix N=8; ADR-023 (cheap; supersedes=null). Closes the slice-023 B4 + slice-024 N=2 phantom-FILE-citation class (slice-023 lesson 5 foreshadowed → promote-at-N=2-when-foreshadowed convention met; developer-process calibration, NOT a risk-register entry). NEW `tools/shippability_path_audit.py` (18th tool; PMI-1/INST-1 registered) + `tools/test_first_audit.py` strict-only `missing-test-path-file` PASSING-gated loop. PMI-1 structural-invariant supersession `_lists_ten`→`_lists_eleven` (N=5→N=6); SCPD-1 proactive-application N=3→N=4 (6 pytest-cmd tokens propagated); EPGD-1 N=11→N=12; N-surface schema-pin N=11→N=12; atomic bump (PMI-1 v1.1 retirement-proof N=10→N=11); shippability 24→25; CAD-1 byte-equal sha256 51041c07. **NEW empirical patterns**: (1) audit-gated -D disciplines close recursive-self-application deeper (4 layers) than prose-only — budget pre-flight + validation attention; (2) a mandatory pipeline step detected only via per-slice AC authorship is not actually enforced (strongest slice-026 candidate: structural `/critique-review` prerequisite-check); (3) function-level phantom-test citation N=1 (foreshadowed) — file-level PTFCD-1 misses it; (4) new-tool slices' UTF8-STDOUT-1 roll-up sentinel is an under-enumerated consumer-propagation site (N=3 recurrence). **Strongest slice-026 candidates**: (a) **HIGH** `enforce-critique-review-prerequisite`; (b) `extend-ptfcd-1-to-function-level` (N=1 foreshadowed); (c) `/critic-calibrate` window-run (slices 16-25). |
| 024 | [slice-024-refine-dim-9-with-fix-block-completeness-sub-clause](slice-024-refine-dim-9-with-fix-block-completeness-sub-clause/) | 2026-05-15 | Standard | medium (critic-required: true — In-house methodology surfaces trigger: `agents/critique.md` + `methodology-changelog.md` + ADR-022 + test files) | SHIPPED — validation PASS (5/5 ACs with concrete evidence against live installed copy; shippability catalog **24/24 PASS** no regression — initial 23 spurious FAILs were a validation-harness backtick-strip footgun NOT a regression, documented; VAL-1 Layer A 0 secrets + Layer B 2 Important deferred = recurring intra-repo `tests` namespace false-positive N=22+ (pre-existing conftest imports, not slice-024-introduced); WS-1 n/a; ETC-1 n/a; 13/13 TF-1 PASSING; full Phase-6 audit gauntlet clean; 1 DEVIATION logged (test_methodology_changelog.py section-header `# ---` design-hint → `# ===` empirical convention per CLAUDE.md code-truth; build-log Phase 1b); m1 ACCEPTED-PENDING resolved at /build-slice Phase 6 — META-3 mnemonic drift corrected; m5 DEFERRED cosmetic) | **19 cumulative Critic-stack findings**: 16 first-Critic (3B + 7M + 6m; 11 ACCEPTED-FIXED inline + 1 ACCEPTED-PENDING m1 resolved at /build-slice Phase 6 + 3 OVERRIDDEN m2/m3/m6 = Critic-self-acknowledged non-findings per honesty rule + 1 DEFERRED m5) + 3 /critique-review missed (M-add-1 Phase-4-scan→Phase-5-scan in Cost-summary + M-add-2 TPHD-1-sub-mode-(c) TF-1 AC#4 function-name mismatch + M-add-3 entry-pin-count internal inconsistency; EXTEND verdict; all ACCEPTED-FIXED inline). **All 19 VALIDATED-or-correctly-FALSE-ALARM; 0 OVERRIDE-MISJUDGED.** **HIGHEST-IMPACT first-Critic catch**: B1 cumulative-count N=9-vs-sum-of-cited-N=10 (slice-021 cited at N=2 in mission-brief vs N=3 in ADR-022; 11-site coordinated sweep; FBCD-1 sub-mode (a) catch on the slice's OWN drafts — the discipline being codified, caught in its own codifying slice). **HIGHEST-IMPACT meta-Critic catch**: M-add-2 — first Critic's Audit 8 self-applied TPHD-1 sub-mode (c) and PASSED erroneously on a TF-1 AC#4 function-name mismatch; meta-Critic caught it (the Critic's own self-audit had the exact gap the meta-layer exists to cover). **MISSED by Critic** (slice-023 B4 class recurrence **N=2**): phantom `tests/methodology/test_shippability_catalog.py` cited in TF-1 row 13 + Verification-plan L56 + design.md Audit 8 — BOTH Critics' TPHD-1-(c)/FBCD-1-(a) audits check name-harmonization but never test-FILE-existence on disk; surfaced ONLY at /validate-slice Step 5.5 when AC#5's literal command executed; slice-023 lessons-learned item 5 explicitly foreshadowed it — **PROMOTION-ELIGIBLE for /critic-calibrate slice-025+: add test-FILE-existence check to TPHD-1 sub-mode (c) / FBCD-1 sub-mode (a) for non-pytest TF-1 / shippability-row rows**. **Recursive-self-application closure observed at THREE Critic-stack layers — deepest in project history** (/critique N=10 sub-mode-(a) + /critique-review N=3 sub-mode-(b) + /validate-slice 3-site sub-mode-(a) citation drift); confirms FBCD-1's empirical thesis maximally (wide-surface discipline self-violation surfaces latest/deepest). 19 < slice-021 N=28 / slice-023 N=23 (higher first-draft design quality) yet a single structural blind-spot survived 19 findings + 2 Critic layers — raw count is not a completeness signal. | **FBCD-1 codified** as `agents/critique.md` Dim 9 **10th sub-clause** `Fix-block-completeness discipline` (methodology v0.37.0 → v0.38.0 atomic) with two sub-modes (a Original-draft cross-file consistency / b Post-ACCEPTED-FIXED sibling-sweep); temporal extension of Dim 9 sub-clause 2 (CCC-1 v1.1); cross-referenced by canonical title strings not ordinal; N=10-cumulative / N=4-distinct-slice evidence base; ADR-022 (cheap; supersedes=null). **First codification slice without a prior dedicated /critic-calibrate run** — extends EPGD-1/SCPD-1 N=2-cross-slice proactive-codification convention to N=4-distinct/N=10-cumulative (ADR-022 documents the extension). PMI-1 structural-invariant supersession `_lists_nine`→`_lists_ten` (N=4→N=5); 5 NEW body-bound tests + `_sub_clause_present`+`_location_pinned` duality N=4→N=5; 3 slice-016 RPCD-1 end_anchors tightened (generic recurrence N=3→N=4; 4 `_location_pinned` siblings untouched); atomic bump (PMI-1 v1.1 retirement-proof N=9→N=10); EPGD-1 N=10→N=11; N-surface schema-pin N=10→N=11; SCPD-1 proactive-application N=2→N=3; shippability 23→24 rows. **NEW empirical patterns**: (1) a structural blind-spot can survive 19 Critic-stack findings + 2 layers and die only at real-environment command execution → every AC whose verification is a literal command MUST be executed verbatim at /validate-slice; (2) foreshadowed-but-not-codified lessons recur fast — promote at N=2 when the lesson names its own recurrence site at N=1; (3) codification-slice recursive-self-application closure deepens with discipline surface-width — budget extra /validate-slice attention for wide-surface codifications; (4) proactive N=2-cross-slice codification without a /critic-calibrate run is empirically sound when cumulative evidence is well past N=2; (5) strip markdown backticks before executing shippability Command cells. **Strongest slice-025 candidates**: (a) **HIGHEST** `/critic-calibrate` proper window-run (slices 16-24; FBCD-1 effectiveness ratification + test-file-existence-gap promotion N=2); (b) **HIGH** `extend-tphd-1-with-test-file-existence-check` (codify the Missed-by-Critic directly); (c) `add-github-enterprise-url-derivation` (slice-022 B3 DEFERRED, SMALL); (d) `add-pipeline-wide-branch-discipline-to-upstream-slice-skills` (slice-021 carveout, LARGE/split). |
| 023 | [slice-023-audit-tools-default-utf8-stdout](slice-023-audit-tools-default-utf8-stdout/) | 2026-05-15 | Standard | medium (critic-required: true via MCT-1 trigger glob — slice modifies `tools/*.py` × 18 + `methodology-changelog.md` + adds NEW `tools/_stdout.py` + NEW `tools/utf8_stdout_audit.py` + NEW ADR-021 + `skills/build-slice/SKILL.md`; MCT-1 default-trigger self-application N=12 → N=13 stable; voluntary-Critic-on-cross-cutting-tooling ROI N=13/13 → N=14/14 stable) | SHIPPED — validation PASS (5/5 ACs with concrete evidence; VAL-1 Layer A 0 secrets + Layer B 0 hallucinated imports CLEAN with documented `--imports-allowlist tests`; WS-1 n/a; ETC-1 n/a; full project 536/536 PASS in 13.56s ZERO regressions; shippability catalog 23/23 PASS in 20.67s well under 2-min target; 0 DEVIATIONs logged — all carry-forward applied; 1 reality surprise captured (recursive-self-application closure observed LIVE at /build-slice's own pre-flight TF-1 audit)) | **23 substantive catches across 2 Critic-stack passes**: 17 first-Critic (5B + 8M + 4m; 14 ACCEPTED-FIXED inline + 3 ACCEPTED-PENDING all applied at /build-slice; 0 OVERRIDDEN; 0 DEFERRED) + 6 /critique-review missed (5M fix-block-completeness lapses on Builder's ACCEPTED-FIXED sweeps M-add-1/2/3/4 + 1m M-add-5 arithmetic + 1 informational M-add-6 SUP-1-verification); all 23 VALIDATED at /build-slice + /validate-slice + ACCEPTED-FIXED inline; **100% Critic-disposition accuracy extends streak to N=17 consecutive** (176/176 first-Critic + 178/178 cross-Critic-stack across slices 6-23); verdict NEEDS-FIXES (per TRI-1; 2 ACCEPTED-PENDING items B2 PMI-1 leading-underscore filter + M1 per-tool argv fixture rigor carried to /build-slice; both applied at Phase 4 + Phase 5; verdict CLEAN per triage_audit + DR-1 EXTEND per /critique-review). **B1 strongest first-Critic catch — `architecture/methodology-changelog.md` path wrong in 6 sites** (file actually at repo root; `Glob "**/methodology-changelog.md"` returns only root file; 4 of 5 Blockers were Dim 9 sub-clause 2 design.md-vs-canonical-inventory drift class — B1 + B3 + B4 + M2; without /critique catch, slice would have crashed at /build-slice WRITTEN-FAILING phase on path resolution). **B2 strongest under-engineering catch — PMI-1 `_list_actual_tools` would fire orphan-tool violation on `tools/_stdout.py`** (`tools/plugin_manifest_audit.py:L137-140` only excludes `__init__.py`; design.md hand-waved fix to /build-slice Step 1; B2 forced commitment-at-design-time to one-line filter extension `not p.name.startswith("_")` + 2-assertion test). **B5 strongest count-drift catch — audit JSON output `tools_scanned: 17, tools_with_main: 16` internally contradictory** (logically impossible: if `_stdout.py` is excluded then 16 scanned ≠ 17 scanned; if scanned then `_with_main` is 17 not 16); B5 fix swept 6 sibling sites + added `test_output_contract_invariant` regression-guard. **M-add-2 strongest meta-Critic catch — per-tool argv mis-grouping** (Builder's M1 disposition listed `install_audit` under `--root-only` but actual flag is `--claude-dir`; `mock_budget_lint` under "no argv" but requires positional `files`; `validate_slice_layers` under "no argv" but requires `--slice`; without M-add-2 the regression test would have silently passed without actually exercising encoding path on 3 of 17 tools — the accidental-PASS pitfall TF-1 PENDING→WRITTEN-FAILING genuineness sub-clause flags). **M-add-1 + M-add-3 + M-add-4 are all fix-block-completeness sibling-site sweep failures** of Builder's ACCEPTED-FIXED dispositions (M-add-1 caught row count drift in mission-brief.md L114; M-add-3 caught ADR-021 helper body still showing M6's old encoding short-circuit after design.md L86 simplified; M-add-4 caught 3 sites still referencing old test_utf8_stdout_audit.py path after M5 split) — class re-fires AT the moment Builder closes first-Critic findings, structurally outside any single Builder-self-check's reach. **0 DEVIATIONs logged at slice-023** — all 23 cumulative Critic-stack findings resolved pre-/build-slice; all 2 ACCEPTED-PENDING items applied at /build-slice; cp1252 class RETIRED post-codification. **MISSED by Critic** (NEW class candidate at N=1): `disposition-promised-test-not-enumerated-in-TF-1-plan` — M5 + M6 + B5 dispositions promised tests but didn't update TF-1 plan; TPHD-1 sub-mode (c) at /build-slice prerequisite-check caught all 3 (within-slice N=3 instances; cumulative N=1 across distinct slices; promote at N=3 across distinct slices). **Wiegers regression-guard coverage-symmetry watch-list N=12 → N=13+ cumulative** (B5 count drift swept 6 sibling sites at /critique time; M-add-1 caught a 7th site at /critique-review). **Cross-mission-brief-vs-design-consistency-checking watch-list N=1 → N=3 cumulative at slice-023** (B5 count drift + B3 ADR-pin path drift + M5 test-file path drift) — **PROMOTION-ELIGIBLE for /critic-calibrate slice-024 Dim 9 sub-clause at N=3 threshold**. **Fix-block-completeness on Builder's ACCEPTED-FIXED sweeps (slice-020 M-add-1 watch-list)** re-fired N=4 within slice-023 fix block alone — **PROMOTION-ELIGIBLE**. **Auto-mode-classifier-as-Critic-stack-layer N=3 → N=4 cumulative** (slice-018 + slice-021 + slice-023). **UTF8-STDOUT-1 self-application N=1 canonical-reference-instance at codification time** (audit returns 17/17/17 clean). **Empirical recursive-self-application closure observed LIVE during /build-slice**: BEFORE Phase 3 wiring TF-1 audit emitted mojibake `�` for em-dash; AFTER Phase 3 wiring same audit emitted `—` correctly under `PYTHONIOENCODING=cp1252` — cp1252 class hit live during the very build that codified UTF8-STDOUT-1. **Recursive-self-application N=23 cumulative within slice-023 alone** (17 first-Critic + 6 meta-Critic; well above slice-022 N=18 and approaching slice-021 HWM N=33). **PMI-1 v1.1 retirement-proof N=8 → N=9 stable** (ninth atomic version bump 0.36.0 → 0.37.0). **EPGD-1 self-application N=9 → N=10 stable** (0 of prior entry-pin functions touched; ADDS-only `_v_0_37_0_utf8_stdout_1_*` ×3 + `_adr_021_*` ×1). **N-surface schema-pin shape N=9 → N=10 stable** (canonical phrase `UTF8-STDOUT-1` pinned across helper module + audit module + changelog entry). **ADR-pin convention N=8 → N=9 stable** (ADR-021 added). **CAD-1 byte-equality preserved** on `agents/critique.md` at slice-017 ship hash `f34c967eaaa34413`. **Mini-CAD-1 byte-equality on `skills/build-slice/SKILL.md`** preserved post-forward-sync. **VAL-1 Layer B intra-repo `tests` class N=20 → N=21 cumulative recurrence**. **Cross-cutting Dim 9 catch rate at slice-023 = 100% across both passes** — N=5 stable 100% streak (slice-019 + slice-020 + slice-021 + slice-022 + slice-023). | **UTF8-STDOUT-1 codified** at N=3 surfaces — (1) `tools/_stdout.py` helper module (~25 LOC; `reconfigure_stdout_utf8()`; idempotent; errors="replace"; no encoding short-circuit per M6 + M-add-3 ACCEPTED-FIXED), (2) `tools/utf8_stdout_audit.py` AST-based structural audit (refuses any `tools/*.py` with main() whose first executable statement is not the canonical reconfigure call; self-applies; JSON output; exit 0/1/2), (3) one-line invocation pattern at first executable statement of every audit tool's main() with canonical import `from tools import _stdout` pinned (M4 ACCEPTED-FIXED; 17 tools conform). PMI-1 `_list_actual_tools` extended to filter `p.name.startswith("_")` (B2 ACCEPTED-PENDING; PMI-1 v1.1 retirement-proof invariant preserved). Behavioural regression covers every audit tool under simulated `PYTHONIOENCODING=cp1252 + PYTHONUTF8=0` with U+2192 fixture content. ADR-021 (reversibility: cheap; supersedes: null — shell-side workaround was DEVIATION pattern not ADR-codified discipline). Methodology v0.36.0 → 0.37.0 atomic. **NEW empirical pattern**: recurrence-pattern (N≥3 same-root-cause class) → audit-codification is a faster path to retirement than per-slice DEVIATION logging — slice-023 retired N=6 cumulative cp1252 instances with one structural audit. **NEW empirical pattern**: codification-slice Critic density follows vault-claim density (paths/conventions/counts in design.md), not LOC — slice-023 = 23 findings on SMALL-by-LOC slice. **NEW empirical pattern**: TPHD-1 sub-mode (c) at /build-slice prerequisite-check catches `disposition-promised-test-not-enumerated-in-TF-1-plan` class that sub-modes (a) + (b) cannot reach (Critic-stack reviews artifacts statically; TPHD-1 sub-mode (c) reviews disposition-execution at build entry). **NEW empirical pattern**: empirical witness anchor for codified class is load-bearing — slice-022 D-5 witness made slice-023's mission-brief defensible; class names without witnesses tend to be over-engineered prematurely. **Strongest slice-024 candidates**: (a) **HIGHEST** `/critic-calibrate slice-023 codification` (3 promotion-eligible classes at N=3+ cumulative); (b) **HIGH** `add-pipeline-wide-branch-discipline-to-upstream-slice-skills` (slice-021 M-add-4 carveout extension); (c) **HIGH** `add-github-enterprise-url-derivation` (slice-022 B3 DEFERRED). |
| 022 | [slice-022-redesign-commit-slice-for-pr-aware-flow](slice-022-redesign-commit-slice-for-pr-aware-flow/) | 2026-05-15 | Standard | medium (critic-required: true via MCT-1 trigger glob — slice modifies `skills/commit-slice/SKILL.md` + `methodology-changelog.md` + ADR-020 NEW; MCT-1 default-trigger self-application N=11 → N=12 stable) | SHIPPED-WITH-DEFERRALS — validation PASS (5/5 ACs with concrete evidence; VAL-1 Layer A 0 secrets + Layer B 0 hallucinated imports CLEAN with documented `--imports-allowlist tests`; WS-1 n/a; ETC-1 n/a; full methodology+diagnose suite 497/497 PASS in 8.18s ZERO regressions; 5 DEVIATIONs logged D-1..D-5) | **18 substantive catches across 2 Critic-stack passes**: 13 first-Critic (4B + 5M + 4m; 11 ACCEPTED-FIXED inline + 1 DEFERRED B3 + 1 OVERRIDDEN m1 origin-URL-identity as Critic over-reach FALSE-ALARM) + 5 /critique-review missed (1B M-add-1 SUP-1-misapplication-as-impossible-pre-finish-gate + 1M M-add-5 Pass-2-3-guards + 3m M-add-2 + M-add-3 sibling-site catches + M-add-4 phantom-`--do-commit`); all 18 VALIDATED at TRI-1 + ACCEPTED-FIXED inline; 100% Critic-disposition accuracy extends streak to **N=16 consecutive** (153/153 first-Critic + 154/154 cross-Critic-stack across slices 6-22); verdict CLEAN per triage_audit + DR-1 EXTEND per critique-review-audit. **B2 strongest first-Critic catch — `git cherry` patch-id is PER-COMMIT not aggregate** (would have shipped Signal-B mechanism that destructively false-NOs on the DOMINANT GitHub squash-merge happy path; fix is two-pass detection cherry Pass 1 + aggregate-tree-diff Pass 2 fallback with superset-pinned "covers FILES" + empty-FILES guard + N=500 perf bound). **M-add-1 strongest meta-Critic catch — Pre-finish gate L139 demanded structurally impossible bidirectional supersession link per SUP-1** (SUP-1 scopes to slice supersession via /supersede-slice NOT ADRs; ADR family convention one-directional verified across ADR-001 through ADR-020; would have caused Builder-vs-audit deadlock at /build-slice Step 6). **M-add-5 strongest meta-Critic refinement — B2 Pass 2 mechanism 3-guards** (empty-FILES → STOP closes data-loss false-YES; "covers FILES" pinned to superset semantics; perf bound N=500 commits with manual-resolution diagnostic). **m1 only Critic over-reach FALSE-ALARM** — origin URL identity OVERRIDDEN by user (Critic acknowledged in own framing "not a new vulnerability vs baseline `git push`"; reality at validation confirmed override). **5 DEVIATIONs logged**: D-1 TPHD-1 sub-mode (c) prerequisite-check CAUGHT TF-1 plan staleness drift across 4 surfaces at /build-slice entry — **strong recursive-self-application catch** (codification slice committed exactly the violation sub-mode (c) was codified to catch); D-2 slice-021 inherited test broadened for 3-mode argument-hint shape; D-3 own test heuristic over-strict on `git branch -D` manual-escape-hatch context broadened to accept Manually/manually allowed-context; D-4 pre-existing CRLF/LF environmental drift on `skills/build-slice/SKILL.md` unrelated to slice-022 — out-of-scope cleanup forward-sync applied; D-5 cp1252 N=5 → **N=6 cumulative** at TF-1 audit U+2192 arrow (`audit-tools-default-utf8-stdout` ELEVATED to slice-023 HIGHEST priority). **Wiegers regression-guard coverage-symmetry watch-list N=9 → N=12 cumulative within slice-022 alone** (M-add-2 + M-add-3 + inline must-not-defer count sweep — 3 sibling-site catches in one slice); promotion to /critic-calibrate Dim 9 sub-clause structurally OVERDUE. **NEW DR-1 catch class candidate at N=1**: `SUP-1-misapplication-as-impossible-pre-finish-gate`. **NEW first-Critic-MISS class candidate at N=1**: `cross-mission-brief-vs-design-consistency-checking`. **BRANCH-1 self-application N=1 bootstrap → N=2 stable** post-slice-022 (slice-022 IS first non-bootstrap canonical-reference instance per /critique m2 + /critique-review M-add-2 off-by-one "second" → "first" correction). **TPHD-1 self-application N=5 → N=6 partial** (sub-mode (c) prerequisite-check empirically demonstrated). **EPGD-1 self-application N=8 → N=9 stable** (0 of 15 prior entry-pin functions touched; ADDS-only `_v_0_36_0_pr_aware_commit_slice_*` ×2 + `_adr_020_*` ×2). **PMI-1 v1.1 retirement-proof N=7 → N=8 stable**. **N-surface schema-pin shape N=8 → N=9 stable** (canonical phrase `3-mode PR-aware /commit-slice taxonomy` pinned across N=3 surfaces). **ADR-pin convention N=7 → N=8 stable** (ADR-020 added). **mini-CAD-1 N=13 → N=14 stable** (commit-slice family). **CAD-1 byte-equality preserved** on `agents/critique.md` at slice-017 ship hash `f34c967eaaa34413...`. **VAL-1 Layer B intra-repo `tests` class N=19 → N=20 cumulative**. **First in-codebase use of ADR partial-supersession** — ADR-020 supersedes ADR-019 Option 1 sub-mode (b) only; one-directional encoding pattern empirically established. **Cross-cutting Dim 9 catch rate at slice-022 = 100%** post-2-pass dual-review convergence — N=4 stable 100% streak (slice-019 + slice-020 + slice-021 + slice-022). | **3-mode PR-aware /commit-slice taxonomy** at `skills/commit-slice/SKILL.md` Step 5 sub-steps 5a (default no-flag) / 5b (--merge slice-021 preserved verbatim) / 5c (--push NEW) / 5d (--sync-after-pr NEW with two-pass Signal B + 3 Pass-2 guards). NEW `## When to use which mode` guidance section. Mutually-exclusive flag set. ADR-020 partially supersedes ADR-019 sub-mode (b) only; sub-modes (a) + (c) inherited UNCHANGED. Methodology v0.35.0 → 0.36.0 atomic. **NEW empirical pattern**: ADR partial-supersession encoding works via single one-directional `supersedes:` slot + body-level scope enumeration; do NOT add `superseded-by:` to prior ADR (violates append-only + ADR family convention). **NEW empirical pattern**: two-pass detection (`git cherry` Pass 1 per-commit + aggregate-tree-diff Pass 2 multi-commit-squash fallback) is the right shape for any commit-reachability check gating destructive action. **NEW empirical pattern**: codification slices commit exactly the violation their codified discipline catches — slice-022 D-1 TPHD-1 sub-mode (c) caught own author's TF-1 plan drift at /build-slice entry. **Strongest slice-023 candidates**: (a) **HIGHEST** `audit-tools-default-utf8-stdout` (cp1252 N=6); (b) **HIGH** `/critic-calibrate slice-023 codification` (Wiegers N=12 + SUP-1-misapplication + cross-mission-brief-vs-design-consistency); (c) **HIGH** `investigate-crlf-lf-drift-on-installed-skills` (D-4 recon); (d) `add-pipeline-wide-branch-discipline-to-upstream-slice-skills`; (e) `add-github-enterprise-url-derivation` (slice-022 B3 DEFERRED); (f) `add-merge-conflict-recovery-to-commit-slice-merge`; (g) VAL-1 Layer B `tests` auto-allowlist (N=20 cumulative). |
| 021 | [slice-021-add-feature-branch-workflow-at-build-and-commit-slice](slice-021-add-feature-branch-workflow-at-build-and-commit-slice/) | 2026-05-14 | Standard | medium (critic-required: true via MCT-1 trigger glob match — slice modifies `skills/build-slice/SKILL.md` + `skills/commit-slice/SKILL.md` + `methodology-changelog.md` + adds NEW ADR-019 + NEW `tools/branch_workflow_audit.py`; MCT-1 default-trigger self-application N=10 → N=11 stable; voluntary-Critic-on-cross-cutting-tooling ROI N=12/12 → N=13/13 stable) | SHIPPED-WITH-DEVIATIONS — validation PASS (5/5 ACs with concrete evidence; VAL-1 Layer A 0 secrets + Layer B 0 hallucinated imports CLEAN with documented `--imports-allowlist tests`; WS-1 n/a; ETC-1 n/a; full project 481/481 PASS in 7.66s ZERO regressions; 4 reality surprises captured; 5 DEVIATIONs logged D-1..D-5) | **33 substantive catches across 4 Critic-stack passes — NEW PROJECT HWM exceeding slice-020 N=17 by +16**: 14 first-Critic (5B + 5M + 4m) + 4 missed first-meta-Critic (1B + 3M) + 11 /critique-rerun (3B + 4M + 4m) + 4 missed /critique-review-rerun (3M + 1m); all 33 VALIDATED at TRI-1 + ACCEPTED-FIXED inline; 100% Critic-disposition accuracy extends streak to N=15 consecutive (127/127 first-Critic + 144/144 cross-Critic-stack across slices 6-21); verdict CLEAN per triage_audit + DR-1 EXTEND per critique-review-audit (twice — both /critique-review passes EXTEND). **B1 strongest first-Critic catch — escape-hatch regex contradicts empirical DEVIATION format** (5 archive samples verified at exact line refs; `rationale:` token has zero precedent; without B1, audit would fire on every empirically-conformant BRANCH=skip line). **B3 strongest under-engineering catch — Step 0.5/Phase 6 vocabulary contradicts heading scheme** (methodology-changelog v0.32.0 L102 verbatim quote canonicalizes step numbering 1-8; slice-017 TPHD-1 explicitly retired Step-0 naming; slice-021 re-introduced exactly that vocabulary — strict B3-class recurrence on own draft). **B5 strongest contract-gap catch — `--merge` post-/reflect-archive merge-conflict recovery state undefined** (commit-slice SKILL.md L14 verified: /reflect auto-archives BEFORE /commit-slice; if `--merge` hits conflict, slice folder already moved + lingering branch + non-canonical commit message all undefined). **M-add-4 strongest meta-Critic ESCALATED — cross-slice-branch leakage at upstream skill pipeline** (BRANCH-1 only fires at /build-slice; /slice + /design-slice + /critique + /critique-review have NO branch guard — surfaced AT /build-slice but recovery non-trivial); TRI-1 set verdict BLOCKED → /design-slice rerun → option (a) /build-slice-only v1 carveout chosen + Limitations item 8 + Error-model surface 4 + queued follow-on slice `add-pipeline-wide-branch-discipline-to-upstream-slice-skills`. **B3-new strongest /critique-rerun catch — 2 NEW Wiegers count-drifts WITHIN /design-slice rerun fix-block** ("7+7=14" empirically 8+6; "28 TF-1 rows" empirically 30 — strict recurrence of Wiegers coverage-symmetry class WITHIN the very rerun intended to retire the class; M1-residual retired the aspirational "design-time fix-block-completeness self-check" Pre-finish item as empirically falsifiable). **M-add-1-rerun + M-add-2-rerun strongest /critique-review-rerun catches — milestone.md surface scope blind spot** (B3-new fix did NOT propagate to milestone.md L43; M1-residual retirement did NOT propagate to milestone.md L46; rerun-Critic's grep verification scoped to mission-brief + design + ADR but missed milestone.md as load-bearing claims-propagation target — NEW DR-1 catch-class candidate at N=1). **DEVIATION-5 strongest user-surfaced post-/validate catch — `opinionated-merge-default-vs-team-workflow`** (ALL 4 Critic-stack passes missed that `--merge` local-only is structurally wrong for non-solo projects; dimensions evaluated `--merge` INTERNALLY without asking EXTERNALLY about typical adopter profile — NEW Dim 9 sub-clause candidate `default-mode-vs-typical-adopter-workflow alignment` at N=1; slice-022 `redesign-commit-slice-for-pr-aware-flow` queued: DROP `--merge` + add `--push`). **DR-1 catch-class diversification N=8 → N=10 stable** with 2 NEW class candidates at N=1: `Cross-slice-branch leakage at upstream skill pipeline` (M-add-4) + `Milestone-summary surface as load-bearing claims-propagation target` (M-add-1-rerun + M-add-2-rerun); plus NEW Dim 9 sub-clause candidate `default-mode-vs-typical-adopter-workflow alignment` from DEVIATION-5. **Wiegers regression-guard coverage-symmetry watch-list N=5 → N=9 cumulative within slice-021 alone** (4 NEW instances: first-Critic M4 4-count drift + meta-Critic M-add-1 14-vs-11 + /critique-rerun B3-new violations 1+2 + /critique-review-rerun M-add-1-rerun + M-add-3-rerun) — well past N=3 promotion threshold; **promotion ELEVATED to /critic-calibrate slice-022** (from slice-024+). **BRANCH-1 self-application N=1 bootstrap-reference instance** (slice that authors `## Prerequisite check ### Branch state` sub-section cannot itself use it; manual branch-create at /build-slice Step 1 + canonical `BRANCH=skip-bootstrap` DEVIATION line in build-log Events; first non-bootstrap canonical reference instance at next non-/repro slice). **TPHD-1 self-application N=4 → N=5 partial** (harmonization required 4 review passes; not single-shot stable; /critique-review-rerun M-add-4-rerun honest accounting). **EPGD-1 self-application N=7 → N=8 stable** (0 of 15 prior entry-pin functions touched; slice-021 ADDS-only `_v_0_35_0_branch_1_*` entry-pin + 1 ADR-pin `test_adr_019_branch_per_slice_workflow_exists_and_links_to_branch_1`). **PMI-1 v1.1 empirical retirement-proof N=6 → N=7 stable** (7th atomic version bump 0.34.0 → 0.35.0; zero gate-body modifications). **Recursive-self-application N=17 → N=33 cumulative HWM** post-RSAD-1 codification (slice-021 first-Critic 14 + meta-Critic 4 missed + /critique-rerun 11 + /critique-review-rerun 4 missed = 33 self-defects on own draft — **new project HWM exceeding slice-020 N=17 by +16**; codification slices commit instances of the disciplines they codify at densities scaling with complexity). **4 classical build-time DEVIATIONs** (D-1 shippability-row-self-test convention drop; D-2 `tests/tools/` namespace collision → relocate; D-3 Windows cp1252 N=4 → N=5 cumulative `write_text` encoding="utf-8"; D-4 global init.defaultBranch leak past `--unset` local). **1 user-surfaced post-/validate DEVIATION** (D-5 `--merge` opinionated-merge-default-vs-team-workflow → slice-022 redesign). **CAD-1 byte-equality preserved** on `agents/critique.md` at slice-017 ship hash `f34c967eaaa34413` (untouched). **Mini-CAD-1 byte-equality pattern extended to 2 NEW skill surfaces** (build-slice + commit-slice mini-CAD drift tests added; pattern N=11 → N=13 cumulative families). **N-surface schema-pin 3-surface shape N=7 → N=8 stable** (`branch-per-slice workflow` canonical phrase pinned across in-repo + installed methodology-changelog + ADR-019 + 2 SKILL.md). **`-D` suffix rule-ID convention N=6 stable** (BRANCH-1 does NOT carry -D suffix — audit-enforced gate semantics; distinct from /critique-time -D prose-heuristic family RSAD-1/EPGD-1/SCPD-1/RPCD-1/TPHD-1/BFRD-1). **ADR-pin convention N=6 → N=7 stable** (ADR-019 added). **Cross-cutting Dim 9 catch rate at slice-021 = 100% but required 4 review passes** (first-Critic 14/14 + meta-Critic 4/4 missed at /critique-review + /critique-rerun 11/11 + meta-Critic 4/4 missed at /critique-review-rerun = empirical convergence required all 4 passes); trajectory: 0% → 25% → 60% → 100% → 60% → 87.5% → 80% → 80-100% → 87.5% → 85.7% → 87.5% → 88.9% → 87.5% → 91.7% → 100% → 100% → **100% (slice-021 — N=3 stable 100% streak)**. **NEW empirical insight**: aspirational Builder-self-check Pre-finish-gate items are provably falsifiable (slice-021's "design-time fix-block-completeness self-check" authored at /design-slice rerun AND immediately falsified at the same round with 5 fresh count-drifts; retired at /critique-rerun M1-residual; structural closure is /critic-calibrate Dim 9 sub-clause adversarial check NOT Builder-self-check); **NEW empirical insight**: auto-mode classifier as fifth-Critic-stack-layer at N=2 → N=3 cumulative (slice-018 DEVIATION-1/2 + slice-021 /validate-slice AC #4 negative-case attempt block). plugin.yaml.version 0.34.0→0.35.0 + VERSION + ai-sdlc-VERSION atomic. Methodology suite 455 → 481 stable (+12 NEW tests for slice-021 + 5 extends + ~14 from pytest discovery accumulating); full project 481/481 PASS in 7.66s (was 4.52s at slice-020 — +3.1s reflects additional test count + 8 BRANCH-1 unit tests with `_run_git` subprocess overhead). Shippability catalog 20 → 21 rows; row 21 enumerates 14 invocation targets (8 whole-file pytest paths + 6 `::test_*` named-function paths) across 11 test files; full row 21 runs 27 tests in 4.38s at /build-slice Phase 5 pre-finish. Validate-using-your-own-ship N=18 → **N=19 stable**. Empirical-verification-at-design-time discipline N=19 → **N=20 stable** (6 audits PMI-1 + INST-1 + CAD-1 + BRANCH-1 + TF-1 + WIRE-1 all VALIDATED at /build-slice + /validate-slice). INST-1 `_CANONICAL_TOOLS` N=15 → **N=16** (`tools.branch_workflow_audit` added). VAL-1 Layer B intra-repo `tests` namespace-package class N=18 → **N=19 cumulative recurrence** (every slice 003-021). Windows cp1252 default encoding class N=4 → **N=5 cumulative** (`write_text` default at test fixture; cumulative across slices 007/016/018/020/021; promotion threshold WELL exceeded; `audit-tools-default-utf8-stdout` slice candidate ELEVATED to HIGHEST-priority for slice-022). Auto-mode-classifier-as-Critic-stack-layer class N=2 → **N=3 cumulative recurrence**. RPCD-1 sub-mode (b) self-application N=2 → **N=3 stable** (SCPD-1 row 21 propagation BEFORE /validate-slice catalog run) | **BRANCH-1 codified** (Branch-1 — audit-enforced gate at /build-slice Step 6) at NEW sub-section `### Branch state` within existing `## Prerequisite check` H2 of `skills/build-slice/SKILL.md` (slice-017 TPHD-1 sub-mode (c) precedent — prerequisite-class disciplines are sub-sections of `## Prerequisite check`, NOT new Steps). Sub-mode (a) build-time branch-create with runtime default-branch resolution via `git symbolic-ref refs/remotes/origin/HEAD` + `init.defaultBranch` fallback (NOT hard-coded master/main); sub-mode (b) commit-time `--merge` flow at `/commit-slice` Step 5 with 2 pre-flight guardrails (stale `slice/*` branch + `git status --porcelain` empty) + 5-step merge + explicit `Confirm merge + delete? (yes/no)` + safe-delete only; sub-mode (c) audit-time pre-finish refusal at `/build-slice` Step 6 via NEW `tools/branch_workflow_audit.py` (~340 lines + 8 unit tests covering 5 violation classes + 3 default-branch-resolution paths). Canonical `BRANCH=skip — rationale: <text>` line shape pinned at Step 7c via regex `^- \d{4}-\d{2}-\d{2} \d{2}:\d{2} DEVIATION: BRANCH=skip\b.+rationale: .+`. ADR-019 (reversibility: cheap; magnitude ~25 source files + 5 test-file extends = 30 total touches per /critique-rerun M4-new ACCEPTED-FIXED option (b) single-canonical-enumeration in design.md §Files changed; supersedes=null). Default-branch resolution canonical phrase pinned across N=3 surfaces (audit Python helper `_resolve_default_branch()` + 2 SKILL.md prose-heuristic). **v1 carveout** per M-add-4 ESCALATED → option (a): /build-slice + /commit-slice only; upstream-pipeline skills /slice + /design-slice + /critique + /critique-review have NO branch guard (deferred to follow-on `add-pipeline-wide-branch-discipline-to-upstream-slice-skills`). 3 stale-doc surfaces updated: `pipeline.md` L97 + `tutorial.md` L750 + `tutorial-site/Hybrid AI SDLC Pipeline.html` L583 (`--do-commit` → `--merge` per slice-021 ship state). CLAUDE.md (root) gains NEW Brownfield-rules bullet `Branch-per-slice`. Methodology v0.34.0 → 0.35.0 atomic. **NEW empirical pattern**: audit-from-day-one is structurally cheaper than prose-pin test mass for programmatically-checkable disciplines (BRANCH-1 ships audit + 8 unit tests at codification; contrast with prose-only -D-suffix family BFRD-1/TPHD-1/RPCD-1/SCPD-1/EPGD-1/RSAD-1 which defer audit to v2 at N≥3 violations). **NEW Critic-stack blind-spot class** `opinionated-default-vs-typical-adopter-profile` (DEVIATION-5 root cause — user-surfaced post-/validate); slice-022 candidate addresses via DROP `--merge` + add `--push`. **NEW empirical insight**: aspirational Builder-self-checks are provably falsifiable (M1-residual retired the "design-time fix-block-completeness self-check" item after the rerun authoring it immediately falsified it); structural closure is /critic-calibrate Dim 9 sub-clause adversarial check. **NEW empirical insight**: Critic-stack's grep verification scopes to mission-brief + design + ADR but consistently misses milestone.md as load-bearing claims-propagation target. **Strongest slice-022 candidates (priority order)**: (a) **HIGHEST** `redesign-commit-slice-for-pr-aware-flow` (DEVIATION-5 fix — DROP `--merge` + add `--push`; ADR-020 supersedes ADR-019 sub-mode (b); SMALL-MEDIUM ~2-3 hours); (b) **HIGH** `audit-tools-default-utf8-stdout` (cp1252 N=5 promotion threshold WELL exceeded; SMALL ~30-45 min); (c) **HIGH** `/critic-calibrate slice-022 codification` (Wiegers coverage-symmetry promotion to Dim 9 sub-clause + 3 NEW class watch-lists + auto-mode-classifier observation + Builder-self-check-falsifiable pattern + adopter-profile dimension); (d) `add-pipeline-wide-branch-discipline-to-upstream-slice-skills` (LARGE ~35+ sites; may split); (e) `add-merge-conflict-recovery-to-commit-slice-merge` (becomes MOOT if slice-022 drops `--merge`); (f) cleanup of stale R-1 + R-2 if pursued — both untouched for 20 slices. |
| 020 | [slice-020-codify-bug-fix-repro-prelude-at-slice](slice-020-codify-bug-fix-repro-prelude-at-slice/) | 2026-05-14 | Standard | medium (critic-required: true via MCT-1 trigger glob match — slice modifies `skills/slice/SKILL.md` matching `skills/*/SKILL.md` + `methodology-changelog.md` + adds NEW ADR-018; MCT-1 default-trigger self-application N=9 → N=10 stable post-slice-020; voluntary-Critic-on-cross-cutting-tooling ROI N=11/11 → N=12/12 stable) | SHIPPED — validation PASS (5/5 ACs with concrete evidence; VAL-1 Layer A 0 secrets + Layer B 0 hallucinated imports CLEAN; WS-1 n/a; ETC-1 n/a; shippability catalog 20/20 PASS in 12.53s; ZERO regressions on prior slices' critical paths; ZERO reality surprises at AC-level) | 17 substantive catches (14 first-Critic + 3 missed meta-Critic; NEW PROJECT HWM exceeding slice-019 N=12) = 3B + 5M + 6m first-Critic + 3 missed (M-add-1 Blocker + M-add-2 Major + M-add-3 Minor) + 1 SUSPICIOUS rationale (M2 OVERRIDDEN strengthened via design.md "Prose-pin test assertion locks" specifying `_prelude_present` asserts literal canonical phrase) + 1 SEVERITY-WRONG informational (B3 Blocker→Major); 14 ACCEPTED-FIXED (11 first-Critic inline at /critique + 3 meta-Critic inline at /critique-review) + 1 OVERRIDDEN (M2 — N=3 schema-pin convention preservation with concrete test-assertion-locking enforcement) + 2 DEFERRED (m1 stylistic + m6 R-1/R-2 reflection mention); verdict CLEAN per triage_audit + DR-1 EXTEND per critique-review-audit; **all 14 first-Critic + 3 meta-Critic missed findings VALIDATED at /critique-disposition AND /validate-slice — 15th consecutive 100% Critic-disposition accuracy slice** (running 131/131 first-Critic / 139/139 cross-Critic-stack across slices 6-20); **B1 strongest first-Critic catch — slice-001 `-fix` suffix empirical counter-example to naïve `fix-*` prefix-only detection** (project archive `slice-001-diagnose-orchestration-fix` demonstrates false-negative class against project's own corpus); **B2 strongest enforcement-primitive catch — verification mechanism unspecified** (discipline would reduce to advisory-only without enforcement primitive; `shippability.md grep verification` canonical phrase pinned via prose-pin test); **B3 strongest count-drift catch SEVERITY-MISCALIBRATED Blocker→Major per /critique-review** — ADR-018 magnitude "~8-10 sites total" inconsistent with 11-item enumeration; **N=4 → N=5 within slice-020 cumulative recurrence** of Wiegers regression-guard coverage-symmetry watch-list class (B3 + L10 + L24 + m3 + L169 = 5 instances within one slice's fix-prose layers; strongest single-slice density in project history); **M5 strongest under-engineering catch — helper-generalization rule-of-three trigger met** (slice-019 aggregated lesson explicitly named `_extract_version_body(content, version)` as next-codification-slice target at N=3; Option C generalized helper chosen at /critique fix-prose; rule-of-three counter N=2 → N=3 stable); **M-add-1 strongest meta-Critic save — ADR-018 L169 Conclusion still said "~8-10 sites" after B3 fix at L132** (same count-drift class RECURRED WITHIN B3 fix block; textbook recursive-self-application; NEW first-Critic-MISS class candidate at N=1 *fix-block-completeness on count-drift Blockers* joins watch-list); **M-add-2 strongest meta-Critic save — `bug:` provenance branch aspirational with zero precedent** (shippability rows 1-19 contain zero `bug:` precedent + no /repro update mandating going forward; RPCD-1 sub-mode (b) class self-application on first-Critic's own fix proposal — NEW recursive-self-application class; DROPPED at /critique-review per Option (a) — `tests/bugs/*` Command-cell match + verbal-claim-with-path covers via /repro skill L87/L94/L114 documented convention); **M2 SUSPICIOUS rationale-strengthening — OVERRIDE rationale "phrase appears naturally" unverified** (design.md "Prose-pin test assertion locks" subsection added at /critique-review locking `_prelude_present` to assert literal canonical phrase `bug-fix repro prelude discipline`, NOT merely `BFRD-1` rule ID); **DR-1 catch-class diversification N=7 → N=8 stable** with NEW class candidate *fix-block-completeness on count-drift Blockers* at N=1 + N=2 cumulative ratchet on *Self-application-qualifier coherence on canonical-reference-instance naming* (M1 contingent-inapplicability framing); **DR-1 ROI charter extension N=2 stable** (slice-018 secondary-symmetry-cost + slice-020 fix-block-completeness + proposed-mechanism-runtime-prerequisite-completeness classes); **BFRD-1 contingent N/A at codification time** per /critique M1 ACCEPTED-FIXED (slice-020 happens to be NEW-feature methodology-codification slice not fixing runtime bug; STOP-route vacuously satisfied; hypothetical future codification-AND-bug-fix slice would self-apply BFRD-1 trivially; contingent inapplicability NOT architectural impossibility); **TPHD-1 self-application N=3 → N=4 stable post-codification** at all 3 sub-modes (slice-020 IS canonical reference instance #4); **EPGD-1 self-application N=6 → N=7 stable** (0 of 15 prior entry-pin functions touched at function-name level; body-only refactor at L12-74 generalizing helpers preserved all v0.22.0..v0.33.0 function names); **SCPD-1 stays at N=3 stable** post-codification (no Dim 9 sub-clause supersession); **PMI-1 v1.1 empirical retirement-proof N=5 → N=6 stable** (6th atomic version bump 0.33.0 → 0.34.0; zero gate-body modifications); **Recursive-self-application N=11 → N=17 cumulative HWM** post-RSAD-1 codification (slice-020 first-Critic 14 catches + meta-Critic 3 missed = 17 self-defects on own draft — **new project HWM exceeding slice-019 N=12**; hypothesis: codification slices with multi-section ADRs commit count-drift at high density on own draft); **ZERO classical build-time DEVIATIONs at slice-020 — N=2 stable streak extended** (slice-019 + slice-020); 1 build-time DEVIATION at methodology-recurrence layer (Windows cp1252 console encoding class N=3 → **N=4 cumulative** at `tools/critique_review_audit.py`; workaround `$env:PYTHONIOENCODING = "utf-8"` applied inline; `audit-tools-default-utf8-stdout` candidate elevated to HIGHEST-priority for slice-021); **Bidirectional sha256 forensic capture N=15 → N=16 stable**: methodology-changelog.md NEW `991a17f439ef7c35...` (replaces slice-019 hash `9d5e664a34fa5017`) + skills/slice/SKILL.md NEW `cc18b5a05c2220dd...` (mini-CAD-1 row 3 PASSING → WRITTEN-FAILING → PASSING transition pattern N=10 → N=11 stable); agents/critique.md unchanged at slice-017 ship hash `f34c967eaaa34413...` (CAD-1 byte-equality preserved); **N-surface schema-pin 3-surface shape N=6 → N=7 stable**; **-D suffix rule-ID convention N=5 → N=6 stable** (BFRD-1 added: RSAD-1 + EPGD-1 + SCPD-1 + RPCD-1 + TPHD-1 + BFRD-1); **ADR-pin convention N=5 → N=6 stable** (ADR-018 added); **Cross-cutting Dim 9 catch rate at slice-020 = 100%** (14/14 first-Critic + 3/3 missed meta-Critic; ZERO MISSED at slice-cycle level — empirically validated by ZERO classical build-time DEVIATIONs; N=2 stable 100% streak post-slice-019). Trajectory: 0% → 25% → 60% → 100% → 60% → 87.5% → 80% → 80-100% → 87.5% → 85.7% → 87.5% → 88.9% → 87.5% → 91.7% → 100% → **100% (slice-020)**. plugin.yaml.version 0.33.0 → 0.34.0 + VERSION + ai-sdlc-VERSION atomic. Methodology suite 447 → **455 stable** (+8 NEW BFRD-1 tests); full project 455/455 PASS in 4.52s. Shippability catalog 19 → 20 rows; 20/20 PASS in 12.53s at /validate-slice Step 5.5. Validate-using-your-own-ship N=17 → **N=18 stable**. Empirical-verification-at-design-time discipline N=18 → **N=19 stable**. VAL-1 Layer B intra-repo `tests` namespace-package class N=17 → **N=18 cumulative recurrence**. MCT-1 default-trigger self-application N=9 → **N=10 stable**. RPCD-1 sub-mode (b) self-application N=1 → **N=2 stable** post-codification at slice-016 | **BFRD-1 codified** (Bug-Fix-Repro-Discipline-1, -D suffix) at `skills/slice/SKILL.md` NEW Step 3c section between Step 3b L140 + Step 4 L142 anchors. Detection mode (a) name-shape fast-path widened to regex set: `fix-*` prefix + `*-fix` suffix witnessed at slice-001 per /critique B1 + bugfix-* + hotfix-* + defect-* + repair-* + patch-* + harden-*-bug. Mode (b) PRIMARY candidate-source signal handles non-naming-shape bug-fix slices. Verification mechanism: grep shippability.md for `tests/bugs/*` Command-cell match (matches /repro skill L87/L94/L114 documented convention) + verbal-claim-with-path fallback per /critique-review M-add-2 Option (a). STOP-and-route verbatim to `/repro <issue>` when failing-test not found. Mission-brief consequences: failing-test path under Dependencies + AC asserting "repro test PASSES at slice end". `bug:` provenance branch DROPPED at /critique-review (zero precedent in shippability rows 1-19; aspirational). ADR-018 (reversibility: cheap; magnitude ~11 sites per /critique B3 ACCEPTED-FIXED recount + /critique-review M-add-1 propagation closure across L132 + L153 + L169); rule-ID `BFRD-1` carries -D suffix per slice-011 RSAD-1 + slice-013 EPGD-1 + slice-015 SCPD-1 + slice-016 RPCD-1 + slice-017 TPHD-1 N=5 → N=6 stable convention. Canonical phrase `bug-fix repro prelude discipline` pinned across N=3 surfaces (skill prose + in-repo changelog + installed changelog) — first-Critic OVERRIDDEN M2 + meta-Critic M2 SUSPICIOUS rationale-strengthening empirically locks via test enforcement contract per design.md "Prose-pin test assertion locks" specifying `_prelude_present` test asserts literal canonical phrase, NOT merely rule ID. Methodology version 0.33.0 → 0.34.0 atomic. Helper-generalization at rule-of-three trigger: `_extract_version_body(content: str, version: str) -> str` introduced at `tests/methodology/test_methodology_changelog.py` L12-74; slice-018 `_extract_v031_body` + slice-019 `_extract_v033_body` refactored as thin wrappers for backward compatibility; 6/6 backward-compat tests PASS unchanged at call-site (single-code-path discipline preserved per slice-018 /critique M2). Rule-of-three counter N=2 → N=3 stable. 5 ACs / 11 TF-1 rows (was 9 pre-fix; +2 verification-mechanism rows per /critique B2 ACCEPTED-FIXED) / 13 must-not-defer (was 12; +1 helper-generalization per /critique M5); SCPD-1 stays at N=3 stable; recursive-self-application N=11 → N=17 cumulative HWM. NEW first-Critic-MISS class candidates at N=1 each: `fix-block-completeness on count-drift Blockers` (M-add-1) + `proposed-mechanism-runtime-prerequisite-completeness on fix proposals` (M-add-2 RPCD-1 self-application) + `Override-rationale-must-name-concrete-test-enforcement` (M2 SUSPICIOUS); all join watch-list. NEW project-pattern observation: codification slices with multi-section ADR documents commit count-drift at high density on own draft (hypothesis ratified by slice-020 N=5 within-slice density). Windows cp1252 console encoding class N=3 → **N=4 cumulative** at `tools/critique_review_audit.py` (slice-007 + 016 + 018 + 020); promotion threshold (N=3) exceeded by N=2 since slice-018; `audit-tools-default-utf8-stdout` candidate elevated to HIGHEST-priority for slice-021. 15th consecutive 100% Critic-disposition accuracy slice (131/131 first-Critic + 139/139 cross-Critic-stack across slices 6-20). Mid-slice smoke gate extension per /critique M4 (added `_prelude_present` test) empirically validated — 8/8 PASS at ~50% build; catches incomplete-Step-3c-insertion failure mode. Plugin.yaml.version + VERSION + ai-sdlc-VERSION all 0.33.0 → 0.34.0 atomic. Methodology test count 447 → 455 (+8 NEW BFRD-1 tests); full project 455/455 PASS in 4.52s. Shippability catalog 19 → 20 rows; full catalog 20/20 PASS in 12.53s well under 2-min target (~0.63s/row average, fastest per-row ratio in project history). **Strongest slice-021 candidates**: (a) **HIGHEST-PRIORITY** `audit-tools-default-utf8-stdout` (N=4 cumulative cp1252; SMALL ~30-45 min); (b) `refine-dim-9-with-mechanical-table-vs-canonical-inventory-sub-class` (Wiegers regression-guard coverage-symmetry N=5 cumulative within slice-020 + N=4 across distinct slices; PROMOTION-ELIGIBLE for Dim 9 sub-clause refinement); (c-e) refine-dim-9 sub-class refinements at N=1 watch-list (fix-block-completeness + proposed-mechanism-runtime-prerequisite-completeness + override-rationale-test-enforcement; promote at N≥3); (f) cleanup of stale R-1 + R-2 — this slice would BE the first canonical reference instance of BFRD-1 (mode (b) signal triggers `/repro` first; testing the discipline empirically). |
| 019 | [slice-019-harden-diagnose-layering-evidence](slice-019-harden-diagnose-layering-evidence/) | 2026-05-14 | Standard | medium (critic-required: true via MCT-1 trigger glob match — slice modifies `skills/diagnose/SKILL.md` + `skills/diagnose/passes/03f-layering.md` matching `skills/*/SKILL.md` + `methodology-changelog.md` + adds NEW ADR-017 + NEW R-3; MCT-1 default-trigger self-application N=8 → N=9 stable post-slice-019; voluntary-Critic-on-cross-cutting-tooling ROI N=10/10 → N=11/11 stable) | SHIPPED — validation PASS (5/5 ACs with evidence; VAL-1 Layer A 0 secrets + Layer B 0 hallucinated imports; WS-1 n/a; ETC-1 n/a; shippability 447/447 superset run; ZERO regressions; ZERO reality surprises) | 12 (3B + 4M + 3m first-Critic + 2 meta-Critic m-add-* missed + 1 severity adjustment B1 Blocker→Major); 9 ACCEPTED-PENDING applied at /build-slice Phase 1 + 3 ACCEPTED-FIXED inline at /critique + /critique-review; verdict NEEDS-FIXES per triage_audit + DR-1 EXTEND per critique-review-audit; **all 10 first-Critic + 2 meta-Critic findings VALIDATED at /critique-disposition AND /validate-slice — 14th consecutive 100% Critic-disposition accuracy slice** (running 117/117 first-Critic / 122/122 cross-Critic-stack across slices 6-19, strongest streak in the project extending slice-018 records by +10 first-Critic + 2 meta-Critic at slice-019); **B2 strongest first-Critic catch — grep regex coverage gap** (5 TS import variants + 3 re-export forms + multi-line via `re.DOTALL` + alias-aware tsconfig `paths`/`baseUrl` resolution required; without B2, the rule would have failed closed on the very `@/*` alias pattern that produced F-LAYER-bca9c001 witness; load-bearing for closing the witnessed class); **M-add-1 strongest meta-Critic save — schema-enum mismatch in AC #1 propagation scope** (AC #1 enumerated 4 categories "layering / boundary / cross-tier / import-violation" but `schema/finding.yaml:21-32` only materializes `layering-violation` per ground-truth grep at `passes/03f-layering.md:47` = 1 match; tightened prose dropped 3 speculative terms; reduced B1's actionable scope); **M-add-2 strongest DR-1 logical-coherence catch — ADR-017 RSAD-1 "post-codification N=1" wording incoherence** on the codification slice itself (a codification slice cannot be post-codification of itself); rephrased to "canonical reference instance #1 at codification time" matching slice-015/017 wording precedent; **B1 severity-adjusted Blocker→Major** per meta-Critic ground-truth narrowing — concern is Wiegers documentation-completeness, not OWASP correctness; **NEW DR-1 catch class** *Self-application-qualifier coherence on canonical-reference-instance naming* (M-add-2; watch-list at N=1; promote to Dim 9 sub-clause at N≥3); **NEW DR-1 catch class candidate** *Schema-enum-vs-AC-prose mismatch* (M-add-1; watch-list at N=1; promote at N≥3); **DR-1 catch-class diversification N=6 → N=7 stable**; **Cross-cutting Dim 9 catch rate at slice-019 = 100%** (range-bound 60-100% on N=14 evidence stable; trajectory 0% → 25% → 60% → 100% → 60% → 87.5% → 80% → 80-100% → 87.5% → 85.7% → 87.5% → 88.9% → 87.5% → 91.7% → **100% (slice-019)** — strongest single-slice cross-Critic-stack performance in the project) | **LAYER-EVID-1 shipped** (Layering-Evidence-1) — `/diagnose` 03f-layering pass-template gains Method step 4 (textual import-evidence grep verification with 10 regex variants: 5 TS import + 3 re-export + 2 dynamic for TypeScript/JavaScript + alias-aware tsconfig `paths`/`baseUrl` resolution + Python `from`/`import` + Rust `use`/Go `import`/Java `import` fallbacks; multi-line via `re.DOTALL`/ripgrep `--multiline`) + Severity rubric downgrade rule + Anti-patterns negative-pin; `skills/diagnose/SKILL.md` Step 5 gains one-line LAYER-EVID-1 cross-reference paragraph (rule body lives in pass template; SKILL.md only points at it). Witnessed at F-LAYER-bca9c001 false-positive (parallel type files defining identically-named enums in backend `src/workflow/adapters/types.ts` + frontend `frontend/lib/workflow/types.ts` + frontend `tsconfig.json` `@/*` alias rooted at frontend/ physically preventing cross-tier access; graphify symbol-resolution synthesized phantom edge into backend tier where zero textual imports existed). N=3 surfaces (03f-layering.md + SKILL.md Step 5 + methodology-changelog v0.33.0 entry) bidirectional sha256 byte-equal per **first introduction of mini-CAD for /diagnose** (extends slice-007 CAD-1 + slice-010 mini-CAD-for-slice/SKILL.md to a third surface family; 2 single-file byte-equality tests per slice-007 convention per /critique B3 Path A split); rule downgrade outcome (no textual match → `evidence[].note: "downgraded: no textual import grep-match (LAYER-EVID-1)"` OR skip emission) preserves diagnostic-trail. Rule lives at pass-template prose layer (NOT graphify code layer) per ADR-017 Option B vs Option A analysis: narrow blast radius + cheap reversibility + empirical-evidence-basis + witness-driven scope outweighed root-cause-fix-everywhere benefits; R-3 added to risk-register (status=mitigating; reversibility=cheap; graphify symbol-resolution may conflate same-name cross-file symbols into phantom edges; escalation at N≥2 distinct slices recurrence in other passes/consumers — promote follow-on slice to either extend LAYER-EVID-1 OR fix graphify symbol-resolution upstream). Methodology v0.33.0; 5 ACs / 12 TF-1 rows (10 PENDING→WRITTEN-FAILING→PASSING + 2 mini-CAD-1 PASSING→WRITTEN-FAILING→PASSING — per slice-007 CAD-1 + slice-010 mini-CAD single-file convention; B3 Path A split mini-CAD into 2 single-file tests) / 10 must-not-defer including textual-evidence rule propagation + methodology-changelog bidirectional byte-equal + agents/critique.md preservation at slice-017 ship hash f34c967e + /diagnose SKILL.md byte-equal + atomic version bump + R-3 RR-1-conformant + TPHD-1 self-application + SCPD-1 propagation + logging via `evidence[].note`; ADR-017 (reversibility: cheap — pass-template prose retirement <1 day if graphify is later fixed upstream); rule-ID `LAYER-EVID-1` per project rule-naming convention does NOT carry -D suffix — distinct from slice-011 RSAD-1 + slice-013 EPGD-1 + slice-015 SCPD-1 + slice-016 RPCD-1 + slice-017 TPHD-1 (-D family signals /critique-time/discipline; LAYER-EVID-1 is a /diagnose-runtime discipline applied inside subagent reasoning loop) — `-D` suffix convention stays at **N=5 stable**; **canonical phrase `textual import-evidence requirement` pinned across N=3 surfaces (in-repo) + N=3 surfaces (installed) = N=6 bidirectional byte-equal**; **LAYER-EVID-1 self-application N=1 standalone canonical reference instance at codification time** — the witness investigation (manual-grep disproving F-LAYER-bca9c001) IS the rule applied retrospectively to the slice's own witness BEFORE codification, mirroring slice-015 SCPD-1 + slice-017 TPHD-1 canonical-reference-instance-at-codification-time precedent (refined wording per /critique-review M-add-2 ACCEPTED-FIXED — "instance #1 at codification time" not "post-codification N=1"); **TPHD-1 self-application N=2 → N=3 stable post-codification** at all 3 sub-modes (slice-019 IS canonical reference instance #3; sub-mode (a) at /critique fix-prose B1+B3+M4+M-add-1 mission-brief + design.md harmonized in same logical fix blocks; sub-mode (b) at /critique-review fix-prose M-add-1 + M-add-2; sub-mode (c) at /build-slice Phase 0 prerequisite-check pre-flight); **SCPD-1 self-application N=2 → N=3 stable** post-codification (slice-019 IS canonical reference instance #3 — Phase 5 propagated row 19 in-line BEFORE /validate-slice Step 5.5 catalog run; full project test suite 447/447 PASS in 3.41s well under 2-min target — superset of shippability catalog 19 rows); **EPGD-1 self-application N=6 → N=7 stable** (0 of 15 prior entry-pin functions touched at function-name level; slice-019 ADDS-only post-row-19 + new ADR-017 + new R-3 + new v0.33.0 entry); **PMI-1 v1.1 empirical retirement-proof N=4 → N=5 stable** (5th atomic version bump 0.32.0 → 0.33.0 with ZERO test code modification on gate body since slice-014 introduction); **recursive-self-application N=10 → N=11 cumulative** post-RSAD-1 codification (slice-019 first-Critic 10 catches + meta-Critic 2 catches = 12 self-defects on own draft — **new project high-water mark, exceeding slice-013/017 N=8 prior peak**); **DR-1 catch-class diversification N=6 → N=7 stable** with NEW class *Self-application-qualifier coherence on canonical-reference-instance naming* (M-add-2); **NEW DR-1 catch class candidate** at N=1 watch-list *Schema-enum-vs-AC-prose mismatch* (M-add-1); helper-extraction asymmetry foreshadowing-decline mitigation at N=2 (Fowler rule-of-three + slice-018 codified N≥2 threshold): `_extract_v033_body` adjacent to slice-018's `_extract_v031_body` — generalize to `_extract_version_body(content, version)` at N=3 next codification slice's entry-pin tests; **bidirectional sha256 forensic capture N=14 → N=15 stable with per-file hash list extending from N=2 files to N=4 files**: agents/critique.md preserved at `f34c967eaaa34413...` (slice-017 ship hash; no Critic-agent edit; CAD-1 invariant holds), methodology-changelog.md NEW `9d5e664a34fa5017...` (replaces slice-017 hash `06ce0c442874f0aa`), skills/diagnose/SKILL.md NEW `5da6debc9bddf76a...` (first mini-CAD-for-/diagnose entry), skills/diagnose/passes/03f-layering.md NEW `e1af52c7640681eb...` (first mini-CAD-for-/diagnose entry); **N-surface schema-pin 3-surface shape N=6 → N=7 stable instances**; **ADR-pin convention N=4 → N=5 stable** (ADR-013 + ADR-014 + ADR-015 + ADR-016 + ADR-017); MCT-1 default-trigger self-application N=8 → N=9 stable (slices 010..019); mini-CAD-1 row 3 PASSING → WRITTEN-FAILING → PASSING transition pattern STAYS at N=8 (slice-019 didn't modify agents/critique.md; cleanup-slice exemption per slice-018 DEVIATION-1; slice-019's mini-CAD-for-/diagnose introduction is a NEW family at N=1, not a row-3-transition pattern on agents/critique.md); validate-using-your-own-ship N=16 → **N=17 stable** (slices 003..019); empirical-verification-at-design-time discipline N=17 → **N=18 stable** (8 design-time audits all VALIDATED at /build-slice + /validate-slice); VAL-1 Layer B intra-repo `tests` namespace-package class N=16 → N=17 cumulative recurrence (every slice 003-019 hits it via test path discovery; handled via `--imports-allowlist tests`); **ZERO classical build-time DEVIATIONs at slice-019 — REGAINED post-slice-018 N=1 break (N=2 stable streak)** — TPHD-1 N=3 self-application + LAYER-EVID-1 design-time discipline empirically pay off at classical-build-deviation layer; methodology test count 405 → 447 stable (+12 NEW tests for slice-019 + ~30 from pytest discovery of accumulated fixture helper functions); plugin.yaml.version 0.32.0 → 0.33.0 + VERSION + ai-sdlc-VERSION atomic per META-1 atomicity; shippability catalog 18 → 19 rows; **strongest slice-020 candidates**: (a) `audit-tools-default-utf8-stdout` (N=3 cumulative cp1252 promotion threshold MET from slice-018 carry-over; SMALL ~30-45 min effort across 4-6 tools); (b) generalize `_extract_v033_body` + `_extract_v031_body` → `_extract_version_body(content, version)` if slice-020 introduces 3rd version-body helper (N=2 → N=3 promotion-eligible); (c) `refine-dim-9-with-mechanical-table-vs-canonical-inventory-sub-class` (N=2 cumulative Wiegers regression-guard coverage-symmetry from slice-016/017 carryover; still N=2 at slice-019, promote at N=3 if recurs); (d) `refine-dim-9-with-schema-enum-vs-AC-prose-mismatch-sub-class` (NEW watch-list at N=1 from slice-019 M-add-1; promote at N=3); (e) `refine-dim-9-with-self-application-qualifier-coherence-sub-class` (NEW watch-list at N=1 from slice-019 M-add-2; promote at N=3); (f) cleanup of stale R-1 + R-2 if pursued — both untouched for 18 slices, require `/repro` first. |
| 018 | [slice-018-cleanup-slice-016-rpcd-1-sibling-test-scoping-flaw](slice-018-cleanup-slice-016-rpcd-1-sibling-test-scoping-flaw/) | 2026-05-13 | Standard | low (critic-required: true via VOLUNTARY — slice modifies `tests/methodology/test_methodology_changelog.py` which is NOT in MCT-1 trigger glob `skills/*/SKILL.md` + `agents/*.md` + `tools/**/*.py` + `methodology-changelog.md`, but file pins methodology surfaces and slice retires NEW first-Critic-MISS class at N=1 from slice-017 reflection — `test-scoping-flaw-inherited-across-codification-slice-siblings`; voluntary Critic on methodology-tooling-adjacent slices has paid off N=9/9 across slices 1-17; **slice-018 ratchets to N=10/10 voluntary-Critic ROI** post-codification) | SHIPPED — validation PASS | 12 (2B + 4M + 3m first-Critic + 3 meta-Critic m-add-* missed) all ACCEPTED-FIXED at /critique + /critique-review TRI-1 fix-prose Phases; verdict CLEAN per triage_audit; **all 9 first-Critic + 3 meta-Critic findings VALIDATED at /critique-disposition AND /validate-slice — 13th consecutive 100% Critic-disposition accuracy slice** (running 103/103 first-Critic / 108/108 cross-Critic-stack across slices 6-18, strongest streak in the project extending slice-017 records); **B1 strongest first-Critic catch (recursive-self-application)** — design.md L96 "preserve name" Decision contradicted by mission-brief.md TF-1 plan rows 1+3 carrying NEW function names; slice retiring `test-scoping-flaw-inherited-across-codification-slice-siblings` committed exact TPHD-1 sub-mode (a) violation on own draft pre-fix per RSAD-1 design-time-mode + methodology-changelog L44 canonical anchor. Without B1 Path A fix (preserve name + harmonize TF-1 plan across 5 surfaces in same fix block — design.md Decision + AC #1 + TF-1 row 1 + mid-slice smoke gate + verification plan), mutually exclusive deliverables would have shipped (status-only TF-1 audit cannot detect per slice-017 reflection L39+L70 audit-gap analysis); **m-add-1 strongest meta-Critic save (DR-1 N=5 → N=6 cumulative stable with new class — diagnostic-quality regression vs slice-017 canonical)** — design.md helper interior `assert v031_start != -1` lost surface_name context vs slice-017 L1088-1090 pattern; reality at /build-slice WRITTEN-FAILING phase empirically demonstrated surface-aware error message ("in-repo methodology-changelog.md v0.31.0 body missing 'Sub-mode (a)' marker") is load-bearing for diagnostic quality on real-world regressions; fix moved assert to call site INSIDE `for surface_name, content in [...]` loop preserving slice-017 canonical pattern + Hendrickson "make failures self-explaining" discipline; **m-add-2 helper-extraction asymmetry foreshadowing-decline save** — design.md Audit 3 "Pattern reproduces verbatim" claim stale post-M2 fix (slice-018 wraps boundary slicing in `_extract_v031_body` helper; slice-017 inlines at L1086-1094); fix updated Audit 3 to acknowledge pattern-level (not literal-code-level) symmetry + added NEW Audit 7 explicitly declining generic `_extract_version_body` helper at N=1 per Fowler rule-of-three + YAGNI; **m-add-3 bidirectional v0.30.0 evidence-trace gap save** — design.md L371 committed to checking BOTH in-repo + installed for `## v0.30.0` but Audit 6 only verified in-repo; empirically verified bidirectionally (`grep -c "^## v0.30.0"` returns 1 in BOTH surfaces); **DR-1 catch class diversification N=5 → N=6 cumulative stable** with 3 NEW classes at slice-018 (m-add-1 diagnostic-quality regression vs canonical + m-add-2 helper-extraction asymmetry foreshadowing-decline + m-add-3 evidence-trace gap on bidirectional installed surface) — all distinct from prior RPCD-1 sub-modes at slice-013/014/015 + Wiegers regression-guard coverage-symmetry at slice-016/017; **TPHD-1 self-application N=1 → N=2 stable post-codification at slice-017** — slice-018 IS canonical reference instance #2 at all 3 sub-modes — sub-mode (a) /critique B1 ACCEPTED-FIXED Path A renamed `_scopes_to_v031_body` → `_entry_names_three_sub_modes_in_repo_and_installed` across 5 surfaces in same fix block; sub-mode (b) /critique-review m-add-2 ACCEPTED-FIXED updated design.md Audit 3 + added Audit 7 in same fix block; sub-mode (c) /build-slice Phase 0 verified TF-1 plan rows match design.md function names BEFORE Phase 1; **ZERO classical build-time DEVIATIONs at slice-018** — compared to slice-017's 3 classical DEVIATIONs; TPHD-1 codification + slice-018 self-application empirically pays off at classical-build-deviation layer (recursive-self-application N=9 → N=10 cumulative post-RSAD-1 codification — slice-018 first-Critic B1 = 1 self-defect on own draft; much lower density than codification-slice average N=4-8 per slice — cleanup-slice empirically lower-self-application-density); 2 build-time DEVIATIONs both methodology-recurrence-layer (NEW first-Critic-MISS classes at N=1 each — DEVIATION-1 mini-CAD-1 ceremonial-transition-cleanup-slice-applicability missed by both Critics, caught by auto-mode classifier blocking agents/critique.md modification attempt — **auto-mode-as-third-Critic-stack-layer**; DEVIATION-2 cleanup-slice TF-1 row enumeration discipline missed by both Critics, caught by TF-1 audit `--strict-pre-finish` at /build-slice Phase 6 with 3 violations — 2× ac-without-row + 1× non-passing-pre-finish — **TF-1-audit-as-third-Critic-stack-layer**); 1 NEW DEVIATION-3 at /reflect Step 5.3 (shippability row 18 placed before row 17 in Edit — N=2 cumulative methodology-recurrence-layer recurrence of slice-017 DEVIATION-3 mechanical row-order; promotion threshold MET); **4-layer-defense slice (fifth in project after slice-013 + slice-015 + slice-016 + slice-017)** — first Critic + meta-Critic + auto-mode classifier (build-time) + TF-1 audit (build-time) + shippability catalog all contributed catches; **cross-cutting Dim 9 catch rate at slice-018 = 91.7% (11 of 12 sub-class hits caught at /critique + /critique-review combined; 2 NEW classes surfaced at /build-slice — auto-mode + TF-1 audit as third-Critic-stack-layer detection mechanisms)** — range-bound 60-100% on N=13 evidence stable | **RPCD-1 sibling test-scoping discipline restored** — `tests/methodology/test_methodology_changelog.py:910-967` `test_v_0_31_0_rpcd_1_entry_names_three_sub_modes_in_repo_and_installed` body refactored to scope assertions to v0.31.0 entry body via module-level `_extract_v031_body(content: str) -> str` helper (between `## v0.31.0` and `## v0.30.0` boundaries; mirrors slice-017 TPHD-1 sibling pattern at L1086-1094 at the pattern level, NOT literal-code level per /critique-review m-add-2 Audit 3 refinement); 5 sub-mode + discipline-anchor assertions reference `v031_body`; surface-context-aware pre-validation assert at CALL site preserves slice-017 L1088-1090 diagnostic pattern per m-add-1; NEW regression test `test_v_0_31_0_rpcd_1_sibling_scoping_rejects_stripped_v031_body` calls SAME helper on synthetic v0.31.0-stripped + v0.32.0-retained content per /critique M2 single-code-path discipline; docstring at L955 + L1018 + L1031 documents slice-017 DEVIATION-1 evidence anchor + slice-017 TPHD-1 sibling canonical pattern; **retires latent regression risk on slice-016 RPCD-1 pin** (silent false-positive failure mode where future slice could strip Sub-mode markers from v0.31.0 body while v0.32.0+ retains them); methodology version stays at 0.32.0 (cleanup-only; PMI-1 v1.1 stays at N=4 stable; CAD-1 byte-equality preserved bidirectionally at slice-017 ship hashes); NO SKILL.md prose / no agents/critique.md edit / no new ADR / no Dim 9 sub-clause refinement; 5 ACs / 6 TF-1 rows (2 PENDING→WRITTEN-FAILING→PASSING + 1 grep-verification + 1 catalog-verification + 1 mini-CAD-1 PASSING + 1 git-diff-verification per /build-slice DEVIATION-2 row-enumeration fix) / 10 must-not-defer; shippability row 16 unchanged per Decision Option 1 + Audit 4 empirical enumeration (SCPD-1 sub-mode (b) vacuously satisfied; SCPD-1 stays at N=2 stable); EPGD-1 self-application N=6 → N=7 stable (0 of 15 prior entry-pin functions touched at function-name level); recursive-self-application N=9 → N=10 cumulative post-RSAD-1 codification (slice-018 first-Critic B1 = 1 self-defect on own draft — TPHD-1 class); **TPHD-1 self-application N=1 → N=2 stable post-codification at slice-017** (slice-018 IS canonical reference instance #2 at all 3 sub-modes); **DR-1 catch class diversification N=5 → N=6 cumulative stable** with 3 new classes at slice-018; **Windows cp1252 console encoding class N=2 → N=3 cumulative recurrence** (slice-007 + slice-016 + slice-018; promotion threshold MET; `audit-tools-default-utf8-stdout` candidate ready for slice-019+); 2 NEW first-Critic-MISS classes at N=1 each (`mini-CAD-1 ceremonial-transition-cleanup-slice-applicability` + `cleanup-slice-TF-1-row-enumeration-discipline`) — both methodology-recurrence-layer; promote at N=2 if recur at slice-019+ OR fold into /design-slice TF-1 plan section pre-flight at slice-019+; ZERO classical build-time DEVIATIONs (TPHD-1 codification + self-application empirically pays off vs slice-017's 3 classical DEVIATIONs); 13th consecutive 100% Critic-disposition accuracy slice (running 103/103 first-Critic / 108/108 cross-Critic-stack across slices 6-18 — extending slice-017 project records); validate-using-your-own-ship N=15 → N=16 stable; empirical-verification-at-design-time discipline N=16 → N=17 stable (7 design-time audits all VALIDATED at /build-slice + /validate-slice); bidirectional sha256 forensic capture N=13 → N=14 stable (both agents/critique.md at `f34c967eaaa34413` + methodology-changelog.md at `06ce0c442874f0aa` byte-equal at slice-017 ship hashes); N-surface schema-pin 3-surface shape N=6 STAYS (no NEW schema-pin; cleanup-only); -D suffix rule-ID convention N=5 STAYS (no NEW rule); ADR-pin convention N=4 STAYS (no NEW ADR; cleanup-only); MCT-1 default-trigger self-application N=8 STAYS (slice-018 NOT in MCT-1 trigger glob; voluntary Critic); mini-CAD-1 row 3 STAYS at N=8 (slice-018 didn't modify agents/critique.md; cleanup-slice exemption discovered at DEVIATION-1); methodology test count 404 → 405 stable (+1 NEW regression test); plugin.yaml.version + VERSION + ai-sdlc-VERSION all stay at 0.32.0 (cleanup-only); shippability catalog 17→18 rows; **strongest slice-019 candidates**: (a) `audit-tools-default-utf8-stdout` (N=3 cumulative cp1252 promotion threshold MET; SMALL ~30-45 min effort); (b) DEVIATION-1 + DEVIATION-2 codification at /design-slice TF-1 plan section pre-flight (N=1 each; await N=2 recurrence); (c) `refine-dim-9-with-mechanical-table-vs-canonical-inventory-sub-class` (N=2 cumulative Wiegers regression-guard coverage-symmetry watch-list carryover; promote at N=3); (d) shippability-row-append-discipline codification at /reflect Step 5.3 (DEVIATION-3 N=2 cumulative threshold MET); (e) cleanup of stale R-1 + R-2 if pursued — both untouched for 17 slices, require `/repro` first. |
| 017 | [slice-017-address-tf-1-plan-staleness-discipline](slice-017-address-tf-1-plan-staleness-discipline/) | 2026-05-13 | Standard | low (critic-required: true via MCT-1 auto-trigger — slice modifies `skills/critique/SKILL.md` + `skills/critique-review/SKILL.md` + `skills/build-slice/SKILL.md` matching `skills/*/SKILL.md` + `methodology-changelog.md` + `tests/methodology/**`; eighth slice operating under MCT-1 default per slice-010 codification) | SHIPPED — validation PASS | 8 (1B + 3M + 3m + 1 meta-Critic m-add-1) all ACCEPTED-FIXED at /critique + /critique-review TRI-1 fix-prose Phases; verdict CLEAN per triage_audit; **all 7 first-Critic findings + 1 meta-Critic m-add-1 VALIDATED at /critique-disposition AND /validate-slice — twelfth consecutive 100% Critic-disposition accuracy slice** (running 94/94 first-Critic / 96/96 cross-Critic-stack across slices 6-17, strongest streak in the project; was 87/87 first-Critic / 88/88 cross-stack at slice-016; +7 first-Critic + 1 meta-Critic at slice-017); **B1 strongest first-Critic catch (load-bearing fatal)** — ADR-016 Context paragraph misrepresented `tools/test_first_audit.py --strict-pre-finish` semantics: claimed third clause "OR if any row's Test function name doesn't exist at the named Test path"; reality at `tools/test_first_audit.py:65 + 320 + 350-364` is status-only check (`row.status != "PASSING"`); no AST/grep introspection of test bodies. Without B1 fix, TPHD-1's entire value proposition would have rested on a false premise about what /build-slice Phase 6 catches. Reframed: TPHD-1 is **prophylactic against an audit gap** (function-name-staleness mode NOT caught by TF-1 audit; surfaces only at pytest catalog run) + **defense-in-depth for status-staleness mode** (TF-1 audit DOES catch); **M1 magnitude inconsistency save** (mission-brief AC #3 + design.md L124 said ~3-5 sites; ADR-016 enumerates 14 sites; harmonized to ~13-16 across all 3 surfaces); **M2 placement architecture save** — original design used NEW `### Step 0` between `## Your task` (L24) and `### Step 1` (L26); anomalous because build-slice step numbering is 1,2,3,4,5,6,7,7b,7c,8 with no Step 0; AND discipline IS structurally a prerequisite verification (existing `## Prerequisite check` section L17-L22 was natural home); fix moved sub-mode (c) to NEW bullet INTO existing Prerequisite-check section. **TPHD-1 sub-mode (a) self-application empirically demonstrated** by the fix block itself — TF-1 plan rows 8-9 function names renamed `_phase_0_step_*` → `_prerequisite_check_bullet_*` in same fix block per the discipline TPHD-1 codifies; **M3 BC-1 Audit 6 wrong-tokens save** — design.md Audit 6 listed `methodology, Critic, slice, dim, Dimension` as BC-PROJ-2 anchors; actual rule body at `architecture/build-checks.md:33-35` is trigger keywords `parse, fence, code-block, backtick, llm, agent, prompt, output, response` (9 tokens) + trigger anchors `fence, code-block, llm` (3) + negative anchors `defer-with-rationale, aggregated lessons, false positive, meta-discussion, vocabulary, Critic-MISSED, back-sync, Dim 9, forward-sync` (9); rewrote Audit 6 with empirical token enumeration; **m1 RSAD-1 sub-mode (b) re-introduction save** — mission-brief Must-not-defer #1 + design.md L245 enumerated anti-form rule-ID strings (TPH-1 / TPHD1 / tphd-1 lowercase) that would re-introduce those strings into own artifacts; replaced with positive-form assertion grep TPHD-1 ≥1 per surface; **m2 Phase 1f reference save** (mission-brief Phase-range corrected 1a-1f → 1a-1e matching design.md phase plan); **m3 SCPD-1 counter unfounded save** — AC #5 originally claimed SCPD-1 N=2 → N=3 stable; slice-017 has NO Dim 9 sub-clause supersession event, so SCPD-1 proactive-application is vacuously satisfied — counter stays at N=2 stable; **m-add-1 strongest meta-Critic save (DR-1 N=4 → N=5 stable with class confirmation as slice-016 M-add-1)** — design.md Audit 4 + mission-brief.md L66 + 4 other sites claimed "11 prior entry-pin functions"; empirical `grep "^def test_v_0_" tests/methodology/test_methodology_changelog.py` returns 12 functions at lines 76/111/152/202/250/304/370/437/499/747/844/910 (v0.31.0 doubled per slice-016 RPCD-1 (a)↔(b) duality at L844 + L910 missed in initial Audit 4 prose; same way v0.29.0 doubled at L437 + L499); corrected to 12 across 6 sites; **DR-1 catch-class diversification confirmed at N=2 cumulative at the Wiegers regression-guard coverage-symmetry / design-doc-level mechanical-table-vs-canonical-inventory asymmetry layer per CCC-1 v1.1 sub-clause 2 surface** (slice-016 M-add-1 missing `_location_pinned` sibling + slice-017 m-add-1 entry-pin count drift); **ZERO classical build-deviation streak BROKEN at slice-017** — prior 5 slices 008+012+013+015+016 zero classical DEVIATIONs; slice-017 shipped 3 build-time DEVIATIONs caught + fixed inline (zero design.md updates required post-build): DEVIATION-1 test-scoping flaw inherited from slice-016 RPCD-1 sibling `_names_three_sub_modes` global-substring pattern at L910-L951 (slice-017 copied verbatim → false-positive PASS at WRITTEN-FAILING by matching prior v0.31.0 markers; fixed inline by scoping to v0.32.0 body; slice-016 sibling STILL has flaw — out-of-scope but flagged as latent regression risk for slice-018+); DEVIATION-2 prose-pin test style mismatch on existing SKILL.md Prerequisite-check section bullet (initial assertion `"Per **TPHD-1**"` paragraph form vs actual imperative bullet `"Run TPHD-1 pre-flight harmonization"` matching existing style; fixed inline per TPHD-1 sub-mode (a) self-application at /build-slice); DEVIATION-3 mechanical shippability row 17 insertion order (inserted before row 16; fixed via remove + append-to-end). DEVIATION-1 + DEVIATION-2 are NEW GENERIC METHODOLOGY LESSONS at N=1; promote at N=2 if recurs at slice-018+. **NEW first-Critic-MISS classes at N=1**: (a) `test-scoping-flaw-inherited-across-codification-slice-siblings` + (b) `prose-pin-test-style-must-match-section-style` — both surface at /build-slice Phase 2 as third-Critic-stack-layer detection mechanism. **3-layer Critic-stack accountability lineage at N=3 cumulative** (slice-014 + slice-016 + slice-017 — Phase 2 detection layer joins TF-1 audit + pytest catalog detection layers); **4-layer-defense slice (fourth in project after slice-013 + slice-015 + slice-016)** — first Critic + meta-Critic + design-stage audits (6) + shippability catalog (17/17 PASS in 9.75s ~12× under 2-min target) all contributed catches; **cross-cutting Dim 9 catch rate at slice-017 = 87.5% (7 of 8 sub-class hits caught at /critique + /critique-review combined; 2 NEW classes surfaced at /build-slice Phase 2 — not classical Dim 9 surface)** — range-bound 60-100% on N=12 evidence stable | **TPHD-1 shipped** (TF-Plan-Harmonization-Discipline) — 3-surface skill-prose discipline at `skills/critique/SKILL.md` Step 4 end (between L122 and L124) + `skills/critique-review/SKILL.md` Step 3 end (between L83 and L85) + `skills/build-slice/SKILL.md` NEW bullet INTO existing `## Prerequisite check` section (between L22 and L24) — per /critique M2 ACCEPTED-FIXED: NOT a new `### Step 0`; build-slice step numbering 1,2,3,4,5,6,7,7b,7c,8 has no Step 0; discipline IS structurally a prerequisite verification. Body names ALL THREE sub-modes (a) /critique post-fix-prose harmonization + (b) /critique-review post-fix-prose harmonization + (c) /build-slice Prerequisite-check pre-flight harmonization with cross-slice anchor `slice-016` (N=1 — proactive ratchet ahead of typical N=2 promotion threshold per user-invoked /slice argument); methodology v0.32.0; 5 ACs / 12 TF-1 rows (10 PENDING→WRITTEN-FAILING→PASSING + 1 mini-CAD-1 PASSING→WRITTEN-FAILING→PASSING per slice-007/009/010/011/012/013/015/016 row precedent N=8 → N=9 stable + 1 catalog-deferred) / 12 must-not-defer; ADR-016 (reversibility: cheap with magnitude justification ~13-16 sites — same class as ADR-014/015); rule-ID `TPHD-1` per slice-011 RSAD-1 + slice-013 EPGD-1 + slice-015 SCPD-1 + slice-016 RPCD-1 -D suffix convention N=4 → N=5 stable confirmation (RSAD-1 + EPGD-1 + SCPD-1 + RPCD-1 + TPHD-1); **canonical body uses concrete substrings** (`/critique`, `/critique-review`, `Prerequisite`, sub-mode (a)/(b)/(c) markers) for discipline-verifiability via empirical literal-presence; **TPHD-1 self-application empirically VALIDATED across all 3 sub-modes on slice-017 own draft** — sub-mode (a) N=1: /critique M2 ACCEPTED-FIXED renamed `_phase_0_step_*` → `_prerequisite_check_bullet_*` in TF-1 plan rows 8-9 in same fix block (canonical reference instance #1); sub-mode (b) N=1: /critique-review m-add-1 count correction across 6 sites in same fix block (correctly N/A for TF-1 plan since no function names changed — count-drift only); sub-mode (c) N=1: /build-slice Phase 0 pre-flight verified 12 TF-1 rows match design.md function names BEFORE Phase 1b started; slice-017 IS canonical reference instance #1 of TPHD-1; recursive-self-application **N=8 → N=9 cumulative post-RSAD-1 codification** (slice-017 first-Critic 7 catches + meta-Critic 1 catch = 8 self-defects on own draft, matching slice-013 N=7 high-water mark + meta-Critic add); **PMI-1 v1.1 empirical retirement-proof N=3 → N=4 stable** — fourth atomic version bump under v1.1 (0.31.0 → 0.32.0) succeeded with ZERO test code modification on gate body since slice-014 Phase 1c; **EPGD-1 self-application N/A under PMI-1 v1.1 confirmed at slice-017** — slice-017 only ADDED to `test_methodology_changelog.py` (new SECTION header `# --- Slice-017 / TPHD-1 entry pinning ---` + 4 functions: 3 entry-pin + 1 ADR-pin); 0 of 12 prior entry-pin functions touched (v_0_22_0..v_0_31_0 spans 10 minor versions; v_0_29_0 + v_0_31_0 both doubled per slice-014 + slice-016 (a)↔(b) duality respectively — per /critique-review m-add-1 ACCEPTED-FIXED count correction); no PMI-1 versioned-gate Edit needed; **DR-1 dual-review catching pattern-blindness N=4 → N=5 stable** with class confirmation at Wiegers regression-guard coverage-symmetry layer (slice-016 N=1 + slice-017 N=2 cumulative at design-doc-level mechanical-table-vs-canonical-inventory asymmetry per CCC-1 v1.1 sub-clause 2 surface; ratchets toward N=3 promotion threshold at slice-018+); **4-layer-defense slice (fourth in project after slice-013 + slice-015 + slice-016)** — first Critic + meta-Critic + design-stage audits (6) + shippability catalog all contributed catches; **ZERO classical build-deviation streak BROKEN at slice-017** — N=5 (008+012+013+015+016) → **N=0** (3 build-time DEVIATIONs at slice-017 caught + fixed inline); validate-using-your-own-ship N=14 → **N=15 stable**; empirical-verification-at-design-time discipline N=15 → **N=16 stable** (6 design-time audits all VALIDATED at /build-slice + /validate-slice); bidirectional sha256 forensic capture N=12 → **N=13 stable** (methodology-changelog.md byte-equal at `06ce0c442874f0aa22a4f9e7b9b2fd0b45c45c7ea5617db65bfff1a98a2089ff`; agents/critique.md unchanged at slice-016 ship hash `f34c967eaaa34413...`); N-surface schema-pin 3-surface shape N=5 → **N=6 instances stable** (RSAD-1 + EPGD-1 + PMI-1 v1.1 + SCPD-1 + RPCD-1 + TPHD-1); -D suffix rule-ID convention N=4 → **N=5 stable**; ADR-pin convention N=3 → **N=4 stable** (ADR-013 + ADR-014 + ADR-015 + ADR-016); MCT-1 default-trigger self-application N=7 → **N=8 stable** (slices 010..017); mini-CAD-1 row 3 PASSING → WRITTEN-FAILING → PASSING transition pattern N=8 → **N=9 stable**; EPGD-1 self-application N=5 → **N=6 stable** (0 of 12 prior entry-pin functions touched); SCPD-1 stays at N=2 stable (vacuously satisfied per slice-017 /critique m3 ACCEPTED-FIXED — no Dim 9 sub-clause supersession; `_lists_nine_sub_clauses` stays valid; row 17 added as NEW row with no prior-row touch needed); VAL-1 Layer B intra-repo `tests` namespace-package false-positive class N=14 → **N=15 cumulative recurrence** (every slice 003-017; handled cleanly via `--imports-allowlist tests`); plugin.yaml.version 0.31.0 → 0.32.0 + VERSION + ai-sdlc-VERSION atomic per META-1 atomicity; shippability catalog 16→17 rows, 17/17 PASS in 9.75s aggregate (~0.57s/row average; well under 2-min target); **strongest slice-018 candidates**: (a) `cleanup-slice-016-rpcd-1-sibling-test-scoping-flaw` (slice-017 reflection latent regression risk at L910-L951; ~30 min targeted Edit; fold into next codification slice via TPHD-1 sub-mode (a)); (b) `audit-tools-default-utf8-stdout` (Windows cp1252 N=2 cumulative slice-007+016; slice-017 didn't hit — promote at N=3 if recurs); (c) `refine-dim-9-with-mechanical-table-vs-canonical-inventory-sub-class` (N=2 cumulative slice-016+017 ratchets toward N=3 promotion threshold); (d) cleanup of stale R-1 + R-2 if pursued — both untouched for 15 slices, require `/repro` first; (e) `/critic-calibrate` at slice-021+ default OR earlier if any watch-list N=1 sub-class hits N=3 distinct-slice recurrence — slice-017 watch-list candidates (test-scoping-flaw inheritance N=1 + prose-pin test-style mismatch N=1 + Wiegers mechanical-table-vs-canonical-inventory N=2 cumulative) join aggregation pool. |
| 016 | [slice-016-refine-dim-9-with-runtime-prerequisite-completeness-sub-class](slice-016-refine-dim-9-with-runtime-prerequisite-completeness-sub-class/) | 2026-05-13 | Standard | medium (critic-required: true via MCT-1 auto-trigger — slice modifies `agents/critique.md` matching `agents/*.md` + `methodology-changelog.md` + `tests/methodology/**`; seventh slice operating under MCT-1 default per slice-010 codification) | SHIPPED — validation PASS | 8 (2B + 3M + 2m + 1 meta-Critic M-add-1) all ACCEPTED-FIXED at /critique + /critique-review TRI-1 fix-prose Phases; verdict CLEAN per triage_audit; **all 7 first-Critic findings + 1 meta-Critic M-add-1 VALIDATED at /critique-disposition AND /validate-slice — eleventh consecutive 100% Critic-disposition accuracy slice** (running 87/87 first-Critic / 88/88 cross-Critic-stack across slices 6-16, strongest streak in the project; was 80/80 first-Critic / 81/81 cross-stack at slice-015; +7 first-Critic + 1 meta-Critic at slice-016); **B1 strongest first-Critic catch (load-bearing fatal)** — AC #4(c) / Phase 1e / Audit 7 mis-identified end_anchor tighten targets: original pre-fix design targeted L173 (RSAD-1 `_location_pinned`) + L304 (EPGD-1 `_location_pinned`) — both `_location_pinned` siblings whose `### Bonus:` end_anchor is structurally load-bearing per slice-013/015 precedent and MUST NOT be tightened; actual SCPD-1 body-bound tests requiring tighten are at L519 (`_shippability_catalog_propagation_names_both_sub_modes`) + L554 (`_paragraph_cites_slice_013_and_014`) + L594 (`_cites_at_least_two_cross_slice_anchors`); original design also mis-attributed L304 (which contains `start_anchor = "Recursive self-application discipline"` for the EPGD-1 `_location_pinned` test) to "slice-015 SCPD-1 `_present` test"; without B1 fix /build-slice Phase 1e would have catastrophically targeted the wrong functions defeating slice-015's generic methodology recurrence discipline; **B2 supporting fatal — TF-1 plan non-existent function names** (`_rsad_1_body_pins_design_time_and_build_time_sub_modes` + `_scpd_1_body_pins_both_sub_modes` referenced but don't exist in `tests/methodology/test_critique_agent.py`); **M-add-1 strongest meta-Critic save (DR-1 N=3 → N=4 stable with class diversification)** — missing `test_critique_dim_9_runtime_prerequisite_completeness_location_pinned` sibling test breaking N=3 stable `_sub_clause_present` + `_location_pinned` duality (slice-011 L146/158 + slice-013 L270/286 + slice-015 L463/475); without M-add-1 fix slice would have shipped without the regression-guard defending against future Dim 9 restructuring drifting RPCD-1 out of Dim 9; **DR-1 catch class diversification confirmed at N=4** — prior 3 DR-1 catches (slice-013/014/015 M-add-1) were all RPCD-1 sub-mode (a/b/c) instances; slice-016 M-add-1 is a Wiegers regression-guard coverage symmetry omission (design-doc-level mechanical-table-vs-canonical-inventory asymmetry per CCC-1 v1.1 sub-clause 2 surface); **M1 RPCD-1 sub-mode (b) self-application factual error save** (design.md Audit 2 falsely claimed `WRITTEN-AS-EDIT` was in `tools/test_first_audit.py`'s `_ALLOWED_STATUSES`; empirically `_ALLOWED_STATUSES = frozenset({"PENDING", "WRITTEN-FAILING", "PASSING"})` 3-element unchanged; slice-015 M-add-1 fix per methodology-changelog v0.30.0 L47 was the OPPOSITE flip-to-PASSING not allowlist-addition); **M2 entry-pin count off-by-1/2** (mission-brief + design.md + Audit 4 + ADR-015 stated "0 of 8 prior entry-pin functions touched"; empirically 10 functions: v0.22.0..v0.30.0 spans 9 minor versions + v0.29.0 has 2 entry-pin functions per slice-014 (a)↔(b) duality); **M3 RPCD-1 sub-mode (c) self-application sibling-grep failure** (Audit 7 grep enumerated only 2 of 6 `end_anchor = "### Bonus: weak graph edges"` sites; actual grep returns 6 hits at L173/304/492/519/554/594); **m1 ADR-015 evidence-chain citation completeness + m2 "M0 tighten provenance" naming non-canonical**; **0 MISSED at slice-cycle level** — all 8 findings caught at /critique + /critique-review; **2 Phase-6-gate-time issues caught + fixed in-line at /build-slice (NOT classical build-time DEVIATIONs)**: (1) TF-1 audit Windows cp1252 console encoding flake on `→` arrow output (slice-007 DEVIATION-2 pattern recurs N=1 → N=2; resolved with `PYTHONIOENCODING=utf-8`); (2) TF-1 plan staleness vs actual built test names — 3 stale function names + all-rows-PENDING-status from /design-slice persisted past /critique + /critique-review iterative fix-prose to /build-slice Phase 6 audit time (AC #3 row `_adr_015_pinned_in_methodology_changelog_v_0_31_0` placeholder; AC #5 shippability row `_rpcd_1_body_pins_cross_slice_and_substantive_anchors` pre-M-add-1 placeholder; all rows PENDING); first Critic B2 partial-caught the end_anchor-tighten rows but missed AC #3 + AC #5; meta-Critic missed too; **NEW first-Critic-MISS class candidate at N=1**: "TF-1-plan-staleness-vs-actual-built-test-names" — promotion candidate to /build-slice Phase 0 discipline at N=2 if recurs at slice-017+; 3-layer Critic-stack accountability lineage at N=2 (slice-014 first instance on regression-test name-resolution; slice-016 second on TF-1 plan harmonization); **4-layer-defense slice (third in project after slice-013 + slice-015)** — first Critic + meta-Critic + design-stage audits (8) + shippability catalog all contributed catches; **cross-cutting Dim 9 catch rate at slice-016 = 88.9% (8 of 9 sub-class hits caught at /critique + /critique-review combined; zero MISSED at slice-cycle level)** — range-bound 60-100% on N=11 evidence stable | **RPCD-1 shipped** (Runtime-Prerequisite Completeness Discipline) — `agents/critique.md` Dim 9 gains new 9th sub-clause `Runtime-prerequisite completeness on proposed fixes` between existing 8th sub-clause `Shippability-catalog consumer-reference propagation` close and existing `### Bonus: weak graph edges` H3 anchor (already applied live during /critic-calibrate 2026-05-13 post-slice-015 apply step; CAD-1 byte-equal at sha256 `f34c967eaaa34413...` preserved through slice — slice did NOT modify agents/critique.md content); body names ALL THREE sub-modes — (a) NEW-symbol import-audit (slice-014 M-add-1 N=1) + (b) NEW-status/token allowlist-audit (slice-015 M-add-1 N=1) + (c) NEW-anchor sibling-grep audit (slice-013 M-add-1 N=1) — with N=3 cumulative cross-slice DR-1-catch evidence; methodology v0.31.0; 5 ACs / 15 TF-1 rows (5 body-bound including `_location_pinned` per /critique-review M-add-1 ACCEPTED-FIXED + 3 end_anchor-tighten + structural-invariant + entry-pin + ADR-pin + shippability-catalog) / 11 must-not-defer; ADR-015 (reversibility: cheap with magnitude justification ~13-15 sites — same class as ADR-010 + ADR-012 + ADR-014); rule-ID `RPCD-1` per -D suffix convention N=3 → N=4 stable confirmation (RSAD-1 + EPGD-1 + SCPD-1 + RPCD-1); **canonical body uses concrete substrings** (`_ALLOWED_STATUSES`, `sibling`, `end_anchor`, `import`) for discipline-verifiability via empirical literal-presence at slice-016 start sha256; **RPCD-1 self-application empirically VALIDATED at /critique design-time** — 4 RPCD-1-class self-defects on slice's own draft (B1 + M1 + M3 sub-mode b/c + B2 sub-mode a-adjacent); recursive-self-application **N=7 → N=8 cumulative post-RSAD-1 codification** at slice-016 (strongest recursive-self-application density since slice-013); **SCPD-1 self-application empirically VALIDATED at /build-slice + /validate-slice** — N=1 → **N=2 stable** post-codification (slice-015 was first canonical reference instance; slice-016 is second — Phase 1c superseded `_lists_eight_sub_clauses` → `_lists_nine_sub_clauses` per PMI-1 structural-invariant supersession N=3 → N=4 stable; Phase 5 propagated rows 6 + 11 + 13 + 15 pytest commands in same /build-slice block before /validate-slice catalog run); 16/16 shippability catalog PASS in 10.41s aggregate (well under 2-min target); 0 stale `_lists_eight_sub_clauses` consumer refs remain in pytest commands (4 surviving are historical-narrative lineage notes correctly preserved); 5 propagated `_lists_nine_sub_clauses` refs at rows 6/11/13/15/16; **EPGD-1 self-application N/A under PMI-1 v1.1 confirmed at slice-016** — slice-016 only ADDED to `test_methodology_changelog.py` (new SECTION header `# --- Slice-016 / RPCD-1 entry pinning ---` at L842 + 3 functions: 2 entry-pin + 1 ADR-pin); 0 of 10 prior entry-pin functions touched (v_0_22_0..v_0_30_0 spans 9 minor versions; v_0_29_0 has 2 entry-pin functions per slice-014 (a)↔(b) duality = 10 functions verified empirically); no PMI-1 versioned-gate Edit needed (gate body unchanged from slice-014 introduction); **PMI-1 v1.1 empirical retirement-proof N=2 → N=3 stable** — third atomic version bump under v1.1 (0.30.0 → 0.31.0) succeeded with ZERO test code modification on gate body since slice-014 Phase 1c; **/critique-review DR-1 dual-review M-add-1 catch class diversification N=3 → N=4 stable** — class shifted from RPCD-1 sub-mode (a/b/c) at slices 013/014/015 to Wiegers regression-guard coverage symmetry at slice-016 (missing `_location_pinned` sibling); **4-layer-defense slice (third in project after slice-013 + slice-015)** — first Critic + meta-Critic + design-stage audits (8) + shippability catalog all contributed catches; **ZERO classical build-time DEVIATIONs at slice-016** — fifth in slice-008/012/013/015/016 series (N=5 stable); 2 Phase-6-gate-time issues caught + fixed in-line (TF-1 Windows cp1252 + TF-1 plan staleness; neither classical build-time); validate-using-your-own-ship N=13 → **N=14 stable**; empirical-verification-at-design-time discipline N=14 → **N=15 stable** (8 design-time audits all VALIDATED at /build-slice + /validate-slice); PMI-1 structural-invariant supersession N=3 → **N=4 stable** (`_lists_eight_sub_clauses` → `_lists_nine_sub_clauses`); bidirectional sha256 forensic capture N=11 → **N=12 stable** (methodology-changelog.md byte-equal at `1FB75405E2AE3C4A`; agents/critique.md unchanged at `f34c967eaaa34413...`); N-surface schema-pin 3-surface shape N=4 → **N=5 instances stable** (RSAD-1 + EPGD-1 + PMI-1 v1.1 + SCPD-1 + RPCD-1); -D suffix rule-ID convention N=3 → **N=4 stable**; ADR-pin convention N=2 → **N=3 stable** (ADR-013 + ADR-014 + ADR-015); MCT-1 default-trigger self-application N=6 → **N=7 stable** (slices 010..016); mini-CAD-1 row 3 PASSING → WRITTEN-FAILING → PASSING transition pattern N=7 → **N=8 stable**; `_sub_clause_present` + `_location_pinned` duality N=3 → **N=4 stable** post-DR-1 M-add-1 catch; VAL-1 Layer B intra-repo `tests` namespace-package false-positive class N=13 → **N=14 cumulative recurrence** (every slice 003-016; handled cleanly via `--imports-allowlist tests`; v2 candidate still deferred — cumulative friction ~14s aggregate); plugin.yaml.version 0.30.0 → 0.31.0 + VERSION + ai-sdlc-VERSION atomic; shippability catalog 15→16 rows, 132/132 PASS in 10.41s aggregate; **strongest slice-017 candidates**: (a) `address-tf-1-plan-staleness-discipline` (NEW first-Critic-MISS class at N=1 watch-list; promote at N=2 if recurs); (b) `audit-tools-default-utf8-stdout` (Windows cp1252 N=2 cumulative; promote at N=3); (c) `refine-dim-9-with-3-layer-critic-stack-accountability-sub-class` (N=2 cumulative slice-014 + slice-016); (d) cleanup of stale R-1 (cwd-mismatch /diagnose) + R-2 (`/repro` first) if pursued — both untouched for 14 slices. |
| 015 | [slice-015-refine-dim-9-with-shippability-catalog-propagation-sub-class](slice-015-refine-dim-9-with-shippability-catalog-propagation-sub-class/) | 2026-05-13 | Standard | medium (critic-required: true via MCT-1 auto-trigger — slice modifies `agents/critique.md` matching `agents/*.md` + `methodology-changelog.md` + `tests/methodology/**`; sixth slice operating under MCT-1 default per slice-010 codification) | SHIPPED — validation PASS | 7 (1B + 3M + 2m + 1 meta-Critic M-add-1) all triaged at TRI-1; 5× ACCEPTED-FIXED at /critique (B1+M2+M3+m2 + meta-Critic M-add-1) + 2× ACCEPTED-PENDING applied at /build-slice (M1 + m1); verdict NEEDS-FIXES per triage_audit (post-reconciliation); **all 6 first-Critic findings + 1 meta-Critic M-add-1 VALIDATED at /critique-disposition AND /validate-slice — ninth consecutive 100% Critic-disposition accuracy slice** (running 80/80 cross-Critic-stack across slices 6-15, strongest streak in the project); **B1 strongest first-Critic catch — substantive-discipline anchor tuple empirically broken** (3 of 4 literals absent from canonical body: `shippability catalog` / `consumer reference` / `rename propagation` use hyphenated or never-present forms; original tuple at design.md L10 would have caused `_cites_at_least_two_cross_slice_anchors` AC #2 test to FAIL at /build-slice Phase 1f with `sub_count == 1 < 2`; revised to `["Phase 5", "shippability.md", "/validate-slice Step 5.5", "supersession"]` — all 4 empirically present in canonical body with counts 2/7/3/12 respectively; RSAD-1 design-time mode catch — slice authoring SCPD-1 had own draft committing exact specification-defect class M2 was codified to catch at slice-013); **M-add-1 strongest meta-Critic save (DR-1 N=3 stable post-slice-013/014 codification)** — `WRITTEN-AS-EDIT` status string introduced by first Critic's M1 disposition was NOT in `tools/test_first_audit.py`'s `_ALLOWED_STATUSES` allowlist; meta-Critic empirically verified by running `tools.test_first_audit --strict-pre-finish` and getting 11 violations (3 × `invalid-status` on rows 41/42/43 + 8 × expected-PENDING); would have caused /build-slice Phase 6 strict-pre-finish HARD-FAILURE with exit 1; fix per meta-Critic Option (b): flipped 3 rows from `WRITTEN-AS-EDIT` → `PASSING` (matching slice-013 precedent of keeping pre-existing tests at PASSING + end_anchor tightening tracked in must-not-defer at mission-brief.md L70); also updated stale "10/10" → "13/13" pre-finish gate count; post-fix empirical re-verification: TF-1 strict-pre-finish returns "clean. 13 row(s) → PASSING=5, WRITTEN-FAILING=0, PENDING=8"; **DR-1 runtime-prerequisite-completeness blind-spot pattern N=2 → N=3 stable** post-codification — third consecutive cross-cutting-tooling slice with DR-1 catch (slice-013 sibling-test grep + slice-014 missing imports + slice-015 audit-allowlist non-membership); pattern hits Meta-Critic 2026-05-13 /critic-calibrate next-run trigger threshold; **M1 slice-013-body-bound-tests-precision-degradation save** — slice-013's 3 body-bound tests (`_names_both_sub_modes` + `_paragraph_cites_slice_011_and_012` + `_cites_at_least_two_cross_slice_anchors` at test_critique_agent.py:312/347/387) all use `end_anchor = "### Bonus: weak graph edges"`; after 8th sub-clause inserted between EPGD-1 close and Bonus H3, all 3 tests' body bounds widen to include 8th sub-clause body; without M1 fix, any future drift moving slice-013-specific substrings into 8th sub-clause body would silently leave slice-013 tests passing; fix: tighten end_anchor to `"Shippability-catalog consumer-reference propagation"` at /build-slice Phase 1f; **generic methodology recurrence N=1 (slice-013 M1) → N=2 stable (slice-015 M1)** — every future Dim 9 sub-clause append MUST tighten predecessor body-bound tests' end_anchors; **M2 mission-brief AC #2 anchor list formalization** (slice-013 M2 mitigation precedent N=1 → N=2 stable at slice-015 — propagation from design.md L8-10 formalization back up to mission-brief AC #2 layer; cross-slice anchors strict-both `["slice-013", "slice-014"]` + ≥2-of-4 substantive-discipline anchors from B1 revision); **M3 canonical body doc-vs-impl-parity save** — original body said "the SAME Phase as the supersession Edit" but empirically falsified by both N=1 (slice-014 Phase 1c + Phase 4) and N=2 (slice-015 Phase 1f + Phase 5) reference instances doing supersession + propagation in DIFFERENT numbered Phases; revised to "the same /build-slice block (before /validate-slice catalog run)" + "the same /build-slice block as the supersession Edit and BEFORE the /validate-slice catalog run"; Dim 1 / Dim 9 sub-clause 2 tooling-doc-vs-impl-parity recurrence at codification moment — CCC-1 v1.1 class defect caught at /critique design-time; **m1 + m2 clean fixes** (post-Phase-5 explicit grep verification for N=3 surface schema-pin returns 10 hits across 4 surfaces ≥4 expected + row 13 dual-reference clarification preserving slice-014's `_invariant` ref); **0 MISSED at slice-cycle level** — all 7 findings caught at /critique + /critique-review; zero surfaced at /build-slice or /validate-slice; **4-layer-defense slice (second in project after slice-013)** — first Critic + meta-Critic + design-stage audits (7) + shippability catalog all contributed catches; layer 2 (meta-Critic) saved /build-slice cycle here — would have hard-failed at Phase 6 strict-pre-finish without M-add-1 catch; **cross-cutting Dim 9 catch rate at slice-015 = 87.5% (7 of 8 sub-class hits caught at /critique + /critique-review combined; zero MISSED at slice-cycle level)** — range-bound 60-100% on N=10 evidence stable | **SCPD-1 shipped** (Shippability-Catalog Propagation Discipline) — `agents/critique.md` Dim 9 gains new 8th sub-clause `Shippability-catalog consumer-reference propagation` between existing 7th sub-clause `Entry-pin-vs-PMI-1-gate semantics conflation` close (L178) and existing `### Bonus: weak graph edges` H3 anchor (now L188); body names BOTH `Reactive-catch mode` (slice-013 N=1 reactive-catch at /validate-slice Step 5.5) + `Proactive-application mode` (slice-014 N=2 proactive-application at /build-slice Phase 4 pre-/validate-slice); methodology v0.30.0; 5 ACs / 13 TF-1 rows (M1 ACCEPTED-PENDING grew plan 10 → 13 via 3 end_anchor tighten Edits on slice-013 body-bound tests; M-add-1 ACCEPTED-FIXED flipped 3 rows from rejected `WRITTEN-AS-EDIT` to `PASSING` per slice-013 precedent + stale "10/10" → "13/13" pre-finish gate count fix) / 11 must-not-defer including SCPD-1 self-application + slice-013 body-bound test end_anchor tightening + explicit post-Phase-5 grep verification; ADR-014 (reversibility: cheap with magnitude justification ~13-15 sites — same class as ADR-010 + ADR-012; +1-3 site delta reflects Phase 5 shippability.md row-propagation surgery); rule-ID `SCPD-1` per slice-011 RSAD-1 + slice-013 EPGD-1 -D suffix convention N=2 → N=3 stable confirmation; **canonical body uses concrete substrings** (`_lists_six_sub_clauses`, `_lists_seven_sub_clauses`, `_at_0_28_0`, `_invariant`) for discipline-verifiability via test-function-rename pattern recognition; **SCPD-1 self-application empirically VALIDATED at /build-slice + /validate-slice** — Phase 1f superseded `_lists_seven_sub_clauses` → `_lists_eight_sub_clauses` per PMI-1 structural-invariant supersession discipline applied at structural-invariant level (N=2 stable → N=3 stable post-slice-015); Phase 5 propagated rows 6 + 11 + 13 pytest commands in-line BEFORE /validate-slice catalog run (preserving slice-014's `_invariant` ref on row 13 per /critique m2); 0 stale `_lists_seven_sub_clauses` consumer refs remain in pytest commands (3 surviving are historical-narrative lineage notes correctly preserved); 4 propagated `_lists_eight_sub_clauses` refs at rows 6/11/13/15; **slice-015 IS canonical reference instance of the discipline it authors** (recursive-self-application N=6 → N=7 cumulative post-RSAD-1); **EPGD-1 self-application N/A under PMI-1 v1.1 confirmed at slice-015** — slice-015 only ADDED to `test_methodology_changelog.py` (new SECTION header `# --- Slice-015 / SCPD-1 entry pinning ---` + 2 functions: entry-pin + ADR-pin); 0 of 7 prior entry-pin functions touched (v_0_22_0..v_0_29_0 persist untouched); no PMI-1 versioned-gate Edit needed (gate body unchanged from slice-014 introduction); **PMI-1 v1.1 empirical retirement-proof N=1 → N=2 stable** — second atomic version bump under v1.1 (0.29.0 → 0.30.0) succeeded with ZERO test code modification on gate body since slice-014 Phase 1c; **DR-1 dual-review catching pattern-blindness N=2 → N=3 stable** post-slice-013/014 codification — three consecutive cross-cutting-tooling slices show runtime-prerequisite-completeness blind-spot pattern (slice-013 sibling-test grep + slice-014 missing imports + slice-015 audit-allowlist non-membership); hits Meta-Critic 2026-05-13 /critic-calibrate next-run trigger threshold; **4-layer-defense slice (second in project after slice-013)** — first Critic + meta-Critic + design-stage audits + shippability catalog all contributed catches; **ZERO build-time DEVIATIONs at slice-015** — fourth in slice-008/012/013/015 series (N=4 stable); validate-using-your-own-ship N=12 → N=13 stable (slices 003..015); empirical-verification-at-design-time discipline N=13 → N=14 stable (slices 003..015 — slice-015 ran 7 design-time audits all VALIDATED at /build-slice + /validate-slice: location-pin uniqueness + canonical-literal absence + BC-1 self-application prediction + SCPD-1 self-application empirical pre-verification + RSAD-1 self-check + EPGD-1 N/A confirmation + RSAD-1 build-time sub-mode prediction); PMI-1 structural-invariant supersession N=2 → N=3 stable (slice-011 + slice-013 + slice-015); bidirectional sha256 forensic capture N=10 → N=11 stable (agents/critique.md byte-equal in-repo↔installed at `b9424ced411e25a5...`; methodology-changelog.md byte-equal at `eba2aeaecb650e43...`); N-surface schema-pin 3-surface shape N=3 → N=4 instances stable (RSAD-1 + EPGD-1 + PMI-1 v1.1 + SCPD-1); -D suffix rule-ID convention N=2 → N=3 stable (RSAD-1 + EPGD-1 + SCPD-1); ADR-pin convention N=1 → N=2 stable (ADR-013 + ADR-014); MCT-1 default-trigger self-application N=5 → N=6 stable (slices 010..015); mini-CAD-1 row 3 PASSING → WRITTEN-FAILING → PASSING transition pattern N=6 → N=7 stable; VAL-1 Layer B intra-repo `tests` package class N=12 → N=13 cumulative recurrence (handled cleanly via `--imports-allowlist tests`); plugin.yaml.version 0.29.0 → 0.30.0 + VERSION + ai-sdlc-VERSION atomic; shippability catalog 14→15 rows, 121/121 PASS in ~3.9s aggregate (well under 2-min target); cumulative cross-cutting misses post-slice-015 = 11 across slices 6-15 (per Meta-Critic 2026-05-13 ≤2 quantitative target retired — qualitative trajectory range-bound 60-100% on N=10 evidence stable is the real signal); **strongest slice-016 candidates**: `/critic-calibrate` (DR-1 runtime-prerequisite-completeness pattern N=3 hits trigger threshold; ~1 hour invocation; walks reflections 6-15) OR `refine-dim-9-with-runtime-prerequisite-completeness-sub-class` (proposed rule-ID `RPCD-1` per -D-suffix N=3 stable; direct Dim 9 9th sub-clause codification ~30-45 min mirroring slice-009/011/013/015 pattern; N=3 evidence ready). Generic methodology recurrence at slice-015 N=2 stable: every future Dim 9 sub-clause append MUST tighten predecessor body-bound tests' end_anchors. Cross-slice methodology-lesson propagation empirically demonstrated as canonical evolution path: reactive (slice-013) → proactive (slice-014) → codified (slice-015). |
| 014 | [slice-014-refactor-pmi-1-gate-to-version-agnostic-shape](slice-014-refactor-pmi-1-gate-to-version-agnostic-shape/) | 2026-05-13 | Standard | medium (critic-required: true via MCT-1 auto-trigger — slice modifies `methodology-changelog.md` + `tests/methodology/**`; fourth slice operating under MCT-1 default per slice-010 codification) | SHIPPED — validation PASS | 6 (0B + 2M + 3m + 1 meta-Critic M-add-1) all ACCEPTED-FIXED at /critique + /critique-review TRI-1; verdict CLEAN per triage_audit (post-reconciliation); **all 5 first-Critic findings + 1 meta-Critic M-add-1 VALIDATED at /critique-disposition AND /validate-slice — eighth consecutive 100% Critic-disposition accuracy slice** (running 73/73 cross-Critic-stack across slices 6-14, strongest streak in the project); **M2 strongest first-Critic catch (VALIDATED-PARTIAL)** — pinned monkeypatch target name-resolution semantics (bare `REPO_ROOT` resolves from running module globals, not conftest's); fix-class was right but environment-interaction dimension (pytest's namespace-package import-mode placing module under bare key `methodology.test_methodology_changelog` not dotted `tests.methodology.test_methodology_changelog`) was MISSED; surfaced as DEVIATION-1 at /build-slice Phase 1d; corrected in-line via `monkeypatch.setattr(sys.modules[__name__], "REPO_ROOT", tmp_path)` object-form; **M-add-1 strongest meta-Critic save (DR-1 N=2 stable post-slice-013 codification)** — `import pytest` + `import ast` not pre-existing in test file imports block; without explicit Phase 1a' imports preamble Edit, Phase 1d smoke gate rows 2+3+4 would NameError, misdiagnosed as EPGD-1 violation per design.md diagnostic guidance (~10-15 min cost); **M1 META-1 atomicity save** — Phase 2 atomic-commit discipline packages changelog v0.29.0 entry write + forward-sync + 3 version bumps + ADR-013 commit as SINGLE atomic build operation; without it, META-1 (`test_version_matches_most_recent_changelog_entry` at `test_methodology_changelog.py:33`) would fire transient at any intermediate partial-state; **m3 TF-1 row-count divergence save (VALIDATED-PARTIAL)** — consolidated 7-row mission-brief table caught by audit's first-integer-only AC parsing (multi-AC cells like `1 + 2 + 5` parse as AC #1 only; AC #5 had `ac-without-row` violation at /build-slice Step 6); corrected in-line by splitting row 1 into separate AC #1 + AC #5 rows (final 8 rows); **m1 + m2 clean fixes** (ADR-013 reversibility wording clarification preserving ADR 003/004/006-012 single-token frontmatter convention + design.md `## Phase plan` section enumerating 8 ordered phases); **3-layer Critic-stack accountability lineage at slice-014** — first Critic M2 + meta-Critic M-add-1 + build-time DEVIATION-1; cumulative coverage complete only post-/build-slice; NEW Dim 9 sub-class candidate N=1; **slice-013 N=1 watch-list class `shippability-catalog-consumer-reference-propagation` reaches N=2 promotion threshold MET at slice-014 (proactive catch at Phase 4)** — row 13's `_at_0_28_0` reference updated to `_invariant` BEFORE running shippability catalog; 14/14 PASS no regressions; **cross-cutting Dim 9 catch rate at slice-014 = 85.7% (6 of 7 sub-class hits caught at /critique + /critique-review combined)** — range-bound 60-100% on N=9 evidence stable | **PMI-1 v1.1 shipped (version-agnostic cleanliness gate)** — `tests/methodology/test_methodology_changelog.py` PMI-1 gate refactored from per-version `test_plugin_yaml_version_matches_version_file_at_0_28_0` shape to single version-agnostic `test_plugin_yaml_version_matches_version_file_invariant`; SOLE assertion `version_file == plugin_version` with pinned error-message naming `"PMI-1"` + `"slice-006 escape"`; methodology v0.29.0; 5 ACs / 8 TF-1 rows (split row 1 into AC #1 + AC #5 per build-time TF-1 audit constraint discovery) / 12 must-not-defer including META-1 atomicity per /critique M1 ACCEPTED-FIXED; ADR-013 (reversibility: cheap with magnitude-of-revert justification — recurring 1-min × N future slices cost reintroduced if reverted); 2 AST meta-tests defend version-agnostic shape against future regression (`_is_version_agnostic_shape` checks no `r"^\d+\.\d+\.\d+$"` `Constant(value=str)` inside gate body; `_no_per_version_pmi_1_gate_functions_remain` checks no `FunctionDef.name` matching `r"^test_plugin_yaml_version_matches_version_file_at_0_\d+_0$"`); 1 regression test exercises FAILURE path with tmpdir + monkeypatch object-form + pinned per-substring asserts; canonical phrase `version-agnostic PMI-1 cleanliness gate` pinned across N=3 surfaces (ADR-013 + in-repo + installed methodology-changelog v0.29.0); supersession-pattern-retired marker phrase `supersession pattern retired at slice-014` pinned bidirectionally; **PMI-1 versioned-gate supersession-event counter TERMINATED at N=6** — slice-007 introduced `_at_0_22_0` → slices 008-013 superseded N=6 times → slice-014 retires the pattern permanently; future slices' version bumps will NOT supersede the gate function; empirical retirement-proof at AC #5 (atomic bump 0.28.0 → 0.29.0 + post-bump `_invariant` PASS with ZERO code change since Phase 1c); **DR-1 dual-review catching pattern-blindness N=1 → N=2 stable post-slice-013 codification** (slice-013 caught body-bound widening defect class; slice-014 caught imports-prerequisite class); **EPGD-1 self-application empirically VALIDATED at the discipline-retirement level (slice-014 (a) ↔ (b) duality)** — Edit follows EPGD-1 narrow-scope (0 of 7 prior entry-pin functions touched) AND Edit retires the very pattern EPGD-1 was codified to govern; **recursive-self-application N=5 → N=6 cumulative post-RSAD-1 codification** (slices 009..014); **NEW Dim 9 sub-class candidate at N=1**: `pytest-namespace-package-import-mode-defeats-dotted-string-monkeypatch-target` — same root-cause family as VAL-1 Layer B `tests` namespace-package class (N=12 cumulative recurrence); **3-layer Critic-stack accountability lineage at N=1 standalone** — design-semantic (first Critic) / runtime-prerequisite (meta-Critic) / environment-interaction (build-time empirical); **TF-1 audit's first-integer-only parsing of multi-AC cells at N=1 standalone** — generic methodology lesson for future TF-1 plans; **slice-013 N=1 watch-list class N=2 promotion threshold MET (proactive catch at slice-014 Phase 4)** — `shippability-catalog-consumer-reference-propagation-after-PMI-1-structural-invariant-supersession`; strongest slice-015 candidate; cross-slice methodology-lesson propagation pattern has first empirical evidence at slice-014; bidirectional sha256 forensic capture N=9 → N=10 stable (methodology-changelog.md byte-equal in-repo↔installed at `7aa14cf5604d3f11...`); N-surface schema-pin 3-surface shape N=2 → N=3 instances stable (RSAD-1 v0.26.0 + EPGD-1 v0.28.0 + PMI-1-v1.1 v0.29.0); validate-using-your-own-ship N=11 → N=12 stable; empirical-verification-at-design-time discipline N=12 → N=13 stable (3 design-time audits all VALIDATED at /build-slice); MCT-1 default-trigger self-application N=4 → N=5 stable; VAL-1 Layer B intra-repo `tests` package class N=11 → N=12 cumulative recurrence (handled cleanly via `--imports-allowlist tests`); plugin.yaml.version 0.28.0 → 0.29.0 + VERSION + ai-sdlc-VERSION atomic via META-1 atomicity Phase 2 single atomic commit; methodology-changelog.md byte-equal in-repo↔installed at slice end; shippability catalog 13→14 rows, 114/114 PASS in ~8.5s aggregate; cumulative cross-cutting misses post-slice-014 = 11 across slices 6-14 (per Meta-Critic 2026-05-13 ≤2 quantitative target retired — qualitative trajectory range-bound 60-100% on N=9 evidence stable is the real signal); **strongest slice-015 candidate**: `refine-dim-9-with-shippability-catalog-propagation-sub-class` — N=2 promotion threshold MET (~30 min skill scope) |
| 013 | [slice-013-refine-dim-9-with-entry-pin-vs-pmi-1-gate-sub-class](slice-013-refine-dim-9-with-entry-pin-vs-pmi-1-gate-sub-class/) | 2026-05-13 | Standard | medium (critic-required: true via MCT-1 auto-trigger — slice modifies `agents/critique.md` matching `agents/*.md` + `methodology-changelog.md` + `tests/methodology/**`; third slice operating under MCT-1 default per slice-010 codification) | SHIPPED — validation PASS | 8 (1B + 4M + 3m) ratified at TRI-1 — 6× ACCEPTED-FIXED at /critique (B1+M2+M3+m1+m2+m3) + 2× ACCEPTED-PENDING applied at /build-slice (M1 + M-add-1); verdict NEEDS-FIXES per triage_audit; **all 7 first-Critic findings VALIDATED at /critique-disposition AND /validate-slice — seventh consecutive 100% Critic accuracy slice** (slice-006: 11/11; slice-007: 8/8; slice-008: 6/6; slice-009: 11/11; slice-010: 11/11; slice-011: 7/7; slice-012: 5/5; slice-013: 7/7 — running **66/66** across slices 6-13, strongest streak in the project); **PLUS meta-Critic M-add-1 VALIDATED (DR-1 dual-review catch)** — first instance of dual-review-catching-pattern-blindness in the project — cross-Critic-stack total **67/67 across slices 6-13**; **B1 strongest catch — rule-ID drift EPG-1 vs EPGD-1** (17 EPG-1 occurrences in mission-brief vs design.md / ADR-012 / milestone.md EPGD-1 locked at /design-slice; recursive-self-application N=5 cumulative — slice authoring EPGD-1 had own draft committing exact specification-defect class RSAD-1 was codified to catch); **M-add-1 strongest meta-Critic save — sibling test precision-degradation** (first Critic identified body-bound widening defect on slice-011's `_names_both_sub_modes` but didn't generalize via grep across siblings sharing identical anchors; meta-Critic surfaced `_cites_at_least_two_cross_slice_anchors` had same defect — symmetric extension applied at /build-slice Phase 1f); **M1 + M-add-1 jointly extend slice-011 end_anchor tighten symmetrically** (both tests' `end_anchor = "### Bonus: weak graph edges"` → `"Entry-pin-vs-PMI-1-gate semantics conflation"` per /build-slice; 2 new symmetric tests on 7th sub-clause body added); **M2 anchor list formalization** (strict-both `["slice-011", "slice-012"]` + ≥2-of-4 `["Phase 1b INSERT", "Phase 1c narrow-scope Edit", "Audit 6 structural-separation", "SECTION header"]` per Newman contract-gap); **M3 N-surface count drift** (N=2 → N=3 per slice-011 RSAD-1 3-surface precedent N=2 instances stable); **1 MISSED at slice-cycle level** — shippability-catalog-consumer-reference-propagation slip (rows 6 + 11 commands referenced `_lists_six_sub_clauses` deleted at Phase 1f supersession; caught at /validate-slice Step 5.5; fixed in-line; **NEW Dim 9 sub-class candidate at N=1** — sibling to EPGD-1; promote at N=2 if recurs); **cross-cutting Dim 9 catch rate at slice-013 = 87.5% (7 of 8)** — range-bound 60-100% on N=8 evidence stable | **EPGD-1 shipped** (Entry-Pin-Gate-Discipline) — `agents/critique.md` Dim 9 gains new 7th sub-clause `Entry-pin-vs-PMI-1-gate semantics conflation` between existing 6th sub-clause `Recursive self-application discipline` close (L172) and existing `### Bonus: weak graph edges` H3 anchor (now L180); body names BOTH `Build-time slip mode` (slice-011 N=1 build-time slip) + `Design-time-pre-empted success mode` (slice-012 N=2 design-time-pre-empted success); methodology v0.28.0; 5 ACs / 12 TF-1 rows (M1+M-add-1 grew plan 8→12 at build) / 12 must-not-defer; ADR-012 (reversibility: cheap with magnitude justification ~12 sites — same class as ADR-010); rule-ID `EPGD-1` per slice-011 RSAD-1 -D suffix convention N=2 stable confirmation; **canonical body documents BOTH sub-modes** with concrete cross-slice anchors (slice-011 + slice-012); **EPGD-1 self-application empirically VALIDATED at /build-slice + /validate-slice** — Phase 1c PMI-1 supersession Edit (`_at_0_27_0` → `_at_0_28_0`) narrow-scoped to gate function body + dedicated SECTION header ONLY; 7 entry-pin functions present (v_0_22_0..v_0_28_0); exactly 1 PMI-1 versioned-gate (`_at_0_28_0`); **0 of 6 prior entry-pin functions touched** — slice-013 IS canonical reference instance of the discipline it authors (recursive-self-application N=5 cumulative post-RSAD-1); **first dual-review (DR-1) catches pattern-blindness** — first Critic M-add-1 missed; meta-Critic at /critique-review caught; reality confirmed both Critics' analyses; **4-layer-defense slice (first in project)** — first Critic catches 7 + meta-Critic catches 1 missed + 6 design-stage audits all VALIDATED + /validate-slice shippability catalog catches shippability-catalog-propagation slip as final layer; **NEW Dim 9 sub-class candidate at N=1** — shippability-catalog-consumer-reference-propagation-after-PMI-1-structural-invariant-supersession (caught at /validate-slice Step 5.5; row 6 + row 11 commands had stale `_lists_six_sub_clauses` references); **`/critic-calibrate` MANDATORY at slice-014** — cumulative cross-cutting misses 11 across slices 6-13 (5.5× the ≤2 target across slices 6-15; 2 slices remaining in window — effectively impossible without recalibration); **ZERO build-time DEVIATIONs at slice-013** — third in slice-005..013 series (slice-008 + slice-012 + slice-013 = N=3 stable); validate-using-your-own-ship N=11 stable; empirical-verification-at-design-time discipline N=12 stable; PMI-1 versioned-gate supersession N=6 events stable (slice-007 introduced → slice-008..013 superseded); bidirectional sha256 forensic capture N=9 stable (methodology-changelog.md baseline `33423327EB101F26` byte-equal → post-edit `03DF671C2CCD4251` byte-equal); N-surface schema-pin 3-surface shape N=2 instances stable (RSAD-1 + EPGD-1); PMI-1 structural-invariant supersession N=2 stable (slice-011 N=1 + slice-013 N=2); mini-CAD-1 row 3 PASSING→WRITTEN-FAILING→PASSING transition N=6 stable; VAL-1 Layer B intra-repo `tests` package false-positive class N=11 cumulative recurrence (handled cleanly via `--imports-allowlist tests`); plugin.yaml.version 0.27.0 → 0.28.0 + VERSION + ai-sdlc-VERSION atomic; methodology-changelog.md byte-equal in-repo↔installed at slice end; shippability catalog 12→13 rows, 84/84 PASS in ~3.85s aggregate (well under 2-min target; row 6 + row 11 commands updated in-line at validate-time for stale `_lists_six` references); cumulative cross-cutting misses post-slice-013 = 11 across slices 6-13. **Errata (post-/reflect correction)**: /critic-calibrate was already run 2026-05-13 (pre-slice-013) with ZERO PROPOSALS per `architecture/critic-calibration-log.md`; the quantitative ≤2-cumulative-misses target was explicitly REFUTED by Meta-Critic as over-ambitious; qualitative 60-100% catch rate range-bound is the real signal. **Slice-014 is NOT a mandatory calibration boundary** — the earlier "/critic-calibrate at slice-014 mandatory" claim throughout slice-013 reflection.md + lessons-learned.md + this row was stale at write time. Next /critic-calibrate trigger: after slice-013-020 archives, OR earlier if a watch-list N=1 hits N=3. Slice-013's NEW N=1 candidates (shippability-catalog-propagation + body-bound-widening-grep-for-siblings) join the watch-list. See slice-013 reflection.md `## Errata` for full correction. |
| 012 | [slice-012-bc-proj-2-negative-anchor-migration](slice-012-bc-proj-2-negative-anchor-migration/) | 2026-05-13 | Standard | medium (critic-required: true via MCT-1 auto-trigger — slice modifies `methodology-changelog.md` + `tests/methodology/**`; second slice operating under MCT-1 default per slice-010 codification) | SHIPPED — validation PASS | 5 (1B + 3M + 1m) all ACCEPTED-FIXED in-round at /critique; verdict CLEAN per triage_audit; **all 5 VALIDATED at /critique-disposition AND /validate-slice — sixth consecutive 100% Critic accuracy slice** (slice-006: 11/11; slice-007: 8/8; slice-008: 6/6; slice-009: 11/11; slice-010: 11/11; slice-011: 7/7; slice-012: 5/5 — running **59/59** across slices 6-12, strongest streak in the project extending slice-011's 54/54 by +5); **B1 strongest catch — recursive-self-application** (slice-012's AC #4 verification approach structurally under-scoped against slice-008's pre-existing BC-PROJ-1 L20 line data — naive substring check would PASS pre-migration because all 9 tokens already exist file-globally on BC-PROJ-1's slice-008 migration line, violating TF-1 PENDING → WRITTEN-FAILING genuine-failure discipline; pinned failure signal `AssertionError: BC-PROJ-2 negative_anchors mismatch: got (), expected (...)` exact match at /build-slice Phase 1a; **B1 is itself a recursive-self-application instance on slice-012's own draft** — the slice authoring data-only migration couldn't enforce its own data because slice-008's same tokens already populated BC-PROJ-1's L20 line; **recursive-self-application N=4 cumulative across slices 9-12** post-RSAD-1 codification at slice-011; RSAD-1 codification empirically load-bearing); **M1 strongest preventive save — entry-pin-vs-PMI-1-gate-conflation discipline** (Phase 1b INSERT under NEW `# --- Slice-012 / BC-PROJ-2 entry pinning ---` SECTION header + Phase 1c narrow-scope Edit on gate body + dedicated SECTION header only + Audit 6 structural-separation empirical verification; **N=2 promotion probe SUCCESS at slice-012** — v0.22.0..v0.26.0 entry-pin functions ALL preserved through PMI-1 gate supersession; slice-011 NEW Dim 9 sub-class candidate N=1 → N=2 promotion threshold MET; promote at slice-013+ via /critic-calibrate or dedicated Dim 9 sub-class refinement slice); **M2 Wiegers AC-trace save** (Audit 3 numerical inflation 47/7 claimed → actual 8/5 distinct-token matches; Critic M2 catches numerically-unreproducible audit claims preventing Wiegers AC-trace sub-class violation N=1 stable post-slice-008); **M3 vault-ref drift save** (mission-brief.md cited `ADR-007-bc-1-v1-2-negative-context-anchors` but actual filename is `ADR-007-bc-1-negative-context-anchors-via-final-filter.md`; ADR-011 filename shortened 75→45 chars per repo convention); **m1 N-surface schema-pin discipline ratchet** (canonical phrase `BC-PROJ-2 negative-anchor migration` pinned across N=2 surfaces in-repo + installed; N=3 → N=4 N-surface schema-pin discipline stable); **0 MISSED at slice-cycle level** — zero build-time DEVIATIONs at slice-012; second slice in series 005-012 with ZERO-build-deviation property (slice-008 N=1 → slice-012 N=2 cumulative; ratchets the "ZERO-build-deviation slice via strong /critique" pattern past slice-008's N=2 watch threshold; promote to tracked design-stage Critic ROI ratio metric at slice-013+); **`/critic-calibrate` strongest slice-013+ candidate** — cumulative cross-cutting misses 10 across slices 6-12 (5× the ≤2 target across slices 6-15; 3 slices remaining in window) | **BC-PROJ-2 negative-anchor migration shipped** (BC-1 v1.3 — data-only migration completing BC-1 v1.2 rollout from slice-008); `architecture/build-checks.md` BC-PROJ-2 rule body gains `Negative anchors:` line with same 9-token methodology-vocabulary set used for BC-PROJ-1 + BC-GLOBAL-1 at slice-008 (`defer-with-rationale, aggregated lessons, false positive, meta-discussion, vocabulary, Critic-MISSED, back-sync, Dim 9, forward-sync`); zero code change to `tools/build_checks_audit.py` — slice-008's BC-1 v1.2 infrastructure (parser + `_negative_anchor_match` final-filter + `negative-anchor-overlaps-positive` violation kind) reused verbatim; methodology v0.27.0; ADR-011 (reversibility: cheap ~10 min revert; extends ADR-007 not supersedes — supersedes: null); 5 ACs / 6 TF-1 rows (4 PENDING→WRITTEN-FAILING→PASSING + 1 mini-CAD-1 PASSING→WRITTEN-FAILING→PASSING per slice-007/009/010/011 row 3 N=4 → N=5 stable + 1 entry-pin pre-fix `## v0.27.0 missing` + 1 PMI-1 gate pre-fix `VERSION 0.26.0 expected 0.27.0`) / 11 must-not-defer; **N=2 BC-PROJ-2 false-positive class empirically retired** (slice-005 + slice-011 backtests both `applicable=[]`, `skipped=['BC-PROJ-1', 'BC-PROJ-2', 'BC-GLOBAL-1']` post-migration); slice-001 backward-compat covenant per ADR-007 / ADR-011 empirically preserved (Audit 2: 0 of 9 negative-anchor tokens in slice-001's text at word-boundary; AC #3 `applicable=['BC-PROJ-1', 'BC-PROJ-2', 'BC-GLOBAL-1']`); cross-cutting-conformance Dim 9 catch rate at slice-012 = 80-100% (4 of 4 Dim 9 sub-class hits caught excluding M2; 4 of 5 including M2) — **range-bound 60-100% on N=7 evidence stable**; trajectory 0% → 25% → 60% → 100% → 60% → 87.5% → 80% → 80-100% (slice-012); validate-using-your-own-ship N=10 stable (slice-012's own ship: BC-PROJ-2 in skipped per 9-of-9 over-determination Audit 5); empirical-verification-at-design-time discipline N=11 stable (6 design-time audits all VALIDATED — disjointness + slice-001 backward-compat + slice-005/011 suppression + algorithm-path-conformance + RSAD-1 build-time recursive-self-application + entry-pin-vs-PMI-1-gate structural-separation); PMI-1 versioned-gate supersession N=5 events stable (slice-007 introduced `_at_0_22_0` → slice-008/009/010/011/012 superseded with `_at_0_27_0`); bidirectional sha256 forensic capture N=8 stable (methodology-changelog.md baseline `5C261C963A5BA2DB` byte-equal in-repo↔installed → post-edit `33423327EB101F26` byte-equal); N-surface schema-pin discipline N=4 stable (canonical phrase `BC-PROJ-2 negative-anchor migration` pinned across 2 surfaces); mini-CAD-1 row 3 PASSING→WRITTEN-FAILING→PASSING transition N=5 stable; TF-1 PENDING→WRITTEN-FAILING transition needs genuine failure N=9 stable; **VAL-1 Layer B intra-repo `tests` package false-positive class N=10 cumulative recurrence** (every slice 003-012 hits it; handled cleanly via `--imports-allowlist tests` per slice-003 SKILL.md prose pin; v2 candidate `[tool.pytest.ini_options]` testpaths auto-allow still deferred — cumulative friction ~1 sec per slice; promote at /critic-calibrate prompt review if friction crosses meaningful threshold); plugin.yaml.version 0.26.0 → 0.27.0 + VERSION + ai-sdlc-VERSION atomic; methodology-changelog.md byte-equal in-repo↔installed at slice end (sha256 `33423327EB101F26`); shippability catalog grew 11→12 rows, 87/87 PASS in 3.36s (catalog FASTER per row at slice-012 by ~3× vs slice-011's 8s; well under 2-min target); cumulative cross-cutting misses post-slice-012 = 10 across slices 6-12 (target ≤2 significantly exceeded — 5×; 3 slices remaining in window; **`/critic-calibrate` at slice-013-015 mandatory** to recalibrate target downward OR refine Critic prompt with entry-pin-vs-PMI-1-gate-conflation Dim 9 sub-class promotion + design-stage Critic ROI ratio tracking metric) |
| 011 | [slice-011-promote-recursive-self-application-discipline-to-critique-skill-prose](slice-011-promote-recursive-self-application-discipline-to-critique-skill-prose/) | 2026-05-13 | Standard | medium (critic-required: true via MCT-1 auto-trigger — first slice operating under MCT-1 default per slice-010 codification; slice modifies `agents/critique.md` matching `agents/*.md` + `methodology-changelog.md` matching MCT-1 trigger glob) | SHIPPED-WITH-DEFERRALS — validation PASS | 7 (1B + 3M + 3m) all ACCEPTED-FIXED in-round at /critique; verdict CLEAN per triage_audit; **all 7 VALIDATED at /critique-disposition — sixth consecutive 100% Critic accuracy slice** (slice-006: 11/11; slice-007: 8/8; slice-008: 6/6; slice-009: 11/11; slice-010: 11/11; slice-011: 7/7 — running **54/54** across slices 6-11, strongest streak in the project); **4 of 7 findings (B1+M1+M2+M3) are themselves recursive-self-application instances on slice-011's own draft** — exactly per the spawning skill's meta-context expectation (the slice authoring the discipline IS the canonical reference instance of the rule it encodes); **B1 fatal save** (lowercase `design-time`/`build-time` at bullet-start inside bold markers — EXACTLY the slice-009 DEVIATION-1 case-sensitivity trap class that design.md explicitly claimed to pre-empt; restructured to capitalized bullet titles + lowercase canonical substrings mid-sentence); **M1 rationale-falsifying save** (BC-1 reads slice mission-brief + design.md NOT `agents/critique.md` content per `tools/build_checks_audit.py::_read_slice_text` L435-L442; the "abstract-framing-keeps-canonical-body-BC-1-clean" rationale was based on a misreading; corrected to stylistic / reusable-artifact-readability framing); **M2 doc-vs-implementation-parity save** (BC-GLOBAL-1's `Applies to:` is `**` glob per slice-005 changeover, NOT `always: true` short-circuit; path attribution corrected); **M3 + m1 jointly resolved** (test allowlist over-promise resolved by naming `BC-PROJ-2` explicitly in canonical body — matches existing Dim 9 convention of naming `BC-1`, `RR-1`, `TF-1`, etc. by ID); **1 MISSED at slice-cycle level** (entry-pin-vs-pmi-1-gate-semantics-conflation at /build-slice Phase 1b — accidental deletion of `test_v_0_25_0_mct_1_entry_present_in_repo_and_installed` when superseding PMI-1 versioned-gate test `_at_0_25_0` → `_at_0_26_0`; my Edit `old_string` spanned the WHOLE slice-010 section block including the persistent entry pin; caught at /validate-slice Step 5.5 by shippability catalog row 10 fail; fixed in-line at validate-time, zero impact on shipped slice content; **NEW Dim 9 sub-class candidate at N=1** — entry-pin-vs-pmi-1-gate-semantics-conflation; promote at N=2 if recurs at slice-012+); **BC-PROJ-2 negative-anchor migration N=2 evidence threshold MET** (slice-005 + slice-011); slice-012+ candidate `bc-proj-2-negative-anchor-migration` | **RSAD-1 shipped** (Recursive Self-Application Discipline) — `agents/critique.md` Dim 9 gains new 6th sub-clause `Recursive self-application discipline` between existing `Language-version conformance` sub-clause close and existing `### Bonus: weak graph edges` H3 anchor; sub-clause body names BOTH design-time AND build-time-via-/critique-fix-prose sub-modes with N=3 cumulative evidence (slice-009 M2 + slice-010 design-time stress-test B1+M1+B5 + slice-010 build-time DEVIATION-3 BC-PROJ-2 fire); methodology v0.26.0; 5 ACs / 8 TF-1 rows (7 PENDING→WRITTEN-FAILING→PASSING + 1 mini-CAD-1 PASSING→WRITTEN-FAILING→PASSING per slice-007/009/010 row 3 precedent N=3 → N=4 stable) / 14 must-not-defer; ADR-010 (reversibility: cheap with magnitude justification ~12 sites; same class as ADR-007 BC-1 v1.2 / ADR-009 MCT-1); **B5 calibration-trail discipline N=2 evidence stable** — slice-010 introduced -T- suffix (MCT-1 /slice-time prose-heuristic); slice-011 extends with -D suffix (RSAD-1 /critique-time adversarial-prompt discipline heuristic); both prose-heuristic distinct from audit-enforced-gate siblings (BC-1/PMI-1/CAD-1/TF-1/RR-1/INST-1/VAL-1/WIRE-1/NFR-1/CSP-1); **MCT-1 self-applies cleanly at slice-011** (first slice operating under MCT-1 default — `critic-required: true` auto-set at /slice time per trigger glob match; codification works as intended); **slice-010 DEVIATION-3 build-time recursive-self-application sub-mode did NOT recur at slice-011** — design-time empirical Audit 3 correctly anticipated build-time BC-PROJ-2 fire on transient artifacts; RSAD-1 codification empirically load-bearing for design-time recursive-self-application catches (N=1 standalone; awaiting N=2 cross-slice evidence at slice-012+); cross-cutting-conformance Dim 9 catch rate at slice-011 = 80% (4 of 5) — falls within range-bound 60-100% framing from slice-008..010 (N=6 evidence stable); validate-using-your-own-ship N=9 stable; empirical-verification-at-design-time discipline N=10 stable; PMI-1 versioned-gate supersession N=4 events stable post-slice-011 (slice-007 introduced → slice-008/009/010/011 superseded); bidirectional sha256 forensic capture N=7 stable; N-substring + N-surface schema-pin discipline applied (substantive canonical phrase `Recursive self-application discipline` pinned across N=3 surfaces); plugin.yaml.version 0.25.0→0.26.0 + VERSION + ai-sdlc-VERSION atomic; methodology-changelog.md byte-equal in-repo↔installed at slice end; shippability catalog 10→11 rows, 75/75 PASS in ~8s; cumulative cross-cutting misses post-slice-011 = 10 across slices 6-11 (target ≤2 significantly exceeded — 5x; recommendation `/critic-calibrate` at slice-012-015 to recalibrate target downward OR refine Critic prompt with entry-pin-vs-pmi-1-gate-conflation sub-class at N=2 evidence) |
| 010 | [slice-010-promote-voluntary-critic-on-cross-cutting-to-slice-default-heuristic](slice-010-promote-voluntary-critic-on-cross-cutting-to-slice-default-heuristic/) | 2026-05-12 | Standard | medium (critic-required: true — cross-cutting tooling slice authoring MCT-1 itself; recursive-self-application N=2 candidate at design-time → N=3 at build-time) | SHIPPED-WITH-DEFERRALS — validation PASS | 11 (5B + 3M + 3m) — 9 ACCEPTED-FIXED applied in-round at /critique + 1 ACCEPTED-PENDING M3 (applied at /build-slice Phase 1a as 5th prose-pin test for sub-class anchors) + 1 DEFERRED m2; verdict NEEDS-FIXES per triage_audit; **all 11 VALIDATED at /critique-disposition — fifth consecutive 100% Critic accuracy slice** (slice-006: 11/11; slice-007: 8/8; slice-008: 6/6; slice-009: 11/11; slice-010: 11/11 — running **47/47** across slices 6-10, strongest streak in the project); **B1+B3 honest-aggregate framing saves** (dropped internally-inconsistent "8 of 9 design-stage catches" claim — project's running counter was broken across slice-007 off-by-one + slice-008→slice-009 rule-change; replaced with verifiable cumulative-post-build framing); **B5 calibration-trail save** (renamed working-draft MCR-1 → MCT-1 with -T- suffix signaling /slice-time prose-heuristic vs audit-enforced gate); **M1 stylistic-asymmetry save** (split single multi-clause bullet into terse bullet + adjacent markdown blockquote evidence prose); voluntary Critic on cross-cutting tooling **N=10/10 paid off — NOW CODIFIED as MCT-1 default** (slice-011+ /slice invocations matching trigger glob auto-set `critic-required: true`); **1 MISSED at slice-cycle level** (DEVIATION-3 BC-PROJ-2 recursive-self-application at BUILD time via /critique B4 fix prose — empirical prose describing BC-PROJ-2's anchors `fence`/`code-block`/`llm` RE-INTRODUCED those substrings into mission-brief + design.md, triggering BC-PROJ-2's anchor path at Phase 4 BC-1 self-application audit; dispositioned defer-with-rationale per BC-1 Important semantics); **slice-009 M2 was design-time-only; slice-010 brings recursive-self-application phenomenon to BUILD time**; **recursive-self-application N=3 cumulative across phases** — MEETS slice-009 reflection's N=3 promotion threshold; slice-011 strongest candidate `promote-recursive-self-application-discipline-to-critique-skill-prose`; **BC-PROJ-2 negative-anchor migration N=2 evidence threshold MET** (slice-005 + slice-010); slice-011+ candidate | **MCT-1 shipped** (Mandatory Critic Trigger for In-house methodology surfaces) — `skills/slice/SKILL.md` Step 4a gains terse bullet `In-house methodology surfaces (skills/*/SKILL.md, agents/*.md, tools/**/*.py, methodology-changelog.md)` between `Security-sensitive paths` and `Heavy mode (always)` + adjacent markdown blockquote evidence prose appended below existing "When producing the mission brief..." paragraph; methodology v0.25.0; 5 ACs / 8 TF-1 rows (7 PENDING→WRITTEN-FAILING→PASSING + 1 mini-CAD-1 PASSING→WRITTEN-FAILING→PASSING per slice-009 row 3 + Critic B2 honest framing) / 13 must-not-defer; ADR-009 (reversibility: cheap with magnitude justification ~11-13 sites per Critic m3 recount; same class as ADR-007 BC-1 v1.2); **slice-009 DEVIATION-1 + DEVIATION-2 pre-emption at slice-010 design time WORKED** — zero recurrence; cross-cutting-conformance Dim 9 catch rate at slice-010 = 87.5% (7 of 8) — falls within range-bound 60-100% framing from slice-008+slice-009 (N=5 evidence stable); MCT-1 self-application clean (validate-using-your-own-ship N=8 stable, slice-003..010); PMI-1 versioned-gate supersession N=3 events stable post-slice-010 (slice-007 introduced → slice-008/009/010 superseded); bidirectional sha256 forensic capture N=6 stable; empirical-verification-at-design-time discipline N=9 stable; N-substring + N-surface schema-pin discipline applied (substantive canonical phrase `In-house methodology surfaces` pinned across N=3 surfaces); plugin.yaml.version 0.24.0→0.25.0 + VERSION + ai-sdlc-VERSION atomic; methodology-changelog.md byte-equal in-repo↔installed at slice end; shippability catalog 9→10 rows, 80/80 PASS in 2.67s; cumulative cross-cutting misses post-slice-010 = 9 across slices 6-10 (target ≤2 significantly exceeded — 4.5x; recommendation `/critic-calibrate` at slice-012-015 to recalibrate target downward OR refine Critic prompt with recursive-self-application discipline at N=3 evidence) |
| 009 | [slice-009-refine-dim-9-with-design-md-tables-sub-clause](slice-009-refine-dim-9-with-design-md-tables-sub-clause/) | 2026-05-12 | Standard | medium (critic-required: true — cross-cutting tooling) | SHIPPED — validation PASS | 11 (2B + 5M + 4m) — 10 ACCEPTED-FIXED inline at /critique time + 1 ACCEPTED-PENDING M1 location-pin (resolved at /build-slice T2 + caught DEVIATION-2 at first mid-slice smoke run); verdict NEEDS-FIXES per triage_audit; **all 11 VALIDATED at /validate-slice — fourth consecutive 100% Critic accuracy slice** (slice-006: 11/11; slice-007: 8/8; slice-008: 6/6; slice-009: 11/11 — running **36/36** across slices 6-9, strongest streak in the project); **B1 + B2 both fatal saves** (B1: existing-test count 11 → actual 13 via grep — would have caused TF-1 audit "13 tests collected" assertion to fail with actual 15 collected; B2: mission-brief AC #3 named non-existent test `test_in_repo_critique_md_byte_equal_to_installed` — actual is `test_in_repo_and_installed_critique_agent_are_content_equal` at line 62; would have caused /build-slice TF-1 audit refusal with `test-function-missing`); voluntary Critic on cross-cutting tooling **N=9/9 paid off with 8 of 9 design-stage catches** — methodology pattern hard-load-bearing; **2 MISSED at slice-cycle level** (DEVIATION-1 case-sensitivity sub-class N=1 + DEVIATION-2 algorithm-path-conformance generalized-to-substring-search N=1 — both surfaced at mid-slice smoke, NOT validate or production); **recursive self-application phenomenon at M2** — Critic caught slice-009's own design.md committing the EXACT class of design-doc-vs-canonical-inventory drift it was encoding (strongest single-instance methodology-internalization finding in the project) | **CCC-1 v1.1 shipped** — `agents/critique.md` Dim 9 sub-clause 2 "Tooling-doc-vs-implementation parity" body refined with design-doc-level surface (design.md mechanical tables vs methodology canonical inventories — names FIVE canonical inventory surfaces per Critic M2: positive-inclusion `_CANONICAL_*` / negative-exclusion INSTALL.md Step 3f / methodology-changelog versioned-entry / plugin.yaml / install-time-rename `VERSION`↔`ai-sdlc-VERSION`); 2 concrete cross-slice examples at N=2 (slice-006 DEVIATION-1+2 + slice-007 B1); preserves 5-sub-clause structural invariant + Dim 9 → Dim 1 cross-reference + Kiczales/honest-out citation; ADR-008 (reversibility: cheap with magnitude justification per Critic M4); methodology v0.24.0; 5 ACs / 6 TF-1 rows / 10 must-not-defer; **TWO build-time DEVIATIONs both M1-class predicted** (DEVIATION-1 lowercase-d sentence-start case-sensitivity; DEVIATION-2 `.find()`-collision in M1 location-pin test = algorithm-path-conformance generalized to substring-search test impl); **M1 location-pin test caught DEVIATION-2 at first mid-slice smoke run** — Critic finding paid off at build time, not just preventively (N=1 generic methodology lesson); **cross-cutting Dim 9 catch rate at slice-009 = 60% — REGRESSION from slice-008's 100%** (monotonic strict-improvement claim from slice-008 broken at N=5; trajectory revised to range-bound 60-100%); PMI-1 versioned-gate supersession N=2 supersession events stable post-slice-009 (per Critic M5 framing correction — slice-008's "N=2" was off-by-one; N=2 achieved post-slice-009); bidirectional sha256 forensic capture N=5 stable; "validate using your own ship" N=7 stable; N-surface schema-pin discipline applied at slice-009 (substantive canonical phrase `design.md mechanical tables` pinned across 3 surfaces — critique.md AC #1 + 2 changelog copies); shippability catalog 8→9 rows, 77/77 PASS in ~4.2s; cumulative cross-cutting misses post-slice-009 = 8 across slices 6-9 (target ≤2 already significantly exceeded; calibration signal for `/critic-calibrate` at slice-012-015) |
| 008 | [slice-008-refine-bc-1-anchors-with-negative-context](slice-008-refine-bc-1-anchors-with-negative-context/) | 2026-05-11 | Standard | low (critic-required: true — cross-cutting tooling) | SHIPPED-WITH-DEFERRALS — validation PASS | 6 (0B + 3M + 3m) all ACCEPTED-FIXED inline at /critique time — verdict CLEAN per triage_audit; **all 6 VALIDATED at /validate-slice — third consecutive 100% Critic accuracy slice** (slice-006: 11/11; slice-007: 8/8; slice-008: 6/6 — running 25/25 across slices 6-8); **all 3 majors design-stage saves** (M1: BC-PROJ-2 migration not gated by AC — would have shipped design element without driving AC per Wiegers AC-trace; M2: TWO-surface schema-pin under-applied vs slice-005 precedent — would have left `final filter` semantic phrase unprotected against doc-refactor drift; M3: always-true + negative-anchor interaction untested — would have shipped untested algorithm branch); voluntary Critic on cross-cutting tooling **N=8/8 paid off with 8 of 8 design-stage catches** — methodology pattern hard-load-bearing; **0 MISSED at /critique time** (cleanest Critic performance in project) | **BC-1 v1.2 negative-context anchors shipped** (per-rule `Negative anchors:` field as final filter on positive applicability decisions; uniform composition across `always: true` / glob / keyword paths — closes the path-uniformity gap slice-005's v1.1 left open); methodology v0.23.0; BC-PROJ-1 + BC-GLOBAL-1 migrated with 9-token methodology-vocabulary set; BC-PROJ-2 deferred to N=2 per Critic M1 (N=1 below promotion threshold); new `negative-anchor-overlaps-positive` parse-violation kind; **ZERO build-time DEVIATIONs** (first in slice-005..008 series — Critic M1+M2+M3 pre-empted them at /critique); **cross-cutting-conformance Dim 9 catch rate at slice-008 = 100% (3 of 3 sub-class hits caught at /critique time)** — strict-improving monotonically vs slice-007 60% vs slice-006 25% vs slices 1-5 0%; **algorithm-path-conformance discipline N=2 stable** (slice-005 BC-GLOBAL-1 always-true short-circuit + slice-008 negative-anchor cross-path uniformity); **PMI-1 versioned-gate test supersession pattern N=2 stable** (slice-007 + slice-008 — each version-bump slice replaces prior `_0_NN_0` gate); **bidirectional sha256 forensic capture N=3 stable** — meets BC-1 / Dim 9 elevation threshold; **TWO-surface × TWO-substring schema-pin discipline generalizes to N-substring** (slice-005 was N-surface; slice-008 generalizes — `Negative anchors` + `final filter` substrings in 2 files); cross-project-applicability sentence added to global file per Critic m1; **NEW Wiegers AC-trace Dim 9 sub-class N=1** (slice-008 Critic M1); cross-cutting-conformance miss class hardens to **N=8 distinct slices, 17 sub-class hits**; slice-006-15 effectiveness target on track with margin (5 missed across slices 6-7 + 0 at slice-008 = 5/2 budget consumed across slices 6-8 with 7 slices remaining); shippability catalog 7→8 rows, 69/69 PASS in 2.84s |
| 007 | [slice-007-add-critique-agent-content-equality-audit](slice-007-add-critique-agent-content-equality-audit/) | 2026-05-10 | Standard | low (critic-required: true — cross-cutting tooling) | SHIPPED-WITH-DEFERRALS — validation PASS | 8 (2B + 3M + 3m) all ACCEPTED-FIXED inline at /critique time — verdict CLEAN per triage_audit; **all 8 VALIDATED at /validate-slice — second consecutive 100% Critic accuracy slice (slice-006: 11/11; slice-007: 8/8)**; **B1 + B2 both fatal saves** (B1: in-repo file is `VERSION` not `ai-sdlc-VERSION` — install-time rename per `INSTALL.md:141`; without B1 the design's forward-sync table would have pointed at a nonexistent in-repo file. B2: design promised to fix slice-006 PMI-1 escape "as a byproduct" but no AC gated it; `python -m tools.plugin_manifest_audit` was failing pre-build with version-mismatch; without B2 escape would silently persist into slice-007); voluntary Critic on cross-cutting tooling N=7/7 paid off with 5/7 design-stage catches — methodology pattern now load-bearing | **CAD-1 audit shipped** (option d hybrid per ADR-006: skill-prose + standalone audit); **slice-006 PMI-1 escape closed atomically** (`plugin.yaml.version` 0.20.0 + `VERSION` 0.21.0 → both 0.22.0; gated by AC #5 + TF-1 row); methodology v0.22.0; INST-1 canonical tools 14 → 15; bidirectional sha256 forensic capture N=2 stable; "validate using own ship" N=5 stable; **cross-cutting-conformance Dim 9 catch rate empirically improving 0% → 25% → 60%** (slices 1-5 → slice-006 → slice-007 with 9-dim Critic); design.md mechanical tables vs canonical references **N=2 promotion threshold MET** (slice-006 DEVIATION-1+2 + slice-007 B1) — slice-008 candidate refine Dim 9 sub-clause; **BC-1 false positives N=3 promotion threshold MET** (slice-005+006+007 BC-PROJ-1+BC-GLOBAL-1) — slice-008 strongest candidate `refine-bc-1-anchors-with-negative-context`; 2 build-time deviations (DEVIATION-1 prose-pin negative-substring uniqueness; DEVIATION-2 em-dash → cp1252 on Windows console — both N=1, watch for recurrence); cross-cutting-conformance miss class hardens to **N=7 distinct slices, 14 sub-class hits**; slice-006-15 effectiveness target ≤2 cross-cutting misses on track but accumulating (5/2 budget consumed across slices 6-7) |
| 006 | [slice-006-update-critic-with-cross-cutting-conformance-dimension](slice-006-update-critic-with-cross-cutting-conformance-dimension/) | 2026-05-10 | Standard | medium | SHIPPED-WITH-DEFERRALS — validation PASS | 11 (2B + 5M + 4m) all ACCEPTED-FIXED inline + 1 ACCEPTED-PENDING (M1 resolved at /build-slice T-late M1 empirical exercise); **all 11 VALIDATED — strongest single-slice Critic performance in project; B1 was a fatal save** — in-repo `agents/critique.md` was missing the surgical Dim 1+4 sub-bullets that Dim 9 cross-references depend on (would have shipped Dim 9 with dangling pointers — exactly the cross-cutting defect class the slice exists to mitigate); user-override of 2026-05-10 Meta-Critic decline of 9th-dim proposal recorded in critic-calibration-log per TRI-1; methodology v0.21.0 / CCC-1 | **9-dim Critic strict-improves over 8-dim** (Phase 4 M1 empirical exercise — caught BC-GLOBAL-1 algorithm-path-conformance miss the 8-dim Critic missed at slice-005's pre-build /critique); **bidirectional out-of-repo sync first occurrence** (installed→in-repo back-sync THEN in-repo→installed forward-sync, 4 sha256 checkpoints captured); **in-repo↔installed content drift is structural class** — every `/critic-calibrate` ACCEPTED proposal creates same drift (slice-007+ candidate `add-critique-agent-content-equality-audit` OR INST-2 OR `update-critic-calibrate-skill-to-edit-in-repo`, hybrid possible); design.md tables vs in-house methodology rules' canonical lists is new cross-cutting-conformance sub-class N=1 (DEVIATION-1 plugin.yaml + DEVIATION-2 ai-sdlc-VERSION drift from INST-1 inventory); BC-1 false positives on methodology-vocabulary slices recur N=2 across slices 005+006 (refine BC-1 anchors with negative-context at N=3); voluntary Critic on cross-cutting tooling slices is N=6/6 with 4-of-6 design-stage fatal catches (now load-bearing methodology); empirical-verification-at-design-time discipline N=5; cross-cutting-conformance miss class hardens to **N=6 distinct slices, 12 sub-class hits** |
| 005 | [slice-005-add-bc-1-keyword-precision](slice-005-add-bc-1-keyword-precision/) | 2026-05-10 | Standard | low | SHIPPED-WITH-DEFERRALS — validation PASS | 9 (2B + 3M + 4m) — 8 ACCEPTED-FIXED inline + 1 ACCEPTED-PENDING (m4 resolved at validate); voluntary critique, low-tier; **5th consecutive pay-off; B2 was a fatal save** — AC #3 used `fenced` while design empirical-table used `fence` (`\bfence\b` doesn't match `fenced`; would have passed AC #3 only via `llm` anchor coincidence) | Pre-existing `audit_slice` heuristic broken for archived slice paths (slice-006+ candidate `fix-bc-1-archived-slice-heuristic`); BC-GLOBAL-1's `Applies to: always: true` short-circuits before anchor filter (DEVIATION-1: `Applies to: **`); empirical-verification-at-design-time should exercise algorithm paths not just metric counts (N=1 — promote to BC-1 if recurs); regression-guard ACs in numbered list trip TF-1 strict (DEVIATION-2: demoted; N=3 confirmation across slice-002+slice-004+slice-005); cross-cutting-conformance Critic-miss class hardens to **N=5 distinct slices, 10 sub-class hits — `/critic-calibrate` threshold MET** with new "algorithm-path-conformance" sub-clause |
| 004 | [slice-004-fix-rr1-audit-docstring-or-regex](slice-004-fix-rr1-audit-docstring-or-regex/) | 2026-05-10 | Standard | low | SHIPPED-WITH-DEFERRALS — validation PASS | 6 (1B + 3M + 2m) all ACCEPTED-FIXED before triage (voluntary critique, low-tier; **4th consecutive pay-off; B1 was a fatal save** — `R-NN` placeholder in design's canonical examples doesn't match `R-?\d+`; would have failed TF-1 strict at /build-slice T5 first run) | Python 3.12+ SyntaxWarning on docstring `\-` literal (deviation rephrased; possible BC-1 candidate if recurs); cross-cutting-conformance miss-class hardens to N=4 with new "language-version conformance" sub-clause; BC-1 keyword-trigger false-positive recurs N=2 (`add-bc-1-keyword-precision` strengthened); "validate using own ship" pattern N=2 stable |
| 003 | [slice-003-add-val-1-imports-allowlist](slice-003-add-val-1-imports-allowlist/) | 2026-05-09 | Standard | low | SHIPPED-WITH-DEFERRALS — validation PASS | 7 (0B + 2M + 5m) all ACCEPTED-FIXED before triage (voluntary critique, low-tier; 3rd consecutive pay-off) | TF-1 false-PASS pitfall (argparse exit-code-2 collision); VAL-1 fixture-meta-irony; BC-1 trigger-keyword false-positive precision; Critic miss-class "cross-cutting conformance" reaches N=3 — `/critic-calibrate` threshold met |
| 002 | [slice-002-fix-diagnose-contract-and-cwd-mismatch](slice-002-fix-diagnose-contract-and-cwd-mismatch/) | 2026-05-09 | Standard | low | SHIPPED-WITH-DEFERRALS — validation PASS | 8 (4M + 4m) all ACCEPTED-PENDING; resolved (voluntary critique, low-tier) | VAL-1 Layer B internal-imports recurring; RR-1 audit docstring vs regex mismatch; Critic miss-class "cross-cutting conformance" accumulating (N=2) |
| 001 | [slice-001-diagnose-orchestration-fix](slice-001-diagnose-orchestration-fix/) | 2026-05-09 | Standard | low | SHIPPED — validation PASS | 12 (2B + 6M + 4m) all ACCEPTED-PENDING; resolved | R1 (cwd-mismatch tool denial); contract over-reach in SKILL.md/templates |
