# Reflection: Slice 011 promote-recursive-self-application-discipline-to-critique-skill-prose

**Date**: 2026-05-13
**Shipped**: YES-WITH-DEFERRALS (5 of 5 ACs PASS; 1 build-time DEVIATION-3-equivalent BC-PROJ-2 Important fire dispositioned defer-with-rationale per slice-010 DEVIATION-3 precedent; 2 VAL-1 Layer B Important findings dispositioned defer-with-rationale as known recurring false-positive class per slice-010 reflection; 1 build-time slip on test_v_0_25_0 entry pin accidental deletion caught at /validate-slice row 10 fail and fixed in-run)

## Validated

- **RSAD-1 codified at slice-011 as Dim 9 6th sub-clause** — `agents/critique.md` Dim 9 section now contains 6 sub-clauses (5 → 6 structural-invariant supersession). Both sub-modes (design-time + build-time-via-/critique-fix-prose) named distinctly with N=3 cumulative evidence anchored to slice-009 M2 + slice-010 design-time stress-test (B1+M1+B5) + slice-010 build-time DEVIATION-3. Bidirectional in-repo + installed byte-equal (CAD-1 sha256 `2ec35939576cdeb0`); methodology-changelog v0.26.0 entry present bidirectionally (sha256 `5c261c963a5ba2db`).
- **RSAD-1 retrospective self-validation at slice-011's own /critique — strongest single-slice methodology-internalization observation in the project** — the slice authoring the discipline had 4 of 7 Critic findings (B1+M1+M2+M3) be themselves recursive-self-application instances on slice-011's own draft. The Critic at /critique caught: (B1) lowercase `design-time`/`build-time` at bullet-start inside bold markers (DEVIATION-1 trap class on canonical body); (M1) BC-1 audit scope misframe (rationale-falsifying); (M2) BC-GLOBAL-1 `always: true` short-circuit mis-attribution (slice-005 changed to `**` glob); (M3) cross-slice-anchor test allowlist over-promise (`BC-PROJ-2` in allowlist but body used abstract `BC-PROJ`). All 4 ACCEPTED-FIXED in-round before /build-slice. RSAD-1 retrospectively self-validates: had the discipline NOT been codified, none of these 4 design-time recursive-self-application catches would have been guaranteed by the Critic prompt's adversarial stance.
- **Empirical-verification-at-design-time discipline N=10 stable** (slice-003..011) — slice-011 ran THREE design-time audits pre-AC-lock: Audit 1 (canonical-literal `Recursive self-application discipline` absent from `agents/critique.md` — confirmed via grep) + Audit 2 (location-pin anchors `Language-version conformance` + `### Bonus: weak graph edges` both unique in `agents/critique.md` — confirmed via grep with scoped `.find()` discipline) + Audit 3 (slice-011's transient artifacts BC-1 prediction — predicted `applicable: [BC-PROJ-2]` per BC-PROJ-2 anchors `fence`/`code-block`/`llm` present in mission-brief + design.md + ADR-010 + abstract framing on canonical body keeping it BC-1-irrelevant per slice-011 Critic M1 correction). All 3 audits predictions VALIDATED at /build-slice Phase 6: zero recurrence of slice-009 DEVIATION-1/2 at slice-011; BC-1 self-application audit returned exactly `applicable: [BC-PROJ-2]` as predicted.
- **Validate-using-your-own-ship pattern N=9 stable** (slice-003..011) — slice-011 self-applies RSAD-1 at its own /critique (slice authoring the discipline is the canonical reference instance of the rule; 4 of 4 design-time cross-cutting catches at /critique on own draft = 100% design-time catch rate per discipline). Generic methodology practice ratchets to N=9.
- **PMI-1 versioned-gate supersession pattern N=4 events stable** (slice-007 introduced `_at_0_22_0` → slice-008 first-superseded with `_at_0_23_0` → slice-009 second-superseded with `_at_0_24_0` → slice-010 third-superseded with `_at_0_25_0` → slice-011 fourth-supersedes with `_at_0_26_0`). Each version-bump slice replaces the prior `_at_0_NN_0` with the new `_at_0_MM_0`. No two version-gates coexist. Friction threshold N≥4 met at slice-011 — but per-bump cost remains ~1 min, trivial; refactor candidate (`refactor-pmi-1-gate-to-version-agnostic-shape`) still deferable.
- **Bidirectional sha256 forensic capture N=7 stable** (slice-005..011) — Phase 0 (pre-edit) + Phase 4 (post-forward-sync) sha256 capture for all 3 in-repo↔installed file pairs (`agents/critique.md`, `methodology-changelog.md`, `VERSION`/`ai-sdlc-VERSION`). Canonical methodology practice.
- **N-substring + N-surface schema-pin discipline applied at slice-011** — substantive canonical phrase `Recursive self-application discipline` pinned across N=3 surfaces (agents/critique.md Dim 9 6th sub-clause title + in-repo methodology-changelog.md v0.26.0 entry + installed methodology-changelog.md v0.26.0 entry). 5 prose-pin tests pin canonical literal + file-class location + sub-mode (design-time + build-time) + strict-both cross-slice anchors + sub-class anchor. The single phrase shared across N surfaces makes drift impossible without breaking ≥2 tests simultaneously.
- **TF-1 PENDING → WRITTEN-FAILING → PASSING transitions all genuine across 8 rows** — 7 rows transitioned PENDING → WRITTEN-FAILING → PASSING with pinned specific failure signals pre-fix (canonical literal absent, structural-invariant count mismatch with substring not found, location AssertionError with `.find()` scoped pre-empting DEVIATION-2 collision, sub-mode substring absent, strict-both anchor count == 0, v0.26.0 entry absent, VERSION 0.25.0 != 0.26.0); 1 row (CAD-1 mini-byte-equality on `agents/critique.md`) transitioned PASSING → WRITTEN-FAILING → PASSING per slice-007/009/010 mini-CAD-1 row 3 precedent at N=3 → N=4 stable.
- **B5 calibration-trail discipline (rule-ID naming convention) N=2 evidence stable** — slice-010 introduced `-T-` suffix for MCT-1 (signal /slice-time prose-heuristic semantics distinct from audit-enforced gate); slice-011 extends with `-D` suffix for RSAD-1 (signal /critique-time adversarial-prompt discipline heuristic). Both prose-heuristic distinct from audit-enforced gate siblings (BC-1 / PMI-1 / CAD-1 / TF-1 / RR-1 / INST-1 / VAL-1 / WIRE-1 / NFR-1 / CSP-1 — each with `tools/*_audit.py`). N=2 convention evidence stable.
- **Slice-010 DEVIATION-3 build-time recursive-self-application sub-mode did NOT recur at slice-011** — slice-011's design-time empirical Audit 3 (BC-1 self-application prediction) correctly anticipated the build-time BC-PROJ-2 fire on slice-011's own mission-brief + design.md + ADR-010 carrying literal `fence`/`code-block`/`llm` substrings. The Critic at /critique caught M1's misframing of WHY BC-1 wouldn't audit `agents/critique.md` content directly; correction applied before /build-slice. Discipline pre-emption works.

## Corrected

- **slice-010 reflection's "recursive-self-application discipline at N=3 evidence threshold met" assertion** — slice-011 codification at `agents/critique.md` Dim 9 6th sub-clause demonstrates the assertion was right: 4 of 4 design-time cross-cutting sub-class catches on slice-011's own draft at /critique validates that the discipline IS load-bearing for cross-cutting tooling slices authoring methodology refinements. Updated [[slices/_index.md]] aggregated-lessons section to reflect codification (lines 27, 40 of slice-010 reflection's promotion-recommendation now closed).
- **slice-011's earlier-drafted "abstract-framing-keeps-canonical-body-BC-1-clean" rationale** (in design.md + ADR-010 + mission-brief.md) — empirically false per slice-011 Critic M1 catch: BC-1 reads slice mission-brief + design.md, NOT `agents/critique.md` content (per `tools/build_checks_audit.py::_read_slice_text` L435-L442). Rationale corrected to stylistic / reusable-artifact-readability framing at /critique time; design.md § Notes on canonical-body content + Audit 3 + ADR-010 § Decision all updated bit-for-bit. The canonical body still uses abstract framing for anchor strings but for the correct stylistic reason; rule-IDs `BC-PROJ-2` named explicitly in body matching existing Dim 9 convention.
- **slice-011's earlier-drafted "BC-GLOBAL-1 fires via `always: true` short-circuit" framing** (in design.md + mission-brief.md) — empirically false per slice-011 Critic M2 catch: per `~/.claude/build-checks.md` L18-L19, slice-005 changed BC-GLOBAL-1's `Applies to: always: true` → `Applies to: **` glob so the per-rule `Trigger anchors:` filter (added in slice-005) can take effect. The `_rule_applies` code at L412 checks `rule.applies_to == ("always",)` for the literal `always: true` sentinel only — `**` is the GLOB path. Path attribution corrected in both files at /critique time.
- **`test_in_repo_and_installed_critique_agent_md_are_content_equal` (with `_md_` infix)** — typo in mission-brief.md + design.md + shippability row 11 — actual test name is `test_in_repo_and_installed_critique_agent_are_content_equal` (no `_md_`). Caught at /build-slice Phase 5; fixed via `replace_all` across all three docs. Build-time deviation from plan: minor typo, not a design deviation.
- **`test_v_0_25_0_mct_1_entry_present_in_repo_and_installed` accidentally deleted at /build-slice Phase 1b** — when superseding the PMI-1 versioned-gate test `_at_0_25_0` → `_at_0_26_0`, my Edit `old_string` spanned the ENTIRE slice-010 section header + the v_0_25_0 entry pin function + the `_at_0_25_0` PMI-1 gate; my `new_string` replaced everything with the new RSAD-1 section, accidentally REMOVING the v_0_25_0 entry pin. **Root cause**: confusing the SUPERSESSION semantics. Entry pins (`test_v_0_NN_0_<rule>_entry_present_in_repo_and_installed`) PERSIST across all versions — every shipped version's entry is permanently tested for bidirectional presence. Only PMI-1 versioned-gate tests (`test_plugin_yaml_version_matches_version_file_at_0_NN_0`) supersede (latest version only; older gates deleted). My Edit at Phase 1b conflated both semantics by replacing too large a block. **Surfaced at /validate-slice Step 5.5 shippability catalog row 10 fail** (slice-010 critical-path command references the deleted test). **Fixed at /validate-slice** by re-adding the v_0_25_0 entry pin function between v_0_24_0 and v_0_26_0 entry pins; full methodology suite re-ran clean (362/362 PASS; +1 from re-added test); row 10 re-ran clean (7/7 PASS). Slice-011's transient artifacts and the canonical Critic-prompt sub-clause body remain unchanged by this validation-time correction.

## Discovered

- **NEW: Entry-pin-vs-PMI-1-gate-semantics-conflation at Edit time (slice-011 build-time slip; N=1)** — when superseding a PMI-1 versioned-gate test via `Edit` with `old_string`/`new_string` parameters, scoping the Edit block to include the SECTION header (`# --- Slice-NNN / RULE entry pinning ---`) + the entry pin function + the PMI-1 gate function — and replacing the WHOLE block with the new version's section — silently deletes the prior version's entry pin. The Critic at /critique did not predict this (it's an Edit-tool-scoping discipline, not a design semantic). Caught at /validate-slice Step 5.5 by the shippability catalog regression check (row 10 FAILED with `no match in any of...`). **Distinct sub-class**: separate from slice-009 DEVIATION-1/-2 (case-sensitivity + `.find()` collision) + slice-010 DEVIATION-3 (recursive-self-application at /critique fix prose level). N=1 at slice-011; **promote to Dim 9 sub-class refinement at N=2 if recurs at slice-012+**. Generic methodology lesson (independently of recurrence): **when superseding a PMI-1 versioned-gate test, the Edit `old_string` should target ONLY the gate function body, NOT a wider section block containing the persistent entry pin for the same version**. Documentation candidate: `~/.claude/CLAUDE.md` or installed CLAUDE.md global instructions section on PMI-1 versioned-gate supersession Edit-scoping discipline.
- **BC-PROJ-2 negative-anchor migration N=2 evidence threshold MET at slice-011** (slice-005 + slice-011 methodology-vocabulary slices triggering BC-PROJ-2 false-positive on transient artifacts via prose-describing-the-rule). Slice-005 was N=1 prior; slice-010 surfaced DEVIATION-3 build-time recursive-self-application (still N=1 for BC-PROJ-2 specifically); slice-011 ratchets BC-PROJ-2 false-positive evidence to N=2 because: (a) slice-011's mission-brief + design.md + ADR-010 contain literal `fence`/`code-block`/`llm` substrings for empirical evidence of RSAD-1's build-time-via-/critique-fix-prose sub-mode; (b) BC-1 audit at Phase 6 fired `applicable: [BC-PROJ-2]` Important; (c) slice has zero LLM-fence-parsing code to which BC-PROJ-2's check applies — the fire is purely from the slice describing the rule. **Slice-012+ candidate**: `bc-proj-2-negative-anchor-migration` — add 9-token methodology-vocabulary negative-anchor set to BC-PROJ-2 mirroring slice-008 BC-PROJ-1 + BC-GLOBAL-1 v1.2 migration. ~30 min skill scope. Closes the noise loop for future methodology-vocabulary slices triggering BC-PROJ-2 via prose-describing-the-rule.
- **VAL-1 Layer B false-positive class on intra-repo `tests` package N=9 recurrence** (slice-003..011) — every slice 003-011 hits `hallucinated-import: tests` because `from tests.methodology.conftest import ...` references the intra-repo namespace package which `pyproject.toml` doesn't (intentionally) declare. VAL-1 v2 enhancement candidate (handle `[tool.pytest.ini_options]` testpaths from pyproject.toml). NOT a slice candidate; defer until VAL-1 surfaces more meaningful findings. **N=9 cumulative recurrence is significant** — if /critic-calibrate uses recurring noise patterns as a calibration signal, this may eventually warrant promotion despite being a known-false-positive.
- **RSAD-1 empirical effectiveness signal at slice-011 (N=1 standalone for codified discipline)** — 4 of 4 cross-cutting design-time sub-class hits caught at /critique on slice-011's own draft (100% design-time catch rate). The slice-010 DEVIATION-3 build-time recursive-self-application sub-mode did NOT recur at slice-011 — slice-011's design-time empirical Audit 3 correctly anticipated the build-time BC-PROJ-2 fire. **Pattern signal**: RSAD-1 codification appears to be load-bearing for design-time recursive-self-application catches; the codified Critic prompt at /critique time worked as intended. Awaiting slice-012+ recurrence evidence: does slice-012 also catch design-time recursive-self-application sub-classes at /critique on its own draft (if slice-012 itself authors a methodology refinement)?
- **MCT-1 self-applies cleanly to slice-011 (validate-using-your-own-ship N=9 ratchet)** — slice-011 modifies `agents/critique.md` (matches MCT-1 trigger glob `agents/*.md`) + `methodology-changelog.md` (matches MCT-1 trigger glob `methodology-changelog.md`); `milestone.md` frontmatter `critic-required: true` was auto-set at /slice time per MCT-1 codification at slice-010. This is the FIRST slice operating under MCT-1 default per slice-010 codification — the codification works as intended. Canonical practice at slice-012+.

## Deferred

- **`bc-proj-2-negative-anchor-migration`** (slice-012+ candidate; **N=2 evidence threshold MET**) — slice-005 + slice-011 trigger BC-PROJ-2 false-positive on methodology-vocabulary prose. ~30 min skill scope; mirrors slice-008 BC-PROJ-1 + BC-GLOBAL-1 v1.2 migration. Add 9-token methodology-vocabulary negative-anchor set (`aggregated lessons`, `forward-sync`, `back-sync`, `Dim 9`, `vocabulary`, `meta-discussion`, `defer-with-rationale`, `cross-cutting-conformance`, `recursive-self-application`) to BC-PROJ-2. Closes the noise loop for future methodology-vocabulary slices triggering BC-PROJ-2 via prose-describing-the-rule.
- **`entry-pin-vs-pmi-1-gate-semantics-conflation` Dim 9 sub-class refinement** (slice-012+ candidate at N=2) — slice-011 N=1 build-time slip discovered at /validate-slice row 10 fail. Promote to Dim 9 sub-class at N=2 if recurs at slice-012+. Generic methodology lesson worth tracking independently of recurrence: **when superseding a PMI-1 versioned-gate test, the Edit `old_string` should target ONLY the gate function body, NOT a wider section block containing the persistent entry pin for the same version**.
- **`refactor-pmi-1-gate-to-version-agnostic-shape`** — slice-007/008/009/010/011 carryover; slice-011 N=4 supersession events stable; friction threshold N≥4 met but per-bump cost remains trivial (~1 min); refactor candidate deferred indefinitely unless version-bump churn becomes friction (current N=5 cumulative supersessions across slice-007..011).
- **INST-2 generalization** (general content-equality audit across all installed files) — slice-007/010 carryover; still N=1 actual-drift evidence (only `agents/critique.md` drift confirmed historically; slice-010's added `skills/slice/SKILL.md` mini-CAD-1 and slice-011's reused `agents/critique.md` CAD-1 both surfaced zero actual drift). Defer to N=2 actual-drift evidence.
- **`case-sensitivity-canonical-literal-pin-discipline`** — slice-009 DEVIATION-1 carryover at N=1 stable. Slice-010 pre-empted via design-time prose-structure discipline; slice-011 pre-empted on own canonical body via slice-011 Critic B1 fix (capitalized bullet titles + lowercase canonical substrings mid-sentence). Did NOT recur at slice-011 BUT was CAUGHT at slice-011 /critique on the slice's own draft as B1 — first time the discipline was caught by an empirical Critic catch (vs design-time prose-structure pre-emption). **Cross-cutting Dim 9 sub-class promotion candidate at N=2 if recurs at slice-012+**. Slice-011 catches but doesn't yet promote — wait for cross-slice empirical recurrence.
- **`substring-search-location-pin-anchor-uniqueness-discipline`** — slice-009 DEVIATION-2 carryover at N=1 stable. Slice-010 + slice-011 both pre-empted via design-time anchor-uniqueness empirical verification + scoped `.find()` test impl; did NOT recur. Promote at N=2 if recurs at slice-012+.
- **VAL-1 Layer B intra-repo-package handling enhancement** — N=9 cumulative recurrence (slice-003..011). v2 candidate (handle `[tool.pytest.ini_options]` testpaths from pyproject.toml). Not a slice candidate yet; defer until VAL-1 surfaces more meaningful findings. **N=9 is significant evidence** — escalating consideration if /critic-calibrate flags as recurring noise pattern.
- **R-1 deeper fix for /diagnose cwd-mismatch** — risk-register HIGH-band open; needs `/risk-spike` first to disambiguate cwd vs parallel-spawn-cascade hypotheses per slice-002 Critic M1.
- **`tools/rsad_1_audit.py` v2 candidate** — programmatic enforcement of RSAD-1 (walk `architecture/slices/*/critique.md` and assert cross-cutting tooling slices' Critic actually probed the slice's own artifacts for rule-class violations). ~0.5 day; deferred at slice-011. Promote if drift surfaces (a future cross-cutting tooling slice ships without recursive-self-application stress-test and surfaces rule-class violations post-build that the Critic should have caught).
- **`tools/mct_1_audit.py` v2 candidate** — slice-010 carryover; programmatic enforcement of MCT-1. ~0.5 day. Promote if drift surfaces (a future slice modifies in-house surface but sets `critic-required: false` slips through). Slice-011 demonstrated MCT-1 works at /slice time — `critic-required: true` was correctly auto-set per the trigger glob.
- **`/critic-calibrate` next-run recalibration significantly overdue** — slice-009 + slice-010 reflections recommended slice-012-015 boundary; cumulative cross-cutting misses through slice-011 = **10 misses across slices 6-11** (5× the ≤2 target with 4 slices remaining in window). **Recommendation post-slice-011**: (a) recalibrate the quantitative target downward (e.g., "≤5 cumulative misses across the window") per slice-009 + slice-010 reflection's recalibration recommendation; AND/OR (b) refine the Critic prompt with slice-011's NEW build-time slip sub-class candidate (entry-pin-vs-pmi-1-gate-semantics-conflation at N=1; promote at N=2). **`/critic-calibrate` at slice-012-015** to settle.

## Critic calibration

Per **TRI-1**, scoring each finding from `critique.md` `## Triage` table against build + validate reality:

- **B1** (canonical body lowercase `design-time`/`build-time` at bullet-start inside bold markers; DEVIATION-1 trap class): **VALIDATED** — disposition ACCEPTED-FIXED at /critique. Empirically verified at /validate-slice AC #2 (sub-mode pin test PASSES with capitalized bullet titles + lowercase canonical substrings mid-sentence). The original draft would have invited markdown auto-capitalization to `Design-time`/`Build-time` at any future editing pass; the test's case-sensitive `in` operator would then fail. **Strong VALIDATED + preventive save** + **first-instance recursive-self-application catch on the canonical body of the rule the slice was encoding**.

- **M1** (BC-1 audit scope misframe — "abstract-framing-keeps-canonical-body-BC-1-clean" rationale rests on false premise): **VALIDATED** — disposition ACCEPTED-FIXED. Empirically verified at /build-slice Phase 6: BC-1 audit fired `applicable: [BC-PROJ-2]` Important on slice-011's TRANSIENT artifacts (mission-brief + design.md + ADR-010) per the corrected Audit 3 prediction. BC-1 reads slice mission-brief + design.md, NOT `agents/critique.md` content (per `tools/build_checks_audit.py::_read_slice_text` L435-L442). Strong VALIDATED.

- **M2** (BC-GLOBAL-1 `always: true` short-circuit mis-attribution; slice-005 changed to `Applies to: **` glob): **VALIDATED** — disposition ACCEPTED-FIXED. Empirically verified at /build-slice Phase 6: BC-GLOBAL-1 not in `applicable` set (silenced by methodology-vocabulary negative anchors per BC-1 v1.2). The original mis-attribution would have made future Critics + Builders mis-predict BC-GLOBAL-1 behavior. Strong VALIDATED.

- **M3** (cross-slice-anchor test allowlist includes `BC-PROJ-2` but body uses abstract `BC-PROJ`): **VALIDATED** — disposition ACCEPTED-FIXED via m1 fix path (name `BC-PROJ-2` explicitly in body matching existing Dim 9 convention naming `BC-1`, `RR-1`, `TF-1`, etc. by ID). Empirically verified at /validate-slice AC #3 (cross-slice-anchor test passes with body containing `BC-PROJ-2` 2×). Test allowlist consistent with body content; future readers won't infer that adding `BC-PROJ-2` to body would be acceptable (it IS acceptable, matches convention). Strong VALIDATED.

- **m1** (`BC-PROJ rule` shorthand loses retrieval traction vs naming `BC-PROJ-2`): **VALIDATED** — disposition ACCEPTED-FIXED jointly with M3. Body now names `BC-PROJ-2` explicitly 2× ("BC-PROJ-2's positive-anchor strings", "BC-PROJ-2's anchor path"). Matches existing Dim 9 convention. Strong VALIDATED.

- **m2** (AC #3 "TWO+" framing structurally weaker than strict-both with 2-element allowlist): **VALIDATED** — disposition ACCEPTED-FIXED. AC #3 + verification plan row 3 + test impl all renamed to strict-both (`cs_count == 2` not `>= 2`). Honesty preserved; future readers won't be misled by the "TWO+" framing implying room for >2. Strong VALIDATED.

- **m3** (test name 87 chars too long; `_between_language_version_and_bonus` suffix bakes anchor identity into function name): **VALIDATED** — disposition ACCEPTED-FIXED. Renamed to `test_critique_dim_9_recursive_self_application_location_pinned` (61 chars); docstring retains anchor identity. Future Dim 9 changes won't invalidate the test name. Cosmetic but preventive. Strong VALIDATED.

**All 7 Critic findings VALIDATED at /critique-disposition level**: 0 FALSE-ALARM, 0 OVERRIDE-MISJUDGED, 0 NOT-YET, 0 unaddressed-MISSED at /critique-disposition. **Sixth consecutive 100% Critic-disposition accuracy slice. Running total 54/54 across slices 6-11** — strongest streak in the project.

### Missed by Critic

The Critic surfaced 4 design-time cross-cutting sub-class hits at /critique (B1 DEVIATION-1 trap; M1 BC-1 scope misframe; M2 BC-GLOBAL-1 path mis-attribution; M3 test-allowlist over-promise). Build/validate surfaced **1 additional build-time slip NOT in the /critique findings**:

- **Entry-pin-vs-PMI-1-gate-semantics-conflation at /build-slice Phase 1b** — Critic-MISSED at /critique. The Critic stress-tested slice-011's design-time prose for rule-class violations (caught B1+M1+M2+M3) but did NOT predict that the Builder would conflate "slice-010 section" with "all of slice-010's additions" in the Edit-tool's `old_string` scope when superseding the PMI-1 versioned-gate test. The accidental deletion of `test_v_0_25_0_mct_1_entry_present_in_repo_and_installed` was caught at /validate-slice Step 5.5 by the shippability catalog regression check (row 10 FAILED). Fixed in-line at validate-time (re-added the entry pin function); zero impact on shipped slice content. **Calibration class for /critic-calibrate**: when superseding a PMI-1 versioned-gate test via Edit-tool block replacement, the Critic should anticipate that the Builder may conflate the SECTION header + entry pin function + PMI-1 gate function as one block — flag the section-block-scoping risk explicitly. **Distinct sub-class** from slice-009 DEVIATION-1/-2 + slice-010 DEVIATION-3 + slice-011's 4 design-time catches; it's an Edit-tool-scoping discipline, not a design semantic. N=1 standalone for build-time entry-pin-vs-PMI-1-gate-conflation; promote to Dim 9 sub-class at N=2 if recurs at slice-012+.

### Pattern

The cross-cutting-conformance miss-class is now **N=11 distinct slices, 25 sub-class hits cumulative**. Slice-011 sub-class hits:
- **CAUGHT at /critique** (recursive-self-application instances on slice-011's own draft): B1 (DEVIATION-1 trap on canonical body) + M1 (BC-1 audit scope misframe) + M2 (BC-GLOBAL-1 path mis-attribution) + M3 (test allowlist over-promise) — 4 of 4 design-time cross-cutting catches at /critique (100% design-time catch rate per discipline).
- **MISSED at /critique, surfaced at build/validate**: 1 build-time slip — entry-pin-vs-pmi-1-gate-semantics-conflation (caught at /validate-slice Step 5.5 shippability catalog row 10 fail).
- **Total at slice-011**: 5 sub-class hits; 4 caught; 1 missed.
- **Catch rate at slice-011**: 4/5 = **80%**.

**Catch-rate trajectory revision** post-slice-011: 0% (slices 1-5, pre-CCC-1) → 25% (slice-006) → 60% (slice-007) → 100% (slice-008) → 60% (slice-009) → 87.5% (slice-010) → **80% (slice-011)**. **Range-bound 60-100% on N=6 evidence stable**. Slice-008's strict-monotonic claim was already falsified at slice-009; confirmed range-bound at slice-010 + slice-011.

Mitigating context: slice-011's 1 miss surfaced at **Step 5.5 of /validate-slice** (~3-5 min resolution time at validate; trivial fix). The failure-mode quality is good — caught immediately by an automated regression-check (shippability catalog), fixed in-line at validate-time, zero impact on the shipped slice content. The shippability catalog regression-check (Step 5.5) is doing its job as a safety net for build-time slips that the design-time Critic cannot anticipate.

**`/critic-calibrate` next-run effectiveness check** (per the 2026-05-10 user-override entry; target ≤2 cross-cutting misses across slices 6-15):
- Cumulative cross-cutting misses post-slice-011:
  - Slice-006: 4 missed
  - Slice-007: 2 missed
  - Slice-008: 0 missed
  - Slice-009: 2 missed
  - Slice-010: 1 missed (DEVIATION-3)
  - Slice-011: 1 missed (entry-pin-vs-pmi-1-gate-conflation at build)
  - **Cumulative: 10 missed across slices 6-11**
- Target: ≤2 across slices 6-15
- Status: **11/15 of the window consumed; 8 misses over budget (10 > 2)** — significantly exceeded target

**Recommendation for `/critic-calibrate` at slice-012-015**: (a) recalibrate the quantitative target downward (e.g., "≤5 cumulative misses across the window") per slice-009 + slice-010 reflection's recalibration recommendation; AND/OR (b) refine the Critic prompt with slice-011's NEW build-time slip sub-class candidate (entry-pin-vs-pmi-1-gate-semantics-conflation at N=1; promote at N=2). **`/critic-calibrate` at slice-012-015 boundary** to settle.

**RSAD-1 empirical effectiveness signal at slice-011** (the canonical reference instance of the rule it encodes): 4 of 4 cross-cutting design-time sub-class hits caught at /critique on slice-011's own draft (100% design-time catch rate). The slice-010 DEVIATION-3 build-time recursive-self-application sub-mode did NOT recur at slice-011 — slice-011's design-time empirical Audit 3 correctly anticipated the build-time BC-PROJ-2 fire on transient artifacts. RSAD-1's codification appears to be load-bearing for design-time recursive-self-application catches.

## Lessons for next slice

- **RSAD-1 codified at slice-011 — empirical effectiveness signal N=1 (own-slice retrospective)**. 4 of 4 cross-cutting design-time sub-class hits caught at /critique on slice-011's own draft (100% design-time catch rate). The slice-010 DEVIATION-3 build-time recursive-self-application sub-mode did NOT recur at slice-011 — design-time empirical Audit 3 correctly anticipated the build-time BC-PROJ-2 fire. RSAD-1 appears load-bearing for design-time recursive-self-application catches; awaiting slice-012+ recurrence for cross-slice evidence (N=2 stable threshold).
- **BC-PROJ-2 negative-anchor migration N=2 evidence MET at slice-011 — slice-012+ STRONGEST candidate**. Slice-005 + slice-011 both trigger BC-PROJ-2 false-positive on methodology-vocabulary prose. ~30 min skill scope (mirrors slice-008 BC-PROJ-1 + BC-GLOBAL-1 v1.2 migration). **Slice-012 candidate**: `bc-proj-2-negative-anchor-migration`.
- **NEW Build-time slip sub-class candidate at N=1 (slice-011)**: entry-pin-vs-pmi-1-gate-semantics-conflation. When superseding a PMI-1 versioned-gate test, the Edit `old_string` should target ONLY the gate function body, NOT a wider section block containing the persistent entry pin for the same version. Cross-cutting Dim 9 sub-class candidate at N=1; promote to Dim 9 sub-class refinement at N=2 if recurs at slice-012+. Generic methodology pattern worth tracking independently of recurrence.
- **Cross-cutting-conformance Dim 9 catch rate at slice-011 = 80% (4 of 5)** — falls within the range-bound 60-100% framing from slice-008..010 (N=6 evidence). Trajectory: 0% (slices 1-5) → 25% (slice-006) → 60% (slice-007) → 100% (slice-008) → 60% (slice-009) → 87.5% (slice-010) → **80% (slice-011)**. **Range-bound 60-100% on N=6 evidence stable** post-slice-011.
- **All 7 Critic findings VALIDATED at slice-011 /critique-disposition level** — 0 FALSE-ALARM, 0 OVERRIDE-MISJUDGED, 0 NOT-YET, **0 unaddressed-MISSED at /critique-disposition** (1 build-time slip MISSED at slice-cycle level). Sixth consecutive 100% Critic-disposition accuracy slice. **Running total 54/54 across slices 6-11** — strongest streak in the project.
- **Validate-using-your-own-ship N=9 stable** (slice-003..011). Slice-011 self-applies RSAD-1 at its own /critique with 4 of 4 design-time catches. The slice authoring the discipline IS the canonical reference instance of the rule. Canonical methodology practice.
- **Empirical-verification-at-design-time discipline N=10 stable** (slice-003..011). Slice-011 ran THREE design-time audits (canonical-literal absence + location-pin anchor uniqueness + Audit 3 BC-1 self-application prediction on transient artifacts). All 3 audits' predictions VALIDATED at /build-slice. Zero recurrence of slice-009 DEVIATION-1/2 + slice-010 DEVIATION-3 sub-modes at slice-011.
- **Bidirectional sha256 forensic capture N=7 stable** (slice-005..011). Phase 0 + Phase 4 sha256 capture for all 3 in-repo↔installed file pairs. Canonical methodology practice.
- **PMI-1 versioned-gate supersession pattern N=4 events stable** (slice-007/008/009/010/011). Each version-bump slice replaces the prior `_at_0_NN_0` with the new `_at_0_MM_0`. Refactor candidate (`refactor-pmi-1-gate-to-version-agnostic-shape`) deferred indefinitely; per-bump cost trivial.
- **B5 calibration-trail discipline (rule-ID naming convention) N=2 evidence stable** — slice-010 introduced -T- suffix (MCT-1); slice-011 extends with -D suffix (RSAD-1). Both prose-heuristic distinct from audit-enforced-gate siblings. N=2 convention evidence stable.
- **MCT-1 self-applies cleanly to slice-011** (first slice operating under MCT-1 default per slice-010 codification). `critic-required: true` auto-set at /slice time per MCT-1 trigger glob match. Codification works as intended.
- **N-substring + N-surface schema-pin discipline N=3 stable** (slice-009/010/011) — substantive canonical phrase pinned across 3 surfaces per slice. Slice-011 pins `Recursive self-application discipline` across N=3.
- **TF-1 PENDING → WRITTEN-FAILING transition N=8 stable** (slice-003..011). Mini-CAD-1 row 3 PASSING → WRITTEN-FAILING → PASSING transition pattern N=4 stable (slice-007/009/010/011).
- **/critic-calibrate significantly overdue post-slice-011** — cumulative cross-cutting misses 10 across slices 6-11 (5× the ≤2 target across slices 6-15). Recommendation per slice-009/010/011 reflection: (a) recalibrate the quantitative target downward (e.g., "≤5 cumulative misses across the window"); AND/OR (b) refine Critic prompt with slice-011's NEW build-time slip sub-class candidate (promote at N=2). **`/critic-calibrate` at slice-012-015 boundary**.
- **From slice-011 candidate**: `case-sensitivity-canonical-literal-pin-discipline` — slice-009 DEVIATION-1 carryover at N=1 stable; slice-011 first time the discipline was CAUGHT by Critic at /critique (vs design-time pre-emption). Cross-cutting Dim 9 sub-class promotion candidate at N=2 if recurs at slice-012+.
- **From slice-011 candidate (deferred)**: `substring-search-location-pin-anchor-uniqueness-discipline` — slice-009 DEVIATION-2 carryover at N=1 stable; slice-010 + slice-011 both pre-empted; did NOT recur. Promote at N=2.
- **VAL-1 Layer B false-positive class on intra-repo `tests` package N=9 recurrence at slice-011** — significant cumulative evidence; VAL-1 v2 enhancement candidate. Not a slice candidate yet; defer.

## Vault updates made

- [[agents/critique.md]] (in-repo) + [[~/.claude/agents/critique.md]] (installed) — new 6th Dim 9 sub-clause `Recursive self-application discipline` appended at L167 (between L166 `Language-version conformance` close and L168 `### Bonus: weak graph edges` H3). CAD-1 byte-equality sha256 `2ec35939576cdeb0` clean.
- [[methodology-changelog.md]] (in-repo) + [[~/.claude/methodology-changelog.md]] (installed) — v0.26.0 RSAD-1 entry appended with substantive canonical phrase + Limitations note per slice-010 B5 convention extension. Bidirectional sha256 `5c261c963a5ba2db` clean.
- [[VERSION]] (in-repo) + [[~/.claude/ai-sdlc-VERSION]] (installed) + [[plugin.yaml]].version — atomic bump 0.25.0 → 0.26.0. Bidirectional sha256 `4f3ca66d226add97` clean.
- [[tests/methodology/test_critique_agent.py]] — replaced `test_critique_dim_9_lists_five_sub_clauses` with `test_critique_dim_9_lists_six_sub_clauses` (structural-invariant supersession) + added 4 new RSAD-1-specific tests (sub_clause_present + location_pinned + names_both_sub_modes + cites_at_least_two_cross_slice_anchors).
- [[tests/methodology/test_methodology_changelog.py]] — replaced `test_plugin_yaml_version_matches_version_file_at_0_25_0` with `_at_0_26_0` (PMI-1 versioned-gate supersession N=4 events) + added `test_v_0_26_0_rsad_1_entry_present_in_repo_and_installed` + **re-added `test_v_0_25_0_mct_1_entry_present_in_repo_and_installed` at /validate-slice** (accidentally deleted at /build-slice Phase 1b; fixed at validate-time).
- [[architecture/shippability.md]] — added row 11 (slice-011 critical path) + updated row 6 (test name `_lists_five_sub_clauses` → `_lists_six_sub_clauses` post-supersession) + updated row 10 (note slice-011 PMI-1 version-gate supersession 0.25.0 → 0.26.0; removed superseded `_at_0_25_0` test reference per row 7/8/9 pattern).
- [[architecture/decisions/ADR-010-promote-recursive-self-application-discipline-to-critique-dim-9-sub-clause.md]] — NEW. Reversibility cheap with ~12 sites magnitude justification (same class as ADR-007/009).
- [[architecture/risk-register.md]] — no changes (no new risks; entry-pin-vs-pmi-1-gate-conflation is methodology-level, not code/system risk).
- [[architecture/concept.md]] — no changes (no scope drift).
- [[architecture/lessons-learned.md]] — slice-011 chronological entry appended at Step 5 below.
