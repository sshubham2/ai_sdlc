# Critique Review: Slice 041 reframe-installed-pin-forward-sync-invariant

**Reviewed by**: critique-review agent (DR-1)
**Date**: 2026-05-18
**First-Critic verdict**: BLOCKED
**Dual-review verdict**: EXTEND

## Summary

The first Critic's central empirical claim is substantively correct and the BLOCKED verdict is justified: the meta-Critic independently re-ran `tools.shippability_decoupling_audit architecture/shippability.md --json` and confirmed `essential count 37` with a real population the design's "32 fns / rows 7–30" scope materially under-counts. B1/B2/M1/M3/M4/m2 are VALID. However: the first Critic's "**8** NOT `_entry_present`" headline is a miscount (real = **6 unique** non-`_entry_present`; the Critic then listed exactly 6); M2 is severity-miscalibrated DOWN (filed Major, verified-low-risk → Minor); m1 is severity-miscalibrated (filed Minor, is a latent silent-disable → Major); and BOTH the design and the first Critic missed a cross-file string-literal coupling surface for the rename that PTFFD-1 does not cover (M-add-1) plus an essential-set ≠ selector-set divergence of exactly one fn (M-add-2). The Builder's recommended B1/B2 resolution (re-home the cross-module pin's changelog leg onto MCFS-1, keep allowlist empty) is independently verified mechanically sound with no hidden contradiction.

## Confirmed findings

- **B1** (essential ≠ the 32; empty allowlist exits 1) — **VALID**, Blocker appropriate. Reproduced: `rows_scanned 40`, `essential` raw = 37, 28 unique `_entry_present_in_repo_and_installed`, plus non-`_entry_present` essentials incl. the cross-module `tests/skills/diagnose/test_skill_md_pins.py::test_textual_evidence_rule_byte_equal_across_n_3_surfaces` (reads `Path.home()/".claude"/"methodology-changelog.md"` L349, cited shippability **row 19**; file also cited whole at **row 29**). design.md never mentions row 19/29, `test_skill_md_pins.py`, or the `_entry_names_*` set. Failure mechanism real; AC3/AC4/AC5 fail as written.
- **B2** (empty-allowlist vs legitimate cross-module pin contradiction) — **VALID**, Blocker appropriate. Re-home-vs-register branch correctly surfaced for TRI-1.
- **B1/B2 recommended resolution soundness** — independently verified: `_ESSENTIAL_SHAPES = ((".claude","methodology-changelog.md"),)` matches *only* the changelog segment. Removing solely the installed-changelog surface (L349) from the cross-module pin re-classifies it `clean`; its other installed legs (installed 03f-layering.md, installed SKILL.md) do not match `_ESSENTIAL_SHAPES`. "Re-home the changelog leg, keep allowlist empty" has **no hidden contradiction**.
- **M3** (no mechanical pre-finish self-classification check for the v0.53.0 pin) — **VALID**, Major appropriate (slice-037 "layers 2&3 reachable only by pre-finish real-artifact runs").
- **M4** (obligation branch / suffix-convention enforcer not verified) — **VALID**, Major appropriate. Independently confirmed NO structural test iterates `## vX.Y.Z` asserting a correspondingly-named `_entry_present_in_repo_and_installed` test. v0.53.0 confirmed correct next version (VERSION 0.52.0, plugin.yaml 0.52.0).
- **m2** (whole-file equality ≠ canonical-phrase presence) — **VALID**, Minor appropriate, reinforced by M-add-1 (the cross-module pin is a phrase-presence pin at L366, not byte-equality despite its name).

## Suspicious findings

None (drop-the-finding). B1's substance is empirically confirmed; the "8" inaccuracy is a precision/severity adjustment (below), not a SUSPICIOUS over-reach — failure mechanism and remediation are unaffected.

## Missed findings

- **M-add-1 (Major): the rename has cross-file string-literal dependents outside the PTFFD-1-covered shippability `Machine-cmd` column.** PTFFD-1 is shippability-catalog-scoped. Executable assertions in other files name the old suffix and PTFFD-1 does not reach them: `tests/methodology/test_critique_agent.py:1426` pins the literal `` `test_v_0_NN_0_<rule>_entry_present_in_repo_and_installed` entry-pin `` inside `agents/critique.md` prose (coupled to CAD-1 Critic-agent content-equality — slice-039 generalized same-fix-block class); `tests/methodology/test_query_design_skill.py:17` and `tests/methodology/test_shippability_decoupling_audit.py:56,128` carry old-suffix string literals in fixture/expected-output assertions. design.md's "any in-repo prose refs" hand-waves what are executable couplings. **Proposed fix**: design.md component entry enumerating the non-shippability cross-file old-suffix literals (incl. the `test_critique_agent.py:1426` → coordinated `agents/critique.md` CAD-1 prose decision); verification-plan row asserting `grep -rn "_entry_present_in_repo_and_installed" tests/ agents/` returns only intentionally-retained occurrences post-rename. **Reservation**: if the rename leaves the Critic-agent prose unchanged, `test_critique_agent.py:1426` still passes but the agent then documents a stale identifier — a soft drift the Builder must consciously decide at redesign, not discover.
- **M-add-2 (Minor): the audit-resolved essential population (34 unique) ≠ the selector-cited population (27 unique) by exactly one fn.** `test_v_0_42_0_utf8_stdout_1_v1_1_entry_present_in_repo_and_installed` is in M1's "5 defined-but-uncited" list (no `::`-selector) yet **is in the audit's `essential` set** (transitively resolved by `_cited()`/`_reachable_path_segments`, not literal selector) → it will `essential-unregistered ⇒ exit 1` under ADR-043 with no selector to update. **Proposed fix**: drive the B1 decouple worklist off `shippability_decoupling_audit --json` `essential` (34 unique incl. cross-module pin), explicitly noting `v_0_42_0_utf8_stdout_1_v1_1` is essential-but-uncited.

## Severity adjustments

- **B1 sub-claim "8 NOT `_entry_present`" — PRECISION-WRONG (correct the count, finding stands).** Independently: raw essential 37, unique 34, unique `_entry_present_in_repo_and_installed` 28, unique **non-`_entry_present` = 6** (`test_v_0_29_0_entry_names_supersession_pattern_retired`, `test_v_0_31_0_rpcd_1_entry_names_three_sub_modes_in_repo_and_installed`, `test_v_0_32_0_tphd_1_entry_names_slice_016_cross_slice_anchor`, `test_v_0_32_0_tphd_1_entry_names_three_sub_modes_in_repo_and_installed`, `test_v_0_33_0_layer_evid_1_entry_names_textual_import_evidence_canonical_phrase`, `test_skill_md_pins.py::test_textual_evidence_rule_byte_equal_across_n_3_surfaces`). The Critic wrote "8 NOT" then listed exactly 6 distinct names; "8" = raw-rows minus 28 (dupes: cross-module pin + `rpcd_1` appear twice). All 6 names real/correct. Correct B1 redesign + M1 to "37 raw / 34 unique / 28 unique `_entry_present` / 6 unique non-`_entry_present`".
- **M2 (sibling-scoping helpers) — SEVERITY-WRONG: Major → Minor.** `_extract_version_body` (L12–38) scopes purely by `## v{version}` / `## v{predecessor}` semver-decremented **text anchors in changelog content**, NOT fn names, NOT installed-half body structure. The slice reshapes test-fn code but does not touch `## v` markers in `methodology-changelog.md`, so scope windows cannot shift. The Critic's own Builder draft concedes "risk is low" — filing as Major while the embedded note concedes low is a calibration mismatch. The cheap regression assertion is still correct to add (= a Minor disposition).
- **m1 (generic `/reflect` post-write wiring) — SEVERITY-WRONG: Minor → Major.** Not cosmetic. BCI-1's `/reflect` wiring is **Step 5b**, gated *"mandatory when a rule was promoted"* (`skills/reflect/SKILL.md` L208). MCFS-1's concern is the PMI-1 changelog forward-sync, which fires on a **version bump**, not rule promotion. A version bump can occur on a slice that promotes no build-check rule. If MCFS-1 is mis-wired into the rule-promotion-gated Step 5b (the obvious place given design.md's generic "post-write fail-loud step"), it would NOT run on the changelog-bumping-but-no-rule-promoted path — a silent-disable of the forward-sync gate for exactly the path it protects (R-7 / slice-022-self-violation class, named in the mission-brief must-not-defer). Latent correctness consequence, not precision.

## Notes

Confidence in the empirical core is high — the audit was reproduced deterministically (`rows_scanned 40`, essential raw 37, unique 34) and the classification path (`_ESSENTIAL_SHAPES`), the cross-module pin's actual assertion (phrase-presence L366, not byte-equality), and the rename's cross-file literal dependents were all traced. The first Critic's pattern: substantively right but arithmetically loose — correctly executed the audit (slice-032 "execute don't reason" honored), correctly identified the failure mechanism + a sound remediation, but mis-stated the count, mis-severitied M2 up and m1 down, and (like the design) missed the cross-file/CAD-1 string-literal coupling (M-add-1) and the essential≠selector divergence (M-add-2). EXTEND (not ACCEPT) because M-add-1 (Major) + M-add-2 (Minor) are genuine additions.
