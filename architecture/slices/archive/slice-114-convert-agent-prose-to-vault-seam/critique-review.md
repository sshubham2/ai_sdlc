# Critique Review: Slice 114 convert-agent-prose-to-vault-seam

**Reviewed by**: critique-review agent (DR-1)
**Date**: 2026-06-05
**First-Critic verdict**: NEEDS-FIXES
**First-Critic finding counts**: 0 blockers, 3 majors, 3 minors
**Dual-review verdict**: EXTEND

## Summary
The first Critic's review is technically sound — all 6 findings (M1/M2/M3/m1/m2/m3) are VALID at correct severity, every load-bearing claim verified against live state (no `test_critic_calibrate_agent_drift.py` on disk; the live classifier reports `rewrite-at-flip`/`operational-reference` not git-pathspec for code-review.md:25; the ratchet does require both carve-out hashes; doc-example legitimately goes 2→4). I add one Minor missed finding: the slice introduces the *second* CAD-1-guarded agent that the root `CLAUDE.md` CAD-1 enumeration does not name, and the design is silent on that prose-parity loose end.

## Confirmed findings

First-Critic findings the meta-Critic agrees with (VALID + correct severity):

- **M1 (no drift guard on critic-calibrate.md): confirmed; Major is appropriate.** Verified on disk — `tests/methodology/` contains `test_critic_calibrate_agent.py` only (META-2 substring pins), NOT `test_critic_calibrate_agent_drift.py`. The installed runtime prompt would silently drift from the converted in-repo prompt with no gate. The recommended fix (mirror `test_code_review_agent_drift.py`, reuse `assert_md_forward_synced`) is the correct CAD-1 family pattern (slice-060 CRSI-1 precedent). `test_critic_calibrate_agent.py` pins NO `architecture/` substring, so the line-22 conversion will not red it — the two tests are non-colliding.
- **M2 (code-review.md:25 mislabeled "class 1-pathspec"): confirmed; Major is appropriate.** Ran the live tool: code-review.md:25 (`architecture/**`) classifies `rewrite-at-flip`/`operational-reference`, NOT a git-pathspec class. `_PATHSPEC_RE` (vault_flip_prose_inventory.py:130) requires `:(exclude|glob|top…)` syntax, absent on line 25's prose. Carve-out *decision* right (mirrors SKILL.md concrete pathspecs per slice-113 /code-review B1) but the evidence/label was wrong. The `[M2-fix]` relabel is accurate to the tool.
- **M3 (both surviving carve-outs MUST be hash-added or --strict exits 2): confirmed; Major is appropriate.** Per `converted_file_regressions()` (460-471), adding `agents/code-review.md` to `_CONVERTED_FILES` forces every surviving `rewrite-at-flip` literal to be a sanctioned `_CONVERTED_CARVEOUTS` member. Computed both keys: `('agents/code-review.md','0a69ee77…')` for `architecture/**` and `('agents/code-review.md','46e2f7eb…')` for `architecture/slices/slice-NNN-<name>/code-review.md`. Genuinely distinct (no collision), one-way-safe — distinct from `skills/code-review/SKILL.md`'s EXPLOITABLE same-value collision (348-355).
- **m1 (DOC_EXAMPLE floor stays 0): confirmed; Minor is appropriate.** The slice-112 pilot note's literal-bearing line (critique.md:11) classifies `doc-example` (plain-prose/un-backticked/op-verb-free); embedding it in two more agents takes doc-example 2→4. `test_per_class_total_count_floor` (179) asserts `>=` floor, so leaving DOC_EXAMPLE at 0 stays green; raising it to 4 would falsely red a future skill conversion that lowers doc-example. The `[m1-note]` is sound — drift fails closed via `_BASELINE_SHA256` + ratchet, not the floor.
- **m2 (stale "rows 113/117"): confirmed; Minor is appropriate.** ADR-105 line 49 hard-states "rows 113 + 117"; slice-113 moved the live-count sites. Verified current set: vault_flip_prose_inventory.py docstring (~21/47/48/51) + `_CLASS_COUNT_FLOOR` (306/308) + `EXPECTED_TOTAL` (332/334), and shippability.md file-lines 122/126/127/128 (markdown rows 113/117/118/119). The `[m2-fix]` per-row disposition is correct: L122's `**132**`/`130/0/2/0` LIVE + L126/L127's forward-looking sentinels update to 131/127; L128's `EXPECTED_TOTAL=132 / …=130` is slice-113 AS-BUILT history (stays) while its trailing `132/130 re-pin reverts` sentinel updates. Historical-vs-forward-looking split correct.
- **m3 (mission-brief row 3 hardcodes 127/4/131 while design says derive): confirmed; Minor is appropriate.** Numbers arithmetically right (130−3=127; 2+2=4; 132−1=131); the annotation preserves AP-3 build-time recalibration. Annotation, not rewrite, is the right disposition.

## Suspicious findings

No suspicious findings. Every first-Critic finding survived verification against live tool output, the test corpus, and ADR-105. The first Critic did not over-reach on any of the six.

## Missed findings

- **m-add-1 (Minor): the root `CLAUDE.md` CAD-1 enumeration names only `agents/critique.md`, and this slice creates the SECOND unenumerated CAD-1-guarded agent — the design is silent on that prose-parity loose end.** `CLAUDE.md:39` CAD-1 bullet enumerates exactly one agent (`agents/critique.md`), yet `test_code_review_agent_drift.py` has guarded `code-review.md` since slice-060 *without* CLAUDE.md enumerating it, and M1 now adds `test_critic_calibrate_agent_drift.py` guarding `critic-calibrate.md` — a second unenumerated member. This is the exact class the slice-096 entry flagged ("`pulse` + `code-review` have drift tests but are not yet enumerated here — a separate courtesy-parity cleanup"). The first Critic's M1 said "+ shippability + TF-1 row" but did NOT mention the CLAUDE.md CAD-1 prose enumeration. **Proposed disposition (not a blocker):** design should add one sentence (§Decisions or §Out-of-scope) recording that the CLAUDE.md CAD-1 enumeration deferral is deliberate (matching the slice-096 courtesy-parity precedent), naming a future cleanup that enumerates `code-review` + `critic-calibrate` under CAD-1. Minor because the *tests* are authoritative and gate regardless of the prose; the gap is documentation-completeness, not enforcement.

(No Major or Blocker missed. Attacked four hypotheses, cleared three: (a) leaving critique-review.md:78 + diagnose-narrator.md:19 untouched is CORRECT, not a gap — both are ADR-105 carve-out classes 5/7 a note-less agent legitimately keeps concrete (M-add-1); the ratchet protects only converted files by design. (b) The M1 MEPD-1 EXCLUDE call is DEFENSIBLE — see Notes. (c) No TF-1 plan row structurally absent in a blocking way: mission-brief Verification row 4 already names `test_critic_calibrate_agent_drift.py`, M1 is ACCEPTED-PENDING. (d) No PMI-1/INST-1 consequence — `plugin.yaml` enumerates no `tests/methodology/` files; `install_audit._CANONICAL_*` covers skills/agents/templates/tools only. The design's omission of PMI-1/INST-1 fan-out is correct.)

## Severity adjustments

No severity adjustments. M1/M2/M3 are correctly Major (each has a fail-closed enforcement or runtime-drift consequence); m1/m2/m3 are correctly Minor (documentation precision / floor-tuning with no enforcement-defeating path).

## Notes

Confidence high — every first-Critic claim verified by running the live tool (`--strict` exit 0 at 130/0/2/0/132), computing the two carve-out SHA-256 keys, and reading the on-disk test files + ADR-105. The first Critic's pattern this slice is well-calibrated: sharp on the two enforcement-mechanics findings (M2 label-vs-classifier, M3 ratchet two-hash requirement), restrained on the minors. No rubber-stamp smell.

On the headline question (is M1's fix genuinely MEPD-1 EXCLUDE?): **yes, defensible**, but the design's cited reasoning is thinner than the actual precedent. Early CAD-1/OSDG-1 member-adds (049/051/088) were MEPD-1 INCLUDE (changelog + version bump) because each *established* the open-ended-set principle. slice-096 explicitly reframed a *pure test-only* member-add as MEPD-1 EXCLUDE — "adding a member to the already open-ended set is the rule operating within its documented scope" (CLAUDE.md:42). M1 is precisely a test-only CAD-1 member-add against a now-established family, minting no RULE-ID → matches slice-096 EXCLUDE more closely than slice-051 INCLUDE. The design's citation ("matches slice-113 prose half + 106/109/111 house style") is correct in direction but the load-bearing precedent is slice-096; the EXCLUDE/INCLUDE call is a /reflect determination anyway, so not a blocker. One caveat on TF-1: the new drift test should be authored with a genuine FAIL→PASS contrast (mutate the installed copy, assert the `DRIFT` message, restore) at build (AP-5 non-vacuity; the slice-051 reflect-drift precedent required exactly that) — the mission-brief smoke gate does not currently mention exercising the new drift test's non-vacuity. Build-time discipline, not a design defect.
