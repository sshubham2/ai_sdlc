# Critique Review: Slice 016 refine-dim-9-with-runtime-prerequisite-completeness-sub-class

**Reviewed by**: critique-review agent (DR-1)
**Date**: 2026-05-13
**First-Critic verdict**: CLEAN
**Dual-review verdict**: EXTEND

## Summary

The first Critic's 7 findings are all empirically grounded, correctly severity-calibrated, and correctly dispositioned to ACCEPTED-FIXED. However, the meta-Critic surfaces ONE missed concern: the slice-016 design omits a `_runtime_prerequisite_completeness_location_pinned` sibling test, breaking the N=3-stable `_sub_clause_present` + `_location_pinned` duality that slice-011 / slice-013 / slice-015 each carry (slice-009 M1 / slice-010 M1 location-pin convention). This is the same regression-guard class the discipline exists to defend.

## Confirmed findings

First-Critic findings the meta-Critic agrees with (VALID + correct severity):

- **B1**: AC #4(c) / Phase 1e / Audit 7 end_anchor tighten targets — VALID; Blocker severity is appropriate. Confirmed empirically against `tests/methodology/test_critique_agent.py:173/304/492` (`_location_pinned` siblings whose `### Bonus:` end_anchor is structurally load-bearing per slice-013/015 precedent) vs L519/554/594 (the actual SCPD-1 body-bound tests requiring tighten). /build-slice would have catastrophically targeted the wrong functions; Blocker correct.
- **B2**: TF-1 plan referenced non-existent test function names — VALID; Blocker severity is appropriate. Empirical grep confirms `_rsad_1_body_pins_*` and `_scpd_1_body_pins_*` do not exist in `tests/methodology/test_critique_agent.py`. `--strict-pre-finish` TF-1 audit would have mapped planned rows to nothing. Blocker correct.
- **M1**: Audit 2 `_ALLOWED_STATUSES` false claim — VALID; Major severity is appropriate. `tools/test_first_audit.py:65` reads `frozenset({"PENDING", "WRITTEN-FAILING", "PASSING"})` — 3 elements, NO `WRITTEN-AS-EDIT`. Factual provenance error in slice's own design.md. Per Wiegers (every claim traces to evidence) — Major correct (factual error in canonical evidence-chain, not blocker because TF-1 plan itself didn't use the non-existent status).
- **M2**: Entry-pin function count off-by-1/2 — VALID; Major severity is appropriate. Empirical grep on `tests/methodology/test_methodology_changelog.py` returns 10 entry-pin functions (L76/111/152/202/250/304/370/437/499/747). Pre-fix "8" off-by-2 vs functions / off-by-1 vs minor versions. Major (numerical-inflation defect in canonical citation surface used across mission-brief + design + audit + ADR).
- **M3**: Audit 7 grep-completeness incomplete — VALID; Major severity is appropriate. Grep returns 6 hits (L173/304/492/519/554/594) vs pre-fix's 2. RPCD-1 sub-mode (c) self-defect on slice's own audit prose. Major correct (audit completeness gap; flowed downstream into B1's mis-targeting).
- **m1**: ADR-015 Consequences evidence-chain — VALID; Minor severity is appropriate. Citation-completeness; directionally correct claim missing slice-011/013/015/016 links. Minor correct (no operational consequence; cosmetic chain hygiene).
- **m2**: "M0 tighten provenance" non-canonical naming — VALID; Minor severity is appropriate. Convention break vs slice-013/015 precedent. Minor correct.

## Suspicious findings

No suspicious findings. Independent empirical verification (grep on test_critique_agent.py end_anchor sites, cat on tools/test_first_audit.py:65, function-count grep on test_methodology_changelog.py, line-content cross-check on slice-013/015 `_location_pinned` precedent) confirmed each of the 7 findings is grounded in design.md's pre-fix factual state. B1/B2/M3 are structurally adjacent but each catches a distinct defect surface (B1: wrong test names / wrong line numbers / mis-attribution of EPGD-1 test to slice-015; B2: planned TF-1 rows referencing non-existent functions; M3: Audit 7 grep-enumeration incompleteness) — not redundant; each blocks a distinct downstream failure mode and warranted separate filing.

## Missed findings

Concerns the first Critic didn't flag but the meta-Critic surfaces from independent re-review:

- **M-add-1: Missing `test_critique_dim_9_runtime_prerequisite_completeness_location_pinned` sibling — regression-guard gap vs N=3 stable `_sub_clause_present` + `_location_pinned` duality**
  - **Issue**: `design.md` lines 12-15 enumerate 4 NEW body-bound tests for RPCD-1: `_sub_clause_present`, `_names_three_sub_modes`, `_paragraph_cites_slice_013_014_015`, `_cites_substantive_discipline_anchors`. The conspicuously ABSENT test is `_location_pinned`. Slice-011 RSAD-1 (L146 `_sub_clause_present` + L158 `_location_pinned`), slice-013 EPGD-1 (L270 `_sub_clause_present` + L286 `_location_pinned`), and slice-015 SCPD-1 (L463 `_sub_clause_present` + L475 `_location_pinned`) ALL carry the duality. The `_location_pinned` test was specifically codified per slice-009 M1 + slice-010 M1 location-pin convention to defend against "a future drift moving the new sub-clause out of Dim 9 (e.g., into Dim 8 or the Bonus section) would silently pass substring-only pin tests; the location-pin guard catches it" (verbatim docstring from `_recursive_self_application_location_pinned` L161-163).
  - **Compounding semantic confusion**: design.md L12 describes the slice-016 `_sub_clause_present` test as performing scoped find between start_anchor + end_anchor — which is the `_location_pinned` semantic, not the `_sub_clause_present` (bare substring) semantic per slice-015 precedent at L463 (which uses only `assert canonical in CRITIQUE`). Either the slice-016 `_sub_clause_present` is mis-named (and should be `_location_pinned`) AND a separate bare-substring `_sub_clause_present` is missing, OR the design intends the duality but only specified one test. Either reading is a coverage gap.
  - **Framework**: Wiegers (regression-guard coverage symmetry across N=3 prior reference instances must be preserved when the codification pattern itself is being ratified at the discipline level) + CCC-1 v1.1 sub-clause 2 design-doc-level surface (mechanical-row symmetry: the design.md mechanical enumeration of body-bound tests should match the canonical-inventory enumeration across slice-011/013/015).
  - **Evidence**: `tests/methodology/test_critique_agent.py:146-183` (RSAD-1 `_sub_clause_present` + `_location_pinned` duality); `:270-314` (EPGD-1 same duality); `:463-502` (SCPD-1 same duality); slice-016 `design.md` L11-15 enumerates only one of the pair. Shippability row 15 pytest command for slice-015 includes BOTH `_sub_clause_present` AND `_location_pinned` explicitly — slice-016 row 16 mirror would similarly need both.
  - **Severity**: Major. The location-pin is regression-guard not blocker (slice ships without it but loses defense against future Dim 9 restructuring drifting RPCD-1 out of Dim 9). Files as Major because: (a) the slice-016 mission specifically codifies RPCD-1 as Dim 9 9th sub-clause — locking that placement is canonical-truth-establishment intent of the slice itself; (b) all three prior reference instances at N=3 stable carry the duality, and N=3 is the slice's own promotion threshold standard. NOT Blocker because /build-slice still ships (the omission is a coverage gap, not a wrong-target).
  - **Proposed fix**: Add a 5th body-bound test `test_critique_dim_9_runtime_prerequisite_completeness_location_pinned` mirroring slice-015 `_shippability_catalog_propagation_location_pinned` (L475-502) — scoped find with `start_anchor = "Shippability-catalog consumer-reference propagation"`, `end_anchor = "### Bonus: weak graph edges"`, `canonical = "Runtime-prerequisite completeness on proposed fixes"`. Rename design.md L12's `_sub_clause_present` test to confirm its semantic (either restate as bare-substring pin, or rename to `_location_pinned` and add a separate bare-substring `_sub_clause_present`). Update shippability row 16 pytest command list to include the new `_location_pinned` test. Update mission-brief AC #4(b) + TF-1 plan accordingly (add 1 row).

## Severity adjustments

No severity adjustments. Each of the 7 first-Critic findings sits at the empirically correct severity tier:

- B1 and B2 are correctly Blockers — /build-slice would have hard-failed (wrong line targets in B1; non-existent test functions in B2 producing strict-pre-finish audit miss-mapping).
- M3 correctly differs from B1 in severity (Major-vs-Blocker): M3 is the upstream audit-completeness incompleteness that produced B1's mis-targeting; B1 is the downstream operational consequence at AC #4(c) + Phase 1e + TF-1 plan. Filing both separately is correct calibration — fixing M3 alone wouldn't have caught the (independent) misattribution of EPGD-1's L304 test to slice-015 SCPD-1, which was a distinct semantic error orthogonal to grep-completeness.
- M2 is correctly Major (not Minor): numerical inflation propagated across 4 surfaces (mission-brief Must-not-defer + design.md Components-touched + Audit 4 + implicit dependency on the EPGD-1 narrow-scope verification) — citation-chain load-bearing for downstream Builder + future calibrations. Not Minor because the citation chain is what /critic-calibrate aggregations consume at slice-017+.
- m1 is correctly Minor (not Major): citation completeness on ADR Consequences section is hygiene, not load-bearing — even without the evidence chain links, the directional claim is correct and future calibration runs can re-derive the chain from the slice-numbered N-stability counters elsewhere in the ADR. Major would have required an empirical-defect class (factual error or missing canonical phrase), not citation hygiene.

## Notes

Empirical verification confidence: high. All 7 first-Critic findings re-validated by independent grep / line-content checks on the underlying artifacts (test_critique_agent.py end_anchor sites at L173/304/492/519/554/594; test_first_audit.py L65 `_ALLOWED_STATUSES`; test_methodology_changelog.py entry-pin function-def grep returning 10 hits; slice-013/015 `_location_pinned` duality precedent at L146-183 / L270-314 / L463-502).

Pattern observation across DR-1 catches N=4 trajectory: slice-013 M-add-1 sibling-test grep + slice-014 M-add-1 missing imports + slice-015 M-add-1 `_ALLOWED_STATUSES` non-membership were each RPCD-1 sub-mode (a/b/c) instances. The slice-016 missed `_location_pinned` is structurally different — it is NOT an RPCD-1 sub-mode (a/b/c) gap; it is a Wiegers-class regression-guard coverage symmetry omission. This signals that DR-1's value extends beyond just-RPCD-1-classes — it catches design-doc-level mechanical-table-vs-canonical-inventory asymmetries (CCC-1 v1.1 sub-clause 2 design-doc-level surface). If this missed finding validates at TRI-1 triage, it becomes a watch-list candidate for /critic-calibrate aggregation at slice-018+ if recurs (potential 10th Dim 9 sub-clause family: "body-bound-test-duality regression-guard preservation" — N=1 at slice-016).

Slight reservation: the slice-016 design.md L12 text describing `_sub_clause_present` as "pins RPCD-1 canonical anchors BETWEEN start_anchor and end_anchor" reads ambiguously. If the Builder interprets this as location-pin semantics (scoped find), the test functionally COVERS the `_location_pinned` defect class even though it carries the `_sub_clause_present` name — in which case the gap is naming-confusion (Minor), not coverage (Major). However, the slice-015 precedent at L463 (`_sub_clause_present`) uses bare substring `assert canonical in CRITIQUE`, NOT scoped find — so name-convention-following readers should interpret slice-016's `_sub_clause_present` as bare-substring too, leaving the location-pin gap genuine. Builder should clarify intent at /build-slice; if the Builder builds scoped-find semantics under the `_sub_clause_present` name, a follow-on naming-cleanup or sibling addition is still indicated.
