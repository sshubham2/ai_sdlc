# Reflection: Slice 010 promote-voluntary-critic-on-cross-cutting-to-slice-default-heuristic

**Date**: 2026-05-12
**Shipped**: YES-WITH-DEFERRALS (5 of 5 ACs PASS; 1 build-time DEVIATION-3 BC-PROJ-2 recursive-self-application defer-with-rationale; 1 /critique m2 deferred to slice-011+; 3 VAL-1 Layer B Important findings dispositioned defer-with-rationale as known false-positive class)

## Validated

- **MCT-1 codified as the new mandatory-Critic trigger for in-house methodology surfaces** — `skills/slice/SKILL.md` Step 4a list grew 7 → 8 bullets + adjacent evidence prose paragraph; both in-repo + installed copies byte-equal (sha256 `ac6fc1e82464d1d6`); `methodology-changelog.md` v0.25.0 entry present bidirectionally (sha256 `1dcfcb97edc7c8fa`). From slice-011+ onward, /slice invocations matching the MCT-1 trigger glob (`skills/*/SKILL.md`, `agents/*.md`, `tools/**/*.py`, `methodology-changelog.md`) auto-set `critic-required: true` — eliminates per-slice voluntary-call friction.
- **Empirical-verification-at-design-time discipline N=9 stable** (slice-003..010) — pre-AC-lock empirical audits CORRECTLY pre-empted slice-009 DEVIATION-1 + DEVIATION-2 traps. Audit 1 (canonical-literal absence) + Audit 2 (location-pin anchor uniqueness) confirmed at design time. Zero recurrence of slice-009's two DEVIATIONs at slice-010 build time.
- **Mini-CAD-1 byte-equality test PASSING → WRITTEN-FAILING → PASSING transition genuine** per slice-007 + slice-009 row 3 precedent (N=3 stable). Test started PASSING at Phase 1b (both files byte-equal at slice start per Phase 0 capture); transitioned to WRITTEN-FAILING at Phase 2 (in-repo edited, observed at Phase 2b mid-slice smoke as `RuntimeError: sha256 mismatch`); transitioned to PASSING at Phase 2c (post-forward-sync). Critic B2 framing correction was right.
- **N-substring + N-surface schema-pin discipline applied** per slice-008 M2 + slice-009 M3 lessons — substantive canonical phrase `In-house methodology surfaces` pinned across 3 surfaces (skills/slice/SKILL.md bullet + in-repo methodology-changelog.md + installed methodology-changelog.md). 5 prose-pin tests pin canonical literal + file-class anchor + N=9/9 + voluntary Critic + ≥2 cross-slice anchors + ≥2 sub-class anchors.
- **PMI-1 atomic version-bump invariant** — `VERSION` + `~/.claude/ai-sdlc-VERSION` + `plugin.yaml.version` all `0.25.0`; `python -m tools.plugin_manifest_audit --root .` exit 0 (24 skills + 5 agents + 15 tools); PMI-1 versioned-gate supersession N=3 events post-slice-010 (slice-007 introduced → slice-008 first-superseded → slice-009 second-superseded → slice-010 third-superseded). No two version-gates coexist.
- **TF-1 PENDING → WRITTEN-FAILING → PASSING transitions all genuine across 8 rows** per slice-003..009 N=6 → N=7 stable pattern. 7 transitioned PENDING → WRITTEN-FAILING → PASSING; 1 (mini-CAD-1) transitioned PASSING → WRITTEN-FAILING → PASSING per slice-009 row 3 precedent. Each row had pinned specific failure signal pre-fix (canonical literal absent, sha256 mismatch, version mismatch).
- **Bidirectional sha256 forensic capture pattern N=6 stable** (slice-005..010) — canonical methodology practice. Phase 0 (pre-edit) + Phase 4 (post-forward-sync) sha256 capture for all 3 in-repo↔installed file pairs.
- **BC-1 v1.2 negative-anchor mechanism silences BC-GLOBAL-1 on slice-010's own ship** — empirically verified at Phase 4 BC-1 self-application audit. BC-GLOBAL-1 did NOT surface (negative anchors `aggregated lessons`, `forward-sync`, `back-sync`, `Dim 9`, `vocabulary`, `meta-discussion`, `defer-with-rationale` all matched per slice-010 prose). "Validate using your own ship" pattern **N=7 → N=8 stable** (slice-003..010).
- **Shippability catalog regression-clean** — 10 rows / 80 tests / 80 PASS in 2.67s aggregate (well under 2-min target). No regression on any past slice.
- **MCT-1 self-applies cleanly to slice-010** — recursive-self-application case clean: slice-010 modifies `skills/slice/SKILL.md` (matches MCT-1 trigger glob); `milestone.md` frontmatter `critic-required: true` matches the rule the slice authored. The slice that encoded the rule itself qualifies under the rule.

## Corrected

- **"8 of 9 design-stage catches" framing dropped from mission-brief + design.md + ADR-009 + proposed SKILL.md bullet body** — internally inconsistent with the project's running counter across slice-007 (off-by-one re-cast: claimed "5 of 7" but enumerated 6 catches) and slice-008→slice-009 (counting-rule change from "8 of 8" to "8 of 9" without retroactive recount). Critic B1 caught at /critique-disposition; would have canonized inconsistency into permanent SKILL.md + append-only changelog. Replaced with honest cumulative-post-build framing: "every voluntary Critic invocation on a cross-cutting tooling slice produced VALIDATED findings post-build with zero FALSE-ALARMs". Verifiable per slice-N reflection records.
- **MCR-1 → MCT-1 rule rename** — Critic B5 caught the rule-class semantic conflation (MCR-1 "Mandatory Critic Rule" implies audit-enforced gate per BC-1/PMI-1/CAD-1/TF-1/RR-1/INST-1/VAL-1/WIRE-1 convention; MCT-1 "Mandatory Critic Trigger" with -T- suffix signals /slice-time prose-heuristic distinct from audit-enforced gate). Rename applied across mission-brief + design + ADR + SKILL.md bullet + methodology-changelog v0.25.0 entry + test name `test_v_0_25_0_mct_1_entry_present_in_repo_and_installed`. ADR-009 prepended with naming-rationale note explaining the -T- distinction.
- **Single multi-clause bullet → terse bullet + adjacent evidence prose paragraph** — Critic M1 caught the bullet body's ~3x length + ~5x complexity asymmetry vs the existing 7 bullets. Restructured: terse one-line bullet `In-house methodology surfaces (skills/*/SKILL.md, agents/*.md, tools/**/*.py, methodology-changelog.md)` matching `External integrations (OAuth, payment gateways, third-party APIs)` shape + adjacent markdown blockquote evidence prose appended after the existing "When producing the mission brief..." paragraph. Canonical-literal pins split across both surfaces.
- **Audit 3 BC-1 prose: uniform "negative-anchor silences" framing → precise distinction** — Critic B4 caught: BC-PROJ-1 + BC-PROJ-2 don't positively-fire at all (empirical word-boundary count = 0 for their trigger anchors in slice-010 prose); only BC-GLOBAL-1 positively-fires and is genuinely silenced by negative anchors. Expected `applicable: []` result comes from no-positive-firing + negative-anchor-silencing, NOT uniform negative-silencing.
- **ADR-009 reversibility surface area "~5-7 sites" → "~11-13 sites"** — Critic m3 caught the undercount. Recount enumerates 13 sites (bullet insert + evidence paragraph insert + installed mirror + methodology-changelog v0.25.0 + installed mirror + VERSION + ai-sdlc-VERSION + plugin.yaml.version + 2 new test files + test_methodology_changelog.py edits + shippability row 10 + ADR-009 file). Closer in magnitude to ADR-007 (~10-15 sites; BC-1 v1.2) than to ADR-008 (~5 sites; CCC-1 v1.1 inline refinement). Cheap-vs-expensive verdict unchanged.
- **Line-number references in ADR-009 + design.md → symbolic** — Critic m1 caught the drift-vector: after slice-010 ships, bullet list line numbers shift; future references mislocate. Replaced with symbolic ("between `Security-sensitive paths` bullet and `Heavy mode (always)` bullet") with single as-of-2026-05-12 anchor preserved once.
- **ADR-009 Context per-slice enumeration rewritten with honest disposition state** — Critic B3 caught: slice-001 + slice-002 had Critic findings dispositioned ACCEPTED-PENDING (post-build) and running counter starts at slice-003. New "Honest aggregate framing" subsection replaces prior "Aggregated metrics" with verifiable cumulative-post-build framing.

## Discovered

- **Recursive-self-application phenomenon at BUILD time (slice-010 DEVIATION-3; N=1 standalone for build-time)** — at /critique B4 fix I added empirical prose **describing** BC-PROJ-2's anchors (the substrings `fence`, `code-block`, `llm`) to mission-brief.md L81 + design.md L193 to explain why BC-PROJ-2 wouldn't fire. The description itself contains those substrings → BC-PROJ-2's anchor path positively fires on slice-010's own prose at Phase 4 BC-1 self-application audit. **Slice-009 M2 was at /critique design-time prose; slice-010 brings the same phenomenon to /build-slice build-time audits.** Distinct sub-class. **Cumulative recursive-self-application evidence**: slice-009 M2 (design-time, N=1) + slice-010 /critique design-time stress-test catches (B1+M1+B5; N=2 cumulative) + slice-010 build-time DEVIATION-3 (N=3 cumulative). **Meets the N=3 promotion threshold per slice-009 reflection language** ("Promote `recursive-self-application-discipline` to /critique skill prose at N=3 if a third instance surfaces at slice-011+"). **Slice-011+ candidate**: codify recursive-self-application expectation into the Critic agent prompt — when a slice authors a methodology refinement, stress-test the draft against the very discipline the slice is encoding, AND expect the discipline to self-apply to the rule's own prose (including any /critique fix prose that empirically describes the rule's anchors).
- **BC-PROJ-2 negative-anchor migration N=2 evidence threshold met** (slice-005 + slice-010 methodology-vocabulary slices triggering BC-PROJ-2 false-positive). Slice-005 was N=1 prior; slice-010 ratchets to N=2. Per BC-1 v1.2 + slice-008 reflection promotion convention. **Slice-011+ candidate**: `bc-proj-2-negative-anchor-migration` — add 9-token methodology-vocabulary negative-anchor set to BC-PROJ-2 mirroring slice-008 BC-PROJ-1 + BC-GLOBAL-1 v1.2 migration. Closes the noise loop for future methodology-vocabulary slices triggering BC-PROJ-2 via prose-describing-the-rule.
- **B5 calibration-trail discipline (rule-ID naming convention)** — methodology-changelog rule-ID naming should signal enforcement model: audit-enforced gate vs /slice-time prose-heuristic. MCT-1's -T- suffix establishes this convention going forward. Without this distinction, future readers reading the changelog would assume MCT-1 has a corresponding `tools/mct_1_audit.py` (which doesn't exist — MCT-1 is prose-only). Generic methodology pattern for future rule additions. Critic caught prophylactically; reflects calibration-trail awareness developed at slice-009 M4.
- **VAL-1 Layer B false-positive class on intra-repo `tests` package** — known recurring pattern: every slice 003-010 that adds tests in `tests/methodology/` triggers `hallucinated-import: tests` because `tests` isn't declared in `pyproject.toml` (intentionally — `tests/` is the project's own intra-repo namespace package, not a published package). VAL-1 Layer B doesn't recognize pytest `rootdir` resolution. **NOT a slice candidate** — VAL-1 v2 enhancement candidate (handle `[tool.pytest.ini_options]` testpaths). Defer until VAL-1 surfaces more meaningful findings. For now, defer-with-rationale at /validate-slice per VAL-1 Important semantics.
- **MCT-1's strongest empirical case across slices 1-9** is also its WEAKEST programmatic enforcement: the rule lives in prose only with no audit. The Critic at /critique can be expected to find rule-class violations in the slice's own draft (recursive-self-application N=3); but no static check guarantees future /slice invocations actually SET `critic-required: true` when MCT-1 applies. v2 candidate `tools/mct_1_audit.py` (slice-N+) would walk `architecture/slices/*/mission-brief.md` + `milestone.md` and assert `critic-required: true` when slice scope references in-house methodology files. Adds scope (~0.5 day); deferred at slice-010 to keep within ~0.5-day budget.

## Deferred

- **`bc-proj-2-negative-anchor-migration`** (slice-011+ candidate; **N=2 evidence threshold met**) — slice-010 surfaces this via DEVIATION-3. ~30 min skill scope; mirrors slice-008 BC-PROJ-1 + BC-GLOBAL-1 v1.2 migration. Add 9-token methodology-vocabulary negative-anchor set to BC-PROJ-2.
- **`promote-recursive-self-application-discipline-to-critique-skill-prose`** (slice-011+ candidate; **N=3 evidence threshold met**) — slice-009 M2 + slice-010 /critique stress-test catches + slice-010 DEVIATION-3 = N=3 cumulative. ~30 min skill-prose update to `~/.claude/agents/critique.md` or `~/.claude/skills/critique/SKILL.md`. Codify the expectation that when a slice authors a methodology refinement, the Critic stress-tests the draft against the very discipline the slice is encoding AND expects the discipline to self-apply to the rule's own prose (including /critique fix prose describing the rule's anchors).
- **`refactor-pmi-1-gate-to-version-agnostic-shape`** — slice-007/008/009 carryover; slice-010 added 4th supersession (`_at_0_24_0` → `_at_0_25_0`); each supersession is ~1 min churn; still not at N≥4 friction threshold (slice-010 IS the 4th but the per-bump cost remains trivial). Defer indefinitely unless version-bump churn becomes friction.
- **INST-2 generalization** (general content-equality audit across all installed files) — slice-007 carryover; still N=1 actual-drift evidence (only `agents/critique.md` drift confirmed). Slice-010 added per-file mini-CAD-1 for `skills/slice/SKILL.md` but no actual drift surfaced. Defer to N=2 actual-drift evidence.
- **`case-sensitivity-canonical-literal-pin-discipline`** — slice-009 DEVIATION-1 carryover at N=1. Slice-010 pre-empted via design-time prose-structure discipline (`In-house methodology surfaces` capitalized-I bullet-title form; `voluntary Critic` placed mid-sentence post-`N=9/9 voluntary Critic catch`); did NOT recur. Promote at N=2 if surfaces at slice-011+.
- **`substring-search-location-pin-anchor-uniqueness-discipline`** — slice-009 DEVIATION-2 carryover at N=1. Slice-010 pre-empted via design-time anchor-uniqueness empirical verification + scoped `text.find()` test impl; did NOT recur. Promote at N=2 if surfaces at slice-011+.
- **R-1 deeper fix for /diagnose cwd-mismatch** — risk-register HIGH-band open; documented-constraint workaround acceptable per slice-002; needs `/risk-spike` first to disambiguate cwd vs parallel-spawn-cascade hypotheses.
- **`fix-bc-1-archived-slice-heuristic`** — slice-005 carryover; --changed-files workaround acceptable.
- **`add-csp-1-docstring-or-regex`** — slice-005 carryover sibling tooling-bug pattern; not at promotion threshold.
- **Em-dash → cp1252 / prose-pin substring uniqueness** — slice-007 carryover; both N=1; slice-010 didn't recur. Continue tracking.
- **m2 from /critique** (anchor uniqueness point-in-time concern) — already DEFERRED at /critique triage; slice-011+ monitoring. Fail-fast AssertionError on any future SKILL.md collision provides detection.
- **VAL-1 Layer B intra-repo-package handling enhancement** — v2 candidate (handle `[tool.pytest.ini_options]` testpaths from pyproject.toml). Not a slice candidate yet; defer until VAL-1 surfaces more meaningful findings.
- **`tools/mct_1_audit.py` v2 candidate** — programmatic enforcement of MCT-1 (walk `architecture/slices/*/milestone.md` + `mission-brief.md` and assert `critic-required: true` when slice scope references in-house methodology files). ~0.5 day; deferred at slice-010. Promote if drift surfaces (future slice modifying in-house surface but setting `critic-required: false` slips through).

## Critic calibration

Per **TRI-1**, scoring each finding from `critique.md` `## Triage` table against build + validate reality:

- **B1** (N=9/9 + "8 of 9 design-stage catches" mathematically inconsistent): **VALIDATED** — disposition ACCEPTED-FIXED at /critique; empirically verified the project's running counter is internally inconsistent across slice-007 off-by-one + slice-008→slice-009 counting-rule change; would have canonized inconsistency into permanent SKILL.md + append-only changelog. **Strong VALIDATED + preventive save** (load-bearing claim that would have shipped forever).
- **B2** (AC #3 TF-1 status PENDING mis-classified): **VALIDATED** — disposition ACCEPTED-FIXED; mini-CAD-1 indeed transitions through PASSING → WRITTEN-FAILING → PASSING per slice-009 row 3 precedent. The original PENDING label was misleading. Strong VALIDATED.
- **B3** (ADR-009 slice-001/002 enumeration hand-waves disposition state): **VALIDATED** — disposition ACCEPTED-FIXED; empirically verified per slice-001 + slice-002 reflection records. Honest aggregate framing applied. Strong VALIDATED.
- **B4** (BC-1 self-application prediction structurally trivial; "negative-anchor silences" misleading framing): **VALIDATED-WITH-CARRYOVER** — disposition ACCEPTED-FIXED at /critique; empirical word-boundary count = 0 confirmed BC-PROJ-1 + BC-PROJ-2 don't positively-fire; BC-GLOBAL-1 genuinely silenced by negative anchors. **HOWEVER**: the B4 fix prose itself introduced the BC-PROJ-2 trigger anchors (`fence`, `code-block`, `llm`) into slice-010's mission-brief L81 + design.md L193 → triggered BC-PROJ-2 at build time (DEVIATION-3 recursive-self-application). So B4 was both VALIDATED (correct empirical framing) AND **introduced a new build-time fire** (recursive-self-application). Strong VALIDATED with build-time recursive-self-application carryover lesson.
- **B5** (MCR-1 rule-class semantics conflate audit-enforced gate with /slice-time prose-heuristic): **VALIDATED** — disposition ACCEPTED-FIXED; rename to MCT-1 + Limitations note preserves convention clarity. Critic caught the calibration-trail damage prophylactically. Strong VALIDATED + preventive (would have damaged rule-ID convention for future readers).
- **M1** (bullet body asymmetry ~3x length vs existing 7 bullets): **VALIDATED** — disposition ACCEPTED-FIXED; split into terse bullet + adjacent evidence prose paragraph (markdown blockquote) preserves stylistic uniformity. The bullet now matches `External integrations (OAuth, payment gateways, third-party APIs)` shape. Strong VALIDATED.
- **M2** (AC #2 cross-slice anchors {6-9} vs bullet body "slices 1-9" semantic mismatch): **VALIDATED** — disposition ACCEPTED-FIXED; dovetailed with B1 fix. Cumulative-post-build framing makes "across slices 1-9" honest; slice-6-9 anchors become illustrative sub-class examples not evidence-of-N-count. Strong VALIDATED.
- **M3** (sub-class anchors unpinned → drift vector): **VALIDATED** — disposition ACCEPTED-PENDING at /critique; applied at /build-slice Phase 1a as 5th prose-pin test `test_slice_step4a_evidence_paragraph_cites_at_least_two_sub_class_anchors`. Reality confirmed N-substring discipline-violation would have surfaced as future drift. Strong VALIDATED + preventive.
- **m1** (line-number references in design.md + ADR-009): **VALIDATED** — disposition ACCEPTED-FIXED; replaced with symbolic refs. Cosmetic but preventive.
- **m2** (anchor uniqueness point-in-time; ADR Option 2 example body): **NOT-YET** — disposition DEFERRED to slice-011+ monitoring. Cannot score yet — fail-fast AssertionError on any future SKILL.md collision would surface as immediate test failure; awaiting evidence.
- **m3** (ADR-009 surface area "5-7 sites" undercount): **VALIDATED** — disposition ACCEPTED-FIXED; recount to ~11-13 sites with ADR-007 magnitude comparison. Cosmetic correctness fix; preserves calibration-trail discipline.

**All 11 Critic findings VALIDATED at /critique-disposition level**: 0 FALSE-ALARM, 0 OVERRIDE-MISJUDGED, 0 NOT-YET (excepting m2 by design — DEFERRED awaits future evidence). **Fifth consecutive 100% Critic-disposition accuracy slice. Running total 47/47 across slices 6-10** — strongest streak in the project.

### Missed by Critic

The Critic surfaced 3 cross-cutting sub-class hits at /critique time (B1 + M1 + B5 recursive-self-application catches against slice-010's own draft prose; M3 N-substring discipline catch). Build/validate surfaced **1 additional cross-cutting sub-class hit NOT in the /critique findings**:

- **DEVIATION-3: recursive-self-application at BUILD time via /critique fix prose** — Critic-MISSED at /critique. The Critic correctly stress-tested slice-010's design-time prose for rule-class violations (caught B1, M1, B5) but did NOT predict that the B4 fix prose ITSELF (empirically describing BC-PROJ-2's anchors with the literal substrings `fence`, `code-block`, `llm`) would re-introduce BC-PROJ-2 trigger anchors at build time. **Calibration class for /critic-calibrate**: when adding empirical-rebuttal prose **describing** a build-check rule's anchors/triggers, the prose itself may trigger the rule at build time. The fix prose is recursive-self-application at the /critique-fix-prose level (one level deeper than slice-009 M2 design-time + slice-010 design-time stress-test). N=1 standalone for build-time recursive-self-application; combined with slice-009 M2 (design-time) + slice-010 /critique catches (design-time) the recursive-self-application phenomenon is N=3 evidence cumulative across phases.

### Pattern

The cross-cutting-conformance miss-class is now **N=10 distinct slices, 20 sub-class hits**. Slice-010 sub-class hits:
- **CAUGHT at /critique**: B1 (N=9/9 internal inconsistency) + B3 (ADR enumeration drift) + B4 (BC-1 self-application precision) + B5 (rule-naming convention) + M1 (bullet-style asymmetry) + M2 (cross-slice-anchor semantic mismatch) + M3 (sub-class anchors unpinned)
- **MISSED at /critique, surfaced at build**: DEVIATION-3 (recursive-self-application at build time via /critique fix prose — N=1 standalone for build-time variant)
- **Total at slice-010**: 8 sub-class hits; 7 caught; 1 missed
- **Catch rate at slice-010**: 7/8 = **87.5%**

**Catch-rate trajectory revision**: 0% (slices 1-5) → 25% (slice-006) → 60% (slice-007) → 100% (slice-008) → 60% (slice-009) → **87.5% (slice-010)**. Slice-008 + slice-009's "range-bound 60-100%" framing on N=4 evidence holds — slice-010 (87.5%) falls within range; N=5 evidence on range-bound 60-100% framing.

Mitigating context: slice-010's 1 miss surfaced at **Phase 4 BC-1 self-application audit** (~2-5 min resolution time, dispositioned defer-with-rationale as Important non-blocking; not at /validate-slice or production). The failure-mode quality is good — short feedback loop + non-blocking disposition.

**`/critic-calibrate` next-run effectiveness check** (per the 2026-05-10 user-override entry; target ≤2 cross-cutting misses across slices 6-15):
- Cumulative cross-cutting misses post-slice-010:
  - Slice-006: 4 missed
  - Slice-007: 2 missed
  - Slice-008: 0 missed
  - Slice-009: 2 missed
  - Slice-010: 1 missed (DEVIATION-3)
  - **Cumulative: 9 missed across slices 6-10**
- Target: ≤2 across slices 6-15
- Status: **10/15 of the window consumed; 7 misses over budget (9 > 2)** — significantly exceeded target

**Recommendation for `/critic-calibrate` at slice-012-015**: (a) recalibrate the quantitative target downward to "≤5 cumulative misses across the window" framing per slice-009 reflection's recalibration recommendation; AND/OR (b) refine the Critic prompt with slice-010's NEW build-time recursive-self-application sub-class (promote `recursive-self-application-discipline` to /critique skill prose at N=3 evidence threshold met per slice-009 reflection language).

**Voluntary Critic on cross-cutting tooling slices is now N=10/10 paid off across slices 1-10** — every voluntary Critic invocation produced VALIDATED findings post-build with zero FALSE-ALARMs. **MCT-1 codification at slice-010 means slice-011+ onward this becomes default mandatory** (no longer voluntary-with-N=10/10-ROI; it's mandatory-with-MCT-1-trigger).

## Lessons for next slice

- **Recursive-self-application discipline at N=3 evidence — promote to /critique skill prose at slice-011+**. Per slice-009 reflection language: "Promote to /critique skill prose at N=3 if a third instance surfaces". Slice-010 contributes 1 design-time stress-test (with 3 Critic catches: B1+M1+B5) + 1 build-time DEVIATION-3 (Critic-missed via /critique fix prose) = N=3 cumulative. **Slice-011 strongest candidate**: `promote-recursive-self-application-discipline-to-critique-skill-prose`. ~30 min skill-prose update to the Critic agent prompt.
- **BC-PROJ-2 negative-anchor migration N=2 evidence — slice-011+ candidate**. Slice-005 + slice-010 both triggered BC-PROJ-2 false-positive on methodology-vocabulary prose. Migration mirrors slice-008 BC-PROJ-1/GLOBAL-1 v1.2 migration. ~30 min.
- **MCT-1 prospective application — /critic-calibrate effectiveness check at slice-012-015**. From slice-011+ onward, /slice invocations matching MCT-1 trigger glob auto-set `critic-required: true`. /critic-calibrate at slice-012-015 boundary should assess whether MCT-1 alone has moved cross-cutting catch rate sufficiently OR whether further Critic-prompt refinements (e.g., recursive-self-application discipline) are needed.
- **Audit 3 framing at slice-design-time should explicitly account for /critique fix prose**: future cross-cutting tooling slices' Audit 3 (BC-1 self-application prediction) should empirically count BOTH the pre-/critique mission-brief/design AND any prose anticipated to be added at /critique fix-time. The prose-add-during-/critique pattern is recursive-self-application N=3 evidence and warrants explicit pre-emption discipline.
- **Validate-using-your-own-ship N=8 stable** — slice-003..010. Canonical methodology practice. The MCT-1 self-application audit ratchets the pattern's reflexivity dimension (the slice authoring the rule itself qualifies under the rule).
- **Empirical-verification-at-design-time discipline N=9 stable** — slice-003..010. Pre-AC-lock grep + anchor-uniqueness verification continues to pre-empt DEVIATION-1/DEVIATION-2 class (zero recurrence at slice-010 of slice-009's two DEVIATIONs). Slice-010 ran THREE design-time audits (canonical-literal absence + location-pin anchor uniqueness + BC-1 self-application prediction); 2 of 3 fully correct (Audits 1+2); Audit 3 partially correct (predicted BC-PROJ-1/2 no-positive-fire and BC-GLOBAL-1 silenced — correct AT design time, but B4 fix prose introduced new build-time fire). Discipline still load-bearing; refinement: extend Audit 3 to anticipate /critique fix-time prose additions.
- **Bidirectional sha256 forensic capture N=6 stable** — slice-005..010. Canonical practice.
- **PMI-1 versioned-gate supersession pattern N=3 events stable** — slice-007/008/009/010. Each version-bump slice replaces the prior `_at_0_NN_0` test with the new `_at_0_MM_0`. Generic methodology pattern; refactor candidate (`refactor-pmi-1-gate-to-version-agnostic-shape`) deferred 4x; ~1 min churn per supersession not yet at friction threshold.
- **N-substring + N-surface schema-pin discipline applied at slice-010** for substantive canonical phrase `In-house methodology surfaces`: 3 surfaces (skills/slice/SKILL.md bullet + in-repo methodology-changelog.md + installed methodology-changelog.md). 5 prose-pin tests pin canonical literal + file-class anchor + N=9/9 + voluntary Critic + cross-slice anchors + sub-class anchors. The single phrase shared across N surfaces makes drift impossible without breaking ≥2 tests simultaneously.
- **TF-1 PENDING → WRITTEN-FAILING transition N=7 stable** — slice-003..010. Mini-CAD-1 row 3 PASSING → WRITTEN-FAILING → PASSING transition pattern N=3 stable (slice-007 + slice-009 + slice-010).
- **B5 calibration-trail discipline (rule-ID naming convention)**: when adding a new methodology rule to `methodology-changelog.md`, the rule-ID's letter convention should signal enforcement model (audit-enforced gate vs /slice-time prose-heuristic). MCT-1's -T- suffix establishes the prose-heuristic convention. Generic methodology pattern; promote to /critique skill prose at N=2 if another methodology-rule-naming-convention break surfaces.
- **VAL-1 Layer B intra-repo-package handling enhancement candidate** (v2): handle `[tool.pytest.ini_options]` testpaths from pyproject.toml so `from tests.methodology.conftest import read_file` doesn't false-positive as `hallucinated-import: tests`. Slices 003-010 all hit this. Not a slice candidate yet; track if VAL-1 surfaces more meaningful findings.

## Vault updates made

- `skills/slice/SKILL.md` — Step 4a bullet insert + evidence prose paragraph append (sha256 `f6969bd9358ff75c` → `ac6fc1e82464d1d6`)
- `~/.claude/skills/slice/SKILL.md` — forward-synced from in-repo — same sha256
- `methodology-changelog.md` — v0.25.0 / MCT-1 entry prepended (sha256 `69cf65050c3704fe` → `1dcfcb97edc7c8fa`)
- `~/.claude/methodology-changelog.md` — forward-synced from in-repo — same sha256
- `VERSION` 0.24.0 → 0.25.0 (sha256 `dbf81d3754264f6d` → `4b67cac8ce2a6e82`)
- `~/.claude/ai-sdlc-VERSION` — forward-synced from in-repo VERSION — same sha256
- `plugin.yaml.version` 0.24.0 → 0.25.0
- `tests/methodology/test_slice_skill.py` — appended 5 new test functions (`test_slice_step4a_*`) + `_step4a_section` helper + section-anchor constants
- `tests/methodology/test_slice_skill_drift.py` — NEW file with `test_in_repo_and_installed_slice_skill_md_are_content_equal` (mini-CAD-1)
- `tests/methodology/test_methodology_changelog.py` — added `test_v_0_25_0_mct_1_entry_present_in_repo_and_installed`; REPLACED `test_plugin_yaml_version_matches_version_file_at_0_24_0` with `..._at_0_25_0` (PMI-1 versioned-gate supersession; 4th supersession event in series)
- `architecture/shippability.md` — added row 10 (slice-010 critical path; 8/8 PASS in <0.1s); updated row 9 to note slice-010 PMI-1 version-gate supersession
- `architecture/decisions/ADR-009-promote-voluntary-critic-on-cross-cutting-to-slice-default-heuristic.md` — created at /design-slice; refined post-Critic (B1, B3, B5, m1, m3 fixes applied)
- `architecture/slices/slice-010-promote-voluntary-critic-on-cross-cutting-to-slice-default-heuristic/` — all phase artifacts (mission-brief.md + design.md + critique.md + build-log.md + validation.md + this reflection.md + milestone.md)
- `architecture/slices/_index.md` — Active table updated through stages
- `architecture/lessons-learned.md` — appended slice-010 entry

No `components/` or `contracts/` or `schemas/` updates (thin vault; Standard mode). No ADRs superseded (ADR-009 added; no prior ADR affected).

No `architecture/risk-register.md` updates — slice-010 didn't retire or introduce risks at the register-entry level. R-1 + R-2 remain unchanged.
