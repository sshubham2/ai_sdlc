# Slice 010: promote-voluntary-critic-on-cross-cutting-to-slice-default-heuristic

**Mode**: Standard
**Estimated work**: 0.5 day (~30 min skill-prose update + tests + methodology-changelog entry + atomic version bump)
**Risk retired**: MEDIUM — promotes the strongest aggregated-lesson promotion candidate in the project's reflection record: voluntary Critic on cross-cutting tooling slices has paid off N=9/9 across slices 1-9 in this project's reflection record (every voluntary Critic invocation on a cross-cutting tooling slice produced VALIDATED findings post-build with zero FALSE-ALARMs; cumulative-post-build framing is the verifiable interpretation), per slice-008 + slice-009 reflections' explicit promotion-at-slice-010+ recommendation. Without this promotion, every future cross-cutting tooling slice (modifying skill prose / agent prompts / in-house audit tooling / methodology rules) relies on per-slice voluntary opt-in to set `critic-required: true`; the N=9/9 evidence base makes that voluntary call effectively a discipline already, but it lacks the codification needed to make it automatic at /slice time. Slice-010 codifies it into the /slice skill's "Always mandatory Critic" list so subsequent cross-cutting tooling slices get the Critic gate without manual reasoning. The slice itself is in the recursive-self-application class (slice-009 M2 phenomenon): slice-010 modifies `skills/slice/SKILL.md` — exactly the in-house methodology surface its new bullet covers — so it qualifies for `critic-required: true` under both the OLD heuristic (voluntary Critic on cross-cutting tooling at N=9/9) AND the NEW heuristic it's encoding.
**Test-first**: true
**Walking-skeleton**: false
**Exploratory-charter**: false

## Intent

Add a new bullet to `skills/slice/SKILL.md` Step 4a "Always mandatory Critic" list covering **In-house methodology surfaces** — slices that modify skill prose (`skills/*/SKILL.md`), agent prompts (`agents/*.md`), custom build / lint / audit tooling under `tools/**/*.py`, or methodology rules in `methodology-changelog.md`. The bullet itself stays terse to match the existing 7 bullets' style; an adjacent evidence prose paragraph (placed AFTER the existing "When producing the mission brief..." paragraph that already follows the bullet list) cites the N=9/9 voluntary-Critic-catch-payoff evidence base across slices 1-9 with TWO+ concrete cross-slice example anchors. Codifies the slice-008 + slice-009 reflections' explicit promotion-at-slice-010+ recommendation. Authored as new methodology rule **MCT-1** ("Mandatory Critic Trigger for in-house methodology surfaces"; -T- distinguishes /slice-time trigger semantics from audit-enforced gate semantics of siblings BC-1 / PMI-1 / CAD-1 / TF-1 — see ADR-009 Option A rename per Critic B5) in `methodology-changelog.md` v0.25.0, with the same TWO-surface (in-repo + installed) + N-substring + N-surface schema-pin discipline established at slice-008 + slice-009 (per BC-1 v1.2 / CCC-1 v1.1 versioning convention). v0.25.0 entry carries a Limitations note explicitly acknowledging MCT-1 is /slice-time heuristic prose, NOT an audit-enforced gate (no automated audit verifies future slices modifying in-house surfaces set `critic-required: true`; v2 candidate at slice-N+ if drift surfaces). PMI-1 atomic version bump 0.24.0 → 0.25.0 with versioned-gate supersession (N=3 supersession event post-completion: slice-007 introduced `_at_0_22_0` → slice-008 first-superseded → slice-009 second-superseded → slice-010 third-superseded).

## Acceptance criteria

1. After this slice ships, `skills/slice/SKILL.md` Step 4a "Always mandatory Critic" section contains BOTH (a) a new terse bullet inserted between the existing `Security-sensitive paths` bullet and the existing `Heavy mode (always)` bullet — bullet body contains the canonical literal `In-house methodology surfaces` (capitalized bullet-title form, honoring sentence-start convention without tripping the slice-009 DEVIATION-1 lowercase-at-sentence-start trap) AND at least one of the file-class anchor substrings `skills/*/SKILL.md`, `agents/*.md`, `tools/**/*.py`, `methodology-changelog.md`; AND (b) an adjacent evidence prose paragraph placed AFTER the existing "When producing the mission brief..." paragraph that follows the bullet list — evidence paragraph body contains the canonical literals `N=9/9` AND `voluntary Critic` (case-sensitive lowercase-v). All four canonical-literal pins are case-sensitive substring assertions; the bullet itself stays terse to match the existing 7 bullets' style (per Critic M1 fix); the evidence prose carries the longer justification clause AND can be refined without bullet-style drift.

2. The evidence prose paragraph carries TWO+ concrete cross-slice example anchors grounded in the project's reflection record: at least two of the canonical substrings `slice-006`, `slice-007`, `slice-008`, `slice-009` appear in the evidence paragraph's text scoped to the Step 4a section bounds (case-sensitive). Each example may optionally carry a one-phrase Critic-catch sub-class anchor (e.g. `INST-1 inventory drift`, `install-time rename`, `negative-anchor uniformity`, `recursive self-application`). Per Critic M3 ACCEPTED-PENDING: at /build-slice add a sibling row pinning ≥2 of those 4 sub-class anchors so the descriptive text doesn't drift unpinned (TF-1 plan grows 7 → 8 rows at /build-slice).

3. After this slice ships, `~/.claude/skills/slice/SKILL.md` is byte-equal to in-repo `skills/slice/SKILL.md` (forward-sync atomicity verified by sha256 capture in `build-log.md` Phase 4; per slice-006..009 N=5 stable bidirectional sha256 forensic-capture pattern). A scope-narrow per-file pytest (`tests/methodology/test_slice_skill_drift.py::test_in_repo_and_installed_slice_skill_md_are_content_equal`, mini-CAD-1 shape mirroring slice-007 CAD-1 row; does NOT generalize INST-2) backs the forensic evidence. Note (per slice-009 mission-brief AC #3 precedent): test starts at TF-1 status PASSING (file pair byte-equal at slice start per Phase 0 capture); transitions PASSING → WRITTEN-FAILING at Phase 2 (in-repo edited; installed not yet synced) → PASSING at Phase 2c (post-forward-sync). Mid-slice smoke gate at Phase 2b is the WRITTEN-FAILING gate observation moment.

4. After this slice ships, `methodology-changelog.md` (BOTH in-repo and installed at `~/.claude/methodology-changelog.md`) contains a new `## v0.25.0 — <YYYY-MM-DD>` entry naming new methodology rule **MCT-1** ("Mandatory Critic Trigger for in-house methodology surfaces"). The bidirectional changelog-pin test (`test_v_0_25_0_mct_1_entry_present_in_repo_and_installed`) asserts the substantive canonical phrase `In-house methodology surfaces` is present in BOTH copies — per slice-008 M2 / slice-009 M3 N-surface schema-pin discipline (ONE canonical phrase pinned across N≥3 surfaces: skills/slice/SKILL.md + in-repo methodology-changelog.md + installed methodology-changelog.md). The entry carries an explicit Limitations note (mirrors v0.24.0 prose) acknowledging MCT-1 is /slice-time prose heuristic only, NOT an audit-enforced gate (per Critic B5; no automated audit verifies future slices modifying in-house surfaces actually set `critic-required: true`; v2 candidate at slice-N+ if drift surfaces).

5. PMI-1 invariant atomic post-build: `VERSION` (in-repo) + `~/.claude/ai-sdlc-VERSION` (installed) + `plugin.yaml.version` all equal `0.25.0`. Verified via `python -m tools.plugin_manifest_audit --root .` exit 0 AND `test_plugin_yaml_version_matches_version_file_at_0_25_0` PASSING. PMI-1 versioned-gate supersession: slice-009's `_at_0_24_0` REPLACED (not added alongside) by `_at_0_25_0` per N=2-stable supersession pattern (slice-007 introduced → slice-008 first-superseded → slice-009 second-superseded; slice-010 ratchets to N=3 supersession events on completion). No two version-gates coexist.

## Test-first plan

(per **TF-1**, `methodology-changelog.md` v0.13.0)

Each AC maps to one or more failing tests written BEFORE implementation. Statuses progress PENDING -> WRITTEN-FAILING -> PASSING through the slice lifecycle. `/build-slice` Step 6 (pre-finish) runs `tools/test_first_audit.py --strict-pre-finish` and refuses if any row is non-PASSING.

| AC | Test type | Test path | Test function | Status |
|----|-----------|-----------|---------------|--------|
| 1 | unit | tests/methodology/test_slice_skill.py | test_slice_step4a_mandatory_critic_section_contains_in_house_methodology_surfaces_bullet | PASSING |
| 1 | unit | tests/methodology/test_slice_skill.py | test_slice_step4a_in_house_methodology_bullet_location_between_security_paths_and_heavy_mode | PASSING |
| 1 | unit | tests/methodology/test_slice_skill.py | test_slice_step4a_evidence_paragraph_cites_n_9_and_voluntary_critic | PASSING |
| 2 | unit | tests/methodology/test_slice_skill.py | test_slice_step4a_evidence_paragraph_cites_at_least_two_cross_cutting_tooling_slices | PASSING |
| 2 | unit | tests/methodology/test_slice_skill.py | test_slice_step4a_evidence_paragraph_cites_at_least_two_sub_class_anchors | PASSING |
| 3 | integration | tests/methodology/test_slice_skill_drift.py | test_in_repo_and_installed_slice_skill_md_are_content_equal | PASSING |
| 4 | unit | tests/methodology/test_methodology_changelog.py | test_v_0_25_0_mct_1_entry_present_in_repo_and_installed | PASSING |
| 5 | unit | tests/methodology/test_methodology_changelog.py | test_plugin_yaml_version_matches_version_file_at_0_25_0 | PASSING |

**Notes**:

- TF-1 PENDING -> WRITTEN-FAILING transitions MUST be genuine per slice-003..009 lesson (N=6 stable). Empirically verified at design time via grep on `skills/slice/SKILL.md` for each canonical literal — confirmed ZERO pre-existing matches for `In-house methodology surfaces` / `N=9/9` / `voluntary Critic` / `cross-cutting tooling` (so PENDING → WRITTEN-FAILING transitions for AC #1 + AC #2 will be authentic). Pin specific failure signals:
  - AC #1 row 1 fails pre-fix with `AssertionError: 'In-house methodology surfaces' not in SLICE_SKILL` (canonical literal not yet in Step 4a bullet list).
  - AC #1 row 2 (location pin) per slice-009 Critic M1 + DEVIATION-2 lessons: scoped section search using UNIQUE start-of-section anchor `Always mandatory Critic` and UNIQUE end-of-section anchor `### Step 5:` (the next H3 header — wider scope than `Heavy mode (always)` so the evidence prose paragraph BELOW the bullet list also falls within scope per Critic M1 split). Anchor uniqueness empirically verified at design time via grep — both substrings appear exactly once in `skills/slice/SKILL.md`. The location-pin test uses `text.find("### Step 5:", text.find("Always mandatory Critic"))` to scope the search; this avoids slice-009 DEVIATION-2 `.find()`-collision class. AC #1 row 2 specifically asserts the new bullet's canonical phrase falls BETWEEN the `Security-sensitive paths` bullet and the `Heavy mode (always)` bullet (so the bullet's position is precisely pinned within the bullet list).
  - AC #1 row 3 fails pre-fix with `AssertionError: 'N=9/9' not in SLICE_SKILL` AND `'voluntary Critic' not in SLICE_SKILL` (case-sensitive — slice-009 DEVIATION-1 mitigation: canonical literal is lowercase `voluntary Critic`; the test asserts the exact case-sensitive substring; the prose MUST be structured so this substring appears as written, not capitalized at sentence start). Both pins target the evidence prose paragraph (per Critic M1 split) within the Step 4a section bounds.
  - AC #2 fails pre-fix with `AssertionError: insufficient cross-slice anchors — need ≥2 of {slice-006, slice-007, slice-008, slice-009} in the evidence prose paragraph` (none present pre-fix).
  - AC #3 starts at TF-1 status PASSING (file pair byte-equal at slice start per Phase 0 capture; mirrors slice-009 mission-brief row 3 precedent). The genuine WRITTEN-FAILING moment is Phase 2 (in-repo edited; installed not yet forward-synced) observed at the Phase 2b mid-slice smoke gate run with `RuntimeError: sha256 mismatch`. Transitions through PASSING (Phase 1b) → WRITTEN-FAILING (Phase 2) → PASSING (Phase 2c post-forward-sync); the WRITTEN-FAILING transition is the slice's actual implementation work (the in-repo edit), per slice-007 CAD-1 row precedent.
  - AC #4 fails pre-fix because the v0.25.0 entry does not exist in either changelog copy.
  - AC #5 fails pre-fix because `plugin.yaml.version` and `VERSION` are still `0.24.0` (slice-009's bumped state).
- Per slice-009 DEVIATION-1 (case-sensitivity in canonical-literal pin sub-class at sentence-start with markdown bold): the canonical literal `In-house methodology surfaces` is capitalized I to honor bullet-start sentence convention without forcing case manipulation. The canonical literal `voluntary Critic` is lowercase-v in body prose; the test enforces this literal case; the bullet must be structured so this phrase appears mid-sentence (e.g., "**In-house methodology surfaces** — N=9/9 voluntary Critic catch payoff across slices 1-9…") rather than at a sentence start that would force capitalization to `Voluntary Critic`. Mitigation pre-empts slice-009 DEVIATION-1 recurrence.
- Per slice-005 + slice-009 algorithm-path-conformance discipline generalized to substring-search test impl (slice-009 DEVIATION-2 sub-class): the location-pin test uses scoped `.find()` with start_idx from a prior unique anchor (`Always mandatory Critic`) — avoids first-occurrence-wins collision class. Anchor uniqueness empirically verified pre-AC-lock via grep.
- Per slice-008 M2 N-substring schema-pin discipline + slice-009 M3 N-surface schema-pin discipline: ACs #1 + #4 jointly pin the same substantive canonical phrase `In-house methodology surfaces` across N≥3 surfaces (skills/slice/SKILL.md + in-repo methodology-changelog.md + installed methodology-changelog.md). Same canonical phrase, multiple surfaces — drift becomes impossible without breaking ≥2 tests simultaneously.
- Per slice-007 + slice-008 + slice-009 PMI-1 versioned-gate supersession pattern: slice-007 introduced `_at_0_22_0`; slice-008 first-superseded → `_at_0_23_0` (N=1 supersession event); slice-009 second-superseded → `_at_0_24_0` (N=2 supersession events); slice-010 third-superseded → `_at_0_25_0` (ratchets to N=3 supersession events on completion). Delete old `_at_0_24_0` test row; add new `_at_0_25_0`. No two version-gates coexist.
- TF-1 plan starts at 7 rows. May grow at /design-slice (slice-008 grew 5→7→9 across mission-brief → design → post-Critic; slice-009 grew 5→6 across the same).

## Verification plan

| # | Criterion | How we verify |
|---|-----------|---------------|
| 1 | New terse bullet + adjacent evidence prose paragraph present in `skills/slice/SKILL.md` Step 4a; 4 canonical substrings pinned across bullet + evidence paragraph (`In-house methodology surfaces` in bullet, file-class anchor in bullet, `N=9/9` in evidence paragraph, `voluntary Critic` case-sensitive in evidence paragraph) — location-pinned within the Step 4a section bounds (`Always mandatory Critic` start → `### Step 5:` end); bullet specifically pinned BETWEEN `Security-sensitive paths` and `Heavy mode (always)` bullets | `pytest tests/methodology/test_slice_skill.py::test_slice_step4a_mandatory_critic_section_contains_in_house_methodology_surfaces_bullet` PASS; `test_slice_step4a_in_house_methodology_bullet_location_between_security_paths_and_heavy_mode` PASS (M1 location-pin per slice-009 lesson with widened section bounds per Critic M1 split); `test_slice_step4a_evidence_paragraph_cites_n_9_and_voluntary_critic` PASS |
| 2 | Two+ concrete cross-slice example anchors present in the evidence prose paragraph | `pytest tests/methodology/test_slice_skill.py::test_slice_step4a_evidence_paragraph_cites_at_least_two_cross_cutting_tooling_slices` PASS; asserts canonical substrings of `slice-006`, `slice-007`, `slice-008`, `slice-009` with count ≥ 2 in evidence paragraph scoped to Step 4a section. Per Critic M3 ACCEPTED-PENDING: at /build-slice add sibling row pinning ≥2 of 4 sub-class anchors (`INST-1 inventory drift`, `install-time rename`, `negative-anchor uniformity`, `recursive self-application`). |
| 3 | `~/.claude/skills/slice/SKILL.md` byte-equal to in-repo post-forward-sync | `pytest tests/methodology/test_slice_skill_drift.py::test_in_repo_and_installed_slice_skill_md_are_content_equal` PASS at slice end (after Phase 2c forward-sync; transitions PASSING → WRITTEN-FAILING (Phase 2 in-repo edit, observed at Phase 2b mid-slice smoke) → PASSING (Phase 2c). Scope-narrow per-file equality assertion; does NOT generalize INST-2. Build-log Phase 4 captures sha256 forensic evidence per N=5 stable bidirectional-capture pattern. |
| 4 | v0.25.0 entry pinned bidirectionally with substantive canonical phrase + MCT-1 rule reference + Limitations note acknowledging heuristic-not-audit semantics | `pytest tests/methodology/test_methodology_changelog.py::test_v_0_25_0_mct_1_entry_present_in_repo_and_installed` PASS; asserts in-repo `methodology-changelog.md` AND `~/.claude/methodology-changelog.md` both contain `## v0.25.0 —`, `MCT-1` rule reference, AND substantive canonical phrase `In-house methodology surfaces` |
| 5 | PMI-1 atomic at 0.25.0; versioned-gate supersession clean | `python -m tools.plugin_manifest_audit --root .` exits 0; `pytest tests/methodology/test_methodology_changelog.py::test_plugin_yaml_version_matches_version_file_at_0_25_0` PASS; slice-009's `_at_0_24_0` test row removed in the same commit (no two version-gates coexist per PMI-1 supersession pattern N=2 stable) |

## Must-not-defer

- [ ] TF-1 PENDING -> WRITTEN-FAILING genuine transitions per slice-003..009 N=6 stable lesson. Pin specific failure signals (substring absent, location AssertionError with `.find()` collision pre-empted via scoped search, sha256 mismatch, version mismatch). Empirical pre-design grep confirmed zero pre-existing matches; PENDING transition will be authentic.
- [ ] Case-sensitivity discipline per slice-009 DEVIATION-1 lesson: canonical literal `In-house methodology surfaces` is capitalized (bullet-start form); canonical literal `voluntary Critic` is lowercase-v (body-prose form). The bullet structure MUST keep the lowercase-v phrase mid-sentence (not at any sentence start) to honor markdown convention without forcing case manipulation. Slice-010's prose MUST NOT trigger the DEVIATION-1 trap on its own canonical pins.
- [ ] Location-pin anchor uniqueness per slice-005 + slice-009 algorithm-path-conformance discipline + DEVIATION-2 sub-class: the location-pin test uses `Always mandatory Critic` (L165) as start-of-section anchor + `Heavy mode (always)` (L172) as end-of-section anchor; both empirically verified UNIQUE in `skills/slice/SKILL.md` at design time. Test impl uses scoped `text.find("Heavy mode (always)", start_idx_after_Always_mandatory_Critic)` to pre-empt `.find()`-collision class. Slice-010 MUST NOT introduce the DEVIATION-2 trap on its own location pin.
- [ ] TWO-surface schema-pin discipline atomically: changes to `skills/slice/SKILL.md` AND `~/.claude/skills/slice/SKILL.md` ship in the same slice. Forward-sync verified by per-file mini-CAD-1 pytest (AC #3) + build-log Phase 0 + Phase 4 sha256 forensic capture per N=5 stable bidirectional pattern.
- [ ] N-substring + N-surface schema-pin discipline applied per slice-008 M2 + slice-009 M3 lessons: substantive canonical phrase `In-house methodology surfaces` pinned across 3 surfaces (skills/slice/SKILL.md + in-repo methodology-changelog.md + installed methodology-changelog.md). Each surface's pin test is independent — drift requires breaking ≥2 tests simultaneously.
- [ ] New rule entry appended to `methodology-changelog.md` (in-repo) AND `~/.claude/methodology-changelog.md` atomically. Per slice-007 PMI-1 escape-closure lesson: every claim made in `## Vault updates made` MUST be verifiable by audit (`tools.plugin_manifest_audit` + the new bidirectional changelog-pin test at AC #4).
- [ ] PMI-1 audit clean post-build (`python -m tools.plugin_manifest_audit --root .` exits 0). Atomic version bump: `VERSION` + `ai-sdlc-VERSION` + `plugin.yaml.version` all to `0.25.0`. Slice-009's `_at_0_24_0` PMI-1 versioned-gate test REPLACED (not additive) by `_at_0_25_0` per N=2-stable supersession pattern from slice-007..009 (slice-010 creates N=3 supersession events post-completion).
- [ ] Bidirectional sha256 forensic capture in `build-log.md` Phase 0 (pre-edit) + Phase 4 (post-forward-sync) for `~/.claude/skills/slice/SKILL.md` + `~/.claude/methodology-changelog.md` + `~/.claude/ai-sdlc-VERSION` per slice-006..009 N=5 stable lesson. Capture in-repo + installed sha256 BEFORE and AFTER the edits. Slice's git diff alone is insufficient evidence for out-of-repo edits.
- [ ] No regression on the broader methodology suite (`pytest tests/methodology/ -q` clean). The slice's surface is narrow (SKILL.md prose + changelog entry + version bump + 2 new test files); broader breakage indicates unintended scope creep.
- [ ] Shippability catalog row 10 added at `architecture/shippability.md` naming this slice's critical path (Step 4a in-house-methodology-surfaces bullet + per-file mini-CAD-1 byte-equality on skills/slice/SKILL.md + v0.25.0 entry + PMI-1 0.25.0 invariant). Updates row 9 (slice-009) to note slice-010 PMI-1 version-gate supersession (mirrors slice-008's row 7 update + slice-009's row 8 update for the prior version-gate supersession).
- [ ] Recursive self-application discipline per slice-009 M2 lesson: slice-010 IS the cross-cutting tooling slice modifying an in-house methodology surface (`skills/slice/SKILL.md`) — exactly the file class its new bullet governs. The Critic at /critique SHOULD be expected to find rule-class violations in slice-010's own draft prose (recursive self-application phenomenon, N=1 at slice-009). `critic-required: true` is set both under the OLD heuristic (voluntary Critic on cross-cutting tooling at N=9/9) AND the NEW heuristic this slice encodes. The duplication is deliberate; both rationales recorded in the milestone.
- [ ] Self-application BC-1 audit per slice-008 + slice-009 N=7 stable "validate using your own ship" pattern: post-build BC-1 audit on THIS slice's own mission-brief.md + design.md returns `applicable: []`. Precise framing per Critic B4 fix: (a) BC-PROJ-1 + BC-PROJ-2 will NOT positively-fire — empirical word-boundary grep at design time confirmed ZERO matches for their positive trigger anchors (`subagent`/`fan-out` for BC-PROJ-1; `fence`/`code-block`/`llm` for BC-PROJ-2) in slice-010's prose — so the negative-anchor filter is never reached for those two rules; (b) BC-GLOBAL-1 WILL positively-fire via its `Applies to: **` always-true glob path under `--changed-files`, and there BC-1 v1.2's negative-anchor mechanism (slice-008) genuinely silences it — slice-010's prose contains methodology-vocabulary anchors `aggregated lessons` (2 + 2 in mission + design), `forward-sync` (9 each), `back-sync` (1 + 2), `Dim 9` (3 each), `vocabulary` (2 each), `meta-discussion` (1 in design), `defer-with-rationale` (1 in mission) per design-time empirical count — at least one matches per file so negative-anchor filter silences. The expected `applicable: []` result comes from no-positive-firing on BC-PROJ-1/2 PLUS negative-anchor-silencing on BC-GLOBAL-1, not uniform negative-silencing on all three. Self-application result captured in `build-log.md` Phase 4 (per slice-009 N=7 stable pattern).
- [ ] Empirical-verification-at-design-time discipline per slice-009 N=8 stable lesson: pre-AC-lock grep confirmed canonical literals absent + location-pin anchors unique. Slice-010 ratchets pattern to N=9 stable.

## Out of scope

- **Generalizing INST-2 (general content-equality audit across all installed files)** — slice-007 carryover; still N=1 actual-drift evidence (only `agents/critique.md` drift confirmed). Slice-010 adds a per-file mini-CAD-1 pytest for `skills/slice/SKILL.md` (AC #3) but does NOT build a generalized audit tool. Per slice-009 reflection: defer INST-2 to N=2 actual-drift evidence.
- **Adding mandatory-Critic bullets for OTHER cross-cutting trigger classes** — the slice promotes only the "in-house methodology surfaces" trigger that has N=9/9 evidence. Other candidates (e.g., "modifies pre-commit hooks", "modifies shippability catalog convention") have insufficient evidence base and are out of scope; promote at N≥3 each.
- **Refining the existing Step 4a bullets** — the existing 7 bullets (Auth / API contracts / Data model / Multi-device sync / External integrations / Security-sensitive paths / Heavy mode) remain unchanged. Slice-010 is additive: appends 1 bullet.
- **`fix-r1-cwd-mismatch-for-diagnose-subagents`** — R-1 HIGH-band open; needs `/risk-spike` first to disambiguate cwd vs parallel-spawn-cascade hypotheses per slice-002 Critic M1 + slice-009 reflection.
- **`refactor-pmi-1-gate-to-version-agnostic-shape`** — N=3 supersession events post-slice-010; still under N≥4 friction threshold. Defer indefinitely.
- **Promoting `case-sensitivity-canonical-literal-pin-discipline` to BC-1 / Dim 9 sub-clause** — slice-009 carryover at N=1 (DEVIATION-1). Promote at N=2 if recurs at slice-011+. Slice-010 pre-empts the trap on its own canonical pins via design-time prose-structure discipline, NOT via Critic-prompt refinement.
- **Promoting `substring-search-location-pin-anchor-uniqueness-discipline` to Dim 9 sub-class refinement** — slice-009 carryover at N=1 (DEVIATION-2; structurally same family as slice-005 algorithm-path-conformance). Promote at N=2 if recurs. Slice-010 pre-empts the trap on its own location pin via design-time anchor-uniqueness verification + scoped-`.find()` test impl.
- **Promoting `recursive-self-application-discipline` to /critique skill prose** — slice-009 carryover at N=1 (M2). Slice-010 IS a recursive-self-application instance (N=2 candidate post-completion) but doesn't promote the discipline yet — the M2 pattern is observational (Critic should EXPECT to find rule-class violations in own-rule-encoding prose); making it actionable requires more evidence. Promote at N=3 if a third recursive-self-application instance surfaces at slice-011+.
- **Wiegers AC-trace sub-class promotion** — slice-008 + slice-009 carryover at N=1. Defer to N=2.
- **BC-PROJ-2 negative-anchor migration** — slice-008 + slice-009 carryover at N=1. Defer to N=2.
- **`/critic-calibrate` next-run recalibration** — slice-009 reflection recommends slice-012-015 boundary; cross-cutting cumulative miss count (8 through slice-009) significantly over the ≤2 target. Slice-010's promotion of voluntary-Critic-to-mandatory is one of two candidate responses; the other is recalibrating the quantitative target downward. /critic-calibrate at slice-012-015 to settle.
- **Em-dash → cp1252 / prose-pin substring uniqueness** — slice-007 carryover at N=1; slice-009 didn't recur. Watch.

## Dependencies

- Prior slices: [[slice-006-update-critic-with-cross-cutting-conformance-dimension]] — CCC-1 baseline (Dim 9 introduced); [[slice-007-add-critique-agent-content-equality-audit]] — CAD-1 byte-equality audit pattern (slice-010 applies per-file mini-CAD-1 shape to `skills/slice/SKILL.md`); [[slice-008-refine-bc-1-anchors-with-negative-context]] — BC-1 v1.2 negative-anchor mechanism + N-substring schema-pin + PMI-1 versioned-gate supersession; [[slice-009-refine-dim-9-with-design-md-tables-sub-clause]] — N-surface schema-pin + algorithm-path-conformance generalized + recursive-self-application phenomenon + N=9/9 voluntary-Critic-payoff evidence base + DEVIATION-1 case-sensitivity + DEVIATION-2 `.find()`-collision lessons (pre-empted at slice-010 design time).
- Vault refs: [[skills/slice/SKILL.md]] Step 4a "Always mandatory Critic" bullet list lines 165-172; [[methodology-changelog.md]] v0.24.0 (current; slice-009) + upcoming v0.25.0 (slice-010); [[architecture/critic-calibration-log.md]] 2026-05-10 calibration run + user-override entry + the slice-010 promotion act (record at /reflect); [[architecture/lessons-learned.md]] voluntary-Critic-on-cross-cutting-tooling pattern aggregate; [[architecture/slices/_index.md]] aggregated lessons section (the N=9/9 lesson is line 25; slice-010 fulfills the explicit slice-010+ recommendation).
- Risk register: none retired (R-1 + R-2 are /diagnose-related; slice-010 doesn't touch them).
- Tooling: `skills/slice/SKILL.md` (Step 4a bullet append + evidence prose paragraph append below the existing "When producing..." paragraph per Critic M1 split); `~/.claude/skills/slice/SKILL.md` (Phase 2 forward-sync target); `methodology-changelog.md` + `~/.claude/methodology-changelog.md` (v0.25.0 entry with MCT-1 rule + Limitations note per Critic B5); `VERSION` + `~/.claude/ai-sdlc-VERSION` (atomic bump to 0.25.0); `plugin.yaml` (version field bump 0.24.0 → 0.25.0); `tests/methodology/test_slice_skill.py` (NEW file — 4 prose-pin tests for AC #1 + AC #2; M3 sub-class anchor row added at /build-slice grows file to 5 tests); `tests/methodology/test_slice_skill_drift.py` (NEW file — 1 byte-equality test for AC #3); `tests/methodology/test_methodology_changelog.py` (replace `_at_0_24_0` with `_at_0_25_0`; add `test_v_0_25_0_mct_1_entry_present_in_repo_and_installed` bidirectional pin per AC #4); `architecture/shippability.md` (add row 10). No code-tooling logic changes (no audit module modified, no skill executable behavior changed beyond the prose-level heuristic).

## Mid-slice smoke gate

At ~50% of build (after `skills/slice/SKILL.md` is edited in-repo and the 4 new prose-pin tests + 1 mini-CAD-1 test are added, but BEFORE Phase 2 forward-sync to `~/.claude/skills/slice/SKILL.md`):

```
<HOME>/.claude/.venv/Scripts/python.exe -m pytest tests/methodology/test_slice_skill.py tests/methodology/test_slice_skill_drift.py -q
```

Expected:
- All 4 new prose-pin tests in `test_slice_skill.py` PASS against in-repo (because in-repo is edited).
- `test_slice_skill_drift.py::test_in_repo_and_installed_slice_skill_md_are_content_equal` FAILS with `RuntimeError: sha256 mismatch` (in-repo edited; installed not yet forward-synced) — this is EXPECTED at this gate, mirrors slice-007 + slice-009 CAD-1 drift signal pattern. The mini-CAD-1 test must report clean PASS only at pre-finish gate after Phase 2 forward-sync.

If any of the 4 prose-pin tests fails: STOP, diagnose. Likely root causes:
- Canonical-literal substring mismatch — re-read the canonical phrase in mission-brief AC #1 and align prose exactly.
- DEVIATION-1 lowercase-at-sentence-start trap: the lowercase `voluntary Critic` phrase placed at a bullet start or sentence start was auto-capitalized; restructure the prose to keep this phrase mid-sentence.
- DEVIATION-2 `.find()`-collision in location-pin test: the start-anchor or end-anchor substring matched an unintended occurrence; verify anchor uniqueness via grep; use scoped `.find(substring, start_idx)`.
- Bullet location: the new bullet was placed outside the "Always mandatory Critic" section (e.g. in a different bulletlist or in narrative text); move it into the correct section before the "Heavy mode (always)" closer.

## Pre-finish gate

- [ ] All 5 acceptance criteria PASS with evidence in `validation.md`
- [ ] Must-not-defer list fully addressed (TF-1 genuineness, DEVIATION-1 case-sensitivity, DEVIATION-2 anchor-uniqueness, TWO-surface pin, N-substring + N-surface schema-pin, v0.25.0 entry bidirectional, PMI-1 0.25.0 clean, sha256 forensic capture, no methodology-suite regression, shippability row 10, recursive-self-application discipline applied, self-application BC-1 clean, empirical-verification-at-design-time N=9)
- [ ] `/drift-check` passes
- [ ] Mid-slice smoke still passes (no regression on the 4 prose-pin tests; mini-CAD-1 transitions from FAIL to PASS post-forward-sync)
- [ ] No new TODOs / FIXMEs / debug prints
- [ ] PMI-1 audit clean: `python -m tools.plugin_manifest_audit --root .` exits 0
- [ ] Per-file mini-CAD-1 clean at slice end: `pytest tests/methodology/test_slice_skill_drift.py::test_in_repo_and_installed_slice_skill_md_are_content_equal` PASS
- [ ] Methodology suite clean: `<HOME>/.claude/.venv/Scripts/python.exe -m pytest tests/methodology/ -q` exits 0 (no regression on the broader suite)
- [ ] Shippability catalog clean: row 10 added; row 9's slice-009 reference updated to note slice-010 PMI-1 versioned-gate supersession (mirrors slice-008's row 7 update for slice-007's v0.22.0 gate + slice-009's row 8 update for slice-008's v0.23.0 gate)
- [ ] Self-application check: post-slice-010 BC-1 audit on THIS slice's own mission-brief.md + design.md does NOT fire BC-PROJ-1 or BC-GLOBAL-1. Negative-anchor mechanism (slice-008) silences the methodology-vocabulary anchors cleanly. Result captured in `build-log.md` Phase 4.
