# Reflection: Slice 008 refine-bc-1-anchors-with-negative-context

**Date**: 2026-05-11
**Shipped**: YES-WITH-DEFERRALS

## Validated

- **BC-1 v1.2 negative-context anchor mechanism silences the N=3 false-positive class** — empirically validated at /validate-slice. slice-005 archive backtest with build-time `--changed-files`: BC-PROJ-1 + BC-GLOBAL-1 silenced (BC-PROJ-2 still fires, acceptable per Critic M1 disposition); slice-006 archive: applicable=[] (all 3 silenced); slice-007 archive: applicable=[] (all 3 silenced). The mechanism applies UNIFORMLY across all three positive-applicability paths (`always: true` short-circuit, glob, keyword) — composes correctly per the design.md Algorithm-path-conformance table.
- **Backward-compat covenant per ADR-004 preserved** — slice-001 archive backtest still fires BC-PROJ-1 + BC-PROJ-2 + BC-GLOBAL-1 at AC #4. Slice-001 mission-brief + design contain ZERO negative-anchor tokens — the discriminating set is robust. Critic m1's over-suppression risk concern is empirically refuted.
- **TWO-surface × TWO-substring schema-pin discipline (per Critic M2 generalization)** — both `Negative anchors` (field name) AND `final filter` (semantic phrase) literal substrings present in BOTH `architecture/build-checks.md` AND `~/.claude/build-checks.md`. Eight prose-pin assertion sites total (2 substrings × 2 files × 2 rules in design.md / methodology-changelog.md cross-refs).
- **Bidirectional sha256 forensic discipline N=3 stable** — slice-006 first occurrence + slice-007 second + slice-008 third. methodology-changelog.md sha256 `0A7AA6CA04E372FB` byte-equal in-repo↔installed; VERSION + ai-sdlc-VERSION byte-equal at `D1994E4942B06EC3`; build-checks.md intentionally different (project vs global rule sets — same schema-prelude TWO-sentence structure + cross-project sentence in global file).
- **TF-1 PENDING → WRITTEN-FAILING → PASSING genuineness for all 9 rows** — pre-fix failure signatures: 3× `AssertionError "BC-PROJ-1 in applicable"` (slice-005/006/007 backtests via kw + glob paths), 2× `AttributeError "negative_anchors"` (AC #4 + migration-tuple, pre-fix dataclass missing field), 2× `AssertionError schema-substring absent`, 1× `AssertionError "BC-PROJ-99 in applicable"` (always-true + negative-anchor fixture), 1× `AssertionError "0 violations"` (parse-violation channel). All non-coincidental. Per slice-005..007 N=4 → now N=5 stable.
- **Voluntary Critic on cross-cutting tooling slices is N=8/8 paid off, with 8 of 8 catching design-stage failures** — slice-008 contributed 3 design-stage catches (M1 BC-PROJ-2 unsupported AC; M2 schema-pin under-applied; M3 always-true coverage gap). All three would have manifested as build-time DEVIATIONs or worse without the /critique catch. Critic ROI on cross-cutting tooling slices climbed from "5 of 7 design-stage catches" at slice-007 to "8 of 8" at slice-008. **Methodology pattern is load-bearing AND empirically strengthening.**
- **All 6 Critic findings VALIDATED** at /validate-slice — 0 FALSE-ALARM, 0 OVERRIDE-MISJUDGED, 0 NOT-YET. **Third consecutive 100% Critic-accuracy slice** (slice-006: 11/11; slice-007: 8/8; slice-008: 6/6 — running 25/25 across slices 6-8).
- **PMI-1 invariant maintained atomically** — `VERSION` + `~/.claude/ai-sdlc-VERSION` + `plugin.yaml.version` all bumped 0.22.0 → 0.23.0 in one atomic operation per slice-007 PMI-1 closure pattern. PMI-1 audit clean (exit 0).
- **"Validate using your own ship" pattern N=6 stable** — slice-008 self-application: `python -m tools.build_checks_audit --slice architecture/slices/slice-008-... --project-checks ... --changed-files tools/build_checks_audit.py architecture/build-checks.md --no-carry-over` returns BC-PROJ-1 + BC-GLOBAL-1 in skipped (slice-008's own text contains `vocabulary, Dim 9, back-sync, forward-sync` → silenced by its own ship). BC-PROJ-2 still fires (acceptable per M1). The slice ships and immediately verifies its own mechanism against itself.
- **Shippability catalog regression-clean** — 8 rows / 69 tests / **69 PASS** in 2.84s (well under 2-min target). No regressions on slice-001..007 critical paths. Catalog grew 7 rows → 8 rows.

## Corrected

- **Slice-007's `test_plugin_yaml_version_matches_version_file_at_0_22_0` superseded by slice-008's `test_plugin_yaml_version_matches_version_file_at_0_23_0`** — slice-007 added a versioned PMI-1 gate test pinning plugin.yaml.version at 0.22.0; slice-008's atomic version bump to 0.23.0 makes the 0.22.0 gate stale (it would always fail post-bump). Replaced + added new 0.23.0 gate. `test_methodology_changelog.py`: 6 tests → 7 tests (net +1). **This is the PMI-1 versioned-gate test supersession pattern, now N=2 stable** (slice-007 implicitly superseded a pre-slice-007 gate; slice-008 explicitly supersedes slice-007's). Future version-bump slices follow this pattern.
- **`architecture/shippability.md` row 7 (slice-007) updated to drop the deleted `test_plugin_yaml_version_matches_version_file_at_0_22_0` reference** — row 7's command no longer cites the removed test; slice-007's other claims (CAD-1 audit, sha256 byte-equality, /critic-calibrate skill prose, methodology-changelog v0.22.0 entry, canonical-tools-match-plugin-yaml) still hold. Row 7's prose now notes the supersession ("slice-008 superseded the 0.22.0 PMI-1 version-gate test with the 0.23.0 gate at slice-008 row 8") for forensic traceability. Row 8 (slice-008) added with 11 critical-path tests.
- **Methodology-changelog version 0.22.0 → 0.23.0** — `methodology-changelog.md` (in-repo + installed) gained a new H2 entry `## v0.23.0 — 2026-05-10` documenting BC-1 v1.2. **NO ADRs superseded** — ADR-007 stands as accepted; ADR-004 (slice-005 BC-1 v1.1) is extended NOT superseded by ADR-007 (BC-1 v1.2 is additive; v1.1's Trigger anchors remain functional unchanged).
- **No design.md corrections at build time** — Critic M1/M2/M3 + m1/m2 caught what would have been design.md corrections at build time. design.md was locked at /critique time and reflects shipped reality. **Zero build-time DEVIATIONs** in slice-008 (vs slice-005's 2 + slice-006's 2 + slice-007's 2).

## Discovered

- **ZERO-build-deviation slice via strong /critique** (slice-008 — first in series 005-008 with this property; N=1 occurrence) — slice-005/006/007 each had 1-2 build-time DEVIATIONs caught at mid-slice smoke or at TF-1 strict-pre-finish. Slice-008 had 0. The 3 Critic majors (M1 BC-PROJ-2 unsupported AC; M2 schema-pin under-applied; M3 always-true coverage gap) pre-empted what would have been DEVIATIONs. **Generic methodology lesson at N=1**: strong /critique reduces build-time deviations. **Watch for recurrence at slice-009+ to confirm the pattern.** If pattern holds at N=2, promote: design-stage Critic ROI metric should track design-stage catches AS A PERCENTAGE of total findings, not just total slices.

- **PMI-1 versioned-gate test supersession pattern is N=2 stable** (slice-007 + slice-008) — every version-bump slice must replace `test_plugin_yaml_version_matches_version_file_at_0_NN_0` with a new `_0_MM_0` gate. The version-gated test name pattern is fragile to version bumps; the slice that bumps the version owns the supersession. **Generic methodology lesson at N=2.** Possible refinement: a single un-versioned gate test `test_plugin_yaml_version_matches_version_file_at_current_version` that reads the canonical version from VERSION file and asserts plugin.yaml matches. Defer to slice-009+ as a refactor candidate (`refactor-pmi-1-gate-to-version-agnostic-shape`). Not load-bearing yet.

- **Algorithm-path-conformance lesson is N=2 stable** (slice-005 BC-GLOBAL-1 always-true short-circuit + slice-008 negative-anchor cross-path uniformity) — when adding a precision mechanism to existing audit, ALL pre-existing branches must compose correctly with the new mechanism. Slice-008 documented this as a table in design.md (5 paths × pre/post). The table format is a generalizable methodology artifact. **Cross-cutting-conformance Dim 9 sub-class candidate (refined)**: "When adding a new branch to existing logic, trace through ALL pre-existing branches AND document composability as a path-table." **Promote at N=3 if recurs in slice-009+.**

- **Voluntary Critic on cross-cutting tooling slices: 8 of 8 design-stage catches at slice-008** — empirical evidence that the methodology pattern is not just N/N paid off but **N/N specifically catching design-stage failures** (not just generic Critic findings). The threshold for "design-stage catch" is: finding would have manifested as build-time DEVIATION or worse without the catch. Slice-008's 3 catches (M1/M2/M3) all meet this threshold. **/slice's default heuristic should set `critic-required: true` when slice scope includes "modifies in-house audit / agent prompt / methodology rule" — N=8/8 with 8/8 design-stage catches is the strongest empirical case yet.** Promote to skill prose at slice-009+ if N=9/9 with 9/9 design-stage catches.

- **Cross-cutting-conformance Dim 9 catch rate computation** — slice-008's 3 design-stage Critic catches were each Dim 9 sub-class hits:
  - M1: cross-cutting-conformance — design.md elements vs explicit AC framework (Wiegers AC-trace; new sub-class candidate at N=1).
  - M2: cross-cutting-conformance — TWO-surface × TWO-substring schema-pin discipline generalization (slice-005 Critic M3 precedent; pre-existing sub-class hardening).
  - M3: cross-cutting-conformance — algorithm-path-conformance (slice-005 lesson; pre-existing sub-class N=2 stable).
  All 3 caught at /critique. **Catch rate at slice-008: 3 of 3 = 100%** (vs slice-007 60% vs slice-006 25% vs slices 1-5 0%). **9-dim Critic empirical effectiveness on cross-cutting conformance findings is strict-improving with each slice.** Slice-006-15 effectiveness target (≤2 cross-cutting misses across the window) recomputed post-slice-008: running total **5 missed across slices 6-7** (per slice-007 reflection) **+ 0 missed at slice-008 = 5 missed across slices 6-8**. On track for ≤2-across-slices-6-15 target with margin (need ≤-3 more misses across 7 remaining slices, achievable if pattern holds).

- **Wiegers AC-trace sub-class (new at slice-008 Critic M1; N=1)** — every design element should trace to a driving AC AND every AC should trace to ≥1 design element. Slice-008's design originally migrated BC-PROJ-2 without a driving AC (no AC #1-3 gated BC-PROJ-2). Critic M1 caught this. **Cross-cutting-conformance Dim 9 sub-class candidate at N=1; watch for recurrence at slice-009+.** Promote to Dim 9 body at N=2.

## Deferred

- **BC-PROJ-2 migration** — per Critic M1 ACCEPTED-FIXED + TRI-1 user ratification; defer until N=2 surfaces (future slice modifying file under `skills/**/*.py` or `tools/**/*.py` AND citing methodology-vocabulary anchors). Currently N=1 (slice-005 only). Watch slice-009+.
- **`refactor-pmi-1-gate-to-version-agnostic-shape`** — NEW slice-008 discovery. ~30 min. Single un-versioned gate test reads VERSION file at runtime; eliminates per-version-bump test churn. Defer to slice-009+ if a version-bump slice happens; otherwise indefinite.
- **`refine-dim-9-with-design-md-tables-sub-clause`** — slice-007 carryover; N=2 met. ~30 min. Add to Dim 9 of `agents/critique.md` body: "design.md mechanical tables vs methodology canonical references / install-time renames / canonical-inventory inventories." Promoted by slice-007 reflection + reinforced by slice-008 M1 (AC-trace) + slice-008 M2 (TWO-surface schema-pin).
- **INST-2 generalization** (general content-equality across all installed files) — slice-007 carryover; still N=1 evidence (only `agents/critique.md` drifts confirmed); defer to N=2 (drift on `agents/critique-review.md` or `agents/critic-calibrate.md` or `~/.claude/build-checks.md`).
- **Em-dash `—` → cp1252 + prose-pin substring uniqueness** — slice-007 carryover; both N=1; still watching. Slice-008 emitted no em-dash or prose-pin substring issues (no recurrence). Continue tracking.
- **Slice-007 supersession formalism** — slice-007 archive could benefit from a `## Supersession` section linking to slice-008 (per SUP-1, `methodology-changelog.md` v0.19.0) noting the 0.22.0 PMI-1 gate test was superseded. **NOT load-bearing** — slice-007's other claims still hold; the test-name supersession is captured in this slice's "Corrected" section and shippability.md row 7's parenthetical. Defer to /supersede-slice if a future reader complains about staleness.
- **`fix-bc-1-archived-slice-heuristic`** (slice-005 carryover; pre-existing `audit_slice` `parent.parent` heuristic broken for archived paths) — workaround `--project-checks` explicit acceptable; ~30 min slice.
- **`add-csp-1-docstring-or-regex`** (slice-004 carryover) — sibling tooling-bug pattern; ~30-60 min slice.
- **R-1 deeper fix** (orchestrator pre-cd or parent-thread pre-compute for /diagnose) — HIGH-band open risk; documented-constraint workaround acceptable per slice-002.
- **R-2 programmatic test of cwd-warning runtime emission** — LOW-band open risk; defer until evidence of regression.
- **From slice-007 carryover (still open)**: `refine-bc-1-anchors-with-negative-context` — **SHIPPED IN THIS SLICE (slice-008) as BC-1 v1.2**.

## Critic calibration

Per **TRI-1**, scoring each finding from `critique.md` `## Triage` table against build + validate reality:

- **M1** (BC-PROJ-2 migration not gated by AC; N=1 below promotion threshold): **VALIDATED** — disposition ACCEPTED-FIXED at /critique; reality confirmed empirically at /validate-slice (BC-PROJ-2 still fires on slice-005 archive's `applicable=['BC-PROJ-2']` post-fix; AC #4 still passes on slice-001 with all 3 rules firing). Without M1 catch, the slice would have migrated BC-PROJ-2 without an AC; possible over-suppression of BC-PROJ-2 on non-methodology-vocabulary slices that happen to use one of the 9 negative-anchor tokens AND have changed-files matching `skills/**/*.py, tools/**/*.py` (low-probability but unspecified scenario). **Strong VALIDATED + design-stage save.** Critic was right; Wiegers AC-trace conformance preserved. **NEW Dim 9 sub-class hit: AC-trace conformance at N=1; watch for recurrence at slice-009+.**

- **M2** (schema-pin TWO-surface discipline under-pinned vs slice-005 precedent — only ONE prose substring per file): **VALIDATED** — disposition ACCEPTED-FIXED at /critique; reality confirmed at /validate-slice (both `Negative anchors` AND `final filter` substrings present in BOTH `architecture/build-checks.md` AND `~/.claude/build-checks.md`). Without M2 catch, the `final filter` semantic phrase would have been unprotected against doc-refactor drift — future doc rewrites could rename "Negative anchors are final filters" to "Negative anchors are exclusionary tokens" without breaking any test, losing the cross-path-uniformity framing. **Strong VALIDATED + design-stage save.** Critic was right; preventive. **Pre-existing Dim 9 sub-class strengthened**: TWO-surface schema-pin generalizes to N-substring (slice-007 reflection's N-surface lemma now formally generalizes to N-substring at slice-008).

- **M3** (no test exercises `applies_to == ("always",)` + negative-anchor interaction): **VALIDATED** — disposition ACCEPTED-FIXED at /critique; `test_always_true_rule_with_negative_anchor_match_is_skipped` added to TF-1; reality confirmed at /validate-slice (synthetic fixture with `Applies to: always: true` + `Negative anchors: foo` + slice text containing `foo` correctly returns rule in skipped not applicable). Without M3 catch, the always-true path's new "always-EXCEPT-when-negative-anchor-matches" semantic would have been untested. **Strong VALIDATED + algorithm-path save.** Critic was right; algorithm-path-conformance Dim 9 sub-class hardened to N=2 (slice-005 BC-GLOBAL-1 always-true short-circuit + slice-008 negative-anchor cross-path uniformity).

- **m1** (`Critic-MISSED` token in negative-anchor list is highly methodology-specific to be in the GLOBAL build-checks file): **VALIDATED** — disposition ACCEPTED-FIXED at /critique (option (a) — added cross-project-applicability sentence to `~/.claude/build-checks.md` schema-prelude); reality confirmed at /validate-slice (the sentence is present in the global file's prelude; adopters in other domains can re-curate per their own meta-vocabulary). Without m1 catch, the global file would have shipped AI-SDLC-internal tokens silently — a future cross-project adopter could either be surprised by under-suppression on their methodology slices OR over-suppression on their unrelated slices. Cosmetic; preventive. Critic was right.

- **m2** (ADR-007 "Reversibility: cheap" estimate may understate the test-deletion cost): **VALIDATED** — disposition ACCEPTED-FIXED at /critique; ADR-007 Reversibility section updated to "~30 minutes" with explicit step #6 enumerating 9 tests to delete. Cosmetic; preventive. Critic was right.

- **m3** (token `false positive` with space — anchor parser dedup-after-lowercase but still fragile to author typos): **VALIDATED** — disposition ACCEPTED-FIXED at /critique via pre-mitigation (existing `test_migrated_rules_have_expected_negative_anchors` row pins exact tuple equality on production rules; a typo `false-positive` instead of `false positive` would fail tuple-comparison loud). Reality confirmed at /validate-slice (canonical 9-token tuple matches across BC-PROJ-1 + BC-GLOBAL-1). Critic was right; pre-resolved by existing TF-1 row.

**All 6 Critic findings VALIDATED. 0 FALSE-ALARM. 0 OVERRIDE-MISJUDGED. 0 NOT-YET.** Third consecutive 100% Critic accuracy slice (slice-006: 11/11; slice-007: 8/8; **slice-008: 6/6**). Running total: **25/25 across slices 6-8.**

**Missed by Critic**: **NONE at slice-008 /critique time.** Zero reality surprises at /validate-slice; zero build-time DEVIATIONs at /build-slice; zero items surfaced during build/validate that the Critic didn't predict. **This is the cleanest Critic performance in the project so far.**

**Pattern (cumulative across slices 001-008)**:

Cross-cutting-conformance miss-class:
- slice-001..005 (8-dim Critic): **0% catch rate**, 8 sub-class hits, 8 misses
- slice-006 (8-dim Critic, post-CCC-1 surgical sub-bullets): **25% catch rate**, 4 sub-class hits, 1 caught
- slice-007 (9-dim Critic, CCC-1 v1): **60% catch rate**, 5 sub-class hits, 3 caught
- **slice-008 (9-dim Critic, CCC-1 v1): 100% catch rate, 3 sub-class hits, 3 caught (M1+M2+M3)**

The 9-dim Critic is **empirically strict-improving on cross-cutting conformance findings, monotonically by slice.** Slice-006-15 effectiveness target (≤2 cross-cutting misses) recomputed: 5 missed across slices 6-7 + 0 missed at slice-008 = **5 missed across slices 6-8**. Margin remains for the target across the slice-9-to-15 window.

**Voluntary Critic on cross-cutting tooling slices is now N=8/8 paid off, with 8 of 8 catching design-stage failures** (slice-003 m1 + slice-004 B1 + slice-005 B2 + slice-006 B1 + slice-007 B1+B2 + slice-008 M1+M2+M3). Methodology pattern is load-bearing. **Slice-008's 3-for-3 design-stage catch rate is the strongest single-slice Critic performance in the project's cross-cutting tooling domain.**

## Lessons for next slice

- **Voluntary Critic on cross-cutting tooling slices is N=8/8 paid off, with 8 of 8 catching design-stage failures.** /slice's default heuristic SHOULD be updated at slice-009+ to set `critic-required: true` when scope includes "modifies in-house audit / agent prompt / methodology rule" — the empirical case is strongest in the project. Promote to skill-prose update at next slice that touches /slice.
- **ZERO build-deviation slice via strong /critique** (slice-008 N=1) — first in slice-005..008 series. Watch for recurrence at slice-009+; if pattern holds at N=2, design-stage Critic ROI metric could promote to a tracked methodology number (% of cross-cutting findings caught at /critique vs at build).
- **PMI-1 versioned-gate test supersession pattern N=2 stable** (slice-007 + slice-008). Generic methodology lesson. Future version-bump slices replace the prior `_0_NN_0` gate with `_0_MM_0`. Refactor candidate: un-versioned `test_plugin_yaml_version_matches_version_file_at_current_version` reads VERSION file at runtime. Defer to slice-009+ as `refactor-pmi-1-gate-to-version-agnostic-shape` if version-bump churn becomes friction.
- **Algorithm-path-conformance lesson is N=2 stable** (slice-005 + slice-008). When adding precision to existing audit, ALL pre-existing branches must compose correctly. Slice-008's design.md path-conformance table is a generalizable artifact. Cross-cutting-conformance Dim 9 sub-class promotion at N=3.
- **Wiegers AC-trace sub-class (new at slice-008 Critic M1; N=1)** — every design element should trace to a driving AC. Watch for recurrence at slice-009+; promote to Dim 9 body at N=2.
- **Cross-cutting-conformance Dim 9 catch rate at slice-008 = 100% (3 of 3 sub-class hits caught at /critique time).** Strict improvement vs slice-007 (60%) vs slice-006 (25%). The dimension is functional, monotonically improving. Slice-006-15 effectiveness target on track with margin.
- **All 6 Critic findings VALIDATED at slice-008** — third consecutive 100% Critic accuracy slice (running 25/25 across slices 6-8). The 9-dim Critic with surgical sub-bullets in Dim 1+4 + back-synced from /critic-calibrate ACCEPTED proposals is hardening as load-bearing methodology.
- **"Validate using your own ship" pattern is N=6 stable** — slice-003..008 all use the slice's own audit at /validate-slice. Standard practice.
- **Empirical-verification-at-design-time discipline is N=7 stable across slices** — added slice-008 negative-anchor curation empirical against slice-001..007 baseline (with build-time `--changed-files` representative). The cost is ~30 seconds; the savings are entire failed-design cycles. Standard practice.
- **Bidirectional sha256 forensic capture is N=3 stable** (slice-006 + slice-007 + slice-008). Future slices touching `~/.claude/...` files follow Phase 0 + Phase 4 capture pattern. **Promote to BC-1 / Dim 9 elevation candidate at N=3** (this slice meets the N=3 threshold per slice-007 reflection; user choice whether to promote).
- **TF-1 PENDING → WRITTEN-FAILING transition needs genuine failure** — N=5 stable (slice-003..008).

## Vault updates made (thin vault)

- `architecture/lessons-learned.md` — slice-008 entry to be appended (see Step 5)
- `architecture/shippability.md` — slice-008 row 8 added at /build-slice T9; slice-007 row 7 updated to drop deleted test reference
- `architecture/slices/slice-008-refine-bc-1-anchors-with-negative-context/` — to be auto-archived in Step 6
- `architecture/slices/_index.md` and `architecture/slices/archive/_index.md` — to be regenerated in Step 6
- This slice's [`design.md`](design.md) — locked at /critique time per Critic M1+M2+M3+m1+m2 corrections; reflects shipped reality (zero build-time DEVIATIONs)
- This slice's [`mission-brief.md`](mission-brief.md) — AC #5 split into 5a + 5b per Critic M2; TF-1 plan grew 5 → 7 → 9 rows (mission-brief → design.md → /critique); statuses PENDING → PASSING at /build-slice T9
- This slice's [`build-log.md`](build-log.md) — Events section captures T0 Phase 0 forensic + T1 RED + T2-T4 GREEN (audit + project + global) + T5 mid-slice smoke + T6 version bumps + T7 changelog forward-sync + T8 Phase 4 forensic + T9 pre-finish gates + DEFERRAL for BC-PROJ-2; Summary section finalized
- This slice's [`validation.md`](validation.md) — captures 6/6 ACs PASS + VAL-1 clean (Layer A 0 secrets + Layer B 0 hallucinated imports) + WS-1/ETC-1 default-off + shippability 8 rows / 69 tests / 69 PASS in 2.84s + 0 reality surprises + PMI-1 invariant + bidirectional sha256
- `methodology-changelog.md` (in-repo + `~/.claude/`) — v0.23.0 / BC-1 v1.2 entry added at /build-slice; sha256 `0A7AA6CA04E372FB` byte-equal both sides
- `VERSION` (in-repo) and `~/.claude/ai-sdlc-VERSION` — `0.22.0` → `0.23.0` (forward-sync with rename per `INSTALL.md`); sha256 `D1994E4942B06EC3` byte-equal both sides
- `plugin.yaml` — `version` 0.22.0 → 0.23.0 (PMI-1 atomic with VERSION)
- `architecture/build-checks.md` — schema-prelude TWO sentences (Negative anchors field + final filter semantics) added; BC-PROJ-1 gets 9-token Negative anchors field
- `~/.claude/build-checks.md` — schema-prelude TWO sentences + cross-project-applicability sentence added; BC-GLOBAL-1 gets 9-token Negative anchors field
- `tools/build_checks_audit.py` — `BuildCheckRule.negative_anchors` field + `to_dict` + `_parse_rules` overlap validation + `_negative_anchor_match` helper + `_rule_applies` uniform final filter + docstring update
- `tests/methodology/test_build_checks_audit.py` — 25 → 34 tests (+9 BC-1 v1.2 tests)
- `tests/methodology/test_methodology_changelog.py` — 6 → 7 tests (replaced 0.22.0 PMI-1 gate with 0.23.0 gate; added v0.23.0 BC-1 v1.2 entry pin)
- `architecture/risk-register.md` — no new risks added (slice's Discovered items are methodology-discipline refinements + pattern observations, not project risks; tracked in this reflection + lessons-learned)
- No ADRs superseded — ADR-007 stands as accepted; ADR-004 (slice-005 BC-1 v1.1) is extended NOT superseded by ADR-007 (BC-1 v1.2 is additive; v1.1's Trigger anchors remain functional)
