# Reflection: Slice 039 apply-critic-calibrate-proposals-to-critique-agent

**Date**: 2026-05-18
**Shipped**: YES-WITH-DEFERRALS

## Validated
- Both 2026-05-17 ACCEPTED proposals applied to-intent (not weakened) — first-Critic + DR-1 both verified verbatim vs calibration-log L457/L458; AC1/AC2 real-artifact offset checks confirm correct positioning (APED-1 Dim 9 #12 between PTFCD-1 and Bonus; MEPD-1 Dim 7 between §7 and §8).
- CAD-1 EOL-agnostic content-equality holds post-edit — `critique_agent_drift_audit` exit 0 (in-repo ≡ installed, sha256 5c186309…).
- The structural-invariant supersession + RPCD-1 sibling-scoping pattern (slice-013/015/024 precedent) applied cleanly: `_lists_eleven`→`_lists_twelve_sub_clauses` + 5 PTFCD-1/PTFFD-1 sibling `end_anchor` tightens — test_critique_agent.py 55/55 PASS.
- The DR-1 M-add-1 selector-prefix discriminator works on the REAL catalog: 14 LIVE `::test_critique_dim_9_lists_eleven_sub_clauses` tokens renamed, frozen line-34 slice-025 narrative preserved (1→1), shippability_runner 39/39 PASS — the predicted guaranteed-false-FAIL of the original `not in catalog` formulation was averted.
- MEPD-1 self-application (the slice-022 self-violation law) discharged by construction: slice-039 carries its own APED-1/MEPD-1 RULE-IDs + v0.52.0 entry-pins + 4-part PMI-1 bump — exactly what MEPD-1 demands of methodology-surface slices.

## Corrected
- (none — no design claim was refuted by reality. The two in-build reformulations — propagation pin `==14`→`>=14`+absence-of-old, and the RPCD-1 sibling end_anchor tighten — were within design intent, documented in build-log "Design deviations"; design.md already specified the selector-prefix discriminator and the precedent chain.)

## Discovered
- **slice-038 (SRSC-1) left a stale slice-031 SCMD-1 prose-pin** — `tests/methodology/test_validate_slice_skill.py::test_step4_5_5_consumes_machine_stable_command` asserts the slice-031 prose `"Run each entry's **Machine-cmd** column"` which slice-038's SRSC-1 replaced with the `tools.shippability_runner`-invocation prose (SKILL.md L216). The stale prose-pin (a B2-v1 mini-CAD anchor) has been FAILing since slice-038 shipped. Surfaced here only because slice-039's pre-finish ran the FULL methodology suite (BC-PROJ-4 discipline) and read its output. Impact: the methodology suite is non-green on master for a slice-038-rooted reason; a future slice that trusts "suite green" as a gate would be blocked by an unrelated stale pin (false-PCA-1-HALT class, adjacent to R-4/R-5). Added to risk-register as **[[risk-register#R-10]]**. NOT a slice-039 defect (rigorously proven: skills/validate-slice/SKILL.md git-identical to master; zero slice-039 surface; not catalogued in shippability.md so the catalog regression check is unaffected). → strongest standing follow-up-slice candidate.
- **First-Critic FBCD-1 sub-mode (a) partial-enumeration recall failure** — the first Critic correctly enumerated the `methodology-changelog.md:266` frozen-history twin (m1) but did NOT re-run the same full-fileset grep against the shippability.md surface where the symmetric line-34 twin lives; m2 then compounded it with a 14/`not in catalog` arithmetic-and-formulation error. DR-1 meta-Critic M-add-1 caught it. Calibration signal (below).

## Deferred
- **R-10 fix** (realign/supersede `test_step4_5_5_consumes_machine_stable_command` to slice-038's runner-invocation prose, OR formally retire the stale slice-031 B2-v1 prose-pin) — reason: zero slice-039 surface; a tooling/methodology-prose change needs its own slice per CLAUDE.md "refactors need a slice"; out-of-scope per slice-039 mission-brief (it is strictly about applying the two calibration proposals). Lands in: next slice (strongest standing candidate) OR a dedicated methodology-hardening slice.

## Critic calibration

Per TRI-1, scored against `critique.md` `## Triage` dispositions + reality observed during build/validate:

- **M1** (APED-1 content-pin lacks behavioral verbs): **VALIDATED** — disposition ACCEPTED-FIXED. Reality confirmed the concern was real: the 4 original APED-1 pins (title/location/rule-ID/evidence) provably tautology-pass under a `MUST Bash-execute`→`should reason about` weakening; the added `_pins_behavioral_obligation` pin asserts the verbs and is green. Correctly Major (not Blocker — build-time real-artifact gate is the backstop; APED-1's own trigger did not fire on slice-039). First-Critic precise.
- **m1** (changelog:266 frozen-history enumeration): **VALIDATED** — disposition ACCEPTED-FIXED. The frozen v0.39.0 occurrence is real; the scope-boundary sentence is correct. Minor band appropriate.
- **m2** (SCPD-1 occurrence count / formulation): **VALIDATED-IN-DIRECTION but specific remedy SUPERSEDED** — disposition recorded ACCEPTED-FIXED→SUPERSEDED-BY-M-add-1. Reality (build) PROVED m2's specific output wrong: count is 15 not 14, and the `"_lists_eleven_sub_clauses" not in catalog` pin it prescribed would have guaranteed-false-FAILed at /validate-slice on the legitimately-preserved line-34 frozen narrative. m2's *instinct* (absence-of-old) was right; its *artifact* was wrong — the classic "Critic reasons about the claim, not the artifact" law applied to a Critic-proposed oracle (slice-036 lesson lineage).
- **M-add-1** (DR-1 EXTEND missed finding — line-34 third frozen occurrence; count 15; m2 pin a guaranteed false-FAIL): **VALIDATED** — load-bearing DR-1 catch. Mechanically confirmed at build (per-line `grep -o` = 2/2/2/2/2/2/**3**; the renamed-vs-preserved split executed correctly on the real catalog; runner 39/39). Without M-add-1 the Builder's already-applied m2 fix would have shipped a guaranteed /validate-slice false-FAIL.

**Missed by Critic**: the slice-038-rooted stale prose-pin (R-10) was not flagged by either Critic layer — but this is arguably *out of design-Critic scope* (a pre-existing slice-038-introduced suite failure, the slice-029/R-4 "slice-innocent pre-existing failure" class, not a slice-039 design defect). The structural backstop that caught it was the BC-PROJ-4 "run the full suite on the real artifact at pre-finish and READ its output" discipline — exactly as the methodology predicts for this class (the dual-Critic stack structurally cannot reach a pre-existing-unrelated-suite-failure; only the real run can). Not a Critic-prompt gap.

**Pattern**: DR-1 dual-review continues to pay decisively on codification slices (M-add-1 prevented a guaranteed false-FAIL — N+1 on "DR-1 keeps paying on codification slices", slice-026/029/038 lineage). The first-Critic's FBCD-1 sub-mode (a) recall pattern — *enumerate the first frozen-history twin but stop before re-running the same grep against the sibling surface* — is a candidate `/critic-calibrate` watch-list item: "when a finding enumerates ONE frozen-history/excluded occurrence of an anchor, the SAME finding must grep that anchor across ALL surfaces (esp. the surface the slice actually edits) and enumerate every twin." Notably this recurred on the very slice codifying APED-1's "execute, do not reason" — fitting.

## Lessons for next slice
- **A SKILL.md-repointing slice MUST supersede/realign any stale prose-pin asserting the OLD prose in the SAME fix block** (the SCPD-1 / mini-CAD propagation class applied to prose-pins). slice-038 SRSC-1 repointed /validate-slice Step 5.5 prose but left `test_step4_5_5_consumes_machine_stable_command` asserting the slice-031 wording → a methodology-suite failure that has been latent since slice-038. When a slice rewrites SKILL.md prose that a `test_*_skill.py` mini-CAD pin asserts, grep `tests/methodology/test_*_skill.py` for the old phrase and update it in lockstep. (R-10; strongest standing follow-up candidate.)
- **The BC-PROJ-4 "run the full suite on the real artifact at pre-finish AND read its output" discipline is the load-bearing backstop for slice-innocent pre-existing failures** — it's the only thing that surfaces a prior slice's reflection gap; the dual-Critic stack structurally cannot. Keep running the FULL methodology suite at pre-finish, not just the slice's targeted tests, and triage every non-green with the slice-029 independence check (`git diff --quiet master -- <file>` + `git status` surface check).
- **A Critic-proposed test oracle (count / `not in catalog` formulation) is a CLAIM about the artifact — recompute it mechanically before baking it in** (slice-036 lesson, N+1). m2's `==14`/`not in catalog` was applied as ACCEPTED-FIXED before the per-line `grep -o` was run; DR-1 M-add-1 had to re-open it. For SCPD-1 propagation on a multi-occurrence anchor, run the per-line occurrence count + the frozen-vs-live discriminator BEFORE writing the propagation pin, not after.
- **Frozen-history occurrences live on MULTIPLE surfaces; enumerate the anchor across the full repo, not just the first hit** — changelog:266 AND shippability line-34 both held a frozen `_lists_eleven` twin; the first-Critic found one and stopped. FBCD-1 sub-mode (a) full-fileset grep must include the surface the slice actually edits.

## Vault updates made (thin vault — small list)
- [[risk-register.md]] — added **R-10** (slice-038 SRSC-1 stale slice-031 SCMD-1 prose-pin; `test_step4_5_5_consumes_machine_stable_command` fails slice-innocently since slice-038).
- [[decisions/ADR-040.md]] + [[decisions/ADR-041.md]] — authored this slice (rule-ID minting + behavioral-class boundary; one-version-two-rules packaging); status accepted.
- This slice's [[design.md]] — corrected twice during /critique→/critique-review (M1 `_pins_behavioral_obligation`; m1 changelog:266 enumeration; m2→M-add-1 selector-token reformulation + 15-occurrence map + both frozen-history twins enumerated). build-log.md "Design deviations" notes the two in-build reformulations.
- `architecture/shippability.md` — row 39 (this slice's catalog entry; binds the v0.52.0 entry-pins + 10 content-pins + CAD-1 drift); 14 selector-token SCPD-1 propagations across rows 6/11/13/15/16/24/25.
- `architecture/critic-calibration-log.md` — 2026-05-17 run Proposals table reconciled (APPLIED slice-039).

## Lessons for next slice (continued — BC-1 / shippability bookkeeping)
- BC-1 promotion: **skipped** — no new evergreen build-check pattern (the R-10 root cause is the already-codified SCPD-1/mini-CAD propagation class applied to prose-pins; the fix is a follow-up slice, not a new always-on check). Surfaced in lessons-learned for promotion in a later slice if it recurs.
- Shippability Step 5.3: **already satisfied** — row 39 was added during /build-slice as the SCPD-1 consumer-propagation row for the two new rules; it IS this slice's one critical-path catalog entry. No double-add.
