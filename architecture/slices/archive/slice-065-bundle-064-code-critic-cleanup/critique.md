# Critique: Slice 065 bundle-064-code-critic-cleanup

**Critic reviewed**: mission-brief.md, design.md, new ADRs (none — voluntary-restraint discipline disposition)
**Date**: 2026-05-23
**Result**: BLOCKED

> First-Critic verdict pre-Builder-drafts. All 8 findings ACCEPTED-FIXED in same fix block per TPHD-1 sub-mode (a) / FBCD-1 cross-surface harmonization (pre-/critique-review). /critique-review meta-pass returned EXTEND with 1 missed Minor (m-add-1 — shippability row #65 Description residual FBCD-1 drift; ACCEPTED-FIXED). Post-TRI-1 user-ratification computes Final verdict mechanically per triage table dispositions.

## Summary

First-Critic returned BLOCKED with 2 Blockers (B1 + B2 — both empirically verified via Critic Bash execution against the current SKILL.md), 3 Majors (M1 backwards "L42 and L41" within-design phrasing; M2 mission-brief vs design.md `exit` granularity drift; M3 weak Inclusion-heuristic class-precedent framing), 3 Minors (m1 verification-harness vs prose-as-executable-contract mismatch; m2 brace-group form pinning; m3 ADR-061 coordinate-vs-semantic citation). Builder ACCEPTED-FIXED all 8 in the same fix block per TPHD-1 sub-mode (a) / FBCD-1 cross-surface harmonization — Critic-grounded edits applied to design.md + mission-brief.md before /critique-review meta-pass and TRI-1 triage. Builder empirically re-verified B1+B2 fix math via Python (`bash_block.count(filter) == bash_block.count(git_diff)` → 2 == 2 PASS on current SKILL.md; STOP guard count flips 1→2 post-AC#3-fix). Slice-040 N+1 doctrine firing on slice-064's section-scoped-substring-assertion pattern (slice-065 first governed slice) — first-Critic caught the canonical drift class (B1+B2 are textbook RSAD-1: slice authoring substring-leak retirement committed substring-leak in its own fix).

## Findings

### Blockers (must address before /build-slice)

#### B1: Design.md's literal AC#2 assertion `diff_with_filter == step_1.count('git diff "$base"')` FAILs on the current SKILL.md (2 != 3) — breaks Phase B's expected-PASS contract and is itself a substring-leak

- **Claim under review**: `design.md` "Components touched" > `tests/skills/code_review/test_code_review_skill.py` (modified): "L166 assertion tightening: `assert diff_with_filter == step_1.count('git diff "$base"')`." AND mission-brief.md AC#2: "tightened to a semantic invariant — `--name-only --diff-filter=ACMR` occurrence count equals the count of `git diff "$base"` leg headers in Step 1." AND mission-brief.md Mid-slice smoke gate: "Expected: AC#1/AC#2 PASS".
- **Issue**: Critic executed proposed assertion empirically against current `skills/code-review/SKILL.md` Step 1 (section-scoped via `body.find("### Step 1: …")` … `body.find("### Step 2:")` as the test does). Result:
  - `step_1.count("--name-only --diff-filter=ACMR")` = **2** (L45 + L51 bash commands)
  - `step_1.count('git diff "$base"')` = **3** (L37 preamble prose `` `git diff "$base"...HEAD` `` + L45 bash + L51 bash)
  - `2 == 3` → **False** → the assertion FAILs the moment it lands.

  Three downstream consequences: (1) Phase B mid-slice smoke gate breaks (no SKILL.md change at Phases A/B could make AC#2 PASS without weakening the L37 preamble — Claude needs it at runtime to understand the union); (2) AC#2 is itself an M1-class substring-leak — the very class the slice retires; (3) mission-brief AC#2 prose says "leg headers" while design.md literal counts ALL substring matches including preamble — FBCD-1 sub-mode (a) cross-surface inconsistency.

  This is textbook RSAD-1 (slice-040 N+1 doctrine in action): slice-064 introduced the section-scoped-substring-assertion pattern; slice-065 is its first governed slice; the canonical drift class is exactly what B1 catches.
- **Evidence**: Critic Bash execution against current SKILL.md (re-verified by Builder via `.scratch_verify_b1_b2.py`):
  ```
  step_1.count("--name-only --diff-filter=ACMR") = 2
  step_1.count('git diff "$base"')                = 3
  AC#2 assertion result if applied: False
  ```
  Occurrence sites of `git diff "$base"` in Step 1: offset 265 (L37 preamble), offset 990 (L45 bash), offset 1285 (L51 bash). The L37 site contains `git diff "$base"...HEAD` — `git diff "$base"` is its substring prefix.
- **Proposed fix**: Tighten the count basis to the bash-fenced block specifically, NOT the section. Concretely: locate the ` ```bash\n` … `\n``` ` boundaries within Step 1 and assert against the block body only. Then the bash-only counts are `--name-only --diff-filter=ACMR` = 2 and `git diff "$base"` = 2 (only the two bash commands; L37 preamble is excluded). The assertion `bash_block.count("--name-only --diff-filter=ACMR") == bash_block.count('git diff "$base"')` PASSes today (2 == 2) AND retires the substring-leak class. This same `bash_block` scoping fix should be re-used for AC#4 (see B2).
- **Builder draft**: ACCEPTED-FIXED at design.md "Components touched" §`tests/skills/code_review/test_code_review_skill.py` (modified) §2 + mission-brief.md AC#2. New canonical `bash_block` scoping idiom added to design.md (used by all three changes: AC#1, AC#2, AC#4). Empirically re-verified via Python: `bash_block.count("--name-only --diff-filter=ACMR")` = 2 AND `bash_block.count('git diff "$base"')` = 2 → semantic invariant PASSes today. Mid-slice smoke gate Phase B expected-PASS contract restored. Mission-brief AC#2 description updated to specify "bash-fenced block ONLY (not the full section)" with explicit reference to Critic B1.

#### B2: AC#4's proposed assertions `'[ -z "$default" ]' in step_1` and `'default-branch-unresolvable' in step_1` ALREADY pass on the current pre-fix SKILL.md — test is not WRITTEN-FAILING for the right reason; same substring-leak class

- **Claim under review**: `design.md` "Components touched" > "New function `test_skill_md_step_1_default_branch_resolver_stops_on_empty` — Section-scoped to Step 1. Assertions: `'[ -z "$default" ]' in step_1`, `'exit 2' in step_1`, `'default-branch-unresolvable' in step_1`." AND mission-brief.md Build phase B Expected: "AC#4 FAIL (WRITTEN-FAILING)".
- **Issue**: Critic executed the three proposed assertions against current unfixed SKILL.md Step 1:
  - `'[ -z "$default" ]' in step_1` → **True** (already present at L41 — the existing second-resolver fallback `[ -z "$default" ] && default=$(git config init.defaultBranch …)`).
  - `'exit 2' in step_1` → False (the only assertion that actually distinguishes pre-fix from post-fix).
  - `'default-branch-unresolvable' in step_1` → **True** (already present at L68 in the documented `Error cases` prose).

  TF-1 PENDING→WRITTEN-FAILING genuineness obligation (Dim 4 sub-bullet): the test must fail BEFORE implementation in a way that *distinguishes the real missing behaviour from accidental sibling matches*. As proposed, AC#4 fails for ONE reason (`exit 2` absent) — but the other two assertions are silently satisfied by **unrelated existing content** the slice does not touch.

  Future regression class this leaks: a refactor that REPLACES the existing L41 second-resolver fallback with a `case`/`set -u`-style construct AND removes the new STOP guard would still PASS AC#4 (the L68 prose line carries the `default-branch-unresolvable` literal; some other `[ -z` test could survive). The test does not actually pin the STOP-guard's structural presence in the bash-fenced block.

  Same root-cause class as B1 — both Blockers share the "scope to bash-fenced block, not the section" fix.
- **Evidence**: Critic Bash execution against current SKILL.md (re-verified by Builder via `.scratch_verify_b1_b2.py`):
  ```
  [ -z "$default" ] currently in Step 1 (pre-fix): True
  exit 2 currently in Step 1: False
  default-branch-unresolvable currently in Step 1: True
  STOP guard count in bash_block (pre-fix) = 1  (just the L41 fallback resolver)
  ```
  L41 verbatim: `[ -z "$default" ] && default=$(git config init.defaultBranch 2>/dev/null)` — already carries the `[ -z "$default" ]` literal.
  L68 verbatim: `` - `default-branch-unresolvable` (neither `git symbolic-ref` nor `git config init.defaultBranch` resolves): … `` — already carries the literal.
- **Proposed fix**: Three composable fixes: (1) Scope to bash-fenced block, not section. (2) Count-based pinning: `bash_block.count('[ -z "$default" ]') >= 2` (additive — one for existing fallback resolver, one for new STOP guard; distinguishes pre-fix count==1 from post-fix count==2). (3) Co-locate assertion: STOP guard appears AFTER second resolver call AND BEFORE `git merge-base` (`fallback_idx < stop_idx < merge_base_idx`). Pick (1) + (2) + (3) for cleanest semantic intent.
- **Builder draft**: ACCEPTED-FIXED at design.md "Components touched" §`tests/skills/code_review/test_code_review_skill.py` (modified) §3 (new function) + mission-brief.md AC#4. All three proposed fixes applied: (1) `bash_block` scoping via canonical idiom; (2) count-based pin `bash_block.count('[ -z "$default" ]') >= 2`; (3) co-location pin `fallback_idx < stop_idx < merge_base_idx`. Empirically re-verified: pre-fix `bash_block.count('[ -z "$default" ]')` = 1; post-AC#3-fix → 2 (distinguishes correctly). Mission-brief AC#4 description updated to explicitly cite /critique B2 + m2 references with the three composable assertion components.

### Majors (address this slice)

#### M1: design.md AC#3 insertion-location phrasing is reversed/garbled — "between current L42 and L41" is structurally backwards; recipe is ambiguous

- **Claim under review**: design.md "Components touched" > `skills/code-review/SKILL.md` (modified): "Insert `[ -z "$default" ] && { echo "default-branch-unresolvable: neither origin/HEAD nor init.defaultBranch resolved" >&2; exit 2; }` between current L42 and L41."
- **Issue**: L41 precedes L42 in the file. "Between L42 and L41" is structurally impossible to interpret without inversion. The clear intent (verified against the surrounding bash block context — L41 is the second resolver, L42 is `git merge-base`) is "between L41 and L42" — i.e., AFTER both resolvers, BEFORE the `git merge-base` call. Design.md's later restatement at Phase C says "between L41 and L42 (after both resolver attempts, before `git merge-base`)" — correct phrasing. So design.md self-contradicts: the "Components touched" cell is reversed, the Phase C cell is correct.

  FBCD-1 sub-mode (a) within-slice inconsistency between two design.md cells (the "Components touched" sub-section vs the Phase C sub-section).
- **Evidence**: design.md "Components touched" "Insert ... between current L42 and L41" vs design.md Phase C "edit SKILL.md L40-L42 to insert ... between L41 and L42 (after both resolver attempts, before `git merge-base`)". File state confirms L41 = second-resolver line, L42 = `git merge-base` line — Phase C is correct, "Components touched" is wrong.
- **Proposed fix**: Edit design.md "Components touched" to "Insert `…` between current L41 (the second-resolver fallback) and L42 (the `git merge-base` line) — i.e., AFTER both resolver attempts, BEFORE the `git merge-base` call."
- **Builder draft**: ACCEPTED-FIXED at design.md "Components touched" §`skills/code-review/SKILL.md` (modified) "What changes" bullet. Phrasing corrected to "between current L41 (the second-resolver fallback `[ -z "$default" ] && default=$(git config init.defaultBranch …)`) and current L42 (`base=$(git merge-base "$default" HEAD)`) — i.e., AFTER both resolver attempts, BEFORE the `git merge-base` call." Surrounding parenthetical context preserved per Critic's proposed-fix wording for downstream-reader clarity.

#### M2: AC#4's proposed `'exit 2' in step_1` may be satisfied by the L69 `exit clean` prose if the substring rule loosens; design hardcodes `exit 2` while mission-brief AC#4 says non-zero `exit` — pick one and pin

- **Claim under review**: mission-brief AC#4: "asserts the SKILL.md Step 1 bash block contains a `[ -z "$default" ]` guard followed by a non-zero `exit` (programmatic STOP, not silent fall-through)." vs design.md AC#4 test body: `'exit 2' in step_1`.
- **Issue**: (1) Mission-brief is intentionally generic ("non-zero `exit`") while design.md hardcodes `exit 2`. If the implementer chooses `exit 1` (also a valid non-zero exit), AC#4 FAILs even though mission-brief AC#3 is satisfied. Pick one canonical exit code and align both surfaces. Design's choice of `exit 2` is sound (parity with NAW-1 usage class) but the mission-brief should be tightened to match. (2) The current Step 1 L69 already contains the substring `exit clean`. The proposed `'exit 2' in step_1` is safe today because the `'2'` distinguishes it; but if a future regression weakens the assertion to `'exit ' in step_1`, it would pass on `exit clean` alone. Lower probability than B2 but in the same class.
- **Evidence**: SKILL.md L69 verbatim contains the substring `exit clean`. mission-brief.md AC#4 uses generic "non-zero `exit`". design.md uses literal `exit 2`. The two surfaces are in tension.
- **Proposed fix**: Tighten mission-brief AC#4 to "non-zero `exit 2`" so the two surfaces agree. Then in AC#4's assertion, scope to bash-block (per B2 fix #1) — eliminates the `exit clean` adjacency risk entirely. Discharges FBCD-1 sub-mode (a) cross-surface drift.
- **Builder draft**: ACCEPTED-FIXED at mission-brief.md AC#4 (already harmonized in same fix block as B2). Mission-brief AC#4 description now reads "`exit 2` and a `default-branch-unresolvable: …` stderr message (mirroring NAW-1's ADR-061 §Decision exit-2 contract)" — explicit canonical exit code, aligns with design.md hardcoded literal. Combined with B2 bash-block scoping fix, the `exit clean` adjacency risk is structurally retired (L69 is outside `bash_block`).

#### M3: Inclusion-heuristic disposition's "Why-none" cites slice-061 R-16 INSTALL.md prose-correctness as class-precedent — but slice-065 is NOT a prose-correctness slice; it's a runtime-behavior-change slice on a methodology-executable surface

- **Claim under review**: design.md "Inclusion-heuristic disposition": "Class precedent: slice-061 R-16 born-retired INSTALL.md prose-correctness fix shipped without methodology-changelog / VERSION / ADR (same class on different surface)" AND "No new architectural decision; inherits ADR-061's reasoning by reference."
- **Issue**: The slice-061 R-16 INSTALL.md fix changed INSTALL documentation prose — a class where "fix lands; no runtime contract changes." Slice-065's AC#3 changes **methodology-executable contract** in SKILL.md: pre-fix, the bash block silently falls through to `git merge-base "" HEAD` (undefined behavior, OS-dependent exit 128); post-fix, it fails fast with `exit 2` and a specific stderr message. This is a **new error contract** on the `/code-review` skill's external surface — any consumer (Claude in the next session, a wrapper script, a future audit) that depends on /code-review's failure mode in the clean-room corner case now observes different behavior.

  Per BC-PROJ-10 Inclusion-heuristic, "new ADR" is not the only axis — "changes a skill's external contract" should also be evaluated. slice-064's own ADR-062 §Consequences explicitly documented changes to /code-review's surface. AC#3 is a smaller change in the same class. The slice may still legitimately ship without an ADR (the precedent for inheriting via ADR-061 reasoning by reference is sound) but the "same class as slice-061 R-16 INSTALL.md prose-correctness" framing is misleading — that precedent is too weak.

  Better precedent: slice-063 NAW-1 carved out the exit-2 + `default-branch-unresolvable` contract via ADR-061; slice-064 ADR-062 extended NAW-1's union-of-three-sources pattern to /code-review's SKILL.md surface WITHOUT minting a new RULE-ID. Slice-065 mirrors ADR-061's exit-2 STOP-guard contract into the same SKILL.md surface — inherits-by-reference is sound for *that* precedent (ADR-061 + ADR-062 in-family extension), not slice-061 R-16. MEPD-1(b) discipline concern: the "Why-none" rationale must verify against actual classification, not lean on a weak precedent.
- **Evidence**: design.md (pre-fix) cites slice-061 R-16 as "same class on different surface" — but slice-061 R-16 fixed INSTALL.md prose-only; no `tools/` audit changed behavior and no skill changed runtime contract. Slice-065 AC#3 changes the `/code-review` skill's error contract.
- **Proposed fix**: Update the Inclusion-heuristic disposition's "Why-none" to: (1) drop the slice-061 R-16 INSTALL.md precedent (too weak); (2) cite slice-064 ADR-062 (in-family extension precedent — extended NAW-1's pattern without minting a new RULE-ID) and slice-063 ADR-061 §Decision (canonical exit-2 contract being mirrored); (3) add explicit "external contract change scope check": "AC#3 changes /code-review's failure mode in the clean-room corner case from undefined (exit 128) to deterministic (exit 2 + stderr). No consumer in the repo depends on the pre-fix undefined behavior (verified by grep for `merge-base` callers); the change is purely a tightening, not a contract widening or breaking."
- **Builder draft**: ACCEPTED-FIXED at design.md "Inclusion-heuristic disposition". All three Critic-proposed changes applied: (1) slice-061 R-16 INSTALL.md precedent dropped (Critic-correct: too weak — prose-correctness vs methodology-executable contract); (2) ADR-061 (canonical exit-2 contract) + ADR-062 (in-family extension without minting new RULE-ID) cited as load-bearing precedent; (3) new "External-contract scope check" bullet added explicitly classifying AC#3 as a tightening (pre-fix undefined behavior → post-fix deterministic), with grep verification statement for `merge-base ""` consumer dependencies (zero). Slice-064 reflection's "single-slice test-tightness defects … task-list items, not risk-register entries (no class signal)" classification retained as corroborating signal.

### Minors (log; address if cheap)

#### m1: AC#3's verification §3 inline-bash test verifies POSIX correctness of the brace-group idiom — but Step 1 bash is **prose-as-executable-contract for Claude**, not bash that is actually run; verification mismatches the load-bearing axis

- **Claim under review**: mission-brief Verification plan §3: "Inline bash script: `default=""; [ -z "$default" ] && { echo "default-branch-unresolvable" >&2; exit 2; }; ...` exits 2 with non-empty stderr (NOT 128 from `git merge-base "" HEAD`)"
- **Issue**: Step 1's bash block is read by Claude at /code-review runtime and emulated — Claude doesn't `exec` it as a subprocess (the skill's Step 1 has no `Bash` tool invocation; it computes the file list via the read prose). The "exits 2" verification is meaningful for POSIX-shell parity but doesn't verify Claude obeys the STOP prose. The actual load-bearing axis is "does Claude reading this prose at runtime STOP rather than fall through" — that requires a self-dogfood prose-comprehension verification, not a POSIX-shell exit-code verification.
- **Evidence**: skills/code-review/SKILL.md Step 1 has no `bash` tool invocation; the prose is read by the orchestrator (Claude main thread) and acted upon. The `## Critical rules` section in CLAUDE.md explicitly says "skill prose IS executable contract".
- **Proposed fix**: Either (a) drop verification §3 as not load-bearing; OR (b) keep it as a parity-with-NAW-1-Python sanity check but call it out as "ancillary POSIX-parity check, not the load-bearing runtime test"; OR (c) add a stronger verification via /code-review self-dogfood in a clean-room corner case.
- **Builder draft**: ACCEPTED-FIXED at mission-brief.md Verification plan §3. Option (b) chosen — kept the POSIX-parity inline-bash check but reframed with explicit "NOTE per /critique m1" caveat clarifying it's "ancillary POSIX-parity check, NOT load-bearing" + AC#4 structural pin + AC#5 self-dogfood named as the actual runtime checks + methodology-discoverability axis explicit. Option (c) (clean-room corner-case self-dogfood) deferred as test-environment setup overhead > benefit at v1 walking-skeleton (slice-066+ candidate if N=2 corner-case-coverage gap surfaces).

#### m2: AC#4's design.md assertions use `'exit 2' in step_1` (loose substring) but design.md "literal" for SKILL.md insertion includes `; exit 2; }` — consider asserting the bash-block-scoped exact-shape including the brace-group terminator

- **Claim under review**: design.md AC#4 assertions: `'exit 2' in step_1`. design.md SKILL.md insertion: `[ -z "$default" ] && { echo "default-branch-unresolvable: neither origin/HEAD nor init.defaultBranch resolved" >&2; exit 2; }`.
- **Issue**: `'exit 2' in step_1` would also match e.g. `if [ ... ]; then exit 2; fi` or `exit 2 # never executed` — anywhere in Step 1. The proposed fix idiom is specifically `&& { …; exit 2; }`. Pinning the brace-group form prevents a future regression from refactoring to `if`/`fi` (functionally equivalent but breaks the canonical NAW-1 parity stated in design.md "Decisions made (ADRs)").
- **Evidence**: design.md "Decisions made (ADRs)" ("STOP-behavior change is mechanically equivalent to the canonical NAW-1 Python implementation"); the equivalence is to the brace-group form, not just "any exit 2".
- **Proposed fix**: Tighten assertion to `'&& { echo "default-branch-unresolvable' in bash_block` (after applying B2 bash-block scoping). Or assert the full literal substring of the insertion. This pins shape, not just exit-code existence.
- **Builder draft**: ACCEPTED-FIXED at design.md "Components touched" §`tests/skills/code_review/test_code_review_skill.py` (modified) §3 (new function) "Canonical brace-group form" sub-assertion + mission-brief.md AC#4. Already composed with B2 fix in the same fix block: assertion now includes `'&& { echo "default-branch-unresolvable' in bash_block` AND `'exit 2' in bash_block` — pins the canonical NAW-1 brace-group form, prevents `if`/`fi`-style refactor regression.

#### m3: design.md citing `ADR-061 §Decision L60-67` is a pin to specific line numbers in another document — fragile to ADR-061 edits (though ADRs are append-only per SUP-1, so the risk is low)

- **Claim under review**: design.md "What's reused": "[[architecture/decisions/ADR-061-mint-naw-1-new-agent-warning.md]] — canonical Python implementation of STOP behavior at `tools/new_agent_warning_audit.py:_resolve_default_branch`" AND "(slice-063 / ADR-061 §Decision L60-67)".
- **Issue**: Pinning to L60-67 is a fragile reference. ADRs are append-only per SUP-1 (no edits in place) so the line numbers won't change, but the L60-67 reference doesn't tell a reader WHAT'S at those lines — it's a coordinate without semantic content. Lower importance than the Blockers but consistent with the FBCD-1 discipline of replacing position-pins with semantic-pins.
- **Evidence**: design.md two cites of `ADR-061 §Decision L60-67`. Per SUP-1 the ADR is immutable so the pin is durable; but readers have to fetch ADR-061 to know what's there.
- **Proposed fix**: Replace `ADR-061 §Decision L60-67` with `ADR-061 §Decision (the three-step resolver: git symbolic-ref → git config init.defaultBranch → None)` — same coordinate, semantic content inline.
- **Builder draft**: ACCEPTED-FIXED at design.md two cite sites: "What's reused" (expanded to "the three-step resolver pattern: `git symbolic-ref refs/remotes/origin/HEAD` → `git config init.defaultBranch` → fail-fast with `exit 2` + `default-branch-unresolvable` stderr") AND "Decisions made (ADRs)" (expanded to "the three-step resolver pattern: `git symbolic-ref refs/remotes/origin/HEAD` → `git config init.defaultBranch` → `None` → caller maps to `exit 2` + `_USAGE_DEFAULT_BRANCH_UNRESOLVABLE` stderr"). Semantic content inlined; coordinate-only pin replaced.

## Dimensions checked

- [x] **Unfounded assumptions** — B1 + B2 + M3 land here. Design.md claims AC#2 "is tightened to a semantic invariant" and AC#4 is "WRITTEN-FAILING" — both claims empirically false on the current SKILL.md (per executed counts). M3's slice-061 R-16 class-precedent claim is also unfounded once tested against the actual class boundary. Per Wiegers, every claim must trace to evidence — the AC#2 / AC#4 claims traced to *intended* behavior, not *realized* behavior. All three ACCEPTED-FIXED.
- [x] **Missing edge cases** — m1 captures the prose-as-executable-contract vs POSIX-bash-exit-code edge: AC#3's verification harness tests a different runtime than where the rule actually fires. ACCEPTED-FIXED via reframe (option b). No further load-edge / concurrency / network / multi-platform findings — N/A on a SKILL.md prose change.
- [x] **Over-engineering** — none. The slice is properly scoped (zero new ADRs, zero new tools, one bash insertion + 2 test tightenings + 1 new test in an existing file). The voluntary-restraint disposition (Inclusion-heuristic) is correctly applied — only the rationale framing was weak (M3 — fixed).
- [x] **Under-engineering** — B2 lands here (TF-1 PENDING→WRITTEN-FAILING genuineness): 2 of 3 AC#4 assertions accidentally pass on the pre-fix SKILL.md. ACCEPTED-FIXED via three composable assertions (count + brace-group + co-location).
- [x] **Contract gaps** — B1 + M2 land here. AC#2's drafted assertion had a semantic contract gap between intent (count `git diff "$base"` leg headers in bash block) and realization (count any substring including preamble prose). M2 captured the same class for AC#4's `exit 2` literal vs canonical brace-group shape. Both ACCEPTED-FIXED via cross-surface harmonization.
- [x] **Security** — none. No new authn/authz/input-validation/secrets/injection paths. The SKILL.md change adds defensive error handling (fails fast on unresolvable default branch instead of silent fall-through), a small security-posture improvement (defense in depth). The `default-branch-unresolvable` stderr message contains no secrets / PII / sensitive paths.
- [x] **Drift from vault** — M3 lands here. Inclusion-heuristic disposition cited slice-061 R-16 INSTALL.md prose-correctness as class-precedent when the change is actually a methodology-executable runtime contract on a SKILL.md surface. CAD-1 / OSDG-1 / PMI-1 / INST-1 status: no new tool, no plugin manifest delta, no installed-side rename — the only forward-sync is OSDG-1 on `skills/code-review/SKILL.md` (mission-brief AC#5 covers it via Phase D + test_code_review_skill_drift.py). MEPD-1 voluntary-restraint discharge: design.md cites `tests/methodology/test_methodology_changelog.py:136` as the META-1 enforcing-assertion. M3 ACCEPTED-FIXED.
- [x] **Web-known issues** — Critic WebSearch confirmed `[ -z … ] && { …; exit 2; }` idiom is POSIX-portable (single `[`, `&&` outside brackets, brace-group `{ …; }` requires `;` before `}` — design's literal has it). No platform-version restriction or deprecation found post-cutoff. No findings.
- [x] **Cross-cutting conformance** — Multiple sub-clauses fired:
  - **RSAD-1 (recursive self-application)**: B1 + B2 textbook RSAD-1 — slice authoring the substring-leak retirement committed the same substring-leak class in its own fix. Slice-040 N+1 doctrine in action (slice-064 introduced the section-scoped-substring-assertion pattern; slice-065 first governed slice). Both ACCEPTED-FIXED via canonical `bash_block` scoping idiom.
  - **FBCD-1 (fix-block-completeness)**: M1 (within-slice design.md self-contradiction) + M2 (mission-brief AC#4 generic vs design.md hardcoded). Both sub-mode (a) "original-draft cross-file/cross-cell consistency" — first-Critic catch class. Both ACCEPTED-FIXED.
  - **MEPD-1(b) (Inclusion-heuristic discharged-by-name)**: M3 — design.md's "Why-none" framing leaned on weak class-precedent. ACCEPTED-FIXED via ADR-062 + ADR-061 in-family extension citation.
  - **TPHD-1 / PTFCD-1 / PTFFD-1**: TF-1 plan rows cite `tests/skills/code_review/test_code_review_skill.py::test_skill_md_step_1_default_branch_resolver_stops_on_empty` (the new function the slice creates) and `tests/methodology/test_code_review_skill_drift.py::test_in_repo_and_installed_code_review_skill_md_are_content_equal` (existing file). All file paths resolve; function-level pin on the new function is appropriate for PENDING status. No phantom citations.
  - **APED-1 (audit-parse-rule empirical-execution)**: slice does NOT modify any `tools/**` audit parse rule — APED-1 doesn't directly apply. Critic correctly applied the execute-don't-reason discipline to test assertions instead — B1 + B2 caught by execution, not by reasoning. Builder also empirically re-verified the proposed fix math via Python (`bash_block.count` checks).
  - **BRANCH-1 / NAW-1 mirror parity**: AC#3's bash idiom mirrors NAW-1's Python pattern at `tools/new_agent_warning_audit.py:_resolve_default_branch`. Mirror parity sound.

## Triage

**Triaged by**: user
**Date**: 2026-05-23
**Final verdict**: CLEAN

| ID | Severity | Disposition | Rationale |
|----|----------|-------------|-----------|
| B1 | Blocker  | ACCEPTED-FIXED | design.md "Components touched" §`tests/...` §2 + mission-brief.md AC#2 — canonical bash_block scoping idiom; empirically re-verified 2 == 2 PASS on current SKILL.md |
| B2 | Blocker  | ACCEPTED-FIXED | design.md "Components touched" §`tests/...` §3 + mission-brief.md AC#4 — three composable assertions (count + brace-group + co-location), all bash_block-scoped; empirically re-verified pre-fix count=1 → post-fix count=2 |
| M1 | Major    | ACCEPTED-FIXED | design.md "Components touched" §`skills/...` "What changes" — phrasing corrected to "between L41 and L42 (after both resolver attempts, before git merge-base)" with parenthetical context per Critic's proposed-fix wording |
| M2 | Major    | ACCEPTED-FIXED | mission-brief.md AC#4 — tightened generic "non-zero exit" to canonical "non-zero `exit 2`" + design.md hardcoded `exit 2` cited; harmonized in same fix block as B2 |
| M3 | Major    | ACCEPTED-FIXED | design.md "Inclusion-heuristic disposition" — slice-061 R-16 INSTALL.md precedent dropped (too weak — prose-correctness vs methodology-executable contract); ADR-062 + ADR-061 in-family extension precedent cited; new "External-contract scope check" bullet added |
| m1 | Minor    | ACCEPTED-FIXED | mission-brief.md Verification plan §3 — reframed as ancillary POSIX-parity check with explicit "NOTE per /critique m1" + AC#4 + AC#5 + methodology-discoverability axis named as load-bearing runtime checks |
| m2 | Minor    | ACCEPTED-FIXED | composed with B2 in same fix block — assertion now includes brace-group form `'&& { echo "default-branch-unresolvable' in bash_block` + `'exit 2' in bash_block`; pins canonical NAW-1 shape, prevents `if`/`fi`-style refactor regression |
| m3 | Minor    | ACCEPTED-FIXED | design.md two cite sites — `ADR-061 §Decision L60-67` replaced with semantic content inline (three-step resolver pattern with full description) at "What's reused" and "Decisions made (ADRs)" cells |
| m-add-1 | Minor (missed-by-first-Critic; meta-Critic EXTEND) | ACCEPTED-FIXED | design.md L210 shippability row #65 Description cell — appended clause naming the new test function `test_skill_md_step_1_default_branch_resolver_stops_on_empty` (AC#4 — bash-block-scoped count + brace-group + co-location, per /critique B2 fix). Cmd column unchanged (load-bearing shippability-runner axis intact pre-fix). FBCD-1 sub-mode (a) cross-cell narrative-manifest parity restored. /critic-calibrate watch-list candidate per /critique-review §Notes: FBCD-1 cross-cell completeness check should enumerate ALL cells naming advisory IDs (M1/m1/m2), not just cells the Critic landed findings on. |

> TRI-1 user-ratification gate pending — Builder draft computes to CLEAN (9× ACCEPTED-FIXED total: 8× from first-Critic + 1× from meta-Critic missed-finding; zero ACCEPTED-PENDING, zero ESCALATED → CLEAN per `tools/triage_audit.py` verdict-pattern rule). All fixes applied in the same fix block per TPHD-1 sub-mode (a)/(b) / FBCD-1 cross-surface harmonization discipline. User may override any disposition; OVERRIDDEN / DEFERRED / ESCALATED would require non-empty rationale per `tools/triage_audit.py`.
