# Reflection: Slice 012 bc-proj-2-negative-anchor-migration

**Date**: 2026-05-13
**Shipped**: YES

## Validated

- **BC-PROJ-2 negative-anchor migration silences the N=2 false-positive class** — empirically validated at /validate-slice on real archived slices: slice-005 archive backtest with `--changed-files tools/build_checks_audit.py` (exercising BC-PROJ-2's `skills/**/*.py, tools/**/*.py` glob path) yields `applicable=[]`, `skipped=['BC-PROJ-1', 'BC-PROJ-2', 'BC-GLOBAL-1']`; slice-011 archive backtest yields same; slice-001 backward-compat preserved with `applicable=['BC-PROJ-1', 'BC-PROJ-2', 'BC-GLOBAL-1']`.
- **Slice-008 BC-1 v1.2 infrastructure reused verbatim — zero code change to `tools/build_checks_audit.py`** — the `_parse_rules` field parser + `_negative_anchor_match` final filter + `negative-anchor-overlaps-positive` parse-violation kind all behave identically on the new BC-PROJ-2 data. Algorithm-path-conformance preserved: filter applies uniformly across `always: true` / glob / keyword-anchor paths per slice-005 lesson (N=2 → N=3 stable).
- **Token disjointness verified Ø at design time (Audit 1) — empirically clean post-build** — zero `negative-anchor-overlaps-positive` violations on BC-PROJ-2's parsed `BuildCheckRule` (`parse, fence, code-block, backtick, llm, agent, prompt, output, response` keywords + `fence, code-block, llm` anchors disjoint from the 9-token methodology-vocabulary negative-anchor set).
- **Slice-001 backward-compat covenant per ADR-007 / ADR-011 empirically preserved (Audit 2)** — slice-001's archived mission-brief + design contain ZERO of the 9 methodology-vocabulary tokens at word-boundary, so negative-anchor filter cannot suppress; BC-PROJ-2 continues to fire via positive anchors + glob path. The design-time Audit 2 grep prediction validated empirically.
- **Bidirectional sha256 forensic discipline N=8 stable** — methodology-changelog.md baseline `5C261C963A5BA2DB` byte-equal in-repo↔installed → post-edit `33423327EB101F26` byte-equal in-repo↔installed. VERSION + ai-sdlc-VERSION byte-equal at "0.27.0".
- **TF-1 PENDING → WRITTEN-FAILING → PASSING genuineness for all 6 rows** — pre-fix failure signatures: 2× `AssertionError "BC-PROJ-2 should NOT apply"` (slice-005 + slice-011 backtests fire BC-PROJ-2 via kw + glob paths pre-migration); 1× `AssertionError "WRITTEN-FAILING stage (intentionally flipped)"` (mini-CAD-1 row 3 transition for test_slice_001_archive_still_fires_proj2 — N=4 → N=5 stable); 1× `AssertionError "BC-PROJ-2 negative_anchors mismatch: got (), expected (...)"` (the B1 pinned signal exact match); 1× `AssertionError "## v0.27.0 missing"` (entry-pin); 1× `AssertionError "VERSION file content is '0.26.0', expected '0.27.0'"` (PMI-1 gate). All non-coincidental.
- **All 5 Critic findings VALIDATED** at /validate-slice — 0 FALSE-ALARM, 0 OVERRIDE-MISJUDGED, 0 NOT-YET. **Sixth consecutive 100% Critic-accuracy slice** (slice-006: 11/11; slice-007: 8/8; slice-008: 6/6; slice-009: 11/11; slice-010: 11/11; slice-011: 7/7; **slice-012: 5/5** — running 59/59 across slices 6-12). Strongest streak in the project, extending slice-011's 54/54 by +5.
- **PMI-1 invariant maintained atomically** — `VERSION` + `~/.claude/ai-sdlc-VERSION` + `plugin.yaml.version` all bumped 0.26.0 → 0.27.0 in one atomic operation per slice-007 PMI-1 closure pattern. PMI-1 audit clean (exit 0; 24 skills, 5 agents, 15 tools).
- **"Validate using your own ship" pattern N=10 stable** — slice-012 self-application: `tools.build_checks_audit --slice architecture/slices/slice-012-...` returns `applicable=[]`, `skipped=['BC-PROJ-1', 'BC-PROJ-2', 'BC-GLOBAL-1']` (slice-012's own text contains 9 of 9 methodology-vocabulary tokens by design-time Audit 5 over-determination → silenced by its own ship). Canonical methodology practice.
- **Shippability catalog regression-clean** — 12 rows / 87 tests / **87 PASS** in 3.36s (well under 2-min target). No regressions on slice-001..011 critical paths. Catalog grew 11 rows → 12 rows.
- **Slice-011 NEW Dim 9 sub-class N=2 promotion probe SUCCESS** (entry-pin-vs-PMI-1-gate-semantics-conflation) — slice-012 Phase 1b INSERT + Phase 1c narrow-scope Edit discipline empirically validated; `test_v_0_26_0_rsad_1_entry_present_in_repo_and_installed` STILL PASSES post-PMI-1 supersession. All prior versions' entry-pin functions (v0.22.0 / v0.23.0 / v0.24.0 / v0.25.0 / v0.26.0) preserved.

## Corrected

- **Slice-008 ADR-007 → ADR-011 extends, not supersedes**: ADR-007 (BC-1 v1.2; slice-008) stands as `accepted`; ADR-011 (slice-012) carries `supersedes: null` and EXTENDS ADR-007 by completing the BC-PROJ-2 migration that ADR-007 explicitly deferred to N=2. The original ADR-007 line "Future deferred work: BC-PROJ-2 migration when N=2 surfaces" is now retired — addressed by slice-012. **NO ADR superseded** at slice-012.
- **No design.md corrections at build time** — Critic B1/M1/M2/M3 + m1 caught what would have been design.md corrections at build time. design.md was locked at /critique time and reflects shipped reality. **Zero build-time DEVIATIONs** at slice-012 — second slice in series 005-012 with this property (slice-008 N=1 → slice-012 N=2 cumulative; ratchets the "ZERO-build-deviation slice via strong /critique" pattern).
- **Slice-011's `test_plugin_yaml_version_matches_version_file_at_0_26_0` superseded by slice-012's `test_plugin_yaml_version_matches_version_file_at_0_27_0`** — slice-011 added a versioned PMI-1 gate test pinning plugin.yaml.version at 0.26.0; slice-012's atomic version bump to 0.27.0 makes the 0.26.0 gate stale (it would always fail post-bump). Replaced + added new 0.27.0 gate. `test_methodology_changelog.py`: 8 tests → 9 tests (net +1: entry-pin added; gate superseded). **PMI-1 versioned-gate supersession N=5 events stable**.
- **`architecture/shippability.md` row 11 (slice-011) updated to drop the deleted `test_plugin_yaml_version_matches_version_file_at_0_26_0` reference** — row 11's command no longer cites the removed test; slice-011's other claims (RSAD-1 sub-clause + canonical phrase pin + CAD-1 byte-equality + v0.26.0 entry-pin) still hold. Row 11's prose now notes the supersession ("slice-012 superseded the 0.26.0 PMI-1 version-gate test with the 0.27.0 gate at slice-012 row 12") for forensic traceability. Row 12 (slice-012) added with 6 critical-path tests.
- **Methodology-changelog version 0.26.0 → 0.27.0** — `methodology-changelog.md` (in-repo + installed) gained a new H2 entry `## v0.27.0 — 2026-05-13` documenting BC-1 v1.3 (data migration, not schema-extension).

## Discovered

- **ZERO-build-deviation slice via strong /critique N=2 cumulative** (slice-008 + slice-012) — slice-008's reflection flagged this as N=1 with the watch language "Watch for recurrence at slice-009+; if pattern holds at N=2, design-stage Critic ROI metric should track design-stage catches AS A PERCENTAGE of total findings, not just total slices." Slice-009/010/011 each had 1-2 build-time DEVIATIONs (DEVIATION-1/2 at slice-009, DEVIATION-3 at slice-010, build-time PMI-1 gate-conflation at slice-011). **Slice-012 ratchets to N=2 cumulative**. Promote to tracked methodology metric: % of Critic findings caught at /critique (vs at build) is the design-stage Critic ROI ratio. Slice-012's ratio is 5/5 = 100% design-stage; slice-008's was 6/6 = 100% design-stage. Promote at slice-013+ via /critic-calibrate prompt addition: "Cross-cutting tooling slices should track design-stage-catch percentage as a recurrence metric."
- **Slice-011 NEW Dim 9 sub-class candidate (entry-pin-vs-PMI-1-gate-semantics-conflation) N=2 promotion threshold MET at slice-012**. Slice-011 N=1: caught at /validate-slice Step 5.5 by shippability catalog row 10 fail; fixed in-line. Slice-012 N=2: design-time codification via /critique M1 ACCEPTED-FIXED (Phase 1b INSERT discipline + Phase 1c narrow-scope Edit discipline + Audit 6 structural-separation empirical verification); validate-time empirical confirmation. **Promote at slice-013+** via /critic-calibrate prompt addition OR a dedicated Dim 9 sub-class refinement slice (`refine-dim-9-with-entry-pin-vs-pmi-1-gate-sub-class`). Generic methodology lesson worth codifying at /critique skill prose: "When superseding a PMI-1 versioned-gate test, the Edit `old_string` MUST target ONLY the gate function body + its dedicated SECTION header — never span any entry-pin function above. Entry-pin functions persist across all versions; only PMI-1 versioned-gates supersede with latest-only."
- **Recursive-self-application N=4 cumulative across slices 9-12** post-RSAD-1 codification at slice-011. Slice-009 M2 (design-time) + slice-010 (design-time × 3 + build-time DEVIATION-3) + slice-011 (B1+M1+M2+M3 on own draft) + **slice-012 (B1 on own draft)**. Slice-012's B1 is particularly elegant: the slice authoring "data-only migration of a Negative anchors line" had its own AC #4 verification approach under-scoped because slice-008's same tokens already populated BC-PROJ-1's `architecture/build-checks.md:20` line. The slice's verification design didn't account for the pre-existing slice-008 data IN THE SAME FILE. **RSAD-1 codification empirically load-bearing at slice-012** — without the Critic agent's adversarial prompt explicitly stress-testing cross-cutting tooling slices' draft prose against the very discipline being encoded, B1 might have been Critic-MISSED. Awaiting N=2 cross-slice recurrence at slice-013+ for "promote-RSAD-1-to-audit-enforced-gate" consideration (currently RSAD-1 is /critique-time prose-only per slice-010 B5 -D suffix convention).
- **N-surface schema-pin discipline N=4 stable** (slice-008 `Negative anchors` + slice-009 `design.md mechanical tables` + slice-010 `In-house methodology surfaces` + slice-011 `Recursive self-application discipline` + slice-012 `BC-PROJ-2 negative-anchor migration`). The canonical phrase is selected from the entry's title; pinned across N=2 surfaces (in-repo + installed methodology-changelog). Discipline is empirically load-bearing across 5 consecutive slices. Promote to /critique skill prose at N=5 if a sixth instance surfaces at slice-013+ — current convention is implicit and codified only in reflection lessons.
- **Cross-cutting-conformance Dim 9 catch rate trajectory N=7 evidence range-bound 60-100% confirmed stable** at slice-012. 0% (slices 1-5) → 25% (slice-006) → 60% (slice-007) → 100% (slice-008) → 60% (slice-009) → 87.5% (slice-010) → 80% (slice-011) → **80-100% at slice-012** (4 of 4 Dim 9 sub-class hits caught excluding M2; 4 of 5 including M2). Slice-008's strict-monotonic claim falsified at slice-009 and confirmed range-bound across slice-010 + slice-011 + slice-012 (N=4 evidence of range-bound behavior, range never below 60% post-CCC-1).
- **VAL-1 Layer B intra-repo `tests` package false-positive class N=10 cumulative recurrence** — every slice 003-012 hits it; handled cleanly via `--imports-allowlist tests` per slice-003 SKILL.md prose pin. v2 candidate `[tool.pytest.ini_options]` testpaths auto-allow STILL deferred at slice-012 — cumulative friction remains ~1 sec per validate-slice invocation; not justifying a dedicated slice. **Threshold consideration**: N=10 is significant; promote to a slice-013+ candidate `fix-val-1-layer-b-intra-repo-tests-namespace-via-pytest-testpaths` if /critic-calibrate flags as recurring noise pattern.

## Deferred

- **`/critic-calibrate` next-run effectiveness check overdue and approaching window edge** — cumulative cross-cutting misses 10 across slices 6-11 + 0 at slice-012 = **10 across slices 6-12** (5× the ≤2 target across slices 6-15; **3 slices remaining in window** for the target to be met = 3 PERFECT slices required which is improbable). **/critic-calibrate at slice-013-015 boundary**, per slice-009/010/011/012 reflections. Recommendation: (a) recalibrate quantitative target downward (e.g., "≤5 cumulative misses across the window") OR (b) refine Critic prompt with NEW sub-class candidates ratcheted at slice-012 (entry-pin-vs-PMI-1-gate-semantics-conflation N=2 promotion-met + design-stage-catch-percentage tracking metric).
- **`refine-dim-9-with-entry-pin-vs-pmi-1-gate-sub-class`** — N=2 promotion threshold MET. ~30 min skill scope. Mirrors slice-009 / slice-010 / slice-011 Dim 9 sub-class refinement pattern (refine-dim-9-with-design-md-tables-sub-clause; promote-voluntary-critic-on-cross-cutting-to-slice-default-heuristic; promote-recursive-self-application-discipline-to-critique-skill-prose). Add Dim 9 sub-class refinement: "When superseding a PMI-1 versioned-gate test via Edit-tool `old_string`/`new_string`, scope the Edit block to ONLY the gate function body + its dedicated SECTION header — never span any entry-pin function. Entry-pin functions persist across all versions; only PMI-1 versioned-gates supersede with latest-only." **Strongest slice-013+ candidate alongside /critic-calibrate**.
- **`refactor-pmi-1-gate-to-version-agnostic-shape`** — friction threshold N=5 met at slice-012 (was N=4 at slice-011). Per-bump cost remains ~1 min trivial; defer one more cycle UNLESS version-bump churn becomes friction OR the user explicitly requests refactor. The N=5 ratchet at slice-012 adds one more data point but doesn't change the cost calculus.
- **RSAD-1 v2 audit tooling (`tools/rsad_1_audit.py`)** — deferred at slice-011 to stay within 0.5-day budget; not changed at slice-012. Recursive-self-application N=4 cumulative makes audit-enforced gate more compelling; promote to slice-014+ candidate if /critic-calibrate flags pattern continuation.
- **MCT-1 v2 audit tooling (`tools/mct_1_audit.py`)** — deferred at slice-010; not changed at slice-012. Mandatory-Critic-trigger glob match self-applied cleanly at slice-011 + slice-012 (validate-using-your-own-ship works); no audit needed yet.
- **`fix-val-1-layer-b-intra-repo-tests-namespace-via-pytest-testpaths`** — N=10 cumulative recurrence at slice-012. Still not load-bearing enough to slice; cumulative friction is ~10 seconds across 10 slices = ~1 sec per slice. Promote at /critic-calibrate prompt review if the threshold of meaningful-friction emerges.
- **R-1 deeper fix** (orchestrator pre-cd or parent-thread pre-compute for /diagnose) — HIGH-band open risk; documented-constraint workaround acceptable per slice-002; no slice-012 change.
- **R-2 programmatic test of cwd-warning runtime emission** — LOW-band open risk; defer until evidence of regression.
- **`fix-bc-1-archived-slice-heuristic`** (slice-005 carryover; pre-existing `audit_slice` `parent.parent` heuristic broken for archived paths) — workaround `--project-checks` explicit acceptable; ~30 min slice; still deferred at slice-012.
- **`add-csp-1-docstring-or-regex`** (slice-004 carryover) — sibling tooling-bug pattern; ~30-60 min slice.
- **BC-1 morphological-variant expansion** (`Critic-MISSED` / `critic-misses` / `Critic miss` variants) — still no recurrence at slice-012. Defer to v2.
- **Slice-011 deferrals carried into slice-012's deferred set**: all carried items above were also deferred at slice-011; no change in priority.

## Critic calibration

Per **TRI-1**, scoring each finding from `critique.md` `## Triage` table against build + validate reality:

- **B1** (AC #4 test-first row structurally unable to produce TF-1 genuine PENDING → WRITTEN-FAILING; 9 tokens pre-exist on BC-PROJ-1's L20 line): **VALIDATED** — disposition ACCEPTED-FIXED at /critique; reality confirmed empirically at /build-slice Phase 1a (pre-fix `AssertionError: BC-PROJ-2 negative_anchors mismatch: got (), expected (...)` matches the Critic's pinned signal EXACTLY) AND at /validate-slice AC #4 (BC-PROJ-2 tuple = 9-token canonical set, zero parse violations). Without B1 catch, the slice would have shipped with a "PASSING" AC #4 test that PASSED pre-migration due to BC-PROJ-1's L20 line containing all 9 tokens — a fundamentally broken regression-guard. **Strongest VALIDATED + recursive-self-application catch** at slice-012. Critic was right; pre-emptive; saved a TF-1 genuineness escape. **Recursive-self-application Dim 9 sub-clause 6 hit** (RSAD-1 codification at slice-011 N=1 standalone evidence ratchet).
- **M1** (ADR-011 Reversibility under-counted supersession surface; Phase 1b INSERT + Phase 1c narrow-scope discipline under-specified for slice-011 NEW Dim 9 sub-class N=1): **VALIDATED** — disposition ACCEPTED-FIXED at /critique; reality confirmed at /build-slice Phase 1b/1c (v0.26.0 RSAD-1 entry-pin function `test_v_0_26_0_rsad_1_entry_present_in_repo_and_installed` STILL PASSES post-PMI-1 supersession; all prior versions' entry-pin functions preserved) AND at /validate-slice AC #5 + shippability row 11. **N=2 promotion probe SUCCESS** for the slice-011 NEW Dim 9 sub-class candidate. Without M1 catch, the supersession Edit might have spanned the entry-pin function's SECTION header (slice-011 DEVIATION-1 pattern recurrence) → silent deletion of v0.26.0 entry-pin → shippability row 11 fail mid-build (slice-011 DEVIATION-1 recurrence). **Strongest preventive save** at slice-012. Critic was right; design-stage Critic ROI confirmed. **Dim 9 sub-clause 1 (methodology-audit conformance) + new sub-class hit** (entry-pin-vs-PMI-1-gate-conflation N=2).
- **M2** (Audit 3 inflated empirical counts: claimed 7/47, actual 5/8 distinct-token matches): **VALIDATED** — disposition ACCEPTED-FIXED at /critique; reality confirmed at /build-slice + /validate-slice (5 + 8 distinct-token counts reproduced exactly by the audit). Without M2 catch, design.md would have shipped numerically-unreproducible audit claims, violating Wiegers AC-trace sub-class discipline (slice-008 N=1 stable; slice-012 doesn't recur the AC-trace violation by virtue of M2's catch). **Slice-008 Wiegers AC-trace sub-class N=1 stable** — no recurrence at slice-012 (Critic's M2 catch prevented it). Generic methodology lesson: design-time empirical claims should match what the verifier reproducibly produces. Critic was right; preventive.
- **M3** (ADR-007 vault-ref filename drift + ADR-011 filename overly long): **VALIDATED** — disposition ACCEPTED-FIXED at /critique; reality confirmed at /build-slice + /validate-slice (mission-brief now correctly references `ADR-007-bc-1-negative-context-anchors-via-final-filter`; ADR-011 renamed to 45 chars). Without M3 catch, the graphify reachability set would have a dangling vault-ref and a non-conforming ADR-naming-convention drift. **Pre-existing Dim 9 sub-class strengthened**: tooling-doc-vs-implementation parity at the vault-ref surface (slice-009 design.md mechanical tables vs canonical inventories sub-class N=2 stable; slice-012 extends to vault-ref filenames). Critic was right; preventive.
- **m1** (canonical phrase pin per N-surface schema-pin discipline): **VALIDATED** — disposition ACCEPTED-FIXED at /critique; reality confirmed at /validate-slice AC #5 part 1 (`BC-PROJ-2 negative-anchor migration` canonical phrase present in BOTH in-repo + installed methodology-changelog). **N-surface schema-pin discipline N=3 → N=4 stable ratchet** at slice-012. Without m1 catch, the entry-pin test would have devolved to rule-ID-substring-presence only (less drift-resistant). Cosmetic; preventive. Critic was right; pre-existing Dim 9 sub-class strengthened (N-surface schema-pin from slice-008 M2 generalized to N=4-stable single-phrase pin shape at slice-012).

**All 5 Critic findings VALIDATED. 0 FALSE-ALARM. 0 OVERRIDE-MISJUDGED. 0 NOT-YET.** **Sixth consecutive 100% Critic accuracy slice** (slice-006: 11/11; slice-007: 8/8; slice-008: 6/6; slice-009: 11/11; slice-010: 11/11; slice-011: 7/7; **slice-012: 5/5**). Running total: **59/59 across slices 6-12** — strongest streak in the project, extending slice-011's 54/54 by +5.

**Missed by Critic**: **NONE at slice-012 /critique time.** Zero reality surprises at /validate-slice; zero build-time DEVIATIONs at /build-slice; zero items surfaced during build/validate that the Critic didn't predict. **Second-cleanest Critic performance in the project** (slice-008 was first with 6/6 + 0 DEVIATIONs; slice-012 ratchets to N=2 cumulative).

**Pattern (cumulative across slices 001-012)**:

Cross-cutting-conformance miss-class:
- slice-001..005 (8-dim Critic): **0% catch rate**, 8 sub-class hits, 8 misses
- slice-006 (8-dim Critic, post-CCC-1 surgical sub-bullets): **25% catch rate**, 4 sub-class hits, 1 caught
- slice-007 (9-dim Critic, CCC-1 v1): **60% catch rate**, 5 sub-class hits, 3 caught
- slice-008 (9-dim Critic, CCC-1 v1): **100% catch rate**, 3 sub-class hits, 3 caught (M1+M2+M3)
- slice-009 (9-dim Critic, CCC-1 v1.1): **60% catch rate**, 5 sub-class hits, 3 caught (2 build-time DEVIATIONs)
- slice-010 (9-dim Critic, CCC-1 v1.1): **87.5% catch rate**, 8 sub-class hits, 7 caught (1 build-time DEVIATION-3)
- slice-011 (9-dim Critic + RSAD-1 codified at end): **80% catch rate**, 5 sub-class hits, 4 caught (1 build-time slip at PMI-1 gate)
- **slice-012 (9-dim Critic + RSAD-1 codified): 80-100% catch rate, 4 of 4 sub-class hits caught (excluding M2 unfounded-assumption catch; including M2 = 4 of 5 = 80%); 0 build-time DEVIATIONs**

**Range-bound 60-100% on N=7 evidence stable.** Slice-008's strict-monotonic claim falsified at slice-009; confirmed range-bound at slice-010/011/012. Slice-006-15 effectiveness target (≤2 cross-cutting misses across the window) **exceeded by 5× at slice-012**: cumulative 10 missed across slices 6-12; 3 slices remaining in window. **`/critic-calibrate` at slice-013-015 boundary mandatory** to recalibrate target OR refine Critic prompt with slice-012's NEW sub-class candidates.

**Voluntary Critic on cross-cutting tooling slices is now N=11/11 paid off** (codified as MCT-1 at slice-010; auto-applied at slice-011 + slice-012). Slice-012's 5-for-5 catch rate is design-stage; methodology pattern is load-bearing AND empirically strengthening.

## Lessons for next slice

- **From slice-012 strongest candidate**: `/critic-calibrate` itself — 4 slices remaining in slice-006-15 window; cumulative cross-cutting misses 10 = 5× target. Per slice-009/010/011/012 reflections. Per `~/.claude/skills/critic-calibrate/SKILL.md`: walks reflections 6-12, classifies misses, proposes Critic prompt additions; human reviews + manually applies. ~1 hour invocation cost. Strongest aggregated-lesson promotion candidate at this point in the project.
- **From slice-012 second-strongest candidate**: `refine-dim-9-with-entry-pin-vs-pmi-1-gate-sub-class` — N=2 promotion threshold MET at slice-012 (slice-011 N=1 + slice-012 N=2 codification). ~30 min skill scope. Mirrors slice-009/010/011 Dim 9 sub-class refinement pattern. Codifies the Edit-discipline lesson: scope `old_string` to gate function body + dedicated SECTION header only; never span entry-pin functions.
- **All 5 Critic findings VALIDATED at slice-012** — sixth consecutive 100% Critic accuracy slice (running 59/59 across slices 6-12; strongest streak). The 9-dim Critic with surgical sub-bullets + recursive-self-application discipline (RSAD-1 codified at slice-011) + voluntary Critic on cross-cutting tooling (MCT-1 codified at slice-010) is hardening as load-bearing methodology. **Recursive-self-application N=4 cumulative across slices 9-12 — RSAD-1 codification empirically load-bearing**.
- **ZERO-build-deviation slice via strong /critique N=2 cumulative** (slice-008 + slice-012; slice-008 reflection's N=2 watch-language threshold MET). Promote at slice-013+: design-stage Critic ROI ratio (% of Critic findings caught at /critique vs at build) as a tracked methodology metric. Slice-008 = 6/6 = 100% design-stage; slice-012 = 5/5 = 100% design-stage.
- **Slice-011 NEW Dim 9 sub-class candidate (entry-pin-vs-PMI-1-gate-semantics-conflation) N=2 promotion threshold MET at slice-012** — promote to Dim 9 sub-class refinement at slice-013+ via /critic-calibrate or dedicated refinement slice.
- **N-surface schema-pin discipline N=4 stable** (canonical phrase `BC-PROJ-2 negative-anchor migration` mirrors slice-008/009/010/011 precedents). Promote to /critique skill prose at N=5 if a sixth instance surfaces at slice-013+ — convention is currently implicit and codified only in reflection lessons.
- **PMI-1 versioned-gate supersession N=5 events stable** (slice-007 introduced → slice-008/009/010/011/012 superseded). Refactor candidate (`refactor-pmi-1-gate-to-version-agnostic-shape`) deferred 6 times across slices 007/008/009/010/011/012; ~1 min churn per supersession, not yet at meaningful-friction threshold.
- **Bidirectional sha256 forensic capture is N=8 stable** (slice-005..012). Canonical methodology practice.
- **Validate-using-your-own-ship N=10 stable** (slice-003..012). Slice-012 self-applies BC-PROJ-2 migration to its own ship with 9-of-9 over-determination. Canonical methodology practice.
- **Empirical-verification-at-design-time discipline N=11 stable** (slice-003..012). Slice-012 ran SIX design-time audits (disjointness + slice-001 backward-compat + slice-005/011 suppression + algorithm-path-conformance + RSAD-1 build-time recursive-self-application + entry-pin-vs-PMI-1-gate structural-separation). All 6 predictions VALIDATED at /build-slice. Zero recurrence of slice-009/010/011 build-time DEVIATIONs at slice-012.
- **VAL-1 Layer B false-positive class on intra-repo `tests` package N=10 cumulative recurrence at slice-012** — every slice 003-012 hits it. v2 candidate threshold remains "not load-bearing enough" — cumulative friction ~1 sec per slice. Escalation consideration if /critic-calibrate flags as recurring noise pattern.
- **Mini-CAD-1 row 3 PASSING → WRITTEN-FAILING → PASSING transition pattern N=5 stable** (slice-007 + slice-009 + slice-010 + slice-011 + slice-012). Generic methodology lesson worth tracking as canonical practice for regression-guard test transitions.
- **TF-1 PENDING → WRITTEN-FAILING transition needs genuine failure — N=9 stable** (slice-003..012). All 6 slice-012 rows had pinned failure signals empirically validated.
- **From slice-012 next-strongest candidate** (defer-with-rationale possibility): `fix-val-1-layer-b-intra-repo-tests-namespace-via-pytest-testpaths` — N=10 cumulative recurrence threshold. ~30 min scope. Promote at /critic-calibrate prompt review if friction crosses meaningful threshold.

## Vault updates made (thin vault — small list)

- [[methodology-changelog.md]] (in-repo + installed) — appended `## v0.27.0 — 2026-05-13` BC-PROJ-2 negative-anchor migration entry; substantive canonical phrase `BC-PROJ-2 negative-anchor migration` pinned across N=2 surfaces (in-repo + installed); bidirectional byte-equal at sha256 `33423327EB101F26`
- [[architecture/build-checks.md]] — BC-PROJ-2 rule body gained `Negative anchors:` line with 9-token methodology-vocabulary set (slice-008 BC-PROJ-1 / BC-GLOBAL-1 set reused verbatim per rule-agnostic uniformity)
- [[architecture/decisions/ADR-011-bc-proj-2-negative-anchor-migration]] — new ADR; reversibility: cheap (~10 min revert); supersedes: null; extends ADR-007 by completing BC-PROJ-2 deferral
- [[architecture/shippability.md]] — appended row 12 (slice-012 critical path: 6 tests); updated row 11 (slice-011) to drop deleted `_at_0_26_0` reference + note slice-012 PMI-1 gate supersession
- VERSION + ~/.claude/ai-sdlc-VERSION + plugin.yaml.version — atomic 0.26.0 → 0.27.0 (PMI-1 invariant)
- tests/methodology/test_build_checks_audit.py — 4 new audit tests + new `_SLICE_011_CHANGED_FILES` constant + new SECTION header
- tests/methodology/test_methodology_changelog.py — 1 new entry-pin function (`test_v_0_27_0_bc_proj_2_entry_present_in_repo_and_installed`) under NEW `# --- Slice-012 / BC-PROJ-2 entry pinning ---` SECTION header + PMI-1 gate renamed `_at_0_26_0` → `_at_0_27_0` with body updated + SECTION header renamed

**Risk register unchanged** at slice-012 — no new R-N entries; R-1 + R-2 (/diagnose-related) unchanged. **Concept unchanged** at slice-012 — methodology-internal slice, no concept-scope delta.
