# Slice 011: promote-recursive-self-application-discipline-to-critique-skill-prose

**Mode**: Standard
**Estimated work**: 0.5 day (~30 min Critic agent prose update + tests + methodology-changelog entry + atomic version bump)
**Risk retired**: MEDIUM — promotes the strongest aggregated-lesson promotion candidate from slice-010's reflection record: recursive-self-application phenomenon at **N=3 cumulative evidence** across two distinct phases. Slice-009 M2 first surfaced the phenomenon at /critique design-time (the Critic caught slice-009's own design.md committing the exact class of design-doc-vs-canonical-inventory drift the slice was encoding into the Critic prompt — N=1). Slice-010 ratcheted to N=3 across two new instances: (a) /critique design-time stress-test catches B1 + M1 + B5 against slice-010's own draft (N=2 cumulative); (b) /build-slice DEVIATION-3 build-time recursive-self-application via /critique B4 fix prose empirically describing BC-PROJ-2's anchors (`fence`, `code-block`, `llm`) RE-INTRODUCING those substrings into mission-brief + design.md, triggering BC-PROJ-2's anchor path at Phase 4 BC-1 self-application audit (Critic-MISSED at /critique; surfaced at build — N=3 cumulative). Per slice-009 + slice-010 reflection language ("Promote `recursive-self-application-discipline` to /critique skill prose at N=3 if a third instance surfaces"): the N=3 promotion threshold is now met. Without codification, the discipline remains tacit reflection-only knowledge; future cross-cutting tooling slices won't benefit from the Critic agent's adversarial prompt explicitly stress-testing the slice's own draft against the very discipline being encoded, NOR from the explicit build-time recursive-self-application sub-mode that catches /critique fix prose introducing anchor substrings into the slice's artifacts. Slice-011 codifies the discipline into `agents/critique.md` Dim 9 as a new 6th sub-clause, with both sub-modes (design-time + build-time-via-/critique-fix-prose) explicitly distinguished and cross-slice anchored to slice-009 + slice-010. Authored as new methodology rule **RSAD-1** ("Recursive Self-Application Discipline"; -D suffix signals discipline / prose-heuristic semantics distinct from audit-enforced gate semantics of siblings BC-1 / PMI-1 / CAD-1 / TF-1 / RR-1 / INST-1 / VAL-1 / WIRE-1 / NFR-1 / CSP-1 — extending the B5 calibration-trail discipline convention slice-010 introduced for MCT-1's -T- suffix) in `methodology-changelog.md` v0.26.0, with bidirectional in-repo + installed pin per slice-008 + slice-009 + slice-010 N-substring + N-surface schema-pin discipline. v0.26.0 entry carries a Limitations note explicitly acknowledging RSAD-1 is /critique-time adversarial-prompt heuristic prose, NOT an audit-enforced gate (no `tools/rsad_1_audit.py` exists; v2 candidate at slice-N+ if drift surfaces). The slice itself is in the recursive-self-application class: slice-011 IS the slice authoring the discipline AND modifying `agents/critique.md` — exactly the in-house methodology surface RSAD-1's discipline governs — so the Critic at /critique SHOULD be expected to find rule-class violations in slice-011's own draft prose (meta-recursive self-application at the methodology-creation level, mirroring slice-010's MCT-1 self-application audit precedent).
**Test-first**: true
**Walking-skeleton**: false
**Exploratory-charter**: false

## Intent

Add a new 6th sub-clause titled `Recursive self-application discipline` to `agents/critique.md` Dimension 9 ("Cross-cutting conformance") after the existing `Language-version conformance` sub-clause and before the `### Bonus: weak graph edges` H3 anchor. The sub-clause body names BOTH sub-modes of the phenomenon: (a) **design-time mode** — the Critic stress-tests the slice's own draft (mission-brief + design.md + ADRs) against the very discipline being encoded, EXPECTING rule-class violations in the slice's own prose; (b) **build-time-via-/critique-fix-prose mode** — when /critique fix prose empirically describes a build-check rule's anchors / triggers (substrings, regex patterns, keyword lists), the prose itself may RE-INTRODUCE those triggers into the slice's mission-brief / design.md, firing the rule at /build-slice Phase 4 self-application audit. Sub-clause carries TWO+ concrete cross-slice example anchors grounded in the project's N=3 reflection record (slice-009 M2 + slice-010 design-time stress-test catches B1/M1/B5 + slice-010 build-time DEVIATION-3 BC-PROJ-2 recursive fire via /critique B4 fix prose). Codifies the slice-009 + slice-010 reflections' explicit promotion-at-N=3 recommendation. Authored as new methodology rule **RSAD-1** in `methodology-changelog.md` v0.26.0, with TWO-surface (in-repo + installed) + N-substring + N-surface schema-pin discipline per slice-008/009/010 lineage. v0.26.0 entry carries Limitations note acknowledging RSAD-1 is /critique-time heuristic prose only, NOT an audit-enforced gate. PMI-1 atomic version bump 0.25.0 → 0.26.0 with versioned-gate supersession (N=4 supersession event post-completion: slice-007 introduced `_at_0_22_0` → slice-008 first-superseded → slice-009 second-superseded → slice-010 third-superseded → slice-011 fourth-supersedes).

## Acceptance criteria

1. After this slice ships, `agents/critique.md` Dim 9 ("Cross-cutting conformance") contains a new sub-clause bullet whose title is the canonical literal `Recursive self-application discipline` (case-sensitive substring), inserted AFTER the existing `Language-version conformance` sub-clause and BEFORE the `### Bonus: weak graph edges` H3 anchor (location-pinned within the Dim 9 section bounds). The Dim 9 structural-invariant test supersedes 5-sub-clauses → 6-sub-clauses (per slice-010 PMI-1 versioned-gate supersession analogy applied at the structural-invariant level): existing `test_critique_dim_9_lists_five_sub_clauses` REPLACED (not added alongside) by `test_critique_dim_9_lists_six_sub_clauses` which asserts ALL 6 sub-clause-title substrings present in CRITIQUE (the 5 existing + `Recursive self-application discipline`). No two structural-invariant tests coexist.

2. The new sub-clause body names BOTH sub-modes distinctly per N=3 evidence (slice-009 M2 design-time + slice-010 design-time stress-test + slice-010 build-time DEVIATION-3). Contains BOTH canonical substrings `design-time` AND `build-time` (case-sensitive lowercase-d / lowercase-b in body prose form — placed mid-sentence to pre-empt slice-009 DEVIATION-1 case-sensitivity-at-sentence-start trap). Plus at least one sub-mode anchor for each mode (e.g., design-time anchor `stress-test` or equivalent describing Critic-stress-tests-own-draft; build-time anchor `/critique fix prose` or `fix prose introduces` describing prose-introduces-trigger-at-build).

3. The new sub-clause body cites BOTH cross-slice anchors `slice-009` AND `slice-010` (strict-both per slice-011 Critic m2 — with 2-element allowlist the `≥2 of 2` semantics is strict-both not permissive) AND at least one of the sub-class-instance anchors `M2`, `DEVIATION-3`, `BC-PROJ-2`, `B1`, `B5`, `M1` (case-sensitive). Per slice-008 M2 + slice-009 M3 + slice-010 M3 N-substring + N-surface schema-pin discipline.

4. After this slice ships, `~/.claude/agents/critique.md` is byte-equal to in-repo `agents/critique.md` (CAD-1 invariant per slice-007 introduction). Existing test `tests/methodology/test_critique_agent_drift.py::test_in_repo_and_installed_critique_agent_are_content_equal` transitions PASSING → WRITTEN-FAILING → PASSING (per slice-007 mini-CAD-1 row + slice-009 row 3 + slice-010 row 3 precedent at N=3 stable). Test starts at TF-1 status PASSING (file pair byte-equal at slice start per Phase 0 sha256 capture); transitions PASSING → WRITTEN-FAILING at Phase 2 (in-repo edited, installed not yet synced — observed at Phase 2b mid-slice smoke as `RuntimeError: sha256 mismatch`); transitions WRITTEN-FAILING → PASSING at Phase 2c (post-forward-sync). Build-log Phase 0 + Phase 4 capture bidirectional sha256 forensic evidence per N=6 stable pattern.

5. After this slice ships, `methodology-changelog.md` (BOTH in-repo and installed at `~/.claude/methodology-changelog.md`) contains a new `## v0.26.0 — <YYYY-MM-DD>` entry naming new methodology rule **RSAD-1** ("Recursive Self-Application Discipline"). The bidirectional changelog-pin test asserts the substantive canonical phrase `Recursive self-application discipline` AND the rule reference `RSAD-1` are present in BOTH copies (per slice-008/009/010 N-surface schema-pin discipline — ONE canonical phrase pinned across N=3 surfaces: `agents/critique.md` sub-clause title + in-repo methodology-changelog.md + installed methodology-changelog.md). The entry carries explicit Limitations note (mirrors slice-010 MCT-1 v0.25.0 prose) acknowledging RSAD-1 is /critique-time adversarial-prompt heuristic prose only, NOT an audit-enforced gate (no `tools/rsad_1_audit.py`; v2 candidate at slice-N+ if drift surfaces — would build a static check walking `architecture/slices/*/critique.md` and asserting cross-cutting tooling slices' Critic actually probed the slice's own artifacts for rule-class violations). PMI-1 atomic version bump: `VERSION` + `~/.claude/ai-sdlc-VERSION` + `plugin.yaml.version` all bump 0.25.0 → 0.26.0; PMI-1 versioned-gate supersession: slice-010's `_at_0_25_0` REPLACED (not added alongside) by `_at_0_26_0` per N=3-stable supersession pattern from slice-007..010 (slice-011 creates N=4 supersession events on completion). No two version-gates coexist.

## Test-first plan

(per **TF-1**, `methodology-changelog.md` v0.13.0)

Each AC maps to one or more failing tests written BEFORE implementation. Statuses progress PENDING -> WRITTEN-FAILING -> PASSING through the slice lifecycle. `/build-slice` Step 6 (pre-finish) runs `tools/test_first_audit.py --strict-pre-finish` and refuses if any row is non-PASSING.

| AC | Test type | Test path | Test function | Status |
|----|-----------|-----------|---------------|--------|
| 1 | unit | tests/methodology/test_critique_agent.py | test_critique_dim_9_lists_six_sub_clauses | PASSING |
| 1 | unit | tests/methodology/test_critique_agent.py | test_critique_dim_9_recursive_self_application_sub_clause_present | PASSING |
| 1 | unit | tests/methodology/test_critique_agent.py | test_critique_dim_9_recursive_self_application_location_pinned | PASSING |
| 2 | unit | tests/methodology/test_critique_agent.py | test_critique_dim_9_recursive_self_application_names_both_sub_modes | PASSING |
| 3 | unit | tests/methodology/test_critique_agent.py | test_critique_dim_9_recursive_self_application_cites_at_least_two_cross_slice_anchors | PASSING |
| 4 | integration | tests/methodology/test_critique_agent_drift.py | test_in_repo_and_installed_critique_agent_are_content_equal | PASSING |
| 5 | unit | tests/methodology/test_methodology_changelog.py | test_v_0_26_0_rsad_1_entry_present_in_repo_and_installed | PASSING |
| 5 | unit | tests/methodology/test_methodology_changelog.py | test_plugin_yaml_version_matches_version_file_at_0_26_0 | PASSING |

**Notes**:

- TF-1 PENDING -> WRITTEN-FAILING transitions MUST be genuine per slice-003..010 lesson (N=7 stable). Empirical pre-design grep on `agents/critique.md` confirmed ZERO pre-existing matches for canonical literal `Recursive self-application discipline` (so AC #1 row 2 PENDING → WRITTEN-FAILING transition will be authentic). Confirmed ZERO pre-existing matches for sub-class anchor substring `DEVIATION-3` AND `M2` in the Dim 9 section bounds (so AC #3 cross-slice-anchor test PENDING → WRITTEN-FAILING will be authentic; both substrings appear elsewhere in the agent file but NOT within the Dim 9 sub-clause bounds — location-pinned scoped grep verified). Pin specific failure signals:
  - AC #1 row 1 `lists_six_sub_clauses` fails pre-fix with `AssertionError: 'Recursive self-application discipline' not in CRITIQUE` (the 6th sub-clause-title substring is absent). The five existing substrings remain unchanged from `lists_five_sub_clauses`.
  - AC #1 row 2 `sub_clause_present` fails pre-fix with `AssertionError: 'Recursive self-application discipline' not in CRITIQUE` (canonical literal absent in any sub-clause body).
  - AC #1 row 3 `location_pinned_between_language_version_and_bonus` per slice-009 Critic M1 + slice-010 M1 location-pin convention + slice-009 DEVIATION-2 + slice-010 anchor-uniqueness pre-emption: scoped section search using UNIQUE start anchor `Language-version conformance` (verified unique in `agents/critique.md` at design time via grep) and UNIQUE end anchor `### Bonus: weak graph edges` (verified unique). Test impl uses `text.find("### Bonus: weak graph edges", text.find("Language-version conformance"))` to scope the search; this avoids slice-009 DEVIATION-2 `.find()`-collision class. AC #1 row 3 specifically asserts the new sub-clause title falls BETWEEN these two anchors so the sub-clause's position is precisely pinned.
  - AC #2 `names_both_sub_modes` fails pre-fix with `AssertionError: 'design-time' or 'build-time' not in sub-clause body` (neither canonical substring present in the absent sub-clause body). Test scoped to the Dim 9 → Recursive-self-application sub-clause bounds via slice-009 DEVIATION-2 mitigation (scoped `.find()`).
  - AC #3 `cites_at_least_two_cross_slice_anchors` fails pre-fix with `AssertionError: insufficient cross-slice anchors — need ≥2 of {slice-009, slice-010} in the sub-clause body` (none present pre-fix in the absent sub-clause body). Sub-class instance anchor (M2 / DEVIATION-3 / BC-PROJ-2 / B1 / B5 / M1) asserted as ≥1 of 6 — count ≥ 1.
  - AC #4 starts at TF-1 status PASSING (file pair byte-equal at slice start per Phase 0 sha256 capture; mirrors slice-007/009/010 row 3 precedent). The genuine WRITTEN-FAILING moment is Phase 2 (in-repo edited; installed not yet forward-synced) observed at the Phase 2b mid-slice smoke gate run with `RuntimeError: sha256 mismatch`. Transitions through PASSING (Phase 1b) → WRITTEN-FAILING (Phase 2) → PASSING (Phase 2c post-forward-sync); the WRITTEN-FAILING transition is the slice's actual implementation work (the in-repo edit), per slice-007/009/010 mini-CAD-1 row precedent at N=3 stable.
  - AC #5 row 1 fails pre-fix because the v0.26.0 entry does not exist in either changelog copy.
  - AC #5 row 2 fails pre-fix because `plugin.yaml.version` and `VERSION` are still `0.25.0` (slice-010's bumped state).
- Per slice-009 DEVIATION-1 (case-sensitivity in canonical-literal pin sub-class) + slice-010 pre-emption discipline: the canonical literal `Recursive self-application discipline` is capitalized R to honor bullet-title sentence convention (mirrors `Methodology-audit conformance`, `Tooling-doc-vs-implementation parity`, etc. bullet-title shape). The body-prose canonical literals `design-time` AND `build-time` are lowercase compound forms; the sub-clause body MUST be structured so these phrases appear mid-sentence rather than at any sentence start that would force capitalization to `Design-time` / `Build-time`. Mitigation pre-empts slice-009 DEVIATION-1 recurrence.
- Per slice-005 + slice-009 algorithm-path-conformance discipline generalized to substring-search test impl (slice-009 DEVIATION-2 + slice-010 pre-emption sub-class): the location-pin test (AC #1 row 3) uses scoped `.find()` with start_idx from a prior unique anchor (`Language-version conformance`) — avoids first-occurrence-wins collision class. Anchor uniqueness empirically verified pre-AC-lock via grep (both `Language-version conformance` and `### Bonus: weak graph edges` appear exactly once each in `agents/critique.md`).
- Per slice-008 M2 N-substring schema-pin discipline + slice-009 M3 + slice-010 M3 N-surface schema-pin discipline: ACs #1 + #5 jointly pin the same substantive canonical phrase `Recursive self-application discipline` across N=3 surfaces (agents/critique.md sub-clause title + in-repo methodology-changelog.md + installed methodology-changelog.md). Same canonical phrase, multiple surfaces — drift becomes impossible without breaking ≥2 tests simultaneously.
- Per slice-007/008/009/010 PMI-1 versioned-gate supersession pattern at N=3 supersession events stable: slice-007 introduced `_at_0_22_0`; slice-008 first-superseded → `_at_0_23_0` (N=1); slice-009 second-superseded → `_at_0_24_0` (N=2); slice-010 third-superseded → `_at_0_25_0` (N=3); slice-011 fourth-supersedes → `_at_0_26_0` (ratchets to N=4 supersession events on completion). Delete old `_at_0_25_0` test row; add new `_at_0_26_0`. No two version-gates coexist.
- TF-1 plan starts at 8 rows. May grow at /design-slice (slice-010 grew 7 → 8 across mission-brief → design → post-Critic).

## Verification plan

| # | Criterion | How we verify |
|---|-----------|---------------|
| 1 | New 6th sub-clause `Recursive self-application discipline` present in `agents/critique.md` Dim 9, location-pinned within Dim 9 section bounds AFTER `Language-version conformance` sub-clause and BEFORE `### Bonus: weak graph edges` anchor; 5-sub-clauses structural-invariant test superseded by 6-sub-clauses test (no two coexist) | `pytest tests/methodology/test_critique_agent.py::test_critique_dim_9_lists_six_sub_clauses` PASS; `test_critique_dim_9_recursive_self_application_sub_clause_present` PASS (canonical literal pin); `test_critique_dim_9_recursive_self_application_location_pinned` PASS (location-pin per slice-009/010 M1 lessons with scoped `.find()` per slice-009 DEVIATION-2 + slice-010 anchor-uniqueness pre-emption); existing `test_critique_dim_9_lists_five_sub_clauses` removed in the same commit |
| 2 | New sub-clause body names BOTH design-time and build-time sub-modes distinctly | `pytest tests/methodology/test_critique_agent.py::test_critique_dim_9_recursive_self_application_names_both_sub_modes` PASS; asserts canonical substrings `design-time` AND `build-time` (case-sensitive lowercase-d / lowercase-b body-prose form) present in the new sub-clause body scoped to the sub-clause bounds via scoped `.find()` |
| 3 | New sub-clause body cites BOTH cross-slice anchors `slice-009` AND `slice-010` (strict-both per slice-011 m2; with 2-element allowlist, `≥2 of 2` semantics is strict-both not permissive) AND at least 1 sub-class anchor at N=3 evidence | `pytest tests/methodology/test_critique_agent.py::test_critique_dim_9_recursive_self_application_cites_at_least_two_cross_slice_anchors` PASS; asserts BOTH canonical substrings `slice-009` AND `slice-010` present in the sub-clause body AND at least 1 of {`M2`, `DEVIATION-3`, `BC-PROJ-2`, `B1`, `B5`, `M1`} as sub-class instance anchor |
| 4 | `~/.claude/agents/critique.md` byte-equal to in-repo post-forward-sync | `pytest tests/methodology/test_critique_agent_drift.py::test_in_repo_and_installed_critique_agent_are_content_equal` PASS at slice end (after Phase 2c forward-sync; transitions PASSING (Phase 1b) → WRITTEN-FAILING (Phase 2 in-repo edit, observed at Phase 2b mid-slice smoke) → PASSING (Phase 2c). Build-log Phase 4 captures sha256 forensic evidence per N=6 stable bidirectional-capture pattern (slice-005..010). Per-file CAD-1 byte-equality on `agents/critique.md` (slice-007 introduction; remains in place). |
| 5 | v0.26.0 entry pinned bidirectionally with substantive canonical phrase + RSAD-1 rule reference + Limitations note; PMI-1 atomic at 0.26.0; versioned-gate supersession clean | `pytest tests/methodology/test_methodology_changelog.py::test_v_0_26_0_rsad_1_entry_present_in_repo_and_installed` PASS (asserts in-repo + installed both contain `## v0.26.0 —`, `RSAD-1` rule reference, AND substantive canonical phrase `Recursive self-application discipline`); `python -m tools.plugin_manifest_audit --root .` exits 0; `pytest tests/methodology/test_methodology_changelog.py::test_plugin_yaml_version_matches_version_file_at_0_26_0` PASS; slice-010's `_at_0_25_0` test row removed in the same commit (no two version-gates coexist per PMI-1 supersession pattern N=3 stable; slice-011 ratchets to N=4) |

## Must-not-defer

- [ ] TF-1 PENDING -> WRITTEN-FAILING genuine transitions per slice-003..010 N=7 stable lesson. Pin specific failure signals (substring absent, structural-invariant count mismatch, location AssertionError with `.find()` collision pre-empted via scoped search, sha256 mismatch, version mismatch). Empirical pre-design grep confirmed zero pre-existing matches for canonical literal `Recursive self-application discipline` in `agents/critique.md`; PENDING transition will be authentic.
- [ ] Case-sensitivity discipline per slice-009 DEVIATION-1 + slice-010 pre-emption: canonical literal `Recursive self-application discipline` is capitalized R (sub-clause-title bullet-start form mirroring `Methodology-audit conformance` etc.); canonical literals `design-time` AND `build-time` are lowercase compound forms (body-prose). The sub-clause body MUST keep lowercase compound forms mid-sentence (not at any sentence start) to honor markdown convention without forcing case manipulation. Slice-011's prose MUST NOT trigger the DEVIATION-1 trap on its own canonical pins.
- [ ] Location-pin anchor uniqueness per slice-005 + slice-009 algorithm-path-conformance discipline + DEVIATION-2 sub-class + slice-010 pre-emption: the location-pin test uses `Language-version conformance` (L166 anchor) as start-of-section anchor + `### Bonus: weak graph edges` (L168 anchor) as end-of-section anchor; both empirically verified UNIQUE in `agents/critique.md` at design time. Test impl uses scoped `text.find("### Bonus: weak graph edges", start_idx_after_Language_version_conformance)` to pre-empt `.find()`-collision class. Slice-011 MUST NOT introduce the DEVIATION-2 trap on its own location pin.
- [ ] TWO-surface schema-pin discipline atomically: changes to `agents/critique.md` AND `~/.claude/agents/critique.md` ship in the same slice. Forward-sync verified by per-file CAD-1 pytest (AC #4 — existing test from slice-007) + build-log Phase 0 + Phase 4 sha256 forensic capture per N=6 stable bidirectional pattern.
- [ ] N-substring + N-surface schema-pin discipline applied per slice-008 M2 + slice-009 M3 + slice-010 M3 lessons: substantive canonical phrase `Recursive self-application discipline` pinned across N=3 surfaces (agents/critique.md sub-clause title + in-repo methodology-changelog.md + installed methodology-changelog.md). Each surface's pin test is independent — drift requires breaking ≥2 tests simultaneously.
- [ ] New rule entry appended to `methodology-changelog.md` (in-repo) AND `~/.claude/methodology-changelog.md` atomically. Per slice-007 PMI-1 escape-closure lesson: every claim made in `## Vault updates made` MUST be verifiable by audit (`tools.plugin_manifest_audit` + the new bidirectional changelog-pin test at AC #5).
- [ ] PMI-1 audit clean post-build (`python -m tools.plugin_manifest_audit --root .` exits 0). Atomic version bump: `VERSION` + `ai-sdlc-VERSION` + `plugin.yaml.version` all to `0.26.0`. Slice-010's `_at_0_25_0` PMI-1 versioned-gate test REPLACED (not additive) by `_at_0_26_0` per N=3-stable supersession pattern from slice-007..010 (slice-011 creates N=4 supersession events post-completion).
- [ ] Bidirectional sha256 forensic capture in `build-log.md` Phase 0 (pre-edit) + Phase 4 (post-forward-sync) for `~/.claude/agents/critique.md` + `~/.claude/methodology-changelog.md` + `~/.claude/ai-sdlc-VERSION` per slice-005..010 N=6 stable lesson. Capture in-repo + installed sha256 BEFORE and AFTER the edits. Slice's git diff alone is insufficient evidence for out-of-repo edits.
- [ ] No regression on the broader methodology suite (`pytest tests/methodology/ -q` clean). The slice's surface is narrow (Critic agent Dim 9 sub-clause append + changelog entry + version bump + 5 new test functions); broader breakage indicates unintended scope creep.
- [ ] Shippability catalog row 11 added at `architecture/shippability.md` naming this slice's critical path (Dim 9 6th sub-clause `Recursive self-application discipline` + CAD-1 byte-equality on `agents/critique.md` + v0.26.0 entry + PMI-1 0.26.0 invariant + sub-mode + cross-slice anchor pins). Updates row 10 (slice-010) to note slice-011 PMI-1 version-gate supersession (mirrors slice-008's row 7 update + slice-009's row 8 update + slice-010's row 9 update for prior version-gate supersessions).
- [ ] **Recursive self-application discipline per slice-009 M2 + slice-010 lessons (meta-recursive)**: slice-011 IS a cross-cutting tooling slice modifying an in-house methodology surface (`agents/critique.md`) AND IS the slice authoring the recursive-self-application discipline itself — exactly the rule class it's codifying. The Critic at /critique SHOULD be expected to find rule-class violations in slice-011's own draft prose (recursive self-application phenomenon AT THE METHODOLOGY-CREATION LEVEL, mirroring slice-010 MCT-1 self-application audit precedent at N=8 → N=9 stable). `critic-required: true` is set under MCT-1 trigger glob automatically (slice-011 modifies `agents/critique.md` matching `agents/*.md` + modifies `methodology-changelog.md` matching `methodology-changelog.md`). This slice is the FIRST slice to operate under MCT-1 default (per slice-010 codification: "From slice-011+ onward, /slice invocations matching the MCT-1 trigger glob auto-set `critic-required: true`"). Validate-using-your-own-ship N=9 stable post-slice-011.
- [ ] Self-application BC-1 audit per slice-008 + slice-009 + slice-010 N=8 stable "validate using your own ship" pattern: post-build BC-1 audit on THIS slice's own mission-brief.md + design.md returns **`applicable: [BC-PROJ-2]`** (NOT `[]` — slice-011 intentionally fires BC-PROJ-2 to demonstrate the build-time sub-mode; dispositioned defer-with-rationale at /validate-slice). Precise framing per slice-010 Critic B4 fix discipline + slice-011 Critic M1 + M2 corrections: (a) BC-PROJ-1 positive trigger fires via `subagent` / `fan-out` substrings in design.md Audit 3 listing → BC-1 v1.2 negative-anchor filter (`Dim 9`, `aggregated lessons`, `forward-sync`, `back-sync`, `vocabulary`, `meta-discussion`, `defer-with-rationale` per slice-008 v1.2) silences → BC-PROJ-1 clean; (b) BC-PROJ-2 positive trigger fires via `fence` / `code-block` / `llm` substrings in mission-brief + design.md + ADR-010 → BC-PROJ-2 has NO negative anchors yet (slice-008 deferred migration to N=2; slice-011 ratchets to N=2 evidence threshold met) → BC-PROJ-2 POSITIVELY FIRES, dispositioned defer-with-rationale per slice-010 DEVIATION-3 precedent; (c) BC-GLOBAL-1 positive trigger fires via `Applies to: **` GLOB path matching `--changed-files` (per slice-005 `Applies to: always: true` → `Applies to: **` changeover; `_rule_applies` glob path matches when `--changed-files` non-empty, NOT the `always: true` short-circuit) → BC-1 v1.2 negative-anchor filter silences via methodology-vocabulary anchors → BC-GLOBAL-1 clean. Per slice-010 DEVIATION-3 + slice-011 Critic M1 build-time recursive-self-application lesson: this slice's mission-brief + design.md + ADR-010 explicitly DO describe BC-PROJ-2's trigger anchors (`fence`, `code-block`, `llm`) for the build-time-via-/critique-fix-prose sub-mode empirical evidence — BC-PROJ-2 negative-anchor migration is a separate slice-011+ candidate at N=2 evidence threshold now met (slice-005 + slice-011); not bundled here. Self-application result captured in `build-log.md` Phase 4. **Note**: this is the FIRST design-time-anticipated invocation of the recursive-self-application discipline being codified — slice-011 demonstrates the build-time sub-mode by acknowledging it at design-time AND empirically demonstrating the BC-PROJ-2 fire on its own transient artifacts (mission-brief + design.md + ADR-010) while keeping the canonical Critic-prompt sub-clause body in `agents/critique.md` stylistically clean of anchor literals (per slice-011 Critic M1 correction: BC-1 doesn't audit `agents/critique.md` content — the clean canonical body is for readability, not BC-1-cleanliness).
- [ ] Empirical-verification-at-design-time discipline per slice-009 N=8 + slice-010 N=9 stable lesson: pre-AC-lock grep confirmed canonical literal `Recursive self-application discipline` absent from `agents/critique.md` + location-pin anchors (`Language-version conformance`, `### Bonus: weak graph edges`) unique. Slice-011 ratchets pattern to N=10 stable.

## Out of scope

- **Building `tools/rsad_1_audit.py`** — RSAD-1 v2 candidate (programmatic enforcement: walk `architecture/slices/*/critique.md` and assert cross-cutting tooling slices' Critic actually probed the slice's own artifacts for rule-class violations). Adds scope (~0.5-1 day); deferred at slice-011 to keep within ~0.5-day budget. Promote at slice-N+ if drift surfaces (a future cross-cutting tooling slice ships without recursive-self-application stress-test and surfaces rule-class violations post-build that the Critic should have caught).
- **Generalizing the discipline beyond Dim 9** — RSAD-1 is encoded as a Dim 9 sub-clause anchored at the cross-cutting-conformance dimension; broader applicability (e.g., the Critic's `## Stance` section pre-amble, or a new top-level "Meta" dimension) is out of scope. Promote to broader scope at N≥5 evidence with additional Dim coverage if surfaces.
- **`bc-proj-2-negative-anchor-migration`** — slice-011+ candidate at N=2 evidence threshold (slice-005 + slice-010). Separate ~30 min slice. Mirrors slice-008 BC-PROJ-1/GLOBAL-1 v1.2 migration. Slice-011's mission-brief / design.md DO describe BC-PROJ-2's trigger anchors (for the build-time sub-mode evidence) — therefore explicit defer-with-rationale at /validate-slice if BC-PROJ-2 fires on slice-011 itself (per slice-010 DEVIATION-3 precedent; BC-PROJ-2 negative-anchor migration is a separate slice).
- **`fix-r1-cwd-mismatch-for-diagnose-subagents`** — R-1 HIGH-band open; needs `/risk-spike` first to disambiguate cwd vs parallel-spawn-cascade hypotheses per slice-002 Critic M1 + slice-009/010 reflections.
- **`refactor-pmi-1-gate-to-version-agnostic-shape`** — slice-007/008/009/010 carryover; slice-011 adds 5th supersession event (`_at_0_25_0` → `_at_0_26_0`); each supersession is ~1 min churn; ratchets pattern from N=3 → N=4 events. Friction threshold at N≥4 — slice-011 IS the 4th-supersession-event-creating slice. The per-bump cost remains trivial. Continue tracking; promote at N≥5 if churn accumulates.
- **`case-sensitivity-canonical-literal-pin-discipline`** — slice-009 + slice-010 carryover at N=1 stable (slice-010 pre-empted; did NOT recur). Slice-011 pre-empts on its own canonical pins via design-time prose-structure discipline. Promote at N=2 if recurs at slice-012+.
- **`substring-search-location-pin-anchor-uniqueness-discipline`** — slice-009 + slice-010 carryover at N=1 stable (slice-010 pre-empted; did NOT recur). Slice-011 pre-empts on its own location pin via design-time anchor-uniqueness verification + scoped `.find()` test impl. Promote at N=2 if recurs at slice-012+.
- **INST-2 generalization (general content-equality audit across all installed files)** — slice-007/010 carryover; still N=1 actual-drift evidence (only `agents/critique.md` drift confirmed historically; slice-010's added `skills/slice/SKILL.md` mini-CAD-1 surfaced zero actual drift). Defer to N=2 actual-drift evidence.
- **Wiegers AC-trace sub-class promotion** — slice-008/009/010 carryover at N=1. Slice-010 did NOT recur. Defer to N=2.
- **`/critic-calibrate` next-run recalibration** — slice-009 + slice-010 reflections recommend slice-012-015 boundary; cross-cutting cumulative miss count (9 through slice-010) significantly over the ≤2 target (4.5x over with 5 slices remaining in window). RSAD-1 codification is one calibration-response candidate (refining the Critic prompt with the new recursive-self-application sub-clause); /critic-calibrate at slice-012-015 will assess whether RSAD-1 alone moves cross-cutting catch rate sufficiently OR whether quantitative-target recalibration is also needed.
- **Effectiveness-tracking-as-metric for ZERO-build-deviation-via-strong-/critique** — slice-008 N=1 lesson observation; slice-009 + slice-010 each had 1 build-deviation (DEVIATION-1/2 at slice-009 mitigated at slice-010; DEVIATION-3 at slice-010). Effectiveness metric defer-promote at N=2 zero-deviation observation.
- **Em-dash → cp1252 / prose-pin substring uniqueness** — slice-007 carryover at N=1; slice-009 + slice-010 didn't recur. Continue tracking.

## Dependencies

- Prior slices: [[slice-006-update-critic-with-cross-cutting-conformance-dimension]] — CCC-1 baseline (Dim 9 introduced with 5 sub-clauses; slice-011 grows to 6); [[slice-007-add-critique-agent-content-equality-audit]] — CAD-1 byte-equality audit + the existing `tests/methodology/test_critique_agent_drift.py` AC #4 reuses (with PASSING → WRITTEN-FAILING → PASSING transition per slice-007/009/010 mini-CAD-1 row pattern at N=3 stable); [[slice-008-refine-bc-1-anchors-with-negative-context]] — BC-1 v1.2 negative-anchor mechanism + N-substring schema-pin + PMI-1 versioned-gate supersession introduction; [[slice-009-refine-dim-9-with-design-md-tables-sub-clause]] — N-surface schema-pin + algorithm-path-conformance generalized + recursive-self-application phenomenon at N=1 (M2 first surfacing) + DEVIATION-1 case-sensitivity + DEVIATION-2 `.find()`-collision lessons; [[slice-010-promote-voluntary-critic-on-cross-cutting-to-slice-default-heuristic]] — MCT-1 codification (slice-011 IS the first slice to operate under MCT-1 default) + recursive-self-application at N=3 cumulative (design-time stress-test catches B1/M1/B5 + build-time DEVIATION-3 BC-PROJ-2 fire) + B5 calibration-trail discipline (rule-ID naming convention; slice-011's RSAD-1 -D suffix extends the convention) + sub-mode anchor pre-emption discipline.
- Vault refs: [[agents/critique.md]] Dim 9 ("Cross-cutting conformance") sub-clause list (L154-L166); upcoming 6th sub-clause inserted at L167 between L166 (Language-version conformance close) and L168 (`### Bonus: weak graph edges` H3 anchor). [[methodology-changelog.md]] v0.25.0 (current; slice-010) + upcoming v0.26.0 (slice-011). [[architecture/critic-calibration-log.md]] 2026-05-10 calibration run + the slice-011 RSAD-1 promotion act (record at /reflect). [[architecture/lessons-learned.md]] recursive-self-application phenomenon aggregate at N=3. [[architecture/slices/_index.md]] aggregated lessons section (the N=3 promotion threshold language is line 27, line 40; slice-011 fulfills the explicit slice-011+ recommendation).
- Risk register: none retired (R-1 + R-2 are /diagnose-related; slice-011 doesn't touch them).
- Tooling: `agents/critique.md` (new 6th Dim 9 sub-clause appended between Language-version conformance and `### Bonus: weak graph edges`); `~/.claude/agents/critique.md` (Phase 2 forward-sync target); `methodology-changelog.md` + `~/.claude/methodology-changelog.md` (v0.26.0 entry with RSAD-1 rule + Limitations note); `VERSION` + `~/.claude/ai-sdlc-VERSION` (atomic bump to 0.26.0); `plugin.yaml` (version field bump 0.25.0 → 0.26.0); `tests/methodology/test_critique_agent.py` (5 new tests for AC #1 + AC #2 + AC #3: `test_critique_dim_9_lists_six_sub_clauses` (REPLACES `_lists_five_sub_clauses`); `test_critique_dim_9_recursive_self_application_sub_clause_present`; `test_critique_dim_9_recursive_self_application_location_pinned`; `test_critique_dim_9_recursive_self_application_names_both_sub_modes`; `test_critique_dim_9_recursive_self_application_cites_at_least_two_cross_slice_anchors`); `tests/methodology/test_critique_agent_drift.py` (existing; AC #4 reuse with mini-CAD-1 row 3 supersession pattern at N=3 stable); `tests/methodology/test_methodology_changelog.py` (replace `_at_0_25_0` with `_at_0_26_0`; add `test_v_0_26_0_rsad_1_entry_present_in_repo_and_installed` bidirectional pin per AC #5); `architecture/shippability.md` (add row 11). No code-tooling logic changes (no audit module modified, no skill executable behavior changed beyond the Critic agent's adversarial-prompt prose-level heuristic).

## Mid-slice smoke gate

At ~50% of build (after `agents/critique.md` is edited in-repo with the new 6th sub-clause AND the 5 new prose-pin tests are added in `tests/methodology/test_critique_agent.py` AND the old `_lists_five_sub_clauses` test is removed, but BEFORE Phase 2 forward-sync to `~/.claude/agents/critique.md`):

```
C:/Users/sshub/.claude/.venv/Scripts/python.exe -m pytest tests/methodology/test_critique_agent.py tests/methodology/test_critique_agent_drift.py -q
```

Expected:
- All 5 new prose-pin tests in `test_critique_agent.py` PASS against in-repo (because in-repo is edited with the new sub-clause).
- The replaced `test_critique_dim_9_lists_six_sub_clauses` PASS (6 sub-clause-title substrings now present).
- `test_critique_agent_drift.py::test_in_repo_and_installed_critique_agent_are_content_equal` FAILS with `RuntimeError: sha256 mismatch` (in-repo edited; installed not yet forward-synced) — this is EXPECTED at this gate, mirrors slice-007/009/010 CAD-1 drift signal pattern. The CAD-1 test must report clean PASS only at pre-finish gate after Phase 2 forward-sync.

If any of the 5 prose-pin tests fails: STOP, diagnose. Likely root causes:
- Canonical-literal substring mismatch — re-read the canonical phrase in mission-brief AC #1 and align prose exactly.
- DEVIATION-1 lowercase-at-sentence-start trap: the lowercase `design-time` or `build-time` phrase placed at a bullet start or sentence start was auto-capitalized; restructure the sub-clause body to keep these phrases mid-sentence.
- DEVIATION-2 `.find()`-collision in location-pin test: the start-anchor or end-anchor substring matched an unintended occurrence; verify anchor uniqueness via grep; use scoped `.find(substring, start_idx)`.
- Sub-clause location: the new sub-clause was placed outside the Dim 9 → Cross-cutting conformance section bounds (e.g., in Dim 8 or in the `### Bonus` section); move it into the correct location AFTER `Language-version conformance` and BEFORE `### Bonus: weak graph edges`.
- Build-time sub-mode anchor missing: AC #2 specifically requires BOTH `design-time` AND `build-time` substrings; if only one is present, the body needs to name both modes distinctly per N=3 evidence.

## Pre-finish gate

- [ ] All 5 acceptance criteria PASS with evidence in `validation.md`
- [ ] Must-not-defer list fully addressed (TF-1 genuineness, DEVIATION-1 case-sensitivity, DEVIATION-2 anchor-uniqueness, TWO-surface pin, N-substring + N-surface schema-pin, v0.26.0 entry bidirectional, PMI-1 0.26.0 clean, sha256 forensic capture, no methodology-suite regression, shippability row 11, recursive-self-application discipline meta-recursive applied, self-application BC-1 clean, empirical-verification-at-design-time N=10)
- [ ] `/drift-check` passes
- [ ] Mid-slice smoke still passes (no regression on the 5 prose-pin tests; CAD-1 mini-row transitions from FAIL to PASS post-forward-sync)
- [ ] No new TODOs / FIXMEs / debug prints
- [ ] PMI-1 audit clean: `python -m tools.plugin_manifest_audit --root .` exits 0
- [ ] Per-file CAD-1 mini-byte-equality clean at slice end: `pytest tests/methodology/test_critique_agent_drift.py::test_in_repo_and_installed_critique_agent_are_content_equal` PASS
- [ ] Methodology suite clean: `C:/Users/sshub/.claude/.venv/Scripts/python.exe -m pytest tests/methodology/ -q` exits 0 (no regression on the broader suite)
- [ ] Shippability catalog clean: row 11 added; row 10's slice-010 reference updated to note slice-011 PMI-1 versioned-gate supersession (mirrors slice-008's row 7 update + slice-009's row 8 update + slice-010's row 9 update for prior version-gate supersessions)
- [ ] Self-application check: post-slice-011 BC-1 audit on THIS slice's own mission-brief.md + design.md does NOT fire BC-PROJ-1 or BC-GLOBAL-1 (or fires with explicit defer-with-rationale per slice-010 DEVIATION-3 precedent on BC-PROJ-2 if the build-time sub-mode evidence prose triggers BC-PROJ-2's anchor path — BC-PROJ-2 negative-anchor migration is a separate slice). Result captured in `build-log.md` Phase 4.
