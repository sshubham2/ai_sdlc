# Build checks (project-specific) — CANONICAL FIXTURE

> **Git-tracked canonical oracle for the BC-1 project rule set (slice-030A, ADR-028).**
> The gitignored live `architecture/build-checks.md` is byte-reconstructed FROM this
> file. BCI-1 (`tools/build_checks_integrity.py`) asserts the live file matches this
> fixture on full per-rule structural identity. The per-rule literal-constant tuples
> in `tests/methodology/test_build_checks_audit.py` are the tracked oracle this
> fixture is asserted *against* (fixture = subject, literal constant = oracle).
> Recovery provenance + best-effort residual for the 3 lost rules: see slice-030A
> design.md M3 table + build-log.md task-1 recovery record.

Evergreen rules promoted from lessons-learned. `/build-slice` reads the live copy
at Step 6. Rules whose `Applies to` glob matches changed files, or whose
`Trigger keywords` appear in this slice's mission-brief.md / design.md, are
surfaced for the builder to address.

Per BC-1 (`methodology-changelog.md` v0.10.0). Promotion is manual at `/reflect`
Step 5b.

## Schema

Each rule is an H2 `## BC-PROJ-NNN — <title>` followed by `**Field**: value`
lines. Fields: **Severity** (Critical | Important), **Applies to** (`always:
true` OR comma-separated globs), **Promoted from**, **Trigger keywords**
(comma-separated; matched case-insensitively against mission-brief.md /
design.md via **word-boundary** regex — slice-005/ADR-004, was bare substring
pre-slice-005), optional **Trigger anchors** (comma-separated subset of Trigger
keywords; when present the keyword path fires only if ≥1 anchor word-boundary-
matches), optional **Negative anchors** (comma-separated tokens acting as a
**final filter** — slice-008/ADR-007, BC-1 v1.2: a rule that would otherwise
fire via any positive path is suppressed when ≥1 negative anchor word-boundary-
matches the slice text; negative anchors must not overlap the rule's own
Trigger keywords/anchors). Then **Check**, **Rationale**, **Validation hint**.

## Rules

## BC-PROJ-1 — Subagent fan-out concerns must be reviewed before parallel dispatch

**Severity**: Important
**Applies to**: agents/**/*.md
**Promoted from**: ai_sdlc slice-001-diagnose-orchestration-fix — parallel subagent fan-out exposed a permission cascade-failure mode; recurring class across diagnose-orchestration slices
**Trigger keywords**: subagent, fan-out, agent, parallel, spawn, orchestrate
**Trigger anchors**: subagent, fan-out
**Negative anchors**: defer-with-rationale, aggregated lessons, false positive, meta-discussion, vocabulary, Critic-MISSED, back-sync, Dim 9, forward-sync

**Check**: When a slice dispatches subagents in a fan-out (multiple `Agent` calls), verify the dispatch is sequential-by-default OR the parallel path is an explicit opt-in, and that subagent tool-permission cascade-failure ([claude-code #57037](https://github.com/anthropics/claude-code/issues/57037)) is mitigated. Do not assume parallel subagent spawn inherits parent permissions.

**Rationale**: Permanent because the AI-SDLC pipeline structurally fans out subagents (Critic, meta-Critic, diagnose passes); the parallel-spawn permission cascade is an invariant exposure of that architecture, not a one-off bug (R-1; recurred slice-001/002/029).

**Validation hint**: Grep slice dispatch code for multiple `Agent(` calls in one message; confirm sequential-by-default or documented `--parallel` opt-in.

## BC-PROJ-2 — LLM-fence / structured-output parsing must handle nested + malformed fences

**Severity**: Important
**Applies to**: skills/**/*.py, tools/**/*.py
**Promoted from**: ai_sdlc slice-001-diagnose-orchestration-fix — LLM-fence parsing of subagent output is a recurring failure surface across diagnose/critique passes
**Trigger keywords**: fence, code-block, llm, parse, structured-output, fenced, output
**Trigger anchors**: fence, code-block, llm
**Negative anchors**: defer-with-rationale, aggregated lessons, false positive, meta-discussion, vocabulary, Critic-MISSED, back-sync, Dim 9, forward-sync

**Check**: Any code parsing LLM-produced fenced / code-block / structured output MUST handle nested triple-backtick fences, missing closing fences, and language-tag variance. Do not assume a single well-formed fence.

**Rationale**: Permanent because the pipeline routinely parses LLM-generated fenced output (subagent results, diagnose passes); malformed/nested fences are an invariant property of LLM output, not a one-off (recurred slice-001/004).

**Validation hint**: Grep for fence/code-block parsing; confirm nested-fence + missing-close + language-tag-variance handling and tests covering each.

## BC-PROJ-3 — Validation/demo harnesses must never destructively revert files carrying uncommitted slice work

**Severity**: Critical
**Applies to**: always: true
**Promoted from**: slice-028-refactor-utf8-rollup-sentinel-version-agnostic (2026-05-16) — a `git checkout -- <test file>` to revert AC2/AC3 demo mutations silently reverted the ENTIRE uncommitted slice refactor to pre-slice HEAD; the post-revert green was the OLD sentinel passing (false-green that nearly masked total loss of the slice's work)
**Trigger keywords**: validate, demo, revert, fixture, mutate, git checkout, git restore, git stash, scratch

**Check**: Any validation/build/demo step that mutates a source file and then reverts it MUST revert via an in-place reverse edit, a temp-copy swap, or `monkeypatch` — NEVER `git checkout -- <path>`, `git restore <path>`, or `git stash` on a path that may carry uncommitted slice work. Under branch-per-slice (BRANCH-1) the slice's work is uncommitted until `/commit-slice`, so any git-level revert of a slice-touched path destroys the slice. After any such mutate-then-revert sequence, assert the file's pre/post content hash is identical to the intended state (not merely "tests pass" — a stale/old version may pass for the wrong reason).

**Rationale**: Permanent because branch-per-slice (BRANCH-1) structurally guarantees slice work is uncommitted through /build-slice and /validate-slice. `git checkout`/`restore`/`stash` operate against the last commit, not the in-progress slice, so they are always destructive to uncommitted slice work — this is not a one-off bug but an invariant of the workflow. The failure is especially dangerous because a silent full-revert often yields a false-GREEN (the prior committed code passes its own old tests), which a casual re-run will not catch.

**Validation hint**: Grep slice harness/validation code for `git checkout --`, `git restore`, `git stash` operating on source paths; for any mutate-then-revert block, confirm the revert is in-place/temp/monkeypatch and that a content-hash equality assertion brackets the sequence.
## BC-PROJ-4 — Methodology-gate slices must exercise every affected gate on the real artifact (audit field-line / folder-name / classifier blind spots)

**Severity**: Important
**Applies to**: tools/**/*.py, skills/**/*.md
**Promoted from**: slice-031-complete-shippability-decoupling (2026-05-17) — N+3 of the slice-024/029/030A "design-stage Critics cannot see tooling-interaction defects" class: a trailing annotation on `**Test-first**: true` silently disabled the TF-1 gate (R-7); a letter-suffixed slice folder broke BRANCH-1's numeric `slice-NNN-` regex (R-6); the SCMD-1 AST classifier's false-positive surface (synthetic tmp trees, unrelated home reads) was invisible until the real-corpus mid-slice smoke gate
**Trigger keywords**: audit, gate, regex, field-line, branch, methodology, prerequisite, pre-finish, skill, classifier, ast

**Check**: When a slice changes an audit/gate's own parse rules (field-line / heading / folder-name regexes), a `skills/*/SKILL.md` gate-prose contract, or an AST/text classifier, you MUST run every affected gate against the ACTUAL slice artifact (real mission-brief / real folder name / real catalog / real codebase) at BOTH the /build-slice prerequisite check AND pre-finish — never only against unit fixtures. Read the gate's human output: "not enabled" / "usage-error" / "carry-over exempt" / "0 rows" on a slice that SHOULD engage the gate is a silent-bypass alarm, not a pass. For AST/text classifiers, a real-corpus smoke gate at ~50% is mandatory — design-stage reasoning cannot enumerate the false-positive surface.

**Rationale**: Permanent because the AI-SDLC pipeline is self-hosting: slices routinely modify the very audits/gates that police them, and a gate's own parse rules (regex anchors, folder-name patterns, default-off semantics, classifier shape-matching) are structurally invisible to design-stage Critics that review intent, not the audit-vs-artifact interaction. The failure mode is silent (default-off / usage-error / vacuous-pass yields a GREEN), so it cannot be caught by "tests pass" — only by executing the real gate on the real artifact and reading its output. Recurred slice-024 / slice-029 / slice-030A / slice-031 (R-6, R-7).

**Validation hint**: For the changed gate, run `$PY -m tools.<gate>_audit <real slice/artifact path>` (not a tmp fixture) at prerequisite + pre-finish; assert the gate reports ENGAGED (e.g. TF-1 "N row(s)" not "not enabled"; BRANCH-1 "clean" not "usage-error"); for classifiers, diff the classification against a hand-eyeballed real-corpus sample at the mid-slice smoke gate.

## BC-PROJ-5 — Identifier/rename/carve-out slices must prove the frozen set untouched by content-hash snapshot (not regex count) and drive every count from one authoritative anchor command

**Severity**: Important
**Applies to**: tests/**/*.py, tools/**/*.py
**Promoted from**: slice-042-realign-entry-present-pin-names-to-decoupled-shape (2026-05-18) — N+1 of slice-035 (identifier rename) / slice-041 (frozen-history carve-out): slice-042's design.md inventory counts were wrong TWICE (rev-0 carried slice-041 prose; the /critique ACCEPTED-FIXED rev-1 corrections were ALSO wrong — recompute-don't-trust on the Builder's own fix, N>=4 with slice-032/034/041), and the first FROZEN-integrity proof method (regex count) silently mismatched (grep -c counts lines not occurrences; a Python non-capturing-group pattern fails to match under ERE grep)
**Trigger keywords**: rename, carve-out, identifier, frozen, snapshot, inventory, realign, drop-suffix, identifier-truth

**Check**: When a slice renames an identifier family or defines a frozen / append-only carve-out, you MUST (1) prove the frozen / untouched set byte-identical with a regex-INDEPENDENT content hash (sha256) snapshot taken pre-edit and re-asserted post-edit — NEVER a regex / `grep -c` count (grep -c counts matching LINES not occurrences; a Python `(?:...)` non-capturing-group pattern silently fails to match under ERE grep); and (2) embed the single authoritative anchor command in design.md and declare the build-step-1 pre-edit grep the single source of truth, so every narrative count is reconciled-to-grep, never hand-transcribed. After applying any identifier-class ACCEPTED-FIXED /critique edit, re-run the authoritative anchor repo-wide at build-T0 BEFORE any other edit (a fix-prose edit can re-introduce the literal it corrected — the slice-032 design-correction-is-unguarded class; invisible to the Critic / DR-1 stack).

**Rationale**: Permanent because the recompute-don't-trust failure recurs specifically on the Builder's OWN /critique fix (N>=4: slice-032 / 034 / 041 / 042) and the measurement method itself (regex count) is a first-class blind spot the dual-Critic stack never reviews — it reasons about the claimed number, not the correctness of the tool producing it. Content-hash equality is regex-independent and admits no transcription drift; embedding the anchor command as the single source of truth removes the hand-typed count surface entirely. slice-035 (rename) and slice-041 (carve-out) are the precedent class; slice-042 is where the double-miss made the discipline non-optional.

**Validation hint**: Grep design.md for an embedded canonical anchor command (a literal `grep -nE` / `rg` invocation) AND a pre/post sha256 snapshot step in the build sequencing; assert the slice's reflection records the frozen set proven by hash, not by count. Re-run the authoritative anchor repo-wide and assert exactly the intended residual count (e.g. the rename's own documenting ADR) with the frozen tree-hash unchanged.

## BC-PROJ-6 — Risk-retirement edits must flip the Status field line and re-derive the verification filter axis from the risk's actual prior state

**Severity**: Important
**Applies to**: architecture/risk-register.md
**Promoted from**: slice-043-codify-split-slice-folder-naming-convention (2026-05-18) — M1+M2 (both VALIDATED): the mission-brief/design copied the R-4/R-5 mitigating-to-retired retirement shape onto R-6 (an open-to-retired risk), targeting `--filter-status mitigating` (vacuous — R-6 never passed through mitigating) and specifying only a `**Retired**:` prose line; RR-1 parses status SOLELY from the `**Status**:` field, so that would have false-greened R-6 as still open. Adjacent precedent: slice-041's `test_r_4_..._stays_mitigating` state-transition FAIL
**Trigger keywords**: retire, retired, retirement, risk-register
**Trigger anchors**: retire, retired, retirement
**Negative anchors**: defer-with-rationale, aggregated lessons, false positive, meta-discussion, vocabulary, Critic-MISSED, back-sync, Dim 9, forward-sync

**Check**: A slice that retires a risk in `architecture/risk-register.md` MUST flip that risk's `**Status**:` field line to `**Status**: retired` — RR-1 (`tools.risk_register_audit`) derives status SOLELY from the `**Status**:` field; adding only a `**Retired**:` prose/narrative line leaves the risk parsed as its OLD status, silently false-greening any status-consuming verification (the slice's own AC, plus risk-first `/slice` / `/pulse` ranking). It MUST ALSO re-derive the verification command's filter axis from the risk's ACTUAL prior state instead of copying a sibling retirement's command: a direct open-to-retired risk is verified by asserting it is ABSENT from `--filter-status open` AND that `--json` reports status retired; `--filter-status mitigating` is vacuous for a risk that never passed through mitigating (that axis is correct only for the mitigating-to-retired class such as R-4 / R-5). Run `tools.risk_register_audit architecture/risk-register.md --json` on the REAL file at pre-finish and assert the retired risk's parsed status is retired.

**Rationale**: Permanent because the self-hosting AI-SDLC pipeline retires risks regularly (R-4 / R-5 / R-7 / R-9 / R-10 / R-6 across recent slices) and RR-1's machine-parsed status surface (the `**Status**:` field) is structurally distinct from the human-readable `**Retired**:` narrative; a retirement that updates only the narrative is a silent false-green no tests-pass run catches — the risk still ranks open in risk-first `/slice` ordering. The sibling-copy failure (reusing R-4/R-5's mitigating filter axis on an open-to-retired risk) shares the copy-a-pattern-without-recomputing-from-this-case's-actuals root with the Status-line miss. slice-043 M1+M2 (both VALIDATED) is the witnessed instance; slice-041's R-4-retirement state-transition FAIL is the adjacent precedent. The defect is invisible to design-stage Critics (an audit-vs-artifact parse interaction) and surfaces only when the real RR-1 audit runs on the real risk-register at validate — the BC-PROJ-4 real-artifact discipline applied to risk-register transitions.

**Validation hint**: For any slice that retires a risk, grep the `architecture/risk-register.md` diff for the retired risk's `**Status**: retired` field line (NOT merely a `**Retired**:` line); run `tools.risk_register_audit architecture/risk-register.md --json` and assert the risk's status is retired and that it is absent from `--filter-status open`; confirm the verification command's filter axis matches the risk's actual prior-state transition (open-to-retired vs mitigating-to-retired).
## BC-PROJ-7 — New audit-tool slices must wire the tool into the cp1252 coverage list and author a pipe-free shippability row

**Severity**: Critical
**Applies to**: tools/*.py
**Promoted from**: slice-044-add-state-transition-stale-pin-audit (2026-05-18) — a new `tools/state_transition_pin_audit.py` (with `main()`) silently FAILed the UTF8-STDOUT-1 cp1252 coverage-parity sentinel until added to `test_utf8_stdout_regression.py`'s argv-classified list, AND its first shippability row embedded a regex with literal pipe alternation in the Critical-path prose, shifting the 6-column markdown schema so SCMD-1 mis-segmented the Machine-cmd. cp1252 recurrence: slice-022 / slice-035; pipe-hazard: first witnessed here
**Trigger keywords**: audit, tool, tools, shippability, machine-cmd, new audit
**Trigger anchors**: audit, tool, shippability
**Negative anchors**: defer-with-rationale, aggregated lessons, false positive, meta-discussion, vocabulary, Critic-MISSED, back-sync, Dim 9, forward-sync

**Check**: A slice that adds a new `tools/<name>.py` module exposing `def main(...)` MUST discharge two self-application obligations that live OUTSIDE the tool's own logic, sequenced LAST in the build (after the function/row names are frozen) and verified by reading the SRSC-1 runner output: (1) add `tools.<name>` to the correct argv-classified list in `tests/methodology/test_utf8_stdout_regression.py` — `_ROOT_ONLY_TOOLS` if the tool's argv is `--root`/`--json`-only, `_POSITIONAL_SLICE_TOOLS` if it takes a positional slice-folder (verify the argv contract empirically, do not assume — the slice-038 list-membership-is-a-claim-about-the-argv-contract law); the UTF8-STDOUT-1 coverage-parity sentinel HARD-FAILs any main()-bearing tool with no cp1252 coverage entry. (2) The slice's `architecture/shippability.md` row MUST be pipe-free: any literal pipe in any of the 6 cells (commonly a regex alternation or a quoted table fragment in the Critical-path prose) shifts the markdown column boundaries, so SCMD-1's per-segment parse mis-reads the Machine-cmd cell — describe regexes prose-only or escape every pipe; a well-formed 6-column row has exactly 7 unescaped pipes. Run `tools.shippability_runner architecture/shippability.md` (SRSC-1) at pre-finish and read its full output: the new tool's cp1252 row and the new shippability row must both PASS.

**Rationale**: Permanent because the self-hosting AI-SDLC pipeline adds new tools/*.py audit gates on a regular cadence (BRANCH-1 / BCI-1 / MCFS-1 / SRSC-1 / STP-1 across recent slices) and BOTH obligations are silent-failure classes a tests-pass run does not surface at authoring time: the cp1252 sentinel only fires when the catalog runner reaches the UTF8-STDOUT-1 row (mid-catalog, easily mistaken for an unrelated regression), and a pipe-shifted shippability row mis-segments without an obvious error until SCMD-1 rejects the mangled segment. Both are structurally invisible to the design-stage dual-Critic stack (audit-vs-real-artifact interactions; slice-037/044 law) and surface only when the real SRSC-1 runner executes the real catalog at validate. The cp1252 obligation recurred N>=3 (slice-022 / slice-035 / slice-044); the pipe-hazard is the slice-044 witnessed instance and shares the new-tool-carries-off-its-own-logic-catalog-obligations root.

**Validation hint**: For any slice adding a tools/*.py with main(): grep `tests/methodology/test_utf8_stdout_regression.py` for `tools.<name>` in `_ROOT_ONLY_TOOLS` or `_POSITIONAL_SLICE_TOOLS` (and confirm the chosen list matches the tool's actual argv via `python -m tools.<name> --help`); grep the new `architecture/shippability.md` row and assert it has exactly 7 unescaped pipes (no raw pipe inside any cell); run `python -m tools.shippability_runner architecture/shippability.md` and confirm both the UTF8-STDOUT-1 row and the new slice's row report PASS.

## BC-PROJ-8 — Methodology-tooling that targets architecture/** must use live on-disk reads, never git-diff/show (cross-machine / shallow-clone safety)

**Severity**: Important
**Applies to**: tools/*.py
**Promoted from**: slice-044-add-state-transition-stale-pin-audit (2026-05-18) — slice-044's Sub-form B was designed to diff `architecture/risk-register.md` at the git merge-base vs the working tree; the entire `architecture/` vault was gitignored at slice-044 time (.gitignore:11, "Local-only AI SDLC vault; never tracked"), so `git show <base>:architecture/risk-register.md` was fatal and `git diff --name-only` never surfaced the vault — the audit could not self-apply (RSAD-1). The dual-Critic + DR-1 stack probed the git mechanism's semantics across both review layers but never asked whether the target path exists in git; caught only at /build-slice plan-mode, costing a full design-deviation + targeted re-critique cycle. Post-slice-069 ([[ADR-066]]) the vault is git-tracked, but the rule's prescription is preserved on cross-machine-safety grounds (see Rationale).
**Trigger keywords**: audit, vault, architecture, risk-register, git, scan
**Trigger anchors**: audit, vault, git
**Negative anchors**: defer-with-rationale, aggregated lessons, false positive, meta-discussion, vocabulary, Critic-MISSED, back-sync, Dim 9, forward-sync

**Check**: Any methodology tool or audit whose inputs include paths under `architecture/**` (the AI-SDLC vault: risk-register.md, slices/**, decisions/**, shippability.md, build-checks.md, etc.) MUST read those inputs as LIVE on-disk files, NEVER via `git show <ref>:architecture/...`, `git diff` of vault paths, `git log` of the vault, or any git-history mechanism. While `architecture/` is now git-tracked post-slice-069 ([[ADR-066]]), vault-targeting audits MUST still prefer live reads for cross-machine-safety reasons: a slice running on a fresh clone, a CI runner with a shallow `git fetch --depth=1`, or a fork-collaborator's machine may have only partial git history for vault files (the file exists in the working tree but `git show <ref>:architecture/...` can fail for refs predating the local clone's history-depth window). Where a tool needs a transitioned-vs-prior signal for a vault file, express it as a git-independent standing invariant against the live file (e.g. no test pins R-N to a status the live register contradicts), not a baseline-diff mechanism. Verify self-application at /build-slice plan-mode by running the tool against the real repo BEFORE building the implementation, not after.

**Rationale**: Permanent because the self-hosting pipeline keeps shipping vault-targeting audits (RR-1 / SCMD-1 / PTFFD-1 / STP-1 ...) and cross-machine-safety is a load-bearing constraint that a design-stage Critic cannot infer from the design text alone (it requires reading the consumer's git environment + the tool's input paths — an audit-vs-real-artifact interaction, the slice-037/044 backstop law). A git-history-based vault mechanism passes every design review and every unit test (fixtures are full-history git repos) yet fails the moment it self-applies on a shallow-clone, fresh-fork, or partial-history repo — exactly the false-PASS-then-real-FAIL class. The motivation history: slice-044 (BC-PROJ-8 promotion) caught the failure as a gitignored-vault structural-impossibility class (R-4 lineage); slice-069 / ADR-066 tracks the vault and reframes the motivation to shallow-clone / partial-history reasoning. The rule's prescription is unchanged — vault-targeting audits use live on-disk reads.

**Validation hint**: For any slice shipping a tool that consumes `architecture/**` paths: grep the tool source for `git show`, `git diff`, `git log`, subprocess git near vault paths (there must be none targeting `architecture/`); confirm the tool reads the live file (e.g. a live-file read or a reused live-file parser); run the tool against the real repo at plan-mode and assert it self-applies (exit 0 on the clean tree) rather than erroring on a partial-history vault path.

## BC-PROJ-9 — A slice adding or removing a tools/*.py module must update every inventory surface in lockstep, including INSTALL.md's hard-coded tool count

**Severity**: Important
**Applies to**: tools/*.py
**Promoted from**: slice-050-add-ai-sdlc-version-forward-sync-gate (2026-05-19) — adding the 26th tool `tools/ai_sdlc_version_forward_sync.py` FAILed `tests/methodology/test_install_md_correctness.py::test_install_md_tool_count_matches_plugin_yaml` because INSTALL.md hard-codes "25 executable methodology tools" in TWO places; neither the first Critic nor the DR-1 meta-Critic flagged the INSTALL.md leg (the inventory fan-out is a standing dual-Critic-stack blind spot — slice-006 INST-1 / slice-035 installed-config-set lineage), surfaced only at the /build-slice full-methodology-suite pre-finish regression sweep (the BC-PROJ-4 real-suite-run backstop, not the Critic stack)
**Trigger keywords**: tool, tools, plugin.yaml, install_audit, inventory, audit module, methodology tool, new tool
**Trigger anchors**: tool, plugin.yaml, install_audit
**Negative anchors**: defer-with-rationale, aggregated lessons, false positive, meta-discussion, vocabulary, Critic-MISSED, back-sync, Dim 9, forward-sync

**Check**: Any slice that adds, removes, or renames a `tools/*.py` module exposing `def main(...)` MUST update the FULL inventory fan-out in the same fix block — not just the PMI-1 "4-part" subset: (1) `VERSION`; (2) `plugin.yaml` `version:`; (3) `plugin.yaml` `- path: tools/<name>.py` enumeration; (4) `tools/install_audit.py` `_CANONICAL_TOOLS`; (5) `tests/methodology/test_utf8_stdout_regression.py` `_ROOT_ONLY_TOOLS` or `_POSITIONAL_SLICE_TOOLS` (whichever the tool's argv contract matches); AND (6) `INSTALL.md`'s hard-coded "N executable methodology tools" count, which appears in MULTIPLE places (slice-050 witnessed ×2). Surface (6) is the one the dual-Critic stack does not flag and that no design-stage review surfaces — it is reachable only by running the full methodology test suite (`test_install_md_correctness`) at pre-finish. Before declaring the slice done, run the full `tests/methodology/` suite (not just the slice's own tests) and confirm `test_install_md_correctness` passes; if INSTALL.md is grep-confirmed to carry the old count, bump every occurrence.

**Rationale**: Permanent because the self-hosting pipeline keeps shipping new `tools/*.py` audits (RR-1 / SCMD-1 / PTFFD-1 / STP-1 / MCFS-1 / AVFS-1 ...) and the inventory fan-out is a fixed structural property of this repo (INST-1 + PMI-1 + UTF8-STDOUT-1 + INSTALL.md correctness all enumerate the tool set on independent surfaces). The `INSTALL.md` hard-coded count specifically is a design-stage blind spot: a Critic reviewing design.md/mission-brief cannot infer it (it requires knowing INSTALL.md hard-codes a literal integer that `test_install_md_correctness` cross-checks against `plugin.yaml`), so it passes every design review and unit test yet FAILs the full-suite sweep the moment a tool is added — the slice-022 self-violation / false-PASS-then-real-FAIL class. slice-050 is the witnessed instance (the slice adding a forward-sync gate nearly shipped an INSTALL.md inventory inconsistency); the cost was a mid-build regression-sweep catch + in-band fix that an explicit pre-finish checklist item pre-empts.

**Validation hint**: For any slice adding/removing a `tools/*.py`: `grep -rn "executable methodology tools" INSTALL.md` and confirm the integer matches `plugin.yaml`'s tool count; run `$PY -m pytest tests/methodology/test_install_md_correctness.py tools/plugin_manifest_audit.py tools/install_audit.py` (or the full `tests/methodology/` suite) at pre-finish and assert green — never rely on the slice's own test subset, which does not exercise the INSTALL.md cross-check.

## BC-PROJ-10 — A slice that mints a new ADR OR adds a user-facing skill capability/mode MUST explicitly classify methodology-changelog Inclusion-heuristic posture (bump-required vs MEPD-1(b) why-none discharged) in mission-brief.md / design.md before /critique

**Severity**: Important
**Applies to**: architecture/slices/**
**Promoted from**: slice-052-add-slice-candidates-obo-mode (2026-05-20) — N=2 missed-by-both-Critic-layers recurrence of the slice-049 B2 class (slice-049 first-Critic-B2 caught it that round but the lesson was never mined into a Critic-prompt dimension): slice-052 added a new --obo skill mode + ADR-054 and asserted "plugin-manifest / drift posture unaffected" in both mission-brief and design without classifying the methodology-changelog Inclusion-heuristic posture; both /critique passes reviewed the claim without flagging it; the missed v0.60.0 4-part PMI-1 bump surfaced only at /build-slice pre-finish, costing a mid-build user-ratified scope expansion + a design.md correction + the conventional test_v_0_60_0_* entry-pin + shippability-consumer-propagation tests. The dual-Critic stack's blind spot is the bump-classification logic for slices that mint a new ADR / add a skill capability but have no other bump reason (no new RULE-ID, no new registered artifact) — exactly the slice-049 B2 wording.
**Trigger keywords**: ADR, new ADR, new mode, new flag, new skill, new capability, new subcommand, mints, adds a new, adds an interactive, adds support for, skill capability, opt-in, --obo, --new
**Trigger anchors**: ADR, new skill, new mode, new flag, new capability, mints
**Negative anchors**: defer-with-rationale, aggregated lessons, false positive, meta-discussion, vocabulary, Critic-MISSED, back-sync, Dim 9, forward-sync

**Check**: Any slice whose mission-brief.md OR design.md introduces (a) a new ADR (`architecture/decisions/ADR-NNN-*.md` created this slice), (b) a new user-facing skill capability / mode / flag / subcommand on an existing skill, or (c) a new pipeline gate or check MUST carry an explicit methodology-changelog Inclusion-heuristic classification statement BEFORE /critique runs. The statement names the posture: either (i) `bump-required` — this slice will land a `## vN.N+1.0` entry + the 4-part PMI-1 bump (VERSION + plugin.yaml + ~/.claude/methodology-changelog.md + ~/.claude/ai-sdlc-VERSION) and the conventional `test_v_0_NN_0_*_entry_present_in_repo` + `*_shippability_consumer_propagation` test pair, OR (ii) `MEPD-1(b) why-none discharged` — a named precedent-class justification (slice-040 R-10 conformance class / slice-045 R-11 INSTALL.md typo class / similar) AND the actual META-1 enforcing assertion grep-cited (slice-032 false-precedent-guard discipline; slice-049 B2 / slice-050 M3 lineage). A bare "drift posture unaffected" / "no new registered artifact" / "PMI-1/INST-1 unaffected" claim is NOT a classification — it covers PMI-1 *artifact enumeration*, NOT the Inclusion-heuristic behavior-change axis (slice-049/052 N=2 misclassification). Surface (b) is the design-stage blind spot the dual-Critic stack does not flag and that no other audit catches at design or critique time — it is reachable only at /build-slice pre-finish. Before /critique, grep the slice's mission-brief.md + design.md for one of the literals `bump-required`, `Inclusion heuristic`, `MEPD-1(b)`, or an explicit `## vN.N+1.0` reference; if absent and any trigger condition fires, the slice MUST be re-scoped (or `MEPD-1(b)` discharged with a cited precedent + META-1 assertion grep) before /critique.

**Rationale**: Permanent because the self-hosting pipeline regularly ships slices that mint a new ADR + a user-facing skill capability/mode without a new RULE-ID / no new registered artifact (slice-049 added drift-guard skills with no other bump reason; slice-052 added --obo + ADR-054 with no new tool; the same class will recur every time the pipeline grows a new skill mode or pipeline gate). The Inclusion-heuristic classification is a design-stage decision that the dual-Critic stack consistently misclassifies as "PMI-1 artifact enumeration" — a structurally narrower axis — and the slice-049 aggregated lesson explicitly stating "a drift-guard family member-addition whose slice has no other bump reason IS a methodology-surface behavior change → ## vN + 4-part PMI-1 bump path; NOT the rode-an-existing-bump non-precedent" was never converted into a Builder-side gate or a Critic-prompt dimension, so it recurred at slice-052 (the same wording, same outcome, missed by both Critic layers again). This rule converts the recurring aggregated lesson into the explicit pre-/critique assertion the lesson called for. Cost of recurrence: a mid-build design deviation + scope expansion + 4-part forward-sync + entry-pin/prop tests, all under PCA-1 halt — expensive vs a one-line classification at design time.

**Validation hint**: For any slice authoring an ADR or adding a new skill capability/mode: `grep -nE "Inclusion heuristic|bump-required|MEPD-1\(b\)|## v[0-9]+\.[0-9]+\.0" architecture/slices/slice-NNN-*/mission-brief.md architecture/slices/slice-NNN-*/design.md` and confirm at least ONE match per slice. If MEPD-1(b) discharge is claimed, also grep for the cited META-1 enforcing assertion path (slice-032 false-precedent-guard discipline) and confirm it is a real cite, not a precedent analogy. Run /critique only after the classification line is present.

## BC-PROJ-11 — Recipe / methodology docs must reference VERSION dynamically, never hard-code a methodology version literal

**Severity**: Important
**Applies to**: INSTALL.md, README.md
**Promoted from**: slice-058-add-install-wakeup-prompt-guardrail (2026-05-22) — N=2 recurrence: slice-045 fixed a stale `v0.20.0` literal in INSTALL.md by substituting a hard `v0.54.0` literal, which itself re-drifted across the v0.48-v0.54 bumps and was stale again (`v0.54.0` while `VERSION` was 0.62.0) by slice-058; the first Critic caught it as B2
**Trigger keywords**: install, recipe, methodology version, version literal
**Trigger anchors**: install, recipe

**Check**: A slice that edits an install-recipe / methodology doc (`INSTALL.md`, `README.md`) MUST NOT hard-code a methodology version literal (a `v?0.NN.0`-shaped string). Any methodology-version mention must reference `VERSION` / `~/.claude/ai-sdlc-VERSION` dynamically (a `cat` / derive-from-file form), never a typed integer literal. A hard-coded literal is correct only at the instant it is written and silently re-drifts at the very next version bump. When fixing a stale version literal, REMOVE the literal (reword to reference `VERSION`) — do not refresh it to the current value; refreshing just resets the drift clock. Grep the changed doc for `v?0.NN.0`-shaped tokens and confirm none is a hard-coded methodology version.

**Rationale**: Permanent because the self-hosting AI-SDLC pipeline bumps its methodology `VERSION` on most codification slices, while `INSTALL.md` (the INST-1 install recipe) and `README.md` are edited only occasionally — so any hard-coded version literal in those docs goes stale within a few slices and stays wrong until the next doc edit. The recurrence is witnessed N=2: slice-045 (`v0.20.0`) and slice-058 (`v0.54.0`, itself introduced by slice-045's own fix). The slice-045 `test_install_md_correctness.py::test_install_md_has_no_stale_v0_20_0_literal` pins only the specific `v0.20.0` literal, so it does not catch a newly-introduced hard literal — the failure recurs silently until a human reads the stale prose. Refreshing a stale literal resets the drift clock; only removing it (reference `VERSION`) is durable.

**Validation hint**: For any slice editing `INSTALL.md` / `README.md`, run `grep -nE 'v?0\.[0-9]+\.[0-9]+' INSTALL.md README.md` and confirm every hit is either inside a `cat ~/.claude/ai-sdlc-VERSION` / `see VERSION` dynamic-reference construction or genuinely not a methodology-version claim; assert no bare typed `vN.N.N` methodology-version literal remains.

## BC-PROJ-12 — New helpers writing markdown files must use `newline=""` to preserve LF on Windows

**Severity**: Important
**Applies to**: tools/**/*.py
**Promoted from**: slice-076-add-pcr-1-conflict-diagnostic-and-soft-regen (2026-05-29) — N=2 cumulative: PSQ-2 (slice-072) `tools/slice_queue_writer.py:790` + `tools/slice_queue_claim.py:535` explicitly opted in to `newline=""` at design time; PCR-1 (slice-076) `tools/parallel_conflict_resolver.py` shipped THREE write sites (`_regen_slice_queue` L561, `_merge_shippability` L702, `_append_audit_log` L787 + L789) WITHOUT `newline=""` at /build-slice Phase C, caught only at /code-review M1 EOL-DRIFT-1 + fixed in-band at Phase H. The design-Critic + meta-Critic stack structurally cannot reach call-site EOL discipline — code-Critic IS the gate for this class.
**Trigger keywords**: write_text, Path.write_text, open("a"), open("w"), newline, markdown, .md, encoding, EOL, CRLF, LF, EOL-DRIFT, ADR-033
**Trigger anchors**: write_text, open("a"), markdown, .md, encoding
**Negative anchors**: aggregated lessons, meta-discussion, false positive, design.md, mission-brief, defer-with-rationale

**Check**: Any new helper in `tools/**/*.py` that writes to a markdown file via `Path.write_text(...)` or `open(...)` MUST pass `newline=""` alongside `encoding="utf-8"`. Default `Path.write_text(content, encoding="utf-8")` translates `\n` → `os.linesep` on Windows, producing CRLF; the sibling PSQ-2 writers (`tools/slice_queue_writer.py:790`, `tools/slice_queue_claim.py:535`) explicitly use `newline=""` for LF-only byte-deterministic emission. Grep the slice's new + modified `tools/*.py` files for `write_text(.*encoding=` AND `open(.*encoding=` invocations targeting `.md` paths; confirm each carries `newline=""`. A regression test asserting `b"\r\n" not in <output>.read_bytes()` for the writer's output is the canonical pin (see `tests/skills/parallel_conflict_resolver/test_audit_log.py::test_append_audit_log_writes_lf_only_no_crlf_translation` for the slice-076 / PCR-1 model).

**Rationale**: Permanent because (a) the project's default platform is Windows (per `CLAUDE.md` "Platform: win32") and Python's `Path.write_text` + `open()` default `newline=None` triggers Windows-only CRLF translation; (b) the project's `.gitattributes` covers only `skills/**/SKILL.md` + `agents/*.md` + `skills/diagnose/passes/*.md` — `architecture/**/*.md` and `architecture/parallel-conflict-resolution-log.md` are NOT pinned, so CRLF leaks into committed files; (c) the EOL-DRIFT-1 / ADR-033 class was minted at slice-033 specifically because this drift produces noisy round-trip diffs + CAD-1 / OSDG-1 byte-equality false-failures; (d) the slice-072 PSQ-2 writers correctly opted in but the slice-076 PCR-1 writers did NOT, demonstrating the lesson does NOT carry over automatically — a writer-level convention needs a build-check, not a code-review-time catch.

**Validation hint**: For any slice adding or modifying `tools/*.py` with markdown writes: `grep -nE 'write_text|open\(' tools/*.py | grep -E '\.md|"a"|"w"'` and confirm every hit targeting `.md` paths includes `newline=""`. Additionally, for new helpers, add a regression test like `def test_<helper>_emits_lf_only(tmp_path): ...; assert b"\r\n" not in out_path.read_bytes()`. Cross-reference PSQ-2's `newline=""` rationale at `tools/slice_queue_writer.py:781-783` for the canonical pattern.

## BC-PROJ-13 — A newly-minted regex/parser needs an APED-1 adversarial-corpus battery written at design time

**Severity**: Important
**Applies to**: tools/**/*.py
**Promoted from**: slice-083-add-pcr-2b-hard-class-conflict-resolution (2026-05-30) — N=3 cumulative: slice-081 DCE-1 anchored matcher (right-anchored-only despite a "slice-anchored" docstring), slice-082 PCR-1 heading regex (no `rstrip` vs the canonical parser → trailing-whitespace bypass), slice-083 PCR-2b marker regex (`{7}`-exact + missing diff3 `|||||||` base marker → real safety false-negative). In ALL THREE the content-shape bug shipped through the design-Critic + meta-Critic stack and was caught ONLY by the code-Critic EXECUTING the regex against an adversarial corpus.
**Trigger keywords**: regex, re.compile, matcher, marker, anchored, parser, conflict-marker, aped-1
**Trigger anchors**: regex, re.compile, matcher, marker, anchored
**Negative anchors**: aggregated lessons, meta-discussion, false positive, design.md, mission-brief, defer-with-rationale

**Check**: When a slice mints a new regex / parser / matcher in `tools/**/*.py` — especially one that (a) must AGREE with an existing canonical parser, or (b) SCANS real-world content (markdown / source / git output) for a pattern — write an APED-1 adversarial-corpus battery that EXECUTES the regex against real fixtures in BOTH directions: false-positive inputs that must NOT match (e.g., Markdown setext `=======` underlines, dividers, table separators, the canonical parser's normalization edges — rstrip / CRLF / leading-ws / annotation suffix) AND false-negative inputs that MUST match (e.g., the full marker-variant set: `<<<<<<<`/`>>>>>>>`/`|||||||`, ≥7-run boundaries, diff-column prefixes). Write the battery AT DESIGN TIME (or as a WRITTEN-FAILING test before the regex), not after. The design-Critic + meta-Critic read the regex literal but do not run it — they structurally cannot catch content-shape bugs; only executing the regex against a corpus does.

**Rationale**: Permanent because the failure mode recurred N=3 across consecutive slices (081/082/083) with the SAME signature — a regex/parser whose content-shape behavior diverged from its docstring/design claim, invisible to two of the three Critic personas, caught only by the code-Critic at lag 1 (or worse, by the full-suite run). A "read the regex" review cannot substitute for executing it: slice-082's heading regex looked correct and was wrong on trailing whitespace; slice-083's marker regex looked correct and missed diff3 base markers. The cost of the discipline (one adversarial-corpus test per regex-minting slice) is far below the cost of a content-shape false-negative shipping in a safety-critical scan (slice-083's `--verify-resolution` would have passed a leftover diff3 conflict block CLEAN). The discipline does NOT carry over automatically — it needs a build-check, not a per-slice memory.

**Validation hint**: For any slice adding a `re.compile(...)` / new parser to `tools/*.py`: confirm a test module EXECUTES it against an adversarial corpus with explicit false-positive (must-not-match) AND false-negative (must-match) cases — `grep -rnE 're\.(compile|search|match|findall)' tools/<new>.py` to find the minted patterns, then confirm `tests/**` has a corresponding battery asserting BOTH directions (e.g. `tests/methodology/test_pcr_2b_verify_resolution.py` for the slice-083 marker regex: setext-CLEAN + diff3-base-STOP + real-marker-STOP). If the regex must agree with a canonical parser, feed that parser's normalization edges.

## BC-PROJ-14 — A structural-pin test on prose-as-executable-contract must pin a unique-to-invocation literal, assert shape, and section/seam-scope its assertion

**Severity**: Important
**Applies to**: tests/**/*.py
**Promoted from**: slice-086-harden-agent-spawn-skills-await-real-output (2026-05-30) — N=3 cumulative pin-precision family: slice-075 (bare `git checkout` / `2.1.` literals matched inside informative narration — not unique-to-invocation), slice-085 (a label *presence* regex `^<label>` masked a genuinely-missing field — presence is not shape), slice-086 (a file-global presence pin would pass a guard RELOCATED out of its spawn→write seam, and the region extractor used bare `.find("### Step 2")` — mis-anchors on inline narration / `### Step 20` prefix). Each shipped through a review layer and gave a false GREEN until caught at build-time or by the code-Critic.
**Trigger keywords**: structural-pin, pin, literal, prose, seam, section-scoped, anchored, substring
**Trigger anchors**: structural-pin, pin, literal, seam, anchored
**Negative anchors**: aggregated lessons, meta-discussion, false positive, design.md, mission-brief, defer-with-rationale

**Check**: When a slice mints a structural-pin test that asserts a literal is present in a prose-as-executable-contract surface (`skills/**/SKILL.md`, `agents/*.md`, or another markdown contract), the test MUST satisfy all three pin-precision sub-rules: (a) **unique-to-invocation** — pin a literal that does NOT also appear in informative narration on the surface (slice-075); a noun-phrase that recurs in prose gives a false GREEN. (b) **shape, not bare presence** — when the contract is "a structural element exists", assert the element's SHAPE (e.g. `^- \*\*\w[\w -]*:\*\*` for a field line), not a bare presence substring a value/continuation line could satisfy (slice-085). (c) **section/seam-scoped + line-anchored** — scope the assertion to the region where the contract is load-bearing (e.g. between two headings), and match those headings LINE-ANCHORED (`(?m)^### Heading\b`), NEVER bare `.find()` substring (slice-086) — a file-global `in` / `.count()` passes a literal relocated out of its region, and `.find()` mis-anchors on inline narration or a `### Step 20`-style prefix collision. Prove the scoping with a relocate/negative fixture that places the literal OUTSIDE the region and asserts the test FAILS.

**Rationale**: Permanent because the pin-precision failure mode recurred N=3 across slices 075/085/086 with the same signature — an imprecise structural pin that reports GREEN while the contract it guards has silently regressed (relocated, narration-shadowed, or shape-degraded). A guard test that can false-pass is worse than no test: it manufactures confidence. The cost (three cheap sub-rules plus one negative fixture per prose-pin slice) is far below the cost of a silently-inert methodology guard. The discipline does NOT carry over as per-slice memory — it needs a build-check.

**Validation hint**: For any slice adding a `tests/**` structural-pin test that asserts a literal in `skills/**/SKILL.md` or `agents/*.md`: (1) confirm the pinned literal is unique-to-invocation (`grep -c "<literal>" <surface>` returns the intended count AND the literal is absent from narration); (2) confirm the assertion is region-scoped, not a whole-file `in` / `.count()`; (3) `grep -nE '\.find\(' tests/<new>.py` returns nothing for heading detection — heading/anchor matching uses a line-anchored regex (`re.compile(r"(?m)^...")`); (4) confirm a relocate/negative fixture exists that places the literal outside the region and asserts FAIL. Reference `tests/methodology/test_r25_await_real_agent_guard.py` (`_step2_to_step3_region` + `test_seam_anchoring_is_line_start_not_substring` + `test_seam_scoping_rejects_relocated_guard`).

## BC-PROJ-15 — Capturing subprocess output in text mode must pass encoding="utf-8", never the host locale code page

**Severity**: Important
**Applies to**: tools/**/*.py
**Promoted from**: slice-090-fix-pcr-git-subprocess-cp1252-decode (2026-05-31) — the PCR auto-resolver's 9 git `subprocess.run(..., text=True)` calls carried no `encoding=`, so on Windows they decoded git's UTF-8 output as cp1252; a byte undefined in cp1252 (an emoji in conflicted content) raised `UnicodeDecodeError` INSIDE subprocess's pipe-reader thread → `subprocess.run` returned `stdout=None` → the helper returned `None` → silent claim-drop (a VAULT_CLAIM collision could auto-resolve as SOFT). cp1252 recurrence N=7: the stdout side (UTF8-STDOUT-1) recurred N=6 across slices 007/016/018/020/021/022; this is the distinct subprocess-INPUT-decode side.
**Trigger keywords**: subprocess, popen, encoding, decode, cp1252, locale, git, capture_output
**Trigger anchors**: subprocess, popen, encoding, decode, cp1252
**Negative anchors**: aggregated lessons, meta-discussion, false positive, design.md, mission-brief, defer-with-rationale

**Check**: When code runs `subprocess.run` / `Popen` / `check_output` that CAPTURES output in TEXT mode (`text=True` / `universal_newlines=True`), it MUST pass `encoding="utf-8"` explicitly. Without it, Python decodes the child's stdout/stderr pipe using `locale.getpreferredencoding(False)` — cp1252 on Windows — and a byte undefined in that code page (0x81/0x8D/0x8F/0x90/0x9D, e.g. an emoji or non-Latin char in the captured output) raises `UnicodeDecodeError` inside the pipe-reader thread; `subprocess.run` does NOT re-raise it, returning `stdout=None`/truncated, so the caller silently proceeds on `None`/garbage — a silent-data-loss site, not merely a crash. Byte-mode captures (`capture_output=True` with NO `text=True`, used only for `{exc!r}` on failure) are EXEMPT — they never decode. Keep `errors` strict (do NOT use `errors="replace"`, which silently mojibakes real data). This is the subprocess-INPUT cousin of UTF8-STDOUT-1's `_stdout.reconfigure_stdout_utf8()` (which fixes the stdout side only).

**Rationale**: Permanent because the cp1252 class is silent-by-default and has recurred N=7 in this self-hosting pipeline. The decode failure raises in a background reader thread and is swallowed, so a tests-pass run on a UTF-8 host (Linux CI) never surfaces it; it only bites on the dev's Windows/cp1252 machine, often as a confusing downstream `None`. The cost (one `encoding="utf-8"` kwarg per text-capturing call) is trivial vs the cost of a silently mis-resolved conflict or a crashed audit tool.

**Validation hint**: For any slice touching a `tools/**/*.py` that shells out: `grep -nE 'subprocess\.(run|Popen|check_output)' <file>` then confirm every call with `text=True` (or `universal_newlines=True`) also passes `encoding="utf-8"`; byte-mode captures (no `text=True`) are exempt. Reference the AST guard `tests/methodology/test_parallel_conflict_resolver_git_encoding.py` (the `argv[0]=="git" AND text=True → require encoding="utf-8"` predicate) and ADR-082.

## BC-PROJ-16 — Version-bumping methodology slices must complete the full version-bump obligation set (5 surfaces + pip + rolling version-sync-test rename + its shippability citation + catalog-index = max+1)

**Severity**: Important
**Applies to**: VERSION, plugin.yaml, pyproject.toml, methodology-changelog.md
**Promoted from**: slice-105-decouple-slice-loop-from-diagnose-out (2026-06-03) — the rolling version-sync pin `test_version_files_synchronized_at_v_0_81_0` (hard-pinned VERSION==0.81.0 on all 4 legs) was the lone seedless-smoke red after the 0.81.0→0.82.0 bump; neither the design's "Tests touched" table nor the design-Critic enumerated the rename, and the design said "Row #105" when the real next catalog index was #112. The rename has recurred N≥11 (slice-067/072/073/078/080/081/083/088/095/099/105 per the test's own docstring) yet lives only in tribal knowledge / that docstring.
**Trigger keywords**: version bump, pmi-1, version cascade, version-sync, ai-sdlc-version

**Check**: A slice that bumps the methodology `VERSION` (a PMI-1 version cascade) MUST complete the FULL version-bump obligation set in the same slice — not just the version-bearing surfaces: (1) bump all five — `VERSION`, `plugin.yaml` `version:`, `pyproject.toml [project].version`, installed `~/.claude/ai-sdlc-VERSION`, and the `## vN.N.0` header in `methodology-changelog.md` (+ the `~/.claude/methodology-changelog.md` mirror) — then run `$PY -m pip install --upgrade .` so the installed `ai-sdlc-tools` dist matches (TVFS-1); (2) **rename the single rolling version-sync test** `tests/methodology/test_methodology_changelog.py::test_version_files_synchronized_at_v_0_<old>` → `_at_v_0_<new>` and update ALL FOUR leg literals (VERSION / plugin.yaml / pyproject / `## v` header) + the docstring — it hard-pins the OLD version on every leg and reds the full suite otherwise; (3) **bump that test's `architecture/shippability.md` citation** — the catalog row whose command runs `test_version_files_synchronized_at_v_0_<old>` (currently row #75) MUST be updated to the new name, or `tools.shippability_path_audit` (PTFFD-1) flags a phantom test-path citation at `/validate-slice` Step 5.5; (4) when adding the slice's OWN catalog row, compute the leading `| N |` index as `max(existing index)+1` by reading the catalog tail — NOT the slice number (catalog indices diverged from slice numbers when slice-079 added two rows). Obligations (2)–(4) are structurally invisible to the design-Critic + meta-Critic stack (they review intent, not the rolling-test/catalog-arithmetic bookkeeping) and surface only at the full-suite / PTFFD-1 / seedless run at pre-finish.

**Rationale**: Permanent because the self-hosting AI-SDLC pipeline bumps its methodology `VERSION` on most codification slices, and three of the obligations are a fixed design-stage blind spot: the rolling `test_version_files_synchronized_at_v_0_NN_0` rename is a standing manual edit (recurred N≥11) that lives only in that test's own docstring; its shippability-row citation is a second hidden surface a PTFFD-1 phantom-path failure catches only at validate; and the catalog-index-≠-slice-number arithmetic was loosely shorthanded as the slice number in the design prose. slice-105 is the witnessed instance — the stale `test_version_files_synchronized_at_v_0_81_0` was the lone seedless-smoke red and the design said "Row #105" when the real index was #112. A design-Critic reviewing intent cannot infer the rolling-test rename or the catalog arithmetic; only the full suite catches them, late and mid-pipeline. This rule converts the recurring obligation into the pre-finish reminder the recurrence calls for.

**Validation hint**: For any slice that bumps `VERSION`: confirm all five version surfaces equal the new version (`tests/methodology/test_methodology_changelog.py::test_version_files_synchronized_at_v_0_<new>` green; AVFS-1 / MCFS-1 / TVFS-1 / PVFS-1 / PMI-1 exit 0); `grep -n "test_version_files_synchronized_at_v_0_" tests/methodology/test_methodology_changelog.py architecture/shippability.md` and confirm BOTH cite the NEW version with zero `_at_v_0_<old>` survivor; confirm the slice's new shippability row's leading `| N |` index equals max-existing-index + 1 (read the catalog tail, do not use the slice number); run the full `tests/methodology/` suite + `$PY -m tools.shippability_path_audit architecture/shippability.md` (PTFFD-1) and assert green.
