# Build checks (project-specific)

## Rules

## BC-PROJ-4 — Rule with missing severity field

**Applies to**: always: true
**Check**: This rule body has no Severity field, so it should be flagged as a parse violation.
