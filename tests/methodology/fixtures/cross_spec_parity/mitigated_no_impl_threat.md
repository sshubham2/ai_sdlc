# Threat model with mitigated status but no Implementation (fixture)

## TM-9 — Mitigated threat with empty Implementation

**Severity**: high
**Status**: mitigated
**Implementation**:
**Mitigation**: claims mitigated but the Implementation field is empty — should be flagged because mitigated status REQUIRES non-empty Implementation.
