# Design: Slice 045 empty-matrix (fixture)

This slice introduces no new modules — it only modifies existing files. The
empty matrix (header + separator only, zero data rows) is treated as clean
by WIRE-1.

## Wiring matrix

| New module | Consumer entry point | Consumer test | Exemption |
|------------|---------------------|---------------|-----------|
