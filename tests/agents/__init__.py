"""Tests for `agents/*.md` adversarial-Critic agent prompts.

Created in slice-060 (CRSI-1) to host `test_code_review_agent.py`. Future
agent-prompt tests live here (currently only the code-review agent; the
critique / critique-review / critic-calibrate / diagnose-narrator /
field-recon agents have their drift guards under `tests/methodology/`).
"""
